---
phase: 33-player-entity-animation
plan: 05
type: execute
wave: 3
depends_on: ["33-03", "33-04"]
files_modified: []
autonomous: false
requirements: [ART-04, ART-05]

must_haves:
  truths:
    - "The full consolidated automated gate suite is green with zero HARD-FAILs across the fully-merged Phase 33 tree."
    - "A genuine, verbatim-quoted human sign-off supersedes the v4.1 player-art lock — never rubber-stamped, per this project's standing precedent (Phases 25/27/28/31)."
  artifacts:
    - "Phase SUMMARY.md recording the full gate-suite results and the verbatim sign-off quote"
  key_links:
    - "This checkpoint is the authoritative closer for ART-04's animation-quality claim per this project's standing \"never rubber-stamp checkpoint:human-verify\" precedent — Task 1's automation alone does not close it."
---

<objective>
Run the full consolidated automated gate suite across the fully-merged Phase 33 tree, then get a genuine, non-rubber-stamped human sign-off on the animated player and mechanic-entity art — explicitly superseding the v4.1 player-art lock.

Purpose: this project's standing precedent (Phases 25/27/28/31, reinforced by an explicit STATE.md note) is that a checkpoint:human-verify gate must NEVER be auto-approved. Roadmap success criterion 1 requires exactly this: a NEW human sign-off superseding the old lock. Success criterion 2 requires the collider-lock proof (16x32, unchanged across flat floor/1-tile platform/lowest ceiling/door) to be part of that same review.
Output: full gate suite green; a recorded, verbatim, genuine human sign-off in this plan's SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-player-entity-animation/33-CONTEXT.md
@.planning/phases/33-player-entity-animation/33-RESEARCH.md
@.planning/phases/33-player-entity-animation/33-VALIDATION.md
@.planning/phases/33-player-entity-animation/33-01-SUMMARY.md
@.planning/phases/33-player-entity-animation/33-02-SUMMARY.md
@.planning/phases/33-player-entity-animation/33-03-SUMMARY.md
@.planning/phases/33-player-entity-animation/33-04-SUMMARY.md
</context>

<artifacts_produced>
- No new source files — this plan's artifact is the recorded, verbatim human sign-off in `33-05-SUMMARY.md`, closing ART-04/ART-05 for the phase.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Run the full consolidated automated gate suite</name>
  <files>(none — verification-only task)</files>
  <read_first>
    .planning/phases/33-player-entity-animation/33-VALIDATION.md ("Full suite command" row — the exact chained invocation this task runs)
  </read_first>
  <action>
    Run, in order, the phase's full gate suite as one chained command from the repo root: `bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs && bash scripts/check-pink-gate.sh && node scripts/check-assets-manifest.mjs`. This is the closing regression proof for the whole phase, run against the fully-merged tree (all four preceding plans landed) — every gate must be green with zero HARD-FAILs before the human checkpoint in Task 2.
  </action>
  <acceptance_criteria>
    - The full chained command exits 0 (a `&&`-chained sequence; any single non-zero exit aborts the chain and fails this task).
    - `check-assets-manifest.mjs`'s output contains zero `MISSING` lines.
    - `audit-phase21-mechanics.mjs`'s output reports every door/math-gate/enemy encounter across all 8 levels as triggered.
    - `check-pink-gate.sh`'s output ends with `pink-gate checks: PASS`.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs && bash scripts/check-pink-gate.sh && node scripts/check-assets-manifest.mjs</automated>
  </verify>
  <done>The full consolidated gate suite is green with zero HARD-FAILs across the fully-merged Phase 33 tree.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off — player animation + collider lock + entity art (supersedes v4.1 lock)</name>
  <files></files>
  <action>
    Present the fully-merged, fully-gated build to the parent (and kid, if available, per this project's Phase-26/27/31 standard) and get an explicit, specific sign-off. This is a genuine second lock, not a rubber stamp of the v4.1 player-art approval it supersedes — the project's own STATE.md records a standing instruction that this exact checkpoint must never be auto-approved during autonomous execution. If any defect surfaces (collider mismatch, a jarring anim transition, art that reads inconsistent with the Phase-32 biome work), stop and route it back to Plan 33-03/33-04 rather than closing the phase.
  </action>
  <what-built>
    Plans 33-01 through 33-04 wired: a full 5-state player animation (idle/run/jump/fall/land) on the already-baked Swamp Hunter sheet, with the physics collider explicitly locked to 16x32 via `area({ shape })` independent of the current anim frame; freshly baked native-color door and math-gate sprite art (replacing the old Kenney-remapped door and the flat-color math-gate box); the Hell hound sprite wired as a real 6-frame looping idle animation replacing the static enemy-1/2/3 set. Every invisible blocker collider (door/math-gate/enemy) is byte-unchanged, and the full interactive mechanic audit already proves every encounter across all 8 levels still triggers.
  </what-built>
  <how-to-verify>
    1. Serve the game locally from the repo root: `python3 -m http.server 8000`.
    2. Open `http://192.168.10.113:8000/src/index.html?debug=1` (this host's LAN IP, so it renders the same way prior sign-offs on this project were done).
    3. Play across at least 2-3 different levels and confirm idle, run, jump, fall, and land each read as visually distinct, with no flicker or stutter on state transitions.
    4. With the debug collider overlay on, stand on a flat floor stretch, a narrow one-tile-wide platform, the tightest-clearance ceiling spot, and directly at a locked door — confirm the rendered collider box stays a fixed 16x32 rectangle in all 4 spots, regardless of which anim frame is currently showing.
    5. Confirm the door, the math-gate (its "?" glyph still legible), and the Hell hound enemy all show real, non-flat, native-color art — the Hell hound should visibly cycle through its 6-frame idle loop, and the new art should read consistently against the Phase-32 biome terrain/parallax.
    6. Ask explicitly: "Does the player's new animation and this collider-lock proof look right to ship — approved to supersede the old player-art lock?" Record the verbatim answer, including any hesitation or follow-up requests, in this plan's SUMMARY.md.
  </how-to-verify>
  <resume-signal>Type "approved" (with the verbatim quote to record), or describe the specific issue to route back to Plan 33-03 (player.js) or Plan 33-04 (build.js) instead.</resume-signal>
  <verify>
    <human-check>Human (parent, and kid if available) confirms: all 5 anim states read as distinct and stutter-free across multiple levels; the debug collider box stays a fixed 16x32 rectangle at a flat floor, a 1-tile platform, the lowest ceiling, and a door; door/math-gate/Hell-hound art reads as real and consistent with the biome art; and gives an explicit, quoted "approved to ship, supersedes the v4.1 lock" confirmation.</human-check>
  </verify>
  <done>A genuine, verbatim-quoted human sign-off is recorded in this plan's SUMMARY.md, confirming the animated player and mechanic-entity art (and the 16x32 collider-lock proof) are approved to ship, explicitly superseding the v4.1 player-art lock — or a specific defect is routed back to Plan 33-03/33-04 instead.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure verification + a recorded human decision — no code changes, no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Repudiation | The player-art/collider-lock human sign-off record | medium | mitigate | The sign-off is quoted VERBATIM in this plan's SUMMARY.md, matching this project's established evidentiary standard (Phases 26/27/31) — the sign-off can never later be characterized as informal, assumed, or auto-approved. `auto_advance` in this project's config must NOT be allowed to skip this checkpoint (standing user-memory precedent). |
</threat_model>

<verification>
1. The full 9-command gate suite chain exits 0.
2. A genuine, verbatim-quoted human sign-off is recorded in `33-05-SUMMARY.md`, explicitly referencing the v4.1-lock supersession.
</verification>

<success_criteria>
- Every automated gate this project has is green in one run, on the fully-merged Phase 33 tree.
- A real, non-rubber-stamped human sign-off closes ART-04's animation-quality claim and the 16x32 collider-lock proof.
</success_criteria>

<output>
Create `.planning/phases/33-player-entity-animation/33-05-SUMMARY.md` when done
</output>
