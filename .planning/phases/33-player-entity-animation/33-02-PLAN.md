---
phase: 33-player-entity-animation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.js
  - src/main.js
  - src/assets-manifest.js
autonomous: true
requirements: [ART-04, ART-05]

must_haves:
  truths:
    - "main.js registers a full 5-state (idle/run/jump/fall/land) player-swamphunter anim set and a real 6-frame looping enemy-hellhound idle anim before any scene boots."
    - "assets-manifest.js's entries exactly mirror every loadSprite path this plan changes, so the existence gate stays meaningful."
  artifacts:
    - "src/config.js: PLAYER_FRAMES bumped to 12, new PLAYER_FALL_SPEED/PLAYER_LAND_SPEED/PLAYER_LAND_HOLD_MS, new CONFIG.ENEMY.IDLE_SPEED/CONFIG.ENEMY.FRAME_W, CONFIG.ENEMY.SPRITES now single-entry"
    - "src/main.js: player loadSprite swapped to player-swamphunter.png with 5 named anims; enemy-1/2/3 loadSprite calls replaced by one enemy-hellhound loadSprite (sliceX 6, looping idle); new math-gate loadSprite added"
    - "src/assets-manifest.js: player path updated, enemy-1/2/3 rows removed, math-gate + enemy-hellhound rows added, header counts corrected"
  key_links:
    - "CONFIG.ENEMY.SPRITES[0] must equal the exact key registered by main.js's new loadSprite(\"enemy-hellhound\", ...) call, or Plan 33-04's build.js sprite() lookup 404s."
    - "The 5 anim names registered here (idle/run/jump/fall/land) must exactly match the target strings Plan 33-03's player.js state machine emits, or player.play(target) throws/no-ops on an anim Kaplay never loaded."
---

<objective>
Register the boot-time sprite/anim loading this phase needs: swap the player sheet to the already-baked 12-frame `player-swamphunter.png` with a full idle/run/jump/fall/land anim set, replace the 3-variant static enemy-1/2/3 array with the already-baked 6-frame looping `enemy-hellhound` idle anim, and add the new `math-gate` sprite load — plus every tunable these loads need and the manifest rows that keep the existence gate accurate.

Purpose: this is the "foundation" wave-1 plan every other code-touching plan in this phase consumes — Plan 33-03 (player.js) needs these exact anim names registered, Plan 33-04 (build.js) needs the CONFIG.ENEMY.SPRITES array and the math-gate/enemy-hellhound sprite keys.
Output: config.js tunables, main.js loadSprite registrations, assets-manifest.js rows — all consistent with each other.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-player-entity-animation/33-CONTEXT.md
@.planning/phases/33-player-entity-animation/33-RESEARCH.md
@.planning/phases/33-player-entity-animation/33-PATTERNS.md
</context>

<artifacts_produced>
- `src/config.js`: `PLAYER_FRAMES` (12), `PLAYER_FALL_SPEED`, `PLAYER_LAND_SPEED`, `PLAYER_LAND_HOLD_MS` (new top-level tunables); `CONFIG.ENEMY.IDLE_SPEED`, `CONFIG.ENEMY.FRAME_W` (new nested tunables); `CONFIG.ENEMY.SPRITES` changed to `["enemy-hellhound"]`
- `src/main.js`: player `loadSprite` call repointed at `../assets/player-swamphunter.png` with 5 named anims (idle/run/jump/fall/land); `loadSprite("enemy-hellhound", ...)` (sliceX 6, looping idle anim) replacing the 3 `enemy-1/2/3` calls; `loadSprite("math-gate", "../assets/math-gate.png")` added
- `src/assets-manifest.js`: `player` row path updated; `enemy-1`/`enemy-2`/`enemy-3` rows removed; `math-gate` (kind `sprite`) and `enemy-hellhound` (kind `sprite-anim`) rows added; header comment counts corrected
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: config.js — player fall/land tunables + enemy-hellhound tunables + single-entry SPRITES</name>
  <files>src/config.js</files>
  <read_first>
    src/config.js lines 84-99 (the existing PLAYER_* visual-tuning block, `PLAYER_FRAMES` through `GROUND_FRAMES`)
    src/config.js lines 172-178 (the existing `ENEMY` block)
    src/config.js lines 223-257 (the `FX` block's `SQUASH_MS`/`STRETCH_MS` — the brief-hold-duration precedent `PLAYER_LAND_HOLD_MS` should feel consistent with)
  </read_first>
  <action>
    In the existing PLAYER_* block, change `PLAYER_FRAMES: 5,` to `PLAYER_FRAMES: 12,` and update its trailing comment to reference `player-swamphunter.png` (12 frames of 16x32) instead of the retired `player.png`. Immediately after the existing `PLAYER_JUMP_SPEED: 1,` line, add three new top-level keys: `PLAYER_FALL_SPEED: 8,` (fps — fall anim frame rate, 2-frame loop over frames 10-11 of player-swamphunter.png), `PLAYER_LAND_SPEED: 1,` (fps — single-frame land anim; speed must be >0 in Kaplay, mirrors PLAYER_JUMP_SPEED's existing precedent), `PLAYER_LAND_HOLD_MS: 120,` (ms — how long the synthesized land pose, fall's last frame at index 11, holds after a genuine falling-to-grounded transition before the state machine reverts to idle/run; brief-hold duration chosen to feel consistent with FX.STRETCH_MS/SQUASH_MS's existing 120-140ms window).

    In the existing `ENEMY` block, add two new fields alongside `W`/`H`/`SPRITES`: `IDLE_SPEED: 8,` (fps — enemy-hellhound idle-loop frame rate) and `FRAME_W: 64,` (px — enemy-hellhound.png's native per-frame width; twice the W/H blocker footprint below, used downstream to center the wider visual panel over the unchanged blocker). Change the existing `SPRITES: ["enemy-1", "enemy-2", "enemy-3"],` line to `SPRITES: ["enemy-hellhound"],` and update its trailing comment to note this is now the single shared animated blocker sprite (ART-05, Phase 33), replacing the 3-variant static set.
  </action>
  <acceptance_criteria>
    - `grep -c "PLAYER_FRAMES: 12" src/config.js` returns 1.
    - `grep -c "PLAYER_FALL_SPEED" src/config.js` returns 1.
    - `grep -c "PLAYER_LAND_SPEED" src/config.js` returns 1.
    - `grep -c "PLAYER_LAND_HOLD_MS" src/config.js` returns 1.
    - `grep -c "IDLE_SPEED: 8" src/config.js` returns 1.
    - `grep -c "FRAME_W: 64" src/config.js` returns 1.
    - `grep -c '"enemy-hellhound"' src/config.js` returns 1.
    - `grep -c '"enemy-1"' src/config.js` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "import { CONFIG } from './src/config.js'; if (CONFIG.PLAYER_FRAMES !== 12 || CONFIG.ENEMY.SPRITES.length !== 1 || CONFIG.ENEMY.SPRITES[0] !== 'enemy-hellhound' || !CONFIG.PLAYER_FALL_SPEED || !CONFIG.PLAYER_LAND_HOLD_MS) process.exit(1); console.log('config OK');"</automated>
  </verify>
  <done>config.js carries every new player-animation and enemy-hellhound tunable, and CONFIG.ENEMY.SPRITES is a single-entry array — no magic numbers left for the consuming modules to introduce.</done>
</task>

<task type="auto">
  <name>Task 2: main.js — player-swamphunter 5-state load, enemy-hellhound load, math-gate load</name>
  <files>src/main.js</files>
  <read_first>
    src/main.js lines 63-130 (every existing `loadSprite` call, especially player at lines 65-72, door at line 112, and the 3 enemy-N calls at lines 113-115)
  </read_first>
  <action>
    Replace the player `loadSprite("player", "../assets/player.png", { sliceX: CONFIG.PLAYER_FRAMES, anims: {...} })` call's path with `"../assets/player-swamphunter.png"`, keep `sliceX: CONFIG.PLAYER_FRAMES` (now resolves to 12 via Plan Task 1's config change), and extend `anims` to the RESEARCH-verified 5-state frame layout — `idle` frames 0-1 (loop:true, speed: CONFIG.PLAYER_IDLE_SPEED), `run` frames 2-7 (loop:true, speed: CONFIG.PLAYER_RUN_SPEED), `jump` frames 8-9 (loop:false, speed: CONFIG.PLAYER_JUMP_SPEED), `fall` frames 10-11 (loop:true, speed: CONFIG.PLAYER_FALL_SPEED), `land` frame 11-11 (loop:false, speed: CONFIG.PLAYER_LAND_SPEED — a synthesized one-shot hold of fall's last frame; no dedicated land art exists in the bake, per this phase's locked "wiring only" scope).

    Remove the three `loadSprite("enemy-1", "../assets/enemy-1.png");` / `enemy-2` / `enemy-3` lines. Replace them with one call: `loadSprite("enemy-hellhound", "../assets/enemy-hellhound.png", { sliceX: 6, anims: { idle: { from: 0, to: 5, loop: true, speed: CONFIG.ENEMY.IDLE_SPEED } } });` — mirrors the existing `coin` loadSprite pattern immediately above in this file.

    Add `loadSprite("math-gate", "../assets/math-gate.png");` immediately after the existing `loadSprite("door", "../assets/door.png");` line — the door call itself needs NO edit (same path, new baked pixels from Plan 33-01).

    Update the surrounding comment block (currently "Door + enemy sprite art (VIS-04; Phase 26 Plan 05)...") to reference ART-04/ART-05 and Phase 33 instead of the stale VIS-04/Phase-26 attribution.
  </action>
  <acceptance_criteria>
    - `grep -c "player-swamphunter.png" src/main.js` returns 1.
    - `grep -c "enemy-hellhound" src/main.js` returns 1 (the loadSprite call site).
    - `grep -c "enemy-1.png\|enemy-2.png\|enemy-3.png" src/main.js` returns 0.
    - `grep -c 'loadSprite("math-gate"' src/main.js` returns 1.
    - `grep -c "land:" src/main.js` returns 1.
    - `grep -c "fall:" src/main.js` returns 1.
    - `grep -c 'loadSprite("door"' src/main.js` returns 1 (unchanged call, still present).
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-import-safety.sh</automated>
  </verify>
  <done>main.js registers the 5-state player-swamphunter sheet, the 6-frame looping enemy-hellhound idle anim, and the new math-gate sprite — zero references to the retired enemy-1/2/3 assets remain, and the door load is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: assets-manifest.js — mirror every path this plan changed</name>
  <files>src/assets-manifest.js</files>
  <read_first>
    src/assets-manifest.js (full file — the header comment's per-kind counts, the `player` sprite-anim row, and the `enemy-1`/`enemy-2`/`enemy-3` sprite rows)
  </read_first>
  <action>
    Update the `player` row's `path` from `"assets/player.png"` to `"assets/player-swamphunter.png"` (key `"player"` and kind `"sprite-anim"` stay unchanged). Remove the `enemy-1`, `enemy-2`, `enemy-3` sprite rows entirely. Add a new row `{ key: "math-gate", path: "assets/math-gate.png", kind: "sprite" }`. Add a new row `{ key: "enemy-hellhound", path: "assets/enemy-hellhound.png", kind: "sprite-anim" }`. Recompute and update the header comment's per-kind entry counts (currently documented as `sprite (12)` / `sprite-anim (2)` / `38 entries total`) to match the final array exactly — count the array programmatically rather than hand-guessing.
  </action>
  <acceptance_criteria>
    - `grep -c "player-swamphunter.png" src/assets-manifest.js` returns 1.
    - `grep -c '"enemy-1"' src/assets-manifest.js` returns 0.
    - `grep -c '"enemy-2"' src/assets-manifest.js` returns 0.
    - `grep -c '"enemy-3"' src/assets-manifest.js` returns 0.
    - `grep -c '"math-gate"' src/assets-manifest.js` returns 1.
    - `grep -c '"enemy-hellhound"' src/assets-manifest.js` returns 1.
    - A node one-liner importing `ASSETS_MANIFEST` confirms its `.length` matches the header comment's stated total exactly.
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "import { ASSETS_MANIFEST } from './src/assets-manifest.js'; console.log(ASSETS_MANIFEST.length);"</automated>
  </verify>
  <done>assets-manifest.js's entries exactly match main.js's updated loadSprite calls, with header counts accurate. (Full on-disk existence proof via check-assets-manifest.mjs is deferred to Plan 33-04/33-05 once Plan 33-01's baked assets are merged into the same tree.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure in-repo config/code edits (config.js, main.js, assets-manifest.js) — no external input, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-04 | Tampering (availability) | Silent asset-load 404 (a wrong `../assets/...` path in any new/changed `loadSprite` call) | medium | mitigate | Every new/changed path this plan introduces (player-swamphunter, enemy-hellhound, math-gate) is mirrored into `assets-manifest.js` in the SAME plan (Task 3), preserving the existence-gate guarantee that kills the silent-404 class; verified on-disk by `check-assets-manifest.mjs` once Plan 33-01's assets merge in. |
</threat_model>

<verification>
1. `node --input-type=module -e "import { CONFIG } from './src/config.js'; ..."` confirms the new tunables and single-entry ENEMY.SPRITES array.
2. `bash scripts/check-import-safety.sh` stays green (no top-level engine-global references introduced).
3. `assets-manifest.js`'s row count and paths exactly track `main.js`'s updated loadSprite calls (grep-verified per task).
</verification>

<success_criteria>
- config.js, main.js, and assets-manifest.js are internally consistent: every sprite key main.js loads has a matching manifest row, and CONFIG.ENEMY.SPRITES resolves to the exact key main.js registers.
- No stale references to the retired player.png or enemy-1/2/3 assets remain in any of the three files.
</success_criteria>

<output>
Create `.planning/phases/33-player-entity-animation/33-02-SUMMARY.md` when done
</output>
