---
phase: 24-fix-lengthen-levels-1-4
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/levels/level-02.js
autonomous: true
requirements: [LVL-01]
must_haves:
  truths:
    - "level-02 has zero known structural defects (23-FINDINGS.md's Post-Plan Correction confirms level-02 requires NO fixes) — this plan is extension-only"
    - "level-02 extends from goal x:2720 to x:4200 (+1480px, 52.9% longer than the v4.1 2800px extent) with every new floor/platform/coin/spike/mathGate/checkpoint appended strictly after x:2800; bounds.right is bumped from 2800 to 4280 so the camera/parallax cover the new content"
    - "every new hazard/mechanic in the extension (spike x:3200, mathGate x:3760) has a checkpoint at FLOOR_Y-48 exactly 80px before it (x:3120, x:3680); enemies/collectZones/answerPickupSlots stay empty, matching level-02's established identity"
  artifacts:
    - src/levels/level-02.js
  key_links:
    - "level-02.js geometry -> scripts/validate-levels.mjs (structural gate, must show zero level-02 HARD-FAIL rows)"
    - "level-02.js bounds.right -> src/scenes/game.js's `level.bounds ?? {...dynamic}` short-circuit (level-02 HAS an explicit bounds field, so it must be manually bumped — unlike level-01)"
    - "level-02.js geometry -> scripts/lib/mechanic-drive.mjs's deriveEncounters (auto-discovers the new mathGate with zero script changes)"
---

<objective>
Extend level-02 52.9% past its v4.1 length with new floors/platforms/coins/hazards/checkpoints appended strictly after the existing kid-validated geometry, and bump its explicit `bounds.right` to cover the new content (LVL-01). Level-02 has zero known structural defects, so no VALID-04 fix work applies here.

Purpose: level-02 is the second of 4 levels this phase must make noticeably longer while preserving its established "no enemies/collectZones" identity and its explicit-bounds camera-clamp convention (unlike level-01, which derives bounds dynamically).
Output: `src/levels/level-02.js` with 2 new floor runs, 2 new bridging platforms, 5 new coins, 1 new spike, 1 new mathGate, 2 new checkpoints, a goal moved to x:4200, and `bounds.right` bumped from 2800 to 4280.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-CONTEXT.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-RESEARCH.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append level-02's new geometry skeleton and bump bounds.right</name>
  <files>src/levels/level-02.js</files>
  <read_first>
    src/levels/level-02.js (full file — bounds line 18, floors 21-26, platforms 28-36, goal 58)
    src/config.js (CONFIG.FLOOR_Y=320, CONFIG.GOAL_SIZE)
    .planning/phases/24-fix-lengthen-levels-1-4/24-RESEARCH.md ("Camera Bounds / Level Extension Mechanics" section — level-02/03/04 all carry an explicit bounds field that game.js uses AS-IS)
  </read_first>
  <action>
    Do not touch `floors`, `platforms`, `doors`, `mathGates`, `coins`, `spikes`, `checkpoints`, `enemies`, `collectZones`, or `answerPickupSlots` entries that already exist — level-02 has no known defects (23-FINDINGS.md's Post-Plan Correction confirms this), so this task is pure append.
    Append exactly 2 new entries to the `floors` array (after the existing `{ x: 2180, w: 620 }`, which ends at 2800): `{ x: 2960, w: 560 }` and `{ x: 3680, w: 600 }`. Comment each as a Phase 24 extension run.
    Append exactly 2 new entries to the `platforms` array: `{ x: 2800, y: 250, w: 128, h: 24 }` (bridges the new 2800..2960 gap, touching floor-3's end at 2800 exactly — rise 70px, well inside JUMP_ENVELOPE.maxRise 88.331) and `{ x: 3520, y: 250, w: 112, h: 24 }` (bridges the new 3520..3680 gap, touching the new floor-4's end at 3520 exactly — same 70px rise).
    Move `goal.x` from 2720 to 4200 (keep `y: FLOOR_Y - CONFIG.GOAL_SIZE`). The new floor-5 ends at 4280, so the buffer is 80px, matching the original's own 2800-2720=80px convention.
    Change `bounds.right` from 2800 to 4280 (keep `left: 0, top: 0, bottom: 360` unchanged) — per 24-RESEARCH.md's verified finding, level-02's explicit `bounds` field is used AS-IS by `src/scenes/game.js` (the `level.bounds ?? {...}` short-circuit means the dynamic computation never runs), so this manual bump is required or the camera/parallax will clip before the new content.
  </action>
  <acceptance_criteria>
    The `floors` array has exactly 6 entries; the 5th is `{ x: 2960, w: 560 }` and the 6th is `{ x: 3680, w: 600 }`.
    The `platforms` array has exactly 9 entries; the 8th is `{ x: 2800, y: 250, w: 128, h: 24 }` and the 9th is `{ x: 3520, y: 250, w: 112, h: 24 }`.
    `goal.x` equals 4200. `bounds.right` equals 4280.
    `node scripts/validate-levels.mjs` prints zero lines matching `^level-02 .*HARD-FAIL`.
  </acceptance_criteria>
  <verify>
    <automated>N=$(node scripts/validate-levels.mjs 2>&1 | grep '^level-02 ' | grep -c 'HARD-FAIL'); echo "level-02 HARD-FAILs: $N"; test "$N" = "0"; grep -c 'right: 4280' src/levels/level-02.js</automated>
  </verify>
  <done>2 new floor runs + 2 new bridging platforms appended after x:2800; goal moved to x:4200; bounds.right bumped to 4280; validate-levels.mjs shows zero level-02 HARD-FAIL rows.</done>
</task>

<task type="auto">
  <name>Task 2: Add level-02's extension coins, hazard, mechanic, and checkpoints</name>
  <files>src/levels/level-02.js</files>
  <read_first>
    src/levels/level-02.js (post-Task-1 state — re-read after Task 1's edits land)
    src/config.js (CONFIG.SPIKE_SIZE, CONFIG.MATH_GATE.H)
    .planning/phases/24-fix-lengthen-levels-1-4/24-PATTERNS.md ("No enemy/collectZone convention" section for level-02)
  </read_first>
  <action>
    Append exactly 5 new entries to the `coins` array (matching level-02's existing ~1-per-280px density): `{ x: 2840, y: 264 }`, `{ x: 3040, y: 184 }`, `{ x: 3260, y: 136 }`, `{ x: 3560, y: 264 }`, `{ x: 3900, y: 176 }`.
    Append exactly 1 new entry to the `spikes` array: `{ x: 3200, y: FLOOR_Y - CONFIG.SPIKE_SIZE }` (sits on the new floor-4, 2960..3520 span).
    Append exactly 1 new entry to the `mathGates` array (in addition to the 2 already present): `{ x: 3760, y: FLOOR_Y - CONFIG.MATH_GATE.H }` (sits on the new floor-5, 3680..4280 span) — level-02 already has doors + mathGates, so this reuses an existing mechanic type per CONTEXT.md's "reuse only mechanic types already present" rule.
    Append exactly 2 new entries to the `checkpoints` array: `{ x: 3120, y: FLOOR_Y - 48 }` (80px lead before the new spike at 3200, on floor-4) and `{ x: 3680, y: FLOOR_Y - 48 }` (80px lead before the new mathGate at 3760, at floor-5's start).
    Do NOT add any `enemies` or `collectZones` entries and do NOT change `answerPickupSlots` — level-02's `enemies: []` and `collectZones: []` are load-bearing signals (per 24-PATTERNS.md) that CONTEXT.md locks: the extension must keep these types absent from level-02.
  </action>
  <acceptance_criteria>
    `coins.length` is 15, `spikes.length` is 5, `mathGates.length` is 3, `checkpoints.length` is 10.
    `enemies.length` is 0 and `collectZones.length` is 0 (unchanged).
    The new mathGate's footprint (3760..3792) is fully contained within floor-5's 3680..4280 span.
    `node scripts/validate-levels.mjs` prints zero lines matching `^level-02 .*HARD-FAIL`.
  </acceptance_criteria>
  <verify>
    <automated>N=$(node scripts/validate-levels.mjs 2>&1 | grep '^level-02 ' | grep -c 'HARD-FAIL'); echo "level-02 HARD-FAILs: $N"; test "$N" = "0"; node --input-type=module -e "import('./src/levels/index.js').then(m => { const g = m.getLevel('level-02').geometry; console.log('enemies:', g.enemies.length, 'collectZones:', g.collectZones.length); })"</automated>
  </verify>
  <done>level-02 extended with 5 new coins, 1 new spike, 1 new mathGate, 2 new checkpoints; enemies/collectZones stay empty; validate-levels.mjs shows zero level-02 HARD-FAIL rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure local static-data edits to a level descriptor, consumed only by the offline single-player Kaplay client and Node-side build/validation tooling. No network input, no auth, no session, no access control, no crypto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | src/levels/level-02.js geometry values | low | accept | Developer-authored static data, not runtime-parsed untrusted input; `node scripts/validate-levels.mjs` gates any malformed/out-of-envelope geometry before it can ship |
| T-24-02 | Information Disclosure | src/levels/level-02.js `bounds.right` mismatch with actual floor extent | low | accept | Both are set to the same value (4280) in this plan by construction, verified by the acceptance criteria's exact-match grep — a stale bounds.right would only under-render, not leak data |

## Package Legitimacy Audit

Not applicable — this plan installs zero new packages.
</threat_model>

<verification>
Run `node scripts/validate-levels.mjs` after Task 1 lands and again after Task 2 lands. Both runs must print zero lines matching `^level-02 .*HARD-FAIL`. WARN rows for level-02 (e.g. the pre-existing gap-width/spawn-goal WARN rows) are expected and non-blocking per CONTEXT.md's locked "zero HARD-FAILs" definition of green.

`node scripts/smoke-progress.mjs` is EXPECTED to fail on its level-02 block after this plan's edits — it is not re-baselined until Plan 24-05 (Wave 2). Do not attempt to fix or work around this failure in this plan; it is an expected, documented interim state.
</verification>

<success_criteria>
- `node scripts/validate-levels.mjs` shows zero `level-02` HARD-FAIL rows.
- level-02's geometry object has: 6 floors, 9 platforms, 15 coins, 5 spikes, 10 checkpoints, 1 door (unchanged), 3 mathGates, 0 enemies (unchanged), 0 collectZones (unchanged), goal.x = 4200, bounds.right = 4280.
- `git diff` on `src/levels/level-02.js` shows only pure appends to floors/platforms/coins/spikes/mathGates/checkpoints/goal plus the `bounds.right` value change — zero edits inside the original 0..2800 extent.
</success_criteria>

<output>
Create `.planning/phases/24-fix-lengthen-levels-1-4/24-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/levels/level-02.js` — edited: 2 new floors, 2 new platforms, 5 new coins, 1 new spike, 1 new mathGate, 2 new checkpoints appended after x:2800; goal moved to x:4200; bounds.right bumped to 4280 (LVL-01)
