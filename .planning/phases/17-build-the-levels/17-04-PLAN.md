---
phase: 17-build-the-levels
plan: 04
type: execute
wave: 4
depends_on:
  - 17-03
files_modified:
  - scripts/browser-boot.mjs
autonomous: false
requirements:
  - LVL-01
  - LVL-04
must_haves:
  truths:
    - "Automated browser boot loads title → select → each new level without runtime errors"
    - "Full static suite (check-gate, check-import-safety, check-safety, smoke-progress) passes"
    - "Human playtest confirms every level is start→goal completable and checkpoints/mechanics feel fair"
  artifacts:
    - path: scripts/browser-boot.mjs
      provides: "Automated navigation through each unlocked level"
      min_lines: 100
  key_links:
    - from: scripts/browser-boot.mjs
      to: src/index.html
      via: "Serves src/ and navigates title→select→each level"
    - from: scripts/browser-boot.mjs
      to: src/levels/index.js
      via: "Level IDs used in navigation come from the registry"
---

<objective>
Extend automated browser verification to cover the new levels and run the full static suite, then get human sign-off that every level is actually completable and fair.

Purpose: Catches runtime errors the headless smoke cannot see and confirms the kid-facing difficulty ramp is gentle.
Output: Updated `scripts/browser-boot.mjs`, green static suite, approved human checkpoint.
</objective>

<execution_context>
@GSD core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-build-the-levels/17-CONTEXT.md
@.planning/phases/17-build-the-levels/17-RESEARCH.md
@scripts/browser-boot.mjs
@src/levels/index.js
@src/scenes/select.js
@src/scenes/game.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend browser-boot.mjs to navigate each new level</name>
  <files>scripts/browser-boot.mjs</files>
  <action>
    Modify scripts/browser-boot.mjs so after navigating title→select→level-01 it also visits level-02, level-03, and level-04. Pre-seed localStorage before page load with a valid v4.0 save blob that marks level-01 cleared (or all levels cleared) so level-02/03/04 are unlocked. For each level: click the unlocked tile, wait for the game scene, and collect any page errors. Keep assertions: no pageerror, no console.error, no HTTP 4xx/5xx. Do not add timers/countdowns; use existing waitForTimeout calls sparingly.
  </action>
  <verify>
    <automated>node scripts/browser-boot.mjs</automated>
  </verify>
  <done>
    browser-boot.mjs loads title → select → level-01 → level-02 → level-03 → level-04 without runtime errors and exits PASS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full static suite</name>
  <files>scripts/check-gate.sh, scripts/check-import-safety.sh, scripts/check-safety.sh, scripts/smoke-progress.mjs</files>
  <action>
    Run the full static suite in order: bash scripts/check-gate.sh, bash scripts/check-import-safety.sh, bash scripts/check-safety.sh, node scripts/smoke-progress.mjs. If any fail, fix the underlying issue before proceeding.
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/smoke-progress.mjs</automated>
  </verify>
  <done>
    All four static checks exit 0 and report PASS.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human completability sign-off for all four levels</name>
  <files>src/levels/level-02.js, src/levels/level-03.js, src/levels/level-04.js, src/scenes/game.js</files>
  <action>
    Human playtests every authored level in a real browser per D-15. Serve the game locally, open src/index.html, clear/unlock each level, and play start→goal. Confirm every gap is reachable, every spike has a preceding checkpoint, every mechanic opens/closes cleanly with the existing forgiving re-ask-on-wrong contract (D-11), camera clamps correctly at level edges, and Escape returns to select cleanly.
  </action>
  <verify>
    <automated>Human approval required — type "approved" or describe issues</automated>
  </verify>
  <done>
    All four levels are confirmed completable and fair; no soft-locks, unfair gaps, or missing checkpoints.
  </done>
  <what-built>
    Four hand-built levels are registered and the automated smoke/browser gates confirm no runtime errors. The remaining verification is whether each level is actually fun and completable start→goal for the kid.
  </what-built>
  <how-to-verify>
    1. Serve the game locally (`python3 -m http.server` or `npx serve src` from the repo root).
    2. Open `http://localhost:8000/src/index.html` in a real browser.
    3. Clear level-01 (or use a pre-cleared save) to unlock level-02, then play level-02 start→goal without getting stuck.
    4. Repeat for level-03 and level-04.
    5. For each level confirm:
       - Every gap is reachable with a normal jump.
       - Every spike has a checkpoint just before it.
       - Every mechanic (door/gate/enemy/collect) opens/closes cleanly and re-asks on wrong answers with no penalty.
       - The camera clamps correctly at the level edges (no void on the right of long levels).
       - Escape → select returns to the level-select screen cleanly.
  </how-to-verify>
  <resume-signal>Type "approved" once all four levels are completable and fair; describe any specific gap/hazard/mechanic that needs tuning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser boot → localStorage | Pre-seeded save blob must be valid or the boot will fail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-05 | Denial of Service | browser-boot.mjs pre-seed localStorage format breaks with future save changes | low | accept | Save format is versioned; if it changes, browser-boot must be updated. Risk is low because the save key and shape are stable this milestone. |
</threat_model>

<verification>
- `node scripts/browser-boot.mjs` passes.
- `bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/smoke-progress.mjs` all pass.
- Human checkpoint is approved.
</verification>

<success_criteria>
- `scripts/browser-boot.mjs` navigates to each new level without runtime errors.
- Full static suite is green.
- Human playtest confirms completability and fairness.
</success_criteria>

<output>
Create `.planning/phases/17-build-the-levels/17-04-SUMMARY.md` when done.
</output>
