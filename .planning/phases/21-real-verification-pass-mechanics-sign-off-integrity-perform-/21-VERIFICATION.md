---
phase: 21-real-verification-pass-mechanics-sign-off-integrity-perform-
verified: 2026-07-04T23:30:00Z
status: passed
score: 4/4 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 1/4
  gaps_closed:
    - "door.js, gates.js, enemy.js, and mathGate.js have each been driven interactively (real player movement + real answer input, not teleport-only), with door.js and enemy.js now genuinely reached via real movement in at least one level per mechanic"
    - "Real bugs found in mechanic #1 are fixed and re-verified — New Finding 4's visual-overlap half is now closed (hide/restore guard), alongside the already-fixed state-corruption half"
    - "The automated boot check actually exercises movement and at least one full mechanic resolution per level — now true for all 4 levels, not just level-01"
  gaps_remaining: []
  regressions: []
gaps: []
---

# Phase 21: Real Verification Pass — Mechanics & Sign-off Integrity Re-Verification Report

**Phase Goal:** Give `door.js`, `gates.js`, `enemy.js`, and `mathGate.js` the same real interactive
scrutiny `collect.js` got during the post-ship diagnostic pass (which found 5 real bugs, including a
total soft-lock, hiding behind claims of "passed") — these four were code-verified but never
screenshot/interaction-audited across all 4 levels. Harden the automated boot gate so it actually
exercises movement and mechanic resolution instead of just confirming scenes load with zero console
errors, and correct the unsupported "human sign-off" claims left in `v4.0-MILESTONE-AUDIT.md` and the
REQUIREMENTS.md traceability table so the project record stops asserting verification that never
happened.

**Verified:** 2026-07-04T23:30:00Z
**Status:** passed
**Re-verification:** Yes — after gap-closure plans 21-05, 21-06, 21-07

## Re-Verification Methodology

This is a re-verification following the initial pass (`status: gaps_found`, 1/4 truths verified,
`.planning/phases/.../21-VERIFICATION.md` previous content), which found that door.js and enemy.js
had NEVER been reached with real player movement in any level, that New Finding 4's visual-overlap
half was left unfixed, and that the hardened boot gate covered level-01 only.

Per this phase's own stated premise (prior "passed" claims can hide real bugs), SUMMARY.md claims
for plans 21-05/21-06/21-07 were **not** trusted at face value. The following was independently
re-run and directly observed during this verification session (not merely re-read from
21-FINDINGS.md or the plan SUMMARYs):

- `node scripts/audit-phase21-mechanics.mjs` — run **twice**, independently, from a clean working
  tree, to check for run-to-run stability (the plan's own SUMMARY documents inherent
  headless-browser timing jitter on some rows).
- `node scripts/browser-boot.mjs` — run once, fresh.
- `bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh
  && node scripts/smoke-progress.mjs` — the full static gate suite.
- Direct `Read` of `scripts/lib/mechanic-drive.mjs`, `scripts/audit-phase21-mechanics.mjs`,
  `scripts/browser-boot.mjs`, `src/ui/challenge.js`, `src/mechanics/enemy.js`,
  `src/mechanics/gates.js` (current source, not summaries of it).
- Direct `Read` (visual inspection) of 3 regenerated screenshots produced by my own re-runs:
  `level-01-door-1400-before.png`, `level-01-enemy-1000-before.png`,
  `level-01-math-gate-600-before.png`.
- `git diff`/`git status` checks to confirm calibration edits were reverted and no debt markers
  were introduced.

Screenshots regenerated by my own verification runs were reverted (`git checkout --`) after
inspection so the working tree is unchanged by this verification pass.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `door.js`, `gates.js`, `enemy.js`, `mathGate.js` each driven interactively (real movement + real answer input, not teleport-only), findings recorded | ✓ VERIFIED | Independently re-ran `node scripts/audit-phase21-mechanics.mjs` TWICE from a clean tree. Both runs: `level-01 door x:1400` triggered:true/resolved:true; `level-01 enemy x:1000` triggered:true/resolved:true; `level-04 door x:900` triggered:true/resolved:true — reproducible across both my runs, not a one-off fluke. Visually confirmed via `level-01-door-1400-before.png` (shows the door glyph in-frame, challenge overlay open, "6 × 6" + 4 answer boxes — not the previous "player stuck at spawn" failure pattern) and `level-01-enemy-1000-before.png` (shows the enemy/spike glyphs in-frame, two-line "Answer to defeat the guard: / 8 × 8" overlay). This is the exact bar the prior VERIFICATION.md's own remediation guidance set ("at least one level" per mechanic, not exhaustive all-3-placements coverage) — met and independently reproduced. 6/16 encounters (level-02 math-gate 1100/door 1540, level-03 enemy 2400, level-04 math-gate 1800/enemy 2400) remain genuinely unreached in both my runs, consistent with 21-FINDINGS.md's documented technical reason (fixed jump-cadence periodicity resonating with static spike positions in denser floor runs) — an honestly disclosed, narrower residual limitation, not a hidden failure of the phase's PRIORITY gap (door.js/enemy.js reachability). |
| 2 | Any real bugs found in mechanic #1 are fixed and re-verified | ✓ VERIFIED | Finding 2 (enemy.js prompt-override) — confirmed fixed in current source (`src/mechanics/enemy.js` line 49 passes `label`, not `prompt`; `src/ui/challenge.js` renders it as its own stacked line). New Finding 4's state-corruption half — confirmed fixed (per-instance `instanceTag` scoping, unchanged from prior verification). New Finding 4's visual-overlap half — confirmed fixed THIS session: `src/ui/challenge.js` now captures `priorChallengeObjs = get("challenge")` before creating its own objects and hides/restores them around `close()` (read directly, lines 116-119 and 320-325); my own independent re-run's regenerated `level-01-math-gate-600-before.png` shows a single, clean "9 × 5" overlay with the collect-zone's pickups visible-but-unobstructed in the background — no ghosted/overlapping second prompt, refuting the pre-fix garbled-UI bug directly. CR-01/CR-02 (found by code review) remain fixed and unchanged. |
| 3 | Automated boot check exercises movement + ≥1 full mechanic resolution per level | ✓ VERIFIED | Independently ran `node scripts/browser-boot.mjs` fresh: exit 0, `"Browser boot: PASS — title -> select -> all levels loaded with no runtime errors."` Direct source read of `scripts/browser-boot.mjs` confirms the previous `if (i === 0)` level-01-only special case is gone, replaced by a generic per-level loop (lines 152-190) driven by `deriveEncounters(level.geometry)`/`driveToXClimbing`/`resolveIfBoxed` for ALL 4 levels, with the plan-checker-flagged one-line scoping-rationale comment present (lines 145-151, explaining the gate stops at each level's first resolvable mechanic and defers the exhaustive sweep to `audit-phase21-mechanics.mjs`). |
| 4 | `v4.0-MILESTONE-AUDIT.md` unsupported sign-off claims corrected/annotated; NAV-04 traceability resolved | ✓ VERIFIED | Re-confirmed unchanged from the prior verification pass: direct file read still shows the Phase 14 row's `[Corrected 2026-07-04, Phase 21 audit]` annotation citing `14-VERIFICATION.md`'s `human_needed` status, and the archived `v4.0-REQUIREMENTS.md`'s NAV-04 row carries its matching dated annotation. No regression. |

**Score:** 4/4 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/lib/mechanic-drive.mjs` | New shared traversal module (`deriveEncounters`, `driveToXClimbing`, `resolveIfBoxed`) | ✓ VERIFIED | Exists, substantive (247 lines, no stub patterns), no Playwright launch code of its own — confirmed via direct read. Exports all 3 functions; `driveToXClimbing` polls live `isGrounded()` state every tick, no precomputed gap table. |
| `scripts/audit-phase21-mechanics.mjs` | Rewired to import shared module; obsolete `driveToX`/`deriveGapRanges`/`assertGapsAreSingleJumpable` removed | ✓ VERIFIED | Confirmed via direct read — imports `deriveEncounters`/`driveToXClimbing`/`resolveIfBoxed` from `./lib/mechanic-drive.mjs`; none of the obsolete functions remain (`grep` negative confirmed). Independently re-run twice, both times producing the expected JSON results. |
| `scripts/browser-boot.mjs` | Level-agnostic hardened check, all 4 levels | ✓ VERIFIED | Confirmed via direct read (imports `getLevel`/shared helpers, no `if (i === 0)` guard) and a fresh independent run (exit 0, PASS). |
| `src/ui/challenge.js` | `openChallenge()` hides prior challenge's objects on entry, restores in `close()` | ✓ VERIFIED | Confirmed via direct read (lines 116-119, 320-325) and visual screenshot evidence from my own re-run (`level-01-math-gate-600-before.png` shows a single clean overlay). |
| `src/mechanics/enemy.js` (fixed) | Uses `label` instead of `prompt` | ✓ VERIFIED | Confirmed unchanged from prior verification — line 49: `label: "Answer to defeat the guard:"`. |
| `21-FINDINGS.md` | Full Mechanic Sweep + Methodology Note rewritten; New Finding 4 disposition updated to both-halves-fixed | ✓ VERIFIED | Confirmed present and consistent with my own independent re-run's results (door.js/enemy.js rows bolded as newly-true; New Finding 4 section documents the Plan 21-06 fix with concrete evidence). |
| `.planning/milestones/v4.0-MILESTONE-AUDIT.md` / `v4.0-REQUIREMENTS.md` (annotated) | Phase 14 row + NAV-04 row corrected | ✓ VERIFIED | Re-confirmed via direct read, unchanged from prior pass. |
| `deferred-items.md` | Out-of-scope discovery log | ✓ VERIFIED (informational) | Documents a pre-existing, unrelated `check-gate.sh` pipefail/`grep -q` flakiness found during Plan 21-07's regression testing — explicitly out of scope for this phase (targets files this phase never touches), correctly not blocking. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `scripts/audit-phase21-mechanics.mjs` | `scripts/lib/mechanic-drive.mjs` | `import { deriveEncounters, driveToXClimbing, resolveIfBoxed }` | ✓ WIRED | Confirmed via direct read and successful execution (both my independent runs completed using this import path). |
| `scripts/browser-boot.mjs` | `scripts/lib/mechanic-drive.mjs` | same import | ✓ WIRED | Confirmed via direct read and successful execution (exit 0). |
| `src/mechanics/enemy.js` | `src/ui/challenge.js` | `openChallenge({ label })` | ✓ WIRED | Confirmed via direct read and screenshot evidence (two-line label/arithmetic render). |
| `src/ui/challenge.js`'s `openChallenge()` | its own `close()` | `priorChallengeObjs` closure capture/restore | ✓ WIRED | Confirmed via direct read: captured before any of the instance's own `add()` calls, restored after `destroyAll(instanceTag)` in `close()`. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full interactive mechanic audit reaches door.js + enemy.js via real movement (run 1, independent) | `node scripts/audit-phase21-mechanics.mjs` | `level-01 door x:1400 triggered:true/resolved:true`; `level-01 enemy x:1000 triggered:true/resolved:true`; `level-04 door x:900 triggered:true/resolved:true` (exit 0, `AUDIT: FAILURES DETECTED` for the 6 still-unreached deep-map rows only) | ✓ PASS |
| Same audit, run again (run 2, independent) to check stability of the priority truth | `node scripts/audit-phase21-mechanics.mjs` | Identical door.js/enemy.js results (all 3 still triggered:true/resolved:true); one previously-flaky math-gate row (level-03 x:420) flipped from false→true between runs, consistent with the documented run-to-run browser-timing jitter, not a regression of the priority truth | ✓ PASS |
| Hardened boot gate exercises all 4 levels | `node scripts/browser-boot.mjs` | `Browser boot: PASS — title -> select -> all levels loaded with no runtime errors.` (exit 0) | ✓ PASS |
| Static gate suite regression check | `check-gate.sh && check-import-safety.sh && check-safety.sh && smoke-progress.mjs` | All 4 print PASS | ✓ PASS |
| Visual-overlap fix confirmed | Read `level-01-math-gate-600-before.png` (regenerated by my own run) | Single clean "9 × 5" overlay, collect-zone pickups visible-but-unobstructed in background, no ghosted second prompt | ✓ PASS |
| Debt-marker scan on all phase-touched files | `grep -nE "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` | No matches in `mechanic-drive.mjs`, `audit-phase21-mechanics.mjs`, `browser-boot.mjs`, `challenge.js`, `enemy.js`, `gates.js`, `door.js` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|-----------------|--------------|--------|----------|
| VERIFY-01 | 21-01, 21-05 | door/gates/enemy/mathGate driven interactively, findings recorded | ✓ SATISFIED | door.js and enemy.js now genuinely triggered+resolved via real movement in at least one level each, reproducibly across 2 independent re-runs I performed this session. |
| VERIFY-02 | 21-04, 21-06 | Real bugs found are fixed and re-verified | ✓ SATISFIED | Both halves of New Finding 4 now fixed and confirmed via direct source read + screenshot evidence; Finding 2/CR-01/CR-02 unchanged and still fixed. |
| VERIFY-03 | 21-02, 21-07 | Boot check exercises movement + mechanic resolution per level | ✓ SATISFIED | All 4 levels now hardened, confirmed via a fresh independent run (exit 0) and direct source read (no `if (i === 0)` special case remains). |
| VERIFY-04 | 21-03 | Unsupported sign-off claims corrected; NAV-04 traceability resolved | ✓ SATISFIED | Unchanged from prior verification, re-confirmed via direct file read. |

No orphaned requirements — all 4 IDs in `.planning/REQUIREMENTS.md`'s Phase 21 mapping are claimed
by exactly one plan-group each (VERIFY-01: 21-01/21-05; VERIFY-02: 21-04/21-06; VERIFY-03:
21-02/21-07; VERIFY-04: 21-03), matching the phase's 7 plans (4 original + 3 gap-closure).
`.planning/REQUIREMENTS.md` itself marks all 4 as `[x]` complete, consistent with this
re-verification's findings.

### Anti-Patterns Found

None. Debt-marker scan (`TBD`/`FIXME`/`XXX`) and warning-marker scan (`TODO`/`HACK`/`PLACEHOLDER`)
across every file modified across all 7 plans returned zero matches. No stub returns, no
hardcoded-empty-data patterns. The one open item (`check-gate.sh`'s pre-existing pipefail/`grep -q`
flakiness) is correctly logged as an out-of-scope deferred item, not silently absorbed or hidden.

## Gaps Summary

All 3 gaps identified in the previous verification pass are closed, confirmed via independent
re-execution (not by trusting SUMMARY.md's narrative):

1. **Door.js/enemy.js reachability (the phase's PRIORITY gap)** — previously ZERO interactive
   evidence anywhere in the phase's artifacts. Now: the shared, committed
   `scripts/lib/mechanic-drive.mjs` traversal module (physics-informed jump-whenever-grounded,
   replacing the retired single-jump-per-gap model) reaches and resolves door.js at level-01
   (x:1400) and level-04 (x:900), and enemy.js at level-01 (x:1000) — confirmed reproducibly
   across 2 independent live re-runs performed during this verification session, plus direct visual
   screenshot inspection showing the player genuinely near each mechanic (not stuck at spawn, the
   exact failure pattern previously flagged). 6 of 16 total encounters remain unreached, with a
   specific, honestly-documented technical reason (spike-hazard timing resonance in denser floor
   runs) rather than the retired generic limitation — this matches the closure bar the prior
   VERIFICATION.md's own remediation guidance explicitly set ("at least one level per mechanic"),
   and is not treated as a residual gap.

2. **New Finding 4's visual-overlap half** — previously explicitly left unfixed per an inline code
   comment. Now: `src/ui/challenge.js`'s `openChallenge()`/`close()` hide/restore guard is
   confirmed present in current source and its effect is confirmed visually (regenerated
   screenshot shows a single clean overlay, no ghosted second prompt) — not merely claimed in a
   SUMMARY.

3. **Boot gate level-01-only scope** — previously only level-01 received real movement + resolution
   hardening. Now: confirmed via direct source read (no `if (i === 0)` special case remains) and a
   fresh independent run (exit 0, all 4 levels visited) that the hardening is level-agnostic and
   applies uniformly.

VERIFY-04 (sign-off/traceability correction) remained satisfied throughout and required no
gap-closure work; re-confirmed unchanged.

No regressions were found: the full static gate suite passes, the working tree (after reverting my
own verification-run screenshot regeneration) is clean, and no debt markers exist in any
phase-touched file.

---

_Verified: 2026-07-04T23:30:00Z_
_Verifier: Claude (gsd-verifier)_
