---
phase: 21-real-verification-pass-mechanics-sign-off-integrity-perform-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/browser-boot.mjs
autonomous: true
requirements: [VERIFY-03]
must_haves:
  truths:
    - "node scripts/browser-boot.mjs holds a real directional key to move the player and fully resolves at least one boxed mechanic (via real 1-4 key input) for level-01, in addition to the existing zero-console-error assertion"
    - "the hardened gate is proven to be a REAL check (not a no-op) via a RED/GREEN calibration: temporarily reintroducing the exact v4.0 collect.js soft-lock pattern makes the gate fail, and reverting makes it pass again"
  artifacts:
    - "scripts/browser-boot.mjs (hardened in place — 2 new errors.push() assertion sites inside the existing i===0 level-01 branch: collect-zone trigger-on-movement at x:300, math-gate full-resolution at x:600)"
  key_links:
    - "scripts/browser-boot.mjs's new assertions read get(\"challenge\").length via page.evaluate() — the same live-scene introspection technique used by scripts/audit-phase21-mechanics.mjs (Plan 21-01), proving the hardened gate exercises the same real trigger/resolve behavior a full interactive audit does, just for 1 level instead of 4"
---

<objective>
Harden the existing automated boot gate (`scripts/browser-boot.mjs`) so it actually exercises
real player movement and fully resolves at least one boxed mechanic per run — not just "scene
loaded, zero console errors." This closes the exact validation gap that let the v4.0 collect.js
total soft-lock ship as "passed": the current gate seeds every level as pre-cleared and never
plays.

Purpose: VERIFY-03 requires the automated boot check to exercise movement and at least one full
mechanic resolution per level, per CONTEXT.md's Area 3 (Automated Boot Gate Hardening). This is
the fast, per-commit gate (unlike Plan 21-01's exhaustive
one-off 4-level audit) — it must stay minimal and fast, per 21-RESEARCH.md's Pitfall 3 guidance,
while still closing the real gap.

Output: `scripts/browser-boot.mjs` hardened in place with 2 new movement+resolution assertions,
proven via a RED/GREEN calibration to be a genuine check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-CONTEXT.md
@.planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-RESEARCH.md
@.planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-PATTERNS.md

# Existing files this plan reads/edits directly
@scripts/browser-boot.mjs
@scripts/check-import-safety.sh
@src/levels/level-01.js
@src/mechanics/collect.js
@src/ui/challenge.js

<interfaces>
Current scripts/browser-boot.mjs structure (full file, 132 lines) relevant to this plan:
  const errors = [];                                    // populated by 3 existing listeners
  page.on("pageerror", ...); page.on("console", ...); page.on("response", ...);
  ...
  for (let i = 0; i < levels.length; i++) {              // levels = ["level-01",...,"level-04"]
    for (let j = 0; j < i; j++) { ArrowRight cursor move }
    await page.keyboard.press("Enter");
    await page.waitForTimeout(1500);                     // <-- insertion point, i===0 branch only
    if (i < levels.length - 1) { Escape + wait }
  }
  ...
  if (errors.length > 0) { ...failed = true... }         // existing pass/fail gate — DO NOT touch

level-01's mechanic-position map (src/levels/level-01.js, byte-for-byte verified this session):
  collectZones: x:300 (floor run 0, x:0-560 — no jump needed to reach it from spawn x:64)
  mathGates:    x:600, x:1300
  enemies:      x:1000
  doors:        x:1400
CONFIG.RUN_SPEED = 240 (px/s, src/config.js) — timing hint only.

challenge.js's answer-key contract: keys "1".."4" resolve a renderChoices:true challenge
(math-gate/door/enemy); collect.js's zone opens with renderChoices:false — no key handlers
exist for it, only get("challenge").length > 0 proves the trigger fired.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add movement + mechanic-resolution assertions to browser-boot.mjs for level-01</name>
  <files>scripts/browser-boot.mjs</files>
  <read_first>
    scripts/browser-boot.mjs (full file, already loaded), src/levels/level-01.js (mechanic
    positions), src/ui/challenge.js (renderChoices/key-binding behavior), src/mechanics/collect.js
    (confirms the collect zone registers no key handlers)
  </read_first>
  <action>
    Immediately after the existing `await page.keyboard.press("Enter"); await
    page.waitForTimeout(1500);` block, inside the `i === 0` iteration only (level-01, the only
    level containing one of every mechanic type), insert a new movement+resolution check reusing
    the polling technique 21-RESEARCH.md's Architecture Patterns section describes: hold
    `ArrowRight` via `page.keyboard.down("ArrowRight")`, wait `1000`ms (level-01's collect zone
    sits at world x:300, ~236px from the default spawn x:64 at `CONFIG.RUN_SPEED` 240px/s — roughly
    983ms, rounded up with margin), release via `page.keyboard.up("ArrowRight")`, then assert
    `await page.evaluate(() => get("challenge").length > 0)` is true; if false, push
    `{ type: "mechanic", message: "collect zone at x:300 never triggered a challenge on real movement" }`
    onto the EXISTING `errors` array (the same array the script's 3 existing listeners already
    populate — reuse it, do not create a second array or a new pass/fail mechanism).

    Then continue holding `ArrowRight` for a further `1250`ms to cover the remaining ~300px to the
    math-gate at world x:600 (crossing floor-run 0, x:0-560, so no jump is needed for this
    specific approach), release, then cycle `page.keyboard.press("1")` through
    `page.keyboard.press("4")`, each followed by `page.waitForTimeout(200)` and a re-check of
    `await page.evaluate(() => get("challenge").length)`, stopping as soon as it reads `0`; if it
    still reads greater than `0` after all four presses, push
    `{ type: "mechanic", message: "math-gate at x:600 never resolved after cycling keys 1-4" }`
    onto `errors`.

    Keep the existing `if (errors.length > 0) { ...failed = true... }` gate and the final
    `process.exit(failed ? 1 : 0)` completely unchanged — this task only adds entries to the same
    `errors` array that gate already checks. Do not touch the loop's behavior for `i` 1-3 (levels
    2-4) — the hardening is level-01-only, per 21-RESEARCH.md's Pitfall 3 guidance to keep this
    per-commit gate fast, reserving the full 4-level sweep for Plan 21-01's one-off audit script.
  </action>
  <verify>
    <automated>node --check scripts/browser-boot.mjs && grep -c "get(\"challenge\")" scripts/browser-boot.mjs && grep -c "errors.push" scripts/browser-boot.mjs && grep -q "if (errors.length > 0)" scripts/browser-boot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/browser-boot.mjs` references `get("challenge")` at least twice (the collect-zone
      trigger check and the math-gate resolution loop).
    - `scripts/browser-boot.mjs` contains at least 2 new `errors.push(` call sites beyond what
      existed before this task.
    - The existing `if (errors.length > 0)` pass/fail gate and `process.exit(failed ? 1 : 0)`
      line are byte-identical to before this task.
    - The new assertions execute only inside the `i === 0` (level-01) branch — the loop body for
      `i` 1-3 is unchanged.
  </acceptance_criteria>
  <done>
    browser-boot.mjs now holds ArrowRight to reach and trigger the level-01 collect zone, and
    cycles keys 1-4 to fully resolve the level-01 math-gate at x:600, both feeding the existing
    errors array/pass-fail gate; the existing zero-console-error assertion and levels 2-4 behavior
    are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: RED/GREEN calibration + full static suite confirmation</name>
  <files>scripts/browser-boot.mjs</files>
  <read_first>
    scripts/browser-boot.mjs (this task's own Task-1 output), src/mechanics/collect.js (the
    `wireCollect` function whose fixed soft-lock this calibration temporarily reintroduces),
    scripts/check-import-safety.sh (Section 1b — the project's own established RED/GREEN
    calibration-gate precedent this task mirrors)
  </read_first>
  <action>
    Run `node scripts/browser-boot.mjs` and confirm it exits 0 with the new movement+resolution
    assertions passing (console output shows no `"mechanic"`-typed error entries). Then perform a
    one-time RED/GREEN calibration proving the hardening is a real check, not a no-op (mirroring
    `scripts/check-import-safety.sh`'s own Section 1b self-test discipline): in
    `src/mechanics/collect.js`'s `wireCollect` function, temporarily re-add `player.paused =
    true;` immediately after the `// CRITICAL:` comment block inside the
    `player.onCollide("answer-zone", ...)` handler (this reintroduces the exact v4.0 soft-lock
    STATE.md already documents as fixed). Run `node scripts/browser-boot.mjs` again and confirm it
    now exits non-zero with a `"mechanic"`-typed error whose message mentions the collect zone
    never triggering (the frozen player can no longer reach/trigger the zone, so Task 1's
    assertion correctly fails) — this is the RED half. Immediately revert the temporary
    `player.paused = true;` line (`git checkout -- src/mechanics/collect.js`) and re-run `node
    scripts/browser-boot.mjs` one final time to confirm it exits 0 again (GREEN). Do not leave
    `src/mechanics/collect.js` modified — it is out of this plan's `files_modified` scope; the
    calibration edit must be fully reverted before this task is done. Finally, run the existing
    full static suite (`check-gate.sh`, `check-import-safety.sh`, `check-safety.sh`,
    `smoke-progress.mjs`) to confirm no regressions from the browser-boot.mjs edit.
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/smoke-progress.mjs && node scripts/browser-boot.mjs && test -z "$(git status --porcelain src/mechanics/collect.js)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/browser-boot.mjs` exits 0 on the current (fixed) codebase.
    - During execution of this task, the calibration step (temporarily reintroducing
      `player.paused = true` in collect.js) caused the hardened gate to exit non-zero with a
      `"mechanic"`-typed error before being reverted.
    - `git status --porcelain src/mechanics/collect.js` is empty (no leftover calibration edit).
    - The full static gate suite (`check-gate.sh`, `check-import-safety.sh`, `check-safety.sh`,
      `smoke-progress.mjs`) all print their PASS line after this task.
  </acceptance_criteria>
  <done>
    Hardened browser-boot.mjs runs green on the current tree, was proven to correctly fail
    (calibration) when the exact v4.0 collect.js soft-lock pattern is temporarily reintroduced,
    and the calibration edit was fully reverted; the full static suite remains green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Playwright-synthesized input → Kaplay canvas/game logic | The hardened gate drives real keyboard events into the same client code a real player uses; local dev-only tooling |
| Node HTTP server (scripts/browser-boot.mjs, existing) → localhost:8765 | Serves src/ statically to the headless browser for the duration of the script only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-02 | Denial of Service (self) | scripts/browser-boot.mjs Node HTTP server (existing, unchanged) | low | mitigate | Task 1 inserts new assertions strictly inside the existing `try` block; the pre-existing `finally { context.close(); browser.close(); server.close(); }` cleanup is untouched, so the hardening cannot introduce a new server-leak path |
| T-21-04 | Tampering (self, temporary) | src/mechanics/collect.js (Task 2's calibration edit) | low | mitigate | The calibration edit is explicitly required to be reverted before task completion, verified by `git status --porcelain` being empty in the automated check — the temporary regression never reaches a commit |
</threat_model>

## Artifacts This Phase Produces (this plan's contribution)

- `scripts/browser-boot.mjs` — hardened in place; adds 2 new `errors.push()` assertion sites
  (collect-zone trigger-on-movement check at x:300, math-gate full-resolution check at x:600)
  inside the existing `i === 0` level-01 branch. No new exported symbols — this is a standalone
  script, not a module.

<verification>
- [ ] `node --check scripts/browser-boot.mjs` passes
- [ ] `node scripts/browser-boot.mjs` exits 0 with no `"mechanic"`-typed errors
- [ ] Calibration proved the new assertions fail correctly when the fixed soft-lock is
      temporarily reintroduced, then pass again once reverted
- [ ] `git status --porcelain src/mechanics/collect.js` is empty at task completion
- [ ] Full static suite (`check-gate.sh`, `check-import-safety.sh`, `check-safety.sh`,
      `smoke-progress.mjs`) all pass
</verification>

<success_criteria>
- The automated boot check actually exercises real movement and fully resolves at least one
  boxed mechanic per level (level-01), not just "scene loaded, zero console errors."
- The hardening is proven to be a genuine check via a RED/GREEN calibration, not a no-op.
- No regression in the existing static gate suite.
</success_criteria>

<output>
Create `.planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-02-SUMMARY.md` when done.
</output>
