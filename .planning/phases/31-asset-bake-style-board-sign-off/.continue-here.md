---
context: phase
phase: 31-asset-bake-style-board-sign-off
task: post-execution (code-review-fix complete, verification not yet run)
total_tasks: n/a (post-execution sequence, not a plan task)
status: in_progress
last_updated: 2026-07-10T18:33:04.128Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Never auto-approve `checkpoint:human-verify` gates — `workflow.auto_advance=true` is ON in this project's config, which by default silently auto-approves human-verify checkpoints during autonomous execution. This project's own standing precedent (Phases 25/27/28) and this session's own memory explicitly override that default: EVERY `checkpoint:human-verify` must be presented genuinely to the actual user and answered for real, never assumed/rubber-stamped. Phase 31's own style-board checkpoint took 5 real rounds (2 real bugs found and fixed) before genuine approval — proof this matters. Phases 33, 36, and 38 (still ahead in this milestone) each carry their own such checkpoint. Structural mitigation: when executor agents return `## CHECKPOINT REACHED` with `type: human-verify`, the orchestrator must stop and actually show the artifact to the user (screenshot/file/URL) and wait for a real, specific answer — never synthesize `user_response: "approved"` itself.

**Do not proceed until this is understood.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Gitignored per-plan artifacts don't survive worktree teardown | `assets/_gothicvania-src/` (Plan 31-01's re-fetched source packs) is intentionally gitignored. Every subsequent plan/continuation that ran in a NEW worktree had to re-fetch it from scratch (documented independently in 31-02, 31-03, 31-04, 31-05's SUMMARYs). This is expected/accepted for this phase, but costs ~5-10 min of redundant re-fetching per new worktree. | advisory | If a future phase splits fetch/consume across many waves again, consider a shared non-gitignored scratch location, or accept the redundant re-fetch cost as this phase did. |
| Absolute-path drift when a continuation agent writes from outside its worktree | Plan 31-02's finalization continuation initially wrote SUMMARY.md to the main repo instead of its worktree because a `pwd`-derived absolute path resolved wrong. It self-caught and fixed this before staging (verified via `git status` in both locations). | advisory | Continuation prompts for existing worktrees should explicitly instruct: derive all paths from `git rev-parse --show-toplevel` run INSIDE the target worktree, never trust an ambient absolute path. |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Autonomous milestone run (`/gsd-autonomous`, no flags — full v6.0 milestone, phases 31-38) is mid-Phase-31. All 6 of Phase 31's plans (31-01 through 31-06) are executed, merged to `main`, and tracking-updated (ROADMAP checkbox shows [x], but this reflects plan-level completion only — NOT the authoritative goal-verified close). Code review ran (0 critical, 4 warning, 2 info), REVIEW.md committed. Code-review-fix just completed (all 4 warnings fixed, committed: `3f081f6`, `99f5e17`, `c82fa7a`, `91aec3d`), pink-gate re-verified PASS. `31-REVIEW-FIX.md` exists on disk but is **not yet committed**. Paused here at user request (quota limits) before running the remaining post-execution steps: regression_gate, verify_phase_goal (gsd-verifier → VERIFICATION.md), update_roadmap (the REAL phase-close), and before advancing to Phase 32.
</current_state>

<completed_work>

**Phase 31 discuss:** Full smart-discuss with 4 grey-area tables, PLUS live human/kid feedback that changed the plan mid-session — Swamp Hunter ships as player (not Gothic Hero), Hell hound replaces the fire skull in castle, pink-scan relaxed to ~8-10% dominant-hue threshold. CONTEXT.md committed.

**Phase 31 research + pattern-mapping + planning:** RESEARCH.md found the Gothicvania source packs aren't present anywhere on disk (only preview crops) — the plan opens with a re-fetch task. PATTERNS.md + VALIDATION.md written. Planner produced 6 plans / 5 waves. Plan-checker passed with 0 blockers, 2 non-blocking process warnings (fixed directly in VALIDATION.md).

**Phase 31 execution — all 6 plans, all waves, all merged to main:**
- 31-01 (wave 1): re-fetched 5 Gothicvania OGA zips, CC0-reverified, gitignored.
- 31-02 (wave 2): style-board regen + **5-round genuine human sign-off** — 2 real bugs found (castle player floor, swamp floor, hell hound floor) and fixed before final "Looks good. Approved."
- 31-03 (wave 2): pink-scan gate (`pink_scan.py` + `check-pink-gate.sh`), RED-first proven.
- 31-04 (wave 3): 4 biome terrain atlases + 4×3 parallax layers baked, Town/Cemetery retinted, pink-gate PASS.
- 31-05 (wave 4): Swamp Hunter + Hell hound sprites baked, anchor/lip convention documented (flagged Cemetery/Castle atlas irregularities for Phase 32's attention).
- 31-06 (wave 5): CREDITS.md + LICENSES proofs written, final full-tree pink-gate PASS.

**Code review + fix:** REVIEW.md found 4 warnings (aspect-ratio skew in atlas baking being the most substantive). Fixer applied and committed all 4 fixes. `31-REVIEW-FIX.md` written but not yet committed.
</completed_work>

<remaining_work>

1. Commit `31-REVIEW-FIX.md` (`docs(31): add code review fix report`).
2. Run `regression_gate` (trivial no-op — no build/test infra in this project).
3. Run `verify_phase_goal` — spawn `gsd-verifier` for Phase 31 against ROADMAP's 4 success criteria + ART-01 requirement, producing `31-VERIFICATION.md`.
4. If verification passes: `update_roadmap` (the real `phase.complete` close, distinct from the plan-progress checkbox already showing complete) → `close_phase_todos` → `update_project_md`.
5. Resume the `/gsd-autonomous` loop: Phase 32 (Terrain & Parallax Rendering) → discuss → plan → execute, then Phases 33 (checkpoint!) → 34 → 35 → 36 (checkpoint!) → 37 → 38 (2 checkpoints!).
6. After Phase 38: milestone lifecycle — `gsd-audit-milestone` → `gsd-complete-milestone v6.0` → `gsd-cleanup`.
</remaining_work>

<decisions_made>

- Swamp Hunter (not Gothic Hero) ships as the player across all 4 biomes — kid's explicit pick after viewing the style board.
- Hell hound (not fire skull) is the castle enemy — kid's explicit complaint, also doubles as Phase 36's patrol-enemy candidate (static sprite only this phase; motion wiring is Phase 36's job).
- Pink-scan gate threshold relaxed to ~8-10% dominant-hue (not zero-tolerance) — explicit user correction ("don't hunt it like it is the plague").
- Gothicvania Church folds into the Castle biome rather than shipping as a 5th biome.
- Castle atlas cap tile has an inverted lip (gold trim at bottom, not top) and Cemetery's cap tile never reaches the tile bottom — both documented in `docs/LEVEL-DESIGN.md` §9 as explicit Phase-32 follow-ups, not silently glossed over.
</decisions_made>

<blockers>
None blocking — the only pause reason is quota management, not a technical or decision blocker.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full machine-readable state, read this FIRST.
2. `.planning/phases/31-asset-bake-style-board-sign-off/31-REVIEW-FIX.md` — what the fixer actually did (uncommitted, still on disk).
3. `.planning/phases/31-asset-bake-style-board-sign-off/31-CONTEXT.md` — the locked decisions above, in full detail with rationale.
4. `.planning/STATE.md` — broader project/milestone context.

## Infrastructure State
- `python3 -m http.server 8000` still running in background (repo root) for browser verification — harmless, kill with `pkill -f 'http.server 8000'` if desired.
- The temporary style-board preview server on port 8001 was already stopped (its worktree has been merged and removed).
- No other background agents or worktrees should remain — all 6 phase-31 plan worktrees were merged and cleaned up; the code-review-fix worktree (`gsd-reviewfix/31-212880`) was also merged and cleaned up per its own report.

<context>
This is a long `/gsd-autonomous` run covering all 8 remaining v6.0 phases (31-38) plus milestone lifecycle. Phase 31 was unusually eventful — genuine, non-trivial human-in-the-loop iteration on the style-board checkpoint (5 real rounds), which is exactly the kind of interaction this project's standing "never rubber-stamp checkpoint:human-verify" precedent exists for. The same discipline is required for Phases 33/36/38's checkpoints later in this run — do not let `workflow.auto_advance=true` silently approve them.
</context>

<next_action>
Start with: commit `31-REVIEW-FIX.md`, then run Phase 31's `verify_phase_goal` step (spawn `gsd-verifier`). If it passes, close out Phase 31 properly (`phase.complete`) and resume `/gsd-autonomous` at Phase 32.
</next_action>
