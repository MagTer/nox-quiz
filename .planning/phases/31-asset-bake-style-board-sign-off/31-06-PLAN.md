---
phase: 31-asset-bake-style-board-sign-off
plan: 06
type: execute
wave: 5
depends_on: ["31-04", "31-05"]
files_modified:
  - CREDITS.md
  - assets/LICENSES/gothicvania-swamp.txt
  - assets/LICENSES/gothicvania-town.txt
  - assets/LICENSES/gothicvania-cemetery.txt
  - assets/LICENSES/gothicvania-church.txt
  - assets/LICENSES/gothicvania-patreon.txt
autonomous: true
requirements: [ART-01]

must_haves:
  truths:
    - "Every new file vendored under assets/ this phase (4 atlases, 12 parallax layers, player-swamphunter.png, enemy-hellhound.png) has a matching CREDITS.md row and a matching assets/LICENSES/<pack>.txt proof file — verified by cross-count, not assumed complete."
    - "bash scripts/check-pink-gate.sh passes cleanly over the FULL final assets/ tree (every file from every plan in this phase included) — the definitive GREEN regression proof, the last step before this phase's automated gates are considered closed."
    - "No CC-BY content (the Pascal Belisle music bundled in 3 of the 5 packs) appears anywhere under assets/ or in any CREDITS.md row — only the CC0 art is vendored."
  artifacts:
    - "CREDITS.md (new rows for all Gothicvania-sourced assets)"
    - "assets/LICENSES/gothicvania-{swamp,town,cemetery,church,patreon}.txt (5 new proof files, one per re-fetched pack)"
  key_links:
    - "Every CREDITS.md row's File column must list an ACTUAL file that exists on disk after Plans 31-04/31-05 — this plan is a final cross-check, not just a template fill."
    - "This plan's final pink-gate run is what the phase's ROADMAP success criterion #3 ('An automated pink-hue scan gate exists and passes over all vendored art') is proven against — it must run AFTER every other plan in this phase, hence wave 5."
---

<objective>
Write the CREDITS.md rows and `assets/LICENSES/*.txt` proof files for every Gothicvania-sourced asset vendored in this phase, then run the final full-tree pink-gate regression proof — closing out ART-01's licensing/credit discipline and its automated no-pink gate requirement.

Purpose: this project treats CREDITS.md as a real audit trail (19 existing rows, 19 existing proof files, cross-checked in multiple prior phase corrections) — every new asset must follow that exact discipline. This is also the phase's final consolidation point: by this wave, every biome/character asset from Plans 31-04/31-05 exists on disk, so this is the first point where a true "scan the whole final tree" pink-gate proof is possible.
Output: 5 new LICENSES proof files, new CREDITS.md rows, and a final documented PASS of `bash scripts/check-pink-gate.sh` over the complete post-phase `assets/` tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-asset-bake-style-board-sign-off/31-CONTEXT.md
</context>

<artifacts_produced>
- `CREDITS.md` — new rows appended to the existing `## Assets` table (one row per pack/purpose group, following the existing multi-file-per-row convention)
- `assets/LICENSES/gothicvania-swamp.txt`, `gothicvania-town.txt`, `gothicvania-cemetery.txt`, `gothicvania-church.txt`, `gothicvania-patreon.txt` — 5 new proof files, one per re-fetched OGA pack
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: CREDITS.md rows + LICENSES proof files for all 5 Gothicvania packs</name>
  <files>CREDITS.md, assets/LICENSES/gothicvania-swamp.txt, assets/LICENSES/gothicvania-town.txt, assets/LICENSES/gothicvania-cemetery.txt, assets/LICENSES/gothicvania-church.txt, assets/LICENSES/gothicvania-patreon.txt</files>
  <read_first>
    CREDITS.md (full file — the exact 6-column `| Asset | File | Author | Source | License | Used for |` row shape, and the existing multi-file-per-row precedent, e.g. the `enemy-1.png, enemy-2.png, enemy-3.png` combined row)
    assets/LICENSES/ground.txt (full file — the exact proof-file structure: Asset/Source pack/Author/Source URL/Source file/Tiles used/License/quoted declaration/CC0 full-text link/Processing note/Verification/Vendor-logo line)
    .planning/phases/31-asset-bake-style-board-sign-off/31-01-SUMMARY.md (Plan 31-01 Task 2's recorded, live-reconfirmed license quotes per pack — use these exact quotes, do not re-derive them)
  </read_first>
  <action>
    Add 5 new rows to CREDITS.md's existing `## Assets` table, author `ansimuz` (Luis Zuno) for all 5, License column `CC0` for all 5, grouped by PACK (not by biome) to mirror the existing convention of one row per distinct source pack/purpose:

    1. Row "Gothicvania Swamp (terrain/parallax/player)" — File: `assets/tiles/atlas-swamp.png, assets/parallax/far-swamp.png, assets/parallax/mid-swamp.png, assets/parallax/near-swamp.png, assets/player-swamphunter.png` — Source: `https://opengameart.org/sites/default/files/gothicvania_swamp_files.zip` — Used for: "Levels 1-2 swamp biome terrain atlas, 3-layer parallax, and the player character sprite sheet (ships across all 4 biomes)".
    2. Row "Gothicvania Town (terrain + dusk sky)" — File: `assets/tiles/atlas-town.png, assets/parallax/far-town.png` — Source: `https://opengameart.org/sites/default/files/gothicvania-town-files.zip` — Used for: "Levels 3-4 town biome terrain atlas and dusk-sky parallax far layer (hue-conformed — the pack's native salmon-pink sky was retinted steel-blue per the no-pink rule)".
    3. Row "Gothicvania Patreon Collection (night-town layers, castle env, hell hound)" — File: `assets/parallax/mid-town.png, assets/parallax/near-town.png, assets/parallax/far-castle.png, assets/parallax/mid-castle.png, assets/enemy-hellhound.png` — Source: `https://opengameart.org/sites/default/files/%20gothicvania%20patreon%20collection.zip` — Used for: "Levels 3-4 night-town parallax detail layers, levels 7-8 castle biome parallax (exterior + interior), and the castle biome enemy sprite (static, idle-only this phase)".
    4. Row "Gothicvania Cemetery" — File: `assets/tiles/atlas-cemetery.png, assets/parallax/far-cemetery.png, assets/parallax/mid-cemetery.png, assets/parallax/near-cemetery.png` — Source: `https://opengameart.org/sites/default/files/gothicvania-cemetery-files_1.zip` — Used for: "Levels 5-6 cemetery biome terrain atlas and 3-layer parallax (horizon glow hue-conformed — the pack's native magenta glow was retinted cold-blue per the no-pink rule)".
    5. Row "Gothicvania Church (castle interior accent)" — File: `assets/parallax/near-castle.png` — Source: `https://opengameart.org/sites/default/files/gothicvania%20church%20files.zip` — Used for: "Levels 7-8 castle biome — church interior background accent, folded in as the castle biome's near parallax layer (per the '3-4 biomes covering 8 levels' requirement — Church does not ship as its own biome)".

    Adjust the exact File-column list for rows 1-5 above if Plan 31-04/31-05's actual chosen near/mid substitute files differ from what was projected during planning (e.g. if a different night-town-background layer was picked for Town's near layer) — this task must describe what ACTUALLY got baked, cross-checked against `ls assets/tiles/ assets/parallax/` and the actual filenames on disk, not restate this plan's own projection blindly.

    Add a short new bullet to CREDITS.md's existing `## Notes` section documenting: (a) the Gothicvania pack family is the first fully style-coherent, single-artist collection in this project (superseding the "grab whatever's CC0" approach of Phases 18/20/26); (b) each pack also bundles CC-BY music (Pascal Belisle) that was explicitly NOT vendored — Phase 27 already owns this game's audio, and this content is not vendorable under this project's CC0-only art discipline; (c) the raw pack files live in a gitignored `assets/_gothicvania-src/` scratch directory, re-fetchable any time from the 5 URLs above, never committed.

    Write 5 new `assets/LICENSES/<name>.txt` proof files (`gothicvania-swamp.txt`, `gothicvania-town.txt`, `gothicvania-cemetery.txt`, `gothicvania-church.txt`, `gothicvania-patreon.txt`), each following `ground.txt`'s exact structure: Asset (the file(s) this proof covers) / Source pack name / Author (Luis Zuno @ansimuz) / Source URL (the exact zip URL) / Tiles/frames used (a brief description of what was cropped and why, cross-referencing the actual `build-art-assets.py` function) / License: CC0 / the quoted license declaration (use Plan 31-01 Task 2's live-reconfirmed exact text: "Public domain and free to use on whatever you want, personal or commercial. Credit is not required but appreciated.") / CC0 full-text link / a Processing note describing any hue-conform retint applied (for the Town and Cemetery proof files specifically) / a Verification line stating both the license page and the pack's own bundled `public-license.txt` were confirmed CC0 this phase (cite Plan 31-01's actual re-verification date) / "Vendor logo / brand art: none."
  </action>
  <acceptance_criteria>
    - `grep -c "gothicvania\|ansimuz" CREDITS.md` returns at least 5 (one match per new row, likely more).
    - Every file path listed in the 5 new CREDITS.md rows exists on disk: `for f in $(grep -A2 "Gothicvania" CREDITS.md | grep -oE 'assets/[A-Za-z0-9_./-]+\.png' | sort -u); do test -f "$f" || echo "MISSING: $f"; done` prints nothing.
    - `ls assets/LICENSES/gothicvania-*.txt | wc -l` reports exactly 5.
    - `grep -c "CC-BY\|Pascal Belisle" CREDITS.md` — if present, confirms it appears ONLY in the explanatory Notes bullet (not as a License-column value on any Asset row); no `assets/music/` or `assets/sfx/` file from these packs exists (`find assets -iname "*.mp3" -o -iname "*.ogg"` shows only the pre-existing Kenney-sourced audio files, no new Gothicvania-pack music).
  </acceptance_criteria>
  <verify>
    <automated>ls assets/LICENSES/gothicvania-*.txt | wc -l; grep -c "gothicvania" CREDITS.md</automated>
  </verify>
  <done>CREDITS.md has 5 new rows accurately describing every vendored Gothicvania file, 5 matching LICENSES proof files exist with the live-reconfirmed CC0 quote, and no CC-BY music was vendored.</done>
</task>

<task type="auto">
  <name>Task 2: Final full-tree pink-gate regression proof</name>
  <files>(none created/modified — final verification run over the complete phase output)</files>
  <read_first>
    scripts/check-pink-gate.sh (Plan 31-03's finished gate)
    scripts/lib/pink_scan.py (Plan 31-03's finished module, including the documented allowlist)
  </read_first>
  <action>
    Run `bash scripts/check-pink-gate.sh` over the complete, final `assets/` tree — every file from every plan in this phase now exists (4 terrain atlases, 12 parallax layers, player-swamphunter.png, enemy-hellhound.png, plus every pre-existing asset). Confirm it exits 0 and prints `pink-gate checks: PASS`. This is the definitive GREEN half of the RED-first/GREEN-after-retint proof this phase's Nyquist validation strategy requires (Plan 31-03 proved RED against the raw pre-retint source crops; this is the final proof the SHIPPED, baked, vendored output is clean).

    Cross-check that the two named pink offenders are genuinely gone by re-running the single-file scan directly against the final baked outputs: `python3 scripts/lib/pink_scan.py assets/tiles/atlas-town.png`, `assets/parallax/far-town.png`, `assets/parallax/far-cemetery.png` — all three must report a fraction below 0.08. If `assets/tiles/atlas-cemetery.png` was retinted in Plan 31-04, check it too; if it was judged clean-as-is (shadow-tone false positive, not genuine pink), confirm its recorded fraction still holds and note which path was taken.

    Also confirm the ONE documented allowlist entry (`assets/player-swamphunter.png`) is still the ONLY allowlist entry in `scripts/lib/pink_scan.py` — grep for the `ALLOWLIST` dict/set and count its entries. If Plan 31-04/31-05's actual bake introduced any OTHER file that needs an exception, that is a signal to investigate why (a genuinely new pink asset slipping through vs. another legitimate shadow-tone artifact) rather than to silently add a second entry without the same level of justification Plan 31-03 gave the first one.
  </action>
  <acceptance_criteria>
    - `bash scripts/check-pink-gate.sh` exits 0 and prints `pink-gate checks: PASS`.
    - `python3 scripts/lib/pink_scan.py assets/tiles/atlas-town.png` → `< 0.08`.
    - `python3 scripts/lib/pink_scan.py assets/parallax/far-town.png` → `< 0.08`.
    - `python3 scripts/lib/pink_scan.py assets/parallax/far-cemetery.png` → `< 0.08`.
    - The allowlist in `scripts/lib/pink_scan.py` contains exactly 1 entry, or a new entry with an equally specific, measured justification.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-pink-gate.sh</automated>
  </verify>
  <done>The full pink-gate passes cleanly over the complete post-phase assets/ tree; both named offenders are confirmed retinted; the allowlist remains minimal and justified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Vendored assets/ tree → CREDITS.md/LICENSES documentation | Internal documentation task; no external input. The only "trust" concern is documentation accuracy (does the paperwork match what's actually on disk), addressed by this plan's cross-count acceptance criteria. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-13 | Repudiation | CREDITS.md / LICENSES completeness | medium | mitigate | Acceptance criteria cross-check every File-column path against the real filesystem (`test -f`), not just template-fill the rows; no automated CREDITS-completeness gate exists anywhere in this project (established precedent per RESEARCH.md), so this manual cross-check is the control. |
| T-31-14 | Tampering (license misattribution) | CC-BY music accidentally vendored/credited as CC0 | high | mitigate | Explicit grep check confirms no `.mp3`/`.ogg` file from any Gothicvania pack exists under `assets/music/` or `assets/sfx/`, and that any CC-BY mention in CREDITS.md is confined to the explanatory Notes bullet, never an Asset row's License column. |
</threat_model>

<verification>
1. `ls assets/LICENSES/gothicvania-*.txt | wc -l` → 5.
2. `grep -c "gothicvania" CREDITS.md` → at least 5.
3. `bash scripts/check-pink-gate.sh` → PASS, over the complete final `assets/` tree.
4. `find assets -iname "*.mp3" -o -iname "*.ogg"` → only pre-existing Kenney-sourced files, no new Gothicvania-pack music.
</verification>

<success_criteria>
- Every vendored Gothicvania asset has a matching CREDITS.md row and LICENSES proof file, cross-checked against the real filesystem.
- The full pink-gate passes cleanly over the phase's complete final output — the phase's ART-01 no-pink requirement is proven, not assumed.
- No CC-BY content leaked into the vendored art.
</success_criteria>

<output>
Create `.planning/phases/31-asset-bake-style-board-sign-off/31-06-SUMMARY.md` when done
</output>
