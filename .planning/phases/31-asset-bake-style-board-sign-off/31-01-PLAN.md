---
phase: 31-asset-bake-style-board-sign-off
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - assets/_gothicvania-src/** (gitignored — never committed)
autonomous: true
requirements: [ART-01]

must_haves:
  truths:
    - "All 5 zip URLs from ASSET-SCOUTING.md's pack table resolve and download a valid zip (confirmed via `python3 -c \"import zipfile,sys; assert zipfile.is_zipfile(sys.argv[1])\"` per file, not just an HTTP 200 status)."
    - "None of the 5 zips contain a path-traversal (`../`) entry — verified via `unzip -l` BEFORE extraction, not assumed safe."
    - "Each of the 5 extracted packs' own bundled license file (`public-license.txt`) is located and its text confirms a public-domain/CC0-equivalent declaration by Luis Zuno (ansimuz) — a live re-verification this session, not a re-quote of ASSET-SCOUTING.md's 2026-07-07 finding."
    - "`git status --short` shows nothing under `assets/_gothicvania-src/` as trackable — the multi-MB raw packs never enter git history."
  artifacts:
    - "assets/_gothicvania-src/gothicvania_swamp_files/ (Swamp pack, extracted)"
    - "assets/_gothicvania-src/gothicvania-town-files/ (Town pack, extracted)"
    - "assets/_gothicvania-src/gothicvania-cemetery-files_1/ (Cemetery pack, extracted)"
    - "assets/_gothicvania-src/gothicvania-church-files/ (Church pack, extracted)"
    - "assets/_gothicvania-src/gothicvaniapatreoncollection/ (Patreon Collection pack, extracted)"
    - ".gitignore (new `assets/_gothicvania-src/` entry)"
  key_links:
    - "The exact outer extraction directory names chosen here are NOT arbitrary — Plan 31-02's styleboard.py already contains hardcoded `load()` path literals (e.g. `gothicvania_swamp_files/Gothicvania Swamp files/Evironment/background.png`) that must resolve without FileNotFoundError once its ROOT constant is repointed at `assets/_gothicvania-src/`. Naming the swamp extraction dir anything other than `gothicvania_swamp_files` breaks Plan 31-02 silently."
---

<objective>
Re-fetch the 5 Gothicvania (ansimuz) OGA zip packs — the actual source art files are NOT present anywhere in this repository or filesystem (confirmed by `find / -iname "*gothicvania*"` returning nothing outside this repo's own preview crops/docs) — extract them into a local, gitignored scratch directory, and re-verify each pack's CC0/public-domain license live, before any downstream baking or style-board work touches them.

Purpose: every later task in this phase (style-board regeneration, terrain/parallax/player/enemy baking, the pink-scan gate's RED-first proof) depends on having the real pack files on disk. Without this plan, every one of those tasks fails immediately with a missing-file error.
Output: `assets/_gothicvania-src/` populated with 5 extracted packs (git-ignored, not committed — these are multi-MB raw archives; only baked, cropped output PNGs get vendored into `assets/` proper in later plans), plus a live-reconfirmed CC0 finding for each.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-asset-bake-style-board-sign-off/31-CONTEXT.md
@.planning/research/v6-scouting/ASSET-SCOUTING.md
</context>

<artifacts_produced>
- `assets/_gothicvania-src/gothicvania_swamp_files/Gothicvania Swamp files/**` — extracted Swamp pack
- `assets/_gothicvania-src/gothicvania-town-files/GothicVania-town-files/**` — extracted Town pack
- `assets/_gothicvania-src/gothicvania-cemetery-files_1/gothicvania-cemetery-files/**` — extracted Cemetery pack
- `assets/_gothicvania-src/gothicvania-church-files/gothicvania church files/**` — extracted Church pack
- `assets/_gothicvania-src/gothicvaniapatreoncollection/ gothicvania patreon collection/**` — extracted Patreon Collection pack (note: the pack's own top-level folder name has a literal leading space — do not rename it, `styleboard.py` already hardcodes this exact string)
- `.gitignore` — new entry excluding `assets/_gothicvania-src/`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Gitignore + fetch/extract with integrity and zip-slip checks</name>
  <files>.gitignore, assets/_gothicvania-src/**</files>
  <read_first>
    .gitignore (full file, 12 lines — existing stanza style/comment convention to match)
    .planning/research/v6-scouting/ASSET-SCOUTING.md (the pack table listing all 5 download URLs, lines ~9-16)
    .planning/research/v6-scouting/styleboard.py (lines 1-10 for the `ROOT`/`REPO` constants; the `load()` call path-literal arguments inside `swamp()`/`town()`/`cemetery()`/`castle()`, e.g. `"gothicvania_swamp_files/Gothicvania Swamp files/Evironment/background.png"` — these exact literals dictate the extraction directory names below)
  </read_first>
  <action>
    First, edit `.gitignore`: append a new stanza (matching the file's existing `# comment` + pattern-line style) excluding `assets/_gothicvania-src/` — note in the comment that this holds third-party Gothicvania source-pack downloads (multi-MB zips, re-fetchable any time from ASSET-SCOUTING.md's URL table), that only baked/cropped output PNGs get vendored into `assets/` proper, and that this deliberately differs from the existing `assets/_kenney-src/`/`assets/_opengameart-src/` convention (which IS committed) because of this pack's larger size and because CONTEXT.md/this phase's planning explicitly calls for a local-only scratch convention here.

    Then create `assets/_gothicvania-src/` and fetch each of these 5 exact URLs with `curl -sSL -o <dest> <url>` (add a request timeout, e.g. `--max-time 120`), saving each into its own scratch zip filename inside that directory (e.g. `assets/_gothicvania-src/_downloads/gv-swamp.zip`, or directly at `assets/_gothicvania-src/gv-swamp.zip` — either is fine since the whole tree is gitignored):
    1. `https://opengameart.org/sites/default/files/gothicvania_swamp_files.zip` (Swamp pack, ~436KB)
    2. `https://opengameart.org/sites/default/files/gothicvania-town-files.zip` (Town pack, ~9.1MB — largest of the 5)
    3. `https://opengameart.org/sites/default/files/gothicvania-cemetery-files_1.zip` (Cemetery pack, ~3.6MB)
    4. `https://opengameart.org/sites/default/files/gothicvania%20church%20files.zip` (Church pack, ~733KB — URL-encoded spaces, quote the URL as-is)
    5. `https://opengameart.org/sites/default/files/%20gothicvania%20patreon%20collection.zip` (Patreon Collection, ~4.0MB — note the LEADING `%20` before "gothicvania" in the URL itself)

    For EACH downloaded file, before extracting: (a) confirm it is a real zip, not an HTML error page saved with a `.zip` extension — `python3 -c "import zipfile,sys; exit(0 if zipfile.is_zipfile(sys.argv[1]) else 1)" <file>` (non-zero exit means re-fetch or investigate, do not proceed); (b) confirm no path-traversal entries — `unzip -l <file>` piped to a check for any listed path containing `../` (abort extraction of that specific zip if found, since a malicious/corrupted archive must never be allowed to write outside its target directory — treat this as a real, not theoretical, gate per this plan's threat model).

    Extract each verified zip into these EXACT directory names under `assets/_gothicvania-src/` (dictated by `styleboard.py`'s existing hardcoded `load()` literals, read above — do not improvise different names):
    1. Swamp zip → `assets/_gothicvania-src/gothicvania_swamp_files/` (the zip's own internal top-level folder, "Gothicvania Swamp files", nests one level inside this)
    2. Town zip → `assets/_gothicvania-src/gothicvania-town-files/` (internal top-level folder "GothicVania-town-files" nests inside)
    3. Cemetery zip → `assets/_gothicvania-src/gothicvania-cemetery-files_1/` (internal top-level folder "gothicvania-cemetery-files" nests inside)
    4. Church zip → `assets/_gothicvania-src/gothicvania-church-files/` (internal top-level folder "gothicvania church files" — literal spaces, no hyphens — nests inside; this outer name is not referenced by any existing script, so any clear name works, but use this one for consistency with the other 4)
    5. Patreon Collection zip → `assets/_gothicvania-src/gothicvaniapatreoncollection/` (internal top-level folder is " gothicvania patreon collection" — note the literal LEADING SPACE in that folder's own name, already baked into `styleboard.py`'s hardcoded path strings — do not trim it)

    Use `unzip -q <zip> -d <target-dir>` for extraction (default `unzip` behavior already refuses to write outside the target directory; the pre-check above is defense-in-depth, not the only guard). If any pack's actual internal folder-name casing or nesting differs from what is described above once you inspect it (this description is based on a live re-fetch performed during planning, 2026-07-10, but re-confirm — do not assume it is stale), note the actual structure for Plan 31-02's benefit rather than silently forcing a mismatched rename.
  </action>
  <acceptance_criteria>
    - `find assets/_gothicvania-src -mindepth 1 -maxdepth 1 -type d | wc -l` reports exactly 5 (one directory per pack).
    - `find assets/_gothicvania-src -iname "background.png" | wc -l` reports at least 3 (swamp/town/cemetery each ship a `background.png` under their `Evironment`/`environment`/`Environment` folder — confirms real extraction, not empty/failed dirs).
    - `git check-ignore -q assets/_gothicvania-src/gothicvania_swamp_files` exits 0 (the directory is genuinely ignored, not merely absent from `git add`).
    - Re-running `python3 -c "import zipfile,sys; assert zipfile.is_zipfile(sys.argv[1])"` against every downloaded zip file exits 0 for all 5.
  </acceptance_criteria>
  <verify>
    <automated>find assets/_gothicvania-src -mindepth 1 -maxdepth 1 -type d | wc -l; git check-ignore -q assets/_gothicvania-src/gothicvania_swamp_files && echo "IGNORED-OK"</automated>
  </verify>
  <done>All 5 packs are downloaded, verified as real zips, checked for zip-slip entries, and extracted into the exact directory names `styleboard.py`'s existing code already expects; `.gitignore` excludes the whole tree from git.</done>
</task>

<task type="auto">
  <name>Task 2: Live CC0 license re-verification per pack</name>
  <files>(none created/modified — read-only verification of Task 1's fetched files)</files>
  <read_first>
    assets/_gothicvania-src/gothicvania_swamp_files/Gothicvania Swamp files/public-license.txt (Task 1's output — the exact bundled license text to quote)
    assets/LICENSES/ground.txt (existing proof-file format this phase's later credits/licenses plan will replicate — read now so the quotes you gather here are recorded in a form that slots directly into that format later)
  </read_first>
  <action>
    For each of the 5 extracted packs, locate its bundled license file (`find <pack-dir> -iname "*licen*"` — every pack ships one named exactly `public-license.txt` at its own top level, confirmed during planning) and read it in full. Confirm each contains the public-domain declaration by Luis Zuno (ansimuz): the phrase "Public domain and free to use on whatever you want, personal or commercial" (verified present, word-for-word, in all 5 packs' license files during this phase's planning session, 2026-07-10) — re-confirm it is STILL there today, not merely trusted from that prior read. Note that 3 of the 5 packs (Swamp, Town, Cemetery) also bundle a SEPARATE "Music License" section crediting Pascal Belisle's music as CC-BY ("free to use... as long as you give appropriate credit") — this confirms ASSET-SCOUTING.md's note #2: the bundled music is CC-BY, not CC0, and must never be vendored (Plan 31-06's credits work depends on this exact distinction being re-confirmed here, not assumed).

    Do not attempt to re-verify each pack's OpenGameArt.org page live in this task (no additional network fetch beyond what Task 1 already did) — the bundled `public-license.txt` re-read above, performed against the pack files this session's Task 1 just fetched, IS the live re-verification CONTEXT.md/RESEARCH.md calls for (RESEARCH.md's Open Question #1 asked whether the packs "still say CC0" as of today; this task answers that with a real, current-session file read, not a re-quote of the 2026-07-07 scouting session).

    Record the 5 quoted license excerpts (pack name, exact quoted CC0/public-domain sentence, and the separate CC-BY music note where present) directly in this plan's own execution notes / the phase SUMMARY.md — Plan 31-06 will copy these exact quotes into the `assets/LICENSES/gothicvania-*.txt` proof files rather than re-reading the source files a second time.
  </action>
  <acceptance_criteria>
    - `grep -rl "Public domain and free to use" assets/_gothicvania-src/*/*/public-license.txt` (or the equivalent nested path per pack) matches all 5 packs' license files.
    - `grep -rl "Music License" assets/_gothicvania-src` matches exactly the 3 packs known to bundle music (Swamp, Town, Cemetery) — Church and Patreon Collection's license files do not carry this section (Church's mentions only "Demo Music" credit, Patreon Collection's has none), confirming no NEW CC-BY-music surprise appeared.
    - No `assets/` (non-`_gothicvania-src`) path yet contains any `.mp3`/`.ogg`/`.wav` from these packs — the music-exclusion decision has not yet been (and must never be) violated even accidentally at this stage.
  </acceptance_criteria>
  <verify>
    <automated>grep -rlc "Public domain and free to use" assets/_gothicvania-src --include="public-license.txt" | wc -l</automated>
  </verify>
  <done>All 5 packs' bundled license files are read this session and reconfirmed CC0/public-domain (with the 3-pack CC-BY-music carve-out explicitly re-noted), ready for Plan 31-06 to quote verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Internet (opengameart.org) → local filesystem | This plan's ONLY external-input surface: 5 third-party zip archives downloaded and extracted onto disk. Everything downstream in this phase (and Phases 32/33/35) consumes these files as trusted local input. |

This phase has zero `src/` runtime-code changes and no browser/server surface — the only real trust boundary in the entire phase is this plan's download/extract step.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | Zip extraction (`unzip -d <target>`) | high | mitigate | Every zip's file listing (`unzip -l`) is scanned for `../` path-traversal entries BEFORE extraction; `unzip`'s own default behavior additionally refuses to write outside the target directory. Both checks are real (not theoretical) per Task 1's acceptance criteria. |
| T-31-02 | Tampering (supply-chain integrity) | Zip download (`curl` from opengameart.org) | medium | mitigate | Each downloaded file is verified as a genuine zip (`zipfile.is_zipfile()`) before extraction — catches a redirected/expired URL silently serving an HTML 404 page saved with a `.zip` extension, which would otherwise fail confusingly deep in a later bake step. |
| T-31-03 | Tampering (license misattribution) | CC0 license re-verification | high | mitigate | Task 2 re-reads each pack's own bundled `public-license.txt` live this session rather than trusting a 3-day-old research quote; the CC-BY-music carve-out is explicitly re-confirmed so no CC-BY content is later mistaken for CC0 and vendored. |
| T-31-04 | Information Disclosure | Raw pack files landing in `assets/_gothicvania-src/` | low | accept | Excluded from git via `.gitignore`; contains only third-party public-domain art assets, no secrets/credentials — no disclosure risk even if a copy leaked. |
</threat_model>

<verification>
1. `find assets/_gothicvania-src -mindepth 1 -maxdepth 1 -type d | wc -l` → 5.
2. `git check-ignore -q assets/_gothicvania-src/gothicvania_swamp_files` → exit 0.
3. `grep -rlc "Public domain and free to use" assets/_gothicvania-src --include="public-license.txt"` → 5 matches.
4. `git status --short` shows no files under `assets/_gothicvania-src/` as staged/trackable.
</verification>

<success_criteria>
- All 5 Gothicvania packs are present locally under `assets/_gothicvania-src/`, gitignored, ready for Plan 31-02's style-board regeneration and Plan 31-04's atlas baking to consume without any FileNotFoundError.
- Each pack's CC0/public-domain status is reconfirmed live this session (not carried forward blind from the 2026-07-07 scouting session), with the CC-BY-music carve-out explicitly re-verified.
</success_criteria>

<output>
Create `.planning/phases/31-asset-bake-style-board-sign-off/31-01-SUMMARY.md` when done
</output>
