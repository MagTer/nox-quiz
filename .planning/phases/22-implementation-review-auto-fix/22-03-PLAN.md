---
phase: 22-implementation-review-auto-fix
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - src/scenes/game.js
  - src/scenes/title.js
  - src/scenes/select.js
  - src/ui/hud.js
  - src/main.js
  - src/index.html
  - .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "Each of the 6 Cluster B files (game.js, title.js, select.js, hud.js, main.js, index.html) has an explicit verdict in 22-FINDINGS.md"
    - "game.js's two onSceneLeave sweeps are verified to fire and to cover every global controller and tween handle currently registered — or a leak finding is recorded and fixed"
    - "The select.js single-row overflow (IN-03) is recorded as an inventory note deferred to Phase 25 — NOT fixed (the 2x4 grid is LVL-04 scope)"
    - "The main.js/index.html scale transform and file:// guard are verified unregressed — the transform is load-bearing for mouse hit-testing and is not 'simplified'"
    - "After all Cluster B commits: 4 static gates green and browser-boot exits 0"
  artifacts:
    - path: ".planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md"
      provides: "Cluster B numbered findings + 6 verdict rows + Cluster B regression record"
      contains: "Cluster B regression: PASS"
  key_links:
    - from: ".planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md"
      to: "scripts/browser-boot.mjs"
      via: "post-cluster boot run + boot-path screenshots as visual evidence"
      pattern: "Cluster B regression"
---

<objective>
Review Cluster B — the three scenes, HUD, and the boot shell — with static evidence plus boot screenshots, landing only objectively-wrong fixes (FIX-01).

This cluster is lower-risk than the mechanics (per 22-RESEARCH.md) but contains the densest file (game.js, 313 lines: checkpoint promotion, respawn seam, goal fire-once, onHide save canceller, two separate onSceneLeave sweeps) and two known "do not touch" landmines: the load-bearing CSS scale transform in main.js/index.html and the deliberately-deferred select.js tile overflow.

Purpose: scene/shell lifecycle leaks are exactly the class that multiplies when content doubles in Phases 24–25.
Output: atomic fix commits (0 or more), Cluster B findings + 6 verdict rows, cluster regression proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-implementation-review-auto-fix/22-CONTEXT.md
@.planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md
@.planning/phases/22-implementation-review-auto-fix/22-PATTERNS.md
@.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
</context>

## Artifacts this phase produces

- New 22-FINDINGS.md content: Cluster B numbered findings, 6 verdict rows, "Cluster B regression: PASS" record, boot-path screenshots (scratchpad paths referenced from findings)
- Conditional: additions to the game.js onSceneLeave sweep IF a controller/tween is found uncovered (closure-local handles only, per the established sweep pattern at game.js ~lines 295/307)
- No new exported symbols, CLI flags, or config tokens

<tasks>

<task type="auto">
  <name>Task 1: Review game.js lifecycle sweeps and title.js input seam</name>
  <files>src/scenes/game.js, src/scenes/title.js, .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md</files>
  <read_first>
    - src/scenes/game.js (313 lines — enumerate EVERY controller and tween handle created in the scene body, then check each against the two onSceneLeave registrations at ~lines 295 and 307: hideCtrl cancel + the tween/controller sweep including clearTransitionTween and _fxScaleTween)
    - src/scenes/title.js (Space-to-start handler; press edge is scene-scoped and auto-cleared on go — verify)
    - src/player.js (jump-buffer write and its paused guard — the consumer of title→game input-edge concerns; READ for context, modified only if the defect is in game.js/title.js wiring)
    - .planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md (Cluster B suspect rows; Bug-Pattern Checklist items 5, 6, 9)
    - .planning/phases/22-implementation-review-auto-fix/22-PATTERNS.md (Shared Patterns — global-controller cancel on scene leave; atomic fix commit format)
  </read_first>
  <action>
    game.js: build an explicit inventory (in the finding entry) of every global/app-bus controller (onHide etc.) and every tween handle created in the scene, and map each to its cancel path: covered by sweep 1, covered by sweep 2, self-cleaning via onEnd-destroy, or UNCOVERED. Confirm both onSceneLeave registrations actually fire (bug-pattern #5: scene-registered handlers are auto-cleared by go(), global ones are not — verify which registration form each sweep uses). Any UNCOVERED handle is an objectively-wrong leak (auto-fix class): add it to the appropriate existing sweep with a comment citing the finding ID — do not create a new teardown mechanism. Also verify: checkpoint promotion and respawn seam (respawn never re-fires goal or duplicates controllers), goal fire-once guard, and the NAV-03 tween-deferred transition (bug-pattern #9 — no synchronous go() in the same tick as celebration draws).

    title.js: verify the Space-to-start press edge cannot leak into game-scene jump buffering (the handler must be scene-scoped so go() clears it; the expectation from research is clean — confirm and record, source-read tier is sufficient for this unconditional registration-form check).

    Record one numbered finding per hypothesis with verdicts; fixes are atomic fix(22-03) commits with node --check + the 4 static gates after each.
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && bash scripts/check-progress.sh && F=.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md && [ "$(grep -c 'src/scenes/game.js' "$F")" -ge 2 ] && echo GAME-TITLE-OK</automated>
  </verify>
  <acceptance_criteria>
    - 22-FINDINGS.md contains the game.js controller/tween inventory finding: every handle listed with its named cancel path (sweep 1 / sweep 2 / self-clean / fixed-in-this-plan), zero left unclassified
    - 22-FINDINGS.md contains a title.js verdict finding stating the registration form observed (scene-scoped vs global) and why it does or does not leak
    - Any fix commit is atomic, cites its finding ID in an in-code comment, and all 4 static gates pass after it
    - check-import-safety.sh passes (no engine globals introduced at module top level)
  </acceptance_criteria>
  <done>game.js lifecycle coverage proven complete (or repaired), title.js input seam verdict recorded, gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Review select.js, hud.js, boot shell; cluster regression + verdicts</name>
  <files>src/scenes/select.js, src/ui/hud.js, src/main.js, src/index.html, .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md</files>
  <read_first>
    - src/scenes/select.js (tile layout: START_X 120 + i*120 — at 4 tiles the last right edge is 528px inside the 640px canvas; the ~5-tile overflow is IN-03, deferred)
    - src/ui/hud.js (one-way contract enforced by check-progress.sh assertion 8; flash self-clean via tween)
    - src/main.js and src/index.html (the CSS scale transform is load-bearing for mouse hit-testing — documented past bug found via Phase 14 browser-boot; the file:// guard is inline pre-module)
    - .planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md (Cluster B suspect rows; IN-03 provenance)
  </read_first>
  <action>
    select.js: verify current 4-tile layout renders fully on-canvas (arithmetic + a boot-run screenshot of the select screen as visual evidence). Record the IN-03 overflow as a finding with verdict deferred-to-phase-25 (LVL-04 owns the 2x4 grid) — do NOT restructure the layout now. Verify locked/unlocked/cleared tile states derive from progress correctly (source read + the boot screenshot).

    hud.js: verify the one-way contract (HUD reads progress, never writes) and the flash tween self-cleans; check-progress.sh assertion 8 is the oracle — record verdict.

    main.js / index.html: verify the scale transform (display 960x540, internal 640x360) and canvas sizing attributes are unregressed and the file:// guard still short-circuits before module load. Explicitly record in the finding that the transform approach is intentional and must not be replaced with width/height styling (the documented hit-testing bug). No "simplification" commits.

    Then run the Cluster B regression: `node scripts/browser-boot.mjs` (must exit 0); capture title, select, and in-level HUD screenshots from the boot run (scratchpad paths, referenced in findings) as the visual-evidence tier for this cluster's claims. Record `Cluster B regression: PASS` and set final verdicts for the 6 Cluster B rows (game.js, title.js, select.js, hud.js, main.js, index.html). Commit docs(22-03).
  </action>
  <verify>
    <automated>node scripts/browser-boot.mjs && bash scripts/check-progress.sh && F=.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md && [ "$(grep -c 'Cluster B regression: PASS' "$F")" -eq 1 ] && [ "$(grep -c '| pending |' "$F")" -eq 12 ] && echo CLUSTER-B-OK</automated>
  </verify>
  <acceptance_criteria>
    - browser-boot.mjs exits 0 on post-fix HEAD (title → select → all 4 levels, zero uncaught errors)
    - IN-03 finding recorded with verdict deferred-to-phase-25 and the 4-tile on-canvas arithmetic shown; no layout restructuring diff exists in this plan
    - main.js/index.html verdict cites the transform-is-load-bearing rationale; `git diff` for these two files in this plan is empty unless an objectively-wrong defect was found and documented
    - Visual claims reference screenshot paths captured this run (not prior-phase screenshots)
    - Per-Entity Verdict Table has exactly 12 rows still pending (24 minus Clusters A+B)
    - "Cluster B regression: PASS" appears exactly once in 22-FINDINGS.md
  </acceptance_criteria>
  <done>All 6 Cluster B verdicts final with visual evidence, boot green, findings committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → scene state | select.js/hud.js render from the validated progress save; must not gain write paths (one-way contract) |
| boot shell → browser | index.html/main.js inline guard code runs pre-module; changes here affect the whole app's integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | progress save consumers (hud.js/select.js one-way contract) | high | mitigate | check-progress.sh (assertion 8: HUD one-way) runs after every commit; no write paths to progress added |
| T-22-02 | Information Disclosure | screenshot evidence runs (embedded servers) | low | accept | Only the existing hardened committed scripts are run in this plan (loopback-bound) |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- All 4 static gates green after every commit; browser-boot green at cluster end
- 6 Cluster B verdict rows final with screenshot evidence for visual claims
- IN-03 deferred (not fixed); transform untouched (or defect-fix documented)
</verification>

<success_criteria>
- Scenes/shell reviewed with lifecycle-leak focus before content doubles; deliberate deferrals (IN-03) recorded instead of silently fixed out of Phase 25's scope
</success_criteria>

<output>
Create `.planning/phases/22-implementation-review-auto-fix/22-03-SUMMARY.md` when done.
</output>
