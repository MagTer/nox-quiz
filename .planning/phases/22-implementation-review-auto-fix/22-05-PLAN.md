---
phase: 22-implementation-review-auto-fix
plan: 05
type: execute
wave: 5
depends_on: ["22-01", "22-02", "22-03", "22-04"]
files_modified:
  - .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
  - src/ui/challenge.js
  - src/config.js
  - src/mechanics/collect.js
  - src/levels/build.js
autonomous: false
requirements: [FIX-01, FIX-02]

must_haves:
  truths:
    - "Every escalation candidate accumulated in 22-FINDINGS.md is presented to the user as ONE batched approve/reject round — no design change was implemented before its recorded approval, and none is implemented silently (FIX-02)"
    - "Approved escalations are implemented as atomic commits AFTER the recorded decisions; rejected items carry a rejected-left-as-designed disposition"
    - "The full suite (4 static gates + smoke + browser-boot + 16-encounter audit) is green on final HEAD with zero regressions vs the Plan 22-01 baseline: every green assertion still green, the 10 resolved audit rows identical, the same 6 rows unreached"
    - "The Per-Entity Verdict Table is complete: all 24 files carry final verdicts, and LOCKED surfaces (src/math/, lib/kaplay.mjs) plus the four level descriptors are diff-proven untouched since baseline"
  artifacts:
    - path: ".planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md"
      provides: "Recorded FIX-02 decisions, implementation dispositions, completed Post-Fix Regression section"
      contains: "FINAL regression: PASS"
  key_links:
    - from: ".planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md"
      to: "git history"
      via: "decision-before-implementation ordering (approval docs commit precedes every escalation fix commit)"
      pattern: "Decision: (APPROVED|REJECTED)"
    - from: ".planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md"
      to: "scripts/audit-phase21-mechanics.mjs"
      via: "final 16-row diff against the baseline table"
      pattern: "FINAL regression"
---

<objective>
Close the phase: run the single batched FIX-02 approve/reject round, implement only what was approved, and prove zero regressions against the Plan 22-01 baseline with the full suite.

CONTEXT-locked escalation format: batched at end of review as ONE approve/reject decision round — not per-finding interrupts. Roadmap success criterion 3: bigger design changes are presented as explicit approve/reject decisions — none implemented silently.

Purpose: FIX-02 satisfied with an auditable decision trail; FIX-01's zero-regression claim closed with interactive proof (v4.1 standard: no phase closes on greps alone).
Output: recorded decisions in 22-FINDINGS.md, 0+ approved-escalation fix commits, completed Post-Fix Regression section, phase ready for /gsd-verify-work.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-implementation-review-auto-fix/22-CONTEXT.md
@.planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md
@.planning/phases/22-implementation-review-auto-fix/22-PATTERNS.md
@.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
</context>

## Artifacts this phase produces

- Decision-dependent (created ONLY if the user approves the corresponding escalation item in Task 1):
  - `CONFIG.GATE.BOX_W`, `CONFIG.GATE.BOX_H`, `CONFIG.GATE.GAP` tokens in `src/config.js` + their use in `src/ui/challenge.js` (escalation: answer-box magic-number extraction; analog: 844cd08 constant lift + existing CONFIG.GATE naming)
  - glyph-clarity affordance for door/gate/enemy markers (escalation item 1; exact symbols determined by the approved option — likely label/hint text in `src/levels/build.js` and/or the mechanic wiring, using existing config tokens only)
  - same-time-open prevention in `src/ui/challenge.js` (escalation item 2; recommendation is leave-as-designed — implemented only on explicit approval)
  - collect.js multi-zone hardening (only if Plan 22-02 escalated it instead of auto-fixing)
- Final 22-FINDINGS.md content: Decision lines per escalation entry, Post-Fix Regression table, "FINAL regression: PASS"
- No unconditional new symbols

<!-- planner-discipline-allow: PENDING-DECISION -->
<!-- planner-discipline-allow: | pending | -->

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: FIX-02 batched escalation round — one approve/reject pass over all candidates</name>
  <files>.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md</files>
  <read_first>
    - .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md (the Escalation Candidates section — every entry still carrying PENDING-DECISION status is the batch)
    - .planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md (Escalation Candidates section — per-item context and recommendations)
  </read_first>
  <decision>Approve or reject each escalated design change from the Phase 22 review — one batched round covering every PENDING-DECISION entry in 22-FINDINGS.md</decision>
  <context>These items were deliberately NOT auto-fixed because they change game feel, mechanic semantics, controls, or visual identity, or they cross the existing-config-tokens-only polish rule (CONTEXT-locked auto-fix boundary). Expected batch (at minimum, as accumulated by Plans 22-01..04): (1) door/gate/enemy glyph clarity — live kid report that the X/?/! markers aren't self-evident; any fix changes UX/visual identity; (2) challenge same-time-open prevention — residual New Finding 4 tech debt; the hide/restore compromise was deliberate (refusing to open would strand a frozen player); recommendation: leave as designed; (3) answer-box layout constants extraction to CONFIG.GATE — zero-behavior-change but creates new config tokens; (4) collect.js multi-zone hardening, only if Plan 22-02 escalated it. Present each entry with its Summary, Why-escalated, evidence refs, and Recommendation from 22-FINDINGS.md.</context>
  <action>
    Present ALL entries whose status line still reads as undecided in the Escalation Candidates section — as a single batch with per-item recommendations. For each item record the user's verdict in 22-FINDINGS.md: replace the pending status line with `Decision: APPROVED — <date>` or `Decision: REJECTED — <date>` plus a one-line rationale, and set the matching Per-Entity Verdict Table rows/finding dispositions accordingly (rejected items become "rejected — left as designed" with the recommendation reasoning preserved). Commit the recorded decisions as docs(22-05) BEFORE any implementation work — this commit is the FIX-02 audit anchor proving no fix predates its approval.
  </action>
  <options>
    <option id="per-item">
      <name>Per-item approve/reject (expected)</name>
      <pros>Each design change gets an explicit, recorded decision — FIX-02's letter and spirit</pros>
      <cons>None — this is the CONTEXT-locked format</cons>
    </option>
    <option id="reject-all">
      <name>Reject all (ship the review as-is)</name>
      <pros>Fastest close; all items are non-blocking observations with working current behavior</pros>
      <cons>Known papercuts (glyph clarity) carry into v5.0 content phases</cons>
    </option>
  </options>
  <resume-signal>Reply with a verdict per item, e.g. "1: approve, 2: reject, 3: approve, 4: reject" (or "reject all"). Optionally add constraints per approved item.</resume-signal>
  <verify>
    <automated>F=.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md && [ "$(grep -c 'PENDING-DECISION' "$F")" -eq 0 ] && [ "$(grep -Ec 'Decision: (APPROVED|REJECTED)' "$F")" -ge 3 ] && echo DECISIONS-RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - Zero PENDING-DECISION status lines remain in 22-FINDINGS.md
    - At least 3 Decision lines exist (the pre-identified glyph-clarity, same-time-open, and answer-box entries at minimum), each with a date and rationale
    - The decisions docs(22-05) commit exists and contains ONLY 22-FINDINGS.md changes — no src/ diff rides along with the decision recording
  </acceptance_criteria>
  <done>Every escalation entry carries a recorded APPROVED/REJECTED decision committed before any implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement approved escalations only — atomic commits after recorded approval</name>
  <files>src/ui/challenge.js, src/config.js, src/mechanics/collect.js, src/levels/build.js, .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md</files>
  <read_first>
    - .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md (the recorded decisions — the ONLY authorization source for this task)
    - .planning/phases/22-implementation-review-auto-fix/22-PATTERNS.md (sections 5 and 6 — the 844cd08 constant-lift analog and CONFIG.GATE naming convention for the extraction item; atomic fix commit format)
    - src/config.js (existing CONFIG.GATE token naming to extend, if extraction approved)
    - src/ui/challenge.js (current inline constants and their use sites, if extraction approved)
  </read_first>
  <action>
    For each APPROVED item (and ONLY approved items — a rejected or undecided item implemented here is a FIX-02 violation): implement per its entry's approved scope and any user constraints from the resume signal, one atomic fix(22-05) commit per item, in-code comments citing the finding ID and decision date. Known implementation shapes: answer-box extraction follows the 844cd08 lift into CONFIG.GATE with call sites updated and zero behavior change; glyph-clarity uses existing config tokens only (no new visual systems — if the approved option requires new tokens, that requirement must have been explicit in the approved entry); same-time-open prevention (if approved against recommendation) must not strand a frozen player — the f58f3fb commit message documents the soft-lock hazard any implementation must solve.

    If nothing was approved, this task is a no-op except updating dispositions.

    Per-fix cadence: node --check on touched files + the 4 static gates after every commit. Update each implemented entry's disposition in 22-FINDINGS.md with the fix commit SHA (docs commit).
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && bash scripts/check-progress.sh && node scripts/smoke-progress.mjs && echo APPROVED-IMPL-OK</automated>
  </verify>
  <acceptance_criteria>
    - git log inspection: every fix(22-05) commit touching an escalated item's files comes AFTER the decisions docs(22-05) commit in history; no escalation implementation exists for a REJECTED or undecided entry
    - Each implemented item's finding disposition cites its fix commit SHA
    - All 4 static gates + smoke green on HEAD after the last commit
    - No commit in this task touches src/math/, lib/kaplay.mjs, or the four level descriptor data files
  </acceptance_criteria>
  <done>Approved items implemented atomically post-approval; rejected items dispositioned; gates green.</done>
</task>

<task type="auto">
  <name>Task 3: Final full-suite regression vs baseline and findings close-out</name>
  <files>.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md</files>
  <read_first>
    - .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md (baseline section: the commit anchor line, gate output shapes, and the 16-row audit table — the comparison target for everything below)
    - .planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md ("Zero-regressions definition" and the Regression Baseline Commands table)
  </read_first>
  <action>
    Run the complete suite on final HEAD in this order and record verbatim outcomes in the `## Post-Fix Regression` section: `bash scripts/check-gate.sh`, `bash scripts/check-import-safety.sh`, `bash scripts/check-safety.sh`, `bash scripts/check-progress.sh`, `node scripts/smoke-progress.mjs`, `node scripts/browser-boot.mjs` (must exit 0), `node scripts/audit-phase21-mechanics.mjs` (compare rows, not exit code). Build the baseline-vs-final table: all five static/smoke green lines identical; browser-boot per-level OK lines present; the 16-row audit diff shows the 10 baseline-resolved rows byte-identical in Triggered/Resolved (reachedX jitter excepted) and the same 6 rows unreached. Any deviation is a regression to fix (new atomic commit + re-run) or, if a documented Plan 22-02 script extension explains it, a cited explanation — never a silently absorbed diff.

    LOCKED-surface proof: extract the baseline SHA from the Baseline commit line and verify `git diff --quiet <sha>..HEAD` passes for src/math, lib/kaplay.mjs, and each of the four level descriptor data files individually.

    Close out 22-FINDINGS.md: confirm the Per-Entity Verdict Table has all 24 final verdicts (no initialized-state cells), every numbered finding has a current dated disposition, and append the line `FINAL regression: PASS` to the Post-Fix Regression section only when the diff is clean. Commit docs(22-05): phase close-out.
  </action>
  <verify>
    <automated>F=.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md && BASE=$(sed -n 's/^Baseline commit: //p' "$F" | head -1) && git diff --quiet "$BASE"..HEAD -- src/math lib/kaplay.mjs src/levels/level-01.js src/levels/level-02.js src/levels/level-03.js src/levels/level-04.js && bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && bash scripts/check-progress.sh && node scripts/smoke-progress.mjs && node scripts/browser-boot.mjs && [ "$(grep -c 'FINAL regression: PASS' "$F")" -eq 1 ] && [ "$(grep -c '| pending |' "$F")" -eq 0 ] && [ "$(grep -c 'PENDING-DECISION' "$F")" -eq 0 ] && echo PHASE-22-CLOSED</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green on final HEAD; the audit's 16-row final table is recorded next to the baseline with an explicit row-by-row diff statement (10 resolved rows identical, same 6 unreached — the roadmap's zero-regressions criterion)
    - `git diff --quiet <baseline-sha>..HEAD -- src/math lib/kaplay.mjs <four level descriptors>` exits 0 (LOCKED surfaces and calibration targets untouched across the whole phase)
    - "FINAL regression: PASS" appears exactly once; zero undecided escalations; zero unreviewed verdict rows
    - The Post-Fix Regression section quotes this run's outputs (not baseline copies)
  </acceptance_criteria>
  <done>Zero regressions proven interactively against the baseline; 22-FINDINGS.md is a complete, closed FIX-01/FIX-02 evidence artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user decision → implementation | FIX-02's integrity boundary: no design change crosses into code without a recorded approval |
| final HEAD → v5.0 content phases | Phases 23–25 build on this tree; unproven regressions here compound downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-08 | Repudiation | escalation decisions | high | mitigate | Decisions committed in a docs-only commit BEFORE any implementation; git-log ordering asserted in Task 2 acceptance; per-item dated Decision lines in 22-FINDINGS.md |
| T-22-01 | Tampering | progress.js protections surviving approved fixes | high | mitigate | check-progress.sh + smoke-progress.mjs after every Task 2 commit; final full-suite run |
| T-22-03 | Tampering | challenge.js rendering if extraction/prevention approved | medium | mitigate | check-gate.sh (DOM-sink + API assertions) after every commit |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- One batched decision round; decisions recorded and committed before any implementation
- Full suite green; audit row-diff clean vs baseline; LOCKED surfaces diff-proven untouched
- 22-FINDINGS.md complete: 24 final verdicts, all findings dispositioned, FINAL regression: PASS
</verification>

<success_criteria>
- FIX-02 satisfied: every bigger design change explicitly approved or rejected, none silent, ordering auditable in git
- FIX-01 closed: entity review complete with zero regressions proven by the same scripts that produced the baseline
- Roadmap success criteria 1–4 all evidenced in 22-FINDINGS.md, ready for /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/22-implementation-review-auto-fix/22-05-SUMMARY.md` when done.
</output>
