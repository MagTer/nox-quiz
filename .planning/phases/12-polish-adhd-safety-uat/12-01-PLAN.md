---
phase: 12-polish-adhd-safety-uat
plan: 01
type: execute
wave: 1
depends_on: ["12-00"]
files_modified:
  - src/fx.js
  - src/player.js
  - src/scenes/game.js
autonomous: true
requirements: [JUICE-01, JUICE-02, JUICE-03]
must_haves:
  truths:
    - "Jumping makes the player stretch and landing makes the player squash, then snap back to neutral (JUICE-01)"
    - "Landing kicks up a few short-lived grey dust particles that rise, fade, and self-destroy (JUICE-01)"
    - "Collecting a coin spawns a quick neon-green scale-pop+fade at the coin's spot, then the marker self-destroys (JUICE-02)"
    - "Clearing the math gate fires a brief, non-strobing celebratory burst layered on the existing LEVEL CLEAR banner + level-up flash (JUICE-03)"
    - "Every effect self-cleans via tween().onEnd(destroy) with a 'fx' tag — no timer, no leak across replay"
  artifacts:
    - path: "src/fx.js"
      provides: "squash/dust/pop/clearBurst pure engine-side effect helpers (globals used only inside fn bodies)"
      exports: ["squash", "dust", "pop", "clearBurst"]
    - path: "src/player.js"
      provides: "scale() comp + stretch-on-jump + squash/dust on body().onGround land hook"
      contains: "onGround"
    - path: "src/scenes/game.js"
      provides: "fx.pop on coin collect + fx.clearBurst on onClear"
      contains: "clearBurst"
  key_links:
    - from: "src/player.js"
      to: "src/fx.js"
      via: "import { fx } and call fx.squash/fx.dust at jump + onGround hooks"
      pattern: "fx\\.(squash|dust)"
    - from: "src/scenes/game.js"
      to: "src/fx.js"
      via: "import fx and call fx.pop(c.pos.clone()) in coin onCollide + fx.clearBurst() in onClear"
      pattern: "fx\\.(pop|clearBurst)"
---

<objective>
Build the single src/fx.js effect helper (squash/dust/pop/clearBurst) and wire it into the player and scene so jumping, landing, collecting a coin, and clearing the level all get satisfying-but-subtle visual juice — JUICE-01, JUICE-02, JUICE-03. Every effect is the SAME proven in-repo primitive: an `add([... "fx"])` transient that animates with `tween(...)` and removes itself on `.onEnd(() => destroy(obj))`. No new animation mechanism per effect; no timer, no leak.

Purpose: make the game "read and feel like a real game" while obeying the locked juice language — subtle, brief, NON-STROBING, dark-grunge grey dust + neon-green pop/burst, no pink, and critically NO scheduler (no setTimeout/setInterval/wait/lifespan/loop). JUICE-03 LAYERS on the existing clear moment (the gate's "LEVEL CLEAR" banner + hud.flashLevelUp()) — it does not replace it.
Output: src/fx.js (new); src/player.js gains scale(1) + stretch-on-jump + squash/dust on the body().onGround land hook; src/scenes/game.js fires fx.pop on coin collect and fx.clearBurst in onClear.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-polish-adhd-safety-uat/12-RESEARCH.md
@.planning/phases/12-polish-adhd-safety-uat/12-PATTERNS.md
@src/ui/hud.js
@src/player.js
@src/scenes/game.js
@src/config.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/fx.js — squash/dust/pop/clearBurst self-cleaning effect helpers</name>
  <files>src/fx.js</files>
  <read_first>
    - src/ui/hud.js lines 105-124 (flashLevelUp) — the EXACT self-cleaning idiom: add([... tagged]) → tween(from,to,MS/1000,(v)=>setter,easings.easeOutQuad).onEnd(()=>destroy(obj)); and lines 33-36 for ACCENT_GREEN = [0x00,0xff,0x88].
    - src/scenes/game.js line 107 — the short tween form `tween(0.2, 1, 0.18, (v) => (player.opacity = v), easings.easeOutQuad)`.
    - 12-PATTERNS.md lines 21-63 (src/fx.js assignment: a727c13 top-level-globals ban, the one idiom, tagged-transient discipline, exported fn list, anti-patterns) and 12-RESEARCH.md lines 177-245 (Patterns 1-4) + lines 384-408 (the dust Code Example).
    - src/config.js (CONFIG.FX from Plan 00) — read SQUASH_*/STRETCH_*/DUST_*/POP_*/BURST_MS.
  </read_first>
  <action>
    Create src/fx.js. The ONLY top-level statement is `import { CONFIG } from "./config.js";` (fx.js lives at src/, so `./config.js`, NOT `../config.js`). HARD a727c13 LESSON: every Kaplay global (add, tween, rect, pos, color, opacity, anchor, z, destroy, easings, vec2) is used ONLY inside exported function bodies — NO Kaplay symbol and NO `typeof <global>` guard at module top level (imports are hoisted and run before kaplay() installs globals; a top-level engine use throws at load and blanks the game). Export four pure functions, each building transients tagged "fx" that self-clean on `.onEnd(() => destroy(obj))`:
    - `squash(obj)`: snap obj to a squashed scale via `obj.scaleTo(CONFIG.FX.SQUASH_X, CONFIG.FX.SQUASH_Y)` then tween back to neutral over CONFIG.FX.SQUASH_MS/1000 with `(v) => obj.scaleTo(2 - v, v)` and easings.easeOutQuad (NOT easeOutBack/Elastic — would read as bouncy/over-stimulating). Provide a stretch path usable on jump (scaleTo STRETCH_X/STRETCH_Y then tween back over STRETCH_MS) — either a second export `stretch(obj)` or a direction arg; keep it subtle.
    - `dust(at)`: loop CONFIG.FX.DUST_COUNT grey `rect(DUST_SIZE,DUST_SIZE)` particles at `at`, color(0x88,0x88,0x88) (grunge grey, NO pink), opacity, anchor("center"), z, tagged "fx"; tween each particle's opacity→0 and pos.y up by DUST_RISE and pos.x out by a spread over DUST_MS/1000 with easeOutQuad, the final tween `.onEnd(() => destroy(p))`. World-space (no fixed()).
    - `pop(at)`: a transient neon-green marker (ACCENT_GREEN) at `at`, scale-pop (scaleTo up to CONFIG.FX.POP_SCALE) + opacity fade over CONFIG.FX.POP_MS, `.onEnd(destroy)`. World-space. No "+1" text (coins are count-only).
    - `clearBurst()`: a brief NON-STROBING neon-green flourish (a single expanding/fading ring or a few rects that fade ONCE) over CONFIG.FX.BURST_MS, `.onEnd(destroy)`, tagged "fx". May use fixed() since it is a screen-space celebration. ONE smooth fade only — never a flicker/strobe (SAFE-03 / ADHD-safety).
    Add a head-comment mirroring hud.js's discipline notes (engine-global-only-in-fn-bodies, self-clean no-timer, tag "fx", no pink). Do NOT use lifespan()/wait()/loop()/setTimeout/setInterval anywhere. Reuse ACCENT_GREEN = [0x00,0xff,0x88] as a module const (a plain array literal, no engine call — that is allowed at top level).
  </action>
  <verify>
    <automated>node --check src/fx.js && grep -q 'export function squash' src/fx.js && grep -q 'export function dust' src/fx.js && grep -q 'export function pop' src/fx.js && grep -q 'export function clearBurst' src/fx.js && grep -q 'onEnd' src/fx.js && ! grep -nE 'setTimeout|setInterval|lifespan\(|[^a-zA-Z]wait\(' src/fx.js; SOFT=$?; bash scripts/check-safety.sh 2>&1 | grep -Ev 'SAFE-02|missing persistent' || true; exit $SOFT
  </verify>
  <acceptance_criteria>
    - `node --check src/fx.js` passes.
    - `src/fx.js` exports squash, dust, pop, clearBurst (grep each `export function <name>`).
    - The ONLY top-level statement that touches a name is `import { CONFIG } from "./config.js"` and the plain ACCENT_GREEN array literal — NO Kaplay global and NO `typeof` guard at module scope (verify: no `add(`/`tween(`/`vec2(`/`rect(` call appears outside an exported function body; structurally, every engine call is indented inside a `function` block).
    - `grep -c 'onEnd' src/fx.js` >= 1 (every effect self-cleans).
    - Comment-stripped no-timer grep finds no setTimeout/setInterval/wait(/loop(/lifespan( in src/fx.js: `sed -E 's://.*$::' src/fx.js | grep -Eq 'setTimeout|setInterval|[^a-zA-Z]wait\(|[^a-zA-Z]loop\(|lifespan\('` returns NO match (exit 1).
    - Dust uses grunge grey color(0x88,0x88,0x88); pop/burst use neon-green; NO pink color literal anywhere.
  </acceptance_criteria>
  <done>src/fx.js exists with the four exported effect helpers, all self-cleaning via tween().onEnd(destroy) + "fx" tag, all using Kaplay globals only inside function bodies (a727c13-safe), no timer/scheduler, no pink. The comment-stripped safety grep is clean against fx.js.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire squash/stretch + dust into the player (JUICE-01)</name>
  <files>src/player.js</files>
  <read_first>
    - src/player.js lines 24-32 (the add([...]) comp list — add scale(1)), lines 40-62 (the onUpdate jump path; the jump fires at line 58 `player.jump(CONFIG.JUMP_FORCE)`), line 19 (the CONFIG import to add the fx import beside).
    - 12-PATTERNS.md lines 114-145 (src/player.js assignment: add scale(1), stretch on jump after line 58, squash+dust on body().onGround, the onGround fallback to an isGrounded() rising-edge, the import line).
    - 12-RESEARCH.md lines 191-216 (Pattern 2 squash + the onGround land hook) and Open Question 1 / Assumption A1 (lines 437, 445-448) for the onGround reliability + fallback.
  </read_first>
  <action>
    Add `import { fx } from "./fx.js";` (or `import * as fx from "./fx.js";`) at module top alongside the existing CONFIG import (line 19). Add `scale(1)` to the player's add([...]) comp list (between opacity(1) and the "player" tag) so .scaleTo()/.scaleBy() exist — VISUAL only; keep squash magnitudes small so the area() collider feel stays fair (the collider tracks the entity; brief small scale is negligible — confirm with the kid in UAT). Stretch on jump: right after the `player.jump(CONFIG.JUMP_FORCE)` consume block (lines 58-61), call the stretch path on the player (a subtle stretch, e.g. fx.stretch(player) or fx.squash(player, "jump")). Squash + dust on LAND: after the player is created (engine init done — globals safe), add `player.onGround(() => { fx.squash(player); fx.dust(player.pos); });` — body().onGround fires when feet hit the floor (bundle-confirmed). FALLBACK (RESEARCH Open Q1 / A1): if onGround misses a landing with this body config, fall back to an isGrounded() rising-edge inside the existing onUpdate (`if (!wasGrounded && isGrounded()) { fx.squash(player); fx.dust(player.pos); }` with a closure-local `wasGrounded`); isGrounded() is already used at lines 49/57. Keep all engine globals bare (player.js already treats add/onUpdate/isKeyDown as bare globals). FIREWALL: do NOT touch src/math/* or src/progress.js.
  </action>
  <verify>
    <automated>node --check src/player.js && grep -q 'from "./fx.js"' src/player.js && grep -Eq 'scale\(1\)' src/player.js && grep -q 'onGround' src/player.js && grep -Eq 'fx\.(squash|dust)' src/player.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/player.js` passes.
    - `src/player.js` imports fx from "./fx.js".
    - The player add([...]) comp list contains `scale(1)`.
    - A land hook exists: `grep -q 'onGround' src/player.js` (or the documented isGrounded() rising-edge fallback is present).
    - `grep -Eq 'fx\.(squash|dust)'` matches — both squash and dust are called at the land hook; a stretch/squash is called at the jump site.
    - No edit references src/math/* or src/progress.js (firewall intact).
    - Comment-stripped safety grep on player.js stays clean (no new timer/scheduler).
  </acceptance_criteria>
  <done>Jumping stretches and landing squashes the player then snaps back; landing kicks up dust; both fire via fx.js with no timer; scale(1) added; firewall untouched; node --check clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fire coin pop and clear burst from the scene (JUICE-02, JUICE-03)</name>
  <files>src/scenes/game.js</files>
  <read_first>
    - src/scenes/game.js lines 19-26 (import block — add the fx import), lines 122-125 (coin onCollide — add fx.pop before destroy(c)), lines 154-171 (the onClear body — add fx.clearBurst() after the hud calls at 164-165), line 202 (onSceneLeave — optional destroyAll("fx") sweep).
    - 12-PATTERNS.md lines 149-177 (src/scenes/game.js assignment: coin pop, clear burst ADD-one-line, the ../fx.js import, the optional onSceneLeave sweep, and the "do NOT modify the gate's gate-cleared banner" note).
    - 12-RESEARCH.md lines 218-245 (Patterns 3-4) and src/ui/mathGate.js lines 185-219 (the gate's terminal "LEVEL CLEAR" gate-cleared banner — JUICE-03 LAYERS on it; do NOT modify it).
  </read_first>
  <action>
    Add `import { fx } from "../fx.js";` to the existing import block (lines 19-26) — scenes/ is one level below src/, so `../fx.js`. JUICE-02: in the coin onCollide handler (lines 122-125), spawn `fx.pop(c.pos.clone());` BEFORE `destroy(c)` (clone because c.pos is live and c is about to be destroyed); leave `coinsCollected += 1;` and `destroy(c);` exactly as they are — count is unaffected, no "+1" text. JUICE-03: in onClear (lines 154-171), add a single line `fx.clearBurst();` after the existing `hud.refresh(); if (leveledUp) hud.flashLevelUp();` (around line 165) and before/around `writeSave(...)` — change NOTHING else in onClear. The burst LAYERS on the existing moment (the gate's "LEVEL CLEAR" gate-cleared banner in mathGate.js:185-219 stays UNTOUCHED + hud.flashLevelUp()). Optionally add a belt-and-braces `onSceneLeave(() => destroyAll("fx"));` near the existing onSceneLeave at line 202 to sweep any in-flight effect on scene leave (anti-leak). Do NOT alter the gate, the firewall modules, or any persistence/XP logic.
  </action>
  <verify>
    <automated>node --check src/scenes/game.js && grep -q 'from "../fx.js"' src/scenes/game.js && grep -Eq 'fx\.pop\(' src/scenes/game.js && grep -q 'fx.clearBurst' src/scenes/game.js && bash scripts/check-safety.sh 2>&1 | grep -Ev 'SAFE-02|FAIL — missing persistent' || true; node --check src/scenes/game.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/scenes/game.js` passes.
    - `src/scenes/game.js` imports fx from "../fx.js".
    - `fx.pop(` is called in the coin onCollide handler BEFORE `destroy(c)` (coin pop at the coin's spot); `coinsCollected += 1` is unchanged.
    - `fx.clearBurst()` is called in onClear, layered after hud.refresh()/hud.flashLevelUp(); nothing else in onClear changed.
    - src/ui/mathGate.js is NOT modified (the gate's gate-cleared "LEVEL CLEAR" banner is untouched) — `git diff --name-only` does not list mathGate.js.
    - The comment-stripped no-timer + forgiving greps in `bash scripts/check-safety.sh` stay clean for game.js (the only allowed remaining failure is the SAFE-02 hint positive, which Plan 02 closes).
  </acceptance_criteria>
  <done>Collecting a coin pops a neon-green marker at its spot then destroys cleanly; clearing the gate fires a brief non-strobing burst layered on the existing LEVEL CLEAR + level-up flash; the gate banner and persistence/XP logic are untouched; node --check clean and the safety audit's no-timer/forgiving greps stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import/load → kaplay init | fx.js is imported (hoisted) BEFORE kaplay() installs globals — top-level engine use would crash at load |
| effect entity lifecycle → scene replay | transient effects must self-destroy and be tagged so they never leak across go()/respawn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | DoS (self-inflicted, availability) | src/fx.js top-level engine call | mitigate | Globals used ONLY inside exported function bodies; the ONLY top-level statement touching a name is the CONFIG import + a plain array literal (a727c13 lesson). node --check + the manual load catch a regression. |
| T-12-04 | DoS (resource leak) | effect entities surviving replay | mitigate | Every transient tagged "fx" and self-destroys on tween().onEnd(destroy); optional onSceneLeave(destroyAll("fx")) sweep. No setTimeout/lifespan/wait. |
| T-12-05 | Tampering | DOM/innerHTML injection via an effect/marker string | mitigate | All visuals are Kaplay text()/rect() canvas objects — no innerHTML/document./alert (mirrors GATE-01). No user-controlled string in any effect. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | N/A — no packages installed this phase (Kaplay vendored; fx.js authored in-repo). |
</threat_model>

<verification>
- `node --check` clean on src/fx.js, src/player.js, src/scenes/game.js.
- `bash scripts/check-safety.sh`: no-timer + forgiving greps green for all three files; fx.js onEnd positive green (only the SAFE-02 hint positive remains red until Plan 02).
- Structural: fx exports squash/dust/pop/clearBurst; player calls fx.squash+fx.dust at the land hook + a stretch on jump + has scale(1); scene calls fx.pop (coin) + fx.clearBurst (onClear); mathGate.js untouched.
- Manual (kid-UAT, routed to verification): jump/land squash+dust subtle not bouncy; coin pop quick not jarring; clear burst brief non-strobing layered on LEVEL CLEAR.
</verification>

<success_criteria>
- JUICE-01: jump stretch + land squash + dust, self-cleaning, no timer.
- JUICE-02: coin pop at the coin's position before destroy, count unaffected.
- JUICE-03: brief non-strobing clear burst layered on the existing LEVEL CLEAR banner + level-up flash.
- One fx.js, four functions, one idiom; no leak; a727c13-safe; no pink; safety audit no-timer/forgiving greps stay green.
</success_criteria>

<output>
Create `.planning/phases/12-polish-adhd-safety-uat/12-01-SUMMARY.md` when done.
</output>
