---
phase: 37-mobile-responsive-canvas-touch-controls
plan: 04
type: execute
wave: 3
depends_on: ["37-02"]
files_modified:
  - src/index.html
autonomous: true
requirements: [MOB-04]
must_haves:
  truths:
    - "Holding a coarse-pointer device in portrait shows a dark-grunge 'rotate your device' overlay (#rotate) and hides #stage; rotating back to landscape hides the overlay and shows the game — driven by CSS `@media (pointer: coarse) and (orientation: portrait)` only, with NO reliance on screen.orientation.lock()"
    - "The canvas has `touch-action: none` so pinch-zoom, scroll, and double-tap-zoom never fight the game, and the viewport meta pins scale (width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover)"
    - "Desktop (pointer:fine) is unaffected — the overlay never shows, #stage stays the fixed 960x540 wrapper, and browser-boot stays green"
    - "The overlay is static markup only (no user/localStorage value interpolated into HTML) — no injection path; the file:// guard stays byte-identical"
  artifacts:
    - "src/index.html — updated viewport meta; `#game { touch-action: none }`; `#rotate` portrait overlay markup + the pointer-coarse+portrait media query that swaps #stage for #rotate"
  key_links:
    - "@media (pointer: coarse) and (orientation: portrait) -> #rotate display:flex + #stage display:none; landscape -> reverse"
    - "meta viewport + #game touch-action:none -> browser gesture suppression over the canvas (the reliable suppressor per research; orientation.lock avoided)"
---

<objective>
Add the portrait "rotate your device" overlay and the browser-gesture suppression that keep the game from fighting the phone's native scroll/zoom — all CSS + static markup in index.html, gated to coarse-pointer devices so desktop is untouched. Landscape is the play orientation; portrait shows a dark-grunge rotate prompt instead of a squashed game. No screen.orientation.lock() (unsupported/permission-gated on iOS Safari).

Purpose: On a real phone, pinch-zoom and scroll would make the platformer unplayable and portrait would crop it. This closes MOB-04 with the reliable primitives (touch-action + viewport meta + a CSS orientation overlay), not the fragile lock API.

Output: index.html with viewport meta, `#game { touch-action: none }`, and a `#rotate` overlay swapped in by a coarse+portrait media query. Desktop byte-unchanged (browser-boot green).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-mobile-responsive-canvas-touch-controls/37-CONTEXT.md
@.planning/phases/37-mobile-responsive-canvas-touch-controls/37-RESEARCH.md
@src/index.html
@scripts/touch-coordinate-probe.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Viewport meta + touch-action + portrait rotate overlay</name>
  <files>src/index.html</files>
  <read_first>
    - src/index.html — the current viewport meta (line 5, `width=device-width, initial-scale=1`), the `<style>` block, the `#stage` wrapper + `#game` canvas (added by 37-02), and the file:// guard inline script (MUST stay byte-identical).
    - 37-RESEARCH.md §"Code Examples" the index.html viewport-meta + gesture-suppression snippet (the exact meta content and the `@media (pointer: coarse) and (orientation: portrait)` overlay rules) and its note: user-scalable=no is honored inconsistently on iOS so `touch-action: none` on the canvas is the reliable suppressor; do NOT rely on screen.orientation.lock().
    - 37-CONTEXT.md §"Orientation + gestures (MOB-04)" — dark-grunge overlay, landscape is play orientation, no orientation.lock.
  </read_first>
  <action>
    Update the viewport meta to `content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover"`. In `<style>` add `#game { touch-action: none; }` (suppress pinch/scroll/double-tap-zoom over the canvas). Add a `#rotate` overlay div (dark-grunge: near-black bg, TEXT-color copy, e.g. "Rotate your device to play" — pick concise copy; Claude's discretion per CONTEXT) placed in the body after #stage, hidden by default (`#rotate { display: none; }`). Add the media query `@media (pointer: coarse) and (orientation: portrait) { #rotate { display: flex; ...center... } #stage { display: none; } }` so portrait on a touch device swaps the game for the overlay and landscape restores it (note the media feature `orientation: portrait` is the CSS condition, NOT the JS lock API). Do NOT use the screen-orientation lock API anywhere (it is unsupported/permission-gated on iOS Safari) — landscape is enforced purely by this CSS overlay. Keep the overlay markup STATIC — no interpolated user/localStorage value (T-37-03). Leave the file:// guard inline script and the module script tag byte-identical. Do NOT change the #stage device-class sizing from 37-02.
  </action>
  <verify>
    <automated>grep -q "touch-action: none" src/index.html && grep -q 'id="rotate"' src/index.html && grep -q "orientation: portrait" src/index.html && grep -q "maximum-scale=1" src/index.html && grep -q "viewport-fit=cover" src/index.html && test $(grep -c "orientation.lock" src/index.html) -eq 0 && grep -q 'location.protocol === "file:"' src/index.html</automated>
  </verify>
  <done>Viewport meta pins scale; #game has touch-action:none; #rotate overlay swaps in under coarse+portrait; no orientation.lock; file:// guard intact.</done>
</task>

<task type="auto">
  <name>Task 2: Prove overlay + gesture suppression headlessly; desktop unaffected</name>
  <files>src/index.html</files>
  <read_first>
    - scripts/touch-coordinate-probe.mjs — the touch-context harness (hasTouch/isMobile) whose newContext viewport can be set portrait (e.g. 540x960) to exercise the overlay media query, or reused landscape for the negative check.
    - scripts/browser-boot.mjs — the non-touch (pointer:fine) desktop parity gate that must stay green (overlay never shows on desktop).
  </read_first>
  <action>
    Prove the overlay behaves: in a coarse-pointer PORTRAIT viewport the `#rotate` overlay is visible and `#stage` is hidden; in a coarse-pointer LANDSCAPE viewport `#rotate` is hidden and `#stage` visible. Use a short Playwright check (either extend the touch probe's context or a tiny inline `page.evaluate` reading `getComputedStyle(document.querySelector('#rotate')).display` at portrait vs landscape emulated viewports) — reuse the dup'd server/resolver convention, do not add a runtime dependency. Also confirm `touch-action: none` is present on the canvas (static grep) and that `node scripts/browser-boot.mjs` (desktop, pointer:fine) stays fully green — the overlay must never show on desktop and no mouse audit regresses. This task adds no runtime source beyond Task 1; if the media query is wrong, fix Task 1.
  </action>
  <verify>
    <automated>grep -q "touch-action: none" src/index.html && node scripts/browser-boot.mjs 2>&1 | tail -6</automated>
  </verify>
  <done>Portrait shows the rotate overlay (game hidden); landscape shows the game; touch-action:none present; desktop browser-boot fully green (overlay never shows on desktop).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM markup (index.html) | New #rotate overlay is static string markup; no dynamic interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-03 | Tampering | portrait overlay markup injection | low | mitigate | Static-string overlay; no user/localStorage value interpolated into HTML; file:// guard untouched |
| T-37-07 | Denial of Service | overlay/media query hides the game on desktop (false portrait) | medium | mitigate | Overlay gated to `(pointer: coarse) and (orientation: portrait)`; browser-boot (pointer:fine) proves desktop unaffected |
| T-37-SC | Tampering | package installs | n/a | accept | Zero new dependencies |
</threat_model>

<verification>
```
grep -q "touch-action: none" src/index.html && grep -q 'id="rotate"' src/index.html
node scripts/browser-boot.mjs        # desktop parity — overlay never shows on pointer:fine
```
</verification>

<success_criteria>
- Portrait (coarse pointer) shows the dark-grunge rotate overlay and hides the game; landscape restores it.
- touch-action:none + scale-pinned viewport meta suppress browser gestures; no screen.orientation.lock().
- Desktop unaffected; browser-boot green; overlay is static markup (no injection path).
</success_criteria>

<output>
Create `.planning/phases/37-mobile-responsive-canvas-touch-controls/37-04-SUMMARY.md` when done.
</output>
