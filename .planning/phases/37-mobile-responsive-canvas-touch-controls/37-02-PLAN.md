---
phase: 37-mobile-responsive-canvas-touch-controls
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/main.js
  - src/index.html
autonomous: true
requirements: [MOB-01]
must_haves:
  truths:
    - "The engine is initialized with `letterbox: true` at internal 640x360 (width+height still declared — letterbox requires them), and the CSS `transform: scale(1.5)` display hack is REMOVED from main.js, so mouse (offsetX) and touch (clientX-rect) now feed the SAME engine Qe window->content transform in one coordinate space"
    - "The 37-01 touch-coordinate probe now goes GREEN — a center tap reads back game-x ~320 (was ~480), the desync is gone, and the probe stays a permanent gate"
    - "Desktop is display-geometry + behavior identical: a fixed 960x540 #stage wrapper (pointer:fine) makes letterbox scale 640->960 = 1.5x with ZERO bars; every mouse-driven audit stays green (browser-boot: level-select tile clicks, mute-icon click, challenge answer clicks all still land — the offsetX/onClick trap does NOT resurface)"
    - "On a coarse-pointer device the #stage wrapper is full-viewport so letterbox fits the game into any screen with pillar/letterbox bars — the canvas is genuinely responsive (engine onResize refits automatically, no app resize code)"
    - "The stale offsetX/offsetY pitfall comments in main.js AND the transform-centering comment in index.html are rewritten to describe the letterbox reality (no lingering executable transform assignment)"
  artifacts:
    - "src/main.js — kaplay({ width:640, height:360, letterbox:true, crisp:true, ... }); the style.transform block deleted; comments rewritten"
    - "src/index.html — a #stage wrapper around #game, device-class container CSS (pointer:fine -> fixed 960x540; pointer:coarse -> full-viewport), rewritten style comment, file:// guard untouched"
  key_links:
    - "kaplay letterbox:true -> engine viewport(x,y,width,height) -> Qe unifies mouse+touch -> box.onClick fires on both click and tap via touchToMouse (default on)"
    - "#stage parent offset size -> canvas width:100%/height:100% backing store -> letterbox fits 640x360 into it -> 960x540 desktop (zero bars) / full-viewport touch"
    - "37-01 probe: RED (~480) BEFORE -> GREEN (~320) AFTER this migration; browser-boot desktop parity gate stays green"
---

<objective>
Perform the letterbox migration: swap Kaplay's init to `letterbox: true`, delete the `transform: scale(1.5)` hack, and restructure index.html so a #stage wrapper defines the display size per device class (fixed 960x540 on desktop, full-viewport on touch). This unifies mouse and touch through the engine's single Qe transform, turning the 37-01 probe GREEN — while keeping the desktop look and ALL mouse behavior identical, proven by the hard browser-boot parity gate.

Purpose: This is the load-bearing change of the phase. Every later touch capability (buttons, tappable answers) falls out of the unified coordinate mapping this establishes. The desktop-parity gate is what guarantees the kid-validated experience is not regressed.

Output: `src/main.js` (letterbox init, transform deleted, comments rewritten), `src/index.html` (#stage wrapper + device-class CSS + rewritten comment). Probe GREEN, browser-boot green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-mobile-responsive-canvas-touch-controls/37-CONTEXT.md
@.planning/phases/37-mobile-responsive-canvas-touch-controls/37-RESEARCH.md
@src/main.js
@src/index.html
@scripts/browser-boot.mjs
@scripts/touch-coordinate-probe.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch kaplay() to letterbox and delete the transform hack</name>
  <files>src/main.js</files>
  <read_first>
    - src/main.js lines 18-51 — the current kaplay() call (width/height/background/crisp/canvas) and the ENTIRE `{ const canvas = document.querySelector("#game"); canvas.style.transform = "scale(1.5)"; }` block plus its long offsetX/offsetY explanatory comment (lines 28-51).
    - 37-RESEARCH.md §"Code Examples" the main.js migration snippet (VERIFIED offsets) — letterbox requires width+height; crisp still emits image-rendering:pixelated under letterbox; touchToMouse defaults ON (leave unset); and §finding 4 (canvas fills parent width:100%/height:100% under letterbox).
    - 37-RESEARCH.md §"Source-Verified Engine Findings" finding 2 — why removing the transform + letterbox unifies offsetX and clientX-rect through one Qe.
  </read_first>
  <action>
    Add `letterbox: true` to the `kaplay({ ... })` options (keep `width: 640`, `height: 360` — letterbox REQUIRES both — plus the existing `background`, `crisp: true`, `canvas` keys). Do NOT set `touchToMouse` (it defaults ON, which is what makes taps fire box.onClick). DELETE the entire canvas transform-scale block — the `{ ... }` block (currently lines 48-51) that assigns a CSS scale transform to the #game canvas element. REWRITE the stale comment (currently lines 28-51) to describe the letterbox reality: the internal buffer stays 640x360; the #stage container in index.html defines display size per device class; the engine letterboxes 640x360 into it and routes BOTH mouse (offsetX) and touch (clientX-rect) through the same Qe window->content transform, so there is exactly one coordinate space and box.onClick works for click and tap alike. Describe this by concept — do NOT reintroduce an executable CSS transform-scale assignment on the canvas, and do NOT leave that removed code token anywhere in the file (not even in a comment). Leave every loadSprite/loadSound/scene/go call below untouched.
  </action>
  <verify>
    <automated>bash scripts/check-import-safety.sh && grep -q "letterbox: true" src/main.js && test $(grep -c "style.transform" src/main.js) -eq 0 && node --check src/main.js</automated>
  </verify>
  <done>kaplay() runs with letterbox:true at 640x360; the transform block is gone (zero `style.transform` occurrences); the comment describes letterbox; import-safety green.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure index.html — #stage wrapper + device-class container</name>
  <files>src/index.html</files>
  <read_first>
    - src/index.html — the whole file: the `<style>` block + its transform-centering comment (lines 8-26), the body flex-centering, the `<canvas id="game">`, and the file:// guard inline script (lines 27-53, MUST stay byte-identical).
    - 37-RESEARCH.md §"Code Examples" the index.html snippet (device-class container CSS) and §"Open Questions" Q3 (add a dedicated #stage wrapper so device-class media queries + the portrait overlay target it cleanly without touching <body>).
    - 37-RESEARCH.md §Pitfall 7 / Assumption A1 — fix the desktop wrapper at 960x540 so letterbox scales 640->960 with ZERO bars, preserving the desktop look.
  </read_first>
  <action>
    Wrap `<canvas id="game">` in a `<div id="stage">` (the canvas's parent — letterbox sizes the canvas to its parent's offset size). Add device-class container CSS: `@media (pointer: fine) { #stage { width: 960px; height: 540px; } }` (desktop: fixed 960x540 = zero-bar 1.5x scale, preserves the current look) and `@media (pointer: coarse) { #stage { width: 100vw; height: 100vh; } }` (touch: full-viewport, letterbox adds bars for non-16:9). Keep the body flex-centering so #stage stays centered on desktop. REWRITE the stale `<style>` comment (currently describing transform-origin centering) to describe the letterbox + #stage-container reality — by concept, without leaving an executable/quoted transform hack. Do NOT change the viewport meta, touch-action, or add the portrait overlay here (those are 37-04's scope — this plan only establishes the container). Leave the file:// guard inline script and the `<script type="module" src="main.js">` byte-identical.
  </action>
  <verify>
    <automated>grep -q 'id="stage"' src/index.html && grep -q "pointer: fine" src/index.html && grep -q "pointer: coarse" src/index.html && grep -q "960px" src/index.html && grep -q 'location.protocol === "file:"' src/index.html</automated>
  </verify>
  <done>#game is wrapped in #stage; desktop=fixed 960x540, touch=full-viewport; comment rewritten; file:// guard intact.</done>
</task>

<task type="auto">
  <name>Task 3: Prove probe GREEN + desktop parity (the hard gate)</name>
  <files>src/main.js</files>
  <read_first>
    - scripts/touch-coordinate-probe.mjs — now expected to PASS (~320) after Tasks 1-2.
    - scripts/browser-boot.mjs — lines 223/370 use a non-touch 960x540 context; lines 394-430 assert AudioContext gesture-gate + mute toggle; the drive loop clicks level-select tiles and reaches each goal. This non-touch run IS the desktop parity gate (pointer:fine -> #stage 960x540 -> letterbox zero-bar), proving mouse offsetX/onClick still land.
  </read_first>
  <action>
    Run the 37-01 probe and confirm it now GREENS (center tap reads game-x ~320). Then run the full desktop parity battery — `node scripts/browser-boot.mjs` — and confirm EVERY mouse-driven assertion stays green: level-select tile clicks land the right level, the mute icon click toggles, challenge answer clicks resolve, and the AudioContext gesture-gate proof still passes. Also run the static safety gates. This task changes no new source (it is the verification gate for Tasks 1-2); if the parity gate regresses (a click misses), the letterbox/#stage sizing is wrong — fix Tasks 1-2 until BOTH the probe is GREEN and browser-boot is fully green. No code edit beyond what Tasks 1-2 required.
  </action>
  <verify>
    <automated>node scripts/touch-coordinate-probe.mjs && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs 2>&1 | tail -8</automated>
  </verify>
  <done>Probe GREEN (~320); browser-boot desktop parity fully green (tile/mute/answer clicks land, audio gesture-gate passes); safety + validate-levels green. The migration preserved the desktop experience and unified coordinates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM markup (index.html) | Static-string only; the #stage wrapper adds no dynamic/interpolated markup |
| client input → engine | Mouse + touch now share the engine Qe transform; no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-02 | Tampering | letterbox/#stage sizing regresses offsetX -> box.onClick misses (the Phase-14 trap) | high | mitigate | browser-boot desktop parity gate (tile/mute/answer clicks) MUST stay green; fixed 960x540 zero-bar desktop wrapper |
| T-37-03 | Tampering | index.html static markup injection | low | accept | #stage wrapper is static markup; no user/localStorage value interpolated; file:// guard untouched |
| T-37-SC | Tampering | package installs | n/a | accept | Zero new dependencies; no Kaplay upgrade |
</threat_model>

<verification>
```
node scripts/touch-coordinate-probe.mjs        # GREEN (~320) — the RED->GREEN flip
bash scripts/check-import-safety.sh
bash scripts/check-safety.sh
node scripts/validate-levels.mjs
node scripts/browser-boot.mjs                   # HARD desktop parity gate — mouse clicks + audio still green
```
</verification>

<success_criteria>
- letterbox:true at 640x360; the transform hack deleted; comments rewritten to letterbox reality (main.js + index.html).
- #stage wrapper: desktop 960x540 zero-bar, touch full-viewport; file:// guard intact.
- 37-01 probe flips RED->GREEN (~480 -> ~320) — a permanent gate.
- browser-boot desktop parity fully green (every mouse-driven audit lands, audio gesture-gate passes).
</success_criteria>

<output>
Create `.planning/phases/37-mobile-responsive-canvas-touch-controls/37-02-SUMMARY.md` when done.
</output>
