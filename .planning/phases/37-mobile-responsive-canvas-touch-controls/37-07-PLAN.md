---
phase: 37-mobile-responsive-canvas-touch-controls
plan: 07
type: execute
wave: 5
depends_on: ["37-04", "37-05", "37-06"]
files_modified:
  - .planning/phases/37-mobile-responsive-canvas-touch-controls/37-VERIFICATION.md
autonomous: true
requirements: [MOB-01, MOB-02, MOB-03, MOB-04, MOB-05]
must_haves:
  truths:
    - "The full consolidated gate suite runs green in ONE pass — the existing CLAUDE.md gates (check-safety, check-import-safety, check-progress, check-gate, validate-levels, browser-boot desktop parity) PLUS the three NEW permanent touch gates (touch-coordinate-probe GREEN ~320, touch-tap-ui-probe, touch-controls-drive) — proving the mobile layer landed without regressing the kid-validated desktop build"
    - "37-VERIFICATION.md documents MOB-01..MOB-05 as delivered with the exact evidence (which gate proves each), and explicitly records the DEFERRED on-device gates rolling to Phase 38: MOB-05 real-device audio-activation proof (headless taps grant activation) and MOB-06 kid touch-layout tuning"
    - "The RED-first probe's RED->GREEN history is recorded (was ~480 on the transform build, ~320 after letterbox) as the empirical proof MOB-01 SC1 required, and all three new probes are named as permanent members of the gate suite"
  artifacts:
    - ".planning/phases/37-mobile-responsive-canvas-touch-controls/37-VERIFICATION.md — the phase verification record: per-requirement evidence, the full green suite run, and the deferred-to-Phase-38 list"
  key_links:
    - "each MOB requirement -> the specific gate/probe that proves it -> its green result in the consolidated run"
    - "deferred items (MOB-05 device audio, MOB-06 kid tuning) -> Phase 38 (already in ROADMAP Phase 38 requirements)"
---

<objective>
Close the phase: run the entire gate suite — every existing CLAUDE.md gate plus the three new permanent touch gates — green in one pass, and write 37-VERIFICATION.md documenting MOB-01..MOB-05 delivered with per-requirement evidence and the explicit list of on-device gates deferred to Phase 38 (MOB-05 device audio proof, MOB-06 kid touch-layout tuning). No source changes — this is the consolidation + record.

Purpose: The phase convention is that nothing closes on greps alone and the deferral line must be explicit. This plan proves the mobile layer landed without regressing desktop and hands Phase 38 a clean statement of what still needs a real device.

Output: a green consolidated run + 37-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-mobile-responsive-canvas-touch-controls/37-CONTEXT.md
@.planning/phases/37-mobile-responsive-canvas-touch-controls/37-RESEARCH.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full consolidated gate suite green in one pass</name>
  <files>.planning/phases/37-mobile-responsive-canvas-touch-controls/37-VERIFICATION.md</files>
  <read_first>
    - .claude/CLAUDE.md §"Verification gates" — the authoritative gate list (check-gate.sh, check-safety.sh, check-import-safety.sh, check-progress.sh, check-assets-manifest.mjs, check-terrain-atlas.sh, validate-levels.mjs, browser-boot.mjs, audit-phase21-mechanics.mjs). Run the ones this phase can affect (all safety/import/progress/validate + browser-boot desktop parity) plus the 3 new touch probes.
    - The three new gates from 37-01/37-05/37-06: scripts/touch-coordinate-probe.mjs, scripts/touch-tap-ui-probe.mjs, scripts/touch-controls-drive.mjs.
  </read_first>
  <action>
    Run the full suite in one pass and confirm every gate is green: `bash scripts/check-safety.sh`, `bash scripts/check-import-safety.sh`, `bash scripts/check-progress.sh`, `bash scripts/check-gate.sh`, `node scripts/validate-levels.mjs`, `node scripts/browser-boot.mjs` (the HARD desktop parity gate — mouse audits + keyboard drive + audio gesture-gate), plus the three NEW permanent touch gates `node scripts/touch-coordinate-probe.mjs` (GREEN ~320), `node scripts/touch-tap-ui-probe.mjs`, and `node scripts/touch-controls-drive.mjs`. Capture the results. If ANY gate is red, STOP and report which — do not paper over it (the phase does not close red). This task makes no source edits; it is the consolidated proof.
  </action>
  <verify>
    <automated>bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && bash scripts/check-gate.sh && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs 2>&1 | tail -4 && node scripts/touch-coordinate-probe.mjs && node scripts/touch-tap-ui-probe.mjs && node scripts/touch-controls-drive.mjs</automated>
  </verify>
  <done>Every existing CLAUDE.md gate this phase can affect + the three new touch gates run green in one pass; desktop parity intact; the touch layer proven.</done>
</task>

<task type="auto">
  <name>Task 2: Write 37-VERIFICATION.md (delivered + deferred)</name>
  <files>.planning/phases/37-mobile-responsive-canvas-touch-controls/37-VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md Phase 37 SC1-5 + Phase 38 requirements (BRAND-01, MOB-06, VER-01..04) — the deferred MOB-05 device audio proof + MOB-06 kid tuning already belong to Phase 38; reference them so the handoff is explicit.
    - .planning/REQUIREMENTS.md MOB-01..MOB-06 lines — the exact requirement text to map evidence against.
    - The SUMMARY files 37-01..37-06 (as they land) for the RED->GREEN probe baseline (~480 -> ~320) and per-plan evidence.
  </read_first>
  <action>
    Write 37-VERIFICATION.md with: (1) a per-requirement table MOB-01..MOB-05 mapping each to the gate/probe that proves it (MOB-01 -> touch-coordinate-probe RED~480->GREEN~320 + browser-boot desktop parity + rewritten comments; MOB-02 -> touch-controls-drive jump/move/variable-height/multi-touch + input-seam a727c13/safety + coarse-pointer gate; MOB-03 -> touch-tap-ui-probe tap-resolves-answer + tap-toggles-mute; MOB-04 -> index.html touch-action/viewport/#rotate overlay + browser-boot desktop unaffected; MOB-05 -> audio unlock via click/pointerup wiring + headless AudioContext gesture-gate green). (2) The consolidated green-suite run result from Task 1. (3) An explicit DEFERRED section listing the on-device gates rolling to Phase 38: MOB-05 real-device audio-activation proof (touchstart-not-activation must be VERIFIED on device; headless synthetic taps grant activation so this cannot be proven here) and MOB-06 kid touch-layout tuning (button size/placement from watching her hands), plus the documented iOS ITP ~7-day storage eviction expectation and Assumption A1 (desktop pixel-parity spot-check) for Phase 38 UAT. Match the format of prior phase VERIFICATION/UAT records in this repo.
  </action>
  <verify>
    <automated>test -f .planning/phases/37-mobile-responsive-canvas-touch-controls/37-VERIFICATION.md && grep -q "MOB-01" .planning/phases/37-mobile-responsive-canvas-touch-controls/37-VERIFICATION.md && grep -q "MOB-05" .planning/phases/37-mobile-responsive-canvas-touch-controls/37-VERIFICATION.md && grep -qi "defer" .planning/phases/37-mobile-responsive-canvas-touch-controls/37-VERIFICATION.md && grep -q "MOB-06" .planning/phases/37-mobile-responsive-canvas-touch-controls/37-VERIFICATION.md</automated>
  </verify>
  <done>37-VERIFICATION.md records MOB-01..05 delivered with per-requirement evidence, the green consolidated run, and the explicit Phase-38 deferrals (MOB-05 device audio proof, MOB-06 kid tuning, ITP note, A1 pixel spot-check).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Consolidation + documentation only; no runtime surface changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-11 | Repudiation | phase closed on greps/automation alone without the deferral line drawn | medium | mitigate | 37-VERIFICATION.md maps each MOB req to real gate evidence and names the deferred on-device gates explicitly |
| T-37-SC | Tampering | package installs | n/a | accept | Zero new dependencies across the phase |
</threat_model>

<verification>
```
bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh \
  && bash scripts/check-gate.sh && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs \
  && node scripts/touch-coordinate-probe.mjs && node scripts/touch-tap-ui-probe.mjs \
  && node scripts/touch-controls-drive.mjs
```
- One green pass across existing + new touch gates.
- 37-VERIFICATION.md present with per-requirement evidence + deferred list.
</verification>

<success_criteria>
- Full consolidated suite green in one pass (existing CLAUDE.md gates + 3 new permanent touch gates); desktop parity intact.
- 37-VERIFICATION.md documents MOB-01..05 delivered + the Phase-38 deferrals (MOB-05 device audio, MOB-06 kid tuning, ITP, A1 pixel spot-check).
</success_criteria>

<output>
Create `.planning/phases/37-mobile-responsive-canvas-touch-controls/37-07-SUMMARY.md` when done.
</output>
