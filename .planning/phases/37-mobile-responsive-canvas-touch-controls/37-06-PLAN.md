---
phase: 37-mobile-responsive-canvas-touch-controls
plan: 06
type: execute
wave: 4
depends_on: ["37-03", "37-02"]
files_modified:
  - src/ui/touchControls.js
  - src/scenes/game.js
  - scripts/touch-controls-drive.mjs
autonomous: true
requirements: [MOB-02]
must_haves:
  truths:
    - "On a touch device the game scene shows discrete left/right virtual buttons bottom-left and a jump button bottom-right (dark-grunge, semi-transparent, >=64px effective hit zones from CONFIG.TOUCH) — VISIBLE ONLY on touch-capable devices via feature-detect matchMedia('(pointer: coarse)') (NOT UA-sniff); desktop (pointer:fine) never mounts them"
    - "Pressing a button feeds the src/input.js seam so the LOCKED coyote/buffer/variable-height jump is REUSED: touching jump calls input.fireJumpPress() (buffer path), releasing calls input.fireJumpRelease() (variable-height cut) — variable height works by press DURATION via press/release edges, with NO timer"
    - "Multi-touch is tracked per Touch.identifier (Map<identifier, buttonName>) so left+jump held simultaneously works and lifting one finger never clears the other; onTouchStart/Move/End AND onTouchCancel are all handled (the engine fires touchcancel)"
    - "Movement buttons are challenge-pause-aware — inert while a math panel is open (mirrors the keyboard, which player.paused already gates); the buttons never mutate held-state while the run is frozen"
    - "The touch controls + their engine controllers are torn down on onSceneLeave and input.reset() is called so no held-state or callback leaks across scene re-entries (anti-leak); all engine globals stay inside function bodies (a727c13)"
    - "A Playwright touch-drive harness proves: a tap on the jump zone makes the player rise (vel.y<0 / y decreases), tapping left/right moves the player, and a held jump rises higher than a tapped jump (variable height) — driven headlessly, not assumed"
  artifacts:
    - "src/ui/touchControls.js — mountTouchControls(): coarse-pointer-gated button draw + onTouchStart/Move/End/Cancel per-identifier handlers feeding src/input.js; returns a teardown handle"
    - "src/scenes/game.js — mount the touch controls (coarse-pointer only) + onSceneLeave teardown + input.reset()"
    - "scripts/touch-controls-drive.mjs — touch-emulated drive proving jump/move + variable-height + multi-touch (dup'd server/resolver)"
  key_links:
    - "onTouchStart(pos, touch) -> hit-test pos vs CONFIG.TOUCH button rects -> active.set(touch.identifier, name) -> input.setLeftHeld/setRightHeld(true) | input.fireJumpPress()"
    - "onTouchEnd/onTouchCancel(pos, touch) -> name = active.get(identifier); active.delete -> input.setLeftHeld/setRightHeld(false) | input.fireJumpRelease()"
    - "matchMedia('(pointer: coarse)') true -> mountTouchControls in game scene; false (desktop) -> not mounted -> browser-boot unaffected"
---

<objective>
Draw the on-screen touch controls and wire them to the input seam so a phone player can run and jump. Discrete left/right (bottom-left) + jump (bottom-right) virtual buttons, oversized hit zones, dark-grunge and semi-transparent, visible only on touch devices, multi-touch per-identifier, challenge-pause-aware — and jump reuses the LOCKED coyote/buffer/variable-height physics through src/input.js (fire press/release edges, no timer). A Playwright touch-drive harness proves the buttons actually move and jump the player, including variable height and simultaneous left+jump.

Purpose: This is the visible half of MOB-02. It must add zero to the desktop build (feature-detect gate) and must NOT re-implement jump — it feeds the seam 37-03 established, so the kid-validated feel is inherited.

Output: src/ui/touchControls.js; game.js mount + teardown; scripts/touch-controls-drive.mjs. Desktop byte-identical (browser-boot green); touch drive green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-mobile-responsive-canvas-touch-controls/37-CONTEXT.md
@.planning/phases/37-mobile-responsive-canvas-touch-controls/37-RESEARCH.md
@src/config.js
@src/input.js
@src/scenes/game.js
@src/audio.js
@scripts/browser-boot.mjs
@scripts/touch-coordinate-probe.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/ui/touchControls.js — buttons + per-identifier multi-touch</name>
  <files>src/ui/touchControls.js</files>
  <read_first>
    - src/input.js (37-03) — setLeftHeld/setRightHeld, fireJumpPress/fireJumpRelease, reset(); this module ONLY drives the seam, never touches player physics.
    - src/config.js CONFIG.TOUCH (37-03) — the LEFT/RIGHT/JUMP rects (X/Y/W/H >=64), OPACITY, glyphs; all numbers come from here (no magic numbers).
    - src/audio.js lines 127-150 — the wireAudioUI mount idiom (add([... fixed(), z(...)]) per scene; go() clears the app bus so per-scene mount + teardown is required) and the a727c13 discipline.
    - src/ui/challenge.js header — the fixed()+high-z() in-world canvas-object convention (buttons are Kaplay draw objects, NOT DOM — continues the no-injection rule).
    - 37-RESEARCH.md §"Pattern 2: Multi-touch virtual buttons via identifier" (the Map<identifier,name> + onTouchStart/End sketch), §Pitfall 2 (track per-identifier, handle touchcancel), §Pitfall 3 (no timer — variable height is press/release edges), §Pitfall 5 (feature-detect matchMedia('(pointer: coarse)'), NOT isTouchscreen/UA), §Don't Hand-Roll (reuse onTouchStart/Move/End engine globals, not raw addEventListener).
  </read_first>
  <action>
    Create src/ui/touchControls.js exporting `mountTouchControls()`. FIRST guard: if `!window.matchMedia("(pointer: coarse)").matches` return a no-op teardown handle (an object with a `destroy()` that does nothing) — desktop never mounts buttons (feature-detect, not UA-sniff). Otherwise draw 3 buttons as fixed()+high-z() Kaplay objects (rect or sprite + glyph text) at CONFIG.TOUCH.LEFT/RIGHT/JUMP with CONFIG.TOUCH.OPACITY, tagged "touchctl". Keep a closure-local `const active = new Map()` (identifier -> "left"|"right"|"jump"). Register `onTouchStart((pos, touch) => ...)`: hit-test `pos` (already in 640x360 game space via Qe) against each button rect; on hit, `active.set(touch.identifier, name)` and drive the seam — left/right call `input.setLeftHeld/setRightHeld(true)`, jump calls `input.fireJumpPress()`. Register `onTouchEnd` AND `onTouchCancel` with the same body: read `name = active.get(touch.identifier)`, `active.delete(touch.identifier)`, then clear held (setLeftHeld/RightHeld(false)) or `input.fireJumpRelease()`. (onTouchMove optional; if implemented, update held-state when a finger slides off/on a button by re-hit-testing — keep it simple and correct.) Challenge-pause-aware: movement must be inert while a math panel is open — gate the setLeftHeld/setRightHeld writes on the same freeze signal the keyboard respects (player.paused, or a challenge-open flag the scene passes into mountTouchControls); never set held-state while frozen. Capture the returned engine controllers and return a teardown handle whose `destroy()` cancels them all and calls `destroyAll("touchctl")`. Keep ALL engine globals (onTouchStart/End/Cancel, add, rect, text, fixed, z, destroyAll) inside the function body (a727c13); matchMedia is a browser global. NO timers.
  </action>
  <verify>
    <automated>bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node --check src/ui/touchControls.js && grep -q "pointer: coarse" src/ui/touchControls.js && grep -q "identifier" src/ui/touchControls.js && grep -q "onTouchCancel" src/ui/touchControls.js && grep -q "fireJumpPress" src/ui/touchControls.js</automated>
  </verify>
  <done>touchControls.js draws coarse-pointer-only buttons, tracks per-identifier multi-touch (start/end/cancel), feeds the input seam for move+jump, is challenge-pause-aware, returns a teardown; a727c13 + safety green; no timer.</done>
</task>

<task type="auto">
  <name>Task 2: Mount + teardown the touch controls in game.js</name>
  <files>src/scenes/game.js</files>
  <read_first>
    - src/scenes/game.js lines 40-48 (the per-scene UI mount region — audio.wireAudioUI() is called here every scene), lines 194-196 (makePlayer), lines 489-499 (the onSceneLeave sweep — destroyAll("parallax")/("fx"), tween cancels — where teardown belongs), and the challenge/pause seam (player.paused is set true while the math gate is open, ~line 370).
    - src/ui/touchControls.js (Task 1) — mountTouchControls() returns a teardown handle.
    - src/input.js (37-03) — input.reset() clears held-state + jump callbacks; must run on scene leave (anti-leak).
  </read_first>
  <action>
    Import `mountTouchControls` from `../ui/touchControls.js` and `import * as input from "../input.js";`. In the scene body (near audio.wireAudioUI()), call `const touchControls = mountTouchControls();` — the module self-gates to coarse pointer, so on desktop it is a harmless no-op. Ensure the buttons' pause-awareness sees the same freeze the keyboard does (pass player.paused access or the challenge-open flag if touchControls needs it — match the API you defined in Task 1). In the existing `onSceneLeave(() => { ... })` sweep, add `touchControls.destroy();` and `input.reset();` so buttons, their engine controllers, and the seam's held-state + jump-callback registry are all cleared on every scene exit (anti-leak — nothing survives go()/respawn). Do NOT change the respawn/checkpoint/goal/mechanic wiring. Keep engine refs inside the scene body (a727c13).
  </action>
  <verify>
    <automated>bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && grep -q "mountTouchControls" src/scenes/game.js && grep -q "touchControls.destroy()" src/scenes/game.js && grep -q "input.reset()" src/scenes/game.js && node scripts/browser-boot.mjs 2>&1 | tail -6</automated>
  </verify>
  <done>game.js mounts the touch controls (coarse-only no-op on desktop) and tears them down + resets the input seam on scene leave; browser-boot (desktop) stays fully green — zero desktop change; safety + import-safety green.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright touch-drive harness — jump/move + variable-height + multi-touch</name>
  <files>scripts/touch-controls-drive.mjs</files>
  <read_first>
    - scripts/browser-boot.mjs — server + resolvePlaywright() + SAVE_BLOB + the drive-into-a-level flow to dup (deliberate duplication).
    - scripts/touch-coordinate-probe.mjs — touch context options (hasTouch/isMobile) + boundingBox/touchscreen pattern; note page.touchscreen.tap is a single tap — for HELD and MULTI-touch use CDP `Input.dispatchTouchEvent` (touchStart/touchMove/touchEnd with explicit touchPoints carrying id) so a finger can be held down and two fingers pressed at once.
    - src/config.js CONFIG.TOUCH — the button rects to compute on-screen tap coordinates from game space (map 640x360 game space back to the 960x540 canvas box).
    - 37-RESEARCH.md §Validation Architecture (MOB-02 test map) + Assumption A5.
  </read_first>
  <action>
    Create scripts/touch-controls-drive.mjs dup'ing browser-boot's hardened server + resolvePlaywright() + ephemeral port. Launch chromium with a TOUCH context (hasTouch:true, isMobile:true, landscape 960x540), seed the unlock save blob, drive into a level (title -> select -> game) and confirm the touch buttons mounted (coarse pointer). Then, using CDP `Input.dispatchTouchEvent` against the JUMP button's on-screen position (computed from CONFIG.TOUCH.JUMP mapped into the canvas box): (1) a SHORT press (touchStart then quick touchEnd) and record the player's peak rise (min y or most-negative vel.y sampled over frames); (2) a HELD press (touchStart, hold across several frames, then touchEnd) and record its peak rise; assert the HELD peak rises HIGHER than the SHORT peak (variable height reuses the seam, no timer). (3) Press the LEFT button (or RIGHT) and assert player.pos.x moves in that direction. (4) MULTI-TOUCH: dispatch two simultaneous touchPoints (distinct ids) on LEFT and JUMP and assert the player both moves horizontally AND rises — proving per-identifier tracking. Read player state via page.evaluate against an engine query (e.g. the player object's pos/vel exposed through a get()/tag lookup, mirroring how browser-boot inspects the scene). Push failures into an errors array; exit non-zero if any.
  </action>
  <verify>
    <automated>node --check scripts/touch-controls-drive.mjs && grep -q "hasTouch: true" scripts/touch-controls-drive.mjs && grep -qi "dispatchTouchEvent" scripts/touch-controls-drive.mjs && node scripts/touch-controls-drive.mjs 2>&1 | tail -10</automated>
  </verify>
  <done>The touch-drive harness proves headlessly: tap-jump rises, held-jump rises higher (variable height), left/right move the player, and left+jump multi-touch works simultaneously — MOB-02 driven, not assumed. Exits green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| touch input → input.js seam | Session-only held-state; no persistence, no new trust boundary |
| touch UI | Kaplay canvas draw objects (not DOM) — no markup sink, no injection path |
| touch-controls-drive HTTP server → filesystem | Dup'd hardened loopback/ROOT-clamped static server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-04 | Tampering | parallel touch jump path re-derives locked physics | high | mitigate | Buttons feed input.js seam only (fireJumpPress/Release); the touch-drive harness proves variable-height matches the seam |
| T-37-06 | Tampering | held-state / controllers leak across scene re-entries | medium | mitigate | onSceneLeave -> touchControls.destroy() + input.reset(); teardown cancels engine controllers + destroyAll("touchctl") |
| T-37-10 | Denial of Service | buttons shown on desktop (breaks byte-identical) | high | mitigate | matchMedia('(pointer: coarse)') gate; browser-boot (pointer:fine) proves no desktop change |
| T-37-05 | Tampering | a timer measuring hold-to-jump trips SAFE-01 | medium | mitigate | Press/release edges only; check-safety green |
| T-37-01 | Information Disclosure | new drive-harness static server path traversal | medium | mitigate | Dup browser-boot's ROOT-clamped + loopback server verbatim |
| T-37-SC | Tampering | package installs | n/a | accept | Zero new dependencies |
</threat_model>

<verification>
```
bash scripts/check-import-safety.sh && bash scripts/check-safety.sh
node scripts/browser-boot.mjs            # desktop: buttons NOT mounted, all mouse/keyboard audits green
node scripts/touch-controls-drive.mjs    # touch: jump/move/variable-height/multi-touch proven
```
</verification>

<success_criteria>
- Coarse-pointer-only left/right/jump buttons (>=64px) feed the input seam; desktop never mounts them.
- Jump reuses coyote/buffer/variable-height via the seam (press/release edges, no timer); multi-touch per-identifier (start/end/cancel).
- Challenge-pause-aware; torn down + input.reset() on scene leave.
- browser-boot desktop parity green; touch-drive harness proves jump/move/variable-height/multi-touch.
</success_criteria>

<output>
Create `.planning/phases/37-mobile-responsive-canvas-touch-controls/37-06-SUMMARY.md` when done.
</output>
