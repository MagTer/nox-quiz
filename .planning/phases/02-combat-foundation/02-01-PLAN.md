---
phase: 02-combat-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - math-lab.html
autonomous: true
requirements:
  - DC-02
  - COMB-03
  - ENE-01
  - ENE-02
  - DIFF-01

must_haves:
  truths:
    - "GameFSM.getState() returns 'EXPLORE' on first call (initial state)"
    - "GameFSM.transition('COMBAT') from EXPLORE succeeds and getState() returns 'COMBAT'"
    - "GameFSM.transition('DEAD') from EXPLORE throws an Error (illegal transition)"
    - "FloorConfig.getFloor(1) returns { enemy: 'Goblin', emoji: '👺', hp: 9, xpReward: 30, tablePools: [2,3,5] }"
    - "FloorConfig.getFloor(2) returns Skeleton 15HP and tablePools [4,6,7]"
    - "FloorConfig.getFloor(3) returns Dragon 21HP and tablePools [7,8,9]"
    - "CONFIG.DUNGEON exists with PLAYER_HP, DAMAGE_CORRECT, DAMAGE_WRONG, and per-enemy HP constants"
    - "window.GameFSM and window.FloorConfig are accessible in browser console"
  artifacts:
    - path: "math-lab.html"
      provides: "GameFSM IIFE module, FloorConfig IIFE module, CONFIG.DUNGEON sub-object"
      contains: "window.GameFSM"
  key_links:
    - from: "GameFSM"
      to: "CONFIG.DUNGEON"
      via: "reads no CONFIG values directly — FSM is pure state logic"
      pattern: "GameFSM"
    - from: "FloorConfig"
      to: "CONFIG.DUNGEON"
      via: "FloorConfig.getFloor() returns HP values from CONFIG.DUNGEON constants"
      pattern: "CONFIG\\.DUNGEON\\."
---

<objective>
Implement CONFIG.DUNGEON sub-object, GameFSM state machine, and FloorConfig module — all in math-lab.html outside the DOMContentLoaded block. No DOM elements are created. All logic is verifiable via browser console.

Purpose: Establishes the pure-logic foundation (state machine + enemy config) that CombatEngine and DungeonState (Plan 02) depend on.

Output: Three additions to math-lab.html — CONFIG.DUNGEON constants appended to the existing CONFIG object, a GameFSM IIFE, and a FloorConfig IIFE — inserted after MODULE 5 (QuestionSelector) and before the DOMContentLoaded listener.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-combat-foundation/02-CONTEXT.md
</context>

## Artifacts this phase produces

New symbols added to math-lab.html in this plan:

| Symbol | Type | Exported on window |
|--------|------|--------------------|
| `CONFIG.DUNGEON` | Object literal appended to existing CONFIG | No (CONFIG already global) |
| `CONFIG.DUNGEON.PLAYER_HP` | Number constant: 100 | — |
| `CONFIG.DUNGEON.DAMAGE_CORRECT` | Number constant: 3 | — |
| `CONFIG.DUNGEON.DAMAGE_WRONG` | Number constant: 8 | — |
| `CONFIG.DUNGEON.GOBLIN_HP` | Number constant: 9 | — |
| `CONFIG.DUNGEON.SKELETON_HP` | Number constant: 15 | — |
| `CONFIG.DUNGEON.DRAGON_HP` | Number constant: 21 | — |
| `CONFIG.DUNGEON.GOBLIN_XP` | Number constant: 30 | — |
| `CONFIG.DUNGEON.SKELETON_XP` | Number constant: 50 | — |
| `CONFIG.DUNGEON.DRAGON_XP` | Number constant: 80 | — |
| `GameFSM` | IIFE module | `window.GameFSM` |
| `GameFSM.getState()` | Function → string | — |
| `GameFSM.transition(next)` | Function → void, throws on illegal | — |
| `GameFSM.reset()` | Function → void, sets state to EXPLORE | — |
| `FloorConfig` | IIFE module | `window.FloorConfig` |
| `FloorConfig.getFloor(floorNumber)` | Function → FloorDef object | — |

<tasks>

<task type="auto">
  <name>Task 1: Extend CONFIG with CONFIG.DUNGEON sub-object</name>
  <files>math-lab.html</files>
  <read_first>
    Read math-lab.html lines 300–322 to see the existing CONFIG object structure.
    Read math-lab.html lines 630–636 to confirm the insertion point (after QuestionSelector closes, before DOMContentLoaded).
  </read_first>
  <action>
    After the closing brace of the CONFIG object (after line `MASTERY_WINDOW: 10` and its closing `};`), append a new block that assigns CONFIG.DUNGEON as a sub-object. Do NOT modify the existing CONFIG object literal — add CONFIG.DUNGEON as a separate statement after it.

    CONFIG.DUNGEON must contain exactly these keys (all required by downstream phases for named-constant access, satisfying COMB-03 and ENE-02):

    - PLAYER_HP: 100
    - DAMAGE_CORRECT: 3  (player deals this to enemy per correct answer — per D-01 from CONTEXT.md)
    - DAMAGE_WRONG: 8    (enemy deals this to player per wrong answer — per D-01 from CONTEXT.md)
    - GOBLIN_HP: 9       (Floor 1 enemy HP — 3 correct answers to kill, per COMB-03)
    - SKELETON_HP: 15    (Floor 2 enemy HP — 5 correct answers to kill, per COMB-03)
    - DRAGON_HP: 21      (Floor 3 enemy HP — 7 correct answers to kill, per COMB-03)
    - GOBLIN_XP: 30      (XP awarded on Goblin defeat)
    - SKELETON_XP: 50    (XP awarded on Skeleton defeat)
    - DRAGON_XP: 80      (XP awarded on Dragon defeat)
    - ROOMS_PER_FLOOR: 5 (5 rooms per floor: 1 entrance + 3 combat + 1 boss; used by Phase 5)
    - SAVE_KEY_V2: 'mathlab_save_v2'  (new localStorage key for v2 save format)

    Place this assignment statement directly after the closing `};` of the existing CONFIG object, before MODULE 2 (XpCalculator). Add a brief comment: "Dungeon Crawler constants — Phase 2 addition".
  </action>
  <verify>
    <automated>Open math-lab.html in browser and run in console: typeof CONFIG.DUNGEON === 'object' && CONFIG.DUNGEON.PLAYER_HP === 100 && CONFIG.DUNGEON.DAMAGE_CORRECT === 3 && CONFIG.DUNGEON.DAMAGE_WRONG === 8 && CONFIG.DUNGEON.GOBLIN_HP === 9 && CONFIG.DUNGEON.SKELETON_HP === 15 && CONFIG.DUNGEON.DRAGON_HP === 21</automated>
  </verify>
  <acceptance_criteria>
    - CONFIG.DUNGEON is a plain object (not null, not undefined)
    - All 11 keys listed above are present with the exact numeric or string values specified
    - Existing CONFIG keys (XP_EASY, XP_HARD, BASE_XP, SAVE_KEY, etc.) are UNCHANGED
    - No syntax errors — math-lab.html opens without console errors
    - CONFIG.DUNGEON.SAVE_KEY_V2 === 'mathlab_save_v2'
  </acceptance_criteria>
  <done>CONFIG.DUNGEON object present in math-lab.html with all 11 named constants at specified values; v1 CONFIG untouched; no browser console errors on load.</done>
</task>

<task type="auto">
  <name>Task 2: Implement GameFSM state machine (DC-02)</name>
  <files>math-lab.html</files>
  <read_first>
    Read math-lab.html lines 628–636 to find the exact insertion point: after the closing `})();` of QuestionSelector (MODULE 5), before `document.addEventListener('DOMContentLoaded', ...)`.
    Read 02-CONTEXT.md section "GameFSM State Machine" to confirm the 5 states and illegal-transition behavior.
  </read_first>
  <action>
    Insert MODULE 6 (GameFSM) as a closure-based IIFE immediately after QuestionSelector's closing `})();` and before the DOMContentLoaded listener. Follow the established IIFE pattern: `const GameFSM = (() => { ... return { publicApi }; })();`

    Internal state:
    - A private `let state = 'EXPLORE';` variable holding the current state string.

    Legal transition map (per D-01 from CONTEXT.md — exactly these and no others):
    - EXPLORE → COMBAT
    - COMBAT → LOOT
    - COMBAT → FLOOR_SUMMARY
    - COMBAT → DEAD
    - LOOT → COMBAT
    - LOOT → FLOOR_SUMMARY
    - FLOOR_SUMMARY → EXPLORE
    - FLOOR_SUMMARY → DEAD
    - DEAD → EXPLORE

    Implement this as a private `const TRANSITIONS` plain object where each key is a source state and the value is a Set (or array) of valid destination states. Example: `EXPLORE: ['COMBAT']`.

    Public API (three methods only):

    `transition(next)`: Validates that `next` is in TRANSITIONS[state]. If not, throws `new Error('[GameFSM] Illegal transition: ' + state + ' → ' + next)`. If valid, sets `state = next`.

    `getState()`: Returns the current state string.

    `reset()`: Sets `state = 'EXPLORE'` unconditionally.

    After the IIFE, expose on window: `window.GameFSM = GameFSM;`

    Add section comment: `/* MODULE 6: GameFSM — Dungeon state machine (Phase 2) */`
  </action>
  <verify>
    <automated>
      In browser console after opening math-lab.html:
      GameFSM.getState() === 'EXPLORE'
      GameFSM.transition('COMBAT'); GameFSM.getState() === 'COMBAT'
      try { GameFSM.transition('DEAD'); } catch(e) { e.message.includes('Illegal transition') }
      GameFSM.reset(); GameFSM.getState() === 'EXPLORE'
      window.GameFSM === GameFSM
    </automated>
  </verify>
  <acceptance_criteria>
    - GameFSM.getState() returns 'EXPLORE' on first call
    - GameFSM.transition('COMBAT') from EXPLORE succeeds silently; subsequent getState() returns 'COMBAT'
    - GameFSM.transition('DEAD') from EXPLORE throws Error with '[GameFSM] Illegal transition' in message
    - All 9 legal transitions succeed without throwing
    - GameFSM.reset() always returns state to 'EXPLORE' regardless of current state
    - window.GameFSM is the same object reference as GameFSM
    - No new DOM elements created; no existing modules touched
  </acceptance_criteria>
  <done>GameFSM IIFE present in math-lab.html; all state machine assertions pass in browser console; window.GameFSM accessible.</done>
</task>

<task type="auto">
  <name>Task 3: Implement FloorConfig module (ENE-01, ENE-02, DIFF-01)</name>
  <files>math-lab.html</files>
  <read_first>
    Read math-lab.html (the GameFSM block just written) to confirm insertion point after GameFSM's window export line.
    Read 02-CONTEXT.md "FloorConfig table pools" section to confirm: Floor 1 ×2×3×5, Floor 2 ×4×6×7, Floor 3 ×7×8×9.
  </read_first>
  <action>
    Insert MODULE 7 (FloorConfig) as a closure-based IIFE immediately after `window.GameFSM = GameFSM;`, before DOMContentLoaded. Follow the IIFE pattern.

    Internal data: a private `const FLOORS` object keyed by floor number (1, 2, 3) with this exact structure for each floor:

    Floor 1: { enemy: 'Goblin', emoji: '👺', hp: CONFIG.DUNGEON.GOBLIN_HP, xpReward: CONFIG.DUNGEON.GOBLIN_XP, tablePools: [2, 3, 5] }
    Floor 2: { enemy: 'Skeleton', emoji: '💀', hp: CONFIG.DUNGEON.SKELETON_HP, xpReward: CONFIG.DUNGEON.SKELETON_XP, tablePools: [4, 6, 7] }
    Floor 3: { enemy: 'Dragon', emoji: '🐉', hp: CONFIG.DUNGEON.DRAGON_HP, xpReward: CONFIG.DUNGEON.DRAGON_XP, tablePools: [7, 8, 9] }

    All HP and XP values reference CONFIG.DUNGEON constants (no magic numbers in FloorConfig). The tablePools arrays satisfy DIFF-01 (distinct table pools per floor) and are ordered easy-to-hard within each floor.

    Public API (two methods):

    `getFloor(floorNumber)`: Returns a shallow copy of FLOORS[floorNumber] (use Object.assign({}, ...) with tablePools: [...arr] for the array). Throws `new Error('[FloorConfig] Unknown floor: ' + floorNumber)` if floorNumber is not 1, 2, or 3.

    `getAllFloors()`: Returns an array of all floor definitions (for console inspection). Each entry includes the floor number as a `floor` property: `[{ floor: 1, ...FLOORS[1] }, ...]`.

    After the IIFE, expose: `window.FloorConfig = FloorConfig;`

    Add section comment: `/* MODULE 7: FloorConfig — Enemy definitions per floor (Phase 2) */`
  </action>
  <verify>
    <automated>
      In browser console:
      const f1 = FloorConfig.getFloor(1); f1.enemy === 'Goblin' && f1.emoji === '👺' && f1.hp === 9 && f1.xpReward === 30 && JSON.stringify(f1.tablePools) === '[2,3,5]'
      const f2 = FloorConfig.getFloor(2); f2.enemy === 'Skeleton' && f2.hp === 15 && JSON.stringify(f2.tablePools) === '[4,6,7]'
      const f3 = FloorConfig.getFloor(3); f3.enemy === 'Dragon' && f3.emoji === '🐉' && f3.hp === 21 && JSON.stringify(f3.tablePools) === '[7,8,9]'
      try { FloorConfig.getFloor(4); } catch(e) { e.message.includes('Unknown floor') }
      window.FloorConfig === FloorConfig
    </automated>
  </verify>
  <acceptance_criteria>
    - FloorConfig.getFloor(1) returns object with enemy: 'Goblin', emoji: '👺', hp: 9, xpReward: 30, tablePools: [2,3,5]
    - FloorConfig.getFloor(2) returns Skeleton, hp: 15, xpReward: 50, tablePools: [4,6,7]
    - FloorConfig.getFloor(3) returns Dragon, emoji: '🐉', hp: 21, xpReward: 80, tablePools: [7,8,9]
    - getFloor() returns a copy — mutating the returned object does not affect internal FLOORS data
    - FloorConfig.getFloor(4) throws an Error with 'Unknown floor' in message
    - FloorConfig.getAllFloors() returns array of length 3 with floor property on each entry
    - All HP values are read from CONFIG.DUNGEON constants — no literal numbers 9, 15, or 21 appear inside the FloorConfig IIFE body
    - window.FloorConfig accessible in console
  </acceptance_criteria>
  <done>FloorConfig IIFE present in math-lab.html; all three floor definitions verified in browser console; window.FloorConfig accessible; no magic numbers inside FloorConfig body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser console → GameFSM | Developer calls transition() from DevTools; state value is user-controlled |
| localStorage → CONFIG | No threat; CONFIG.DUNGEON constants are code-level, not read from storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | GameFSM.transition() | mitigate | Validate next state against TRANSITIONS whitelist; throw on unknown state names (prevents injected state strings from reaching app logic) |
| T-02-02 | Information Disclosure | window.GameFSM / window.FloorConfig | accept | DevTools-level exposure; no PII, no secrets; mirrors existing window.debugAccuracy pattern; acceptable for local-only app |
| T-02-03 | Spoofing | FloorConfig.getFloor() return value | mitigate | Return shallow copies (Object.assign), not references to internal FLOORS — prevents caller from mutating internal state |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; single HTML file, no package manager |
</threat_model>

<verification>
After all three tasks complete, verify end-to-end in browser console:

```
// State machine
GameFSM.getState()              // → 'EXPLORE'
GameFSM.transition('COMBAT')
GameFSM.getState()              // → 'COMBAT'
GameFSM.transition('LOOT')
GameFSM.getState()              // → 'LOOT'
GameFSM.transition('FLOOR_SUMMARY')
GameFSM.getState()              // → 'FLOOR_SUMMARY'
GameFSM.transition('EXPLORE')
GameFSM.getState()              // → 'EXPLORE'
GameFSM.reset()

// Floor config
FloorConfig.getAllFloors().length  // → 3
FloorConfig.getFloor(1).hp        // → 9 (CONFIG.DUNGEON.GOBLIN_HP)
FloorConfig.getFloor(2).hp        // → 15
FloorConfig.getFloor(3).hp        // → 21

// CONFIG.DUNGEON
CONFIG.DUNGEON.DAMAGE_CORRECT     // → 3
CONFIG.DUNGEON.DAMAGE_WRONG       // → 8
CONFIG.DUNGEON.SAVE_KEY_V2        // → 'mathlab_save_v2'

// v1 CONFIG untouched
CONFIG.SAVE_KEY                   // → 'mathlab_save_v1'
CONFIG.XP_EASY                    // → 10
```
</verification>

<success_criteria>
- math-lab.html opens in browser with zero console errors
- CONFIG.DUNGEON has all 11 named constants at specified values
- GameFSM enforces 9 legal transitions and rejects all others with descriptive Error
- FloorConfig returns correct enemy definitions for floors 1–3 with copies, not references
- window.GameFSM and window.FloorConfig exposed for Plan 02 and console testing
- Existing v1 modules (CONFIG, XpCalculator, PlayerState, PersistenceStore, QuestionSelector) are unmodified
</success_criteria>

<output>
Create `.planning/phases/02-combat-foundation/02-01-SUMMARY.md` when done
</output>
