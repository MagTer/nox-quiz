---
phase: 35-biome-re-dress-props
plan: 07
type: execute
wave: 5
depends_on: ["35-05"]
files_modified:
  - src/levels/level-07.js
  - src/levels/level-08.js
autonomous: true
requirements: [ART-06, ART-07]
must_haves:
  truths:
    - "level-07 and level-08 (castle) each carry a restrained top-level `props` array using the baked castle sprites, dressed to the plan-03-approved approach"
    - "Both levels' geometry objects are byte-identical to the frozen baseline"
    - "Both levels boot with props under the object/FPS budget; level-07 (horizontal ramparts, bounds.top 0) gets a spawn shot only, level-08 (vertical castle climb, bounds.top −720) gets spawn AND climb-altitude shots"
  artifacts:
    - "src/levels/level-07.js + src/levels/level-08.js — the castle `props` arrays"
    - ".planning/phases/35-biome-re-dress-props/prop-shots/ — level-07 spawn + level-08 spawn + climb screenshots"
  key_links:
    - "baked prop-castle-* keys → the two castle descriptor props arrays"
---

<objective>
Dress the two castle levels — level-07 ("The Ramparts", a HORIZONTAL lateral wall-walk, bounds.top 0) and level-08 (castle switchback, the final level and the tallest vertical climb, bounds.top −720) — with a restrained castle props layer (column/torches/banners/chains) using the sprites baked in plan 05, to the plan-03-approved density/placement discipline.

Purpose: castle is the thinnest-vocabulary biome, so props must stay legible and non-obscuring across both a lateral wall-walk (07) and a tall climb (08). Only level-08 is vertical (bounds.top −720), so ONLY level-08 needs a climb-altitude screenshot — the parallax "black mess" was worst at climb altitude, and level-08's props must read all the way up; level-07 is flat (bounds.top 0) and gets a spawn shot only, like level-01/03/05. Runs in parallel with plan 06 (town) — different descriptors.

HARD constraints: geometry BYTE-FROZEN (top-level `props` only); RESTRAINED / legibility-first (never obscure route/coins/hazards/door/enemy/goal); background-depth or on-surface only, NO foreground over the play lane; on-surface `y = surfaceY − spriteHeight`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-biome-re-dress-props/35-CONTEXT.md
@.planning/phases/35-biome-re-dress-props/35-RESEARCH.md
@.planning/phases/35-biome-re-dress-props/35-PATTERNS.md
@.planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md
@.planning/phases/35-biome-re-dress-props/35-05-SUMMARY.md
@src/levels/level-07.js
@src/levels/level-08.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Place the castle props array on level-07 ("The Ramparts" — horizontal wall-walk)</name>
  <files>src/levels/level-07.js</files>
  <read_first>
    - src/levels/level-07.js — the `geometry` object. NOTE: level-07 is HORIZONTAL — `bounds.top` is 0 (the whole lateral wall-walk stays within the 360px screen, highest point y:122); it is NOT a tall climb. The path is the WIDE elevated parapet segments (WALK1..WALK4), merlons, the drawbridge, the tower-side fork, and the final tower. DOOR (W1 @380) / ENEMY (W3 @3700) / spikes / coins / GOAL (tower top @5580) and the top-level mechanics/biome/parallax slots.
    - .planning/phases/35-biome-re-dress-props/35-05-SUMMARY.md — which castle props exist (tileset crops vs new CC0) and their pixel heights.
    - .planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md — the approved density template; 35-RESEARCH.md §"Pattern 1", Pitfalls 1/2/4.
  </read_first>
  <action>
    Append a top-level `props: [...]` to LEVEL_07 (sibling of mechanics/biome/parallax) using the baked `prop-castle-*` keys — geometry untouched. Restrained castle dressing: columns and wall torches/banners at `layer:"back"` for depth against the battlement walls, a few on-surface accents at `layer:"surface"` on the wide parapet ledges. Keep props OFF the wall-walk lane (the WALK/merlon/drawbridge/fork/tower path) and clear of the door/enemy/spikes/coins/goal. On-surface `y = surfaceY − spriteHeight`. Because level-07 is a lateral traverse (bounds.top 0, no tall shaft), place back props as intentional wall/battlement dressing along the horizontal run — not floating clutter high in a shaft.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-assets-manifest.mjs && node scripts/check-geometry-frozen.mjs</automated>
  </verify>
  <acceptance_criteria>
    - LEVEL_07 has a top-level `props` array (declared `prop-castle-*` keys only); geometry byte-identical to baseline.
    - validate-levels 0 HARD-FAIL; no prop on the wall-walk lane, a hazard, coin, mechanic, or goal.
  </acceptance_criteria>
  <done>level-07 dressed to the approved castle approach; geometry frozen; gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Place the castle props array on level-08 (final vertical switchback) + capture screenshots</name>
  <files>src/levels/level-08.js</files>
  <read_first>
    - src/levels/level-08.js — the `geometry` object. NOTE: level-08 IS the tall vertical climb — `bounds.top` is −720; the h:16 climb tiers have tight headroom (props must not visually crowd the switchback slots) and the top-level slots.
    - scripts/screenshot-phase35-props.mjs — the climb-altitude capture fires for any level with `bounds.top < 0` (the vertical levels 02/04/06/08); level-07 (bounds.top 0) yields a spawn shot only.
    - .planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md + 35-05-SUMMARY.md.
  </read_first>
  <action>
    Append a top-level `props: [...]` to LEVEL_08 using `prop-castle-*` keys — geometry untouched. Restrained, off the switchback climb lane and the tight-headroom tiers, clear of door/enemy/spikes/coins/goal. On-surface `y = surfaceY − spriteHeight`. Then run `node scripts/screenshot-phase35-props.mjs level-07 level-08` to capture the castle screenshots into prop-shots/ — level-07 (bounds.top 0) emits a spawn shot only, level-08 (bounds.top −720) emits spawn AND climb shots — and run browser-boot to confirm both stay budget-safe with props.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs && node scripts/screenshot-phase35-props.mjs level-07 level-08 && ls .planning/phases/35-biome-re-dress-props/prop-shots/level-08-castle-climb.png</automated>
  </verify>
  <acceptance_criteria>
    - LEVEL_08 has a top-level `props` array (declared `prop-castle-*` keys only); geometry byte-identical to baseline.
    - browser-boot green: level-07 + level-08 boot, reach goal, under OBJECT_BUDGET / above FPS_FLOOR with props.
    - prop-shots/ contains a level-07 spawn shot, and level-08 spawn AND climb shots (no level-07 climb shot — level-07 is horizontal).
  </acceptance_criteria>
  <done>level-08 dressed; level-07 spawn + level-08 spawn+climb screenshots captured; both budget-safe; geometry frozen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor data → build.js | In-repo authored data; validate-levels + check-geometry-frozen coverage |

Static offline game; placement-only.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-10 | Tampering | geometry drift under an art commit | medium | mitigate | check-geometry-frozen.mjs per descriptor edit |
| T-35-11 | Information Disclosure | props illegible/floating at climb altitude (the "black mess" class) | medium | mitigate | Climb-altitude screenshot for level-08 (the vertical castle level, bounds.top −720); level-07 back props anchored as horizontal wall/battlement dressing |
| T-35-SC | Tampering | package installs | n/a | accept | Zero new dependencies; reuses baked castle art |
</threat_model>

<verification>
```
node scripts/validate-levels.mjs && node scripts/check-assets-manifest.mjs \
  && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs \
  && node scripts/screenshot-phase35-props.mjs level-07 level-08
```
</verification>

<success_criteria>
- level-07 + level-08 dressed to the approved castle approach; budget-safe.
- Geometry byte-frozen; validator-neutral; level-07 spawn shot + level-08 spawn+climb shots captured (all 8 levels now dressed).
</success_criteria>

<output>
Create `.planning/phases/35-biome-re-dress-props/35-07-SUMMARY.md` when done.
</output>
