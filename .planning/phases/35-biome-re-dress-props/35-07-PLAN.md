---
phase: 35-biome-re-dress-props
plan: 07
type: execute
wave: 5
depends_on: ["35-05"]
files_modified:
  - src/levels/level-07.js
  - src/levels/level-08.js
autonomous: true
requirements: [ART-06, ART-07]
must_haves:
  truths:
    - "level-07 and level-08 (castle) each carry a restrained top-level `props` array using the baked castle sprites, dressed to the plan-03-approved approach"
    - "Both levels' geometry objects are byte-identical to the frozen baseline"
    - "Both levels boot with props under the object/FPS budget; spawn AND climb-altitude screenshots captured (these are the tall bounds.top −360 climbs)"
  artifacts:
    - "src/levels/level-07.js + src/levels/level-08.js — the castle `props` arrays"
    - ".planning/phases/35-biome-re-dress-props/prop-shots/ — level-07 + level-08 spawn + climb screenshots"
  key_links:
    - "baked prop-castle-* keys → the two castle descriptor props arrays"
---

<objective>
Dress the two castle levels — level-07 (castle staircase) and level-08 (castle switchback, the final level) — with a restrained castle props layer (column/torches/banners/chains) using the sprites baked in plan 05, to the plan-03-approved density/placement discipline.

Purpose: castle is the thinnest-vocabulary biome and the tallest climbs (bounds.top −360), so this plan needs BOTH spawn and climb-altitude screenshots — the parallax "black mess" was worst at climb altitude (levels 07/08), and props must remain legible and non-obscuring all the way up. Runs in parallel with plan 06 (town) — different descriptors.

HARD constraints: geometry BYTE-FROZEN (top-level `props` only); RESTRAINED / legibility-first (never obscure route/coins/hazards/door/enemy/goal); background-depth or on-surface only, NO foreground over the play lane; on-surface `y = surfaceY − spriteHeight`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-biome-re-dress-props/35-CONTEXT.md
@.planning/phases/35-biome-re-dress-props/35-RESEARCH.md
@.planning/phases/35-biome-re-dress-props/35-PATTERNS.md
@.planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md
@.planning/phases/35-biome-re-dress-props/35-05-SUMMARY.md
@src/levels/level-07.js
@src/levels/level-08.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Place the castle props array on level-07 (staircase climb)</name>
  <files>src/levels/level-07.js</files>
  <read_first>
    - src/levels/level-07.js — the `geometry` object (the staircase end-climb, tall `bounds.top` −360; DOOR/ENEMY/spikes/coins/GOAL) and the top-level mechanics/biome/parallax slots.
    - .planning/phases/35-biome-re-dress-props/35-05-SUMMARY.md — which castle props exist (tileset crops vs new CC0) and their pixel heights.
    - .planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md — the approved density template; 35-RESEARCH.md §"Pattern 1", Pitfalls 1/2/4.
  </read_first>
  <action>
    Append a top-level `props: [...]` to LEVEL_07 (sibling of mechanics/biome/parallax) using the baked `prop-castle-*` keys — geometry untouched. Restrained castle dressing: columns and wall torches/banners at `layer:"back"` for depth against the interior walls, a few on-surface accents at `layer:"surface"` on wide ledges. Keep props OFF the staircase climb lane and clear of the door/enemy/spikes/coins/goal. On-surface `y = surfaceY − spriteHeight`. Because the camera climbs to bounds.top −360, ensure back props placed high in the shaft read as intentional wall dressing, not floating clutter.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-assets-manifest.mjs && node scripts/check-geometry-frozen.mjs</automated>
  </verify>
  <acceptance_criteria>
    - LEVEL_07 has a top-level `props` array (declared `prop-castle-*` keys only); geometry byte-identical to baseline.
    - validate-levels 0 HARD-FAIL; no prop on the climb lane, a hazard, coin, mechanic, or goal.
  </acceptance_criteria>
  <done>level-07 dressed to the approved castle approach; geometry frozen; gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Place the castle props array on level-08 (final switchback) + capture spawn+climb screenshots</name>
  <files>src/levels/level-08.js</files>
  <read_first>
    - src/levels/level-08.js — the `geometry` object (the final switchback, tall bounds.top; the h:16 climb tiers with tight headroom — props must not visually crowd the switchback slots) and the top-level slots.
    - scripts/screenshot-phase35-props.mjs — the climb-altitude capture for vertical even levels.
    - .planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md + 35-05-SUMMARY.md.
  </read_first>
  <action>
    Append a top-level `props: [...]` to LEVEL_08 using `prop-castle-*` keys — geometry untouched. Restrained, off the switchback climb lane and the tight-headroom tiers, clear of door/enemy/spikes/coins/goal. On-surface `y = surfaceY − spriteHeight`. Then run `node scripts/screenshot-phase35-props.mjs level-07 level-08` to capture spawn AND climb shots for both castle levels (both are tall climbs) into prop-shots/, and run browser-boot to confirm both stay budget-safe with props.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs && node scripts/screenshot-phase35-props.mjs level-07 level-08 && ls .planning/phases/35-biome-re-dress-props/prop-shots/level-08-castle-climb.png</automated>
  </verify>
  <acceptance_criteria>
    - LEVEL_08 has a top-level `props` array (declared `prop-castle-*` keys only); geometry byte-identical to baseline.
    - browser-boot green: level-07 + level-08 boot, reach goal, under OBJECT_BUDGET / above FPS_FLOOR with props.
    - prop-shots/ contains level-07 and level-08 spawn AND climb screenshots.
  </acceptance_criteria>
  <done>level-08 dressed; castle spawn+climb screenshots captured; both budget-safe; geometry frozen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor data → build.js | In-repo authored data; validate-levels + check-geometry-frozen coverage |

Static offline game; placement-only.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-10 | Tampering | geometry drift under an art commit | medium | mitigate | check-geometry-frozen.mjs per descriptor edit |
| T-35-11 | Information Disclosure | props illegible/floating at climb altitude (the "black mess" class) | medium | mitigate | Climb-altitude screenshots for both castle levels; back props anchored as wall dressing |
| T-35-SC | Tampering | package installs | n/a | accept | Zero new dependencies; reuses baked castle art |
</threat_model>

<verification>
```
node scripts/validate-levels.mjs && node scripts/check-assets-manifest.mjs \
  && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs \
  && node scripts/screenshot-phase35-props.mjs level-07 level-08
```
</verification>

<success_criteria>
- level-07 + level-08 dressed to the approved castle approach; budget-safe.
- Geometry byte-frozen; validator-neutral; spawn+climb screenshots captured (all 8 levels now dressed).
</success_criteria>

<output>
Create `.planning/phases/35-biome-re-dress-props/35-07-SUMMARY.md` when done.
</output>
