---
phase: 35-biome-re-dress-props
plan: 08
type: execute
wave: 6
depends_on: ["35-04", "35-06", "35-07"]
files_modified:
  - scripts/build-art-assets.py
  - src/main.js
  - src/levels/build.js
  - scripts/screenshot-phase26.mjs
  - src/fx.js
autonomous: true
requirements: [ART-06, ART-07]
must_haves:
  truths:
    - "The dead pre-Phase-32 tint-theme bake code is deleted; re-running the bake produces NO *theme*.png files, and the LIVE biome bake path is untouched"
    - "The three stale theme-N comments (main.js, build.js, screenshot-phase26.mjs) are reworded to reflect the biome path; check-contrast.mjs and the biome path still work"
    - "The coin-collect pop() in fx.js no longer reads as a missing-sprite placeholder — it is restyled to a dark-grunge accent (no pink, no timers)"
    - "All 8 levels are dressed; the full gate suite + validator + frozen-geometry gate are green; per-level spawn (+ climb) screenshots exist for a spot-check"
  artifacts:
    - "scripts/build-art-assets.py — dead theme-N functions + __main__ loop removed"
    - "src/main.js + src/levels/build.js + scripts/screenshot-phase26.mjs — reworded stale comments"
    - "src/fx.js — restyled coin-collect pop()"
    - ".planning/phases/35-biome-re-dress-props/prop-shots/ — all-8 screenshots for the spot-check"
  key_links:
    - "delete THEME_PALETTES loop (1476–1478) + build_ground_theme/build_parallax_theme/_THEME_ACCENTS → no orphaned theme PNGs regenerate"
    - "pop() → CONFIG.PALETTE.REWARD accent → self-cleaning tween().onEnd(destroy) (no scheduler)"
---

<objective>
Close Phase 35: delete the dead pre-Phase-32 tint-theme bake code (so a future bake can't re-litter `assets/` with orphaned theme PNGs), restyle the deferred coin-collect pop() so it stops reading as a missing-sprite placeholder, and prove the whole phase green — full gate suite + validator + geometry-freeze + all-8 screenshots.

Purpose: The theme-N cleanup is a locked CONTEXT deliverable and Pitfall 5 (a future bake regenerating dead `ground-theme-*.png` / `{far,mid,near}-theme-*.png`). The coin-pop restyle is the deferred item from quick 260717-j24 folded into this art pass (recorded in 35-CONTEXT.md Deferred Ideas). The final green suite + spot-check screenshots are the phase's closeout evidence before `/gsd-verify-work`.

HARD constraints: do NOT touch the LIVE biome bake path (`build_biome_atlas_*`, `build_biome_parallax_*`, `_bake_biome_atlas`, `build_parallax`, `build_ground`); do NOT delete `CONFIG.PALETTE.ACCENT_*` (dead for render but referenced by check-contrast.mjs — out of scope); geometry stays byte-frozen; no timers/punishment in the fx restyle (tween().onEnd self-clean only); no pink.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-biome-re-dress-props/35-CONTEXT.md
@.planning/phases/35-biome-re-dress-props/35-RESEARCH.md
@.planning/phases/35-biome-re-dress-props/35-PATTERNS.md
@.planning/research/ART-PARITY-STEERING.md
@scripts/build-art-assets.py
@src/fx.js
@src/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the dead tint-theme bake code + rework the three stale theme-N comments</name>
  <files>scripts/build-art-assets.py, src/main.js, src/levels/build.js, scripts/screenshot-phase26.mjs</files>
  <read_first>
    - .planning/phases/35-biome-re-dress-props/35-RESEARCH.md — §"Old Tint-Theme Cleanup (Q5 — dead vs. live, verified)": DEAD = build-art-assets.py `__main__` lines 1476–1478 (the `for theme_id, palette in THEME_PALETTES.items(): build_ground_theme(...); build_parallax_theme(...)` loop) + `build_ground_theme` (@230), `build_parallax_theme` (@508), `_accent_sub` (@305), `_mid_accent_sub` (@330), `_near_accent_sub` (@383), `_THEME_ACCENTS` (@434), `THEME_PALETTES` (@445). LIVE (do NOT touch) = `build_biome_atlas_*`, `build_biome_parallax_*`, `_bake_biome_atlas` (@745), `build_parallax` (@465), `build_ground` (@202). BORDERLINE (leave alone) = `CONFIG.PALETTE.ACCENT_*` (referenced by check-contrast.mjs).
    - .planning/phases/35-biome-re-dress-props/35-PATTERNS.md — the build-art-assets.py delete list + the stale comments at src/main.js:103, src/levels/build.js:79, scripts/screenshot-phase26.mjs:171.
    - .planning/research/ART-PARITY-STEERING.md — the re-bake-and-look discipline (Pitfall 5: confirm no theme PNGs reappear).
  </read_first>
  <action>
    In scripts/build-art-assets.py delete the dead theme-N bake code IN ONE COMMIT: the `__main__` loop lines that call `build_ground_theme`/`build_parallax_theme` over `THEME_PALETTES`, and the now-unused defs `build_ground_theme`, `build_parallax_theme`, `_accent_sub`, `_mid_accent_sub`, `_near_accent_sub`, `_THEME_ACCENTS`, `THEME_PALETTES`. Confirm each has no remaining caller (grep) before deleting. Do NOT touch any `build_biome_*` / `_bake_biome_atlas` / `build_parallax` / `build_ground` / `bake_prop` function — those are the LIVE biome + props path.

    Rework the three stale theme-N COMMENTS (code is live, only the wording is stale): src/main.js:103 ("old hand-written per-theme-N block"), src/levels/build.js:79 ("old theme-aware groundSprite ternary"), scripts/screenshot-phase26.mjs:171 ("theme-tinted ground/parallax") — reword each to describe the current per-biome atlas/parallax path, not the deleted theme system. Do NOT change the live code those comments sit above.

    Re-run the bake and confirm NO `*theme*.png` file appears in `assets/` (Pitfall 5), then re-run the pixel gate.
  </action>
  <verify>
    <automated>python3 scripts/build-art-assets.py && bash scripts/check-terrain-atlas.sh && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs && test -z "$(find assets -name '*theme*' -not -path '*/_*-src/*')" && echo NO-THEME-PNGS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'build_ground_theme\|build_parallax_theme\|THEME_PALETTES\|_THEME_ACCENTS' scripts/build-art-assets.py` returns nothing (all deleted).
    - After a bake, `find assets -name '*theme*' -not -path '*/_*-src/*'` is empty (no orphaned theme PNGs).
    - check-terrain-atlas green (LIVE biome bake untouched); check-assets-manifest + validate-levels green (biome + props path intact).
    - The three stale comments no longer say "theme-N" and describe the biome path; `node scripts/check-contrast.mjs` still passes (ACCENT_* untouched).
  </acceptance_criteria>
  <done>Dead theme-N bake code is gone (no regenerating orphans), stale comments reflect the biome path, and every LIVE bake/render path is intact and green.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the coin-collect pop() (dark-grunge accent, no pink, no timers)</name>
  <files>src/fx.js</files>
  <read_first>
    - src/fx.js — `pop(at)` (lines ~180–205): the current `rect(F.POP_SIZE, F.POP_SIZE)` + `color(CONFIG.PALETTE.REWARD...)` + scale-pop + easeOutQuad tween → `.onEnd(destroy)`. Also read `clearBurst` (lines ~208+) and `popupText` (~145) for the established juice idioms + the `z(60)` "fx" tag.
    - src/config.js — `CONFIG.PALETTE.REWARD` (line 32: [0x00,0xff,0x88] neon-green) and `CONFIG.FX` (lines 319+: POP_SIZE 9, POP_SCALE 1.5, POP_MS 220).
    - .planning/phases/35-biome-re-dress-props/35-CONTEXT.md — Deferred Ideas: "restyle the neon-green coin-pickup pop() in src/fx.js — it currently reads as a missing-sprite placeholder; re-dress during the art pass."
  </read_first>
  <action>
    Restyle `pop()` so the coin-collect feedback reads as an intentional dark-grunge collect flourish rather than a flat neon rectangle that looks like a missing sprite. Keep it a self-contained fx change: replace the bare `rect()` marker with a more legible collect mark — e.g. a small ring/spark burst or a softened multi-particle pop using the existing juice primitives (mirror `clearBurst`/`popupText` shapes), tuned via CONFIG.FX tunables (add new tunables to CONFIG.FX if needed — no magic numbers in fx.js). Keep the accent color sourced from `CONFIG.PALETTE.REWARD` (or another non-pink grunge accent already in CONFIG.PALETTE) — NO pink. Keep it a single self-cleaning `tween().onEnd(() => destroy(...))` — NO setTimeout/wait/loop/lifespan (check-safety). Keep the `z(60)` "fx" tag and the world-space `at` argument contract unchanged so the coin-collect call site needs no change. Reference engine globals only inside the function body (a727c13).
  </action>
  <verify>
    <automated>bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-pink-gate.sh && node scripts/browser-boot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `pop()` no longer renders a bare flat rectangle; it uses a restyled collect flourish via existing juice primitives + CONFIG tunables.
    - check-safety green (single tween().onEnd self-clean, no scheduler/punishment); check-import-safety green (no top-level engine globals).
    - check-pink-gate green (accent is non-pink); browser-boot green (coins still collect, fx fires, no console errors).
    - The `pop(at)` signature + "fx" tag + z are unchanged (call site untouched).
  </acceptance_criteria>
  <done>The coin-collect pop reads as an intentional dark-grunge accent, no longer a missing-sprite placeholder; safety/pink/boot green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite closeout — all-8 gates + frozen-geometry proof + spot-check screenshots</name>
  <files>scripts/screenshot-phase35-props.mjs</files>
  <read_first>
    - .planning/phases/35-biome-re-dress-props/35-VALIDATION.md — the full-suite command + the sign-off checklist.
    - scripts/screenshot-phase35-props.mjs — run with no args to shoot all 8 levels (spawn + climb for evens).
    - .planning/research/ART-PARITY-STEERING.md — "any task that touches level visuals MUST end with in-engine screenshots compared side-by-side against the board" (spot-check evidence for the remaining-6, per RESEARCH Sampling Rate).
  </read_first>
  <action>
    Run the full verification suite green across all 8 dressed levels and capture the final all-8 screenshots for the remaining-6 spot-check. Do NOT modify any descriptor here — this task is the closeout gate; if anything fails, fix in the owning plan's file (report which). Run `node scripts/screenshot-phase35-props.mjs` (no args) to (re)capture all 8 levels' spawn shots + the vertical evens' climb shots into prop-shots/, for a side-by-side spot-check against the boards before `/gsd-verify-work`. Confirm the frozen-geometry gate is green (all 8 geometries still byte-identical to the post-34.6 baseline) — the definitive proof that the entire re-dress touched zero geometry.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-assets-manifest.mjs && bash scripts/check-terrain-atlas.sh && bash scripts/check-pink-gate.sh && bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs && node scripts/screenshot-phase35-props.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The full gate suite (validate-levels, check-assets-manifest, check-terrain-atlas, check-pink-gate, check-gate, check-safety, check-import-safety, check-progress) is green.
    - check-geometry-frozen green: all 8 levels' geometry byte-identical to the post-34.6 baseline (the byte-frozen proof).
    - browser-boot green across all 8 dressed levels (object budget + FPS floor).
    - prop-shots/ contains spawn shots for all 8 levels + climb shots for 02/04/06/07/08.
  </acceptance_criteria>
  <done>Phase 35 is provably green: all 8 levels dressed, geometry byte-frozen, dead theme code gone, coin-pop restyled, and the full suite + all-8 screenshots confirm it — ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-art-assets.py bake → assets/ | Deleting dead code must not regenerate orphans or touch the live path; terrain-atlas + manifest gates cover it |
| fx.js render | Session-only visual; no persistence/input; check-safety/pink cover it |

Static offline game; no new input surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-12 | Tampering | deleting theme code also breaks the live biome bake | high | mitigate | Grep-confirm no callers before delete; live `build_biome_*` untouched; re-bake + check-terrain-atlas green; no theme PNGs reappear |
| T-35-13 | Tampering | ACCENT_* deletion breaks check-contrast.mjs | low | accept | ACCENT_* explicitly out of scope, left untouched (RESEARCH A4) |
| T-35-14 | DoS (self) | coin-pop fx introduces a scheduler/strobe | low | mitigate | Single tween().onEnd self-clean; check-safety enforces no scheduler/punishment |
| T-35-SC | Tampering | package installs | n/a | accept | Zero new dependencies |
</threat_model>

<verification>
Full suite (VALIDATION.md):
```
node scripts/validate-levels.mjs && node scripts/check-assets-manifest.mjs \
  && bash scripts/check-terrain-atlas.sh && bash scripts/check-pink-gate.sh \
  && bash scripts/check-gate.sh && bash scripts/check-safety.sh \
  && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh \
  && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs \
  && node scripts/screenshot-phase35-props.mjs
```
Plus: `find assets -name '*theme*' -not -path '*/_*-src/*'` is empty; `node scripts/check-contrast.mjs` green.
</verification>

<success_criteria>
- Dead theme-N bake code removed; no orphaned theme PNGs regenerate; live biome path + check-contrast intact.
- Coin-collect pop() restyled (dark-grunge, no pink, no timers); safety/pink/boot green.
- All 8 levels dressed; full suite + frozen-geometry gate green; all-8 spot-check screenshots captured.
</success_criteria>

<output>
Create `.planning/phases/35-biome-re-dress-props/35-08-SUMMARY.md` when done — attach/reference the all-8 spot-check screenshots.
</output>
