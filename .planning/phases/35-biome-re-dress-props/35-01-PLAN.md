---
phase: 35-biome-re-dress-props
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.js
  - src/levels/build.js
  - src/main.js
  - scripts/check-geometry-frozen.mjs
  - scripts/fixtures/geometry-frozen-baseline.json
  - scripts/screenshot-phase35-props.mjs
autonomous: true
requirements: [ART-06, ART-07]
must_haves:
  truths:
    - "A descriptor's top-level `props: [{ sprite, x, y, layer }]` renders as sprite entities behind the player and behind every z(0) gameplay entity (coins/terrain/mechanics), never occluding them"
    - "Prop entities carry NO area() and NO body() — validate-levels.mjs output is byte-identical before and after props exist (validator-neutral by construction)"
    - "A levels with no `props` field still builds (the `?? []` guard) — the 6 not-yet-dressed levels are unaffected"
    - "check-geometry-frozen.mjs HARD-FAILs the moment any level's geometry object drifts from the post-34.6 baseline, naming the level + key"
    - "screenshot-phase35-props.mjs boots the real engine and captures a per-level spawn shot (plus a climb-altitude shot on vertical even levels) into prop-shots/"
  artifacts:
    - "src/config.js — CONFIG.PROPS.{Z_BACK:-8, Z_SURFACE:-3} tunable block"
    - "src/levels/build.js — the guarded `for (const pr of levelData.props ?? [])` emit loop (sprite+pos+z ONLY)"
    - "src/main.js — `kind === 'prop'` branch in the ASSETS_MANIFEST load loop"
    - "scripts/check-geometry-frozen.mjs + scripts/fixtures/geometry-frozen-baseline.json — the snapshot freeze gate"
    - "scripts/screenshot-phase35-props.mjs — the trial/spot-check evidence capture script"
  key_links:
    - "level descriptor.props (TOP-LEVEL) → build.js prop loop → add([sprite, pos, z(CONFIG.PROPS.Z_*)]) with no collider"
    - "assets-manifest kind:'prop' → main.js load branch → loadSprite(key, webPath)"
    - "getLevel(id).geometry → JSON.stringify → compare vs geometry-frozen-baseline.json"
---

<objective>
Build the props INFRASTRUCTURE the whole phase stands on, with ZERO art and ZERO level dressing yet: the `props` renderer path (config z tunables + the collider-free `build.js` emit loop + the `main.js` manifest load branch), the geometry-freeze snapshot gate that proves geometry stays byte-identical through the re-dress, and the per-level screenshot script that feeds the human sign-off.

Purpose: Everything downstream (the 2-level trial, the remaining-6 rollout) depends on this seam existing and being provably validator-neutral + geometry-frozen. This plan is pure plumbing — no descriptor gains a `props` array here, so `validate-levels.mjs` and `check-geometry-frozen.mjs` must be byte-identical-green before and after.

Output (artifacts):
- `CONFIG.PROPS.{Z_BACK, Z_SURFACE}` (both NEGATIVE — props always render behind the z(0) player/coins/terrain and in front of PARALLAX.NEAR_Z −10).
- The guarded prop emit loop in `buildLevel` (sprite+pos+z ONLY — no `area()`, no `body()`, no `rect()`).
- The `kind === "prop"` branch in main.js's manifest load loop.
- `scripts/check-geometry-frozen.mjs` + `scripts/fixtures/geometry-frozen-baseline.json` (baseline captured from the current committed post-34.6 geometry).
- `scripts/screenshot-phase35-props.mjs` (cloned from the Phase-33 terrain screenshot script, copy-not-extract per the standing duplication convention).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/35-biome-re-dress-props/35-CONTEXT.md
@.planning/phases/35-biome-re-dress-props/35-RESEARCH.md
@.planning/phases/35-biome-re-dress-props/35-PATTERNS.md
@.planning/research/ART-PARITY-STEERING.md
@src/config.js
@src/levels/build.js
@src/main.js
@src/assets-manifest.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the props renderer path — CONFIG.PROPS z block + build.js emit loop + main.js load branch</name>
  <files>src/config.js, src/levels/build.js, src/main.js</files>
  <read_first>
    - src/config.js — the `PARALLAX` block (lines 94–100: FAR_Z −30/MID_Z −20/NEAR_Z −10, the only other negative-z tunables) and `TERRAIN.OBJECT_BUDGET`/`FPS_FLOOR` (lines 120/130). The new PROPS block sits adjacent to PARALLAX.
    - src/levels/build.js — `buildLevel(levelData)` (line 49), `const g = levelData.geometry` (line 76), and the optional-array emit loops that are the exact idiom analog: `g.secretAlcove ?? []` (line 453), `g.doors ?? []` (line 292), `g.coins` (line 238). Note coins carry `area()` — props must NOT.
    - src/main.js — the ASSETS_MANIFEST load loop (lines ~106–117: the `biome-atlas`/`biome-bg` branches). The new `prop` branch is appended here.
    - .planning/phases/35-biome-re-dress-props/35-RESEARCH.md — §"Pattern 2" (build.js loop), §"Pattern 3" (config z), §"Loading props via the manifest loop".
    - .planning/phases/35-biome-re-dress-props/35-PATTERNS.md — the build.js/config.js/main.js analog sections.
  </read_first>
  <action>
    Add a `PROPS` block to `CONFIG` adjacent to `PARALLAX`: `Z_BACK: -8` (parallax-adjacent background props) and `Z_SURFACE: -3` (on-surface props resting on ledges/floors). Comment that BOTH are NEGATIVE by mandate so a prop can NEVER render over the player, a coin, a route, or a mechanic (all at z 0) — this is the locked legibility-first / §8.5 constraint expressed structurally, and both sit inside the open (NEAR_Z −10, 0) band so props draw in front of the nearest parallax layer. Do not touch PARALLAX or the dead `ACCENT_*` palette entries.

    In `buildLevel`, AFTER the terrain + mechanics emit loops (place it after the `g.secretAlcove ?? []` loop near the end), add a guarded loop `for (const pr of levelData.props ?? [])` that does `add([ sprite(pr.sprite), pos(pr.x, pr.y), z(pr.layer === "surface" ? CONFIG.PROPS.Z_SURFACE : CONFIG.PROPS.Z_BACK), "prop" ])` and NOTHING else. It reads `levelData.props` (TOP-LEVEL) — NOT `g.props` — so `g = levelData.geometry` stays byte-frozen. The loop must add NO `area()`, NO `body()`, NO `rect()`, NO tall apex-blocker: props are the ONLY pure sprite+pos+z entity class in this file, which is what makes them validator-neutral. Default `layer` to back (the ternary's else branch) when omitted. Keep all engine globals inside the `buildLevel` body (a727c13). Do NOT reword the stale theme-N comment at build.js:79 here — plan 08 owns all theme-N cleanup.

    In main.js's manifest load loop, add an `else if (a.kind === "prop") { loadSprite(a.key, webPath); }` branch (single static frame — Phase 36 animates torches later). Do NOT touch the stale theme-N comment at main.js:103 — plan 08 owns it.
  </action>
  <verify>
    <automated>bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/validate-levels.mjs && node -e "import('./src/config.js').then(m=>{const p=m.CONFIG.PROPS; if(p.Z_BACK>=0||p.Z_SURFACE>=0){console.error('PROPS z must be negative',p);process.exit(1)} console.log('PROPS',JSON.stringify(p))})"</automated>
  </verify>
  <acceptance_criteria>
    - `CONFIG.PROPS.Z_BACK === -8` and `CONFIG.PROPS.Z_SURFACE === -3` (both negative; asserted above).
    - `grep -n 'levelData.props ?? \[\]' src/levels/build.js` shows exactly the new guarded loop; the loop body contains `sprite(`, `pos(`, `z(` and the `"prop"` tag but NO `area(`, `body(`, or `rect(` on the prop entity.
    - main.js has an `a.kind === "prop"` branch calling `loadSprite`.
    - check-import-safety (a727c13) + check-safety (no timers/punishment) pass; validate-levels stays 0 HARD-FAIL (byte-identical to pre-change — no descriptor gained props yet).
  </acceptance_criteria>
  <done>The props renderer path exists end-to-end (config z → build.js loop → main.js load) and is provably collider-free; no level is dressed yet, so all structural gates are unchanged-green.</done>
</task>

<task type="auto">
  <name>Task 2: The geometry-freeze snapshot gate + committed baseline</name>
  <files>scripts/check-geometry-frozen.mjs, scripts/fixtures/geometry-frozen-baseline.json</files>
  <read_first>
    - scripts/check-assets-manifest.mjs — the whole file: the pure-data, standalone `failures`-counter + `process.exit(failures>0?1:0)` gate idiom (NEVER wired into check-gate.sh).
    - scripts/validate-levels.mjs — its `import { LEVEL_ORDER, getLevel } from "../src/levels/index.js"` and per-descriptor iteration (it reads `descriptor.geometry` ONLY — confirming props are structurally invisible to it).
    - .planning/phases/35-biome-re-dress-props/35-RESEARCH.md — §"Geometry-Frozen Enforcement" (Q4): capture `JSON.stringify(getLevel(id).geometry)` per LEVEL_ORDER id into a committed golden, then compare.
    - smoke-progress golden-fixture convention (scripts/fixtures/*) for the fixture-file placement pattern.
  </read_first>
  <action>
    Write `scripts/check-geometry-frozen.mjs` as a pure-data, node-importable gate (NOT wired into check-gate.sh, matching check-assets-manifest.mjs): import `{ LEVEL_ORDER, getLevel }` from `../src/levels/index.js`; for each id build `JSON.stringify(getLevel(id).geometry)`; load the golden `scripts/fixtures/geometry-frozen-baseline.json`; compare each level's serialized geometry against the golden; on any mismatch print a HARD-FAIL line naming the level id (and, best-effort, the first differing top-level geometry key) and increment `failures`; `process.exit(failures > 0 ? 1 : 0)`. If a level id is present in one set but not the other, that too is a HARD-FAIL. Add a `--write` flag (or a sibling one-off capture path) so the SAME script can regenerate the baseline deterministically from current geometry.

    Capture the baseline NOW from the current committed post-34.6 geometry: run the script's `--write` path to produce `scripts/fixtures/geometry-frozen-baseline.json` (a map of `{ "level-01": "<json>", ... }` for all 8 LEVEL_ORDER ids). This is the frozen reference for the entire phase — the working tree is clean at a78e545 per STATE, so current geometry IS the sanctioned post-34.6 state. Commit both the script and the baseline.
  </action>
  <verify>
    <automated>node scripts/check-geometry-frozen.mjs && echo "FROZEN-GATE-GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/fixtures/geometry-frozen-baseline.json` exists and contains an entry for every id in LEVEL_ORDER (8 levels).
    - `node scripts/check-geometry-frozen.mjs` exits 0 (green) against the just-captured baseline.
    - Negative proof: temporarily nudging any coin/platform coordinate in a descriptor makes the gate exit non-zero and name the level (verify by a scratch edit, then revert — do NOT commit the nudge).
    - The gate imports the registry the same way validate-levels does and is NOT added to check-gate.sh.
  </acceptance_criteria>
  <done>The frozen-geometry proof exists: a committed golden of all 8 levels' geometry and a gate that HARD-FAILs on any drift — the real defense the standing "geometry-frozen art diffs" mitigation (#4) requires, since props share a file with the frozen arrays.</done>
</task>

<task type="auto">
  <name>Task 3: The per-level props screenshot script (clone of the Phase-33 terrain shooter)</name>
  <files>scripts/screenshot-phase35-props.mjs</files>
  <read_first>
    - scripts/screenshot-phase33-terrain.mjs — the whole file: the server/guard boot, the all-levels-unlocked save seed, the title→select→game row/col cursor nav, and the `page.screenshot({ path })` per level. Per the binding "Playwright script duplication is deliberate" rule, CLONE it — copy, do NOT extract a shared module.
    - .planning/phases/35-biome-re-dress-props/35-RESEARCH.md — §"Per-level screenshot capture" + §"Per-Level Screenshot Verification": output to `.planning/phases/35-biome-re-dress-props/prop-shots/`; for vertical even levels (02/04/06/08) also drive/teleport toward `bounds.top` and capture a climb-altitude shot, not only spawn.
    - .planning/research/ART-PARITY-STEERING.md — the "screenshots prove the SCENE is [sane]" mandate; this evidence is where the black-mess regressions were caught.
  </read_first>
  <action>
    Clone `scripts/screenshot-phase33-terrain.mjs` to `scripts/screenshot-phase35-props.mjs` verbatim (copy the server/guard/boot/nav code per the duplication convention), then change only: (1) `OUT_DIR` → `.planning/phases/35-biome-re-dress-props/prop-shots/` (mkdir recursive); (2) filenames → `level-NN-<biome>-spawn.png`; (3) for vertical even levels (02/04/06/08) add a second capture after driving/teleporting the camera toward the level's `bounds.top` altitude → `level-NN-<biome>-climb.png` (reuse whatever teleport/drive helper the phase33 script already uses; if it only spawn-shots, add a minimal camera-move-then-shoot following its existing idiom). Accept an optional CLI arg listing which level ids to shoot (default: all 8) so the trial plan can shoot just level-01 + level-06. Keep it node-runnable with the dynamically-resolved Playwright, matching browser-boot.mjs's resolution.
  </action>
  <verify>
    <automated>node scripts/screenshot-phase35-props.mjs level-01 && ls .planning/phases/35-biome-re-dress-props/prop-shots/level-01-swamp-spawn.png</automated>
  </verify>
  <acceptance_criteria>
    - Running the script with `level-01` produces `.planning/phases/35-biome-re-dress-props/prop-shots/level-01-swamp-spawn.png` (a non-empty PNG).
    - Running with no args shoots all 8 levels; even levels (02/04/06/08) also emit a `*-climb.png`.
    - The script reuses the Phase-33 boot/nav/guard code (copy-not-extract); it does NOT import a shared screenshot module.
  </acceptance_criteria>
  <done>A repeatable evidence capture exists for the trial + spot-check sign-offs — spawn shots for all levels and climb shots for the vertical evens, into the phase's prop-shots/ dir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → progress.js | Only persisted state; unchanged this plan (props are session-only visual sprites) |

Static, offline, single-origin browser game — no network/server/auth/accounts, no user-supplied data. Adding decorative sprite entities + Node gate/screenshot scripts introduces no trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Tampering | props entity as phantom collider | low | mitigate | Prop loop emits sprite+pos+z ONLY (no area/body); validate-levels stays byte-identical → props can never gate a route |
| T-35-02 | Tampering | unauthorized geometry drift under an art diff | medium | mitigate | check-geometry-frozen.mjs snapshot gate HARD-FAILs any geometry change vs the committed post-34.6 baseline |
| T-35-SC | Tampering | package installs | n/a | accept | Zero new dependencies this phase (vanilla ES2020 + pinned Kaplay; Pillow/Playwright already present) |
</threat_model>

<verification>
```
bash scripts/check-import-safety.sh && bash scripts/check-safety.sh \
  && node scripts/validate-levels.mjs && node scripts/check-assets-manifest.mjs \
  && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs
```
- import-safety/safety: no top-level engine globals, no timers/punishment in the new build.js loop.
- validate-levels: byte-identical 0 HARD-FAIL (no descriptor dressed yet — props path is inert).
- check-geometry-frozen: green against the fresh baseline.
- browser-boot: all 8 levels still boot + reach goal (the inert `?? []` loop changes nothing at runtime).
</verification>

<success_criteria>
- The props renderer path exists (config z → build.js collider-free loop → main.js load branch) and is provably validator-neutral.
- The geometry-freeze gate + committed baseline exist and are green; a scratch geometry nudge fails it.
- The screenshot script captures spawn + climb shots into prop-shots/.
- No level is dressed; all structural gates unchanged-green; no geometry touched.
</success_criteria>

<output>
Create `.planning/phases/35-biome-re-dress-props/35-01-SUMMARY.md` when done.
</output>
