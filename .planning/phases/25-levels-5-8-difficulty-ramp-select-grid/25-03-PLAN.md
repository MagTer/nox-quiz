---
phase: 25-levels-5-8-difficulty-ramp-select-grid
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/levels/level-05.js
  - src/levels/level-06.js
  - src/levels/level-07.js
  - src/levels/level-08.js
  - src/levels/index.js
autonomous: true
requirements: [LVL-02, LVL-03, LVL-05, LVL-06]
must_haves:
  truths:
    - "levels 5-8 exist as pure-data descriptors (id/displayName/allowedTables/geometry, importing ONLY ../config.js) reachable through getLevel()/LEVEL_ORDER, and node scripts/validate-levels.mjs shows zero HARD-FAIL rows for any of the four"
    - "allowedTables strictly equals [2, 3, 4, 5] (level-05), [4, 5, 6, 7] (level-06), [6, 7, 8] (level-07), [6, 7, 8, 9] (level-08) -- the CONTEXT-locked 4-step gentle climb mirroring ROADMAP's literal wording"
    - "level-07 and level-08 each carry a COMPLETE 4-field bounds object (left/right/top/bottom, top well below 0) enabling an ascending, net-rightward verticality climb; level-05/level-06 stay single-screen-tall (bounds omitted, or a complete 0..360-height object if specified)"
    - "each of the four new levels has exactly one secretAlcove entry, placed off the main path"
  artifacts:
    - src/levels/level-05.js
    - src/levels/level-06.js
    - src/levels/level-07.js
    - src/levels/level-08.js
  key_links:
    - "src/levels/index.js's LEVELS array append -> LEVEL_ORDER/BY_ID/getLevel/isUnlocked (all derive automatically from array length/order -- zero other code change in this file)"
    - "each new level's geometry -> scripts/validate-levels.mjs's auto-discovery via LEVEL_ORDER (zero validator code change needed)"
    - "each new level's geometry.secretAlcove -> Plan 25-01's build.js loop + src/mechanics/secretAlcove.js (already landed, Wave 1)"
---

<objective>
Author levels 5-8 as pure-data descriptors through the existing registry/builder (LVL-02), with a gentle 4-step table-pool ramp and level-07/08 verticality (LVL-05), and append them to the registry. Every new level gets exactly one secret alcove (LVL-06). Level-03 ("The Hollow") is already the CONTEXT-locked mixed-review level required by LVL-03's "including one mixed-review level" clause -- zero new authoring needed for that part; this plan closes LVL-03's remaining "gentle ramp across all 8 levels" clause via the new levels' table pools.

Purpose: 25-RESEARCH.md establishes this as almost entirely a "generalize an existing pattern" exercise -- the registry has zero hardcoded level count, the validator auto-discovers `LEVEL_ORDER`, and the camera clamp is already bounds-driven. The one real design constraint is verticality (level-07/08): `bounds` must be a COMPLETE 4-field literal (Pitfall 2 -- `game.js`'s `level.bounds ?? {...}` is all-or-nothing, never a per-field merge) and the climb must be ascending-only with net-rightward hops (Pitfall 4 -- the interactive audit driver's stall heuristic assumes rightward progress).
Output: `src/levels/level-05.js` through `level-08.js` (new files, following the proven `level-01.js`..`level-04.js` schema), and `src/levels/index.js` with 4 new imports + 4 new `LEVELS` array entries (mechanical append -- `LEVEL_ORDER`/`BY_ID`/`getLevel`/`isUnlocked` need zero changes).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-CONTEXT.md
@.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-RESEARCH.md
@.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author level-05.js and level-06.js (single-screen, no verticality)</name>
  <files>src/levels/level-05.js, src/levels/level-06.js</files>
  <read_first>
    src/levels/level-02.js (full file -- the softest existing level; structural template for level-05)
    src/levels/level-01.js (full file -- mixed mechanic-type precedent; structural template for level-06's added variety)
    src/config.js (CONFIG.FLOOR_Y=320, CONFIG.SPIKE_SIZE, CONFIG.DOOR/MATH_GATE/ENEMY/COLLECT dimension constants)
    scripts/lib/jump-envelope.mjs (JUMP_ENVELOPE.maxRise = 88.331 -- the validator's calibrated rise ceiling; every platform rise must stay comfortably under this)
    .planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-RESEARCH.md (Pattern 1's numeric precedent bands: 60-65px rises for narrow platforms, uniform 70px for wide spans, 120-200px gaps, 64-80px checkpoint lead)
    .planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-CONTEXT.md ("New Level Authoring & Verticality" and "Secret XP Alcove" sections)
  </read_first>
  <action>
    Create `src/levels/level-05.js`: import ONLY `{ CONFIG } from "../config.js"` (no other import -- the pure-data firewall). Export `LEVEL_05` with `id: "level-05"`, a themed `displayName` in the project's "The ___" dark-grunge naming style (no pink, no bubbly/childish wording), and `allowedTables: [2, 3, 4, 5]` EXACTLY (this literal spacing/order, matching the CONTEXT-locked pool). Mirror `level-02.js`'s structure: single-screen height (no verticality -- `bounds` may be OMITTED entirely, relying on `game.js`'s dynamic `right` derivation, since a single-screen-tall level needs no explicit `top`/`bottom` override; OR specify a complete 4-field `bounds` object if you prefer explicit camera clamps -- either is safe here). Author floors/platforms/coins/spikes/goal/checkpoints reaching a total horizontal extent of roughly 3200-4300px (CONTEXT's "similar length/scale to the post-Phase-24 extended levels 1-4" decision). Keep every platform rise comfortably under 88.331px, using the 60-65px band for narrower platforms or the 70px band for wider spans. Include a LIGHT mechanic density mirroring level-02 (a door and/or 1-2 mathGates; no enemy/collectZone required for this, the softest new level). Place one checkpoint `{ x, y: FLOOR_Y - 48 }` 64-80px before EVERY hazard/mechanic. Append exactly ONE `secretAlcove: [{ x, y }]` entry to `geometry`, placed off the main path (a short detour a player would only find by exploring, not gating or blocking normal progress, not signposted).
    Create `src/levels/level-06.js` the same way: `id: "level-06"`, themed `displayName`, `allowedTables: [4, 5, 6, 7]` EXACTLY. Mirror `level-01.js`'s mixed mechanic variety -- introduce a SECOND mechanic type beyond level-05's (e.g. an enemy encounter AND a collect-the-answer zone, in addition to a door/mathGate), at 65-70px rises -- "one step harder" per CONTEXT.md. Still single-screen-tall (no verticality). Same length target, same 64-80px checkpoint-lead convention, same exactly-one `secretAlcove` entry requirement.
    Do not reference `src/levels/index.js` in this task -- the registry append happens in Task 2, after both level-07/level-08 also exist.
  </action>
  <acceptance_criteria>
    `level-05.js` and `level-06.js` each contain exactly ONE `import` statement (`^import`), importing only `../config.js`.
    `LEVEL_05.allowedTables` deep-equals `[2, 3, 4, 5]`; `LEVEL_06.allowedTables` deep-equals `[4, 5, 6, 7]`.
    `LEVEL_05.geometry.secretAlcove` and `LEVEL_06.geometry.secretAlcove` each have exactly 1 entry.
    `LEVEL_06.geometry` has at least 2 of `{doors, mathGates, enemies, collectZones}` with `length > 0` (mixed mechanic variety); `LEVEL_05.geometry` has at least 1 of `{doors, mathGates}` with `length > 0`.
    `node scripts/validate-levels.mjs --fixture src/levels/level-05.js` and `--fixture src/levels/level-06.js` each print zero `HARD-FAIL` lines.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^import" src/levels/level-05.js && grep -c "^import" src/levels/level-06.js && node -e "Promise.all([import('./src/levels/level-05.js'),import('./src/levels/level-06.js')]).then(([m5,m6])=>{const t5=JSON.stringify(m5.LEVEL_05.allowedTables)==='[2,3,4,5]';const t6=JSON.stringify(m6.LEVEL_06.allowedTables)==='[4,5,6,7]';const a5=(m5.LEVEL_05.geometry.secretAlcove||[]).length===1;const a6=(m6.LEVEL_06.geometry.secretAlcove||[]).length===1;const g6=m6.LEVEL_06.geometry;const mixed6=['doors','mathGates','enemies','collectZones'].filter(k=>(g6[k]||[]).length>0).length>=2;console.log('level-05',t5,a5,'level-06',t6,a6,mixed6);process.exit(t5&&t6&&a5&&a6&&mixed6?0:1);})" && node scripts/validate-levels.mjs --fixture src/levels/level-05.js && node scripts/validate-levels.mjs --fixture src/levels/level-06.js</automated>
  </verify>
  <done>level-05.js and level-06.js authored as pure-data descriptors with the CONTEXT-locked table pools, exactly one secretAlcove each, and zero validator HARD-FAILs when checked in isolation via --fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Author level-07.js and level-08.js (verticality) and append all 4 new levels to the registry</name>
  <files>src/levels/level-07.js, src/levels/level-08.js, src/levels/index.js</files>
  <read_first>
    src/levels/level-04.js (full file -- the hardest existing level; explicit full `bounds` template, densest mechanic mix)
    src/levels/index.js (full file -- current 4-level registry, the exact append target)
    src/camera.js (full file -- confirms `bounds?.left/right/top/bottom ?? CONFIG default` per-field fallback INSIDE followCamera)
    .planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-RESEARCH.md (Pattern 3's full "bounds must be COMPLETE" explanation, Pitfall 2, Pitfall 4's ascending-zigzag/net-rightward requirement)
    .planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-CONTEXT.md ("New Level Authoring & Verticality" section -- ascending-only, ~1 extra screen of climb, fall-respawn threshold at world-Y 480)
  </read_first>
  <action>
    Create `src/levels/level-07.js`: `id: "level-07"`, themed `displayName`, `allowedTables: [6, 7, 8]` EXACTLY. MUST carry a COMPLETE 4-field `bounds` object: `{ left: 0, right: <the level's actual horizontal extent, ~3200-4300px>, top: <well below 0, e.g. -360 for one extra screen of climb>, bottom: 360 }` -- all 4 fields present together in ONE literal (never partial: `game.js:88`'s `level.bounds ?? {...}` is all-or-nothing, so an incomplete object silently loses the dynamic `right` derivation and falls back to the wrong `CONFIG.LEVEL_RIGHT`). Author an ascending-only platform chain for the verticality segment: each subsequent platform in the climb must have a SMALLER Kaplay-Y (higher up) than the one before it, AND make NET-RIGHTWARD x progress on every hop -- a gentle "capstone climb" zigzag, never a pure vertical shaft and never a hop requiring net-leftward x movement (this defeats the interactive audit driver's forward-progress stall heuristic, per Pitfall 4, even though the static validator itself is direction-agnostic). Every vertical tier needs a full-width floor run underneath it (not just a narrow platform), so a fall during the climb lands on solid ground rather than dropping past the global fall-respawn threshold (`CONFIG.LEVEL_BOTTOM + CONFIG.FALL_MARGIN` = 480 world-Y, a GLOBAL constant that does NOT read per-level `bounds`). Keep rises under 88.331px (use the 65-70px band). Checkpoint 64-80px lead before every hazard/mechanic AND before each climb tier (a fall during a long climb must never cost more than one tier). Include at least 2 mechanic types. Exactly ONE `secretAlcove` entry.
    Create `src/levels/level-08.js` the same way: `id: "level-08"`, themed `displayName`, `allowedTables: [6, 7, 8, 9]` EXACTLY (the hardest, capstone pool). Same verticality rules as level-07 (complete 4-field `bounds`, ascending-only, net-rightward, full-width floor under every tier). Densest mechanic mix, mirroring `level-04.js`'s density (door + mathGate + enemy + collectZone), 70px-band rises. Exactly ONE `secretAlcove` entry.
    In `src/levels/index.js`: add 4 new import lines after the existing 4 (`import { LEVEL_05 } from "./level-05.js";` through `import { LEVEL_08 } from "./level-08.js";`), and append all 4 new constants to the `LEVELS` array literal in order: `const LEVELS = [LEVEL_01, LEVEL_02, LEVEL_03, LEVEL_04, LEVEL_05, LEVEL_06, LEVEL_07, LEVEL_08];`. Do NOT touch `BY_ID`, `LEVEL_ORDER`, `getLevel`, or `isUnlocked` -- they derive automatically from the array's new length/order with zero code change.
  </action>
  <acceptance_criteria>
    `LEVEL_07.allowedTables` deep-equals `[6, 7, 8]`; `LEVEL_08.allowedTables` deep-equals `[6, 7, 8, 9]`.
    Both `LEVEL_07.bounds` and `LEVEL_08.bounds` are objects containing all 4 keys `left`, `right`, `top`, `bottom`, with `top <= -300` (comfortably below 0, at least most of one extra screen of climb).
    `LEVEL_07.geometry.secretAlcove` and `LEVEL_08.geometry.secretAlcove` each have exactly 1 entry.
    `LEVEL_ORDER` (from `src/levels/index.js`) has length 8, with `LEVEL_ORDER[4..7]` equal to `"level-05"`, `"level-06"`, `"level-07"`, `"level-08"` in that order.
    `node scripts/validate-levels.mjs` (full registry run, no `--fixture`) prints zero `HARD-FAIL` lines for `level-05` through `level-08`; levels 1-4 remain unaffected (still whatever pass/fail state Phase 24 left them in).
  </acceptance_criteria>
  <verify>
    <automated>node -e "Promise.all([import('./src/levels/level-07.js'),import('./src/levels/level-08.js'),import('./src/levels/index.js')]).then(([m7,m8,idx])=>{const t7=JSON.stringify(m7.LEVEL_07.allowedTables)==='[6,7,8]';const t8=JSON.stringify(m8.LEVEL_08.allowedTables)==='[6,7,8,9]';const keys=['left','right','top','bottom'];const b7=m7.LEVEL_07.bounds&&keys.every(k=>k in m7.LEVEL_07.bounds)&&m7.LEVEL_07.bounds.top<=-300;const b8=m8.LEVEL_08.bounds&&keys.every(k=>k in m8.LEVEL_08.bounds)&&m8.LEVEL_08.bounds.top<=-300;const a7=(m7.LEVEL_07.geometry.secretAlcove||[]).length===1;const a8=(m8.LEVEL_08.geometry.secretAlcove||[]).length===1;const order=idx.LEVEL_ORDER;const reg=order.length===8&&order[4]==='level-05'&&order[5]==='level-06'&&order[6]==='level-07'&&order[7]==='level-08';console.log('level-07',t7,b7,a7,'level-08',t8,b8,a8,'registry',order,reg);process.exit(t7&&t8&&b7&&b8&&a7&&a8&&reg?0:1);})" && N=$(node scripts/validate-levels.mjs 2>&1 | grep -E '^level-0[5678] ' | grep -c 'HARD-FAIL'); echo "levels 5-8 HARD-FAILs: $N"; test "$N" = "0"</automated>
  </verify>
  <done>level-07.js and level-08.js authored with complete 4-field bounds, ascending net-rightward verticality, and exactly one secretAlcove each; index.js registry appended to 8 levels with zero changes to LEVEL_ORDER/BY_ID/getLevel/isUnlocked derivation logic; validate-levels.mjs shows zero HARD-FAILs for levels 5-8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure developer-authored static level data, consumed only by the offline single-player Kaplay client and Node-side validation tooling. No network input, no auth, no session, no access control, no crypto. Per 25-RESEARCH.md's Security Domain section, this phase introduces no new threat surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-05 | Tampering | `src/levels/level-05.js`..`level-08.js` geometry values | low | accept | Developer-authored static data, not runtime-parsed untrusted input; `node scripts/validate-levels.mjs` gates any malformed/out-of-envelope geometry (over-hole, unreachable, gap-too-wide) before it can ship |
| T-25-06 | Tampering | `level-07.js`/`level-08.js`'s partial-vs-complete `bounds` object | medium | mitigate | Task 2's acceptance criteria explicitly require all 4 `bounds` keys present with `top <= -300` via a direct node-import check -- a partial object (silently falling back to the wrong `CONFIG.LEVEL_RIGHT` per Pitfall 2) fails this gate before it can ship |

## Package Legitimacy Audit

Not applicable -- this plan installs zero new packages.
</threat_model>

<verification>
Run `node scripts/validate-levels.mjs --fixture <path>` per new level file immediately after authoring it (fast, isolated feedback), then the full `node scripts/validate-levels.mjs` (no `--fixture`) after the registry append in Task 2 to confirm the complete 8-level registry is clean. `secretAlcove` reachability is NOT checked by this validator by design (Pitfall 3) -- a manual spot-check for all 8 alcoves is Plan 25-07's job, after all levels (including the 1-4 retrofit) exist.
</verification>

<success_criteria>
- `src/levels/level-05.js` through `level-08.js` exist as pure-data descriptors with the CONTEXT-locked `allowedTables` pools.
- `level-07.js`/`level-08.js` each carry a complete 4-field `bounds` object enabling ascending, net-rightward verticality.
- Every new level has exactly one `secretAlcove` entry.
- `src/levels/index.js`'s `LEVEL_ORDER` has length 8, in the correct order.
- `node scripts/validate-levels.mjs` shows zero `HARD-FAIL` rows for levels 5-8.
</success_criteria>

<output>
Create `.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/levels/level-05.js` -- new: pure-data descriptor, `allowedTables: [2, 3, 4, 5]`, no verticality, 1 secretAlcove (LVL-02, LVL-03, LVL-06)
- `src/levels/level-06.js` -- new: pure-data descriptor, `allowedTables: [4, 5, 6, 7]`, no verticality, mixed mechanic variety, 1 secretAlcove (LVL-02, LVL-03, LVL-06)
- `src/levels/level-07.js` -- new: pure-data descriptor, `allowedTables: [6, 7, 8]`, complete 4-field `bounds`, ascending verticality, 1 secretAlcove (LVL-02, LVL-03, LVL-05, LVL-06)
- `src/levels/level-08.js` -- new: pure-data descriptor, `allowedTables: [6, 7, 8, 9]`, complete 4-field `bounds`, ascending verticality, densest mix, 1 secretAlcove (LVL-02, LVL-03, LVL-05, LVL-06)
- `src/levels/index.js` -- edited: 4 new imports + 4 new `LEVELS` array entries; `LEVEL_ORDER` now has length 8 (LVL-02)
