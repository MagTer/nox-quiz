---
phase: 25-levels-5-8-difficulty-ramp-select-grid
plan: 06
type: execute
wave: 3
depends_on: ["25-03", "25-04"]
files_modified:
  - scripts/smoke-progress.mjs
autonomous: true
requirements: [LVL-02, LVL-04, LVL-06]
must_haves:
  truths:
    - "node scripts/smoke-progress.mjs prints PASS -- the 4 existing level 1-4 expectedGeometry deepEqual blocks now include each level's real secretAlcove entry (added by Plan 25-04), and the LEVEL_ORDER.length assertion reads 8, not 4"
    - "a progress seeded from a simulated pre-v5.0 save (only levels 1-4 known, all cleared) reports level-05 unlocked and level-06 still locked -- proving the registry's derived-unlock logic needs zero migration code for the new 8-level roster"
    - "progress.addBonusXp(CONFIG.PROGRESS.XP_ALCOVE) is now a PERMANENT regression-suite assertion (not just Plan 25-01's throwaway verify command), pinning the flat +5 XP, no-level-up-at-level-1 behavior"
  artifacts: []
  key_links:
    - "src/levels/level-01.js..level-04.js's real (Plan 25-04-authored) secretAlcove coordinates -> this plan's 4 expectedGeometry literals (must byte-match, or deepEqual's strict Object.keys().length check fails the whole level's regression row)"
    - "src/levels/index.js's 8-entry LEVEL_ORDER -> this plan's new pre-v5.0-save-resume assertion (isUnlocked(\"level-05\"/\"level-06\", ...))"
---

<objective>
Close the regression gap Plan 25-04's additive `secretAlcove` retrofit deliberately left open: update `scripts/smoke-progress.mjs`'s 4 byte-pinned `expectedGeometry` literals (levels 1-4) to include each level's real new `secretAlcove` entry, and bump the `LEVEL_ORDER.length` assertion from 4 to 8 now that Plan 25-03's registry append has landed. Also add two new PERMANENT regression assertions: a pre-v5.0-save-resume check across the now-8-level registry (LVL-04), and a pin for Plan 25-01's `addBonusXp`/`XP_ALCOVE` behavior (LVL-06).

Purpose: `scripts/smoke-progress.mjs`'s `deepEqual` helper does a STRICT `Object.keys(a).length !== Object.keys(b).length` comparison -- adding a new `secretAlcove` key to a level's REAL geometry (Plan 25-04) without also adding it to the matching `expectedGeometry` LITERAL in this test file breaks that level's regression row, even though nothing else about the geometry changed. This plan is the deliberate, planned fix for that documented interim-red state (see Plan 25-04's `<verification>` block), plus the mechanical `LEVEL_ORDER.length` bump that 25-VALIDATION.md's Wave 0 requirements call out as required once the registry has 8 entries.
Output: `scripts/smoke-progress.mjs`'s 4 `expectedGeometry` blocks (level-01..04) each gain a `secretAlcove: [{ x, y }]` key matching the real file exactly. Its `LEVEL_ORDER.length === 4` assertion becomes `=== 8`. Two new assertion blocks are added: one exercising `isUnlocked` across a simulated pre-v5.0 save against the 8-level registry, one pinning `addBonusXp`/`CONFIG.PROGRESS.XP_ALCOVE`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-CONTEXT.md
@.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-RESEARCH.md
@.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the 4 expectedGeometry blocks with secretAlcove + bump LEVEL_ORDER.length to 8</name>
  <files>scripts/smoke-progress.mjs</files>
  <read_first>
    scripts/smoke-progress.mjs (the `deepEqual` helper at lines 271-280 -- note its STRICT key-count check; the 4 `expectedGeometry` blocks: level-01 ~304-377, level-02 ~398-470, level-03 ~496-585, level-04 ~616-719; the `LEVEL_ORDER.length === 4` assertion at line 723)
    src/levels/level-01.js, level-02.js, level-03.js, level-04.js (post-Plan-25-04 state -- read each file's REAL `geometry.secretAlcove` entry to get its exact `{x, y}` value; do not guess or invent a value)
    .planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-RESEARCH.md (Open Question 2's recommendation -- only the length-assertion bump is strictly required for NEW levels; this task additionally MUST update the 4 EXISTING blocks because they now carry a new key, per Plan 25-04's documented interim-red state)
  </read_first>
  <action>
    For EACH of the 4 `expectedGeometry` literals (level-01 through level-04), add a new key `secretAlcove: [{ x: <exact value>, y: <exact value> }],` matching BYTE-FOR-BYTE the real `secretAlcove` entry Plan 25-04 authored in that level's actual `src/levels/level-0N.js` file (read the real file first -- these coordinates are executor-authored data from a prior plan, not something to invent here). Place the new key in the SAME relative position the real descriptor uses (immediately after `answerPickupSlots`, matching the level files' own key order -- though `deepEqual`'s key-count check does not care about order, matching it keeps the test file readable and consistent with the real descriptors).
    Change line 723's `check(LEVEL_ORDER.length === 4, ...)` to `check(LEVEL_ORDER.length === 8, ...)`, updating the assertion message's expected-count wording accordingly (e.g. "LVL-01/04: LEVEL_ORDER should have 8 levels, got ${LEVEL_ORDER.length}").
    Do not touch any other key inside any of the 4 `expectedGeometry` blocks, and do not touch the `LEVEL_ORDER[1] === "level-02"` assertion on the next line (still correct, unaffected by the registry's new length).
  </action>
  <acceptance_criteria>
    Each of the 4 `expectedGeometry` blocks (level-01 through level-04) contains a `secretAlcove` key whose value byte-matches that level's real `src/levels/level-0N.js` `geometry.secretAlcove` array exactly.
    The `LEVEL_ORDER.length` assertion now checks `=== 8`.
    `node scripts/smoke-progress.mjs` prints `smoke-progress: PASS` (all `deepEqual` checks, including the 4 geometry blocks, now succeed).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "LEVEL_ORDER.length === 8" scripts/smoke-progress.mjs && N=$(grep -c "secretAlcove:" scripts/smoke-progress.mjs); echo "secretAlcove blocks in smoke-progress.mjs: $N"; test "$N" -ge "4" && node scripts/smoke-progress.mjs</automated>
  </verify>
  <done>All 4 expectedGeometry blocks pin their level's real secretAlcove entry; LEVEL_ORDER.length assertion reads 8; smoke-progress.mjs prints PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pre-v5.0-save-resume assertion + addBonusXp/XP_ALCOVE permanent regression pin</name>
  <files>scripts/smoke-progress.mjs</files>
  <read_first>
    scripts/smoke-progress.mjs (the existing SAVE-06 derived-unlock block at lines 173-187 -- the pattern to mirror; the `check`/`failures` idiom at lines 30-35)
    src/levels/index.js (post-Plan-25-03 state -- confirms `LEVEL_ORDER` has 8 entries: level-01..level-08, and `isUnlocked`'s single-predecessor-chain logic)
    src/progress.js (post-Plan-25-01 state -- `addBonusXp(amount)`, `createProgress(saved)`)
    src/config.js (`CONFIG.PROGRESS.XP_ALCOVE`)
    .planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-CONTEXT.md ("Level Select 2x4 Grid" section -- "A pre-v5.0 save resumes with levels 5-8 rendering locked-grey by default... isUnlocked's existing single-predecessor-chain logic already generalizes to 8 entries with no code change")
  </read_first>
  <behavior>
    - A progress seeded with `{ levels: { "level-01": {cleared:true}, "level-02": {cleared:true}, "level-03": {cleared:true}, "level-04": {cleared:true} } }` (simulating a pre-v5.0 save that only ever knew about 4 levels, all cleared) reports `isUnlocked("level-05", progress) === true` (level-04 is cleared) and `isUnlocked("level-06", progress) === false` (level-05 has never been cleared in this old save) -- zero migration code needed.
    - `createProgress().addBonusXp(CONFIG.PROGRESS.XP_ALCOVE)` on a fresh progress leaves `getXp() === 5` and returns `false` (no level-up from a single alcove bonus at level 1, threshold(1)=200).
  </behavior>
  <action>
    Add a new assertion block near the existing SAVE-06 registry-derived-unlock block (after line 187): construct `const oldSave = createProgress({ levels: { "level-01": { cleared: true }, "level-02": { cleared: true }, "level-03": { cleared: true }, "level-04": { cleared: true } } });` and assert `isUnlocked("level-05", oldSave) === true` and `isUnlocked("level-06", oldSave) === false`, with messages citing LVL-04's pre-v5.0-save-resume requirement.
    Add a second new assertion block (near the existing SAVE-01 XP/threshold blocks, or immediately after): construct a fresh `createProgress()`, call `.addBonusXp(CONFIG.PROGRESS.XP_ALCOVE)`, assert `getXp() === 5` and the call's return value `=== false`, with a message citing LVL-06's addBonusXp/XP_ALCOVE regression pin.
    Do not remove or alter any existing assertion block -- these are pure additions.
  </action>
  <acceptance_criteria>
    `scripts/smoke-progress.mjs` contains a new block calling `isUnlocked("level-05"` and `isUnlocked("level-06"` against a progress seeded with only levels 1-4 cleared.
    `scripts/smoke-progress.mjs` contains a new block calling `addBonusXp(CONFIG.PROGRESS.XP_ALCOVE)`.
    `node scripts/smoke-progress.mjs` prints `smoke-progress: PASS`.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'isUnlocked("level-05"' scripts/smoke-progress.mjs && grep -c 'isUnlocked("level-06"' scripts/smoke-progress.mjs && grep -c "addBonusXp(CONFIG.PROGRESS.XP_ALCOVE)" scripts/smoke-progress.mjs && node scripts/smoke-progress.mjs</automated>
  </verify>
  <done>smoke-progress.mjs gains a permanent pre-v5.0-save-resume assertion (levels 5/6 unlock semantics) and a permanent addBonusXp/XP_ALCOVE regression pin; full suite still prints PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure Node-side regression-test-file edits, exercising only the offline, engine-agnostic progress/registry modules directly (no browser, no network, no auth, no session, no crypto). Per 25-RESEARCH.md's Security Domain section, this phase introduces no new threat surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-11 | Tampering | The 4 `expectedGeometry` literals' new `secretAlcove` values | low | mitigate | Acceptance criteria require the values to byte-match the REAL level files (read first, not invented), and the overall `node scripts/smoke-progress.mjs` PASS is the final arbiter -- a mismatched literal fails loudly, it cannot silently under-pin the regression |
| T-25-12 | Repudiation | The pre-v5.0-save-resume claim (LVL-04) | low | mitigate | Encoded as a concrete, automated `isUnlocked` assertion pair (not a prose claim) -- proves the derived-unlock behavior on every future run, not just at authoring time |

## Package Legitimacy Audit

Not applicable -- this plan installs zero new packages.
</threat_model>

<verification>
Run `node scripts/smoke-progress.mjs` after each task. Both tasks must leave it printing `smoke-progress: PASS`. This is the last piece of the automated regression suite Plan 25-04 deliberately left red -- after this plan, the full `bash scripts/check-progress.sh` (which invokes `smoke-progress.mjs` as its final step) should also pass cleanly again.
</verification>

<success_criteria>
- `node scripts/smoke-progress.mjs` prints PASS.
- All 4 `expectedGeometry` blocks (levels 1-4) include their level's real `secretAlcove` entry.
- `LEVEL_ORDER.length` assertion reads 8.
- New permanent assertions pin the pre-v5.0-save-resume behavior and `addBonusXp`/`XP_ALCOVE`.
</success_criteria>

<output>
Create `.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-06-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `scripts/smoke-progress.mjs` -- edited: 4 `expectedGeometry` blocks gain their level's `secretAlcove` entry; `LEVEL_ORDER.length` assertion bumped 4->8; 2 new permanent assertion blocks (pre-v5.0-save-resume unlock semantics, `addBonusXp`/`XP_ALCOVE` regression pin) (LVL-02, LVL-04, LVL-06)
