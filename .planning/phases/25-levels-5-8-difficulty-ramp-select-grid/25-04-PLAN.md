---
phase: 25-levels-5-8-difficulty-ramp-select-grid
plan: 04
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/levels/level-02.js
  - src/math/brain.js
  - scripts/check-progress.sh
  - src/levels/level-01.js
  - src/levels/level-03.js
  - src/levels/level-04.js
autonomous: true
requirements: [MATH-01, MATH-02, LVL-03, LVL-06]
must_haves:
  truths:
    - "level-02.js's allowedTables is exactly [2, 3, 4, 5, 6, 7] -- table 1 dropped, 6/7 kept, no table added to compensate"
    - "src/math/brain.js's multiplicand roll is exactly Math.floor(Math.random() * 9) + 1, and git diff on brain.js shows exactly 1 changed line -- no other '10' literal in the file (parseInt radix args, CONFIG.BRAIN.MASTERY_WINDOW) is touched"
    - "levels 1-4 each gain exactly one new secretAlcove entry, added purely additively -- git diff on each of the 4 files shows zero deleted/modified lines, only insertions"
  artifacts: []
  key_links:
    - "level-02.js's allowedTables -> src/math/brain.js's createBrain({allowedTables}) validTables filter (already generalizes to the new 6-table pool with zero brain.js logic change)"
    - "level-01..04.js's new secretAlcove entries -> src/levels/build.js's Wave-1 secretAlcove loop (already landed) -> src/mechanics/secretAlcove.js's wireSecretAlcove (already landed, Wave 1)"
---

<objective>
Apply the two authorized math edits (MATH-01: drop table 1 from level-02's pool; MATH-02: narrow the LOCKED brain's second-factor roll from 1-10 to 1-9) and retrofit exactly one secret alcove into each of levels 1-4, purely additively (LVL-06 applied to the existing, already-shipped levels).

Purpose: MATH-01/MATH-02 are the two ONLY authorized edits touching the LOCKED math brain/level-pool surface this milestone -- 25-CONTEXT.md pins their exact before/after literals so there is zero ambiguity about scope. LVL-06's success criterion 5 ("every level hides one optional secret XP alcove") applies to ALL 8 levels, not just the new ones -- levels 1-4 need the SAME mechanism (already built in Plan 25-01) wired into their existing, kid-validated geometry without touching a single existing floor/platform/checkpoint value.
Output: `src/levels/level-02.js`'s `allowedTables` becomes `[2, 3, 4, 5, 6, 7]`. `src/math/brain.js:247`'s multiplicand roll becomes `Math.floor(Math.random() * 9) + 1` -- the ONE authorized literal change to this LOCKED file. `scripts/check-progress.sh` gains 2 new grep-based regression assertions guarding both edits. `src/levels/level-01.js` through `level-04.js` each gain exactly one new `secretAlcove: [{ x, y }]` geometry array, added as a pure append with zero edits to any existing key/value.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-CONTEXT.md
@.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-RESEARCH.md
@.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MATH-01 + MATH-02 literal edits + check-progress.sh regression grep</name>
  <files>src/levels/level-02.js, src/math/brain.js, scripts/check-progress.sh</files>
  <read_first>
    src/levels/level-02.js (line 15 -- the exact `allowedTables` literal)
    src/math/brain.js (line 247 -- the exact `nextQuestion()` multiplicand roll; also note the unrelated `parseInt(k, 10)` calls at lines 96/104/183/189 that must NOT be touched)
    src/config.js (CONFIG.BRAIN.MASTERY_WINDOW: 10 -- an unrelated EWMA window size that must NOT be touched)
    scripts/check-progress.sh (full file -- the numbered assertion style, and the exact insertion point immediately before the "# 14. Final step -- invoke the headless math/seed smoke." comment)
    .planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-CONTEXT.md ("Difficulty Ramp & Table Pools" section -- the exact MATH-01/MATH-02 before/after literals)
  </read_first>
  <action>
    In `src/levels/level-02.js` line 15, change `allowedTables: [1, 2, 3, 4, 5, 6, 7],` to `allowedTables: [2, 3, 4, 5, 6, 7],` -- drop the `1` only, keep `6`/`7` exactly as they are. Do NOT add `8` or `9` to compensate.
    In `src/math/brain.js` line 247, change `const multiplicand = Math.floor(Math.random() * 10) + 1;` to `const multiplicand = Math.floor(Math.random() * 9) + 1;` -- this is the ONE authorized literal change to this LOCKED file. Do NOT touch the `parseInt(key, 10)` calls at lines 96/104/183/189, and do NOT touch `CONFIG.BRAIN.MASTERY_WINDOW: 10` in `src/config.js` -- both are unrelated "10" literals.
    In `scripts/check-progress.sh`, insert two new numbered assertions immediately before the existing `# 14. Final step -- invoke the headless math/seed smoke.` comment (renumber that comment to `# 16.` and the new ones to `# 14.`/`# 15.`): (14) a positive `grep -q "allowedTables: \[2, 3, 4, 5, 6, 7\]" "$ROOT/src/levels/level-02.js"` with `fail "level-02.js's allowedTables must be exactly [2, 3, 4, 5, 6, 7] (MATH-01: table 1 dropped)"` on failure; (15) a positive `grep -q "Math.floor(Math.random() \* 9) + 1" "$ROOT/src/math/brain.js"` with a MATH-02 fail message, PLUS a negative check that `Math.floor(Math.random() * 10) + 1` no longer appears in `brain.js`, failing loudly if it does. Follow the file's existing `strip_comments`/`fail()` idiom exactly.
  </action>
  <acceptance_criteria>
    `src/levels/level-02.js` contains the exact string `allowedTables: [2, 3, 4, 5, 6, 7],` and does NOT contain `[1, 2, 3, 4, 5, 6, 7]` anywhere.
    `src/math/brain.js` contains the exact string `Math.floor(Math.random() * 9) + 1` and does NOT contain `Math.floor(Math.random() * 10) + 1`.
    `git diff --stat src/levels/level-02.js` reports exactly `1 insertion(+), 1 deletion(-)`.
    `git diff --stat src/math/brain.js` reports exactly `1 insertion(+), 1 deletion(-)` (confirming the LOCKED file's diff is scoped to precisely one line).
    `bash scripts/check-progress.sh` prints `progress checks: PASS`.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "allowedTables: \[2, 3, 4, 5, 6, 7\]" src/levels/level-02.js && ! grep -q "\[1, 2, 3, 4, 5, 6, 7\]" src/levels/level-02.js && grep -c "Math.floor(Math.random() \* 9) + 1" src/math/brain.js && ! grep -q "Math.floor(Math.random() \* 10) + 1" src/math/brain.js && git diff --stat src/levels/level-02.js | grep -E "1 insertion\(\+\), 1 deletion\(-\)" && git diff --stat src/math/brain.js | grep -E "1 insertion\(\+\), 1 deletion\(-\)" && bash scripts/check-progress.sh</automated>
  </verify>
  <done>level-02.js's allowedTables is exactly [2, 3, 4, 5, 6, 7]; brain.js's multiplicand roll is exactly * 9 + 1 with a single-line diff; check-progress.sh gains 2 regression assertions guarding both edits and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit one secretAlcove into each of levels 1-4, purely additively</name>
  <files>src/levels/level-01.js, src/levels/level-02.js, src/levels/level-03.js, src/levels/level-04.js</files>
  <read_first>
    src/levels/level-01.js, level-02.js (post-Task-1 state), level-03.js, level-04.js (full files -- identify an existing floor/platform footprint suitable for an off-path nook in each)
    src/levels/build.js (the Wave-1 `secretAlcove ?? []` loop -- confirms this is a NEW, additive-only geometry key with no interaction with any other array)
    .planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-CONTEXT.md ("Secret XP Alcove" section -- "For levels 1-4, the alcove is placed as a new, purely-additive nook that does not touch any existing floor/platform/checkpoint geometry")
  </read_first>
  <action>
    In EACH of `level-01.js`, `level-02.js`, `level-03.js`, `level-04.js`: append a NEW `secretAlcove: [{ x, y }]` array as the LAST key inside `geometry` (after `answerPickupSlots`), with a comment: "Phase 25 retrofit (LVL-06) -- purely additive, does not touch any existing floor/platform/checkpoint geometry." Choose an `{x, y}` coordinate that sits within (or just off the edge of) an EXISTING floor or platform's footprint in that level -- reachable via a brief detour that is not otherwise required to reach the goal, not signposted, not gating anything. Do NOT add, remove, or modify any other geometry entry (floors/platforms/coins/spikes/goal/checkpoints/doors/mathGates/enemies/collectZones/answerPickupSlots) in any of these 4 files -- the ONLY diff in each file is the new `secretAlcove` array itself.
    Do not touch `src/levels/index.js`, `src/levels/build.js`, or `src/mechanics/secretAlcove.js` in this task -- the mechanism already landed in Plan 25-01.
    NOTE (expected, documented interim state): `node scripts/smoke-progress.mjs` (and therefore `bash scripts/check-progress.sh`, which invokes it) is EXPECTED to FAIL its `deepEqual` regression checks for levels 1-4 after this task, because the smoke test's 4 `expectedGeometry` literals do not yet include the new `secretAlcove` key (their strict `Object.keys` length comparison will now see one extra key on the real geometry vs. the pinned literal). This is NOT re-baselined until Plan 25-06 (Wave 3) updates those 4 literals with the exact alcove coordinates this task authors. Do NOT attempt to fix or work around this expected red state in this task -- it mirrors the project's own established "expected red until X lands" convention (e.g. Plan 24-01's identical documented interim state for `smoke-progress.mjs`).
  </action>
  <acceptance_criteria>
    Each of `level-01.js`/`level-02.js`/`level-03.js`/`level-04.js` has exactly ONE `secretAlcove` array with exactly 1 entry.
    `git diff --numstat` on each of the 4 files shows `0` in the deletions column (pure additions only, zero existing lines touched).
    `node scripts/validate-levels.mjs` shows zero `HARD-FAIL` rows for `level-01` through `level-04` (the validator does not recognize `secretAlcove` as a checked kind, so this task cannot introduce a new HARD-FAIL there).
    `node scripts/smoke-progress.mjs` is EXPECTED to exit non-zero on its level-01..04 geometry `deepEqual` assertions at this point -- this is a documented, deliberate interim state, not a regression to fix in this task.
  </acceptance_criteria>
  <verify>
    <automated>node -e "Promise.all(['level-01','level-02','level-03','level-04'].map(id=>import('./src/levels/'+id+'.js'))).then(mods=>{const consts=['LEVEL_01','LEVEL_02','LEVEL_03','LEVEL_04'];const ok=mods.every((m,i)=>(m[consts[i]].geometry.secretAlcove||[]).length===1);console.log('alcove-retrofit-count-ok',ok);process.exit(ok?0:1);})" && git diff --numstat src/levels/level-01.js src/levels/level-02.js src/levels/level-03.js src/levels/level-04.js | awk '{if ($2 != 0) { print "FAIL: deletions found in " $3; exit 1 } } END { print "all-additive: PASS" }' && N=$(node scripts/validate-levels.mjs 2>&1 | grep -E '^level-0[1234] ' | grep -c 'HARD-FAIL'); echo "levels 1-4 HARD-FAILs: $N"; test "$N" = "0"</automated>
  </verify>
  <done>Each of levels 1-4 has exactly one new secretAlcove entry, added as a pure append (zero deletions/modifications to any existing line); validate-levels.mjs still shows zero HARD-FAILs for levels 1-4; smoke-progress.mjs's expected-red state on the 4 geometry blocks is documented, not fixed here (Plan 25-06's job).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure local static-data/literal edits, consumed only by the offline single-player Kaplay client and Node-side validation tooling. No network input, no auth, no session, no access control, no crypto. Per 25-RESEARCH.md's Security Domain section, this phase introduces no new threat surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-07 | Tampering | `src/math/brain.js`'s ONE authorized literal change (MATH-02) | medium | mitigate | `brain.js` is the LOCKED math brain; Task 1's acceptance criteria require an automated `git diff --stat` assertion showing EXACTLY `1 insertion(+), 1 deletion(-)` before the task is done, plus a new permanent `check-progress.sh` grep gate -- both catch any accidental additional edit to the locked file, now and on every future commit |
| T-25-08 | Tampering | `src/levels/level-01.js`..`level-04.js`'s additive `secretAlcove` retrofit | low | accept | Purely additive data with zero existing key/value touched; verified by the `git diff --numstat` deletions-must-be-zero acceptance check for all 4 files |

## Package Legitimacy Audit

Not applicable -- this plan installs zero new packages.
</threat_model>

<verification>
Run Task 1's verify command (which ends with `bash scripts/check-progress.sh`) to confirm both math edits land cleanly with the new permanent regression gate in place. Run Task 2's verify command to confirm the additive retrofit. `node scripts/smoke-progress.mjs` (and therefore `bash scripts/check-progress.sh`) going RED on levels 1-4's geometry `deepEqual` checks AFTER Task 2 is an EXPECTED, documented interim state -- it is not fixed until Plan 25-06 (Wave 3) updates the 4 `expectedGeometry` literals with the new `secretAlcove` coordinates this task authors. Do not attempt to fix `smoke-progress.mjs` in this plan.
</verification>

<success_criteria>
- `src/levels/level-02.js`'s `allowedTables` is exactly `[2, 3, 4, 5, 6, 7]`.
- `src/math/brain.js`'s multiplicand roll is exactly `Math.floor(Math.random() * 9) + 1`, with a single-line diff on the whole file.
- `scripts/check-progress.sh` gains 2 permanent regression assertions for both edits and passes.
- Levels 1-4 each carry exactly one new, purely-additive `secretAlcove` entry; `validate-levels.mjs` shows zero HARD-FAILs for them.
- `smoke-progress.mjs`'s level 1-4 geometry `deepEqual` checks are EXPECTED to fail until Plan 25-06 lands -- documented, not a regression.
</success_criteria>

<output>
Create `.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/levels/level-02.js` -- edited: `allowedTables` `[1, 2, 3, 4, 5, 6, 7]` -> `[2, 3, 4, 5, 6, 7]` (MATH-01); plus 1 new `secretAlcove` entry (LVL-06)
- `src/math/brain.js` -- edited: line 247's multiplicand roll `* 10 + 1` -> `* 9 + 1`, the ONE authorized literal change (MATH-02)
- `scripts/check-progress.sh` -- edited: 2 new permanent regression assertions guarding MATH-01/MATH-02
- `src/levels/level-01.js` -- edited: 1 new, purely-additive `secretAlcove` entry (LVL-06)
- `src/levels/level-03.js` -- edited: 1 new, purely-additive `secretAlcove` entry (LVL-06)
- `src/levels/level-04.js` -- edited: 1 new, purely-additive `secretAlcove` entry (LVL-06)
