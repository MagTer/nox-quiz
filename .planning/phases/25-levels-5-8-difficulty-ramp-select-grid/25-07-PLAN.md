---
phase: 25-levels-5-8-difficulty-ramp-select-grid
plan: 07
type: execute
wave: 4
depends_on: ["25-02", "25-05", "25-06"]
files_modified:
  - .planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-FINDINGS.md
autonomous: false
requirements: [LVL-02, LVL-03, LVL-04, LVL-05, LVL-06, MATH-01, MATH-02]
must_haves:
  truths:
    - "the full automated suite (check-gate.sh, check-safety.sh, check-import-safety.sh, check-progress.sh, smoke-progress.mjs, validate-levels.mjs, browser-boot.mjs, audit-phase21-mechanics.mjs) runs green across all 8 levels in one pass, with zero triggered:false mechanic encounters"
    - "a human has walked to each of the 8 levels' secret alcove and confirmed: it awards +5 XP exactly once, never freezes the player or opens a challenge, and skipping it costs nothing (level still clears normally)"
    - "a human has confirmed the 2x4 select grid's Up/Down/Left/Right navigation matches the locked wrap semantics, and locked/unlocked/cleared tile states render correctly across all 8 tiles"
  artifacts:
    - .planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-FINDINGS.md
  key_links:
    - "scripts/audit-phase21-mechanics.mjs's deriveEncounters -> levels 5-8's doors/mathGates/enemies/collectZones (auto-discovered, zero script changes needed beyond Plan 25-02's nav fix)"
    - "25-FINDINGS.md -> .planning/phases/24-fix-lengthen-levels-1-4/24-FINDINGS.md (historical baseline: 16 encounters across 4 levels) for a before/after comparison"
---

<objective>
Run the full automated regression + interactive-audit suite across the now-complete 8-level game, and get human sign-off on the two things that suite deliberately cannot check: the 8 secret alcoves (validator/audit kind-lists intentionally exclude `secretAlcove`, per 25-RESEARCH.md Pitfall 3) and the select-grid's actual feel in a live browser.

Per 25-RESEARCH.md's Open Question 1 resolution: this plan closes Phase 25's OWN scope only -- the existing, already-retry-hardened `audit-phase21-mechanics.mjs` run across all 8 levels, now that Plan 25-02's nav fix lets it actually reach levels 5-8. It is explicitly NOT Phase 28's full, formal, human-signed-off VALID-03 closure (STATE.md's blocker note: "Full 8-level closure... remains Phase 28's job"). Do not add new retry infrastructure here.

Purpose: ROADMAP Phase 25's success criteria require levels 5-8 to "play start->goal green on both the static validator and the interactive audit," and LVL-06's "missing it costs nothing" claim for all 8 secret alcoves has zero automated coverage by design. v4.1's own lesson (checks that don't play the game lie) applies here too.
Output: A full green run of every automated gate, captured as evidence. `25-FINDINGS.md` documenting the run, the audit's triggered/resolved counts (compared against the pre-Phase-25 16-encounter/4-level baseline), the MATH-01/MATH-02 diff confirmation, and the human sign-off notes from the checkpoint below.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-CONTEXT.md
@.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-RESEARCH.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated suite across all 8 levels and write 25-FINDINGS.md</name>
  <files>.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-FINDINGS.md</files>
  <read_first>
    scripts/audit-phase21-mechanics.mjs (full file -- confirms it drives all `LEVEL_ORDER` entries automatically, no per-level args needed, always exits 0)
    scripts/browser-boot.mjs (confirms the same auto-discovery via `LEVEL_ORDER`)
    .planning/phases/24-fix-lengthen-levels-1-4/24-FINDINGS.md (tail section -- the pre-Phase-25 baseline: 16 encounters across 4 levels, and the exact "triggered:true is non-negotiable, resolved:true is the goal but not blocking" acceptance bar this plan reuses verbatim)
    .planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-RESEARCH.md (Open Question 1's exact scope-boundary resolution)
  </read_first>
  <action>
    Run, in order: `bash scripts/check-gate.sh`, `bash scripts/check-safety.sh`, `bash scripts/check-import-safety.sh`, `bash scripts/check-progress.sh` (which itself invokes `node scripts/smoke-progress.mjs` as its final step) -- all four must print their respective `PASS` line.
    Run `node scripts/validate-levels.mjs` -- must show zero `HARD-FAIL` rows across all 8 levels (WARN rows are acceptable, matching the project's established "zero HARD-FAILs" definition of green).
    Run `node scripts/browser-boot.mjs` -- must exit 0, printing "Browser boot: PASS", now visiting all 8 levels via Plan 25-02's row/col navigation fix.
    Run `node scripts/audit-phase21-mechanics.mjs`, capturing its full stdout (the JSON `results` array plus the final `AUDIT:` line). Per the SAME acceptance bar Phase 24's Plan 24-06 established: this run PASSES this task's gate if and only if every entry in `results` has `"triggered": true` -- do not block on `"resolved": false` rows (pre-existing, documented resolution-timing flakiness, not a regression). If any row shows `"triggered": false`, diagnose it against that level's geometry (a genuine placement defect in Plans 25-03/25-04's authored content) before this task can be marked done -- do not weaken the audit or the acceptance bar.
    Confirm via `git diff src/math/brain.js` (or re-check Plan 25-04's own evidence) that the LOCKED brain file's total diff for this milestone is exactly the one authorized MATH-02 line.
    Write `.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-FINDINGS.md` (new file) documenting: (a) all 4 shell-gate PASS confirmations plus `validate-levels.mjs`/`smoke-progress.mjs` PASS; (b) the audit's full triggered/resolved counts per level (1-8), comparing the new total encounter count against the pre-Phase-25 16-encounter/4-level baseline (`24-FINDINGS.md`) -- expect a materially higher count now that levels 5-8 contribute their own encounters; (c) the MATH-01/MATH-02 diff confirmation; (d) a placeholder section reserved for Task 2's human sign-off notes, to be filled in after that checkpoint completes.
  </action>
  <acceptance_criteria>
    `check-gate.sh`, `check-safety.sh`, `check-import-safety.sh`, `check-progress.sh` each print their PASS line.
    `node scripts/validate-levels.mjs` shows zero `HARD-FAIL` rows across all 8 levels.
    `node scripts/browser-boot.mjs` exits 0.
    The captured `audit-phase21-mechanics.mjs` JSON output contains zero `"triggered": false` entries, and its total entry count exceeds the pre-Phase-25 16-encounter baseline.
    `25-FINDINGS.md` exists with sections (a) through (d) described above.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs > /tmp/nox-25-audit.json 2>&1; F=$(grep -c '"triggered": false' /tmp/nox-25-audit.json); echo "triggered:false count: $F"; test "$F" = "0"; T=$(grep -c '"triggered": true' /tmp/nox-25-audit.json); echo "triggered:true count: $T"; test "$T" -gt "16"; test -f .planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-FINDINGS.md</automated>
  </verify>
  <done>All automated gates pass; the interactive audit shows zero triggered:false encounters across all 8 levels with a higher total than the 16-encounter/4-level baseline; 25-FINDINGS.md documents the full run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off -- secret alcove walkthrough (all 8 levels) + 2x4 select-grid navigation feel</name>
  <files></files>
  <action>
    Serve the game over HTTP and manually verify the two things Task 1's automated suite deliberately cannot check: the 8 secret alcoves (validator/audit kind-lists exclude `secretAlcove` by design) and the 2x4 select grid's real keyboard-navigation feel. This is a verification-only checkpoint; write no code. Follow the <how-to-verify> steps precisely and record findings in `25-FINDINGS.md`'s reserved sign-off section. If a defect appears, route it back to the relevant Wave 2 plan (25-03/25-04 for alcove placement, 25-05 for the grid/cursor) with the exact symptom and re-verify after the fix.
  </action>
  <what-built>All 8 levels now include a hidden secret XP alcove (flat +5 XP bonus on touch, never freezes the player, no penalty for missing it). The level-select screen now shows a 2x4 grid (4 columns, 2 rows) with Up/Down/Left/Right keyboard navigation plus mouse click, preserving the existing locked/unlocked/cleared tile states. Table 1 and all x10 questions are gone from the math.</what-built>
  <how-to-verify>
    1. Serve the game locally (`python3 -m http.server 8000` from the repo root, then visit `http://localhost:8000/src/index.html`, or open `src/index.html` directly per CLAUDE.md).
    2. Press Space at the title screen to reach the level-select grid. Confirm 8 tiles render in a 4-wide, 2-row layout; on a fresh/incognito session only tile "1" is unlocked (green/accent border), the rest are locked (grey).
    3. Use the arrow keys: confirm Left/Right move within the CURRENT row and wrap at that row's own edges (never spilling into the other row); confirm Up/Down jump between rows WITHOUT wrapping past the top/bottom edge (pressing Up while on row 1, or Down while on row 2, does nothing).
    4. To unlock every tile quickly for testing, open the browser devtools console on the select/title screen and run: `localStorage.setItem("mathlab_platformer_v2", JSON.stringify({version:2,xp:0,level:1,accuracy:{},history:{},levels:Object.fromEntries(["level-01","level-02","level-03","level-04","level-05","level-06","level-07"].map(id=>[id,{cleared:true}]))}))`, then reload -- this marks levels 1-7 cleared so all 8 tiles show unlocked/cleared states.
    5. For EACH of the 8 levels: open `src/levels/level-0N.js` and note its `geometry.secretAlcove` `{x, y}` coordinate as a landmark, then enter that level and walk/jump to it. Confirm: (a) touching it visibly increments the HUD's XP display by exactly 5; (b) the player is never frozen/paused, and no challenge/answer-box UI appears; (c) touching the same alcove again does nothing further; (d) completing the level WITHOUT touching its alcove still lets it clear normally.
    6. Confirm level-07 and level-08 each require climbing UP through a platform chain (verticality) to reach the goal, and the camera visibly pans vertically during the climb.
    7. Spot-check ~10-15 questions across a few levels: confirm no question ever shows "x1" as the visible table, and no question's second number is ever 10.
  </how-to-verify>
  <resume-signal>Reply "approved" if every check in steps 2-7 passes, or describe exactly which level/alcove/behavior failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Read-only interactive-audit run against the local, offline, single-player client (served on loopback-only 127.0.0.1 by the pre-existing, already-reviewed audit scripts) plus a documentation-only findings write. No network input from an untrusted source, no auth, no session, no access control, no crypto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-13 | Repudiation | The "every mechanic encounter triggered:true, all 8 alcoves confirmed" claim | low | mitigate | Task 1's acceptance criteria require the literal captured JSON output (grep-counted, not eyeballed) showing zero `triggered: false` entries with a higher total than the documented 16-encounter baseline; Task 2's checkpoint requires an explicit human "approved" or an itemized-failure resume-signal, never a rubber-stamp |
| T-25-14 | Tampering | `25-FINDINGS.md` (new file) | low | accept | New evidence document, not an edit to any historical FINDINGS.md -- no existing content at risk |

## Package Legitimacy Audit

Not applicable -- this plan installs zero new packages.
</threat_model>

<verification>
Task 1's full-suite run is the final automated proof required by ROADMAP Phase 25's success criteria. Task 2's human checkpoint is the final proof for the two things automation deliberately cannot check (secret-alcove reachability/reward, and live select-grid feel) -- neither `validate-levels.mjs` nor `audit-phase21-mechanics.mjs` recognizes `secretAlcove` as a checked kind, by design (25-RESEARCH.md Pitfall 3).
</verification>

<success_criteria>
- `check-gate.sh`, `check-safety.sh`, `check-import-safety.sh`, `check-progress.sh`, `validate-levels.mjs`, `browser-boot.mjs` all pass.
- `audit-phase21-mechanics.mjs` shows zero `triggered:false` encounters across all 8 levels, with a higher total encounter count than the pre-Phase-25 16-encounter baseline.
- `25-FINDINGS.md` exists with full run evidence and the human sign-off notes.
- A human has approved the secret-alcove walkthrough and the select-grid navigation feel.
</success_criteria>

<output>
Create `.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-07-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `.planning/phases/25-levels-5-8-difficulty-ramp-select-grid/25-FINDINGS.md` -- new: full-suite run evidence, audit triggered/resolved counts (vs. the 16-encounter/4-level baseline), MATH-01/MATH-02 diff confirmation, and human sign-off notes (LVL-02, LVL-03, LVL-04, LVL-05, LVL-06, MATH-01, MATH-02)
