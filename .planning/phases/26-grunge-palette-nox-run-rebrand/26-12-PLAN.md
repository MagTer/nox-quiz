---
phase: 26
plan: 12
type: execute
wave: 3.5
depends_on: ["26-03"]
files_modified:
  - src/config.js
  - scripts/check-contrast.mjs
  - scripts/build-art-assets.py
  - .planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTRAST.md
  - .planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md
  - .planning/ROADMAP.md
  - assets/parallax/far-theme-1.png
  - assets/parallax/mid-theme-1.png
  - assets/parallax/near-theme-1.png
  - assets/tiles/ground-theme-1.png
  - "(same 4-file pattern x8 themes — 32 rebaked PNGs total under assets/parallax/ and assets/tiles/)"
autonomous: false
requirements: [VIS-02, VIS-03]

must_haves:
  truths:
    - "CONFIG.PALETTE has 8 total accent hues (up from 3), forming one dedicated, WCAG-clearing, non-pink accent per level — replacing the 3-hue scheme that produced identical themes for level pairs (1==2, 3==4, 7==8)."
    - "A human has explicitly signed off, against a rendered swatch proof of the 5 NEW accent hues only (the 3 pre-existing hues already carry 26-02's sign-off and are not re-reviewed), that none reads as pink/magenta/mauve."
    - "All 8 per-level theme bakes (parallax far/mid/near + ground tileset) are regenerated so every one of the 8 levels reads as visually distinct — no two levels share an identical theme."
  artifacts:
    - "CONFIG.PALETTE: 5 new keys ACCENT_FERN, ACCENT_TEAL, ACCENT_STEEL, ACCENT_CLAY, ACCENT_EMBER (8 accents total with the pre-existing MOSS/SLATE/RUST)"
    - "26-CONTRAST.md — refreshed WCAG contrast table covering all 8 accents"
    - "26-PALETTE-SWATCH.png — regenerated swatch proof of all 19 CONFIG.PALETTE roles"
    - "scripts/build-art-assets.py THEME_PALETTES — rewritten to one dedicated accent per level, 32 theme PNGs rebaked"
  key_links:
    - "THEME_PALETTES entries key off CONFIG.PALETTE's ACCENT_* tokens the same way 26-03 established — this plan changes the assignment (1 accent per level instead of 3 shared accents), not the mechanism"
---

<objective>
Mid-execution correction, requested directly by the user after reviewing Wave 3's output: the phase's original "exactly 3 new hue-tinted darks" decision (26-CONTEXT.md, VIS-02) produced only 3 accent hues shared across 8 levels, which 26-03's THEME_PALETTES assignment exposed as literal duplication — theme-1 and theme-2 are pixel-identical, as are theme-3/theme-4 and theme-7/theme-8. That directly undercuts VIS-03's own success criterion ("each of the 8 levels has a distinct background/accent theme tint"). The user asked for more color variety ("bring more life into the game"); after discussion the concrete, load-bearing number is 8 — one genuinely distinct accent hue per level, not an arbitrary round number. This plan expands CONFIG.PALETTE's accents from 3 to 8, re-verifies WCAG/banned-hue compliance, gets human sign-off on only the 5 net-new hues (the original 3 already have 26-02's sign-off and are reused as-is, not re-litigated), then reassigns and rebakes all 8 per-level themes so each level gets its own dedicated accent instead of the old moss/slate/rust-repeated-across-pairs scheme.

Output: 5 new CONFIG.PALETTE accent tokens; updated 26-CONTRAST.md; regenerated 26-PALETTE-SWATCH.png; a closed human sign-off checkpoint on the 5 new hues; THEME_PALETTES rewritten to one-accent-per-level; all 32 theme PNGs rebaked; 26-CONTEXT.md and ROADMAP.md annotated with this revised decision (dated, matching the phase's existing "Superseded"/"Added" annotation convention).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-02-SUMMARY.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand CONFIG.PALETTE from 3 to 8 accent hues</name>
  <files>src/config.js</files>
  <read_first>src/config.js's PALETTE object — the 3 existing ACCENT_MOSS/ACCENT_SLATE/ACCENT_RUST entries added by 26-02</read_first>
  <action>
Add exactly 5 new keys to the PALETTE object (do not rename, remove, or change the RGB values of the 3 existing ACCENT_* entries — they already carry human sign-off from 26-02 and must be reused as-is): ACCENT_FERN: [0x4a, 0x70, 0x58] (level 2 — warm mid-green, between moss and teal), ACCENT_TEAL: [0x45, 0x70, 0x70] (level 3 — green-to-blue transitional), ACCENT_STEEL: [0x4a, 0x56, 0x68] (level 5 — cooler/darker blue-grey than slate), ACCENT_CLAY: [0x70, 0x5a, 0x48] (level 6 — warm grey-brown, transitional toward rust), ACCENT_EMBER: [0xa8, 0x50, 0x2c] (level 8 — the harshest/most saturated stop, past rust). These 5 values are starting points, not final — Task 2 will re-run scripts/check-contrast.mjs; if any new token fails its WCAG 3:1 UI-component threshold against BG or reads as a banned hue, brighten/adjust it (same pattern 26-02 used for the original 3 tokens: iterate the literal until check-contrast.mjs passes) and record the final values actually shipped.
  </action>
  <acceptance_criteria>
    - CONFIG.PALETTE has exactly 8 ACCENT_* keys total: MOSS, FERN, TEAL, SLATE, STEEL, CLAY, RUST, EMBER.
    - The 3 pre-existing tokens (MOSS, SLATE, RUST) are byte-unchanged from their 26-02 values.
    - `node scripts/check-contrast.mjs` exits 0 with all 8 accents clearing their WCAG threshold and zero banned-hue hits.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-contrast.mjs; echo "exit:$?"</automated>
  </verify>
  <done>CONFIG.PALETTE has 8 accent hues, all WCAG-clearing and non-pink, verified by check-contrast.mjs exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Update 26-CONTRAST.md and regenerate the swatch proof image</name>
  <files>.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTRAST.md, scripts/build-art-assets.py (no code change expected — build_palette_swatch() already reads CONFIG.PALETTE live)</files>
  <action>
Re-run `node scripts/check-contrast.mjs` and update 26-CONTRAST.md's role table to reflect all 8 accent hues (was 3) — same doc, same format established by 26-02, just refreshed output. Then run `python3 scripts/build-art-assets.py` to regenerate 26-PALETTE-SWATCH.png — build_palette_swatch() already reads CONFIG.PALETTE live via the node subprocess call from 26-02, so it will automatically include the 5 new roles with zero code changes needed; do not hand-edit the swatch generator unless the existing grid layout breaks with 19 total roles (13 base + 8 accents — up from 16).
  </action>
  <acceptance_criteria>
    - 26-CONTRAST.md's table shows 8 accent rows with real (not placeholder) computed ratios.
    - 26-PALETTE-SWATCH.png regenerated and contains all 19 CONFIG.PALETTE roles, including all 8 accents.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-contrast.mjs && python3 scripts/build-art-assets.py && test -f .planning/phases/26-grunge-palette-nox-run-rebrand/26-PALETTE-SWATCH.png</automated>
  </verify>
  <done>26-CONTRAST.md and 26-PALETTE-SWATCH.png both reflect the full 8-accent palette.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off — the 5 NEW accent hues only</name>
  <files></files>
  <action>
    Open the regenerated swatch proof image and record a blocking human-verify sign-off — scoped ONLY to the 5 net-new hues (ACCENT_FERN, ACCENT_TEAL, ACCENT_STEEL, ACCENT_CLAY, ACCENT_EMBER). The 3 pre-existing hues (MOSS, SLATE, RUST) already carry 26-02's sign-off and are not being re-litigated here. This is a verification-only checkpoint; write no code. If any new hue reads as pink/magenta/mauve, or breaks the intended calm-to-harsh progression, route back to Task 1 and re-verify.
  </action>
  <what-built>Updated .planning/phases/26-grunge-palette-nox-run-rebrand/26-PALETTE-SWATCH.png with 5 new accent swatches (ACCENT_FERN, ACCENT_TEAL, ACCENT_STEEL, ACCENT_CLAY, ACCENT_EMBER) alongside the 16 already-approved roles.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/26-grunge-palette-nox-run-rebrand/26-PALETTE-SWATCH.png`.
    2. Look specifically at the 5 new swatches: ACCENT_FERN, ACCENT_TEAL, ACCENT_STEEL, ACCENT_CLAY, ACCENT_EMBER.
    3. Confirm none reads as pink, magenta, or mauve — ACCENT_EMBER (the most saturated/warm of the 8) is the one most worth a close look.
    4. Confirm the 8 accents, read left-to-right in level order (MOSS→FERN→TEAL→SLATE→STEEL→CLAY→RUST→EMBER), form a plausible calm-green-to-harsh-rust progression rather than a random scatter.
  </how-to-verify>
  <resume-signal>Type "approved" or name the specific new hue(s) that read as pink/magenta/mauve or break the progression</resume-signal>
  <verify>
    <human-check>Human confirms from the real swatch image that none of the 5 new accent hues reads as pink/magenta/mauve, and the 8-hue progression reads as coherent.</human-check>
  </verify>
  <done>Human sign-off recorded that the 5 new accent hues meet the non-pink bar and complete a coherent 8-stop progression; or a defect list routed back to Task 1.</done>
</task>

<task type="auto">
  <name>Task 4: Reassign THEME_PALETTES to one dedicated accent per level and rebake</name>
  <files>scripts/build-art-assets.py, assets/parallax/*-theme-{1..8}.png, assets/tiles/ground-theme-{1..8}.png</files>
  <read_first>scripts/build-art-assets.py's THEME_PALETTES dict and _accent_sub() helper added by 26-03</read_first>
  <action>
Rewrite THEME_PALETTES so each of the 8 theme entries uses exactly ONE dedicated accent for all 4 of its far/mid/near/ground slots, in level order: theme-1→ACCENT_MOSS, theme-2→ACCENT_FERN, theme-3→ACCENT_TEAL, theme-4→ACCENT_SLATE, theme-5→ACCENT_STEEL, theme-6→ACCENT_CLAY, theme-7→ACCENT_RUST, theme-8→ACCENT_EMBER. Remove the old 2-color blending special-case previously used for theme-5/theme-6 (no longer needed — the 8-hue set now provides organic transitions on its own). Re-run the existing bake pipeline (`python3 scripts/build-art-assets.py`) to regenerate all 32 PNGs (8 themes x 4 files: far/mid/near parallax + ground tileset).
  </action>
  <acceptance_criteria>
    - THEME_PALETTES has exactly one accent per theme entry, no multi-accent blending.
    - All 8 themes produce visually distinct baked output — no two theme-N directories are byte-identical (spot check: `cmp` theme-1 vs theme-2 ground PNGs, expect them to differ; previously they were identical).
    - `bash scripts/check-safety.sh` still passes (no src/ behavior touched by this plan beyond config.js data and build-time asset generation).
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/build-art-assets.py && ! cmp -s assets/tiles/ground-theme-1.png assets/tiles/ground-theme-2.png && bash scripts/check-safety.sh</automated>
  </verify>
  <done>All 8 per-level themes are genuinely distinct (no duplicate pairs), rebaked from the 8-accent palette.</done>
</task>

<task type="auto">
  <name>Task 5: Annotate 26-CONTEXT.md and ROADMAP.md with the revised decision</name>
  <files>.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md, .planning/ROADMAP.md</files>
  <read_first>26-CONTEXT.md's existing VIS-02 decision bullet ("Add exactly 3 new hue-tinted darks..."); ROADMAP.md's Phase 26 success criterion 1 and its existing dated "Superseded"/"Added" annotation convention (see criteria 5 and 6)</read_first>
  <action>
In 26-CONTEXT.md, append a dated addendum note to the VIS-02 decision bullet (do not delete the original text — match the file's existing amendment convention): "**Revised 2026-07-07 (mid-execution, user request):** expanded from 3 to 8 accent hues — one dedicated accent per level — after Wave 3's bake exposed that 3 shared hues produced identical themes for levels 1/2, 3/4, and 7/8, undercutting VIS-03's distinctness requirement. See 26-12-PLAN.md." In ROADMAP.md's Phase 26 success criterion 1, append a matching inline dated note in the same style as criteria 5/6: "**Revised 2026-07-07 (26-12-PLAN.md):** expanded to 8 total accent hues (one per level) — see 26-CONTEXT.md addendum."
  </action>
  <acceptance_criteria>
    - 26-CONTEXT.md's original VIS-02 decision text is preserved; the addendum is appended, not a replacement.
    - ROADMAP.md's criterion 1 carries the same dated annotation style already used for criteria 5/6 in this phase's section.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Revised 2026-07-07" .planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md && grep -q "Revised 2026-07-07" .planning/ROADMAP.md</automated>
  </verify>
  <done>Both docs transparently record the mid-execution palette-size revision with dated annotations, matching this phase's existing convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure palette-data expansion + rebake of already-reviewed build-time art pipeline; no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-12a | Tampering | scripts/build-art-assets.py THEME_PALETTES rewrite | low | accept | Pure data reassignment within an already-reviewed pipeline (26-03); regression risk is a wrong/duplicate theme mapping, caught by Task 4's own cmp-based distinctness check |

</threat_model>

<verification>
`node scripts/check-contrast.mjs` exits 0 with 8 accents. `python3 scripts/build-art-assets.py` regenerates the swatch and all 32 theme PNGs with no errors. Human checkpoint (Task 3) explicitly closed with "approved" for the 5 new hues. `bash scripts/check-safety.sh` still passes. No two theme-N baked outputs are identical.
</verification>

<success_criteria>
CONFIG.PALETTE carries 8 total accent hues, WCAG-clearing and non-pink, with human sign-off on the 5 net-new ones. All 8 per-level themes are genuinely visually distinct — the level-1==level-2 style duplication from the original 3-hue scheme is eliminated. Both 26-CONTEXT.md and ROADMAP.md transparently record this as a dated, user-requested mid-execution revision rather than silently overwriting the original decision.
</success_criteria>

<output>
Create `.planning/phases/26-grunge-palette-nox-run-rebrand/26-12-SUMMARY.md` when done
</output>
