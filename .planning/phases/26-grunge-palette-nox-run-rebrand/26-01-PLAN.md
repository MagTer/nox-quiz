---
phase: 26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.js
  - src/scenes/title.js
  - src/scenes/select.js
  - src/ui/hud.js
  - src/ui/challenge.js
  - src/ui/mathGate.js
  - src/fx.js
  - src/levels/build.js
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "Every color used in src/scenes/, src/ui/, src/fx.js, and src/levels/build.js is read from CONFIG.PALETTE — zero raw [0x..,0x..,0x..] color-array literals remain in those files."
    - "config.js's own DOOR/MATH_GATE/ENEMY/COLLECT blocks source their color values from CONFIG.PALETTE instead of re-declaring hex literals (closes the DOOR/MATH_GATE mirror duplication RESEARCH.md documented)."
  artifacts:
    - "src/config.js: new module-level `const PALETTE = {...}` (12 named color-role keys), exposed as `CONFIG.PALETTE`"
  key_links:
    - "src/config.js CONFIG.PALETTE -> src/scenes/title.js, src/scenes/select.js, src/ui/hud.js, src/ui/challenge.js, src/ui/mathGate.js, src/fx.js, src/levels/build.js (every color()/rect()/outline() call site)"
---

<objective>
Centralize every duplicated (and un-duplicated-but-raw) color literal across the codebase into one `CONFIG.PALETTE` object in `src/config.js`, per VIS-01. This is the FIRST plan of the phase — every later palette-expansion, per-level theming, door/enemy sprite, and logo task reads from `CONFIG.PALETTE`, so centralizing first (per 26-RESEARCH.md's explicit sequencing recommendation) avoids rework.

Purpose: VIS-01 is a hard prerequisite for VIS-02 (expansion) and the contrast-check script — you cannot document per-role WCAG contrast for a palette that doesn't exist as a single source of truth yet.
Output: `CONFIG.PALETTE` in `src/config.js`; zero raw color-array literals remaining in `src/scenes/`, `src/ui/`, `src/fx.js`, `src/levels/build.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-RESEARCH.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CONFIG.PALETTE and rewire config.js's own DOOR/MATH_GATE/ENEMY/COLLECT blocks</name>
  <files>src/config.js</files>
  <read_first>src/config.js (full file — DOOR block lines 98-104, MATH_GATE lines 108-114, ENEMY lines 118-123, COLLECT lines 129-144)</read_first>
  <action>
Add a new module-top-level `const PALETTE = {...}` object literal in src/config.js, placed BEFORE `export const CONFIG = {...}` (plain data literal — no engine calls, safe at module top level per a727c13). Define exactly these 12 keys with these exact RGB triples:
BG:[0x0a,0x0a,0x0a], SURFACE:[20,20,20], SURFACE_ALT:[30,30,30], SURFACE_UNLOCKED:[0x11,0x11,0x11], BORDER:[0x33,0x33,0x33], MUTED:[0x44,0x44,0x44], MUTED_BORDER:[0x55,0x55,0x55], TEXT:[0xe8,0xe8,0xe8], TEXT_DIM:[0x88,0x88,0x88], DANGER:[0xff,0x44,0x33], REWARD:[0x00,0xff,0x88], CLEARED:[0x66,0xcc,0xff], CURSOR:[0xff,0xff,0xff].

Expose it as the first key of the exported CONFIG object: `PALETTE,` (shorthand, so external code reads `CONFIG.PALETTE.REWARD` etc.).

Then rewire config.js's own blocks to read the VALUE from PALETTE instead of re-declaring the hex literal — keep every existing KEY NAME unchanged (only the value source changes, so no consumer call site elsewhere in the codebase needs to change in this task):
- CONFIG.DOOR.LOCKED_GREY value becomes PALETTE.MUTED; CONFIG.DOOR.LOCKED_BORDER value becomes PALETTE.MUTED_BORDER.
- CONFIG.MATH_GATE.LOCKED_GREY value becomes PALETTE.MUTED; CONFIG.MATH_GATE.LOCKED_BORDER value becomes PALETTE.MUTED_BORDER. (This keeps the DOOR/MATH_GATE mirror intentional and now genuinely single-sourced instead of two independent literals that happened to match.)
- CONFIG.ENEMY.COLOR value becomes PALETTE.DANGER.
- CONFIG.COLLECT.PICKUP_BG value becomes PALETTE.SURFACE_ALT; CONFIG.COLLECT.PICKUP_BORDER value becomes PALETTE.MUTED; CONFIG.COLLECT.PICKUP_FG value becomes PALETTE.TEXT.

Do not touch CONFIG.DOOR.W/H/GLYPH_SIZE, CONFIG.MATH_GATE.W/H/GLYPH_SIZE, CONFIG.ENEMY.W/H/GLYPH_SIZE, or CONFIG.COLLECT.ZONE_W/H/PICKUP_W/H/PICKUP_SIZE — only the color-array fields.
  </action>
  <acceptance_criteria>
    - `CONFIG.PALETTE` exists with exactly the 12 keys listed above, each a 3-element array.
    - `CONFIG.DOOR.LOCKED_GREY === CONFIG.PALETTE.MUTED` (same array reference or deep-equal values) and likewise for the other 7 rewired fields.
    - No other CONFIG block's numeric (non-color) fields changed.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/config.js && node --input-type=module -e "import { CONFIG } from './src/config.js'; if (!CONFIG.PALETTE || Object.keys(CONFIG.PALETTE).length !== 12) throw new Error('PALETTE missing/wrong shape'); if (CONFIG.DOOR.LOCKED_GREY[0] !== CONFIG.PALETTE.MUTED[0]) throw new Error('DOOR not rewired'); console.log('ok');"</automated>
  </verify>
  <done>CONFIG.PALETTE exists with all 12 named keys; DOOR/MATH_GATE/ENEMY/COLLECT color fields reference PALETTE values; node --check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep title.js, hud.js, fx.js to read CONFIG.PALETTE</name>
  <files>src/scenes/title.js, src/ui/hud.js, src/fx.js</files>
  <read_first>src/scenes/title.js (lines 22-35), src/ui/hud.js (lines 31-37), src/fx.js (lines 33-37)</read_first>
  <action>
In each file, delete the local color-array `const` declarations and replace every reference with the equivalent `CONFIG.PALETTE.<KEY>` (CONFIG is already imported in all three files). Exact mapping:
- title.js: ACCENT_GREEN -> CONFIG.PALETTE.REWARD, HINT_FG -> CONFIG.PALETTE.TEXT, RESET_FG -> CONFIG.PALETTE.TEXT_DIM, DANGER_RED -> CONFIG.PALETTE.DANGER, PANEL_BG -> CONFIG.PALETTE.SURFACE, PANEL_BORDER -> CONFIG.PALETTE.BORDER.
- hud.js: TRACK_GREY -> CONFIG.PALETTE.BORDER, ACCENT_GREEN -> CONFIG.PALETTE.REWARD, HINT_FG -> CONFIG.PALETTE.TEXT.
- fx.js: ACCENT_GREEN -> CONFIG.PALETTE.REWARD.
Every `color(X[0], X[1], X[2])` call site keeps the identical 3-index-destructure call shape — only the array reference changes (e.g. `color(CONFIG.PALETTE.REWARD[0], CONFIG.PALETTE.REWARD[1], CONFIG.PALETTE.REWARD[2])`). Do not change any layout/sizing/z-order/tween logic in these files — this is a color-source swap only.
  </action>
  <acceptance_criteria>
    - No `const X = [0x` declarations remain in title.js, hud.js, or fx.js.
    - Every prior color() call site now references CONFIG.PALETTE.&lt;KEY&gt;.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "= \[0x" src/scenes/title.js src/ui/hud.js src/fx.js | awk -F: '{s+=$2} END {exit (s==0)?0:1}'</automated>
  </verify>
  <done>Zero raw color-array literals remain in title.js, hud.js, fx.js; all reference CONFIG.PALETTE.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep select.js, challenge.js, mathGate.js, and build.js's LABEL_FG to read CONFIG.PALETTE</name>
  <files>src/scenes/select.js, src/ui/challenge.js, src/ui/mathGate.js, src/levels/build.js</files>
  <read_first>src/scenes/select.js (lines 32-49), src/ui/challenge.js (lines 41-50), src/ui/mathGate.js (lines 16-17), src/levels/build.js (lines 33-38, and the 3 LABEL_FG usages in the door/math-gate/enemy glyph blocks)</read_first>
  <action>
select.js: ACCENT_GREEN and SELECTABLE_BORDER -> CONFIG.PALETTE.REWARD, UNLOCKED_FILL -> CONFIG.PALETTE.SURFACE_UNLOCKED, LOCKED_GREY -> CONFIG.PALETTE.MUTED, CLEARED_BLUE -> CONFIG.PALETTE.CLEARED, LABEL_FG -> CONFIG.PALETTE.TEXT, LOCKED_BORDER -> CONFIG.PALETTE.MUTED_BORDER, CURSOR_BORDER -> CONFIG.PALETTE.CURSOR.
challenge.js: PANEL_BG -> CONFIG.PALETTE.SURFACE, PANEL_BORDER -> CONFIG.PALETTE.BORDER, BOX_BG -> CONFIG.PALETTE.SURFACE_ALT, BOX_BORDER -> CONFIG.PALETTE.MUTED, ACCENT_RED -> CONFIG.PALETTE.DANGER, LABEL_FG -> CONFIG.PALETTE.TEXT.
mathGate.js: ACCENT_GREEN -> CONFIG.PALETTE.REWARD.
build.js: delete the module-level `const LABEL_FG = [0xe8, 0xe8, 0xe8]` (line 38) entirely and switch its 3 usages (door-glyph text color, math-gate-glyph text color, enemy-glyph text color) to `CONFIG.PALETTE.TEXT` (build.js already imports CONFIG from "../config.js").
Delete every local const declaration you replace — do not leave an unused alias.
  </action>
  <acceptance_criteria>
    - No `const X = [0x` declarations remain in select.js, challenge.js, mathGate.js.
    - build.js has no module-level LABEL_FG const; its 3 glyph text() calls reference CONFIG.PALETTE.TEXT.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "= \[0x" src/scenes/select.js src/ui/challenge.js src/ui/mathGate.js src/levels/build.js | awk -F: '{s+=$2} END {exit (s==0)?0:1}' && bash scripts/check-safety.sh && node scripts/validate-levels.mjs</automated>
  </verify>
  <done>Zero raw color-array literals in select.js/challenge.js/mathGate.js/build.js; check-safety.sh PASS; validate-levels.mjs PASS zero HARD-FAILs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure internal color-literal refactor; no new input, no new external data, no new trust boundary crosses this plan's scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering | src/config.js CONFIG.PALETTE | low | accept | Pure refactor of existing, already-shipped color values — no new value is introduced, only re-sourced; `check-safety.sh` and `validate-levels.mjs` regression-gate the change |

</threat_model>

<verification>
Full VIS-01 gate (also runs as the phase-level VALIDATION.md check for VIS-01): `grep -rn "= \[0x" src/scenes/ src/ui/ src/fx.js src/levels/build.js` must return zero rows. `bash scripts/check-safety.sh` and `node scripts/validate-levels.mjs` must both PASS (zero-regression proof — this plan touches build.js's glyph rendering, which validate-levels.mjs's structural checks exercise indirectly via the level registry).
</verification>

<success_criteria>
CONFIG.PALETTE exists as the single source of truth for every color used across src/scenes/, src/ui/, src/fx.js, and src/levels/build.js. Zero raw `[0x..,0x..,0x..]` literals remain in those files. No visual change to the running game (identical hex values, just re-sourced).
</success_criteria>

## Artifacts This Phase Produces

- `src/config.js`: new module-level `const PALETTE = {...}` (12 keys: BG, SURFACE, SURFACE_ALT, SURFACE_UNLOCKED, BORDER, MUTED, MUTED_BORDER, TEXT, TEXT_DIM, DANGER, REWARD, CLEARED, CURSOR), exposed as `CONFIG.PALETTE`.
- `CONFIG.DOOR.LOCKED_GREY/LOCKED_BORDER`, `CONFIG.MATH_GATE.LOCKED_GREY/LOCKED_BORDER`, `CONFIG.ENEMY.COLOR`, `CONFIG.COLLECT.PICKUP_BG/PICKUP_BORDER/PICKUP_FG` — re-sourced to read from `PALETTE.*` (key names unchanged, only value source).

<output>
Create `.planning/phases/26-grunge-palette-nox-run-rebrand/26-01-SUMMARY.md` when done
</output>
