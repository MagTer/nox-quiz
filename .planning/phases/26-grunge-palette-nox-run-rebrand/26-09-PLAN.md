---
phase: 26
plan: 09
type: execute
wave: 7
depends_on: ["26-07", "26-08"]
files_modified:
  - src/index.html
  - src/config.js
  - src/progress.js
  - src/main.js
  - src/scenes/title.js
  - scripts/check-progress.sh
  - scripts/browser-boot.mjs
  - scripts/audit-phase21-mechanics.mjs
  - scripts/screenshot-phase20.mjs
  - scripts/screenshot-phase26.mjs
autonomous: true
requirements: [BRAND-02]

must_haves:
  truths:
    - "No 'Math Lab'/'mathlab' string remains in src/index.html, src/config.js, src/progress.js, src/main.js, or src/scenes/title.js, except the explicitly allowlisted src/progress.js school-game references."
    - "The localStorage save key is renamed to a Nox Run-branded key, and every script that hardcodes the literal (check-progress.sh's grep assertion + all 4 Playwright scripts' SAVE_KEY consts) is updated in this SAME plan — per 26-RESEARCH.md Pitfall A, a stale grep assertion after a key rename produces a misleading FAIL, not a real regression."
  artifacts:
    - "src/config.js: CONFIG.SAVE.KEY renamed to \"noxrun_platformer_v1\""
  key_links:
    - "src/config.js CONFIG.SAVE.KEY -> scripts/check-progress.sh's grep assertion, and the SAVE_KEY const in browser-boot.mjs/audit-phase21-mechanics.mjs/screenshot-phase20.mjs/screenshot-phase26.mjs (all 5 must agree on the literal, in the same commit)"
---

<objective>
Sweep every "Math Lab"/"mathlab" occurrence out of the core src/ runtime files and rename the localStorage save key, per BRAND-02. The save key is explicitly NOT required to survive this rebrand (26-CONTEXT.md's confirmed override) — renaming it (rather than keeping the old branded literal and permanently special-casing it in the new BRAND-02 regression gate) is the cleaner path per 26-RESEARCH.md's own recommendation, and the user already confirmed the resulting fresh-start reset is acceptable.

Purpose: this is the core code half of BRAND-02's full sweep; 26-10 covers the docs/deploy/scripts-docstring half in parallel. Sequenced after 26-07 (logo, touches config.js/main.js/title.js) and 26-08 (creates scripts/screenshot-phase26.mjs, whose SAVE_KEY const this plan must also sync).
Output: zero "Math Lab" strings in the 5 core src/ files; a renamed save key consistently applied across config.js + all 5 scripts that reference it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html + config.js sweep and save-key rename</name>
  <files>src/index.html, src/config.js</files>
  <read_first>src/index.html full file (lines 6, 37, 41 — title/h1/fallback-title); src/config.js lines 161-170 (SAVE block + its comment), 217-241 (TITLE block comments referencing "Math Lab")</read_first>
  <action>
<!-- planner-discipline-allow: math lab -->
This task's own literal target string is named below so the executor knows exactly what to remove — this is the rename source text, not example/illustration code.
index.html: change `&lt;title&gt;Math Lab — loading&lt;/title&gt;` to `&lt;title&gt;Nox Run — loading&lt;/title&gt;`; in the file:// guard's injected HTML, change the fallback `&lt;title&gt;Math Lab — run via a web server&lt;/title&gt;` to `&lt;title&gt;Nox Run — run via a web server&lt;/title&gt;` and the `&lt;h1&gt;Run via a web server&lt;/h1&gt;` block's surrounding copy stays as-is (it doesn't name the game) — only the two literal `Math Lab` occurrences in that inline script change.

config.js: rename `CONFIG.SAVE.KEY` from `"mathlab_platformer_v2"` to `"noxrun_platformer_v1"`. Rewrite the SAVE block's comment (currently "v4.0 clean-reset key — new namespace, no migration... The v3.0 blob under the old key (mathlab_platformer_v?) is NOT migrated...") to record this as the Nox Run rebrand's clean-reset key: note that the prior `mathlab_platformer_v2` key is intentionally orphaned (not migrated, not deleted) per the explicit user-confirmed decision in 26-CONTEXT.md, and that `CONFIG.SAVE.KEY` remains the single source of truth read by scripts/check-progress.sh and every Playwright script's `SAVE_KEY` const (updated together in this same plan, Task 3). Also rewrite the two "Math Lab" wordmark comments in the TITLE block (lines ~217, ~221 — these are stale anyway since 26-07 already replaced the actual wordmark with the logo sprite) to reference "Nox Run" / the logo.
  </action>
  <acceptance_criteria>
    - index.html has zero "Math Lab" occurrences; both &lt;title&gt; strings read "Nox Run".
    - CONFIG.SAVE.KEY === "noxrun_platformer_v1".
    - config.js's SAVE and TITLE block comments no longer say "Math Lab".
  </acceptance_criteria>
  <verify>
    <automated>! grep -qi "math lab" src/index.html && node --input-type=module -e "import { CONFIG } from './src/config.js'; if (CONFIG.SAVE.KEY !== 'noxrun_platformer_v1') throw new Error('key not renamed');"</automated>
  </verify>
  <done>index.html shows "Nox Run" everywhere a title renders; CONFIG.SAVE.KEY is the new noxrun_platformer_v1 literal.</done>
</task>

<task type="auto">
  <name>Task 2: progress.js, main.js, title.js sweep</name>
  <files>src/progress.js, src/main.js, src/scenes/title.js</files>
  <read_first>src/progress.js lines 26-30 and 202-205 (the ALLOWLISTED mathlab_save_* school-game references — do NOT touch these) and lines 299/304/310/329/331/350 (the 6 console.warn("[MathLab] ...") calls); src/main.js line 1 (header comment); src/scenes/title.js lines 1, 38 (header/inline comments referencing "Math Lab" title scene)</read_first>
  <action>
<!-- planner-discipline-allow: math lab -->
<!-- planner-discipline-allow: mathlab_save -->
This task's own literal target strings are named below so the executor knows exactly what to remove vs. what to preserve untouched — these are rename-source/allowlist text, not example/illustration code.
progress.js: rewrite each of the 6 `console.warn("[MathLab] ...")` calls to `console.warn("[NoxRun] ...")` (same message text after the tag, only the bracketed tag changes). Do NOT touch lines 28/204's "mathlab_save_*"/"mathlab_save_v1/v2" comments — those describe the ORIGINAL SCHOOL GAME's unrelated keys and are explicitly allowlisted per 26-CONTEXT.md; leave them byte-identical, including their surrounding "school game" framing.
main.js: rewrite the header comment "src/main.js — Math Lab v3.0 game shell (Phase 8 platformer boot)" to reference "Nox Run" instead of "Math Lab" (keep the rest of the comment's meaning intact — it's describing the boot module, not the brand).
title.js: rewrite the header comment "the Math Lab title scene" and any remaining inline "Math Lab" references (the wordmark text() call itself was already replaced by 26-07's logo sprite, so only comment prose remains) to "Nox Run".
  </action>
  <acceptance_criteria>
    - All 6 console.warn tags in progress.js read "[NoxRun]"; lines 28/204's school-game comments are byte-unchanged.
    - main.js and title.js header/inline comments no longer say "Math Lab".
  </acceptance_criteria>
  <verify>
    <automated>grep -c "\[NoxRun\]" src/progress.js | grep -qx 6 && grep -q "mathlab_save" src/progress.js && ! grep -qi "math lab" src/main.js src/scenes/title.js</automated>
  </verify>
  <done>progress.js's 6 dev-console tags are rebranded while its 2 allowlisted school-game comments survive untouched; main.js/title.js carry no "Math Lab" prose.</done>
</task>

<task type="auto">
  <name>Task 3: Sync the save-key literal across all 5 scripts that hardcode it</name>
  <files>scripts/check-progress.sh, scripts/browser-boot.mjs, scripts/audit-phase21-mechanics.mjs, scripts/screenshot-phase20.mjs, scripts/screenshot-phase26.mjs</files>
  <read_first>scripts/check-progress.sh lines 50-53 (the grep assertion checking for the OLD literal); each of the 4 .mjs scripts' `const SAVE_KEY = "mathlab_platformer_v2";` line</read_first>
  <action>
<!-- planner-discipline-allow: mathlab_platformer_v2 -->
This task's own literal target string is named below so the executor knows exactly what to remove — this is the rename source text, not example/illustration code.
This is the exact scenario 26-RESEARCH.md's Pitfall A warns about: a save-key rename that misses one of these 5 locations produces either a silently-stale test (an .mjs script seeding under the wrong key, so its save-seed no-ops and every level appears locked) or a misleadingly-worded FAIL (check-progress.sh's grep looking for a literal that intentionally no longer exists). Update ALL 5 together in this one task:
- scripts/check-progress.sh: change the assertion's grep pattern from `'mathlab_platformer_v2'` to `'noxrun_platformer_v1'`, and update its fail message from "missing NEW versioned save key 'mathlab_platformer_v2' in src/config.js (Phase 13 clean reset)" to reference the new key and the Phase 26 rebrand instead of Phase 13.
- scripts/browser-boot.mjs, scripts/audit-phase21-mechanics.mjs, scripts/screenshot-phase20.mjs, scripts/screenshot-phase26.mjs: change each file's `const SAVE_KEY = "mathlab_platformer_v2";` to `const SAVE_KEY = "noxrun_platformer_v1";`.
  </action>
  <acceptance_criteria>
    - All 5 files reference "noxrun_platformer_v1" and zero files reference "mathlab_platformer_v2" as a live literal.
  </acceptance_criteria>
  <verify>
    <automated>! grep -rn "mathlab_platformer_v2" scripts/ src/config.js && grep -l "noxrun_platformer_v1" scripts/check-progress.sh scripts/browser-boot.mjs scripts/audit-phase21-mechanics.mjs scripts/screenshot-phase20.mjs scripts/screenshot-phase26.mjs | wc -l | grep -qx 5 && bash scripts/check-progress.sh</automated>
  </verify>
  <done>All 5 scripts agree on the new save-key literal; zero references to the old key remain anywhere in scripts/ or src/config.js; check-progress.sh's own gate passes green under the new key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage key rename | Browser-only, origin-scoped persistence; renaming intentionally orphans any existing `mathlab_platformer_v2` blob in a real player's browser (explicitly confirmed acceptable by the user in 26-CONTEXT.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-11 | Repudiation / Tampering | scripts/check-progress.sh save-key assertion going stale | high | mitigate | Task 3 updates the grep assertion in the SAME commit as config.js's key rename (Task 1) and all 4 Playwright scripts' SAVE_KEY consts — exactly the fix 26-RESEARCH.md's Pitfall A prescribes; the plan's own <verify> re-runs check-progress.sh to prove the gate is green under the new literal, not just edited |
| T-26-12 | Tampering | docker/nginx (not touched) | low | accept | This plan never touches docker/Dockerfile or nginx.conf functional directives — save-key rename is entirely client-side/localStorage, no server-side surface |

</threat_model>

<verification>
`bash scripts/check-progress.sh` passes green end-to-end (proves config.js's new key + all 4 script SAVE_KEY consts + the shell assertion all agree). `grep -rni "mathlab" src/index.html src/config.js src/main.js src/scenes/title.js` returns only the 2 allowlisted src/progress.js school-game lines (not in this file list, so zero hits expected here) — confirms the core sweep is clean. `bash scripts/check-safety.sh` and `node scripts/validate-levels.mjs` both still pass (no behavior regression).
</verification>

<success_criteria>
The core src/ runtime carries zero "Math Lab" branding outside the allowlisted school-game comments; the save key is renamed to a Nox Run-branded literal, consistently applied across config.js and all 5 scripts that reference it, with the full check-progress.sh gate proving the consistency automatically rather than by inspection.
</success_criteria>

## Artifacts This Phase Produces

- `src/config.js`: `CONFIG.SAVE.KEY` value changed to the new branded save-key literal.
- `src/progress.js`: 6 `console.warn` tags rebranded.
- `scripts/check-progress.sh`: save-key grep assertion + fail message updated to the new literal.
- `scripts/browser-boot.mjs`, `scripts/audit-phase21-mechanics.mjs`, `scripts/screenshot-phase20.mjs`, `scripts/screenshot-phase26.mjs`: `SAVE_KEY` const updated to the new literal.

<output>
Create `.planning/phases/26-grunge-palette-nox-run-rebrand/26-09-SUMMARY.md` when done
</output>