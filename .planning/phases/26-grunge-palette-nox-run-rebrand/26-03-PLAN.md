---
phase: 26
plan: 03
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - scripts/build-art-assets.py
  - assets/parallax/far-theme-1.png
  - assets/parallax/mid-theme-1.png
  - assets/parallax/near-theme-1.png
  - assets/tiles/ground-theme-1.png
  - "(same 4-file pattern x8 themes — 32 baked PNGs total under assets/parallax/ and assets/tiles/)"
autonomous: true
requirements: [VIS-03]

must_haves:
  truths:
    - "8 distinct per-level theme sub-palettes exist, each derived from CONFIG.PALETTE's new hues (ACCENT_MOSS/ACCENT_SLATE/ACCENT_RUST) plus the existing neutrals, following a documented calmer-to-harsher progression (green early, blue-grey mid, rust late)."
    - "Every theme's parallax far/mid/near layers and ground tileset are baked as real PNGs via the SAME _remap_luminance pipeline already used for the untouched base layers — not a new tinting mechanism."
  artifacts:
    - "assets/parallax/{far,mid,near}-theme-{1..8}.png (24 files)"
    - "assets/tiles/ground-theme-{1..8}.png (8 files)"
    - "scripts/build-art-assets.py: THEME_PALETTES dict (8 entries) + theme-parameterized build_ground_theme()/build_parallax_theme() functions"
  key_links:
    - "scripts/build-art-assets.py's THEME_PALETTES reuses CONFIG.PALETTE's ACCENT_MOSS/ACCENT_SLATE/ACCENT_RUST hex values (hand-mirrored — Python has no import path into the JS CONFIG object; keep the two in sync by convention, same as the existing ENVIRONMENT_PALETTE mirror)"
---

<objective>
Extend the existing Pillow art pipeline (`scripts/build-art-assets.py`) to bake 8 distinct per-level theme variants of the parallax background and ground tileset, per VIS-03. This is pure build-time asset generation — no game code changes yet (that's 26-05).

Purpose: per-level theming requires NEW baked sprite variants, not a code-only change (26-RESEARCH.md Pattern 2 — runtime shader tinting is explicitly rejected). This plan produces the PNGs; 26-05 wires the game to select them per level.
Output: 32 new baked PNGs (8 themes x 4 assets each); `THEME_PALETTES` + 2 new bake functions in build-art-assets.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-RESEARCH.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add THEME_PALETTES (8 entries) and theme-parameterized bake functions</name>
  <files>scripts/build-art-assets.py</files>
  <read_first>scripts/build-art-assets.py full file (already read this session — ENVIRONMENT_PALETTE lines 65-73, ENVIRONMENT_PALETTE_FAR/MID/NEAR/TITLE lines 234-249, `_remap_luminance` lines 116-151, `build_ground()` lines 196-221, `build_parallax()` lines 261-301)</read_first>
  <action>
Add a `PALETTE_HEX` mirror-comment block near the top noting these values MUST stay hand-synced with src/config.js's CONFIG.PALETTE (same convention as the existing ENVIRONMENT_PALETTE mirror — there is no cross-language import). Define 3 new Python RGB tuples matching CONFIG.PALETTE exactly: `ACCENT_MOSS = (0x2a, 0x3d, 0x2a)`, `ACCENT_SLATE = (0x2c, 0x38, 0x44)`, `ACCENT_RUST = (0x5a, 0x33, 0x22)`.

Define `THEME_PALETTES`, a dict of 8 entries keyed `"theme-1"` through `"theme-8"`, each value itself a small dict with `far`, `mid`, `near`, `ground` sub-palette lists. Build each sub-palette by taking the EXISTING `ENVIRONMENT_PALETTE_FAR`/`_MID`/`_NEAR` lists (for far/mid/near) and `ENVIRONMENT_PALETTE` (for ground) and substituting ONE accent hue into index [4] of each list (the existing "mid grey / clearly-visible material highlight" slot) per this exact level-order-driven mapping (calmer green early, harsher rust late, per 26-RESEARCH.md's Open Question 3 recommendation):
theme-1 (level-01), theme-2 (level-02): substitute ACCENT_MOSS.
theme-3 (level-03), theme-4 (level-04): substitute ACCENT_SLATE.
theme-5 (level-05): substitute ACCENT_SLATE into far/ground, ACCENT_RUST into index [5] of mid/near (transitional blend).
theme-6 (level-06): substitute ACCENT_RUST into index [4], keep ACCENT_SLATE in index [5] (rust-leaning transitional).
theme-7 (level-07), theme-8 (level-08): substitute ACCENT_RUST.
Record this exact theme-to-level mapping as a comment above THEME_PALETTES so 26-06 (which sets each level descriptor's `theme` field) and 26-CONTRAST.md's reader can cross-reference it without re-deriving it.

Add `build_ground_theme(theme_id, palette)` — copies build_ground()'s existing body exactly (same source tiles, same `_remap_luminance` call) but takes the sub-palette as a parameter and saves to `assets/tiles/ground-{theme_id}.png` instead of the fixed `ground.png` path. Add `build_parallax_theme(theme_id, palette)` similarly, copying build_parallax()'s existing body (same source silhouette elements, same compositing) but parameterized to save `assets/parallax/far-{theme_id}.png`, `mid-{theme_id}.png`, `near-{theme_id}.png`. Do NOT touch the existing `build_ground()`/`build_parallax()` functions or their un-suffixed output paths — the base `ground.png`/`far.png`/`mid.png`/`near.png` files remain untouched fallback assets (used by any code path that has no theme set). Add a loop in `if __name__ == "__main__":` calling `build_ground_theme`/`build_parallax_theme` for all 8 entries in THEME_PALETTES, after the existing build_player()/build_ground()/build_parallax()/build_title_bg() calls.
  </action>
  <acceptance_criteria>
    - THEME_PALETTES has exactly 8 keys "theme-1".."theme-8", each with far/mid/near/ground sub-palette lists.
    - build_ground_theme/build_parallax_theme exist and are called for all 8 themes in `__main__`.
    - Original build_ground()/build_parallax() functions are byte-unchanged (grep confirms their bodies still write to the un-suffixed `assets/tiles/ground.png`/`assets/parallax/{far,mid,near}.png` paths).
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/build-art-assets.py').read())" && grep -c "THEME_PALETTES" scripts/build-art-assets.py</automated>
  </verify>
  <done>build-art-assets.py parses; THEME_PALETTES + both new functions exist; original build_ground/build_parallax untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Run the pipeline and verify all 32 theme PNGs exist with correct dimensions</name>
  <files>assets/parallax/, assets/tiles/</files>
  <read_first>build_ground()'s dimension assert (80x16) and build_parallax()'s dimension asserts (far 640x120, mid 640x144, near 640x90) — the theme variants must match these exact dimensions since main.js/parallax.js/build.js consume them by fixed sprite size assumptions</read_first>
  <action>
Run `python3 scripts/build-art-assets.py` from the repo root. Confirm it prints a "generated ..." line for every one of the 32 new theme files (8 x 4) in addition to the 4 pre-existing base outputs. If Pillow is not installed or the run fails, do not substitute placeholder PNGs — halt and report the exact error (VIS-03 explicitly requires REAL themed art, not stand-ins).
  </action>
  <acceptance_criteria>
    - `assets/parallax/far-theme-N.png`, `mid-theme-N.png`, `near-theme-N.png` exist for N=1..8, each matching its layer's fixed dimensions (640x120 / 640x144 / 640x90).
    - `assets/tiles/ground-theme-N.png` exists for N=1..8, each 80x16.
    - The pre-existing `assets/tiles/ground.png` and `assets/parallax/{far,mid,near}.png` are byte-unchanged (git diff shows zero changes to those 4 files).
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/build-art-assets.py && for n in 1 2 3 4 5 6 7 8; do test -f "assets/parallax/far-theme-$n.png" && test -f "assets/parallax/mid-theme-$n.png" && test -f "assets/parallax/near-theme-$n.png" && test -f "assets/tiles/ground-theme-$n.png" || exit 1; done && echo "all 32 theme assets present"</automated>
  </verify>
  <done>All 32 theme PNGs exist with correct dimensions; the 4 pre-existing base assets are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline input | scripts/build-art-assets.py reads ALREADY-vendored, already-license-proofed source PNGs (Kenney background-elements, Kenney pixel-platformer) — no new external download in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-03 | Tampering | scripts/build-art-assets.py | low | accept | Reuses already-vetted, already-vendored source images and the existing proven `_remap_luminance` function; no new source download, no new license surface in this plan |

</threat_model>

<verification>
`python3 scripts/build-art-assets.py` runs clean and produces exactly 32 new files plus the 4 unchanged base outputs. `git status` shows no modification to the 4 pre-existing base PNGs, only new files.
</verification>

<success_criteria>
8 distinct, real, baked per-level theme variants (parallax x3 + ground) exist on disk, derived from CONFIG.PALETTE's new hues via the project's proven remap pipeline, following a documented calm-to-harsh progression.
</success_criteria>

## Artifacts This Phase Produces

- `scripts/build-art-assets.py`: `ACCENT_MOSS`/`ACCENT_SLATE`/`ACCENT_RUST` Python constants, `THEME_PALETTES` dict (8 entries), `build_ground_theme(theme_id, palette)`, `build_parallax_theme(theme_id, palette)` functions.
- `assets/parallax/far-theme-{1..8}.png`, `assets/parallax/mid-theme-{1..8}.png`, `assets/parallax/near-theme-{1..8}.png` (24 files).
- `assets/tiles/ground-theme-{1..8}.png` (8 files).

<output>
Create `.planning/phases/26-grunge-palette-nox-run-rebrand/26-03-SUMMARY.md` when done
</output>
