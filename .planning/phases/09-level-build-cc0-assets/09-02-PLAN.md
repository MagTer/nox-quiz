---
phase: 09-level-build-cc0-assets
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/config.js
  - src/level.js
  - src/main.js
  - src/player.js
  - src/scenes/game.js
autonomous: true
requirements: [LEVEL-01, LEVEL-02, LEVEL-03]
must_haves:
  truths:
    - "The CC0 sprites load and render dark/grunge against #0a0a0a, no pink, readable silhouettes (LEVEL-02)"
    - "One hand-authored ~3-4 screen level renders with platforms, gaps, and solid ground (LEVEL-01)"
    - "Collision is reliable on the real level — no seam-stick on flat runs, no tunneling on the tallest drop (LEVEL-03)"
    - "The camera clamps to the new wider level bounds with no void at the edges"
  artifacts:
    - path: "src/level.js"
      provides: "Hand-authored LEVEL data + buildLevel() that emits merged-floor colliders + visual tiles + platforms"
      exports: ["LEVEL", "buildLevel"]
      contains: "export const LEVEL"
    - path: "src/config.js"
      provides: "Updated LEVEL_* bounds for the new level + tile/coin/spike/goal constants"
      contains: "TILE_SIZE"
    - path: "src/main.js"
      provides: "loadSprite calls for ground/spike/goal/player/coin before go('game')"
      contains: "loadSprite"
    - path: "src/player.js"
      provides: "Player factory with sprite('player') visual (movement unchanged)"
      contains: "sprite(\"player\")"
    - path: "src/scenes/game.js"
      provides: "Scene calls buildLevel(LEVEL); test-strip geometry replaced; respawn/camera/checkpoints kept"
      contains: "buildLevel"
  key_links:
    - from: "src/main.js"
      to: "assets/coin.png"
      via: "loadSprite('coin', '../assets/coin.png', { sliceX }) — web-root ../assets path (Pitfall 1)"
      pattern: "loadSprite\\(\"coin\""
    - from: "src/scenes/game.js"
      to: "src/level.js"
      via: "import { LEVEL, buildLevel } and call buildLevel(LEVEL)"
      pattern: "buildLevel\\(LEVEL\\)"
    - from: "src/config.js"
      to: "src/camera.js"
      via: "followCamera reads CONFIG.LEVEL_RIGHT/BOTTOM (camera.js unchanged) — widened bounds"
      pattern: "LEVEL_RIGHT"
---

<objective>
Replace the Phase 8 test-strip geometry with ONE hand-authored, sprite-rendered
~3-4 screen level: load the vendored CC0 sprites, author the level data + builder,
widen the camera bounds, and swap the player rect for `sprite("player")` — all WITHOUT
touching the Phase 8 movement, camera, respawn, or checkpoint machinery (covers
LEVEL-01, LEVEL-02, LEVEL-03). Coin/spike/goal COLLISION wiring is Plan 03.

Purpose: deliver the polished, traversable, dark/grunge level body with reliable
collision, reusing Phase 8's verified merged-floor + anti-tunnel levers.

Output: `src/level.js` (NEW), updated `src/config.js`, `src/main.js`, `src/player.js`,
and the geometry portion of `src/scenes/game.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-level-build-cc0-assets/09-RESEARCH.md
@.planning/phases/09-level-build-cc0-assets/09-PATTERNS.md
@src/scenes/game.js
@src/player.js
@src/config.js
@src/main.js
@src/camera.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load CC0 sprites + add level/content CONFIG constants + widen bounds</name>
  <files>src/main.js, src/config.js</files>
  <read_first>
    - src/main.js (lines 11-28) — current boot shape: kaplay() init, scene("game", gameScene), go("game", {...}). New loadSprite calls go between init and scene().
    - src/config.js (lines 12-32) — existing `KEY: value, // unit — explanation` convention; LEVEL_LEFT/RIGHT/TOP/BOTTOM at lines 27-30 (the 1600 test-strip width to replace).
    - .planning/phases/09-level-build-cc0-assets/09-RESEARCH.md — Pattern 1 (loadSprite/sliceX/anims; loadSpriteSheet is ABSENT in 3001) + "Boot-time asset loading" Code Example + Pattern 6 (camera bounds, ~3-4 screens ≈ 1920-2560px on a 640px canvas).
    - .planning/phases/09-level-build-cc0-assets/09-PATTERNS.md — `src/main.js` MODIFY section (../assets path rule, Pitfall 1) + `src/config.js` MODIFY section (constants to ADD + bounds to UPDATE).
    - assets/ — confirm Plan 01 vendored ground.png/spike.png/goal.png/player.png/coin.png.
  </read_first>
  <action>
    In `src/main.js`: between the `kaplay({...})` init and `scene("game", gameScene)`, add
    `loadSprite` calls for `ground`, `spike`, `goal`, `player` (single sprites) and `coin`
    (gridded spritesheet with `sliceX` = the coin frame count and a named `anims.spin`
    `{ from, to, loop, speed: CONFIG.COIN_SPIN_SPEED }`). ALL paths use the `../assets/...`
    web-root convention exactly like the existing `import ... from "../lib/kaplay.mjs"` —
    a non-`../assets` path is a silent 404 (Pitfall 1). Use `loadSprite` with `sliceX`, NOT
    the absent old-Kaboom `loadSpriteSheet`. Kaplay queues the loads and shows its loading
    screen until they resolve, so the scene never draws against missing sprites. Import
    CONFIG into main.js if referencing COIN_SPIN_SPEED there (otherwise inline the frame
    count consistently with config).

    In `src/config.js`: UPDATE `LEVEL_RIGHT` (and `LEVEL_BOTTOM` if the level is taller than
    one 360px screen) to the authored level's true pixel extent (~1920-2560px wide per A3).
    ADD a new `// --- Level / content (Phase 9) ---` section following the existing
    `KEY: value, // unit — explanation` convention with at least: `TILE_SIZE` (px, CC0 pack
    native tile size), `COIN_SPIN_SPEED` (fps, coin anim frame rate), and any tile/coin/spike/
    goal placement constants the level data will reference. No magic numbers leak into logic
    modules (CONTEXT-locked). Do NOT alter the movement/physics/camera constants.
  </action>
  <verify>
    <automated>node --check src/main.js &amp;&amp; node --check src/config.js &amp;&amp; grep -q 'loadSprite("coin"' src/main.js &amp;&amp; grep -q '\.\./assets/' src/main.js &amp;&amp; grep -q 'TILE_SIZE' src/config.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` passes for both files.
    - `src/main.js` registers `loadSprite` for ground, spike, goal, player, and coin (coin with `sliceX` + a `spin` anim) before `go("game", ...)`.
    - Every asset path in main.js begins with `../assets/`.
    - `src/config.js` defines `TILE_SIZE` and `COIN_SPIN_SPEED` and has `LEVEL_RIGHT` widened beyond 1600.
    - `loadSpriteSheet` does NOT appear in main.js.
  </acceptance_criteria>
  <done>The five CC0 sprites are registered with correct ../assets paths before the scene boots, and CONFIG carries the new level bounds + content constants.</done>
</task>

<task type="auto">
  <name>Task 2: Author src/level.js — level data + buildLevel() with merged-floor colliders</name>
  <files>src/level.js</files>
  <read_first>
    - src/config.js (banner lines 1-12) — copy this module-banner + single exported `const` shape for level.js.
    - src/scenes/game.js (lines 32-43) — the merged-floor + platform `add([...])` idiom buildLevel reproduces (ONE wide `body({isStatic:true})` collider per floor run; raised platforms same component list).
    - .planning/phases/09-level-build-cc0-assets/09-PATTERNS.md — `src/level.js (NEW)` section (leaf-data module + merged-collider CRITICAL note: render tiles as visual-only sprites with NO area()/body(); merge each contiguous floor run into one wide collider — do NOT give each tile its own body).
    - .planning/phases/09-level-build-cc0-assets/09-RESEARCH.md — Pattern 2 Option B (merged floor collider + tiled visuals + sparse coins/spikes/goal data list) + Pitfall 2 (seam-stick regression) + Pitfall 3 (tunneling — keep thick colliders).
  </read_first>
  <action>
    Create `src/level.js`. Open with a `// src/level.js — <one-line role>.` banner + a short
    paragraph naming its single responsibility (hand-authored level data + a builder that
    instantiates the level body), per the shared module-banner pattern. Import is light —
    at most `import { CONFIG } from "./config.js"` (it lives in `src/`, so siblings are `./`,
    NOT `../`).

    Export `const LEVEL` as author-written data describing a medium linear ~3-4 screen level
    with a gentle difficulty curve: contiguous floor runs (each as start-x/width/y so the
    builder can merge them), raised platforms, gaps, plus `coins: [...]`, `spikes: [...]`,
    `goal: {x,y}`, and checkpoint positions (or leave checkpoints to the scene). Coins ~8-12
    placed throughout (count is exercised in Plan 03). Place a generous checkpoint just
    before each spike location (ADHD-safe — a respawn never costs meaningful progress).

    Export `buildLevel(LEVEL)` that loops the data and emits Kaplay `add([...])` calls
    (engine globals — do NOT import add/sprite/rect/pos/area/body): for EACH contiguous floor
    run, ONE merged wide `add([rect(width, h), pos(x, y), area(), body({ isStatic: true }),
    "ground"])` collider (preserves the Phase 8 anti-seam-stick property — Pitfall 2), with
    the floor tiles rendered as SEPARATE visual-only `add([sprite("ground"), pos(...)])`
    entities carrying NO `area()`/`body()`. Raised platforms follow the same merged-collider
    idiom. Make platform/floor colliders thick enough to resist tunneling on tall drops
    (Pitfall 3). buildLevel does NOT place coins/spikes/goal collision handlers — it only
    builds the static body + visuals (and MAY add the coin/spike/goal entities themselves,
    but their `onCollide` wiring is Plan 03). Keep the authored extent consistent with the
    widened `CONFIG.LEVEL_RIGHT`/`LEVEL_BOTTOM` from Task 1.
  </action>
  <verify>
    <automated>node --check src/level.js &amp;&amp; grep -q 'export const LEVEL' src/level.js &amp;&amp; grep -q 'export function buildLevel' src/level.js &amp;&amp; grep -q 'isStatic: true' src/level.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/level.js` passes.
    - `src/level.js` exports `LEVEL` (with floor runs, platforms, coins, spikes, goal) and `buildLevel`.
    - `buildLevel` emits at least one merged wide `body({ isStatic: true })` floor collider, with floor tile visuals added separately (no per-tile body).
    - level.js imports only `./config.js` (or nothing) — no engine-global imports, no `../` sibling imports.
  </acceptance_criteria>
  <done>level.js holds the authored ~3-4 screen level data and a builder that reproduces the merged-floor collider (anti seam-stick) with separate visual tiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Swap player sprite + wire buildLevel into the scene (geometry only)</name>
  <files>src/player.js, src/scenes/game.js</files>
  <behavior>
    - The player renders as `sprite("player")` (no colored rect) and still runs/jumps exactly as Phase 8 (coyote/buffer/variable-height untouched).
    - Loading the scene over HTTP renders the authored level (floor, platforms, gaps) from buildLevel(LEVEL), not the old test strip.
    - A full-speed flat run does not snag on floor seams; the tallest authored drop does not tunnel through the floor.
    - The camera follows and clamps to the new wider bounds with no void at the level edges.
  </behavior>
  <read_first>
    - src/player.js (lines 24-32) — the exact rect+color block to swap: replace `rect(24,32)` with `sprite("player")`, REMOVE `color(0,255,136)`, KEEP `area()`, KEEP `body({ maxVelocity: CONFIG.MAX_FALL_SPEED })` (anti-tunnel cap — do NOT drop), KEEP `opacity(1)` (respawn flash). Everything below line 33 (coyote/buffer/jump) is UNTOUCHED.
    - src/scenes/game.js (lines 17-19 imports; 32-43 test-strip geometry to REPLACE; 21-31 closure state, 50-65 checkpoints, 67-81 reset()/respawn(), 83-92 onUpdate camera + fall-off — all KEEP).
    - .planning/phases/09-level-build-cc0-assets/09-PATTERNS.md — `src/player.js` MODIFY (one-line swap) + `src/scenes/game.js` MODIFY (imports add level.js; replace geometry; KEEP respawn/camera/checkpoints) + "Reposition-in-place respawn" shared pattern.
    - .planning/phases/09-level-build-cc0-assets/09-RESEARCH.md — "Sprite swap in makePlayer" + "Scene wiring" Code Examples; Pitfall 2/3 (re-run Phase 8 flat-run + tall-drop checks on the real level).
  </read_first>
  <action>
    In `src/player.js`: replace ONLY the visual — `rect(24, 32)` becomes `sprite("player")`
    and remove the `color(0, 255, 136)` placeholder line. KEEP `area()`,
    `body({ maxVelocity: CONFIG.MAX_FALL_SPEED })`, `opacity(1)`, and the `"player"` tag. If
    the sprite has transparent padding, tune the collider with `area({ shape, offset })` so
    it matches the visible body. Do NOT touch any movement/jump logic below the component
    list.

    In `src/scenes/game.js`: ADD `import { LEVEL, buildLevel } from "../level.js";` beside
    the existing `../config.js`/`../player.js`/`../camera.js` imports (keep the `../`
    convention). REPLACE the test-strip geometry block (the `add([rect(...)...])` floor +
    ledge + gap-platform lines) with a single `buildLevel(LEVEL)` call placed before
    `makePlayer`. KEEP unchanged: the closure run-state (`startX`/`startY`/`lastCheckpoint`),
    the `addCheckpoint`/`onCollide("checkpoint", ...)` machinery, `reset()`/`respawn()`
    (reposition-in-place — never game-over), and the `onUpdate` camera-follow + fall-off-world
    respawn. Seed checkpoint positions from the authored level (place one just before each
    spike). Do NOT add coin/spike/goal `onCollide` wiring here — that is Plan 03; this task
    delivers the geometry + sprite render only.
  </action>
  <verify>
    <automated>node --check src/player.js &amp;&amp; node --check src/scenes/game.js &amp;&amp; grep -q 'sprite("player")' src/player.js &amp;&amp; ! grep -q 'color(0, 255, 136)' src/player.js &amp;&amp; grep -q 'buildLevel(LEVEL)' src/scenes/game.js &amp;&amp; grep -q 'maxVelocity' src/player.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` passes for both files.
    - `src/player.js` uses `sprite("player")`, no longer contains `color(0, 255, 136)`, and STILL contains `body({ maxVelocity: ... })` and `opacity(1)`.
    - `src/scenes/game.js` imports from `../level.js`, calls `buildLevel(LEVEL)`, and the old test-strip `rect(1600, 32)` floor line is gone.
    - `src/scenes/game.js` still contains `reset`/`respawn` and the `onUpdate` fall-off-world + `followCamera` logic (not regressed).
    - Manual: served over HTTP the level renders dark/grunge with readable sprites (no pink), a full-speed flat run does not seam-stick, the tallest drop does not tunnel, and the camera clamps with no edge void.
  </acceptance_criteria>
  <done>The player renders as a CC0 sprite with movement intact; the scene draws the authored level via buildLevel with reliable collision and clamped camera; Phase 8 respawn/checkpoint/camera machinery is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset files → canvas | Author-controlled `../assets/*` PNGs loaded by Kaplay (same-origin, no remote fetch) |
| keyboard → player state | Only key events + author-written level data drive state (no untrusted strings) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Denial of service (visual) | Sprite load (404 on wrong path) | mitigate | All `loadSprite` paths use the verified `../assets/...` web-root convention; verify 200s in the Network tab; author-controlled constant paths only |
| T-09-04 | Tampering | Level data | accept | Level data is author-written JS constants, not user input (RESEARCH Security Domain) |
| T-09-05 | Information disclosure | Console errors | mitigate | Keep console clean; catch failed sprite loads in dev before shipping |
</threat_model>

<verification>
- `node --check` passes on main.js, config.js, level.js, player.js, scenes/game.js.
- Served over HTTP: DevTools Network tab shows 200 for every `../assets/*` request; console clean.
- The authored level renders dark/grunge, readable, no pink (LEVEL-02).
- Full-speed flat run = no seam-stick; tallest drop = no tunneling (LEVEL-03, Phase 8 stress re-check).
- Camera clamps to the widened bounds with no void at the edges.
- The Phase 8 respawn/checkpoint/camera machinery is intact (not regressed).
</verification>

<success_criteria>
LEVEL-01/02/03 met: one hand-authored ~3-4 screen dark/grunge sprite-rendered level
renders and is traversable with reliable collision (no seam-stick, no tunneling) and a
clamped camera — built on Phase 8's preserved movement/respawn/camera spine. Coin/spike/
goal interactions remain for Plan 03.
</success_criteria>

<output>
Create `.planning/phases/09-level-build-cc0-assets/09-02-SUMMARY.md` when done
</output>
