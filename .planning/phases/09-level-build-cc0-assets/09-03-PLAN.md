---
phase: 09-level-build-cc0-assets
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/scenes/game.js
autonomous: true
requirements: [LEVEL-04, LEVEL-05, LEVEL-07]
must_haves:
  truths:
    - "The player can collect coins; each collected coin disappears and a scene-closure count increments (LEVEL-04)"
    - "Touching a spike triggers the existing gentle checkpoint respawn — never a game-over (LEVEL-05)"
    - "Reaching the goal fires a single onReachGoal() once, with a fire-once guard and a Phase-9 stub (LEVEL-07)"
  artifacts:
    - path: "src/scenes/game.js"
      provides: "coin/spike/goal onCollide wiring + scene-closure coinsCollected + onReachGoal seam"
      contains: "onReachGoal"
  key_links:
    - from: "src/scenes/game.js (onCollide('spike'))"
      to: "src/scenes/game.js (respawn())"
      via: "spike collision calls the existing Phase 8 respawn() — never game-over"
      pattern: "onCollide\\(\"spike\""
    - from: "src/scenes/game.js (onCollide('goal'))"
      to: "onReachGoal()"
      via: "single-point goal seam Phase 10's math gate attaches to"
      pattern: "onReachGoal"
---

<objective>
Wire the three interactables onto the level built in Plan 02 — coins, the spike hazard,
and the goal — using the repo's one collision idiom (`player.onCollide(tag, handler)`).
Coins increment a scene-closure count and destroy themselves (LEVEL-04); spikes route into
the existing Phase 8 `respawn()` (LEVEL-05, never game-over); the goal fires a single,
fire-once `onReachGoal()` stub that is the clean single-point seam Phase 10's math gate
attaches to (LEVEL-07).

Purpose: complete the level's interactive behavior while keeping the goal handoff a
single, unambiguous call site for the next phase.

Output: the collision-wiring additions to `src/scenes/game.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-level-build-cc0-assets/09-RESEARCH.md
@.planning/phases/09-level-build-cc0-assets/09-PATTERNS.md
@src/scenes/game.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire coin + spike collisions (count + gentle respawn)</name>
  <files>src/scenes/game.js</files>
  <behavior>
    - Touching a coin removes that coin and increments a scene-closure `coinsCollected` counter (no XP — count only).
    - Touching a spike repositions the player to the last checkpoint with momentum zeroed and a quick flash — no lives, no game-over screen.
    - `coinsCollected` is declared inside the scene closure, never at module level (no leak across go()/respawn).
  </behavior>
  <read_first>
    - src/scenes/game.js — closure run-state block (where `lastCheckpoint` is declared) for adding `coinsCollected`; the `player.onCollide("checkpoint", ...)` idiom to mirror; the `reset()`/`respawn()` seam that spikes reuse; the `buildLevel(LEVEL)` call (Plan 02) that has ALREADY created the tagged `"coin"`/`"spike"`/`"goal"` entities this task wires handlers onto.
    - src/level.js — confirm the coin/spike/goal `area()` entities are created by `buildLevel` (Plan 02 owns entity creation; the tightened spike `area({shape,offset})` is already set there).
    - .planning/phases/09-level-build-cc0-assets/09-RESEARCH.md — Pattern 3 (coin: onCollide + destroy + scene-closure count, NO XP) + Pattern 4 (spike: reuse respawn(), checkpoint before each spike) + Pitfall 4 (unfair spike hitbox — already mitigated by the tight collider in Plan 02).
    - .planning/phases/09-level-build-cc0-assets/09-PATTERNS.md — "Tagged-entity + onCollide collision idiom" + "Reposition-in-place respawn" + "Scene-closure run state" shared patterns; the "NEW collision wiring to ADD" block.
  </read_first>
  <action>
    The coin/spike/goal tagged `area()` entities ALREADY EXIST — `buildLevel(LEVEL)` (Plan 02,
    called in the scene before `makePlayer`) created them, including the tightened spike
    `area({ shape, offset })` hitbox. This task ONLY adds the collision handlers; it does NOT
    create entities and does NOT touch the spike collider shape.

    In `src/scenes/game.js`, add `let coinsCollected = 0;` inside the scene closure beside
    `lastCheckpoint` (NOT module-level — anti-leak). After `makePlayer` (the level entities
    from `buildLevel` already exist), ADD two collision handlers using the established
    `player.onCollide("<tag>", ...)` idiom:

    - `player.onCollide("coin", (c) => { coinsCollected += 1; destroy(c); });` — count only,
      remove the collected coin. NO XP (Phase 11) and NO juice/sfx (Phase 12) here.
    - `player.onCollide("spike", () => respawn());` — route into the EXISTING Phase 8
      `respawn()` seam (reposition-in-place, zero momentum, flash). Do NOT build any new
      death/respawn/lives system. Never a game-over.

    Confirm a generous checkpoint sits just before each spike (from the authored level / the
    scene's checkpoint placement) so a respawn never costs meaningful progress.
  </action>
  <verify>
    <automated>node --check src/scenes/game.js &amp;&amp; grep -q 'onCollide("coin"' src/scenes/game.js &amp;&amp; grep -q 'onCollide("spike"' src/scenes/game.js &amp;&amp; grep -q 'coinsCollected' src/scenes/game.js &amp;&amp; ! grep -qiE 'game ?over|lives' src/scenes/game.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/scenes/game.js` passes.
    - The `"coin"` and `"spike"` tagged `area()` entities exist (created by `buildLevel` in `src/level.js`, Plan 02) — the handlers wire onto real entities, not no-ops.
    - `coinsCollected` is declared inside `gameScene(...)` (closure), not at module level.
    - `player.onCollide("coin", ...)` increments the count and calls `destroy()` on the coin.
    - `player.onCollide("spike", ...)` calls the existing `respawn()` — no new death/lives/game-over construct is introduced.
    - Manual: collecting a coin removes it and increments the count; touching a spike respawns at the last checkpoint with no game-over.
  </acceptance_criteria>
  <done>Coins are collectible (count tracked in the closure) and the spike hazard routes into the gentle Phase 8 respawn — never game-over. LEVEL-04 + LEVEL-05 satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the goal — single-point onReachGoal seam with fire-once stub</name>
  <files>src/scenes/game.js</files>
  <behavior>
    - Reaching the goal fires `onReachGoal()` exactly once even though onCollide fires every overlap frame (a `goalReached` guard prevents re-entry).
    - The Phase 9 stub pauses the player and shows a placeholder message; it does NOT implement any math-gate logic.
    - There is exactly ONE `onReachGoal` function and ONE `onCollide("goal", ...)` wiring — the single seam Phase 10 attaches to.
  </behavior>
  <read_first>
    - src/scenes/game.js — the closure run-state block (add `goalReached` beside `coinsCollected`/`lastCheckpoint`); the established `player.onCollide("<tag>", ...)` idiom; the `buildLevel(LEVEL)` call that has ALREADY created the `"goal"` tagged entity this handler attaches to.
    - src/level.js — confirm the `"goal"` `area()` entity is created by `buildLevel` (Plan 02 owns entity creation).
    - .planning/phases/09-level-build-cc0-assets/09-RESEARCH.md — Pattern 5 (goal → single onReachGoal seam, fire-once guard, Phase 9 stub vs Phase 10 real gate) + Pitfall 5 (goal fires repeatedly / multiple handoff sites) + "Scene wiring" Code Example.
    - .planning/phases/09-level-build-cc0-assets/09-PATTERNS.md — "Single-point discipline" note (exactly ONE onReachGoal + ONE onCollide("goal", ...)); Phase 10 replaces the stub body HERE, nowhere else.
    - .planning/phases/09-level-build-cc0-assets/09-CONTEXT.md — Coins/Hazard & Goal decision ("keep the seam clean and single-point").
  </read_first>
  <action>
    In `src/scenes/game.js`, add `let goalReached = false;` inside the scene closure. Define
    a SINGLE `onReachGoal()` function that: returns early if `goalReached` is already true,
    sets `goalReached = true`, then runs the Phase 9 STUB — stop the player (e.g.
    `player.paused = true` or zero velocity + disable input) and `add([...])` a placeholder
    Kaplay `text(...)` HUD message (use `fixed()` for screen-space, `anchor("center")`,
    `center()` for position) such as a "GOAL" placeholder. Wire exactly ONE
    `player.onCollide("goal", onReachGoal);`. This is the clean single-point seam: Phase 10
    replaces the stub BODY of `onReachGoal` (or has it invoke the Phase-10 math-gate module)
    at this one call site — do NOT add any other goal collision logic or second handoff path.
    Keep the placeholder message a canvas `text()` (NOT DOM innerHTML) for the no-XSS posture.
  </action>
  <verify>
    <automated>node --check src/scenes/game.js &amp;&amp; grep -q 'function onReachGoal' src/scenes/game.js &amp;&amp; grep -q 'goalReached' src/scenes/game.js &amp;&amp; [ "$(grep -c 'onCollide(.goal.' src/scenes/game.js)" -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/scenes/game.js` passes.
    - The `"goal"` tagged `area()` entity exists (created by `buildLevel` in `src/level.js`, Plan 02) — the handler wires onto a real entity, not a no-op.
    - `goalReached` is declared inside the scene closure (not module-level).
    - Exactly ONE `onReachGoal` function and exactly ONE `onCollide("goal", ...)` wiring exist.
    - The stub uses a Kaplay canvas `text(...)` placeholder (not DOM innerHTML) and contains no math-gate logic.
    - Manual: reaching the goal fires the stub once (pause + placeholder), with no repeated/stacked messages on continued overlap.
  </acceptance_criteria>
  <done>The goal fires a single fire-once onReachGoal() stub — the clean single-point seam Phase 10's math gate attaches to. LEVEL-07 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collision events → scene state | Engine-detected overlaps mutate author-controlled scene-closure state (coins, goal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | Scene run state (coinsCollected/goalReached) | mitigate | All run state in the scene closure (anti-leak); no module-level `let`; survives go()/respawn |
| T-09-07 | Elevation / cross-site scripting | Goal placeholder message | mitigate | Render via Kaplay canvas `text()`, not DOM innerHTML (RESEARCH Security Domain — no XSS sink) |
| T-09-08 | Denial of service (UX) | Goal fires every overlap frame | mitigate | `goalReached` fire-once guard + pause player on first contact (Pitfall 5) |
</threat_model>

<verification>
- `node --check src/scenes/game.js` passes.
- Served over HTTP: collecting each coin removes it + increments the closure count (LEVEL-04).
- Touching a spike respawns at the last checkpoint, momentum zeroed, NO game-over/lives (LEVEL-05).
- Reaching the goal fires `onReachGoal` exactly once (pause + placeholder), no repeat spam (LEVEL-07).
- Exactly one `onReachGoal` function + one `onCollide("goal", ...)` — the single Phase-10 seam.
- Console clean; no module-level run state introduced.
</verification>

<success_criteria>
LEVEL-04/05/07 met: coins are collectible (count in the closure), the spike hazard routes
into the gentle Phase 8 respawn (never game-over), and the goal fires a single fire-once
`onReachGoal()` stub — a clean, unambiguous single-point seam for the Phase 10 math gate.
</success_criteria>

<output>
Create `.planning/phases/09-level-build-cc0-assets/09-03-SUMMARY.md` when done
</output>