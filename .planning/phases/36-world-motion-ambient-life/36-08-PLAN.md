---
phase: 36-world-motion-ambient-life
plan: 08
type: execute
wave: 6
depends_on: ["36-06"]
files_modified:
  - src/levels/level-05.js
  - src/levels/level-07.js
  - src/levels/level-08.js
autonomous: true
requirements: [MOT-01, MOT-02, MOT-03, MECH-05]
must_haves:
  truths:
    - "level-05 (cemetery, calm odd), level-07 (castle, calm odd, monotonic staircase), level-08 (castle, intense even, switchback spire) each carry FULL motion — ≥1 moving platform + ≥1 patroller + ambient flicker + the alcove-linked torch — tuned to the calm/intense rhythm, following the trial's approved template"
    - "Every placed mover passes mover-reachability (both endpoints rightward-reachable, worst-case-extreme) and obeys §6b — checkpoint before every mover, WAIT-not-death, no killing pit under a mover; movers MAY be on the critical path but NEVER strand her"
    - "Static geometry of all three levels stays byte-frozen (motion is add-only via geometry.movers/geometry.patrollers + the alcove-link field); level-07 and level-08 stay mechanically distinct (staircase vs switchback — LVL-02)"
    - "The interactive audit rides every mover + crosses every patroller on all three; check-safety stays green"
  artifacts:
    - "src/levels/level-05.js, level-07.js, level-08.js — geometry.movers + geometry.patrollers + flicker/alcove-link fields, densities tuned per calm/intense"
  key_links:
    - "each level-0N geometry.movers/patrollers → build.js loops → mover-reachability + ride/cross audit; alcove torch → MECH-05 lightAmbient"
---

<objective>
Complete FULL motion across all 8 levels by rolling out to levels 05, 07, and 08 on the human-approved trial template — tuned to the calm/intense biome-pair rhythm (calm odd 05/07 lighter; intense even 08 heavier, movers allowed on the critical path). level-08 is the switchback-spire stress test (tightest climbs, real ceilings); level-07 stays the monotonic staircase (they may not converge — LVL-02). Every mover obeys the §6a/§6b no-softlock rules; every level gets ambient flicker + the MECH-05 alcove-linked torch.

Purpose: The second half of the remaining-6 rollout — with plan 07 this brings motion to all 8 levels. Runs only after the SC5 sign-off (36-06).

Output: geometry.movers + geometry.patrollers (+ flicker + alcove-link) on levels 05/07/08, all gates green including mover-reachability + the ride/cross audit + geometry-freeze.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-world-motion-ambient-life/36-CONTEXT.md
@.planning/phases/36-world-motion-ambient-life/36-05-SUMMARY.md
@.planning/phases/36-world-motion-ambient-life/36-06-SUMMARY.md
@docs/LEVEL-DESIGN.md
@src/levels/level-05.js
@src/levels/level-07.js
@src/levels/level-08.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author motion on level-08 (castle, intense even, switchback spire) — HEAVIER density</name>
  <files>src/levels/level-08.js</files>
  <read_first>
    - src/levels/level-08.js — the WHOLE descriptor: the switchback spire (summit at y≈-125), `bounds` (used AS-IS — §7; do not bump), checkpoints, enemies, the secretAlcove (x≈2650), and the light-source prop nearest it. level-08 has the tightest climbs + real ceilings — mover vertical travel must stay within one jump at the worst endpoint (Pitfall 7).
    - docs/LEVEL-DESIGN.md — §6a/§6b/§8.5 + §8 (L7/L8 must stay mechanically distinct — level-08 keeps the switchback).
    - .planning/phases/36-world-motion-ambient-life/36-06-SUMMARY.md — approved intense-level template + any rejection notes.
  </read_first>
  <action>
    Add `geometry.movers` + `geometry.patrollers` at INTENSE-even density (multiple, some on the switchback critical path), each authored to §6b no-softlock rules: both endpoints reachable rightward from spawn (mind the rightward-only validator limitation on a switchback — place endpoints so each is reachable walking rightward along the forward path), behind a checkpoint, WAIT-not-death recovery, no killing pit under a mover, far end telegraphed, vertical travel within one jump at the worst endpoint. Add ambient flicker to the castle light-source props (castle-candles / candle-stand). Link the MECH-05 torch nearest the alcove. Do NOT edit any static array or bounds — ADD-ONLY. Keep level-08's mover layout its own (switchback-spire signature), distinct from level-07.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs 2>&1 | grep -i "mover\|level-08\|HARD-FAIL"; node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && bash scripts/check-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - level-08 carries geometry.movers + geometry.patrollers (intense density); every mover-reachability row PASS/WARN (never HARD-FAIL); bounds unchanged.
    - check-geometry-frozen PASSES; check-safety passes; layout keeps the switchback-spire signature, distinct from level-07.
  </acceptance_criteria>
  <done>level-08 has intense, on-path, no-softlock motion + ambient + alcove torch on the switchback spire.</done>
</task>

<task type="auto">
  <name>Task 2: Author motion on level-05 (cemetery, calm odd) and level-07 (castle, calm odd, staircase) — LIGHT density</name>
  <files>src/levels/level-05.js, src/levels/level-07.js</files>
  <read_first>
    - src/levels/level-05.js + level-07.js — the WHOLE descriptors: geometry, bounds (used AS-IS), checkpoints, secretAlcove, and the light-source prop nearest each alcove. level-07 is the monotonic staircase (horizontal, bounds.top 0) — must stay mechanically distinct from level-08 (LVL-02).
    - docs/LEVEL-DESIGN.md — §6a/§6b/§8.5 (calm odd = FORGIVING: light density, generous margins, still ≥1 mover + ≥1 patroller) + §8 (L7 staircase).
    - .planning/phases/36-world-motion-ambient-life/36-06-SUMMARY.md — approved calm-level template.
  </read_first>
  <action>
    For EACH of level-05 and level-07, add `geometry.movers` + `geometry.patrollers` at LIGHT calm-odd density (≥1 each, forgiving margins), each authored to §6b no-softlock rules (both endpoints rightward-reachable, behind a checkpoint, WAIT-not-death, no killing pit, telegraphed). Add ambient flicker to each level's light-source props (cemetery for 05; castle-candles for 07). Link the MECH-05 torch nearest each secretAlcove. Do NOT edit any static array or bounds — ADD-ONLY. Keep level-05 distinct from level-06 (cemetery pair) and level-07 distinct from level-08 (castle pair) and from every other level's mover layout.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs 2>&1 | grep -i "mover\|level-05\|level-07\|HARD-FAIL"; node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && bash scripts/check-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - level-05 + level-07 each carry geometry.movers + geometry.patrollers (light density); every mover-reachability row PASS/WARN.
    - check-geometry-frozen PASSES; check-safety passes; layouts distinct from their biome-pair partners and all others.
  </acceptance_criteria>
  <done>The two calm-odd levels 05/07 carry forgiving, no-softlock motion + ambient + alcove torch — completing motion on all 8 levels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor → build/validate | Static Node data; no network/user input |

Static, offline browser game — authoring motion data adds no trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-17 | Denial of Service | an on-path mover strands the player (softlock) | high | mitigate | mover-reachability green + §6b authoring; ride-audit confirms |
| T-36-18 | Tampering | a static-array coordinate nudged while adding motion | high | mitigate | check-geometry-frozen PASSES (add-only via excluded keys) |
| T-36-19 | Tampering | level-07/08 converge (staircase vs switchback) | low | mitigate | Author distinctly; keep L07 staircase, L08 switchback (LVL-02) |
| T-36-SC | Tampering | package installs | n/a | accept | Zero new dependencies this phase |
</threat_model>

<verification>
```
bash scripts/check-safety.sh && node scripts/validate-levels.mjs \
  && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs \
  && node scripts/audit-phase21-mechanics.mjs
```
- validate-levels: mover-reachability PASS/WARN for every mover on 05/07/08.
- check-geometry-frozen: static arrays byte-frozen.
- audit: 05/07/08 movers ridden + patrollers crossed (read output).
- browser-boot: all three boot + reach goal with motion live.
</verification>

<success_criteria>
- levels 05/07/08 carry full motion tuned to calm/intense rhythm, no-softlock, geometry byte-frozen.
- Every mover reachability-green; audit rides/crosses all three; MECH-05 torch linked on each.
- All 8 levels now carry motion (with plan 07).
</success_criteria>

<output>
Create `.planning/phases/36-world-motion-ambient-life/36-08-SUMMARY.md` when done.
</output>