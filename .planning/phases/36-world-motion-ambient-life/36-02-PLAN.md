---
phase: 36-world-motion-ambient-life
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/mechanic-drive.mjs
  - scripts/lib/audit-retry.mjs
  - scripts/audit-phase21-mechanics.mjs
  - scripts/fixtures/motion-audit-fixture.mjs
  - scripts/audit-motion-fixture.mjs
autonomous: true
requirements: [MOT-01, MOT-02]
must_haves:
  truths:
    - "deriveEncounters(geometry) emits a `mover` encounter for every `geometry.movers` entry and a `patroller` encounter for every `geometry.patrollers` entry — so the interactive audit knows to drive to each"
    - "The REAL audit dispatch (auditLevelWithRetries in scripts/lib/audit-retry.mjs — the `tag === 'secret-alcove'` else-branch) gains `tag === 'mover'` → driveToMover and `tag === 'patroller'` → driveToPatroller branches, and the non-blocking-`break` guard exempts mover + patroller exactly as it already exempts secret-alcove (an unresolved mover/patroller must not silently abort the pass)"
    - "The audit RIDES every mover (proves the player was carried: player tracked the platform for N grounded frames) and CROSSES every patroller (proves contact fired the existing respawn seam: player pos snapped back to lastCheckpoint) — reported per-encounter like door/mathGate/enemy"
    - "The extension is proven RED-first: against a fixture level carrying an unridden/uncrossed mover+patroller the audit REPORTS the gap (before the drive/detect logic exists it cannot show ridden/crossed); after the extension it shows ridden:true / crossed:true, and the fixture runner EXITS NON-ZERO if either row is not resolved"
  artifacts:
    - "scripts/lib/mechanic-drive.mjs — deriveEncounters() + mover/patroller cases; a driveToMover() ride-detector and driveToPatroller() cross-detector following the driveAndDetectAlcove() precedent (both exported for audit-retry.mjs to import)"
    - "scripts/lib/audit-retry.mjs — auditLevelWithRetries dispatch: + mover/patroller branches; + mover/patroller added to the `!everTriggered` non-blocking-break exemption; + driveToMover/driveToPatroller imports"
    - "scripts/audit-phase21-mechanics.mjs — the stale 'every encounter is door/math-gate/enemy' comment corrected; the unchanged triggered/resolved pass gate now covers mover/patroller rows"
    - "scripts/fixtures/motion-audit-fixture.mjs — a minimal GREEN companion fixture (a reachable mover + a crossable patroller) for the RED-first proof"
    - "scripts/audit-motion-fixture.mjs — a fixture-audit runner that drives the fixture through auditLevelWithRetries and EXITS NON-ZERO unless the fixture's mover is ridden + patroller crossed (the assertive RED→GREEN verify)"
  key_links:
    - "geometry.movers/geometry.patrollers → deriveEncounters() (mechanic-drive.mjs) → auditLevelWithRetries dispatch (audit-retry.mjs) → driveToMover/driveToPatroller (mechanic-drive.mjs) → ride/cross signal → triggered/resolved row"
---

<objective>
Extend the interactive mechanic audit so it RIDES every moving platform and CROSSES every patroller — the in-engine proof the phase's verification standard demands ("checks that don't play the game lie"). Today `deriveEncounters()` (mechanic-drive.mjs, line 43) emits door/math-gate/enemy/secret-alcove encounters, and the REAL per-encounter dispatch lives in `scripts/lib/audit-retry.mjs`'s `auditLevelWithRetries` (the `tag === "secret-alcove"` else-branch, ~lines 142-167) — `audit-phase21-mechanics.mjs` only boots the browser and calls that wrapper (line 34/252). The audit is blind to movers and patrollers. Add both, following the Phase-30 alcove precedent (`driveAndDetectAlcove`, mechanic-drive.mjs line 744 — the model for a NEW encounter kind with its own drive-and-detect signal dispatched from audit-retry.mjs), and prove it RED-first against a fixture.

Purpose: The SC5 human sign-off and the consolidation gate both rely on this audit reporting that every mover was actually ridden and every patroller actually respawned her. Without the extension a placed mover ships with zero interactive proof — exactly the gap the standard forbids.

Output: `deriveEncounters` mover/patroller cases + `driveToMover`/`driveToPatroller` detectors (mechanic-drive.mjs); the mover/patroller dispatch branches + the non-blocking-break exemption in `auditLevelWithRetries` (audit-retry.mjs); a GREEN companion fixture and an assertive fixture-audit runner for the RED-first proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-world-motion-ambient-life/36-CONTEXT.md
@.planning/phases/36-world-motion-ambient-life/36-RESEARCH.md
@scripts/lib/mechanic-drive.mjs
@scripts/audit-phase21-mechanics.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: deriveEncounters mover + patroller cases + a GREEN companion fixture</name>
  <files>scripts/lib/mechanic-drive.mjs, scripts/fixtures/motion-audit-fixture.mjs</files>
  <read_first>
    - scripts/lib/mechanic-drive.mjs — `deriveEncounters(geometry)` (line 43-55: the door/math-gate/enemy/secret-alcove maps) and the Phase-30 `driveAndDetectAlcove()` block (line 744 + its header note ~lines 725-743 + the beforeCount/afterCount entity-count detection idiom) — this is the precedent for a NEW encounter kind with its own drive+detect signal. Note: `driveAndDetectAlcove` is a standalone export SELECTED by tag in audit-retry.mjs, NOT dispatched here.
    - scripts/lib/reachability.mjs lines 1455-1504 — the existing Case K/L/M mover fixtures (`movers: [{x1,y1,x2,y2}]`) — mirror that shape for `patrollers` (`{x1,y1,x2,y2,speed?}`).
    - .planning/phases/36-world-motion-ambient-life/36-RESEARCH.md — Open Question 2 (RESOLVED): ride signal = player tracked the platform for N grounded frames; cross signal = respawn fired / pos snapped to lastCheckpoint; dispatch site is audit-retry.mjs. Also §Recommended Project Structure.
  </read_first>
  <action>
    In `deriveEncounters(geometry)` (mechanic-drive.mjs line 43) add two guarded maps alongside the existing ones: `...(geometry.movers ?? []).map((m, i) => ({ x: m.x1, y: m.y1, tag: "mover", idx: i }))` and `...(geometry.patrollers ?? []).map((p, i) => ({ x: p.x1, y: p.y1, tag: "patroller", idx: i }))`. Use the platform/patroller START endpoint (x1,y1) as the drive target so the audit approaches the entity along the forward path. Keep every existing encounter untouched (the ascending-x sort at line 53 already orders them).

    Add and EXPORT a `driveToMover(page, encounter)` detector following the `driveAndDetectAlcove()` shape: drive to the mover's x-span (reuse `driveToXPlanned`), then sample the player's grounded state + x/y over a short window and assert the player was carried — the ride signal is "the player's x (or y) tracked the platform entity's pos for N consecutive grounded frames" (query `get("mover")` positions in `page.evaluate`, compare to player pos frame-to-frame). Return `{ triggered, resolved }` where `triggered` = the player reached and stood on the mover, `resolved` = it carried them (tracked N frames). Add and EXPORT a `driveToPatroller(page, encounter)` detector: drive into the patroller's path and assert contact fired the EXISTING respawn seam — the cross signal is "player pos snapped back toward `lastCheckpoint` after contact" (detect the position reset the same way the spike respawn would: player x/y jumps backward to a checkpoint on collision). Return `{ triggered: reached the patroller, resolved: respawn fired }`. Both must be `export`ed so audit-retry.mjs can import them (Task 2). Follow the copy-not-extract convention already in this directory; do NOT extract a shared helper across the phase21/browser-boot scripts.

    Create `scripts/fixtures/motion-audit-fixture.mjs` — a minimal node-importable descriptor (mirror the reachability Case-K/L fixtures) with an `id` + a `geometry` carrying ONE reachable mover (both ping-pong endpoints reachable rightward from spawn) and ONE crossable patroller on flat ground behind a checkpoint. This is the GREEN companion the RED-first proof rides.
  </action>
  <verify>
    <automated>node -e "import('./scripts/lib/mechanic-drive.mjs').then(m=>{const enc=m.deriveEncounters({movers:[{x1:150,y1:320,x2:250,y2:170}],patrollers:[{x1:400,y1:320,x2:500,y2:320}]});const tags=enc.map(e=>e.tag);if(!tags.includes('mover')||!tags.includes('patroller')){console.error('MISSING mover/patroller encounter',tags);process.exit(1)}console.log('ENCOUNTERS',JSON.stringify(enc))})" && node -e "import('./scripts/fixtures/motion-audit-fixture.mjs').then(m=>console.log('FIXTURE-OK',Object.keys(m)))"</automated>
  </verify>
  <acceptance_criteria>
    - `deriveEncounters({movers:[...],patrollers:[...]})` emits one `mover` encounter per mover and one `patroller` encounter per patroller, each carrying `x`/`y`/`idx`; every pre-existing encounter kind is unchanged.
    - `driveToMover` and `driveToPatroller` exist and return `{triggered,resolved}` following the driveToAlcove precedent; ride signal = N grounded carried frames, cross signal = respawn/pos-snap fired.
    - `scripts/fixtures/motion-audit-fixture.mjs` is node-importable and carries a reachable mover + a crossable patroller.
    - No shared module extracted across the Playwright scripts (copy-not-extract).
  </acceptance_criteria>
  <done>deriveEncounters knows movers + patrollers, ride/cross detectors exist mirroring the alcove precedent, and a GREEN fixture is ready for the RED-first proof.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch mover/patroller in auditLevelWithRetries + assertive RED-first fixture runner</name>
  <files>scripts/lib/audit-retry.mjs, scripts/audit-phase21-mechanics.mjs, scripts/audit-motion-fixture.mjs</files>
  <read_first>
    - scripts/lib/audit-retry.mjs — the WHOLE `auditLevelWithRetries` (line 64+): the import line (37: `deriveEncounters, driveToXPlanned, resolveIfBoxed, driveAndDetectAlcove`), the per-encounter dispatch (lines 140-167: `if (encounter.tag === "secret-alcove") { driveAndDetectAlcove } else { driveToXPlanned + resolveIfBoxed }`, each OR-ed across attempts), and the non-blocking-break guard at line 175 (`if (!everTriggered && encounter.tag !== "secret-alcove") break;`). THIS is the real dispatch site — mover/patroller branches go HERE, not in mechanic-drive.mjs.
    - scripts/audit-phase21-mechanics.mjs — the caller (line 34 imports auditLevelWithRetries; line 252 drives each level through it) and the `allResolved = results.every(r => r.triggered===true && r.resolved===true)` pass gate at line 293. Note the stale comment at lines 289-292 ("every encounter is now door/math-gate/enemy") — correct it to include mover/patroller (and it already covers alcove).
  </read_first>
  <action>
    In `scripts/lib/audit-retry.mjs`: extend the import at line 37 to also pull `driveToMover, driveToPatroller` from `./mechanic-drive.mjs`. In `auditLevelWithRetries`'s per-encounter dispatch (the `if (encounter.tag === "secret-alcove") … else …` block, lines ~142-167), add two branches BETWEEN the alcove branch and the final `else`: `else if (encounter.tag === "mover")` calling `driveToMover(page, encounter)` and `else if (encounter.tag === "patroller")` calling `driveToPatroller(page, encounter)`, each returning `{ triggered, resolved }` and OR-ed across attempts EXACTLY like the alcove branch (`everTriggered = outcome.triggered || (previous?.triggered ?? false); resolved = outcome.resolved || (previous?.resolved ?? false);`). Then extend the non-blocking-break guard at line 175 so mover + patroller get the SAME exemption the alcove has: `if (!everTriggered && encounter.tag !== "secret-alcove" && encounter.tag !== "mover" && encounter.tag !== "patroller") break;` — an unresolved mover/patroller must not silently abort the pass and starve later encounters. Do NOT weaken the `everyEncounterDone` early-exit (line 192) or the caller's `allResolved` gate — both still require triggered+resolved for every row, so an un-ridden mover / un-crossed patroller still FAILS the audit.

    In `scripts/audit-phase21-mechanics.mjs`: correct only the stale comment (lines 289-292) to state that every encounter is now door/math-gate/enemy/secret-alcove/mover/patroller and each must be triggered AND resolved. Do NOT change the `allResolved` gate.

    Create `scripts/audit-motion-fixture.mjs` — an ASSERTIVE fixture-audit runner. Clone audit-phase21-mechanics.mjs's server/guard/browser boot (copy-not-extract), but instead of iterating LEVEL_ORDER, load `scripts/fixtures/motion-audit-fixture.mjs`, make the game build ITS geometry (inject the fixture into the level registry via `page.evaluate`, or serve it as a scratch level — whichever the boot pattern allows), and drive it through `auditLevelWithRetries`. After the run, ASSERT the fixture's `mover@…` and `patroller@…` rows are BOTH `triggered:true && resolved:true` and `process.exit(1)` otherwise (this runner is a real gate, NOT the always-exit-0 diagnostic that audit-phase21 is). This is the assertive verify below.

    RED-first proof: run `node scripts/audit-motion-fixture.mjs` BEFORE the Task-1 drivers + this dispatch exist — it must FAIL (the mover/patroller cannot be ridden/crossed); record the gap in the SUMMARY. Then AFTER wiring, it must PASS (exit 0). Because the fixture is not one of the 8 shipped levels, it is never added to LEVEL_ORDER.
  </action>
  <verify>
    <automated>node scripts/audit-motion-fixture.mjs && echo FIXTURE-RIDE-CROSS-GREEN && node scripts/audit-phase21-mechanics.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/audit-retry.mjs`'s `auditLevelWithRetries` dispatches `mover`→driveToMover and `patroller`→driveToPatroller (imported from mechanic-drive.mjs), OR-ed across attempts like the alcove branch.
    - The non-blocking-break guard (line 175) exempts mover + patroller as well as secret-alcove; the `everyEncounterDone` early-exit and the caller `allResolved` gate are UNCHANGED (still triggered+resolved for every row).
    - `node scripts/audit-motion-fixture.mjs` EXITS 0 with the wiring and EXITS NON-ZERO without it (RED→GREEN, recorded in the SUMMARY) — it genuinely asserts the fixture's mover is ridden + patroller crossed, and its exit code is NOT swallowed.
    - `node scripts/audit-phase21-mechanics.mjs` against the 8 shipped levels still passes on the existing door/math-gate/enemy/alcove encounters (no mover/patroller placed on real levels yet — those land in later plans); the stale comment is corrected.
  </acceptance_criteria>
  <done>The REAL audit dispatch (auditLevelWithRetries in audit-retry.mjs) rides movers and crosses patrollers through the same triggered/resolved gate as every other mechanic, with the alcove's non-blocking-break exemption extended to them, proven RED→GREEN by an assertive fixture runner whose exit code fails on a missed ride/cross.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor → audit driver | Static Node data driven through a headless browser; no network/user input |

Static, offline browser game — no trust boundary added by extending Node/Playwright audit scripts.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-03 | Tampering | audit reports ridden/crossed without real proof (false green) | high | mitigate | Ride signal = N grounded carried frames; cross signal = respawn/pos-snap fired; RED-first proof shows the gap before wiring |
| T-36-04 | Repudiation | pass gate weakened to absorb flaky mover rows | medium | mitigate | Gate keeps `triggered===true && resolved===true` for every row; not relaxed |
| T-36-SC | Tampering | package installs | n/a | accept | Zero new dependencies this phase |
</threat_model>

<verification>
```
node scripts/audit-motion-fixture.mjs && node scripts/audit-phase21-mechanics.mjs
```
- audit-motion-fixture: EXITS NON-ZERO unless the fixture's mover is ridden + patroller crossed (the assertive RED→GREEN gate — no swallowed exit code).
- audit-phase21: read the OUTPUT (it always exits 0) — existing door/math-gate/enemy/alcove encounters on the 8 shipped levels still triggered+resolved; no mover/patroller placed on real levels yet.
</verification>

<success_criteria>
- deriveEncounters emits mover + patroller encounters; auditLevelWithRetries (audit-retry.mjs) dispatches them to driveToMover/driveToPatroller and exempts them from the non-blocking break, through the unchanged triggered/resolved gate.
- RED-first proven against a GREEN companion fixture via the assertive `audit-motion-fixture.mjs` runner (fails on a missed ride/cross).
- The 8 shipped levels' existing audit still passes (no mover placed yet).
</success_criteria>

<output>
Create `.planning/phases/36-world-motion-ambient-life/36-02-SUMMARY.md` when done.
</output>
