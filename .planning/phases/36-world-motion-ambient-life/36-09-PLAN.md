---
phase: 36-world-motion-ambient-life
plan: 09
type: execute
wave: 7
depends_on: ["36-07", "36-08"]
files_modified:
  - .planning/phases/36-world-motion-ambient-life/36-VERIFICATION.md
autonomous: true
requirements: [MOT-01, MOT-02, MOT-03, MECH-05]
must_haves:
  truths:
    - "With motion LIVE across all 8 levels, the FULL CLAUDE.md gate suite is green — check-gate, check-safety, check-import-safety, check-progress, check-assets-manifest, check-terrain-atlas, validate-levels, browser-boot, and (if new art was added) check-pink-gate"
    - "The interactive audit RIDES every mover and CROSSES every patroller across all 8 levels (read the output, not just the exit code) — every mover/patroller encounter shows triggered+resolved"
    - "check-geometry-frozen proves every level's static geometry is STILL byte-identical to the post-34.6 baseline with all motion keys present — motion added zero static drift"
    - "check-safety confirms NO banned scheduler (setTimeout/setInterval/wait()/loop()/lifespan()) and no punishment construct anywhere in src/ with motion live"
  artifacts:
    - "36-VERIFICATION.md — the recorded full-suite consolidation results (per-gate PASS + the audit ride/cross evidence per level) closing MOT-01/02/03 + MECH-05"
  key_links:
    - "all 8 levels' motion → full gate suite + interactive audit → recorded green consolidation → phase requirements satisfied"
---

<objective>
Prove the phase closed clean: run the ENTIRE CLAUDE.md verification gate suite plus the interactive audit with motion live on all 8 levels, and record the results. This is the consolidation checkpoint — geometry byte-frozen with motion present, no scheduler/punishment introduced, and every mover ridden + every patroller crossed by the in-engine audit. No new features; this plan is the phase-gate proof.

Purpose: Motion is now on the critical path across all 8 levels; the phase must demonstrate — with the full suite, not a subset — that the static world stayed frozen, the ADHD-safe mandate held, and the interactive proof (not greps alone) shows the world actually rides and crosses. This closes MOT-01/02/03 + MECH-05.

Output: a recorded `36-VERIFICATION.md` capturing per-gate PASS + the audit's per-level ride/cross evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-world-motion-ambient-life/36-CONTEXT.md
@.claude/CLAUDE.md
@.planning/phases/36-world-motion-ambient-life/36-05-SUMMARY.md
@.planning/phases/36-world-motion-ambient-life/36-07-SUMMARY.md
@.planning/phases/36-world-motion-ambient-life/36-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full gate suite + interactive audit with motion live; record 36-VERIFICATION.md</name>
  <files>.planning/phases/36-world-motion-ambient-life/36-VERIFICATION.md</files>
  <read_first>
    - .claude/CLAUDE.md — the "Verification gates" list (check-gate, check-safety, check-import-safety, check-progress, check-assets-manifest, check-terrain-atlas, validate-levels, browser-boot, audit-phase21-mechanics) — run ALL of them.
    - scripts/audit-phase21-mechanics.mjs — the triggered/resolved pass gate; read the OUTPUT to confirm every mover/patroller encounter across all 8 levels shows triggered+resolved (not just the exit code).
    - .planning/phases/35-biome-re-dress-props/35-VERIFICATION.md — the recording FORMAT to mirror.
  </read_first>
  <action>
    Run the FULL CLAUDE.md gate suite with motion live on all 8 levels and capture each result:
    `bash scripts/check-gate.sh`, `bash scripts/check-safety.sh`, `bash scripts/check-import-safety.sh`, `bash scripts/check-progress.sh`, `node scripts/check-assets-manifest.mjs`, `bash scripts/check-terrain-atlas.sh`, `node scripts/validate-levels.mjs`, `node scripts/check-geometry-frozen.mjs`, `node scripts/browser-boot.mjs`, and `node scripts/audit-phase21-mechanics.mjs`. This phase DID add art (plan 36-10: swamp + cemetery lanterns + the patroller walk sprite), so ALSO run `bash scripts/check-pink-gate.sh` and `node scripts/check-assets-manifest.mjs` and `bash scripts/check-terrain-atlas.sh` — mandatory here, not conditional. For the audit, READ the output and confirm EVERY mover encounter shows ridden (triggered+resolved) and EVERY patroller shows crossed (triggered+resolved) on all 8 levels — record the per-level evidence. Write `36-VERIFICATION.md` mirroring the Phase-35 verification format: a per-gate PASS/FAIL table, the audit ride/cross evidence per level, and a short statement closing MOT-01/02/03 + MECH-05. If ANY gate fails or any mover/patroller is not ridden/crossed, do NOT paper over it — record the failure and route it back to the owning placement plan (07/08) or the harness/infra plan; the phase does not close on a red gate. Use `PLAYWRIGHT_MJS_PATH` if the repo default is stale on this host (per the 35-01 SUMMARY note).
  </action>
  <verify>
    <automated>bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && node scripts/check-assets-manifest.mjs && bash scripts/check-terrain-atlas.sh && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs 2>&1 | tail -30 && test -f .planning/phases/36-world-motion-ambient-life/36-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Every CLAUDE.md gate PASSES with motion live on all 8 levels (recorded in 36-VERIFICATION.md).
    - check-geometry-frozen PASSES (static geometry byte-identical to the post-34.6 baseline with all motion keys present).
    - The audit shows every mover ridden + every patroller crossed across all 8 levels (per-level evidence recorded — read the output, not just exit code).
    - check-safety confirms no scheduler/punishment; if new art was added, the pink-gate passes.
    - 36-VERIFICATION.md exists with the per-gate table + audit evidence + the MOT-01/02/03/MECH-05 closure statement.
  </acceptance_criteria>
  <done>The phase closes on the FULL suite green with motion live: geometry byte-frozen, ADHD-safe mandate held, and the interactive audit proving the world rides and crosses across all 8 levels — MOT-01/02/03 + MECH-05 satisfied under genuine automated + interactive proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate scripts → verification record | Static; the consolidation reads gate output, adds no runtime surface |

Static, offline browser game — running gates + recording results adds no trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-20 | Repudiation | phase closed on a subset of gates / audit exit code only | high | mitigate | Run the FULL CLAUDE.md list; READ audit output per level; record every result in 36-VERIFICATION.md |
| T-36-21 | Tampering | a red gate papered over to "finish" the phase | high | mitigate | Plan forbids closing on a red gate; failures route back to the owning plan |
| T-36-SC | Tampering | package installs | n/a | accept | Zero new dependencies this phase |
</threat_model>

<verification>
```
bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh \
  && bash scripts/check-progress.sh && node scripts/check-assets-manifest.mjs \
  && bash scripts/check-terrain-atlas.sh && node scripts/validate-levels.mjs \
  && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs \
  && node scripts/audit-phase21-mechanics.mjs
```
- The full CLAUDE.md suite green with motion live; geometry byte-frozen; audit rides/crosses all 8 levels (read output).
</verification>

<success_criteria>
- Full CLAUDE.md gate suite green with motion live on all 8 levels.
- Geometry byte-frozen with motion keys present; no scheduler/punishment.
- Audit rides every mover + crosses every patroller across all 8 levels.
- 36-VERIFICATION.md records the evidence and closes MOT-01/02/03 + MECH-05.
</success_criteria>

<output>
Create `.planning/phases/36-world-motion-ambient-life/36-09-SUMMARY.md` when done.
</output>
