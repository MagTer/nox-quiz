---
phase: 36-world-motion-ambient-life
plan: 06
type: execute
wave: 5
depends_on: ["36-05"]
files_modified: []
autonomous: false
requirements: [MOT-01, MOT-02, MOT-03, MECH-05]
must_haves:
  truths:
    - "A human plays/reviews the two trial levels' motion IN-ENGINE and explicitly approves or rejects the hazard placement — density, telegraphing, no-softlock feel, and the ADHD-safe forgiving-respawn character"
    - "The approval covers BOTH the calm level-01 (light density) AND the intense on-path level-06 (heavier, movers on the critical path) — the two extremes of the biome-pair rhythm"
    - "Downstream rollout (plans 07-08, the remaining 6 levels) does NOT proceed until this sign-off passes — and it is NOT rubber-stamped (auto_advance must not auto-approve; standing SC5 precedent)"
  artifacts:
    - "A recorded human decision (approved / rejected-with-notes) referencing the specific motion-shots + live playthrough"
  key_links:
    - "motion-shots/ + live playthrough of level-01 & level-06 → human hazard-placement judgment → approve/reject → gates plans 07-08"
---

<objective>
The load-bearing SC5 human sign-off on hazard placement. A real person plays the two trial levels' motion in-engine — the calm forgiving level-01 and the intense, on-critical-path level-06 — and confirms the moving platforms and patrollers read right: telegraphed, no-softlock, genuinely dynamic but never frustrating, forgiving checkpoint-respawn only. This gates the remaining-6 rollout.

Purpose: Movement and risk are now on the critical path (CONTEXT decision), so the human look matters MORE than in prior phases — this is where a mistimed-jump-feels-unfair or a patroller-feels-like-punishment problem is caught, which no automated gate can judge ("checks that don't play the game lie"). Standing precedent (Phases 25/27/28/31/33/35): this MUST NOT be rubber-stamped, and `auto_advance` MUST NOT auto-approve it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-world-motion-ambient-life/36-CONTEXT.md
@docs/LEVEL-DESIGN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human sign-off — hazard placement on the 2 trial levels (swamp calm + cemetery intense)</name>
  <files></files>
  <action>
    Review the two trial levels' motion IN-ENGINE and record a blocking human-verify sign-off — the exact evidence, what to look for, and the resume signal are in the &lt;what-built&gt;/&lt;how-to-verify&gt;/&lt;resume-signal&gt; blocks below; follow them precisely. This is a verification-only checkpoint; write no code. Do NOT auto-approve (auto_advance must not rubber-stamp this — standing SC5 precedent, and movement is now on the critical path). If placement is rejected, route the notes back to plan 05 (density / speed / endpoint placement / telegraph / checkpoint cadence) and re-capture before any further level gets motion.
  </action>
  <what-built>
    FULL motion on two trial levels of two biomes at two densities:
    - level-01 (swamp, calm odd): LIGHT density — ≥1 moving platform + ≥1 patroller, forgiving margins, ambient flicker.
    - level-06 (cemetery, intense even, vertical switchback): HEAVIER density — multiple movers/patrollers, some on the critical path, the math-skip key spur, ambient flicker, and the MECH-05 alcove-linked torch that lights on discovery.
    Every mover passes mover-reachability (both endpoints reachable rightward, worst-case-extreme), sits behind a checkpoint, and has a WAIT-not-death recovery (no killing pit under a mover). Patroller contact = the existing gentle checkpoint-respawn (zero hurt/score/game-over). Geometry byte-frozen; check-safety/validate-levels/check-geometry-frozen/browser-boot green; the interactive audit rides every mover + crosses every patroller. Evidence: `.planning/phases/36-world-motion-ambient-life/motion-shots/`.
  </what-built>
  <how-to-verify>
    1. Open the motion-shots in `.planning/phases/36-world-motion-ambient-life/motion-shots/` — confirm each mover's path and each patroller's path are visible/telegraphed.
    2. Play both levels LIVE: from the repo root run `python3 -m http.server 8000`, browse to `http://localhost:8000/src/index.html`, reach level-01 + level-06 (use `?debug=1` to see colliders/checkpoints and confirm no mover hides a killing pit).
    3. Judge, explicitly, for BOTH levels:
       - NO SOFTLOCK (the gate that matters): ride every mover; miss one on purpose — can you always WAIT for it to return, never trapped, never a killing pit under it? Is there a checkpoint before every mover?
       - TELEGRAPH: can you see the far endpoint of every mover BEFORE you commit to the jump? Is every patroller slow + readable (visible walk cycle, clear path)?
       - ADHD-SAFE / FORGIVING: touching a patroller respawns at a NEAR checkpoint (a few seconds lost) — never hurt, never score loss, never a game-over. Does it feel like fair challenge, not punishment?
       - DENSITY & RHYTHM: level-01 reads calm/light; level-06 reads intense but fair. Distinct from each other.
       - DISTINCTNESS: are patrollers visually distinct from the stationary math-blocker enemy?
    4. MECH-05: on level-06, discover the secret alcove — does a torch permanently light? Re-enter the level — is it already lit?
  </how-to-verify>
  <resume-signal>
    Type "approved" to unlock the remaining-6 rollout (plans 07-08), OR describe exactly what to change (density, patroller speed, mover endpoints/telegraph, checkpoint cadence, alcove torch) — rejection sends the fixes back to plan 05 before any further level gets motion. Do NOT auto-approve.
  </resume-signal>
  <done>A human has played both trial levels in-engine and recorded an explicit approve/reject decision referencing the specific motion-shots + live feel; on approval the swamp+cemetery placement approach becomes the proven template for the remaining-6 rollout.</done>
</task>

</tasks>

<verification>
This plan gates on a genuine human decision — there is no automated pass. The prerequisite gates (check-safety, validate-levels/mover-reachability, check-geometry-frozen, browser-boot, the ride/cross audit) are already green from plan 05; this checkpoint adds the no-softlock/telegraph/forgiving-feel judgment those gates structurally cannot make.
</verification>

<success_criteria>
- A human has played both trial levels in-engine and recorded an explicit approve/reject decision referencing the specific motion-shots.
- If approved: the level-01 + level-06 placement approach (density, telegraph, checkpoint cadence, no-softlock authoring) is the proven template for the remaining-6 rollout.
- If rejected: concrete notes exist and plan 05 is revised before any further level gets motion.
</success_criteria>

<output>
Create `.planning/phases/36-world-motion-ambient-life/36-06-SUMMARY.md` recording the decision (approved/rejected) and any notes that constrain the rollout.
</output>
