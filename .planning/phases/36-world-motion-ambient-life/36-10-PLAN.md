---
phase: 36-world-motion-ambient-life
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-art-assets.py
  - src/assets-manifest.js
  - src/main.js
  - src/levels/level-01.js
  - src/levels/level-06.js
  - assets/props/swamp-lantern.png
  - assets/props/cemetery-lantern.png
  - assets/patroller.png
  - CREDITS.md
autonomous: true
requirements: [MOT-01, MOT-03, MECH-05]
must_haves:
  truths:
    - "A biome-coherent SWAMP light-source prop (`prop-swamp-lantern` — e.g. a bog lantern / will-o'-wisp) and a CEMETERY light-source prop (`prop-cemetery-lantern` — e.g. a grave lantern / brazier) are baked, registered in the manifest, loaded in main.js, and pass the pink-gate + assets-manifest + terrain-atlas gates — the swamp (levels 01/02) and cemetery (levels 05/06) biomes finally HAVE a light to flicker (MOT-03) and light-up on alcove discovery (MECH-05)"
    - "A DISTINCT patroller sprite (`patroller`) with a real `walk` animation is baked, registered, and loaded — visually distinct from `enemy-hellhound` (the idle-only math-blocker) so a patroller reads as a moving hazard with a walk-cycle telegraph (MOT-01 / SC1)"
    - "One swamp lantern is PLACED in level-01's top-level `props` array near its secret alcove (x≈320) and one cemetery lantern in level-06's `props` array near its alcove (x≈360) — top-level props are OUTSIDE geometry, so check-geometry-frozen stays green — giving the two trial levels (36-05) a light BEFORE the trial runs"
    - "All new art follows ART-PARITY-STEERING: styleboard-normative source/crop/retint, any new CC0 licensed in assets/LICENSES/ + credited in CREDITS.md, and no pink"
  artifacts:
    - "assets/props/swamp-lantern.png + assets/props/cemetery-lantern.png — baked light-source props (build-art-assets.py bake_prop calls)"
    - "assets/patroller.png — baked patroller sprite sheet with a walk anim"
    - "src/assets-manifest.js — +prop-swamp-lantern, +prop-cemetery-lantern (kind:prop), +patroller (kind:sprite-anim)"
    - "src/main.js — loadSprite('patroller', ...) with a walk anim; the two new lantern props load through the existing kind:'prop' manifest loop"
    - "src/levels/level-01.js + level-06.js — the swamp/cemetery lantern placed in the top-level props array near each alcove"
  key_links:
    - "build-art-assets.py bake_prop → assets/props/<biome>-lantern.png → assets-manifest.js kind:prop → main.js kind:prop loop → build.js prop emit (36-04 flicker selector matches the *-lantern key)"
    - "build-art-assets.py → assets/patroller.png → assets-manifest.js kind:sprite-anim → main.js loadSprite('patroller',{anims:{walk}}) → CONFIG.PATROLLER.SPRITE (set in 36-03) → build.js patroller loop foe.play('walk')"
    - "level-01/06 props[*-lantern] → 36-04 lightAmbient() linked light (the alcove-torch the discovery brightens, MECH-05)"
---

<objective>
Bake and register the two motion art assets the phase is currently MISSING, before any level places motion:

1. A **swamp light-source prop** and a **cemetery light-source prop**. Today only town (`town-street-lamp`) and castle (`castle-candles`/`castle-candle-stand`) ship a light — the swamp (levels 01/02) and cemetery (levels 05/06) biomes have NONE. Without them there is nothing to flicker (MOT-03) and nothing to light on alcove discovery (MECH-05) on four of the eight levels, including BOTH trial levels (level-01 swamp, level-06 cemetery). This plan bakes a biome-coherent light for each and PLACES one in each trial level's props array so the trial (36-05) and its human sign-off (36-06) are satisfiable.
2. A **distinct patroller sprite with a `walk` animation**. Today only `enemy-hellhound` exists — an idle-only math-blocker. A cosmetic patroller (MOT-01) must be visually DISTINCT from it and carry a real walk-cycle telegraph (SC1). This plan sources/bakes that sprite, registers it, and loads it so 36-03's `CONFIG.PATROLLER.SPRITE` points at a real loaded key and 36-05 can place patrollers with a readable walk cycle.

Purpose: These are the art prerequisites for the whole motion track — the flicker (36-04), the trial (36-05), the human sign-off (36-06), and the rollout (36-07/08) all reference these keys. Landing them in Wave 1 (no dependencies) keeps them BEFORE the trial with a clean DAG.

Output: two baked lantern props + one baked patroller walk sprite, all manifest-registered, main.js-loaded, pink-gate/assets-manifest/terrain-atlas clean; the two lanterns placed in level-01 + level-06 props arrays near each alcove.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-world-motion-ambient-life/36-CONTEXT.md
@.planning/phases/36-world-motion-ambient-life/36-RESEARCH.md
@.planning/research/ART-PARITY-STEERING.md
@scripts/build-art-assets.py
@src/assets-manifest.js
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bake + register the swamp and cemetery light-source props</name>
  <files>scripts/build-art-assets.py, src/assets-manifest.js, CREDITS.md</files>
  <read_first>
    - scripts/build-art-assets.py — `build_props()` (line 1381), `bake_prop()` (line 1356), and the swamp block (1393-1406) + cemetery block (1408-1419): mirror these `bake_prop(...)` calls for a swamp lantern and a cemetery lantern. Prefer a crop from an ALREADY-vendored Gothicvania pack (crops-before-new-CC0, per the castle-props precedent at 1441-1464); if the swamp/cemetery packs carry no lantern/glow object, source a small CC0 lantern/brazier and record it. The town retint precedent (1430-1439) shows how a no-pink conform pass is applied when a source trips the gate.
    - src/assets-manifest.js — the `kind:"prop"` block (lines 72-99) and the `swamp x4 / cemetery x7` grouping comment (17-19): add `prop-swamp-lantern` and `prop-cemetery-lantern` (kind:"prop"), and bump the header counts (line 10-19 + line 21's "58 entries total").
    - .planning/research/ART-PARITY-STEERING.md — the WHOLE file: styleboard.py is normative for source/crop/retint/base-color; any deviation must be justified IN THIS PLAN's SUMMARY. New CC0 requires assets/LICENSES/ + CREDITS.md entries.
  </read_first>
  <action>
    In `build_props()`, add two `bake_prop(...)` calls: `bake_prop("swamp-lantern", <swamp source>, crop=..., retint=... as needed)` producing `assets/props/swamp-lantern.png`, and `bake_prop("cemetery-lantern", <cemetery source>, crop=..., ...)` producing `assets/props/cemetery-lantern.png`. Each must be a SINGLE light-source object (a bog lantern / hanging swamp lantern / will-o'-wisp for swamp; a grave lantern / standing brazier for cemetery), biome-coherent, dark-grunge, NO pink. Prefer crops from the already-vendored swamp/cemetery Gothicvania packs (measure-first: if under the pink threshold, bake native; if it trips the gate, apply a board-faithful no-pink conform pass exactly like the town retint). If — and only if — no vendored source carries a suitable light, source a small CC0 lantern/brazier, add its license file under `assets/LICENSES/`, and credit it in `CREDITS.md`. Register both in `src/assets-manifest.js` under `kind:"prop"` (`prop-swamp-lantern`, `prop-cemetery-lantern`) and update the count comments. Do NOT touch any other prop, atlas, or the biome retint tuples. Because this edits `build-art-assets.py`, the terrain-atlas PIXEL gate must be re-run (it guards ground atlases against sawtooth/grey/float regressions).
  </action>
  <verify>
    <automated>python3 scripts/build-art-assets.py && test -f assets/props/swamp-lantern.png && test -f assets/props/cemetery-lantern.png && node scripts/check-assets-manifest.mjs && bash scripts/check-pink-gate.sh && bash scripts/check-terrain-atlas.sh && grep -c "lantern" src/assets-manifest.js</automated>
  </verify>
  <acceptance_criteria>
    - `assets/props/swamp-lantern.png` and `assets/props/cemetery-lantern.png` exist and are single biome-coherent light-source objects.
    - Both are registered in `src/assets-manifest.js` (kind:"prop") and the entry-count comments are updated; `node scripts/check-assets-manifest.mjs` passes.
    - `bash scripts/check-pink-gate.sh` passes (no pink) and `bash scripts/check-terrain-atlas.sh` passes (build-art-assets.py change did not regress any ground atlas).
    - Any new CC0 is licensed under `assets/LICENSES/` and credited in `CREDITS.md`; a vendored-crop source needs no new license (note the choice in the SUMMARY).
  </acceptance_criteria>
  <done>Swamp and cemetery biomes have a baked, license-clean, no-pink light-source prop — the missing light that MOT-03 flicker and MECH-05 alcove-torch depend on for four levels.</done>
</task>

<task type="auto">
  <name>Task 2: Bake + register + load a distinct patroller sprite with a walk anim</name>
  <files>scripts/build-art-assets.py, src/assets-manifest.js, src/main.js, CREDITS.md</files>
  <read_first>
    - scripts/build-art-assets.py — `build_enemy_hellhound()` (line 1056): the sprite-bake precedent (a direct style-board creature pick, sliced into frames). Mirror it for a DISTINCT patroller creature that has a WALK cycle (the hellhound bake is idle-only). Prefer a vendored Gothicvania creature with a walk sheet; new CC0 only if none exists (license + credit it).
    - src/main.js — the sprite loads (lines 63-137), especially the `enemy-hellhound` loadSprite with `sliceX:6` + `anims:{idle:...}` (132-137). Add a sibling `loadSprite("patroller", "../assets/patroller.png", { sliceX: N, anims: { walk: { from, to, loop:true, speed: CONFIG.PATROLLER.WALK_SPEED ?? CONFIG.ENEMY.IDLE_SPEED } } })`. Keep every engine ref inside the module's post-init load section as the existing loads are (a727c13 — these run after kaplay init).
    - src/assets-manifest.js — `sprite-anim` block (lines 55-58): add `{ key: "patroller", path: "assets/patroller.png", kind: "sprite-anim" }` and bump the count comment (line 14).
    - .planning/research/ART-PARITY-STEERING.md — normative for the creature pick/crop/retint; new CC0 → license + credit.
  </read_first>
  <action>
    Add a `build_patroller()` (or equivalent bake) to `build-art-assets.py` that produces `assets/patroller.png` as a sliced walk-cycle sheet for a creature VISUALLY DISTINCT from `enemy-hellhound` (a different silhouette/palette — e.g. a shambling skeleton, ghoul, or bat with a walk animation), dark-grunge, no pink; wire it into the `__main__` build list. Register it in `src/assets-manifest.js` under `kind:"sprite-anim"` (`patroller`) and bump the count. In `src/main.js`, add a `loadSprite("patroller", "../assets/patroller.png", { sliceX: N, anims: { walk: { from: 0, to: N-1, loop: true, speed: <config-driven> } } })` next to the `enemy-hellhound` load — the `walk` anim name is what `build.js`'s patroller loop calls via `foe.play("walk")` (36-03). Do NOT modify the `enemy-hellhound` load. If the sprite is new CC0, license it under `assets/LICENSES/` and credit it in `CREDITS.md`. The final sprite/telegraph is confirmed at the human checkpoint (36-06) — pick a sane distinct default here.
  </action>
  <verify>
    <automated>python3 scripts/build-art-assets.py && test -f assets/patroller.png && node scripts/check-assets-manifest.mjs && bash scripts/check-pink-gate.sh && grep -n '"patroller"' src/main.js && grep -n "walk" src/main.js</automated>
  </verify>
  <acceptance_criteria>
    - `assets/patroller.png` exists as a sliced walk-cycle sheet, visually distinct from `enemy-hellhound`.
    - Registered in `src/assets-manifest.js` (kind:"sprite-anim"); `node scripts/check-assets-manifest.mjs` passes.
    - `src/main.js` loads `patroller` with a `walk` anim (the name `build.js` plays); the hellhound load is unchanged.
    - `bash scripts/check-pink-gate.sh` passes; new CC0 (if any) licensed + credited.
  </acceptance_criteria>
  <done>A distinct, walk-animated patroller sprite exists and is loaded under the key `patroller` — CONFIG.PATROLLER.SPRITE (36-03) can point at it and 36-05 can place a readable, telegraphed hazard.</done>
</task>

<task type="auto">
  <name>Task 3: Place the swamp lantern in level-01 and the cemetery lantern in level-06 (props, near each alcove)</name>
  <files>src/levels/level-01.js, src/levels/level-06.js</files>
  <read_first>
    - src/levels/level-01.js — the top-level `props` array (lines 232-246) and `secretAlcove: [{ x: 320, y: 184 }]` (line 212). Props are TOP-LEVEL and VISUAL-ONLY (comment 220-230) — OUTSIDE `geometry`, so adding one does NOT trip check-geometry-frozen. Follow the existing prop entry shape (`{ sprite, x, y, layer }`) and on-surface placement convention (y = surfaceY − spriteHeight).
    - src/levels/level-06.js — the top-level `props` array (lines 192-208) and `secretAlcove: [{ x: 360, y: 50 }]` (line 172). Same conventions; the level is a vertical switchback, so place the lantern on a readable surface near the alcove.
    - .planning/phases/36-world-motion-ambient-life/36-04-PLAN.md — the flicker selector (the `*-lantern` key is what the 36-04 substring selector matches) and the MECH-05 alcove-torch link (36-05 designates THIS lantern as the one lightAmbient() brightens).
  </read_first>
  <action>
    Add exactly ONE `{ sprite: "prop-swamp-lantern", x, y, layer }` entry to level-01's top-level `props` array, positioned near the secret alcove (x≈320) on a visible surface. Add exactly ONE `{ sprite: "prop-cemetery-lantern", x, y, layer }` entry to level-06's top-level `props` array, near its alcove (x≈360). These are ADD-ONLY to the top-level `props` array — do NOT touch `geometry` (floors/platforms/coins/checkpoints/goal/enemies/secretAlcove/bounds stay byte-frozen). The lantern is the light 36-04 flickers and 36-05 links to the alcove for the MECH-05 lit-on-discovery / lit-on-entry behavior. Keep placement clear of the door/enemy/goal per the existing prop-clearance comments.
  </action>
  <verify>
    <automated>node scripts/check-geometry-frozen.mjs && node scripts/validate-levels.mjs && grep -n "prop-swamp-lantern" src/levels/level-01.js && grep -n "prop-cemetery-lantern" src/levels/level-06.js && bash scripts/check-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - level-01 props includes one `prop-swamp-lantern`; level-06 props includes one `prop-cemetery-lantern`, each near its alcove.
    - `node scripts/check-geometry-frozen.mjs` PASSES (props are top-level, outside geometry — no static-array drift).
    - `node scripts/validate-levels.mjs` stays 0 HARD-FAIL; check-safety passes.
  </acceptance_criteria>
  <done>Both trial levels now carry a biome light near their alcove — the flicker (36-04) has a target and the MECH-05 alcove-torch (36-05) has a light to link, BEFORE the trial runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| art source → bake pipeline | CC0/vendored source PNG crops processed by Pillow; no network/user input at runtime |

Static, offline browser game — baking sprites + placing visual-only props adds no trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-22 | Tampering | new art trips the pink gate / lies about a solid surface | high | mitigate | check-pink-gate + check-terrain-atlas re-run after the build-art-assets.py change; props are collider-free visual-only |
| T-36-23 | Spoofing | patroller sprite indistinguishable from the math-blocker enemy (misreads as a challenge) | medium | mitigate | Distinct silhouette/palette + a real walk anim vs the hellhound's idle-only; confirmed at the 36-06 human checkpoint |
| T-36-24 | Tampering | placing a lantern accidentally nudges a frozen geometry array | high | mitigate | Lanterns go in the TOP-LEVEL props array (outside geometry); check-geometry-frozen PASSES |
| T-36-SC | Tampering | package installs | n/a | accept | Zero new runtime deps (Pillow already present); any new art is CC0, licensed + credited |
</threat_model>

<verification>
```
python3 scripts/build-art-assets.py && node scripts/check-assets-manifest.mjs \
  && bash scripts/check-pink-gate.sh && bash scripts/check-terrain-atlas.sh \
  && node scripts/check-geometry-frozen.mjs && node scripts/validate-levels.mjs \
  && bash scripts/check-safety.sh && node scripts/browser-boot.mjs
```
- assets-manifest + pink-gate + terrain-atlas: new art registered, no pink, no ground regression.
- check-geometry-frozen + validate-levels: lanterns are top-level props (geometry byte-frozen, 0 HARD-FAIL).
- browser-boot: all 8 levels still boot + reach goal (lanterns are visual-only; patroller sprite loaded but unplaced).
</verification>

<success_criteria>
- Swamp + cemetery light-source props baked, registered, loaded, pink/manifest/terrain-atlas clean.
- Distinct walk-animated patroller sprite baked, registered, loaded under key `patroller`.
- Swamp lantern placed in level-01, cemetery lantern in level-06 (near each alcove); geometry byte-frozen.
- All art ART-PARITY-STEERING-compliant; new CC0 licensed + credited.
</success_criteria>

<output>
Create `.planning/phases/36-world-motion-ambient-life/36-10-SUMMARY.md` when done.
</output>
</content>
</invoke>
