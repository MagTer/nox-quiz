---
phase: 36-world-motion-ambient-life
plan: 05
type: execute
wave: 4
depends_on: ["36-03", "36-04"]
files_modified:
  - src/levels/level-01.js
  - src/levels/level-06.js
  - scripts/screenshot-phase36-motion.mjs
autonomous: true
requirements: [MOT-01, MOT-02, MOT-03, MECH-05]
must_haves:
  truths:
    - "level-01 (swamp, calm odd) carries LIGHT mover density and level-06 (cemetery, intense even, vertical switchback) carries HEAVIER density — each with ≥1 moving platform + ≥1 patroller, ambient flicker, and (level-06) the alcove-linked torch — proving the calm/intense rhythm on two biomes"
    - "Every placed mover passes the Phase-30 mover-reachability check (both ping-pong endpoints reachable rightward from spawn, worst-case-extreme) — NO SOFTLOCK; every mover sits behind a checkpoint with a WAIT-not-death recovery (§6b rules 1-2), no killing pit under a mover"
    - "The static geometry of both levels stays byte-frozen (motion is added ONLY via the new geometry.movers/geometry.patrollers keys, never by editing floors/platforms/coins/goal/doors/enemies/keys)"
    - "The interactive audit RIDES both levels' movers and CROSSES both levels' patrollers; check-safety stays green"
  artifacts:
    - "src/levels/level-01.js — geometry.movers + geometry.patrollers (light) + flicker/alcove-link fields"
    - "src/levels/level-06.js — geometry.movers + geometry.patrollers (heavier) + the alcove-linked torch"
    - "scripts/screenshot-phase36-motion.mjs — telegraph/motion evidence capture (spawn + mover mid-travel frames) for the human checkpoint"
  key_links:
    - "level-0N geometry.movers → build.js mover loop → mover-reachability row (validate-levels) + ride-audit"
    - "level-0N geometry.patrollers → build.js patrol loop → onCollide('patroller')→respawn + cross-audit"
    - "level-06 alcove-linked torch → onDiscover lightAmbient → persistent lit (MECH-05)"
---

<objective>
Place FULL motion on the two trial levels — level-01 (swamp, calm odd, the most forgiving of the eight) with LIGHT density and level-06 (cemetery, intense even, vertical switchback with the math-skip key spur) with HEAVIER density — as the proven template the human checkpoint (36-06) signs off before the remaining-6 rollout. Each gets moving platforms + patrollers on the biome-pair rhythm, ambient flicker, and (level-06) the alcove-linked persistent torch (MECH-05). Every mover is authored to the LEVEL-DESIGN §6a/§6b HARD rules so the mover-reachability validator stays green and NO placement can strand her.

Purpose: This is the TRIAL in the harness-first→trial→sign-off→rollout pattern. Two biomes at two densities exercise both extremes before any bulk placement; the screenshot evidence + interactive audit feed the SC5 human sign-off. Movers MAY sit on the critical path (CONTEXT decision) — so the no-softlock authoring rules are load-bearing here.

Output: geometry.movers + geometry.patrollers (+ flicker/alcove-link fields) on level-01 and level-06; a motion-evidence screenshot script; all gates green including mover-reachability + the ride/cross audit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-world-motion-ambient-life/36-CONTEXT.md
@.planning/phases/36-world-motion-ambient-life/36-RESEARCH.md
@docs/LEVEL-DESIGN.md
@src/levels/level-01.js
@src/levels/level-06.js
@src/levels/build.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author motion on level-01 (swamp, calm odd — LIGHT density)</name>
  <files>src/levels/level-01.js</files>
  <read_first>
    - src/levels/level-01.js — the WHOLE descriptor: geometry (floors/platforms/coins/checkpoints/goal), bounds (level-01 derives its right edge from geometry — §7), and the top-level props array. Identify where a checkpoint already sits (a mover MUST sit behind one — §6b rule 1) and where the forward rightward path has room for a mover whose BOTH endpoints are reachable rightward from spawn.
    - docs/LEVEL-DESIGN.md — §6a (worst-case-extreme, rightward-travel-only limitation), §6b (all 4 HARD motion rules), §8.5 rule 4 (close checkpoints, falls-can-kill-because-respawn-is-close). Level-01 stays the MOST FORGIVING — light density, generous margins.
    - .planning/phases/36-world-motion-ambient-life/36-RESEARCH.md — Pattern 1 CONTRACT (x1/y1↔x2/y2 are the true extremes), Pitfall 2 (rightward-only), Pitfall 3 (no killing pit under a mover).
  </read_first>
  <action>
    Add a `movers` and a `patrollers` array INSIDE `geometry` (the new keys the freeze gate now excludes) — LIGHT density for the calm odd level: ≥1 moving platform and ≥1 patroller, no more than the level's forgiving character warrants. Author each mover's `{x1,y1,x2,y2}` so BOTH ping-pong endpoints are reachable by walking/jumping RIGHTWARD from spawn (validator limitation) and both sit behind an existing checkpoint; leave a ledge/safe floor so a missed mover means WAIT, never death — NO killing pit under a mover (§6b rule 2). Keep the far endpoint visible from the ledge you jump from (§6b rule 4, telegraphed). Author each patroller's `{x1,y1,x2,y2}` (+ optional `speed`) on a readable path behind a checkpoint, slow and telegraphed, DISTINCT from any stationary math-blocker enemy. Add flicker to the swamp's light-source props (the `flicker`/light-source selector from 36-04). Do NOT touch any existing static array (floors/platforms/coins/checkpoints/goal/enemies) — motion is ADD-ONLY via the new keys. Do NOT edit inside the kid-validated geometry; append the new arrays.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs 2>&1 | grep -i "mover\|level-01\|HARD-FAIL" ; node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && bash scripts/check-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - level-01 geometry.movers + geometry.patrollers exist (light density: ≥1 each); mover-reachability reports PASS/WARN (never HARD-FAIL) for every mover.
    - check-geometry-frozen PASSES (static arrays byte-frozen; only motion keys added).
    - check-safety passes; no existing static array changed (diff shows only the two new keys + flicker fields).
  </acceptance_criteria>
  <done>level-01 has forgiving, telegraphed, no-softlock motion — the calm end of the biome-pair rhythm.</done>
</task>

<task type="auto">
  <name>Task 2: Author motion on level-06 (cemetery, intense even — HEAVIER density) + the alcove torch</name>
  <files>src/levels/level-06.js</files>
  <read_first>
    - src/levels/level-06.js — the WHOLE descriptor: the vertical switchback geometry, `bounds: { left:0, right:4560, top:-240, bottom:360 }` (used AS-IS — §7; bump `bounds.right` by hand ONLY if a mover pushes the play space right, which it should not since geometry is frozen), the checkpoints, the enemy at x:2600, the math-skip KEY spur, and the secretAlcove entry (for the MECH-05 linked torch). Identify the light-source prop nearest the alcove to link.
    - docs/LEVEL-DESIGN.md — §6a/§6b/§8.5 as Task 1; level-06 is the DENSITY + no-softlock stress test (movers on the switchback critical path).
    - .planning/phases/36-world-motion-ambient-life/36-RESEARCH.md — Pitfall 7 (mover collider vs jump envelope — keep vertical travel within one jump's reach at the worst endpoint), Pattern 3 (alcove-linked torch).
  </read_first>
  <action>
    Add `geometry.movers` + `geometry.patrollers` — HEAVIER density (multiple movers/patrollers, some on the switchback critical path per the CONTEXT decision) — each authored to the SAME no-softlock rules: both endpoints reachable rightward from spawn, behind a checkpoint, WAIT-not-death recovery, no killing pit under a mover, far end telegraphed, vertical travel within a single jump's reach at the worst endpoint. Add flicker to the cemetery light-source props. Link the MECH-05 torch: designate the light-source prop nearest the secret alcove as the one `lightAmbient()` brightens on discovery (the field/tag the 36-04 wiring reads) — so discovering the alcove permanently lights it, and a prior-run discovery renders it already lit. Do NOT edit any existing static array or bump geometry coordinates; motion + the alcove-torch link are ADD-ONLY via new keys/fields. Confirm `bounds` is unchanged (movers live within existing bounds).
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs 2>&1 | grep -i "mover\|level-06\|HARD-FAIL" ; node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && bash scripts/check-safety.sh && node scripts/audit-phase21-mechanics.mjs 2>&1 | grep -i "mover\|patroller\|level-06" | head</automated>
  </verify>
  <acceptance_criteria>
    - level-06 geometry.movers + geometry.patrollers exist (heavier density); every mover-reachability row PASS/WARN (never HARD-FAIL).
    - The alcove-linked torch field is set; check-geometry-frozen PASSES; bounds unchanged.
    - check-safety passes; the audit shows level-06 movers ridden + patrollers crossed (read the output per SUMMARY).
  </acceptance_criteria>
  <done>level-06 has intense, on-path but no-softlock motion + the MECH-05 alcove torch — the intense end of the biome-pair rhythm and the density stress test.</done>
</task>

<task type="auto">
  <name>Task 3: Motion-evidence screenshot script for the human checkpoint</name>
  <files>scripts/screenshot-phase36-motion.mjs</files>
  <read_first>
    - scripts/screenshot-phase35-props.mjs — the WHOLE file: the server/guard boot, all-levels-unlocked save seed, title→select→game nav, per-level `page.screenshot`, and the `repositionAndSettle` climb idiom. CLONE it (copy-not-extract per the duplication convention).
    - .planning/phases/36-world-motion-ambient-life/36-RESEARCH.md — §6b rule 4 (telegraph: the far end must be visible from the ledge). The evidence must show the mover mid-travel + the patroller path so a human can judge telegraphing.
  </read_first>
  <action>
    Clone `screenshot-phase35-props.mjs` to `scripts/screenshot-phase36-motion.mjs` verbatim, then change only: OUT_DIR → `.planning/phases/36-world-motion-ambient-life/motion-shots/` (mkdir recursive); accept an optional CLI list of level ids (default level-01 + level-06 for the trial); for each level capture a spawn shot PLUS at least one MID-TRAVEL frame of a mover (let the scene run a beat so the platform has moved off its start endpoint — sample the `get("mover")`/`get("patroller")` positions and shoot when a mover is visibly between endpoints) → `level-NN-<biome>-motion.png`. Reuse whatever settle/pin idiom the clone already has; keep it node-runnable with the dynamically-resolved Playwright (honor `PLAYWRIGHT_MJS_PATH`). This is telegraph evidence for the SC5 sign-off — it must show WHERE movers/patrollers are and that their paths read.
  </action>
  <verify>
    <automated>node scripts/screenshot-phase36-motion.mjs level-01 && ls .planning/phases/36-world-motion-ambient-life/motion-shots/ | grep level-01</automated>
  </verify>
  <acceptance_criteria>
    - Running with `level-01` produces a non-empty PNG under `motion-shots/`; default (no args) shoots level-01 + level-06.
    - Each shot shows a mover mid-travel (not only parked at an endpoint) and the patroller path.
    - The script reuses the Phase-35 boot/nav/guard code (copy-not-extract).
  </acceptance_criteria>
  <done>Repeatable motion/telegraph evidence exists for the two trial levels — the visual input the human hazard-placement sign-off needs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor → build/validate | Static Node data; no network/user input |

Static, offline browser game — authoring motion data + a screenshot script adds no trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-11 | Denial of Service | a required on-path mover strands the player (softlock) | high | mitigate | mover-reachability green (both endpoints rightward-reachable, worst-case-extreme); WAIT-not-death authoring (§6b); no killing pit under a mover; human checkpoint rides it |
| T-36-12 | Tampering | a static-array coordinate nudged while adding motion | high | mitigate | check-geometry-frozen PASSES (motion is add-only via excluded keys); diff shows only new keys |
| T-36-13 | Repudiation | patroller feels like punishment (too fast / untelegraphed) | medium | mitigate | Slow CONFIG.PATROLLER.SPEED + walk-cycle telegraph; human sign-off (36-06) judges it |
| T-36-SC | Tampering | package installs | n/a | accept | Zero new dependencies this phase |
</threat_model>

<verification>
```
bash scripts/check-safety.sh && node scripts/validate-levels.mjs \
  && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs \
  && node scripts/audit-phase21-mechanics.mjs
```
- validate-levels: mover-reachability PASS/WARN for every trial mover (no HARD-FAIL).
- check-geometry-frozen: static arrays byte-frozen (motion keys excluded).
- audit: level-01 + level-06 movers ridden, patrollers crossed (read the output).
- browser-boot: both levels boot + reach goal with motion live.
</verification>

<success_criteria>
- level-01 (light) + level-06 (heavy) carry no-softlock motion + ambient flicker + (L06) the alcove torch.
- Every mover reachability-green; geometry byte-frozen; audit rides/crosses both levels.
- Motion evidence captured for the human sign-off.
</success_criteria>

<output>
Create `.planning/phases/36-world-motion-ambient-life/36-05-SUMMARY.md` when done.
</output>
