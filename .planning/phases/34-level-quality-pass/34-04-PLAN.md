---
phase: 34-level-quality-pass
plan: 04
type: execute
wave: 3
depends_on: ["34-01", "34-02"]
files_modified:
  - src/levels/level-08.js
autonomous: false
requirements: [LVL-02, LVL-01]

must_haves:
  truths:
    - "Level-08's end climb is visibly and mechanically a different experience from level-07's: 07 stays a monotonic up-and-right staircase with shrinking tiers; 08 becomes a switchback that reverses direction twice and summits on a wide balcony."
    - "The redesigned climb is validator-green (zero HARD-FAILs) and driven end-to-end in a real browser before the user ever sees it."
    - "Level-08's coins — including every coin on the new climb — are reachable, proven statically and collected in-engine."
    - "The user sees and approves the shape before the plan closes. This checkpoint is never auto-approved."
  artifacts:
    - "src/levels/level-08.js: switchback end climb (platforms), per-tier checkpoints, repositioned coins, relocated secret alcove, relocated goal, bounds.right verified/hand-bumped"
    - "level-08 climb screenshots (?debug=1) presented at the human checkpoint"
  key_links:
    - "level-08 carries an explicit `bounds` field used AS-IS (level-01 derives its right edge from geometry; level-02+ do not) — if the new climb's rightmost edge exceeds bounds.right (3540), the camera clamps short of the goal. This is the documented bounds-convention trap."
    - "Consecutive climb tiers need ~70px x-overlap: the rising-jump root's short reach (~39px at a 70px rise) must land inside the overlap window, or the hop reads as unreachable to the BFS graph even though it looks like a tiny step (25-03 produced exactly this false HARD-FAIL on levels 07 and 08)."
    - "The secret alcove is checked by the RIGHTWARD-TRAVEL-ONLY point model (bestMarginToPoint) — unlike coins, which Plan 34-01 made bidirectional. Place the alcove so it is reachable by a rightward hop from a surface at or before it, or it HARD-FAILs even if it is genuinely reachable in play."
---

<objective>
Level-07 and level-08 currently end with near-identical monotonic up-and-right staircases — ~65-70px rise per step, widths shrinking 280→220, identical `h: 24`. Redesign level-08's end climb into a genuinely different experience, keep the math density locked, keep the level playable and gentle, and show the user the shape before it lands.

```
level-07 tail: (2650,255,280) (2860,190,260) (3050,125,250) (3230,60,240) (3400,-5,230) (3560,-70,220)
level-08 tail: (2410,250,280) (2620,180,260) (2810,110,250) (2990,40,240) (3160,-30,230) (3320,-100,220)
```

Purpose: LVL-02. Level-08 is the capstone — its finale should feel like a different building, not the same staircase repainted.
Output: a switchback climb in `src/levels/level-08.js` — validator-green, driven in a real browser, coins collectable in-engine, and approved by the user at a blocking checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-level-quality-pass/34-CONTEXT.md
@.planning/phases/34-level-quality-pass/34-01-SUMMARY.md
@.planning/phases/34-level-quality-pass/34-02-SUMMARY.md
@docs/LEVEL-DESIGN.md
</context>

<artifacts_produced>
- `src/levels/level-08.js`: the six climb tiers replaced by a switchback; checkpoints, coins, secret alcove, goal and bounds updated to match
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author level-08's switchback end climb</name>
  <files>src/levels/level-08.js</files>
  <read_first>
    src/levels/level-08.js (whole file — the 4 floors, the 3 gap-bridging platforms, the 6 climb tiers and their ~70px-overlap rationale comment, the COMPLETE 4-field bounds literal and its Pitfall-2 warning, the per-tier checkpoints, coins, the secretAlcove, the goal, and the locked math density comment)
    src/levels/level-07.js (whole file — the climb you are differentiating FROM; do not edit this file)
    docs/LEVEL-DESIGN.md sections 1, 2, 3, 5, 6, 7 (physics envelope maxRise 88.331px / ~162px flat reach; the platform rise 60-75px + ~70px overlap rules; verticality climbs are PLATFORMS never floors; checkpoint policy; alcove placement; the bounds-convention trap)
  </read_first>
  <action>
    Replace level-08's six monotonic climb tiers with a SWITCHBACK: the player climbs rightward, then must turn around and hop up-LEFT, then reverse again and continue up-right to a wide summit balcony. Two direction reversals — level-07 has none. Also invert the width trend (07 shrinks 280→220; 08 grows toward a broad balcony) and vary the rises (level-07's are a uniform ~65-70 band).

    Everything OUTSIDE the climb stays untouched: the 4 `floors` runs (0/560/1160/1800), the 3 gap-bridging platforms (480/1080/1720), the spikes, the door at x:700, the enemy at x:1600, the pre-climb checkpoints, `allowedTables`, `biome`. Math density is LOCKED at exactly 1 door + 1 enemy + the end goal gate — this plan may not add or remove a single mechanic (`mathGates` stays `[]`).

    Candidate shape (a STARTING POINT, not a spec — validate and adjust it; every number must survive `node scripts/validate-levels.mjs`):

    ```
    { x: 2410, y: 250, w: 300, h: 24 },  // T1  rise 70 from floor-3, entered rightward
    { x: 2640, y: 180, w: 280, h: 24 },  // T2  rise 70, overlaps T1 by 70
    { x: 2850, y: 115, w: 300, h: 24 },  // T3  rise 65, overlaps T2 by 70
    { x: 2600, y:  50, w: 350, h: 24 },  // T4  rise 65, overlaps T3 by 100 — REVERSAL: hop up-LEFT
    { x: 2880, y: -15, w: 320, h: 24 },  // T5  rise 65, overlaps T4 by 70  — REVERSAL: back up-RIGHT
    { x: 3130, y: -85, w: 380, h: 24 },  // T6  rise 70, overlaps T5 by 70  — wide summit balcony
    ```

    Hard rules these numbers must keep satisfying after any adjustment:
    - Tiers are PLATFORMS, never floors (floors are pinned to FLOOR_Y in build.js).
    - Every rise stays at or below the calibrated maxRise (88.331px); the 60-75px band is the soft target.
    - Every consecutive pair overlaps in x by ~70px, in BOTH the rightward and the leftward hops. A 20-30px overlap produced false spawn-goal HARD-FAILs on both 07 and 08 in Phase 25 — the short-time-of-flight root's ~39px reach must land inside the window.
    - All play space stays within the level's `bounds` (`top: -360` gives headroom above the summit).

    Then update, in the same file, everything coupled to the climb:
    - **checkpoints:** one at each tier's LANDING side — where the player actually arrives (near a tier's left end for a rightward hop, near its right end for the leftward hop onto T4). Keep the existing per-tier `tier.y - 48` convention and keep every pre-climb checkpoint. A fall during the climb must never cost more than one tier (the ADHD-safe policy; there is no game-over anywhere in this game).
    - **goal:** on the T6 summit balcony, keeping the level's own 80px goal-to-edge convention (`goal.x = T6.xEnd - 80`, `goal.y = T6.y - CONFIG.GOAL_SIZE`).
    - **bounds:** compute the rightmost geometry edge. Level-08 carries an explicit `bounds` field used AS-IS — if the new climb's rightmost edge plus the goal's footprint would exceed `bounds.right` (3540), HAND-BUMP `bounds.right`. This is the documented bounds trap: forget it and the camera clamps short of the goal. Keep the bounds literal COMPLETE (all 4 fields in one object — Pitfall 2); never write a partial bounds.
    - **coins:** reposition the climb coins (currently the 6 at x 2450-3360) onto the new tiers, plus fix any other level-08 coin the validator flags. Keep the level's coin count at 18 — move, never delete. A coin roughly `tier.y - 56` above a tier surface sits in the player's walk-through box; a coin at `tier.y - 90` needs a real hop. Mix both so the climb still rewards jumping.
    - **secretAlcove:** the existing alcove ({x:2950, y:40}) sits off the OLD tier-4 and must move. It stays a sideways detour off the main climb path, ~70px above a tier, never gating and never on the required path. CRITICAL: the alcove is judged by the RIGHTWARD-TRAVEL-ONLY point model (`bestMarginToPoint`) — place it so a rightward hop from a tier at or before it reaches it, or it HARD-FAILs even though a player could walk left to it. (Coins are bidirectional as of Plan 34-01; alcoves are not.)

    Iterate against `node scripts/validate-levels.mjs` until level-08 shows zero HARD-FAIL rows of any kind — spawn-goal, gap-width, mechanic-reachability, secret-alcove-reachability and coin-reachability all clean. The validator's overall run may still be red on other levels (Plan 34-05 has not landed yet); scope your green to level-08.
  </action>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs 2>/dev/null | grep 'level-08' | grep -c 'HARD-FAIL'` returns 0.
    - The floors array is untouched: `node -e 'import("./src/levels/level-08.js").then(m=>{const f=m.LEVEL_08.geometry.floors; const exp=[[0,420],[560,480],[1160,520],[1800,560]]; const ok=f.length===4&&f.every((r,i)=>r.x===exp[i][0]&&r.w===exp[i][1]); console.log(ok?"FLOORS-UNCHANGED":"FLOORS-CHANGED"); process.exit(ok?0:1);})'` prints `FLOORS-UNCHANGED`.
    - Math density unchanged: `node -e 'import("./src/levels/level-08.js").then(m=>{const g=m.LEVEL_08.geometry; const ok=g.doors.length===1&&g.enemies.length===1&&g.mathGates.length===0; console.log(ok?"DENSITY-LOCKED":"DENSITY-CHANGED"); process.exit(ok?0:1);})'` prints `DENSITY-LOCKED`.
    - Coin count held at 18 and exactly one secret alcove: `node -e 'import("./src/levels/level-08.js").then(m=>{const g=m.LEVEL_08.geometry; const ok=g.coins.length===18&&g.secretAlcove.length===1; console.log(ok?"CONTENT-OK":"CONTENT-CHANGED"); process.exit(ok?0:1);})'` prints `CONTENT-OK`.
    - The climb genuinely reverses direction: at least one consecutive tier pair moves net-LEFT (a later tier whose xStart is smaller than the previous tier's xStart) — verified by hand against the authored array and stated in the SUMMARY with the tier coordinates.
    - `bounds` is a complete 4-field literal and `bounds.right` is greater than or equal to the rightmost platform edge; `bash scripts/check-safety.sh && bash scripts/check-import-safety.sh` green.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validate-levels.mjs 2>/dev/null | grep 'level-08' | grep -c 'HARD-FAIL' | grep -qx 0 && node -e 'import("./src/levels/level-08.js").then(m=>{const g=m.LEVEL_08.geometry; const f=g.floors; const exp=[[0,420],[560,480],[1160,520],[1800,560]]; const ok=f.length===4&&f.every((r,i)=>r.x===exp[i][0]&&r.w===exp[i][1])&&g.doors.length===1&&g.enemies.length===1&&g.mathGates.length===0&&g.coins.length===18&&g.secretAlcove.length===1; process.exit(ok?0:1);})' && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh</automated>
  </verify>
  <done>Level-08's end climb is a switchback with two direction reversals and a wide summit balcony, validator-green with zero HARD-FAILs, with floors, math density, coin count and the single alcove all intact.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the new climb in a real browser and capture the evidence</name>
  <files>(none — verification-only task)</files>
  <read_first>
    scripts/browser-boot.mjs (header + the per-level drive loop, the FPS floor, the far-end non-blank check and the object-count budget assertion — the budget is the one most likely to trip, since the switchback's wider tiers emit more terrain tiles than the old narrow staircase)
    scripts/screenshot-phase33-terrain.mjs (the existing screenshot-script convention, if you need a fresh capture harness)
    CLAUDE.md (the `?debug=1` debug overlay — colliders, blockers, checkpoints and alcoves render as overlays; never hand-edit opacity for playtesting)
  </read_first>
  <action>
    Prove the climb in-engine before showing it to anyone. Green greps are not evidence — this project has shipped a sawtoothed floor and grey-static ground past a fully green suite.

    1. `node scripts/browser-boot.mjs` — all 8 levels boot and drive; the level-08 far-end/FPS/object-budget assertions must pass. If the object-count budget trips (the switchback's wider tiers emit more terrain tiles than the old staircase), reduce tier widths and re-validate rather than raising the budget.
    2. `node scripts/audit-coins.mjs` — every level-08 coin, including all the new climb coins, is collected in the real engine (zero skipped, zero uncollected).
    3. `node scripts/audit-phase21-mechanics.mjs` — level-08's door and enemy still report `triggered: true` (`resolved: false` is known headless-timing flakiness).
    4. Capture screenshots of the climb for the checkpoint: serve the repo root over HTTP and screenshot level-08 with `?debug=1` at a few climb altitudes (entry, the T3->T4 reversal, and the summit balcony), so the user can see the shape, the colliders, and the per-tier checkpoints. Save them under `.planning/phases/34-level-quality-pass/` and reference the absolute paths in the checkpoint.

    Record all four results in the SUMMARY with the numbers from the runs themselves.
  </action>
  <acceptance_criteria>
    - `node scripts/browser-boot.mjs` exits 0 with level-08's FPS/far-end/object-budget assertions green.
    - `node scripts/audit-coins.mjs` exits 0 with zero skipped and zero uncollected coins on level-08.
    - `node scripts/audit-phase21-mechanics.mjs` shows level-08's door and enemy `triggered: true`.
    - At least three level-08 climb screenshots exist under `.planning/phases/34-level-quality-pass/`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/browser-boot.mjs && node scripts/audit-coins.mjs && node scripts/audit-phase21-mechanics.mjs && test "$(ls .planning/phases/34-level-quality-pass/*.png 2>/dev/null | wc -l)" -ge 3</automated>
  </verify>
  <done>The new climb boots, drives, keeps its coins collectable and its mechanics triggerable in a real browser, and there are screenshots to show for it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off — the shape of level-08's new end climb (LVL-02)</name>
  <files></files>
  <action>
    Present the redesigned climb to the user and get an explicit, specific sign-off on its SHAPE before this plan closes — 34-CONTEXT.md decision 3 requires the user to see it before it lands. This project has a standing instruction (STATE.md, Phases 25/27/28/31/33) that a checkpoint:human-verify is NEVER auto-approved during autonomous execution, regardless of any auto-advance setting: ask, and wait. Hand over the three `?debug=1` screenshots from Task 2 with their absolute paths, plus the level-07 tail coordinates for comparison, plus a serve command so the climb can actually be played. If the user asks for changes, re-author the descriptor inside THIS plan (re-running the validator, browser-boot and audit-coins) and present it again — do not close the plan on an unapproved shape, and do not defer the change to a later phase.
  </action>
  <what-built>
    Level-08's end climb has been rebuilt as a switchback — it reverses direction twice and summits on a wide balcony — so it is no longer a near-duplicate of level-07's monotonic up-and-right staircase (LVL-02). Coins, per-tier checkpoints, the secret alcove, the goal and the camera bounds all moved with it. The validator is green on level-08 with zero HARD-FAILs, every level-08 coin is collected by a real driven player in the real engine, and the door/enemy still trigger. Level-07 was not touched. Math density is untouched (1 door + 1 enemy + the end gate).
  </what-built>
  <how-to-verify>
    1. Look at the three climb screenshots under `.planning/phases/34-level-quality-pass/` (paths listed in the checkpoint message) side-by-side with the level-07 tail coordinates in this plan's objective — the shapes should read as different buildings, not the same staircase repainted.
    2. Play it. From the repo root: `python3 -m http.server 8000`, then open `http://localhost:8000/src/index.html?debug=1` (or `http://192.168.10.113:8000/src/index.html?debug=1` from another device), pick level 8, and climb it.
    3. Check specifically:
       - Does the turnaround read clearly, or is it confusing about where to go next?
       - Does every tier have a checkpoint, so a fall never costs more than one tier?
       - Are the climb coins on the path a real player takes, or floating somewhere you have to fight for?
       - Does the summit balcony feel like an arrival?
       - Does the camera follow the whole climb without clamping short of the goal?
    4. Then play level 7's climb right after, and say whether the two now feel like different experiences.
  </how-to-verify>
  <resume-signal>Type "approved" (with the verbatim quote to record) to land the climb as-is, or describe what to change (tier shape, turnaround placement, coin placement, checkpoints, summit) and it will be re-authored and re-validated inside this plan before it closes.</resume-signal>
  <verify>
    <human-check>User confirms, in their own words: level-08's climb reads as a genuinely different experience from level-07's staircase; the turnaround is legible in play; every tier has a checkpoint so a fall costs at most one tier; the climb coins sit on the path a real player takes; the summit reads as an arrival; and the camera follows the whole climb without clamping short of the goal. Record the verbatim answer, including any hesitation, in this plan's SUMMARY.md.</human-check>
  </verify>
  <done>The user has played level-08's new climb and given a genuine, quoted approval of its shape (or the descriptor has been re-authored until they did) — never auto-approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure level-data edit. No new input surface, no dependency, no runtime code change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-12 | Denial of service (unfinishable level) | the switchback climb geometry | critical | mitigate | A too-narrow tier overlap makes a hop unreachable to the BFS graph (25-03 shipped exactly this class on 07/08) and a real turnaround can trap a player. Mitigated by the ~70px-overlap rule, a zero-HARD-FAIL validator run, a real driven browser-boot pass, and a blocking human playthrough. |
| T-34-13 | Tampering (camera clamps short of the goal) | level-08's explicit `bounds.right` | high | mitigate | The documented bounds-convention trap: level-02+ use `bounds` AS-IS. Task 1 requires computing the rightmost edge and hand-bumping `bounds.right`; the human checkpoint explicitly asks the user to confirm the camera follows the climb to the goal. |
| T-34-14 | Elevation (scope creep into locked decisions) | math density / floors / level-07 | high | mitigate | Density is locked at 1 door + 1 enemy + end gate for Phases 34-38 and may not move; the floors array and level-07 are out of bounds. All three are gated automatically (density assertion, floors-unchanged assertion, level-07 not in files_modified). |
| T-34-15 | Repudiation (a rubber-stamped sign-off) | the human checkpoint | high | mitigate | Standing project precedent (Phases 25/27/28/31/33): a `checkpoint:human-verify` is never auto-approved, regardless of any auto-advance setting. `autonomous: false` on this plan; the checkpoint hands the user real screenshots plus a real playable URL, not a summary to nod at. |
</threat_model>

<verification>
1. `node scripts/validate-levels.mjs` reports zero HARD-FAIL rows for level-08 across every check (spawn-goal, gap-width, mechanic-reachability, secret-alcove, coin).
2. Floors, math density (1 door + 1 enemy + 0 mathGates), coin count (18) and the single secret alcove are all provably unchanged in count.
3. At least one consecutive tier pair moves net-LEFT — the climb genuinely reverses, it is not a restyled staircase.
4. `node scripts/browser-boot.mjs`, `node scripts/audit-coins.mjs`, `node scripts/audit-phase21-mechanics.mjs` all green on level-08.
5. The user has played the climb and given a genuine sign-off (never auto-approved).
</verification>

<success_criteria>
- Level-07 and level-08's end climbs are visibly and mechanically different experiences (LVL-02).
- Level-08's climb is reachable, checkpointed per tier, coin-complete and camera-correct — proven in the engine, not just in the graph.
- The user has seen the shape and approved it.
</success_criteria>

<output>
Create `.planning/phases/34-level-quality-pass/34-04-SUMMARY.md` when done
</output>
