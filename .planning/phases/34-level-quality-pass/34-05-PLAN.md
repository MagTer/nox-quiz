---
phase: 34-level-quality-pass
plan: 05
type: execute
wave: 4
depends_on: ["34-03"]
files_modified:
  - src/levels/level-01.js
  - src/levels/level-02.js
  - src/levels/level-03.js
  - scripts/smoke-progress.mjs
autonomous: true
requirements: [LVL-01]

must_haves:
  truths:
    - "Every coin in levels 01, 02 and 03 is reachable — proven by the static coin model AND collected by a real driven player in the real engine."
    - "The kid-validated platforming feel is byte-identical: floors and platforms in all three levels are unchanged, not one number."
    - "This plan lands as its own commit, so the kid-validated diff stays auditable in isolation (34-CONTEXT.md decision 1)."
  artifacts:
    - "src/levels/level-01.js, level-02.js, level-03.js: repositioned coins arrays"
    - "scripts/smoke-progress.mjs: re-baselined level-01/02/03 golden coins fixtures"
  key_links:
    - "smoke-progress.mjs deep-equals the FULL geometry of levels 01-04 (coins included) against golden literals — three fixtures must be re-baselined here, and level-04's must NOT be touched (Plan 34-03 owns it)."
    - "This plan is in Wave 4, not Wave 3, purely because it shares scripts/smoke-progress.mjs with Plan 34-03 — the level descriptors themselves are disjoint."
---

<objective>
Fix the unreachable coins in the three kid-validated levels — the ones she has actually played — by moving COINS ONLY, with zero geometry edits, in a separate, auditable commit.

Levels 1-3 are nominally byte-frozen, but Phase 34 IS the sanctioned geometry-fix window, and an unreachable coin in level-01 is among the first things she sees (34-CONTEXT.md decision 1). The freeze that matters — the platforming feel — is preserved absolutely: `floors` and `platforms` are not touched.

Purpose: LVL-01, on the levels where it matters most.
Output: three level descriptors whose coins all pass the coin model AND get collected in-engine, with their golden fixtures honestly re-baselined, in one isolated commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-level-quality-pass/34-CONTEXT.md
@.planning/phases/34-level-quality-pass/34-01-SUMMARY.md
@.planning/phases/34-level-quality-pass/34-02-SUMMARY.md
@.planning/phases/34-level-quality-pass/34-03-SUMMARY.md
@docs/LEVEL-DESIGN.md
</context>

<artifacts_produced>
- `src/levels/level-01.js`, `src/levels/level-02.js`, `src/levels/level-03.js`: coins repositioned onto reachable arcs
- `scripts/smoke-progress.mjs`: levels 01/02/03 golden-geometry coins fixtures re-baselined
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Reposition the unreachable coins in levels 01-03 (coins only, zero geometry edits)</name>
  <files>src/levels/level-01.js, src/levels/level-02.js, src/levels/level-03.js</files>
  <read_first>
    Run `node scripts/validate-levels.mjs 2>/dev/null | grep 'coin-reachability | HARD-FAIL'` and use ITS live output as the work list — never a list copied out of a SUMMARY, and never 34-CONTEXT.md's 32-coin table (that came from the secret-alcove POINT model and over-reports).
    src/levels/level-01.js (whole file — especially the coin-coordinate convention comment at lines 61-68: {x, y} is the coin's TOP-LEFT corner, the 32x32 sprite's visual CENTER sits ~16px right/down of it, coins are deliberately off-grid authored visual positions, NOT grid coordinates)
    src/levels/level-02.js, src/levels/level-03.js (whole files — geometry, coins, and their inline authoring comments)
    .planning/phases/34-level-quality-pass/34-03-SUMMARY.md (how the same job was done for levels 04-07 — reuse its approach)
    docs/LEVEL-DESIGN.md sections 1-3 (physics envelope: maxRise 88.331px; ~162px flat reach)
  </read_first>
  <action>
    Move each coin the validator HARD-FAILs in levels 01/02/03. Edit ONLY the `coins` array in each file. Do NOT change a single number in `floors` or `platforms` — this is 34-CONTEXT.md decision 2 and it is what keeps the kid-validated platforming feel byte-identical. Do not touch spikes, checkpoints, doors, enemies, the goal, the secret alcove, or bounds either.

    Reposition each offending coin onto a place the player actually goes: walking height over a surface is roughly `surfaceY - 56` (which is exactly why the shipped floor coins sit at `y: 264` above FLOOR_Y 320); an arc coin must sit within the calibrated maxRise (88.331px) of the highest reachable surface beneath it, or no jump touches it at any x.

    Preserve the level's visual intent — coins arc over jumps and string along runs. Do not delete a coin to make the gate happy: the counts (16 / 15 / 17 for levels 01 / 02 / 03) must not change. Update the inline comment beside any coin you move with a one-line reason.

    Levels 01-03 are also the levels whose authoring comments explicitly document the extension convention ("append new sections after existing geometry — never edit inside it"). That convention is about GEOMETRY. This plan does not extend or edit geometry at all; it repositions pickups within the existing geometry, which is the sanctioned Phase-34 fix window.

    Iterate against `node scripts/validate-levels.mjs` until levels 01/02/03 report zero `coin-reachability | HARD-FAIL` rows. With Plans 34-03 and 34-04 already landed, the WHOLE validator run should now go green (zero HARD-FAILs across all 8 levels) — if it does not, say exactly what is still red in the SUMMARY rather than papering over it.
  </action>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs 2>/dev/null | grep 'coin-reachability | HARD-FAIL' | grep -cE 'level-0[123]'` returns 0.
    - Zero geometry edits: `git diff -U0 -- src/levels/level-01.js src/levels/level-02.js src/levels/level-03.js | grep -E '^[+-][^+-]' | grep -c 'w:'` returns 0 (floors and platforms are the only arrays carrying a width key, so no changed line may contain one). <!-- planner-discipline-allow: w: -->
    - Coin counts unchanged: `node -e 'Promise.all([1,2,3].map(n=>import("./src/levels/level-0"+n+".js"))).then(ms=>{const c=ms.map(m=>Object.values(m)[0].geometry.coins.length); const ok=JSON.stringify(c)==="[16,15,17]"; console.log(ok?"COUNTS-OK":"COUNTS-CHANGED "+c); process.exit(ok?0:1);})'` prints `COUNTS-OK`.
    - `node scripts/validate-levels.mjs` now exits 0 — zero HARD-FAILs across all 8 levels (ROADMAP success criterion 4).
    - `bash scripts/check-safety.sh && bash scripts/check-import-safety.sh` green.
  </acceptance_criteria>
  <verify>
    <automated>test "$(git diff -U0 -- src/levels/level-01.js src/levels/level-02.js src/levels/level-03.js | grep -E '^[+-][^+-]' | grep -c 'w:')" = "0" && node scripts/validate-levels.mjs && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh</automated>
  </verify>
  <done>Levels 01-03 have zero unreachable coins, their floors/platforms are byte-identical, and the structural validator is green with zero HARD-FAILs across all 8 levels.</done>
</task>

<task type="auto">
  <name>Task 2: Re-baseline smoke-progress.mjs's level-01/02/03 golden fixtures</name>
  <files>scripts/smoke-progress.mjs</files>
  <read_first>
    scripts/smoke-progress.mjs lines 300-400 (the recursive deepEqual, the golden-geometry block structure, and the historical-values comment convention above each block), plus the level-02 and level-03 golden blocks (coins arrays around lines 441 and 544, assertions around lines 493 and 606)
    .planning/phases/34-level-quality-pass/34-03-SUMMARY.md (how level-04's fixture was re-baselined — follow the same convention)
  </read_first>
  <action>
    Update the `expectedGeometry.coins` arrays in the level-01, level-02 and level-03 golden blocks to the new post-move values, lifted exactly from the descriptors. Follow the file's own re-baseline convention (as used at the Phase 24 re-baseline and Phase 29's density change): retain the OLD coin values in each block's header comment for historical traceability, and note the reason — Phase 34 LVL-01 coin-reachability fix, coins moved onto reachable arcs, zero geometry change.

    Do NOT touch level-04's golden block (Plan 34-03 already re-baselined it, in its own commit). Do NOT touch any floors/platforms/spikes/goal/checkpoints/doors/enemies golden value in any block: if one of those needs changing, Task 1 broke its own zero-geometry-edit rule and must be corrected instead.
  </action>
  <acceptance_criteria>
    - `bash scripts/check-progress.sh` exits 0.
    - `git diff -U0 -- scripts/smoke-progress.mjs | grep -E '^[+-][^+-]' | grep -c 'w:'` returns 0 — no floor/platform golden value changed. <!-- planner-discipline-allow: w: -->
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-progress.sh && test "$(git diff -U0 -- scripts/smoke-progress.mjs | grep -E '^[+-][^+-]' | grep -c 'w:')" = "0"</automated>
  </verify>
  <done>check-progress.sh is green with the new levels 01-03 coins, and the golden fixtures' only change is their coins arrays.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the kid-validated levels' coins are collectable IN-ENGINE</name>
  <files>(none — verification-only task)</files>
  <read_first>
    .planning/phases/34-level-quality-pass/34-02-SUMMARY.md (what audit-coins.mjs proves and what it deliberately does not)
  </read_first>
  <action>
    These are the levels she actually plays. Static green is not enough — the full suite was green while the game shipped unreachable coins.

    1. `node scripts/audit-coins.mjs` — every coin across ALL 8 levels now carries a witness (zero skipped) and every one is collected in the real running engine. This is the first point in the phase where that is true globally; state it explicitly in the SUMMARY with the per-level numbers.
    2. `node scripts/browser-boot.mjs` — all 8 levels boot and drive, FPS/screenshot/object-budget assertions green.
    3. `node scripts/audit-phase21-mechanics.mjs` — every door/enemy encounter across all 8 levels still `triggered: true` (`resolved: false` rows are known headless-timing flakiness).
  </action>
  <acceptance_criteria>
    - `node scripts/audit-coins.mjs` exits 0, reports zero skipped (model HARD-FAIL) coins across all 8 levels, and zero uncollected coins.
    - `node scripts/browser-boot.mjs` exits 0.
    - `node scripts/audit-phase21-mechanics.mjs` shows `triggered: true` for every door/enemy encounter across all 8 levels.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs</automated>
  </verify>
  <done>All 132 coins across all 8 levels are reachable in the model AND collected by a real driven player in the real engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure level-data edit. No new input, no dependency, no runtime code change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-16 | Tampering (of kid-validated game feel) | floors/platforms of levels 01-03 | critical | mitigate | These three levels have been played and validated by the kid. Any geometry edit here silently changes the feel she signed off on. Forbidden by 34-CONTEXT.md decision 2 and double-gated: no changed line may carry a width key, AND smoke-progress.mjs's golden fixtures still deep-equal the original floors/platforms literals. |
| T-34-17 | Repudiation (an unauditable diff) | commit isolation | medium | mitigate | 34-CONTEXT.md decision 1 requires levels 1-3's fixes in their own commit so the kid-validated diff stays reviewable in isolation. This plan is the only one touching level-01/02/03, so its commit IS that isolated diff. |
| T-34-18 | Tampering (silent scope loss) | the coins arrays | medium | mitigate | Deleting a coin would "fix" reachability by removing content. Gated: per-level coin counts (16/15/17) asserted unchanged. |
| T-34-19 | Spoofing (a green gate that lies) | validate-levels.mjs green | high | mitigate | Task 3 requires audit-coins.mjs to collect every coin across all 8 levels in the real engine, plus browser-boot and the mechanic audit. |
</threat_model>

<verification>
1. Zero `coin-reachability | HARD-FAIL` rows for levels 01/02/03, and `node scripts/validate-levels.mjs` exits 0 — zero HARD-FAILs across all 8 levels (ROADMAP success criterion 4).
2. No changed line in level-01/02/03 carries a width key — floors and platforms byte-frozen.
3. Coin counts per level unchanged (16/15/17).
4. `bash scripts/check-progress.sh` green with the re-baselined golden fixtures; level-04's block untouched.
5. `node scripts/audit-coins.mjs` collects all 132 coins in the real engine; browser-boot and the mechanic audit green.
</verification>

<success_criteria>
- Every pickup in every level is actually reachable in play — proven statically and in-engine.
- The kid-validated levels' platforming geometry is byte-identical, and their fix lands as one isolated, auditable commit.
- The structural validator is green with zero HARD-FAILs across all 8 levels.
</success_criteria>

<output>
Create `.planning/phases/34-level-quality-pass/34-05-SUMMARY.md` when done
</output>
