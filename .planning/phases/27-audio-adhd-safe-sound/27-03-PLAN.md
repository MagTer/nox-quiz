---
phase: 27-audio-adhd-safe-sound
plan: 03
type: execute
wave: 2
depends_on: ["27-01", "27-02"]
files_modified:
  - src/main.js
  - src/player.js
  - src/ui/challenge.js
autonomous: true
requirements: [AUD-01, AUD-02]
must_haves:
  truths:
    - "Jump and land each play an audibly distinct SFX at the existing player-feel hook points"
    - "Correct and wrong answers each play an audibly distinct SFX at the ONE shared challenge-resolution seam, covering every mechanic (door/gates/mathGate/collect) that routes through it"
    - "All 7 SFX + the ambient music track are registered via loadSound()/loadMusic() at boot, before any go() call"
  artifacts:
    - "src/main.js: 7 loadSound() calls + 1 loadMusic() call"
    - "src/player.js: audio.playSfx(\"jump\") on press, audio.playSfx(\"land\") on player.onGround()"
    - "src/ui/challenge.js: audio.playSfx(\"correct\") / audio.playSfx(\"wrong\") in choose()"
  key_links:
    - "src/ui/challenge.js's choose() is the ONE seam all mechanics (door/gates/mathGate/collect) route through for correct/wrong SFX — never duplicated per-mechanic"
---

<objective>
Wire the boot-time asset registration and the two highest-traffic SFX seams: the player's jump/land feel, and the single shared correct/wrong resolution point every mechanic in the game already funnels through.

Purpose: AUD-01 requires jump/land/correct/wrong to be distinct, CC0 sounds wired at their existing mechanic seams — not duplicated per caller. This plan proves the two lowest-risk, highest-confidence seams (RESEARCH.md rates both HIGH confidence, exact line numbers already confirmed against the live tree). AUD-02 requires the ambient track registered (not played) at boot, ready for Plan 27-05's gesture-gated start.
Output: main.js registers all 8 audio assets; player.js plays jump+land; challenge.js plays correct+wrong for every mechanic that opens a challenge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-audio-adhd-safe-sound/27-RESEARCH.md
@.planning/phases/27-audio-adhd-safe-sound/27-PATTERNS.md
@src/main.js
@src/player.js
@src/ui/challenge.js
@src/audio.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register all audio assets in main.js</name>
  <files>src/main.js</files>
  <read_first>src/main.js (full file — the existing loadSprite() block at lines 62-117 is the exact convention to mirror: `../assets/...` web-root paths, called after kaplay() init, before any go())</read_first>
  <action>
Add loadSound() calls for the 7 SFX and one loadMusic() call for the ambient track to src/main.js, placed alongside the existing loadSprite() calls (after the door/enemy sprite loads around line 117, before the scene registration block at line 126) — same "queue loads before any go()" convention already used for every sprite.

Register exactly these 7 named sounds via loadSound(name, url), using the `../assets/sfx/...` path convention (mirrors the existing `../assets/...` pattern for sprites): loadSound("jump", "../assets/sfx/jump.ogg"), loadSound("land", "../assets/sfx/land.ogg"), loadSound("correct", "../assets/sfx/correct.ogg"), loadSound("wrong", "../assets/sfx/wrong.ogg"), loadSound("door", "../assets/sfx/door.ogg"), loadSound("clear", "../assets/sfx/clear.ogg"), loadSound("pickup", "../assets/sfx/pickup.ogg").

Register the ambient track via loadMusic("ambient", "../assets/music/ambient.ogg") — per 27-RESEARCH.md, loadMusic() only prefetches (constructs a throwaway `new Audio(url)` with preload="auto" and stores the URL string; it never calls .play()), so this is safe to call here at boot, before any user gesture, exactly like every SFX load above.

These are bare Kaplay engine-global calls at main.js's post-kaplay()-init module scope — main.js is explicitly EXCLUDED from check-import-safety.sh's negative a727c13 scan (confirmed in that script's own comments), so no import-safety change is needed for this file.
  </action>
  <acceptance_criteria>
    - main.js contains exactly 7 loadSound() calls for jump/land/correct/wrong/door/clear/pickup and exactly 1 loadMusic() call for ambient
    - Every path uses the ../assets/sfx/ or ../assets/music/ convention, matching the existing loadSprite() path style
    - node --check src/main.js passes
  </acceptance_criteria>
  <verify>
    <automated>node --check src/main.js && for s in jump land correct wrong door clear pickup; do grep -q "loadSound(\"$s\"" src/main.js || { echo "MISSING loadSound: $s"; exit 1; }; done && grep -q 'loadMusic("ambient"' src/main.js && echo "main.js audio registration OK"</automated>
  </verify>
  <done>All 7 SFX + the ambient track are registered in main.js via loadSound()/loadMusic() before any go() call, and main.js still parses.</done>
</task>

<task type="auto">
  <name>Task 2: Wire jump + land SFX in player.js</name>
  <files>src/player.js</files>
  <read_first>src/player.js (full file — the JUMP_KEYS onKeyPress handler at lines 84-87 and the player.onGround() landing-juice callback at lines 41-44 are the two exact insertion points)</read_first>
  <action>
Add `import * as audio from "./audio.js";` to src/player.js's import block (alongside the existing `import * as fx from "./fx.js";` at line 20 — same sibling-module shape).

Insert `audio.playSfx("jump");` as the first statement inside the existing `onKeyPress(JUMP_KEYS, () => { if (player.paused) return; ... })` body (lines 84-87), placed AFTER the `if (player.paused) return;` guard (so a jump press while frozen — e.g. mid-challenge — does not play a phantom jump sound) and before the `buffer = CONFIG.BUFFER_MS / 1000;` line. This plays on PRESS (immediate feedback), matching CONTEXT.md's decision, not deferred to the actual physics jump inside onUpdate.

Insert `audio.playSfx("land");` inside the existing `player.onGround(() => { fx.squash(player); fx.dust(player.pos); });` callback (lines 41-44), alongside the existing fx.squash/fx.dust calls — this fires on every landing, mirroring the existing landing-juice call site exactly.
  </action>
  <acceptance_criteria>
    - src/player.js imports "./audio.js" as `audio`
    - audio.playSfx("jump") is called inside the JUMP_KEYS press handler, after the player.paused guard
    - audio.playSfx("land") is called inside the player.onGround() callback
    - node --check src/player.js passes
    - bash scripts/check-safety.sh and bash scripts/check-import-safety.sh both still pass (player.js is not in either script's scoped negative-scan list, so this is a regression check, not a new gate)
  </acceptance_criteria>
  <verify>
    <automated>node --check src/player.js && grep -q 'import \* as audio from "./audio.js"' src/player.js && grep -q 'audio.playSfx("jump")' src/player.js && grep -q 'audio.playSfx("land")' src/player.js && echo "player.js SFX wiring OK"</automated>
  </verify>
  <done>player.js imports audio.js, plays "jump" on press (after the paused guard) and "land" on every grounded landing, and still parses cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Wire correct/wrong SFX at the shared challenge seam</name>
  <files>src/ui/challenge.js</files>
  <read_first>src/ui/challenge.js (full file — the choose(i) function at lines 276-303 is the ONE seam; the `if (!correct) { ... }` branch at lines 288-294 and the `cleared = true;` branch at lines 296-303 are the two exact insertion points)</read_first>
  <action>
Add `import * as audio from "../audio.js";` to src/ui/challenge.js's import block (alongside the existing `import { createBrain } from "../math/brain.js";` and `import { CONFIG } from "../config.js";` at lines 38-39 — same `../` sibling-import depth, since src/ui/ is one directory below src/).

Inside choose(i)'s `if (!correct) { ... }` branch (lines 288-294), add `audio.playSfx("wrong");` as the FIRST statement in the branch, before the existing `if (box) box.color = ...` line. This is the hard requirement from CONTEXT.md/REQUIREMENTS.md: the "wrong" sound (vendored in Plan 27-01, specifically selected for a soft/neutral character) must never read as a buzzer or startle stinger — do not substitute a different, harsher sound file here.

After `cleared = true;` (line 298) and before `close();` (line 300), add `audio.playSfx("correct");`. Per 27-RESEARCH.md and 27-PATTERNS.md, this SINGLE insertion point is why door.js, gates.js, and mathGate.js do NOT each need their own correct/wrong SFX call — every mechanic (door, gates, mathGate, collect) resolves through this same choose() function via openChallenge(). Do not add a duplicate correct/wrong playSfx call in any mechanic file (Plan 27-04 only adds mechanic-SPECIFIC "unlock"/"celebrate" sounds layered in each mechanic's own onSuccess callback, never a second correct/wrong call).
  </action>
  <acceptance_criteria>
    - src/ui/challenge.js imports "../audio.js" as `audio`
    - audio.playSfx("wrong") is called inside the `if (!correct)` branch, before the box-recolor line
    - audio.playSfx("correct") is called after `cleared = true;` and before `close();`
    - node --check src/ui/challenge.js passes
    - grep confirms exactly one playSfx("correct") call site and exactly one playSfx("wrong") call site exist anywhere under src/ (proving no mechanic file duplicates this seam)
  </acceptance_criteria>
  <verify>
    <automated>node --check src/ui/challenge.js && grep -q 'import \* as audio from "../audio.js"' src/ui/challenge.js && grep -q 'audio.playSfx("wrong")' src/ui/challenge.js && grep -q 'audio.playSfx("correct")' src/ui/challenge.js && [ "$(grep -rl 'playSfx("correct")' src | wc -l)" = "1" ] && [ "$(grep -rl 'playSfx("wrong")' src | wc -l)" = "1" ] && echo "challenge.js SFX wiring OK, single seam confirmed"</automated>
  </verify>
  <done>challenge.js plays "wrong" (soft/neutral) on every incorrect pick and "correct" on every successful resolution, and this is the ONLY place either SFX is triggered anywhere in src/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| src/main.js loadSound()/loadMusic() call sites | All 8 asset paths are hardcoded string literals identical in shape to the existing loadSprite() calls — no path is ever constructed from user input, localStorage, or a level descriptor |
| src/ui/challenge.js's choose() -> audio.playSfx() | The `name` argument passed at every call site in this plan is a hardcoded literal ("jump"/"land"/"correct"/"wrong") — never derived from the answer the player picked or any other runtime/user-controlled value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-02 | Tampering (path injection, latent) | src/main.js, src/player.js, src/ui/challenge.js | low | accept | Every asset name/path in this plan is a hardcoded literal, mirroring the existing loadSprite() convention exactly — no dynamic construction from any trust-boundary-crossing input exists in this plan (27-02-PLAN.md's T-27-02 entry covers the underlying audio.js API surface; this plan's call sites are the first concrete instantiation and confirm the mitigation held) |
| T-27-05 | Denial of Service (very low) | src/player.js's playSfx("jump") on the JUMP_KEYS press handler | low | accept | play() is a fire-and-forget call with no return value consumed synchronously in a loop; a rapid jump-key mash cannot accumulate unbounded state because each SFX instance self-cleans on its buffer's onended (27-RESEARCH.md Don't Hand-Roll table) |

</threat_model>

<verification>
Run `node --check src/main.js src/player.js src/ui/challenge.js` and `bash scripts/check-safety.sh && bash scripts/check-import-safety.sh` (both must still pass — this plan touches no file in either script's negative-scan scope, so this is purely a regression check). Confirm via grep that playSfx("correct") and playSfx("wrong") each appear in exactly one file under src/.
</verification>

<success_criteria>
- main.js registers all 7 SFX + the ambient track before any go() call
- player.js plays "jump" on press (respecting the paused guard) and "land" on every grounded landing
- challenge.js plays "correct"/"wrong" at its one shared resolution point, with zero duplication across mechanic files
- check-safety.sh and check-import-safety.sh remain green
</success_criteria>

<output>
Create `.planning/phases/27-audio-adhd-safe-sound/27-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan's contribution)

- `src/main.js`: `loadSound("jump"|"land"|"correct"|"wrong"|"door"|"clear"|"pickup", ...)` (7 calls) + `loadMusic("ambient", ...)` (1 call)
- `src/player.js`: `audio.playSfx("jump")` in the `JUMP_KEYS` press handler, `audio.playSfx("land")` in `player.onGround()`
- `src/ui/challenge.js`: `audio.playSfx("wrong")` and `audio.playSfx("correct")` inside `choose(i)` — the single shared seam for every mechanic
