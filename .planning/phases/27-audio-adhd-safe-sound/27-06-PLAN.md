---
phase: 27-audio-adhd-safe-sound
plan: 06
type: execute
wave: 3
depends_on: ["27-03", "27-04", "27-05"]
files_modified:
  - scripts/browser-boot.mjs
autonomous: true
requirements: [AUD-04]
must_haves:
  truths:
    - "At every scene-transition stop (title, select, each level, back to select, back to title) at most one <audio> element exists in the page, proving the idempotent music manager holds under real browser conditions"
    - "Pressing M actually silences audio output (master gain reaches 0), not just a UI label change, and pressing it again restores audio output"
    - "The full automated gate suite (check-safety.sh, check-import-safety.sh, check-audio.sh, browser-boot.mjs) passes green in one run"
  artifacts:
    - "scripts/browser-boot.mjs extended with document.querySelectorAll('audio').length <= 1 assertions at each scene-transition stop"
    - "scripts/browser-boot.mjs extended with an M-key mute-toggle assertion via page.evaluate(() => getVolume())"
  key_links:
    - "This is the ONE automated proof of AUD-04's 'no stacking/leaking across scenes' claim under real browser conditions — the audio.js idempotency guard (Plan 27-02) is the code-level mitigation, this plan is the runtime verification of it actually holding"
---

<objective>
Extend the existing real-browser boot check with the two automated assertions AUD-04 needs but no prior script covers: an audio-element-count ceiling at every scene-transition stop, and a functional mute-toggle check — then run the complete phase-closing gate suite and confirm it is green in one pass.

Purpose: this is the Wave 0 gap 27-RESEARCH.md flagged as load-bearing, not optional polish — "checks that don't play the game lie" (CLAUDE.md standing rule) applies especially hard to a stacking-music bug, which is silent in a screenshot and only audible/countable in a live DOM. This plan is also the phase's own closing regression sweep before the human sign-off checkpoint (Plan 27-07).
Output: scripts/browser-boot.mjs drives title->select->game->select->title (extending its existing round-trip loop) with an audio-count assertion at each stop and a mute-toggle assertion; a full-suite run recorded green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-audio-adhd-safe-sound/27-RESEARCH.md
@.planning/phases/27-audio-adhd-safe-sound/27-VALIDATION.md
@scripts/browser-boot.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend browser-boot.mjs with audio-count + mute assertions</name>
  <files>scripts/browser-boot.mjs</files>
  <read_first>scripts/browser-boot.mjs (full file — the title->select gesture at lines 142-144, the per-level loop at lines 149-219, and the existing "Phase 18 round-trip leak check" at lines 221-226 that already drives title->select a second time, which is the exact stop-sequence this task adds assertions to, not a new drive path)</read_first>
  <action>
Add a reusable helper near the top of the script (after the existing MIME/SAVE_BLOB constants, before the server starts) that evaluates `document.querySelectorAll('audio').length` in the page via page.evaluate() and pushes an entry into the existing `errors` array if the count exceeds 1 — mirroring this script's existing pattern of pushing typed objects into `errors` (e.g. the `{ type: "mechanic", message: ... }` shape already used at lines 194-198/206-209) rather than throwing directly, so one failed audio-count check does not abort the rest of the drive.

Call this audio-count check at these existing stops in the drive (all already present as waitForTimeout pauses after a scene transition — insert the check into each, do not add new navigation): (1) right after the title->select transition (after the existing `await page.waitForTimeout(800);` at line 144), (2) right after loading each level inside the per-level loop (after `await page.waitForTimeout(1500);` at line 168, before the mechanic-drive logic), (3) right after the escape-back-to-select transition inside the loop (after the existing `await page.waitForTimeout(800);` at line 217), and (4) at the final round-trip re-entry into select (after the existing `await page.waitForTimeout(800);` at line 226).

Add ONE mute-toggle functional assertion, placed once after the title->select transition (stop 1 above): press "m" via `await page.keyboard.press("m");`, wait a short beat, then evaluate `getVolume()` in the page (this Kaplay global is safe to call from page.evaluate() since it runs after kaplay() has initialized) and push an error if it is not 0; press "m" again, wait a short beat, and push an error if getVolume() is not back to 1. This proves the M key actually reaches the master-gain call end-to-end (audio.js's toggleMute -> setVolume), not just that a key handler exists.

Keep the existing final `if (errors.length > 0) { ... } else { console.log("Browser boot: PASS...") }` block unchanged in shape — the new assertions simply add more entries to the same `errors` array this block already reports.
  </action>
  <acceptance_criteria>
    - An audio-count helper exists and is called at all 4 scene-transition stops listed above
    - A mute-toggle assertion exists that presses "m" twice and checks getVolume() reaches 0 then returns to 1
    - node --check scripts/browser-boot.mjs passes
    - The script's existing per-level mechanic-drive logic and final PASS/FAIL reporting block are unchanged in shape (only new error-producing checks are added, no existing check is removed or altered)
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/browser-boot.mjs && grep -q "querySelectorAll('audio')" scripts/browser-boot.mjs && grep -q 'getVolume()' scripts/browser-boot.mjs && grep -q 'page.keyboard.press("m")' scripts/browser-boot.mjs && echo "browser-boot.mjs audio assertions added"</automated>
  </verify>
  <done>browser-boot.mjs asserts at most one audio element exists at 4 scene-transition stops and functionally proves the M key reaches the master gain, without altering its existing mechanic-drive or reporting logic.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full phase-closing gate suite and confirm green</name>
  <files>(none — verification only)</files>
  <read_first>scripts/check-audio.sh (created in Plan 27-01, now expected to be fully green for the first time since CONFIG.AUDIO and all wiring now exist), scripts/check-import-safety.sh, scripts/check-safety.sh</read_first>
  <action>
Run the complete gate suite in sequence and confirm every command exits 0: bash scripts/check-safety.sh, bash scripts/check-import-safety.sh, bash scripts/check-audio.sh, node scripts/browser-boot.mjs. This is the first point in the phase where check-audio.sh's Plan-27-01-authored assertions (asset existence, license proofs, CREDITS rows, and the CONFIG.AUDIO.MUTE_STORAGE_KEY-distinct-from-SAVE.KEY check) can legitimately turn green, since CONFIG.AUDIO now exists (Plan 27-02) and every wiring plan (27-03/27-04/27-05) has landed. If any command fails, fix the specific regression it names (do not silence or skip the assertion) and re-run the full sequence from the top before marking this task done — per CLAUDE.md's "no phase closes on greps/automation alone" standard, this task's OWN pass does not substitute for the human sign-off in Plan 27-07, but it is the gate that must be clean before that checkpoint is meaningful.
  </action>
  <acceptance_criteria>
    - bash scripts/check-safety.sh exits 0
    - bash scripts/check-import-safety.sh exits 0
    - bash scripts/check-audio.sh exits 0 (first time this script has run to green in the phase)
    - node scripts/browser-boot.mjs exits 0, including the new audio-count and mute-toggle assertions from Task 1
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-audio.sh && node scripts/browser-boot.mjs && echo "FULL SUITE GREEN"</automated>
  </verify>
  <done>All 4 gate commands exit 0 in one sequential run, with the new audio-specific assertions included.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Playwright-driven headless browser -> live game DOM | The audio-count assertion reads document.querySelectorAll('audio') from inside the actual rendered page, not a mock — this is the closest this project's test harness gets to observing the real HTMLAudioElement the music path creates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-09 | Repudiation (an unverified AUD-04 claim shipping without proof) | scripts/browser-boot.mjs | high | mitigate | This plan is itself the mitigation — it converts AUD-04's "no stacking/leaking" claim from an unverified code-review assertion into an automated, repeatable, real-browser proof that runs on every future check-import-safety-adjacent regression pass, not just once during this phase |
| T-27-10 | Denial of Service (a genuinely broken mute/stacking bug shipping silently) | scripts/browser-boot.mjs | medium | mitigate | Task 1's assertions push into the SAME `errors` array the script already uses to fail the whole run non-zero — a real regression here fails CI-equivalent local verification, not just a human's manual listen-through |

</threat_model>

<verification>
Task 2's own action IS the phase-level verification step — run the full 4-command gate sequence and confirm all exit 0.
</verification>

<success_criteria>
- scripts/browser-boot.mjs proves at most one audio element exists across 4 real scene-transition stops
- scripts/browser-boot.mjs proves the M key reaches the master gain (functional, not just structural, mute check)
- The complete gate suite (check-safety.sh, check-import-safety.sh, check-audio.sh, browser-boot.mjs) is green in one run
</success_criteria>

<output>
Create `.planning/phases/27-audio-adhd-safe-sound/27-06-SUMMARY.md` when done. Record the full-suite pass confirmation (command + exit code) as the evidence this plan's SUMMARY should carry forward into Plan 27-07's checkpoint.
</output>

## Artifacts this phase produces (this plan's contribution)

- `scripts/browser-boot.mjs` extended: an audio-count helper asserting `document.querySelectorAll('audio').length <= 1` at 4 scene-transition stops, plus a functional M-key mute-toggle assertion via `getVolume()`
- A recorded full-suite green run (check-safety.sh + check-import-safety.sh + check-audio.sh + browser-boot.mjs)
