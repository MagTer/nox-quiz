---
phase: 15-challenge-seam-locked-door-mechanic
plan: 04
type: execute
wave: 3
depends_on: ["15-01", "15-03"]
files_modified:
  - src/scenes/game.js
autonomous: false
requirements: [MECH-01, MECH-02]
must_haves:
  truths:
    - "scenes/game.js calls wireDoor({player, brain}) once, after both the player and the brain exist, wiring every door-tagged entity buildLevel(level) created"
    - "The full static suite (check-gate.sh + check-import-safety.sh + check-safety.sh + smoke-progress.mjs) is green — the first time in this phase all four are simultaneously satisfiable"
    - "In a real browser: the door blocks the player until answered, opens on a correct answer, the path is clear afterward, and the player can still move/jump/fall normally post-open (no soft-lock)"
    - "In a real browser: a wrong answer at the door never consumes progress, never locks her out, and never sends her back — the same question re-asks"
    - "In a real browser: the end-of-level math gate still behaves identically after the extraction (same completion banner, same XP award, same return-to-select) — the MECH-01 no-drift proof"
    - "In a real browser: the overlay pauses the world (no move/fall/hurt) and renders above the nearby hazard when opened next to it (success criterion #3)"
  artifacts:
    - path: "src/scenes/game.js"
      provides: "door mechanic wiring call site (MECH-02 integration) — the ONLY change to this file in the whole phase"
      contains: "wireDoor("
  key_links:
    - from: "src/scenes/game.js"
      to: "src/mechanics/door.js"
      via: "imports wireDoor, calls it once after player+brain both exist"
      pattern: "wireDoor\\("
    - from: "real browser boot"
      to: "MECH-01/MECH-02 no-drift + no-soft-lock proof"
      via: "blocked->answer->open->path-clear->still-movable, plus a byte-identical end-gate replay"
      pattern: "manual"
---

<objective>
Wire `wireDoor({player, brain})` into `src/scenes/game.js` — the single line this whole
phase has been building toward — then run the full static gate suite (now green for the
first time: `check-gate.sh` + `check-import-safety.sh` + `check-safety.sh` +
`smoke-progress.mjs`) and the mandatory real-browser boot that proves both halves of this
phase together: the end-of-level gate still behaves byte-identically after the
`challenge.js` extraction (MECH-01), and the locked door blocks/opens/clears the path
without ever soft-locking the player or punishing a wrong answer (MECH-02).

This is the ONLY task in the entire phase that touches `src/scenes/game.js`, and it is a
single additive call — everything else (the extraction, the door entity, the mechanic)
was built and statically verified in 15-01/15-02/15-03 without this file changing at all.

Purpose: complete the integration and prove it live — greps are not enough (STATE.md
cross-cutting mitigation 2: this is the single most expensive v3.0 lesson).
Output: `src/scenes/game.js` (one new import + one new call); a human-verified pass (or a
defect list routed back to 15-02/15-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-challenge-seam-locked-door-mechanic/15-CONTEXT.md
@.planning/phases/15-challenge-seam-locked-door-mechanic/15-RESEARCH.md
@.planning/phases/15-challenge-seam-locked-door-mechanic/15-VALIDATION.md

# The edit target — the existing goal-collision wiring (player.onCollide("goal", ...),
# line 223) is the insertion anchor; player/brain both already exist by line 98/81:
@src/scenes/game.js

# The exports this wiring call consumes (both created in prior waves of this phase):
@src/mechanics/door.js
@src/levels/build.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire wireDoor into game.js + run the full static suite green</name>
  <files>src/scenes/game.js</files>
  <read_first>
    - src/scenes/game.js lines 20-29 (the existing import block — where the new
      `wireDoor` import is added), lines 96-98 (player creation — `wireDoor` must be
      called AFTER this), lines 156-223 (`onReachGoal()` + its
      `player.onCollide("goal", onReachGoal);` wiring at line 223 — the insertion anchor)
    - src/mechanics/door.js (confirm the exact exported signature: `wireDoor({player, brain})`)
    - .planning/phases/15-challenge-seam-locked-door-mechanic/15-VALIDATION.md "Full suite
      command" (the four-script sequence this task's verify runs end-to-end for the first
      time this phase)
  </read_first>
  <action>
    Two surgical edits to `src/scenes/game.js`:

    1. Add one import alongside the existing sibling-module imports (near the
       `import { openMathGate } from "../ui/mathGate.js";` line):
       `import { wireDoor } from "../mechanics/door.js";`.
    2. Add one call, placed right after the existing
       `player.onCollide("goal", onReachGoal);` line (after both `player` and `brain`
       already exist in this closure): `wireDoor({ player, brain });`. Precede it with a
       short comment identifying this as the Phase 15 MECH-02 wiring — the SAME
       closure-local `brain` instance the end-of-level gate above already uses, registered
       inside the scene body (never module top level — a727c13).

    Make NO other change to this file. Do not touch `onReachGoal()`, the checkpoint/coin/
    spike wiring, the Escape controller, the `onUpdate` loop, or the `onSceneLeave` sweeps
    — none of them need to change for this integration.

    After the edit, run the full static suite in order and confirm every stage prints its
    own PASS: `bash scripts/check-gate.sh` (now green for the first time — challenge.js,
    mathGate.js, and door.js all exist and are all thin/well-formed callers),
    `bash scripts/check-import-safety.sh` (now green — challenge.js and door.js both exist
    and are a727c13-clean), `bash scripts/check-safety.sh` (whole-tree no-timer/forgiving,
    unaffected by this wave but re-confirmed), and `node scripts/smoke-progress.mjs` (the
    LVL-02 regression with the doors field, still green). If ANY stage is red, STOP and
    report exactly which assertion failed — do NOT proceed to Task 2's browser boot on a
    red static gate (the browser boot is the runtime backstop, not a substitute for the
    static one).
  </action>
  <acceptance_criteria>
    - `node --check src/scenes/game.js` passes.
    - `grep -q 'import { wireDoor } from "../mechanics/door.js"' src/scenes/game.js`.
    - `grep -q 'wireDoor({ player, brain })' src/scenes/game.js`.
    - The existing goal wiring is untouched: `grep -q 'player.onCollide("goal", onReachGoal)' src/scenes/game.js`.
    - `bash scripts/check-gate.sh` exits `0` and prints `"gate checks: PASS"`.
    - `bash scripts/check-import-safety.sh` exits `0` and prints `"import-safety checks: PASS"`.
    - `bash scripts/check-safety.sh` exits `0` and prints `"safety checks: PASS"`.
    - `node scripts/smoke-progress.mjs` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/scenes/game.js && grep -q 'import { wireDoor } from "../mechanics/door.js"' src/scenes/game.js && grep -q 'wireDoor({ player, brain })' src/scenes/game.js && grep -q 'player.onCollide("goal", onReachGoal)' src/scenes/game.js && bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/smoke-progress.mjs && echo OK</automated>
  </verify>
  <done>src/scenes/game.js imports and calls wireDoor({player, brain}) once, right after the existing goal wiring, with no other change to the file; the FULL static suite (check-gate.sh, check-import-safety.sh, check-safety.sh, smoke-progress.mjs) is green for the first time this phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mandatory real browser boot — door blocked/answer/open/path-clear + end-gate no-drift</name>
  <files></files>
  <action>
    Serve the game over HTTP and drive both halves of this phase in a real browser, then
    record a blocking human-verify sign-off. The exact steps, what to look for, and the
    resume signal are in the <how-to-verify>/<resume-signal> blocks below — follow them
    precisely. This is a verification-only checkpoint; write no code. If a defect appears,
    route it back to 15-02 (seam extraction) or 15-03 (door mechanic) with the exact
    symptom and re-verify after the fix.
  </action>
  <what-built>
    The shared `ui/challenge.js` overlay now backs TWO callers: the unchanged end-of-level
    gate (via `mathGate.js`'s thin wrapper) and a new mid-level locked door
    (`mechanics/door.js`) built into level-01's geometry near the x:1520 spike. The static
    gate suite (check-gate.sh, check-import-safety.sh, check-safety.sh, smoke-progress.mjs)
    is fully green. This checkpoint is the irreplaceable runtime half — the highest-risk
    defect this phase can produce (a permanent soft-lock after the door opens, or silent
    behavior drift on the pre-existing end gate) is invisible to every static gate and can
    only be observed live.
  </what-built>
  <how-to-verify>
    1. Serve the game over HTTP from the repo root (file:// is blocked for assets), e.g.:
       `python3 -m http.server 8000` then open `http://localhost:8000/` in a real browser.
    2. MECH-01 — end-of-level gate, no drift: play level-01 to the goal exactly as before
       this phase. Confirm the math gate opens, a wrong answer re-asks the SAME question
       with a red tint + shake and no penalty, and a correct answer shows the SAME
       completion celebration (dim backdrop + banner) as before, awards XP, and returns to
       level-select — identical to the pre-extraction behavior. This is the core MECH-01
       proof: the extraction changed nothing observable.
    3. MECH-02 — locked door, blocked: on the way to the goal, walk into the new door near
       the second spike (around x:1480-1520 on the final run). Confirm the player is
       physically BLOCKED — cannot walk through it.
    4. MECH-02 — challenge opens, pauses the world, renders above the hazard: confirm the
       challenge overlay opens automatically on touch (no key press needed), the player
       cannot move/jump/fall while it is open, and the overlay renders clearly ABOVE both
       the level and the nearby spike (not hidden behind them).
    5. MECH-02 — wrong answer never punishes: pick a wrong answer at the door. Confirm the
       SAME question stays up, the picked box tints red and shakes briefly, the door stays
       locked, and nothing about her progress, position, or the door itself changes. Try
       this at least twice.
    6. MECH-02 — correct answer opens the door, clears the path, NO SOFT-LOCK: pick the
       correct answer. Confirm the overlay closes, the door visually disappears (both the
       body AND the "X" glyph — no orphaned glyph floating in place), and — critically —
       confirm the player can immediately move LEFT/RIGHT and JUMP again. This is the
       single highest-risk defect in the phase: a visually-open door with a stuck player
       is the signature of a missed `player.paused = false`.
    7. MECH-02 — door stays open, no re-ask on respawn: walk past the (now-open) door to
       the nearby spike and let it respawn you at the checkpoint just before the door.
       Walk back through where the door was. Confirm it does NOT reappear and does NOT
       re-ask the question — the door must stay open for the rest of the level session.
    8. Confirm `bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/smoke-progress.mjs` are all still green alongside the boot.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all of the following hold: the end-of-level gate behaves
    identically to before this phase (MECH-01); the door blocks until answered, opens on
    correct with no soft-lock and no orphaned glyph, and a wrong answer never
    punishes/locks-out/consumes progress (MECH-02); the overlay pauses the world and
    renders above the nearby hazard; the door stays open across a respawn. If anything
    fails, describe the exact symptom (which step, what you observed vs. expected) so it
    can be routed back to 15-02/15-03 and re-verified.
  </resume-signal>
  <verify>
    <human-check>Human confirms in a real browser: end-of-level gate is byte-identical to pre-extraction behavior (MECH-01); the locked door blocks, opens on correct answer with the player fully movable afterward (no soft-lock, no orphaned glyph), never punishes a wrong answer, and stays open across a respawn (MECH-02); the overlay pauses the world and renders above a nearby hazard; the full static suite is green alongside the boot.</human-check>
  </verify>
  <done>Human sign-off recorded that the end-of-level gate behaves identically and the locked door opens cleanly with no soft-lock and no punishment on a wrong answer; or a defect list routed back to 15-02/15-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static gate ↔ runtime boot | A green grep/smoke suite does not prove the player can still move after the door opens or that the end gate truly renders identically — the browser run is the authoritative proof for both. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04-01 | Repudiation | "the static suite is green so MECH-01/MECH-02 hold" (skipping the boot) | high | mitigate | Task 2 is `autonomous:false` — a blocking human-verify makes the real browser boot non-skippable (STATE.md cross-cutting mitigation 2), directly observing the soft-lock/orphaned-glyph/behavior-drift failure modes no static gate can see. |
| T-15-04-02 | Denial of Service | leaked `door.js` collision handler across scene re-entry | low | accept | `wireDoor` registers a single OBJECT-SCOPED `player.onCollide` handler — the same idiom already used for the existing checkpoint/coin/spike/goal handlers, auto-cleaned when the player/scene is torn down on `go()`. No new global app-bus controller is introduced, so no additional `onSceneLeave` cancellation is required this phase. |
| T-15-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | No installs in this plan. |
</threat_model>

<verification>
- Task 1: the full static suite is green (`check-gate.sh && check-import-safety.sh && check-safety.sh && smoke-progress.mjs`), for the first time all four are simultaneously satisfiable this phase.
- Task 2: a human confirms the end-of-level gate is byte-identical and the door mechanic works end-to-end with no soft-lock and no punishment, in a real browser, with the static suite still green alongside the boot.
</verification>

<success_criteria>
- `src/scenes/game.js` wires `wireDoor({player, brain})` in one additive call, with zero other change to the file.
- The full static gate suite (`check-gate.sh`, `check-import-safety.sh`, `check-safety.sh`, `smoke-progress.mjs`) is green.
- A human confirms, live in a browser, that the end-of-level gate is unchanged (MECH-01) and the locked door blocks/opens/clears the path with no soft-lock and no punishment on a wrong answer (MECH-02).
- Human sign-off recorded (or a defect list routed back to 15-02/15-03).
</success_criteria>

<output>
Create `.planning/phases/15-challenge-seam-locked-door-mechanic/15-04-SUMMARY.md` when done.
</output>
