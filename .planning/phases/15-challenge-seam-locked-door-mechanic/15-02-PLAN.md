---
phase: 15-challenge-seam-locked-door-mechanic
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/challenge.js
  - src/ui/mathGate.js
autonomous: true
requirements: [MECH-01]
must_haves:
  truths:
    - "src/ui/challenge.js exports openChallenge({ brain, onSuccess, prompt }) and is a structural lift of mathGate.js's prior body with the tag renamed 'math-gate' -> 'challenge' everywhere it appeared"
    - "A wrong answer in challenge.js re-asks the SAME question with no penalty (forgiving) — the bounds guard and wrong-branch tint+shake are preserved verbatim"
    - "src/ui/mathGate.js is now a thin wrapper: it imports openChallenge from ./challenge.js and its exported openMathGate({ brain, onClear }) signature is UNCHANGED, so src/scenes/game.js's existing call site needs ZERO edits"
    - "The persistent end-of-level completion celebration (tag 'gate-cleared', z 9990/9994) still renders from mathGate.js's wrapper — challenge.js itself carries no end-of-level-specific celebration UI"
  artifacts:
    - path: "src/ui/challenge.js"
      provides: "the shared in-world challenge overlay (MECH-01) — question render, dual-input answer boxes, forgiving retry, fire-once success"
      exports: ["openChallenge"]
      contains: "onSuccess"
    - path: "src/ui/mathGate.js"
      provides: "thin wrapper preserving the openMathGate({ brain, onClear }) contract + the end-of-level celebration banner"
      exports: ["openMathGate"]
      contains: "openChallenge"
  key_links:
    - from: "src/ui/mathGate.js"
      to: "src/ui/challenge.js"
      via: "openMathGate forwards onClear -> onSuccess via openChallenge"
      pattern: "openChallenge"
    - from: "src/scenes/game.js"
      to: "src/ui/mathGate.js"
      via: "existing openMathGate({ brain, onClear }) call site — UNCHANGED, zero edits"
      pattern: "openMathGate\\("
---

<objective>
Extract the shared, mechanic-agnostic in-world challenge overlay (`src/ui/challenge.js`)
out of `src/ui/mathGate.js`'s current 231-line body, and rewrite `mathGate.js` into a
thin wrapper that preserves its exact existing public contract
(`openMathGate({ brain, onClear })`) so `src/scenes/game.js`'s end-of-level call site
needs zero changes. This is the MECH-01 seam: `onClear` becomes the neutral `onSuccess`
name in the shared component (a door doesn't "clear a level"), and the end-of-level-only
"LEVEL CLEAR" celebration banner relocates into `mathGate.js`'s wrapper — `challenge.js`
stays free of any level-specific vocabulary so Phase 16's other mechanics can reuse it
untouched.

Purpose: prove the extraction is a pure, no-behavior-change refactor before a second
consumer (the door, 15-03) ever touches it.
Output: `src/ui/challenge.js` (new, the shared seam); `src/ui/mathGate.js` (rewritten,
thin wrapper + banner).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-challenge-seam-locked-door-mechanic/15-CONTEXT.md
@.planning/phases/15-challenge-seam-locked-door-mechanic/15-RESEARCH.md
@.planning/phases/15-challenge-seam-locked-door-mechanic/15-PATTERNS.md

# The full extraction source — every line of this becomes either challenge.js or the
# mathGate.js wrapper (header 1-32, imports 34-35, palette 38-43, signature+question-pull
# 45-65, overlay construction 70-106, answer boxes 108-150, keyCtrls 152-158, choose()
# 160-172, wrong branch 176-182, correct branch teardown-vs-banner split 184-219, close()
# 222-230):
@src/ui/mathGate.js

# The call site that MUST need zero edits after this plan (onClear callback 174-221):
@src/scenes/game.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/ui/challenge.js — the shared overlay, extracted from mathGate.js</name>
  <files>src/ui/challenge.js</files>
  <read_first>
    - src/ui/mathGate.js (the full 231-line source this task lifts — already read; this is a
      structural extraction, not a fresh design)
    - .planning/phases/15-challenge-seam-locked-door-mechanic/15-PATTERNS.md section
      "src/ui/challenge.js (component, request-response) — NEW" (the exact line-by-line
      transformation table)
  </read_first>
  <action>
    Create `src/ui/challenge.js` as a structural lift of `mathGate.js`'s current body with
    these transformations:

    1. Header comment: carry mathGate.js's discipline comment (one-way dependency, engine-
       global discipline, in-world-not-DOM, forgiving/no-time-pressure, anti-leak) forward
       almost verbatim, but drop the "math gate" framing in favor of "shared in-world
       challenge component" and keep every invariant citation (GATE-01/04/05/06) — they
       still apply to the shared seam.
    2. Imports unchanged: `createBrain` from `../math/brain.js`, `CONFIG` from
       `../config.js`.
    3. Palette constants: keep `PANEL_BG`, `PANEL_BORDER`, `BOX_BG`, `BOX_BORDER`,
       `ACCENT_RED` verbatim. DROP `ACCENT_GREEN` — it was only used by the persistent
       completion celebration, which is end-of-level-specific and moves to mathGate.js's
       wrapper (this file must never reference it).
    4. Signature: `export function openChallenge({ brain, onSuccess, prompt } = {})` (was
       `openMathGate({ brain, onClear })`). Keep the `if (!brain) brain = createBrain();`
       fallback. Pull the question the same way, but the display line becomes
       `const display = prompt ?? \`${q.a} × ${q.b}\`;` — `prompt` is an OPTIONAL plain
       STRING override (per CONTEXT's explicit discretion note: keep this minimal, not a
       richer options object — Phase 16 can extend it, not redesign it).
    5. `let cleared = false;` unchanged.
    6. Overlay construction (root `add([fixed(), z(9999), ...])`, dim layer, panel,
       question text): identical structure and z-values, but every `"math-gate"` tag
       string becomes `"challenge"`.
    7. Answer boxes (dual mouse+key input, `BOX_W`/`BOX_H`/`GAP`/`totalW`/`startX`/`rowY`,
       the `boxes` array, the label text, `box.onClick(() => choose(i))`): copy verbatim,
       tag `"challenge"` instead of `"math-gate"` everywhere it appears (overlay root, dim,
       panel, question, boxes, labels).
    8. `keyCtrls` (the four `onKeyPress("1".."4", ...)` global controllers): copy verbatim.
    9. `choose(i)`: copy the bounds guard (`i < 0 || i >= q.choices.length`) and
       `brain.reportResult(q.a, correct)` verbatim (V5 input-validation invariant — must
       not be loosened). The WRONG branch (tint the picked box `ACCENT_RED`, `shake(6)`,
       `return`) copies verbatim. The CORRECT branch becomes ONLY:
       `cleared = true; close(); onSuccess?.({ table: q.a });` — no completion banner, no
       "level" vocabulary of any kind. This is the one substantive behavioral delta from
       the source file.
    10. `close()`: copy verbatim except the tag argument — `destroyAll("challenge")`
        instead of `destroyAll("math-gate")`.
  </action>
  <acceptance_criteria>
    - `node --check src/ui/challenge.js` passes.
    - `grep -q 'export function openChallenge' src/ui/challenge.js` (new public API).
    - `grep -q 'onSuccess' src/ui/challenge.js` AND `grep -q 'prompt' src/ui/challenge.js` (renamed callback + optional override present).
    - `grep -c '"math-gate"' src/ui/challenge.js` is `0` (tag fully renamed, no old tag string remains).
    - `grep -c '"challenge"' src/ui/challenge.js` is `6` or more (the same number of tag sites mathGate.js had, renamed).
    - `grep -c 'ACCENT_GREEN' src/ui/challenge.js` is `0` (the celebration-only palette constant did not carry over — indirect proof the banner itself did not carry over either).
    - `grep -q 'i < 0 || i >= q.choices.length' src/ui/challenge.js` (the bounds guard is preserved verbatim).
    - `grep -q 'ACCENT_RED' src/ui/challenge.js` (forgiving wrong-branch tint preserved).
  </acceptance_criteria>
  <verify>
    <automated>node --check src/ui/challenge.js && grep -q 'export function openChallenge' src/ui/challenge.js && grep -q 'onSuccess' src/ui/challenge.js && grep -q 'prompt' src/ui/challenge.js && test "$(grep -c '"math-gate"' src/ui/challenge.js)" = "0" && test "$(grep -c 'ACCENT_GREEN' src/ui/challenge.js)" = "0" && grep -q 'i < 0 || i >= q.choices.length' src/ui/challenge.js && grep -q 'ACCENT_RED' src/ui/challenge.js && echo OK</automated>
  </verify>
  <done>src/ui/challenge.js exists, exports openChallenge({brain, onSuccess, prompt}), tags every overlay object "challenge" (zero "math-gate" strings remain), preserves the bounds guard + forgiving wrong-branch verbatim, and carries no end-of-level celebration vocabulary (no ACCENT_GREEN).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite src/ui/mathGate.js as a thin wrapper over challenge.js</name>
  <files>src/ui/mathGate.js</files>
  <read_first>
    - src/ui/mathGate.js (the CURRENT file, about to be overwritten — the correct-branch
      celebration render this task relocates, lines 198-217: the dim layer + banner text,
      tag "gate-cleared", z 9990/9994, colored with the local ACCENT_GREEN)
    - src/ui/challenge.js (created in Task 1 — the onSuccess({ table }) callback shape this
      wrapper's forward call must match)
    - src/scenes/game.js lines 174-221 (the openMathGate({ brain, onClear({ table }) {...} })
      call site that MUST need zero edits after this task)
  </read_first>
  <action>
    Replace the entire body of `src/ui/mathGate.js`:

    1. Header comment: rewrite to describe this file's NEW role — a thin wrapper that
       preserves the pre-existing `openMathGate({ brain, onClear })` contract for
       `game.js`'s end-of-level call site, and owns ONLY the end-of-level-specific
       completion celebration. State plainly that the interactive overlay itself now lives
       in `./challenge.js` and this file no longer constructs it directly.
    2. Imports: `import { openChallenge } from "./challenge.js";` and
       `import { CONFIG } from "../config.js";` (still needed for
       `CONFIG.GATE.DIM_OPACITY` in the banner's dim layer). DROP the `createBrain` import
       — the brain fallback now lives inside `challenge.js`, so this wrapper never needs it.
    3. Keep a LOCAL `const ACCENT_GREEN = [0x00, 0xff, 0x88];` — it is now used only by
       this file's own celebration banner.
    4. Export: `export function openMathGate({ brain, onClear } = {})` — the signature is
       UNCHANGED from today. Its body becomes a single `openChallenge({ brain, onSuccess({ table }) { ... } })`
       call. Inside the `onSuccess` callback, render the SAME persistent celebration the
       original correct-branch rendered (byte-identical visuals): the dim layer
       (`rect(width(), height())`, `pos(0,0)`, `color(0,0,0)`,
       `opacity(CONFIG.GATE.DIM_OPACITY)`, `fixed()`, `z(9990)`, tag `"gate-cleared"`) and
       the banner text (same size/anchor/position/z(9994)/color as the ACCENT_GREEN
       constant, tag `"gate-cleared"`) — copy the exact `add([...])` calls from the
       original file's correct-answer branch verbatim, unchanged in every value. After
       rendering the banner, forward: `onClear?.({ table });` — this is the exact same
       payload shape `game.js`'s existing `onClear({ table })` callback already expects.
    5. This file no longer defines its own `close()`, `keyCtrls`, `choose()`, or answer-box
       construction — all of that now lives in `challenge.js` and is reached only through
       the `openChallenge(...)` call.

    Do NOT touch `src/scenes/game.js` in this task — its `openMathGate({ brain, onClear({table}){...} })`
    call site (lines 174-221) must continue to work completely unmodified; that is the
    MECH-01 "behaves identically" proof this whole plan exists to deliver.
  </action>
  <acceptance_criteria>
    - `node --check src/ui/mathGate.js` passes.
    - `grep -q 'export function openMathGate' src/ui/mathGate.js` (signature preserved).
    - `grep -q 'openChallenge' src/ui/mathGate.js` (thin wrapper calls the shared seam).
    - `grep -q '"gate-cleared"' src/ui/mathGate.js` AND `grep -q 'z(9994)' src/ui/mathGate.js` (the completion celebration's structural markers — tag and banner z — are present, proving the banner relocated here intact).
    - `grep -c 'createBrain' src/ui/mathGate.js` is `0` (the fallback-brain import is gone — challenge.js owns it now).
    - `grep -q 'onClear?.({ table' src/ui/mathGate.js` (forwards the same `{ table }` payload shape game.js already expects).
    - `src/scenes/game.js` is untouched: `git diff --quiet src/scenes/game.js` (no changes to this file from this plan).
  </acceptance_criteria>
  <verify>
    <automated>node --check src/ui/mathGate.js && grep -q 'export function openMathGate' src/ui/mathGate.js && grep -q 'openChallenge' src/ui/mathGate.js && grep -q '"gate-cleared"' src/ui/mathGate.js && grep -q 'z(9994)' src/ui/mathGate.js && test "$(grep -c 'createBrain' src/ui/mathGate.js)" = "0" && grep -q 'onClear?.({ table' src/ui/mathGate.js && git diff --quiet src/scenes/game.js && echo OK</automated>
  </verify>
  <done>src/ui/mathGate.js exports the unchanged openMathGate({brain, onClear}) signature, forwards to openChallenge with an onSuccess callback that renders the byte-identical "gate-cleared" celebration then calls onClear?.({table}); src/scenes/game.js has zero diff from this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player mouse/keyboard input → `choose(i)` index | The only untrusted input surface in this module: a click or a 1-4 keypress selects an array index into `q.choices`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02-01 | Denial of Service | src/ui/challenge.js `choose(i)` | medium | mitigate | The existing `i < 0 \|\| i >= q.choices.length` bounds guard is preserved verbatim in the extraction (Task 1 acceptance criteria assert this literally) — an out-of-range index can never read outside `q.choices`. |
| T-15-02-02 | Tampering | question/answer rendering | low | mitigate | Every visual in challenge.js is a Kaplay canvas `text()`/`rect()` object — no `innerHTML`/`document.*`/markup-string sink exists by construction; enforced going forward by 15-01's repointed `check-gate.sh` negative grep. |
| T-15-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No installs in this plan — pure extraction of existing, already-shipped source. |
</threat_model>

<verification>
- Per task: `node --check` + the grep batteries in each task's `<verify>`.
- After this plan (wave merge): `node --check src/ui/challenge.js && node --check src/ui/mathGate.js` both pass; `git diff --quiet src/scenes/game.js` confirms zero drift at the untouched call site.
- Full structural-gate proof (check-gate.sh, once 15-03/15-04 also land) and the real-browser byte-identical replay of the end-of-level gate happen in 15-04 — this plan's own verification is necessarily static (no door exists yet to integration-test against).
</verification>

<success_criteria>
- `src/ui/challenge.js` exists, exports `openChallenge({brain, onSuccess, prompt})`, and is a faithful, tag-renamed extraction of mathGate.js's prior interactive overlay with no end-of-level vocabulary.
- `src/ui/mathGate.js`'s `openMathGate({brain, onClear})` signature and the "gate-cleared" celebration are both byte-identical in effect to before this plan.
- `src/scenes/game.js` has zero diff — proving the seam extraction required no changes at the existing call site (the core MECH-01 claim).
</success_criteria>

<output>
Create `.planning/phases/15-challenge-seam-locked-door-mechanic/15-02-SUMMARY.md` when done.
</output>
