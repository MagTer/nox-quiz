---
phase: 19-polish-consolidated-kid-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-safety.sh
autonomous: true
requirements:
  - SAFE-04
must_haves:
  truths:
    - "D-01: keep check-safety.sh whole-src coverage as-is; add only a comment/header note that Phase 19 verified it covers src/mechanics/ and src/parallax.js"
    - "scripts/check-safety.sh scans every .js file under src/ including src/mechanics/, src/parallax.js, src/levels/, src/scenes/, src/ui/, src/math/, src/player.js, src/fx.js, src/progress.js, src/camera.js, src/config.js and prints PASS"
    - "No timer/scheduler token (setTimeout|setInterval|countdown|wait(|loop(|lifespan() and no punishment construct (gameOver|game_over|loseLife|lives--|subtractXp|deductXp|xpLoss|penaliz) appears in src/ code with comments stripped"
    - "src/fx.js still self-cleans via tween().onEnd and the persistent controls hint 'SPACE jump' remains mounted in src/ code"
  artifacts: []
  key_links: []
---

<objective>
Run and extend the whole-project ADHD-safety audit for Phase 19. Confirm that `scripts/check-safety.sh` already covers every new module and asset consumer added in Phases 13–18 (mechanics, parallax, levels, art/animation) and that the no-timer / forgiving / no-game-over mandate holds across the entire `src/` tree. If any new file is outside the scan, extend the script's `find` scope; otherwise document the coverage and keep the script green.

Purpose: SAFE-04 demands an audited guarantee that nothing in the assembled milestone counts down, punishes a wrong answer, or ends the run. This plan produces the static half of that evidence.
Output: Updated `scripts/check-safety.sh` (comment/header only unless a gap is found) and a clean static gate run.
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-polish-consolidated-kid-uat/19-CONTEXT.md
@scripts/check-safety.sh
@src/config.js
@src/fx.js
@src/player.js
@src/parallax.js
@src/mechanics/door.js
@src/mechanics/gates.js
@src/mechanics/enemy.js
@src/mechanics/collect.js
@src/ui/challenge.js
@src/ui/mathGate.js
@src/scenes/game.js
@src/scenes/title.js
@src/scenes/select.js
@src/levels/level-01.js
@src/levels/level-02.js
@src/levels/level-03.js
@src/levels/level-04.js
@src/levels/build.js
@src/levels/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify whole-src coverage and run check-safety.sh</name>
  <files>scripts/check-safety.sh, src/**/*.js</files>
  <read_first>
    - scripts/check-safety.sh
  </read_first>
  <action>
    Confirm that `scripts/check-safety.sh` uses `find "$ROOT/src" -name '*.js'` for its negative no-timer and no-punishment scans, which already covers every new module added in Phases 13–18. If the `find` scope is narrower, extend it to `$ROOT/src` recursively. Add a header/comment note that Phase 19 verified the scan covers `src/mechanics/`, `src/parallax.js`, `src/levels/`, and the art/animation consumers. Do not change the banned-token patterns or the positive SAFE-02 / fx.js checks.

    Then run `bash scripts/check-safety.sh` from the repo root. If it fails, read the failing file, determine whether the match is a false-positive (e.g., a comment not stripped, a legitimate string like "gameOver" in a docstring) or a real violation. Fix real violations by routing to 19-04; document false-positives in the plan deviations.
  </action>
  <acceptance_criteria>
    - `bash scripts/check-safety.sh` exits 0 and prints "safety checks: PASS".
    - The script's no-timer scan and no-punishment scan both iterate over every `.js` file under `src/` (verified by reading the `find` command).
    - A comment in `scripts/check-safety.sh` near the `find` loop notes Phase 19 whole-src coverage including mechanics/parallax/levels.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-safety.sh</automated>
  </verify>
  <done>check-safety.sh covers all src/ modules and prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Spot-check new modules for accidental timer or punishment drift</name>
  <files>src/mechanics/door.js, src/mechanics/gates.js, src/mechanics/enemy.js, src/mechanics/collect.js, src/ui/challenge.js, src/parallax.js, src/player.js, src/fx.js</files>
  <read_first>
    - src/mechanics/door.js
    - src/mechanics/gates.js
    - src/mechanics/enemy.js
    - src/mechanics/collect.js
    - src/ui/challenge.js
    - src/parallax.js
    - src/player.js
    - src/fx.js
  </read_first>
  <action>
    Do a manual spot-read of the listed files to confirm no scheduler calls, no countdown, no lives system, and no XP-loss / penalty code paths exist outside the automated scan's patterns. Pay special attention to:
    - `src/mechanics/enemy.js`: enemy never deals contact damage and never ends the run.
    - `src/mechanics/collect.js`: wrong pickup never punishes.
    - `src/ui/challenge.js`: wrong answer re-asks with no timer and no lockout.
    - `src/parallax.js` and `src/fx.js`: motion is camera-driven or tween-based, never `wait()`/`loop()`/timer-driven, and respects the ≤400–500 ms flash cap.

    If any new construct is found that the existing grep patterns miss, extend the patterns in `scripts/check-safety.sh` and re-run. Otherwise record "no drift found" in the plan deviations.
  </action>
  <acceptance_criteria>
    - Every listed file uses tween-based or camera-driven motion only; no `setTimeout`, `setInterval`, `wait(`, `loop(`, `countdown`, or `lifespan(` calls in code.
    - No XP-loss, lives, game-over, or penalty construct appears in any listed file.
    - `src/fx.js` tween durations remain ≤ 500 ms (verified by reading CONFIG.FX or literal durations).
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-safety.sh && grep -RinE 'setTimeout|setInterval|countdown|[^a-zA-Z]wait\(|[^a-zA-Z]loop\(|lifespan\(' src/ --include='*.js' | grep -v '//' || echo 'no timer tokens in code'</automated>
  </verify>
  <done>Spot-check confirms no timer/punishment drift in new modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static grep → runtime behavior | A green check-safety.sh proves absence of banned tokens, not that every mechanic feels forgiving at runtime; that is covered by 19-02 and 19-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01-01 | Elevation of Privilege | check-safety.sh scan scope accidentally narrows | medium | mitigate | Verify `find "$ROOT/src" -name '*.js'` covers all new modules; extend if needed |
| T-19-01-02 | Tampering | New mechanic introduces a punishment construct not caught by current patterns | medium | mitigate | Spot-read new modules and extend banned-token list if a new pattern appears |
</threat_model>

<verification>
- `bash scripts/check-safety.sh` exits 0 and prints "safety checks: PASS".
- Every `.js` file under `src/` is included in the scan scope.
- Spot-check of new modules records no drift.
</verification>

<success_criteria>
- The no-timer and no-punishment static audit passes for the entire `src/` tree including all new mechanics, parallax, levels, and art/animation modules.
- Any scan-scope gap is closed; any false-positive is documented.
</success_criteria>

<output>
Create `.planning/phases/19-polish-consolidated-kid-uat/19-01-SUMMARY.md` when done.
</output>
