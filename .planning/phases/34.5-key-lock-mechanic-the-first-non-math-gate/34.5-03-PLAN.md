---
phase: 34.5-key-lock-mechanic-the-first-non-math-gate
plan: 03
type: execute
wave: 3
depends_on: [34.5-01, 34.5-02]
files_modified:
  - src/levels/level-02.js
  - scripts/audit-key-lock.mjs
autonomous: true
requirements: [KEY-01, KEY-02]
must_haves:
  truths:
    - "level-02 carries one key (spawn-side, well before the door at x:1540) and one lock (x~3960, guarding the exit corridor BEFORE goal x:4200) — validate-levels.mjs PASSes (the level is proven softlock-free by the now-green key-lock check)"
    - "audit-key-lock.mjs drives a real player to the key then the lock and asserts get('key') and get('lock') are both empty (triggered:true) — the SC3 in-engine proof a static model alone cannot give"
    - "the lock sits AFTER the enemy (x:1860) and door (x:1540) so it never obstructs the earlier math encounters — audit-phase21-mechanics.mjs and browser-boot stay green"
  artifacts:
    - "src/levels/level-02.js — geometry.keys and geometry.locks fields (one throwaway pair; Phase 34.6 rebuilds this level)"
    - "scripts/audit-key-lock.mjs — driven-player in-engine audit (copies audit-coins.mjs boilerplate, new PORT)"
  key_links:
    - "the lock at x~3960 gates the corridor BEFORE goal x:4200 on floor run {x:3680,w:600} — if co-located with the goal/math-gate it is never evaluated (Pitfall 1: the end math-gate go()s away on a correct answer)"
    - "audit drives key.x then lock.x; entity-gone from get(tag) IS the in-engine signal (audit-coins.mjs reasoning) — key destroyed only by wireKey's pickup, lock destroyed only by the key-held branch"
    - "audit-key-lock.mjs copies audit-coins.mjs server/guard/nav VERBATIM with a NEW port — duplication is the deliberate convention (CLAUDE.md); do NOT extract a shared module"
---

<objective>
Prove the key/lock mechanic works end-to-end IN THE ENGINE: place one throwaway key-lock pair on level-02 (the shortest real level, already has a door) and add a driven-player audit that makes a real player pick up the key and open the lock. The static model passing is NOT sufficient — this milestone has repeatedly shipped bugs past a green static model (sawtooth floor, grey ground, ceiling-bonk coins), so KEY-02's success criterion 3 mandates a real in-engine driven proof.

This is Wave 3 and depends on Plan 01 (the validator that must PASS the level-02 placement) and Plan 02 (the mechanic + build.js emission that make the entities real). The level-02 pair is acknowledged throwaway — Phase 34.6 rebuilds level-02 from scratch — but it is the only way to get REAL in-engine coverage now.

Purpose: KEY-01 + KEY-02 — a real driven player collects the key and opens the lock (`audit-*.mjs`), and the static validator confirms level-02 is softlock-free with the pair in place.
Output: level-02.js keys/locks fields + `scripts/audit-key-lock.mjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.5-key-lock-mechanic-the-first-non-math-gate/34.5-RESEARCH.md
@.planning/phases/34.5-key-lock-mechanic-the-first-non-math-gate/34.5-PATTERNS.md
@.planning/phases/34.5-key-lock-mechanic-the-first-non-math-gate/34.5-VALIDATION.md
@src/levels/level-02.js
</context>

<artifacts_produced>
## Artifacts this plan produces (new symbols/files)

- **`src/levels/level-02.js`** — new `geometry.keys: [{ x, y }]` and `geometry.locks: [{ x, y }]` fields (one pair). Key spawn-side on floor run {x:700,w:560}; lock at x~3960 guarding the exit corridor before goal x:4200. Pure data — no engine refs; the registry stays node-importable.
- **`scripts/audit-key-lock.mjs`** — NEW driven-player audit. Copies audit-coins.mjs's resolvePlaywright/static-server/path-guard/SAVE seeding/title→select→level nav VERBATIM with a new PORT (e.g. 8773). Drives to the key then the lock via driveToXPlanned and asserts both entities dropped (triggered:true), process.exit(1) otherwise.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Place the throwaway key-lock pair on level-02</name>
  <read_first>
    - src/levels/level-02.js (read in full) — the geometry block: floor runs (spawn on {x:0,w:520}; the key's run {x:700,w:560}; the goal's run {x:3680,w:600}), the existing door/enemy/checkpoint entries and their x positions (door x:1540, enemy x:1860 per RESEARCH), goal at x:4200, and bounds.right:4280. Note FLOOR_Y = CONFIG.FLOOR_Y (320).
    - 34.5-RESEARCH.md Finding 6 (the level-02 minimal entry: keys [{x:760,y:FLOOR_Y-32}], locks [{x:3960,y:FLOOR_Y-CONFIG.LOCK.H}]) and Pitfall 1 (the lock must gate BEFORE the goal — the end math-gate go()s away on a correct answer) and Assumption A5 (place the lock after enemy/door so audit-phase21 still passes).
    - 34.5-PATTERNS.md "MODIFY src/levels/level-02.js" section.
  </read_first>
  <files>src/levels/level-02.js</files>
  <action>
    Add two fields to level-02's geometry object (import CONFIG is already present via FLOOR_Y). keys: [{ x: 760, y: FLOOR_Y - 32 }] — spawn-side, on floor run {x:700,w:560} (700..1260), reachable early and WELL before the door at x:1540 so the sequence reads get-key → door → enemy → locked corridor → math gate. locks: [{ x: 3960, y: FLOOR_Y - CONFIG.LOCK.H }] — on the goal's floor run {x:3680,w:600} (3680..4280), guarding the exit corridor BEFORE the goal at x:4200 (Pitfall 1: the lock MUST gate before the goal/math-gate can be touched, since the end math-gate transitions the scene on a correct answer). The lock at x:3960 sits AFTER the enemy (x:1860) and door (x:1540) so it never obstructs those earlier math encounters (Assumption A5). Do NOT change bounds.right — 4280 already covers x:3960+CONFIG.LOCK.W. This is a throwaway proof (34.6 rebuilds level-02); verify against the validator + audit before considering it done.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` exits 0 — level-02 now emits a `level-02 | key-lock | PASS` (or WARN) row proving the key at x:760 is reachable spawn-side of the lock at x:3960; no HARD-FAIL anywhere.
    - `node scripts/browser-boot.mjs` boots and drives all 8 levels clean, including level-02 with the key-lock now in the path.
    - `node scripts/audit-phase21-mechanics.mjs` green — the door (x:1540) and enemy (x:1860) encounters on level-02 still trigger; the lock at x:3960 does not obstruct them.
  </acceptance_criteria>
  <done>level-02 carries a softlock-free key-lock pair (validator PASS); browser-boot + audit-phase21 stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the driven-player in-engine audit scripts/audit-key-lock.mjs</name>
  <read_first>
    - scripts/audit-coins.mjs (read in full) — copy VERBATIM: resolvePlaywright(), the static server, the path-traversal guard, SAVE_KEY/SAVE_BLOB seeding, the title→select→level-02 navigation, and the "entity gone from get(tag) IS the in-engine signal" assertion reasoning. Note the header's list of used PORTs (8765-8772 taken).
    - scripts/lib/mechanic-drive.mjs — driveToXPlanned(page, x, geometry, opts) returning `{ reachedX, triggered }`, bidirectional since Plan 34-07 (handles gaps, mounts, respawns, leftward legs).
    - src/levels/index.js / the getLevel accessor used by audit-coins.mjs to read level-02's geometry (keys[0], locks[0]).
    - 34.5-RESEARCH.md Finding 5 and 34.5-PATTERNS.md "NEW scripts/audit-key-lock.mjs" section.
  </read_first>
  <files>scripts/audit-key-lock.mjs</files>
  <action>
    Create scripts/audit-key-lock.mjs by copying audit-coins.mjs's server/guard/nav boilerplate VERBATIM with a NEW PORT (e.g. 8773 — 8765-8772 are listed as taken in headers). Duplication is the DELIBERATE convention (CLAUDE.md; STATE.md Blockers) — do NOT extract or import a shared server module. After navigating into level-02 and building it, read its geometry (getLevel("level-02").geometry), take key = geometry.keys[0] and lock = geometry.locks[0]. Drive to the key: driveToXPlanned(page, key.x, geometry, { targetY: key.y }); then assert keyGone via page.evaluate(() => get("key").length === 0) — the key entity is destroyed ONLY by wireKey's pickup handler, so its absence proves pickup fired. Then drive to the lock: driveToXPlanned(page, lock.x, geometry); assert lockGone via page.evaluate(() => get("lock").length === 0) — the lock blocker is destroyed ONLY by the key-held open branch, so its absence proves the player opened it WITH the key. Set triggered = keyGone && lockGone; report a clear `{ triggered }`/offender line and process.exit(1) if not triggered (the SC3 in-engine proof). Mirror audit-coins.mjs's teardown (close browser, stop server) so the process exits cleanly.
  </action>
  <verify>
    <automated>node scripts/audit-key-lock.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/audit-key-lock.mjs` exits 0 and reports triggered:true — a real driven player collected the key (get("key") empty) THEN opened the lock (get("lock") empty) on level-02.
    - The script uses a PORT not already claimed by another audit (no port collision) and does not import a shared server module (boilerplate copied per the deliberate-duplication convention).
    - Running it a second time still passes (the SAVE seeding / nav is self-contained and repeatable).
  </acceptance_criteria>
  <done>node scripts/audit-key-lock.mjs exits 0 with triggered:true — a real driven player collected the key then opened the lock in-engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in the web/app-sec sense) | Static client-only browser game — no backend, no network I/O. The audit drives a locally-served instance over localhost with a path-traversal-guarded static server (copied verbatim from audit-coins.mjs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.5-05 | Denial (of progress) | worst-case softlock: missed key at the point of no return | high | mitigate | The lock guards the exit corridor (the point of no return); a missed key would be the worst softlock. Static proof: validate-levels.mjs confirms the key is reachable spawn-side of the lock. In-engine proof: audit-key-lock.mjs drives a real player to collect the key THEN open the lock — the static model alone is insufficient (SC3). |
| T-34.5-06 | Spoofing (false green) | audit asserts the wrong thing | medium | mitigate | Assert entity-gone from get("key")/get("lock") — each entity is destroyed by exactly ONE code path (pickup handler / key-held open branch), so its absence is an unambiguous in-engine signal, not a proxy. |
| T-34.5-SC | Tampering | package installs | low | accept | Zero package installs this phase — Playwright is already resolved dynamically in-tree; no new dependency. Nothing to audit. |
</threat_model>

<verification>
- `node scripts/validate-levels.mjs` — level-02 with the pair PASSes (softlock-free); all 8 levels green.
- `node scripts/audit-key-lock.mjs` — triggered:true (key collected, lock opened) in-engine.
- `node scripts/browser-boot.mjs` — all 8 levels boot/drive clean with the level-02 key-lock in the path.
- `node scripts/audit-phase21-mechanics.mjs` — the earlier door/enemy encounters unaffected.
- **Full-suite gate after this wave:** `bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs && node scripts/audit-key-lock.mjs` (check-terrain-atlas.sh NOT required — no assets/tiles change).
</verification>

<success_criteria>
The key/lock mechanic is proven end-to-end in the engine (KEY-01 + KEY-02): level-02 carries a softlock-free key-lock pair (validator PASS) placed so the lock gates the exit corridor before the goal without obstructing the earlier math encounters, and a driven-player audit proves a real player picks up the key and opens the lock. The static model is backed by a real in-engine drive — the SC3 requirement that a green static model alone is not proof.
</success_criteria>

<output>
Create `.planning/phases/34.5-key-lock-mechanic-the-first-non-math-gate/34.5-03-SUMMARY.md` when done.
</output>
