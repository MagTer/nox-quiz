---
phase: 39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi
plan: 07
type: execute
wave: 2
depends_on: ["39-01", "39-03"]
files_modified:
  - src/levels/level-08.js
autonomous: true
requirements: [POL-01, POL-03, POL-05]
must_haves:
  truths:
    - "Level 8 skeletons walk on the floor with a wide visible sweep off the coins; contact respawns (POL-01)."
    - "Level 8's moving platforms span the REAL pits — the MOAT (1880..2520) and the DRAWBRIDGE CHASM (4480..5000) — with the static stepping-stones that used to bridge them REMOVED; a missed hop falls to respawn (checkpoint reachable), never a true softlock (POL-03)."
    - "Both endpoints of each relocated mover sit inside the ~88px jump envelope of a reachable ledge (mover-reachability holds)."
    - "The L8 face-columns are repositioned off the two gaps (x3160, x6380) and off the spawn (x40) to read as intentional (POL-05)."
  artifacts:
    - src/levels/level-08.js
  key_links:
    - "Removing the BARBICAN/drawbridge stepping-stone PLATFORMS is a FROZEN edit → it INTENTIONALLY trips check-geometry-frozen. The --write re-baseline is deliberately deferred to plan 39-08 (its own task, after all frozen edits)."
    - "movers + patrollers + props (columns) are EXEMPT; only the platform removals trip the hash."
    - "The walk-only browser-boot driver must RIDE the relocated mover across each pit — capability was built in plan 39-03."
---

<objective>
The big lift: relocate Level 8's movers over the REAL pits (the moat and the drawbridge chasm), REMOVE the static stepping-stones that currently bridge them, ground the L8 skeletons, and reposition the L8 face-columns. This is the ONLY plan with FROZEN edits (platform removals) — it intentionally leaves `check-geometry-frozen` RED; the deliberate `--write` re-baseline is plan 39-08's own task, after all frozen edits are complete (per the sequencing rule).

Purpose: User locked decision #3 — movers over holes = full re-placement: relocate over real pits, remove static stepping-stones, ride via the updated harness, re-freeze baseline. CONTEXT concretely maps L8's two real holes. POL-05: the three columns currently float over the gaps/spawn.
Output: L8 movers spanning the moat + chasm with stepping-stones gone; grounded L8 skeletons; repositioned columns. Frozen gate left RED for plan 39-08 to acknowledge.

Spec-less probe fallback (VISIBLE SKIP): no SPEC/probe predicates; truths derived from POL-01/03/05 text + CONTEXT.md decisions #3, #4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/CONTEXT.md
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md
@.claude/CLAUDE.md
@src/levels/level-08.js
@docs/LEVEL-DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ground L8 skeletons + reposition the face-columns (EXEMPT)</name>
  <read_first>
    - src/levels/level-08.js:346,350 (two patrollers, y:214, 960-1060 and 3560-3660)
    - src/levels/level-08.js:374-376 (three prop-castle-columns at x:40 spawn, x:3160 over F3/F4 gap 3080-3240, x:6380 over F7/F8 gap 6280-6440, all layer:"back",y:130)
    - src/config.js:296 (PATROLLER.SPEED) + .planning/.../39-PATTERNS.md (POL-01 + POL-05 (c) columns)
  </read_first>
  <action>
    (a) POL-01: ground both L8 patrollers (level-08.js:346, :350) — y 214 → ~268, widen sweep to ~200-320px, `speed:70-90`, shift off floor coins, keep the F1 and F4 lanes clearable.
    (b) POL-05: reposition the three `prop-castle-column` props (level-08.js:374-376) so none floats over a gap or the spawn: move x:40 off the spawn onto a solid bailey backdrop, move x:3160 off the F3/F4 gap (3080-3240) onto solid F3 or F4 depth, move x:6380 off the F7/F8 gap (6280-6440) onto solid ground — all keep layer:"back",y:130. They must read as intentional wall dressing behind traversal surfaces.
    Both edits are EXEMPT (patrollers + props) — hash-neutral.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: neither L8 patroller still at y:214; the three columns no longer sit at x:3160/x:6380 over the named gaps nor at x:40 over spawn (grep shows new x values on solid ground).
    - `node scripts/validate-levels.mjs` zero HARD-FAIL for L8.
    - `node scripts/check-geometry-frozen.mjs` still GREEN AT THIS POINT (only EXEMPT edits so far — proves Task 1 touched no frozen array; Task 2 is what will trip it).
  </acceptance_criteria>
  <done>L8 skeletons grounded; columns repositioned off gaps/spawn; still frozen-hash-neutral before the platform removals.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate L8 movers over the real pits + remove stepping-stones (FROZEN)</name>
  <read_first>
    - src/levels/level-08.js:322-334 (geometry.movers — current placements riding solid floor)
    - src/levels/level-08.js:91-95, :111-126 (the BARBICAN / broken-drawbridge static stepping-stone PLATFORMS: BA @1920 bridging the 640px MOAT 1880..2520; SS1 @4560 bridging the 520px DRAWBRIDGE CHASM 4480..5000)
    - src/config.js:279-286 (CONFIG.MOVER — WIDTH 48, PERIOD_S 4; per-mover w/period overrides) + docs/LEVEL-DESIGN.md jump envelope (~88px)
    - scripts/lib/reachability.mjs:1104-1132 (mover-reachability — BOTH endpoints must be reachable rightward from spawn)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (POL-03 section)
  </read_first>
  <action>
    FIRST — cross-level confirmation (WARNING-1 guard against silently under-delivering POL-03): scan ALL 8 levels for a static `platforms` entry whose x-span sits OVER a floor gap (a "hole" = an empty x-range between two `geometry.floors` spans, per the CONTEXT data model) — i.e. a stepping-stone that exists only to bridge a real pit. Confirm ONLY level-08's moat (1880..2520) and drawbridge chasm (4480..5000) qualify. If ANY other level hides a stepping-stone-bridged real pit, STOP and flag it to the coordinator — POL-03 (full re-placement) must extend the relocate-and-remove treatment to that level too (that would add a frozen edit there, changing plan 39-08's re-baseline diff scope). Record the scan result in the SUMMARY.
    THEN, for EACH of L8's two real pits (MOAT 1880..2520, DRAWBRIDGE CHASM 4480..5000):
    1. REMOVE the static stepping-stone platform(s) that currently bridge it (the BARBICAN BA @1920 for the moat; SS1 @4560 for the chasm — and any other pure-bridging tiers in :91-95/:111-126 that only exist to walk the pit). This is a FROZEN `platforms`-array edit — it WILL trip check-geometry-frozen (intended).
    2. Place/retune a `geometry.movers` entry (EXEMPT) to SPAN the pit: mover travels from a reachable ledge on the near side to a reachable ledge on the far side, with BOTH endpoints inside the ~88px jump envelope of solid ground (mover-reachability at reachability.mjs:1104-1132 must hold). Give it enough width (`w`) and a sane `period` so a rider crosses comfortably.
    3. Ensure a MISSED hop falls into the pit → respawn to a checkpoint that is reachable WITHOUT the removed stepping-stones (verify the nearest checkpoint still lands the player on a solid, re-approachable ledge — NO true softlock for a human player).
    Do NOT edit any other level. Do NOT run `--write` here (plan 39-08 owns the re-baseline).
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/browser-boot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Cross-level scan (WARNING-1): the SUMMARY records that all 8 levels were checked for platforms bridging a real floor gap, and ONLY level-08's moat + chasm qualified (or, if another level hid one, the coordinator was flagged and the treatment was extended there — with the extra frozen edit noted for plan 39-08's diff).
    - Source assertion: the BARBICAN moat-bridge (BA @1920) and drawbridge-chasm bridge (SS1 @4560) stepping-stone platforms are REMOVED from level-08.js; the two movers now span the moat and the chasm (endpoints on opposite sides of each pit).
    - `node scripts/validate-levels.mjs` zero HARD-FAIL — mover-reachability holds (both endpoints reachable), no coin/route made permanently unreachable.
    - `node scripts/browser-boot.mjs` crosses L8 spawn→goal by RIDING each relocated mover across its pit (the plan-39-03 ride capability), no softlock; a missed hop respawns to a reachable checkpoint.
    - `node scripts/check-geometry-frozen.mjs` is EXPECTED RED here (the platform removals intentionally trip it) — do NOT re-baseline in this plan; plan 39-08 acknowledges it.
  </acceptance_criteria>
  <done>L8 movers span the moat and chasm; the static stepping-stones are gone; browser-boot rides across without softlock; the frozen gate is intentionally RED awaiting plan 39-08's --write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser localStorage → game | Only external surface; no network/backend/auth. |

## STRIDE Threat Register (proportionate — ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `localStorage` progress key | low | accept | Guarded `src/progress.js` seams already mitigate. Level-data edits only — no new surface. |

No package-manager installs. No new external threat surface. (Softlock risk is a PLAYABILITY concern, not a security threat — mitigated by the no-true-softlock acceptance criterion + browser-boot proof + the plan-39-08 human playthrough.)
</threat_model>

<verification>
- `node scripts/validate-levels.mjs` zero HARD-FAIL (mover-reachability holds).
- `node scripts/browser-boot.mjs` crosses L8 by riding movers, no softlock.
- `node scripts/check-geometry-frozen.mjs` intentionally RED (re-baseline deferred to plan 39-08).
</verification>

<success_criteria>
- L8 movers span the two real pits; stepping-stones removed; grounded skeletons; repositioned columns; browser-boot rides across cleanly; frozen gate red awaiting acknowledgment.
</success_criteria>

<output>
Create `.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-07-SUMMARY.md` when done. NOTE explicitly in the SUMMARY that check-geometry-frozen is intentionally RED pending plan 39-08's --write re-baseline.
</output>
