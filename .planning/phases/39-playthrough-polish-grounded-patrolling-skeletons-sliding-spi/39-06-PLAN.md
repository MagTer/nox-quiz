---
phase: 39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi
plan: 06
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - src/levels/level-05.js
  - src/levels/level-06.js
  - src/levels/level-07.js
autonomous: true
requirements: [POL-01, POL-02, POL-03]
must_haves:
  truths:
    - "Levels 5, 6 & 7 skeletons walk on the floor with a wide visible sweep off the coins; contact respawns (POL-01)."
    - "At least two chosen levels (L5 and L7) carry a geometry.slidingSpikes hazard that slides horizontally along the ground and respawns on contact; each level still validates and is crossable (POL-02)."
    - "L7's near-zero mover sweep is widened (EXEMPT) so it visibly moves (POL-03 motion-only — no frozen edit)."
  artifacts:
    - src/levels/level-05.js
    - src/levels/level-06.js
    - src/levels/level-07.js
  key_links:
    - "geometry.slidingSpikes is EXEMPT (added to the frozen strip in plan 39-01) — authoring it does NOT trip the frozen hash."
    - "A sliding spike must sit on a floor span with room to pass between sweeps (reachability treats it as passable, per plan 39-01)."
---

<objective>
Ground the cemetery/castle-approach skeletons on Levels 5, 6, 7 (POL-01), place the NEW sliding-spike hazard on two chosen levels (POL-02, first real use of the plan-39-01 mechanic), and widen L7's near-zero mover sweep (POL-03 motion-only). All freeze-EXEMPT — no re-baseline.

Purpose: POL-01 locked decision #4 (ground patrol). POL-02 locked decision #2 (moving spikes slide along the ground), authored "at a few chosen spots" — L5 and L7 are chosen for their open floor lanes with room to time a crossing.
Output: L5/L6/L7 grounded patrols; sliding spikes on L5 & L7; L7 mover sweep widened.

Spec-less probe fallback (VISIBLE SKIP): no SPEC/probe predicates; truths derived from POL-01/02/03 text + CONTEXT.md decisions #2, #3, #4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/CONTEXT.md
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md
@.claude/CLAUDE.md
@src/levels/level-05.js
@src/levels/level-06.js
@src/levels/level-07.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ground the L5/L6/L7 skeleton patrols</name>
  <read_first>
    - src/levels/level-05.js:214 (patroller y:214, 1150-1250)
    - src/levels/level-06.js:217,221 (two patrollers, y:214, 1900-2020 and 2470-2560)
    - src/levels/level-07.js:207 (patroller y:214, 450-550)
    - src/levels/build.js:580-595 (patroller factory) + src/config.js:296 (SPEED default 40)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (POL-01 grounding math: y ≈ 268)
  </read_first>
  <action>
    For each patroller in L5 (level-05.js:214), L6 (level-06.js:217, :221), L7 (level-07.js:207): lower y 214 → ~268 (feet on FLOOR_Y 320), widen sweep to ~200-320px, add per-patroller `speed:70-90`, shift x off floor coins, keep each lane walk-clearable (leave a passing gap so browser-boot crosses). Contact seam unchanged (game.js:277).
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` zero HARD-FAIL for L5/L6/L7.
    - `node scripts/check-geometry-frozen.mjs` GREEN (patrollers EXEMPT — hash-neutral).
    - Source assertion: no patroller in L5/L6/L7 still at y:214; sweeps wider than ~100px.
    - `node scripts/browser-boot.mjs` clears L5/L6/L7 spawn→goal without softlock.
  </acceptance_criteria>
  <done>L5/L6/L7 skeletons grounded, wide-sweeping, off the coins, still clearable; validate + frozen + boot green.</done>
</task>

<task type="auto">
  <name>Task 2: Place sliding spikes on L5 and L7 + widen the L7 mover</name>
  <read_first>
    - src/levels/build.js (the new slidingSpike loop added in plan 39-01) + src/config.js CONFIG.SLIDING_SPIKE
    - src/levels/level-08.js:322-334 (geometry.movers authoring shape — the analog for a `{x1,y1,x2,y2,period}` motion descriptor)
    - src/levels/level-05.js and level-07.js floor spans (choose a flat lane with room to time a crossing) + FLOOR_Y=320, CONFIG.SPIKE_SIZE=16
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (POL-02 "Descriptor key decision")
  </read_first>
  <action>
    Add a `slidingSpikes:` array to `geometry` on L5 and L7. Author each entry as `{ x1, y1, x2, y2, period? }` with `y1 = y2 = FLOOR_Y - CONFIG.SPIKE_SIZE = 304` (ground-sliding) and `x1 ≠ x2` spanning a clear flat floor lane (e.g. ~200-400px travel). Choose a lane with ≥ the jump envelope of clear approach so the player can time a hop or wait for the sweep to clear — NOT over a pit, NOT crowding a checkpoint. Optionally set `period` (default 3s from CONFIG.SLIDING_SPIKE). Place 1 sliding spike per level (2 total). It reuses the "spike" respawn seam automatically.
    Also POL-03 (motion-only): widen L7's near-zero (~40px) mover sweep (EXEMPT `x2-x1`) so it visibly slides; keep both endpoints jump-reachable; do NOT remove any platform (L7's mover stays on solid floor — real-hole work is L8's in plan 39-07).
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && node scripts/audit-phase21-mechanics.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'slidingSpikes' src/levels/level-05.js` ≥ 1 AND `grep -c 'slidingSpikes' src/levels/level-07.js` ≥ 1; each entry has y=304 and x1≠x2.
    - `node scripts/validate-levels.mjs` zero HARD-FAIL (the sliding spike does not make any coin/route unreachable — reachability treats it as passable per plan 39-01).
    - `node scripts/check-geometry-frozen.mjs` GREEN (slidingSpikes is EXEMPT; L7 mover widen is EXEMPT).
    - `node scripts/browser-boot.mjs` crosses L5 and L7 spawn→goal without softlock (the walk-only driver hops/waits out the sweep).
    - `node scripts/audit-phase21-mechanics.mjs` — sliding-spike encounters (via deriveEncounters, plan 39-01) report `triggered: true`.
  </acceptance_criteria>
  <done>L5 and L7 each have a ground-sliding spike that respawns on contact and is crossable; L7 mover visibly moves; all gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser localStorage → game | Only external surface; no network/backend/auth. |

## STRIDE Threat Register (proportionate — ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `localStorage` progress key | low | accept | Guarded `src/progress.js` seams already mitigate. Level-data edits only — no new surface. |

No package-manager installs. No new external threat surface.
</threat_model>

<verification>
- `node scripts/validate-levels.mjs` zero HARD-FAIL.
- `node scripts/check-geometry-frozen.mjs` green (slidingSpikes + patrollers + mover-widen all EXEMPT).
- `node scripts/browser-boot.mjs` + `node scripts/audit-phase21-mechanics.mjs` green for L5/L6/L7.
</verification>

<success_criteria>
- L5/L6/L7 grounded patrols; a ground-sliding spike on L5 and L7 that respawns + is crossable; L7 mover visibly moving; all edits frozen-hash-neutral.
</success_criteria>

<output>
Create `.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-06-SUMMARY.md` when done.
</output>
