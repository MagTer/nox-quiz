---
phase: 39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-art-assets.py
  - assets/props/prop-swamp-lantern.png
  - src/assets-manifest.js
autonomous: false
requirements: [POL-05]
must_haves:
  truths:
    - "The L1/L2 alcove light sprite is a NON-SKULL lantern (the fire-skull pixels are gone) at the same asset key/path so the secret-alcove flicker light survives (POL-05)."
    - "The re-baked sprite still matches LIGHT_RE (/lantern|lamp|candle/) so the flicker + alcove-link branch still fires."
    - "The asset-manifest gate passes (the sprite path exists on disk)."
  artifacts:
    - assets/props/prop-swamp-lantern.png
    - scripts/build-art-assets.py
  key_links:
    - "The prop key must keep matching LIGHT_RE in build.js:490 so the alcove-light link + flicker survive the swap."
    - "The output path must stay stable so src/assets-manifest.js and main.js's loadSprite need no change (level data keeps referencing the same key)."
---

<objective>
Bake a non-skull lantern into the `prop-swamp-lantern` asset (the L1/L2 secret-alcove light is currently a fire-skull). This is the ASSET half of POL-05 — the level-data swap/confirmation lands in plan 39-04. Keeping the same output key/path means zero manifest/loader/level-data churn and a guaranteed-intact alcove-light link; only the pixels change from skull → hanging lantern.

Purpose: The user's locked decision (2026-07-19 #1): swap the skull for a lantern, KEEP the secret-alcove flicker light (MECH-05).
Output: A re-baked `assets/props/prop-swamp-lantern.png` (non-skull), plus manifest/credits consistency.

Spec-less probe fallback (VISIBLE SKIP): no SPEC/probe predicates; truths derived from POL-05 text + CONTEXT.md decision #1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/CONTEXT.md
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md
@.claude/CLAUDE.md
@scripts/build-art-assets.py
</context>

## Artifacts this phase produces (declared for source-grounding)
- `assets/props/prop-swamp-lantern.png` — re-baked from a NON-SKULL source (skull pixels removed; key + path stable). Declared so the source-grounding pass does not flag the content change as drift.

<tasks>

<task type="auto">
  <name>Task 1: Re-bake prop-swamp-lantern from a non-skull source</name>
  <read_first>
    - scripts/build-art-assets.py:1520 (bake_prop signature) AND :1571-1583 (the CURRENT skull bake `prop-swamp-lantern` from fire-skull.png — the call to change) AND :1608 (the church-candle `cemetery-lantern` bake — a proven non-skull light crop to model on)
    - src/levels/build.js:490 (LIGHT_RE = /lantern|lamp|candle/ — the swapped art MUST keep matching this)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (POL-05 (a) skull→light)
  </read_first>
  <action>
    Change the `prop-swamp-lantern` bake at build-art-assets.py:1571-1583 to source a NON-SKULL hanging lantern/candle (reuse the church-candle crop pattern at :1608, e.g. `bake_prop("swamp-lantern", church_ts, crop=(...))`, or another curated CC0 lantern source already vendored under assets/). Keep the OUTPUT name `prop-swamp-lantern` (so key/path stay stable and level data + manifest + loader are untouched). Preserve a small warm-toned lantern glow silhouette so it still reads as a light. Update CREDITS.md / assets/LICENSES if the source changed. Do NOT rename the key. Run the bake to regenerate the PNG.
  </action>
  <verify>
    <automated>node scripts/check-assets-manifest.mjs && bash scripts/check-terrain-atlas.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-assets-manifest.mjs` green (assets/props/prop-swamp-lantern.png exists on disk).
    - `bash scripts/check-terrain-atlas.sh` green (build-art-assets.py change did not break the terrain atlas pixel gate).
    - The bake call at build-art-assets.py:1571-1583 no longer references `fire-skull` (source assertion: `grep -c 'fire-skull' scripts/build-art-assets.py` reflects the removed/retargeted swamp-lantern source).
    - The output key remains `prop-swamp-lantern` (still matches LIGHT_RE) — grep confirms the name is unchanged.
  </acceptance_criteria>
  <done>prop-swamp-lantern.png shows a lantern, not a skull; key/path stable; manifest + atlas gates green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off — lantern reads correctly + alcove flicker</name>
  <action>Pause for a human verification gate (no code change) — present the re-baked lantern and the in-game alcove light for sign-off per the steps below before closing the plan.</action>
  <what-built>Re-baked `assets/props/prop-swamp-lantern.png` — the L1/L2 secret-alcove light is now a hanging lantern, not a fire-skull, with the alcove-light key/path unchanged.</what-built>
  <how-to-verify>
    1. Open `assets/props/prop-swamp-lantern.png` — confirm it reads as a lantern/candle light, NOT a skull, and is dark-grunge (no pink, no bubbly/childish look).
    2. Serve the game (`python3 -m http.server 8000` from repo root → `http://localhost:8000/src/index.html?debug=1`), enter Level 1, walk to x~320 — confirm the light prop above the secret alcove is the lantern and still flickers.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what still looks wrong (skull-ish, off-palette, wrong glow).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser localStorage → game | Only external surface; no network/backend/auth. |

## STRIDE Threat Register (proportionate — ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `localStorage` progress key | low | accept | Guarded `src/progress.js` seams already mitigate. This plan bakes a static PNG only — no new surface. |

No package-manager installs (Pillow already vendored). No new external threat surface.
</threat_model>

<verification>
- `node scripts/check-assets-manifest.mjs` green.
- `bash scripts/check-terrain-atlas.sh` green.
- Human sign-off that the lantern reads correctly and the alcove light flickers.
</verification>

<success_criteria>
- The skull art is gone; the L1/L2 alcove light is a lantern at the stable key/path; alcove flicker survives.
</success_criteria>

<output>
Create `.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-02-SUMMARY.md` when done.
</output>
