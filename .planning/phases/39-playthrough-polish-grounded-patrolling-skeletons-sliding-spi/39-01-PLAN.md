---
phase: 39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.js
  - src/levels/build.js
  - scripts/check-geometry-frozen.mjs
  - scripts/lib/mechanic-drive.mjs
  - scripts/lib/reachability.mjs
  - scripts/validate-levels.mjs
autonomous: true
requirements: [POL-02, POL-04]
must_haves:
  truths:
    - "A spike authored under geometry.slidingSpikes slides horizontally along the ground (dt-based raised-cosine, no timer) and respawns the player on contact via the existing spike seam (POL-02)."
    - "A prop authored with solid:true blocks the player (static collider sized from a config/descriptor primary source, at play depth) while default props stay collider-free (POL-04)."
    - "geometry.slidingSpikes is stripped from the frozen hash (EXEMPT) so placement tuning never trips check-geometry-frozen."
    - "The walk-only audit driver and reachability checker know BOTH a sliding spike AND a solid prop exist — so a level with either still validates (clearability sees the solid prop) and can be crossed (the driver clears a jump-over solid prop instead of stalling)."
  artifacts:
    - src/levels/build.js
    - src/config.js
    - scripts/check-geometry-frozen.mjs
    - scripts/lib/mechanic-drive.mjs
    - scripts/lib/reachability.mjs
    - scripts/validate-levels.mjs
  key_links:
    - "slidingSpike loop tags the entity 'spike' → reuses game.js:269 player.onCollide('spike') respawn seam (ZERO new game.js wiring)."
    - "check-geometry-frozen.mjs destructure must strip slidingSpikes exactly like movers/patrollers."
    - "Solid props are TOP-LEVEL props[] (invisible to solidBoxes(geometry) and to driveToXPlanned(page,x,geometry) today) → both the reachability model (solidBoxes) and the shared walk-only driver (mechanic-drive.driveToXPlanned) must learn about them, else 39-05's browser-boot acceptance is unachievable (the prop body({isStatic:true}) physically stalls the driver → far-end-unreachable)."
---

<objective>
Extend the ONE builder (`src/levels/build.js`) and its config/harness with two net-new capabilities — the POL-02 sliding-spike moving hazard and the POL-04 opt-in solid prop — plus the gate/harness awareness both need. This is the CODE FOUNDATION wave-1 plan: it ships no level-data placement (that lands in the data plans 39-04..39-07). Building the mechanics first means the level plans can author `slidingSpikes` and `solid:true` against a builder that already understands them.

Purpose: A walk-through with cosmetic hazards becomes real platforming (per the user's 2026-07-19 kid-playthrough feedback) — but only if the builder can render a moving spike and a solid crate, the frozen gate ignores the new motion key, and the automated drivers can cross/see the new hazards.
Output: `slidingSpike` builder loop + config, `pr.solid` builder branch + config (collider sized from a config/descriptor PRIMARY source), `slidingSpikes` added to the frozen-hash strip, sliding-spike AND solid-prop awareness in `mechanic-drive.mjs` (driveToXPlanned stall-recovery jump) + `reachability.mjs` (solidBoxes models solid props) + `validate-levels.mjs` (captures + passes solid props).

Spec-less probe fallback (VISIBLE SKIP): this phase has no SPEC file and POL-01..05 are not registered requirement IDs, so no probe-derived edge/prohibition predicates were generated. `must_haves.truths` are derived from the ROADMAP Phase 39 goal + POL requirement text + CONTEXT.md locked decisions instead.

<assumption_delta_decision>
The assumption-delta detector fired (`detected: true`) on POL-02 introducing a SECOND spike variant (static → static + sliding). Decision: **add-alongside** — static spikes (`geometry.spikes`) remain unchanged; the sliding spike is a new MOTION variant under `geometry.slidingSpikes` that shares the SAME `"spike"` tag and the SAME respawn seam (`game.js:269`). Rationale: both are legitimate distinct hazards; the shared tag keeps the respawn contract single-sourced (no second `onCollide` wire). Accepted, low-risk (NOT debt) — the shared tag is the deliberate coupling point, not an accident.
</assumption_delta_decision>
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/CONTEXT.md
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md
@.claude/CLAUDE.md
@src/levels/build.js
@src/config.js
@scripts/check-geometry-frozen.mjs
</context>

## Artifacts this phase produces (declared for source-grounding — these are NEW symbols)
- `geometry.slidingSpikes` — new EXEMPT level-descriptor motion key. Shape: `{ x1, y1, x2, y2, period? }`.
- `CONFIG.SLIDING_SPIKE` — new config block, at minimum `{ PERIOD_S }` (default full round-trip period). Hitbox reuses `CONFIG.SPIKE_SIZE` / `SPIKE_HITBOX_W` / `SPIKE_HITBOX_H`.
- The `slidingSpike` loop in `build.js` — new builder branch (spike sprite + tightened `area()` tagged `"spike"` + `onUpdate`/`dt()` raised-cosine; NO `body()`).
- `CONFIG.PROPS.SOLID_Z` — new config value (`0`) placing a solid prop at play depth.
- `CONFIG.PROPS.SOLID_W` / `CONFIG.PROPS.SOLID_H` — new config default collider box for a solid prop (PRIMARY sizing source; magic-number-free).
- The `pr.solid` branch in `build.js` props loop — new opt-in branch (adds `area()` + `body({ isStatic: true })` sized from the descriptor/config primary source; raises `z` to `CONFIG.PROPS.SOLID_Z`). Prop descriptor gains an opt-in `solid: true` (+ optional per-prop `solidW`/`solidH` override).
- `solidBoxes(geometry, solidProps = [])` — EXTENDED signature in `reachability.mjs` (now also models solid top-level props as solid boxes); `checkLevelReachability(geometry, envelope, solidProps = [])` / `planCoinWitnesses(geometry, envelope, solidProps = [])` thread the solid props; `validate-levels.mjs:62` captures top-level `props` and passes the `solid:true` subset.
- Stall-recovery jump-over-solid in shared `mechanic-drive.driveToXPlanned` — new reactive branch: benefits browser-boot.mjs AND audit-phase21-mechanics.mjs via IMPORT (single shared source — no copied-code duplication).

<tasks>

<task type="auto">
  <name>Task 1: POL-02 sliding-spike builder loop + config</name>
  <read_first>
    - src/levels/build.js:243-260 (spike entity: tightened hitbox — Analog A) AND src/levels/build.js:530-570 (mover raised-cosine oscillation — Analog B, the motion pattern to copy)
    - src/config.js:154-158 (SPIKE_SIZE/HITBOX) AND src/config.js:279-286 (CONFIG.MOVER, PERIOD_S pattern)
    - src/scenes/game.js:269 (the "spike" → respawn seam this reuses — DO NOT edit)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (POL-02 section, "Composed target")
  </read_first>
  <action>
    Add `CONFIG.SLIDING_SPIKE = { PERIOD_S: 3 }` to src/config.js (a config comment explaining dt raised-cosine, per-slidingSpike `period` override allowed; no magic number in build.js). Reuse existing `CONFIG.SPIKE_SIZE`(16)/`SPIKE_HITBOX_W`(12)/`SPIKE_HITBOX_H`(8) for the hitbox.
    In build.js, immediately AFTER the static spike loop (build.js:250-260), add a `for (const s of g.slidingSpikes ?? [])` loop that COMPOSES: the spike sprite + the SAME tightened `area({ shape: new Rect(vec2(0), SPIKE_HITBOX_W, SPIKE_HITBOX_H), offset: vec2(spikeOffX, spikeOffY) })` tagged `"spike"` (reuse the existing spikeOffX/spikeOffY), PLUS the mover's `onUpdate`+`dt()` raised-cosine from build.js:562-569 to move `pos` between `(s.x1,s.y1)`↔`(s.x2,s.y2)` with period `s.period ?? CONFIG.SLIDING_SPIKE.PERIOD_S`. Use a closure-local `let t = 0` PER entity (anti-leak). NO `body()` — a spike is a hazard trigger, not a standable ledge (the ONE structural difference from a mover). The `area()` tracks automatically because it is pos-relative.
    Guard with `?? []` so levels without the key still build. Do NOT touch the static spike loop, game.js, or add a second onCollide wire.
  </action>
  <verify>
    <automated>bash scripts/check-safety.sh && bash scripts/check-import-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-safety.sh` green (no setTimeout/setInterval/wait/loop/lifespan — motion is `onUpdate`+`dt()` only).
    - `bash scripts/check-import-safety.sh` green (no engine global at module top level; the loop body is inside the builder function).
    - Source assertion: `grep -c 'slidingSpikes' src/levels/build.js` ≥ 1 AND the loop tags the entity `"spike"` AND contains no `body(` inside the slidingSpike loop block.
    - Source assertion: `grep -c 'SLIDING_SPIKE' src/config.js` ≥ 1.
  </acceptance_criteria>
  <done>build.js renders a horizontally-sliding, `"spike"`-tagged, body-less hazard from `geometry.slidingSpikes`; config holds the period; safety + import gates green.</done>
</task>

<task type="auto">
  <name>Task 2: POL-04 opt-in solid-prop branch + config</name>
  <read_first>
    - src/levels/build.js:463-528 (props loop; header :467-469 mandates NO area/body as the DEFAULT — keep it)
    - src/levels/build.js:204-234 (static-collider component stack to graft on: rect+pos+area+body({isStatic:true}))
    - src/config.js:116-119 (CONFIG.PROPS.Z_SURFACE/Z_BACK — both negative)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (POL-04 section)
  </read_first>
  <action>
    Add to src/config.js: `CONFIG.PROPS.SOLID_Z = 0` (comment: solid props sit at play depth, NOT the negative decoration depth, so they visually block) AND `CONFIG.PROPS.SOLID_W` / `CONFIG.PROPS.SOLID_H` — a default collider box for a solid prop (e.g. a barrel/crate footprint ~24×24; tune to the town sprite sizes).
    In the props loop (build.js:492-528), when `pr.solid === true`: add `area()` + `body({ isStatic: true })` and `z(CONFIG.PROPS.SOLID_Z)`. Size the collider from a PRIMARY source that is available synchronously at build time (Kaplay loads sprites async, so the loaded sprite's width/height may NOT be readable inside build.js at entity construction — do NOT depend on it as the source of truth). Primary sizing order: per-prop `pr.solidW`/`pr.solidH` if set, else `CONFIG.PROPS.SOLID_W`/`SOLID_H`. Runtime sprite-dimension introspection is at most a FALLBACK, never the primary source. No magic numbers (all dims come from config or the descriptor). Keep the default (no `solid`) branch collider-free and negative-z — do NOT regress the whole layer. A solid prop must NOT be a light (do not combine with the flicker branch).
  </action>
  <verify>
    <automated>bash scripts/check-safety.sh && bash scripts/check-import-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'pr.solid' src/levels/build.js` ≥ 1 AND the solid branch contains `body({ isStatic: true })` and `CONFIG.PROPS.SOLID_Z`.
    - Source assertion: the solid branch's collider dims come from `pr.solidW`/`pr.solidH` or `CONFIG.PROPS.SOLID_W`/`SOLID_H` (config/descriptor PRIMARY) — no bare numeric literal for width/height, and NOT gated on a synchronous read of the loaded sprite.
    - Source assertion: default props (no `solid`) still add NO area/body — the header invariant at build.js:467-469 holds for the non-solid path.
    - `grep -c 'SOLID_Z' src/config.js` ≥ 1 AND `grep -c 'SOLID_W' src/config.js` ≥ 1.
    - `bash scripts/check-import-safety.sh` green.
  </acceptance_criteria>
  <done>A prop with `solid:true` gets a static collider at play depth sized from a config/descriptor primary source (not async sprite dims); default props remain pure sprite+pos+z; gates green.</done>
</task>

<task type="auto">
  <name>Task 3: Gate + harness awareness of BOTH new capabilities (sliding spike + solid prop)</name>
  <read_first>
    - scripts/check-geometry-frozen.mjs:55-64 (the `const { movers, patrollers, ...frozen } = getLevel(id).geometry` strip — line ~60)
    - scripts/lib/mechanic-drive.mjs:43-60 (deriveEncounters — the movers/patrollers idx-keyed emits at :59-60), :369 (driveToXPlanned signature + its walk/jump/stall loop), :386,:415,:650 (spike-hop fire window / hold logic), and resolveIfBoxed (barrier-stall handler the solid-prop recovery must NOT collide with)
    - scripts/lib/reachability.mjs:571 (solidBoxes(geometry) — models ONLY geometry.floors + geometry.platforms), :868 (planCoinWitnesses), :954 (checkLevelReachability), :~568 (spikes treated as passable/non-blocking)
    - scripts/validate-levels.mjs:62 (descriptor built as `{ id, biome, geometry }` — DROPS top-level props) and :100 (`checkLevelReachability(descriptor.geometry)`)
    - docs/LEVEL-DESIGN.md (jump envelope ~88px — a solid prop must be jump-clearable)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (POL-02 "Harness must learn the new hazard" + POL-04 clearability)
  </read_first>
  <action>
    SLIDING SPIKE (POL-02):
    - check-geometry-frozen.mjs ~:60 → `const { movers, patrollers, slidingSpikes, ...frozen } = getLevel(id).geometry;` so slidingSpikes is STRIPPED (EXEMPT).
    - mechanic-drive.mjs deriveEncounters() (:43-60) → add `...(geometry.slidingSpikes ?? [])` idx-keyed emits mirroring :59-60, mapped to the spike-hop fire path (reuse FIRE_WINDOW.spike / spikeJumpHoldMs at :386/:415/:650).
    - reachability.mjs ~:568 → add `slidingSpikes` to the spike passable-collection + a short comment that it can never make a coin/route permanently unreachable (walkable between sweeps).

    SOLID PROP (POL-04) — the clearability half; solid props are TOP-LEVEL props[], invisible to solidBoxes(geometry) and to driveToXPlanned(...,geometry) today:
    - reachability.mjs: EXTEND `solidBoxes(geometry, solidProps = [])` to also push a solid AABB box per solid prop (x0=pr.x, using the same CONFIG.PROPS.SOLID_W/H / pr.solidW/H sizing as build.js Task 2, so the model matches the built collider). Thread an optional `solidProps` through `planCoinWitnesses(geometry, envelope, solidProps = [])` and `checkLevelReachability(geometry, envelope, solidProps = [])` down to the solidBoxes call.
    - validate-levels.mjs: at :62 also capture the level's top-level `props`, and at :100 pass its `solid:true` subset: `checkLevelReachability(descriptor.geometry, JUMP_ENVELOPE, (level.props ?? []).filter(p => p.solid))`. (Keep the default empty so a level with no solid props behaves identically.)
    - mechanic-drive.mjs `driveToXPlanned` (:369): add a REACTIVE stall-recovery — when the walk-only driver is grounded, driving forward, and x fails to advance for a small threshold while NOT explained by resolveIfBoxed (math-gate/enemy) and NOT at goal, attempt ONE envelope-bounded jump (≤ the docs/LEVEL-DESIGN.md ~88px jump reach) to clear a jump-over solid prop, then resume the walk. This lives in the SHARED lib and is inherited by browser-boot.mjs AND audit-phase21-mechanics.mjs via their existing `import` — apply it ONCE here; do NOT copy into either harness script (the shared-lib import, not copied code, is what carries it — so the Playwright-duplication rule is satisfied without touching those files).
  </action>
  <verify>
    <automated>node scripts/check-geometry-frozen.mjs && node scripts/validate-levels.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'slidingSpikes' scripts/check-geometry-frozen.mjs` ≥ 1 (in the destructure) AND `grep -c 'slidingSpikes' scripts/lib/mechanic-drive.mjs` ≥ 1 AND `grep -c 'slidingSpikes' scripts/lib/reachability.mjs` ≥ 1.
    - Solid-prop awareness (mirrors the slidingSpikes assertion): `grep -c 'solidProps' scripts/lib/reachability.mjs` ≥ 1 AND `solidBoxes` accepts a second arg AND `grep -c 'solid' scripts/validate-levels.mjs` ≥ 1 (validate-levels now captures + passes solid props).
    - `grep -Eic 'solid|stall' scripts/lib/mechanic-drive.mjs` shows the driveToXPlanned stall-recovery jump branch exists; no new `import` of mechanic-drive added to browser-boot.mjs/audit-phase21-mechanics.mjs (the fix is in the already-imported shared lib) — `git diff --name-only` for this plan does NOT list browser-boot.mjs or audit-phase21-mechanics.mjs.
    - `node scripts/check-geometry-frozen.mjs` GREEN (strip key is hash-neutral; no level authors slidingSpikes yet).
    - `node scripts/validate-levels.mjs` zero HARD-FAIL (solidProps default empty → no level regresses; the plumbing is inert until a level sets solid:true).
  </acceptance_criteria>
  <done>The frozen gate strips slidingSpikes; the shared driver + reachability recognise BOTH a sliding spike AND a solid prop (clearability sees solid props via validate-levels plumbing; the driver jumps a jump-over solid prop instead of stalling); gates green with no level yet using either key. This is the capability 39-05's L3/L4 browser-boot acceptance depends on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser localStorage → game | The ONLY external surface: player progress read from `localStorage`. No network, no backend, no auth, no server. |

## STRIDE Threat Register (proportionate — ASVS L1, offline localStorage-only static game)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `localStorage` progress key | low | accept | Already mitigated by the guarded `src/progress.js` seams (never throw, forgiving defaults). This phase touches builder/config/harness only — introduces no new storage surface. No new mitigation required. |

No package-manager installs in this phase (zero new dependencies) → no supply-chain (T-39-SC) surface. No new external threat surface introduced.
</threat_model>

<verification>
- `bash scripts/check-safety.sh`, `bash scripts/check-import-safety.sh`, `bash scripts/check-gate.sh` green.
- `node scripts/check-geometry-frozen.mjs` green (strip-key addition is hash-neutral).
- `node scripts/validate-levels.mjs` zero HARD-FAIL.
</verification>

<success_criteria>
- build.js can render a sliding spike and a solid prop; config holds their tunables.
- slidingSpikes is EXEMPT from the frozen hash; the walk-only driver + reachability know the hazard.
- All gates green with no level yet authoring the new keys.
</success_criteria>

<output>
Create `.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-01-SUMMARY.md` when done.
</output>
