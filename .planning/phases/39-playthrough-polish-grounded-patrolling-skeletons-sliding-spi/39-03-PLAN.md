---
phase: 39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/browser-boot.mjs
  - scripts/audit-phase21-mechanics.mjs
autonomous: true
requirements: [POL-03]
must_haves:
  truths:
    - "The walk-only spawn→goal driver (browser-boot.mjs) can board a moving platform, ride it, and step off — so once a mover spans a real hole (plan 39-07) it can still cross without a softlock (POL-03)."
    - "audit-phase21-mechanics.mjs rides a mover for its mover encounters (mount → ride → step-off) using the same ported recipe."
    - "The mover-ride logic is copied IDENTICALLY into each duplicated harness copy by hand (no shared-module extraction — CLAUDE.md)."
  artifacts:
    - scripts/browser-boot.mjs
    - scripts/audit-phase21-mechanics.mjs
  key_links:
    - "The ride recipe is ported from scripts/lib/mechanic-drive.mjs:844+ driveAndDetectMover (walk to a staging point left of x1, mount, ride, detect grounded platform-moving frames)."
    - "Playwright script duplication is deliberate — the SAME fix goes in every copy by hand."
---

<objective>
Teach the WALK-ONLY automated drivers to RIDE a moving platform. `browser-boot.mjs` (the spawn→goal driver) presses ArrowRight and holds — today it cannot board a mover, so the moment plan 39-07 relocates a mover over a real killing pit (removing the static stepping-stones), an un-taught driver would fall to respawn forever and the no-softlock proof would break. This wave-1 plan builds the RIDE CAPABILITY ahead of the level-data edit that needs it. `audit-phase21-mechanics.mjs` already dispatches mover encounters but must use the ported ride recipe too.

Purpose: POL-03's core promise (movers over real holes) is only verifiable if the walk-only harness can cross a mover-bridged pit. Capability first, data second.
Output: Mover mount/ride/step-off logic in browser-boot.mjs and audit-phase21-mechanics.mjs, ported verbatim from the proven `driveAndDetectMover` recipe.

Spec-less probe fallback (VISIBLE SKIP): no SPEC/probe predicates; truths derived from POL-03 text + CONTEXT.md decision #3 (harness update).

NOTE: full cross-a-real-hole verification cannot happen here (no level has a mover over a real hole until plan 39-07). This plan verifies ride CAPABILITY against an EXISTING mover level; the real-hole crossing is verified end-to-end in plan 39-08.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/CONTEXT.md
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md
@.claude/CLAUDE.md
@scripts/browser-boot.mjs
</context>

## Artifacts this phase produces (declared for source-grounding)
- A mover mount/ride/step-off routine added to `scripts/browser-boot.mjs` (new — the walk-only driver had none).
- The same ride recipe applied in `scripts/audit-phase21-mechanics.mjs` (mover-encounter path uses the ported logic).

<tasks>

<task type="auto">
  <name>Task 1: Port the mover-ride recipe into browser-boot.mjs</name>
  <read_first>
    - scripts/lib/mechanic-drive.mjs:844+ (driveAndDetectMover — the RIDE recipe: walk to a staging point left of x1, mount, ride, detect grounded platform-moving frames) — READ, do NOT edit (leave the shared lib change to plan 39-01's ownership boundary; copy the logic BY HAND)
    - scripts/browser-boot.mjs (the walk-only spawn→goal driver: driveToXPlanned / planTakeoffs / the ArrowRight-hold loop) — the file to edit
    - src/config.js:279-286 (CONFIG.MOVER — WIDTH 48, PERIOD_S 4 — mover geometry the ride must account for)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (POL-03 "Harness — teach the drivers to RIDE a mover")
  </read_first>
  <action>
    In browser-boot.mjs, add a mover-ride branch to the spawn→goal drive: when the planned path reaches a gap that is bridged only by a `geometry.movers` entry (both floor edges are real pit, no static platform between), walk to a staging point just left of the mover's near endpoint, WAIT for the mover to arrive (poll its x each frame), board it (stand — the engine `body()` stickToPlatform carries the rider), ride until the far endpoint is reachable, then resume the ArrowRight walk. Copy the mount/ride detection (grounded + platform-moving frames) from mechanic-drive.mjs:844+ VERBATIM by hand — do NOT import or extract a shared module (CLAUDE.md: Playwright duplication is deliberate). Keep the existing walk-only path for levels with no mover-bridged pit.
  </action>
  <verify>
    <automated>node scripts/browser-boot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/browser-boot.mjs` GREEN across all 8 levels (no regression — no level has a mover-over-hole yet, so the walk path still succeeds; the new branch is inert until a mover bridges a real pit).
    - Source assertion: browser-boot.mjs contains the mount/ride logic (`grep -c 'mover' scripts/browser-boot.mjs` ≥ 1 and a ride/mount block exists).
    - No `import` of mechanic-drive.mjs added (duplication is by-hand; `grep -c "mechanic-drive" scripts/browser-boot.mjs` stays 0).
  </acceptance_criteria>
  <done>browser-boot.mjs can ride a mover across a pit; the walk-only path is unchanged for non-mover levels; boot green on all 8 levels.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the same ride recipe in audit-phase21-mechanics.mjs</name>
  <read_first>
    - scripts/audit-phase21-mechanics.mjs:292-297 (existing mover-encounter dispatch) and its server/guard/drive block
    - scripts/lib/mechanic-drive.mjs:844+ (the ride recipe — same source as Task 1)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (Harness-parity mandate)
  </read_first>
  <action>
    In audit-phase21-mechanics.mjs, ensure the mover encounter path (:292-297) drives the mount → ride → step-off recipe (ported identically by hand from mechanic-drive.mjs:844+, matching Task 1's copy exactly — same variable names, same detection). Do NOT extract a shared module. The goal: every mover encounter reports `triggered: true` (a `resolved: false` row remains acceptable known headless-timing flakiness per CLAUDE.md).
  </action>
  <verify>
    <automated>node scripts/audit-phase21-mechanics.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/audit-phase21-mechanics.mjs` runs; every mover encounter shows `triggered: true`.
    - The ride block in audit-phase21-mechanics.mjs is byte-equivalent in logic to browser-boot.mjs's (parity mandate) — verified by eye during review.
    - No shared-module import added.
  </acceptance_criteria>
  <done>The interactive audit rides movers via the same ported recipe; all mover encounters trigger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser localStorage → game | Only external surface; no network/backend/auth. |

## STRIDE Threat Register (proportionate — ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `localStorage` progress key | low | accept | Guarded `src/progress.js` seams already mitigate. This plan edits test harness scripts only — no runtime/storage surface. |

No package-manager installs (Playwright already resolved). No new external threat surface.
</threat_model>

<verification>
- `node scripts/browser-boot.mjs` green on all 8 levels (ride branch inert until a mover bridges a real pit).
- `node scripts/audit-phase21-mechanics.mjs` — mover encounters trigger.
</verification>

<success_criteria>
- Both walk-only drivers can board, ride, and step off a moving platform; the recipe is duplicated identically by hand; no regression on the current (stepping-stone-bridged) levels.
</success_criteria>

<output>
Create `.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-03-SUMMARY.md` when done.
</output>
