# Phase 39 — Resume Handoff (paused 2026-07-20)

**Status:** EXECUTING — 7 of 8 plans complete; **39-08 is BLOCKED at its human playthrough sign-off**. The user played it, found **"a lot of issues"** (acknowledged "a lot of progress" too), and paused to start a fresh session. The phase is NOT closed and must not be marked complete.

## FIRST THING NEXT SESSION
The specific playthrough issues were **not enumerated** before the pause. Before fixing anything, **ask the user to list the issues they saw** (or do a fresh playthrough to surface them): `python3 -m http.server 8000` from repo root → `http://localhost:8000/src/index.html` (`?debug=1` for colliders). Then fix via `/gsd-quick` (small) or `/gsd-debug` (mechanic bugs), and only then re-open 39-08's sign-off.

## What is committed (all on `main`, tree clean)
| Plan | What | Commits |
|------|------|---------|
| 39-01 | Sliding-spike + solid-prop builder foundation + harness/validator awareness | `10ce1e2` `93a98c5` `65cc305` `7d49f2c` |
| 39-02 | Non-skull lantern re-bake (POL-05 asset), alcove-light link preserved — human-approved | `76baffe` `637f699` |
| 39-03 | browser-boot wired to shared `driveToMover` (POL-03 harness) | `2970525` `1467548` `08bf73f` |
| 39-04 | L1/L2 grounded skeletons + lantern link (POL-01/05) | `050b4d5` `9074701` |
| 39-05 | L3/L4 grounded skeletons, 5/6 solid props, well off spike, L4 mover widen | `848a9e5` `d55578c` `87db7a8` |
| 39-06 | L5/L6/L7 grounded skeletons, sliding spikes L5+L7, L7 mover widen — **found+fixed the frozen-`patrol()` engine bug** | `938faae` `ad3d3e6` `78f933d` |
| 39-07 | L8 movers over real moat+chasm, stepping-stones removed, columns repositioned (+ reachability/route-planner ride edge) | `70b2b07` `9788e02` `248d6f2` `34341ab` `7c20a69` |
| 39-08 | Task 1 frozen re-baseline (L8-only) + Task 2 full gate suite. **Task 3 (human sign-off) NOT done.** | `71137e7` |
| harness | Audit-phase21 re-tuned for node v24 + moving targets (Fable escalation) | `6bae6d1` |
| — | 39-PATTERNS.md tracked | `806955d` |

39-01..39-07 each have a `-SUMMARY.md`. **39-08 has NO SUMMARY** (correctly — it's paused at the checkpoint).

## Root-cause win (why this phase matters)
The kid's "stationary skeleton" report was **not** placement/tuning — the vendored Kaplay 3001.0.19 `patrol()` inits its finished flag as `waypoints != null`, so **every patroller had been frozen at x1 since Phase 36**. Fixed in `src/levels/build.js` (re-assign `foe.waypoints` through the setter), commit `938faae`. Skeletons now actually walk.

## Gate status at pause
- `check-geometry-frozen.mjs`: **GREEN** (re-baselined for the L8 stepping-stone removals, diff confirmed L8-only).
- `validate-levels.mjs`: PASS (0 HARD-FAIL). `browser-boot.mjs`: PASS (all 8 levels, rides both L8 ferries).
- `check-gate / safety / import-safety / progress / assets-manifest / terrain-atlas`: PASS.
- `audit-phase21-mechanics.mjs`: substantially fixed (`6bae6d1`). Isolated L1/L5/L8 = **18/18 triggered+resolved**. Full 8-level run reached verdict (45/47 triggered, 43/47 resolved); the **4 residual rows (L4 mover@4460, L7 mover@4450 untriggered; L8's two ferries triggered-but-unresolved)** coincided with a concurrent browser-boot Chromium dragging fps → skewed phase math. **Re-verify on a QUIET machine, sequentially** — no env override needed anymore. If L4/L7 still fail untriggered, investigate their takeoff surroundings (L4 has solid props, L7 a 60px deck). `AUDIT_LEVELS=level-04,level-07` bounds the diagnosis.

## Outstanding to close the phase
1. Capture + fix the user's playthrough issues (unknown specifics — ask first).
2. Re-run 39-08's human playthrough sign-off (Task 3) + the **Phase-38 VER-02 kid-UAT re-run** (now covers the revised platforming).
3. Quiet-machine sequential re-verify of `audit-phase21-mechanics.mjs` + `browser-boot.mjs`.
4. Then close 39-08 (write SUMMARY, mark POL-01..05, update STATE/ROADMAP), and run phase verification.

## Model guidance (user directive)
**Opus struggled on this game's mechanic debugging; Fable found the root causes** (the `patrol()` bug and the 3 audit-harness root causes). Per the user, **escalate hard bugs to Fable** — run the executor on Fable or spawn a Fable debug subagent. Watch Fable session limits (it hit one mid-run; a fresh Fable context in another session still worked). See memory `escalate-to-fable-on-issues`.

## Notes
- `NOT PUSHED`: local `main` is well ahead of `origin/main`. Consider pushing before a dev-host switch (off-machine backup).
- Pre-existing untracked strays (NOT ours — leave alone): `.planning/phases/26-*/`, `assets/enemy-{1,2,3}.png`.
- No `.planning/config.json` change needed. `branching_strategy: none` — all work is on `main`.
