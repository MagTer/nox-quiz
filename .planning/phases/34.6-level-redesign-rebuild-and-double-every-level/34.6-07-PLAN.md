---
phase: 34.6-level-redesign-rebuild-and-double-every-level
plan: 07
type: execute
wave: 4
depends_on: [34.6-04]
files_modified:
  - src/levels/level-06.js
autonomous: true
requirements: [LEN-01, LEN-02]
must_haves:
  truths:
    - "level-06 is REBUILT from scratch at ~2x length as the intense + vertical CEMETERY (even archetype)"
    - "level-06 places an OPTIONAL math-skip key on a high route (geometry.keys, NO geometry.locks), collectable by a driven player in a walked stretch"
    - "A driven player who holds level-06's key clears it with full XP and no end math; one who does not gets the end math"
    - "level-06 uses the L3–6 band with real ceilings where present (overlapping tiers rise 72–75px, headroom ≥24); every coin collectable; completable from spawn; density 1 door + 1 enemy + end gate"
  artifacts:
    - "src/levels/level-06.js — rebuilt intense/vertical cemetery descriptor with a math-skip key and NO locks; bounds.right hand-bumped"
  key_links:
    - "level-06 geometry.keys (no locks) → game.js heldKeyIds → clearLevel key path"
    - "audit-endgate-key.mjs auto-covers level-06 (keys && !locks): Path A key→free clear, Path B no-key→math"
---

<objective>
Rebuild `src/levels/level-06.js` from scratch as the intense + vertical CEMETERY (even archetype) at ~2x length, to the checkpoint-proven shape, carrying an OPTIONAL math-skip key on a high route (no lock). Cemetery is the L3–6 band, so real ceilings may appear here (overlapping tiers with headroom ≥24). Even level → intense/vertical + key. Pure-data rebuild; append-only suspended. Level-06 has NO smoke fixture (only 01–04), so no re-baseline.

The two-path audit (audit-endgate-key.mjs, built in 34.6-03) auto-discovers level-06 once it has keys-without-locks and must prove BOTH end-gate paths for it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-PATTERNS.md
@docs/LEVEL-DESIGN.md
@src/levels/level-06.js
@src/levels/level-02.js
@src/levels/level-08.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild level-06 (cemetery, intense + vertical, math-skip key, L3–6 band)</name>
  <files>src/levels/level-06.js</files>
  <read_first>
    - src/levels/level-06.js — the file being rebuilt (discard current geometry, do NOT append). Preserve id "level-06", allowedTables [4,5,6,7], biome "cemetery", explicit `bounds` (hand-bump bounds.right).
    - src/levels/level-02.js (rebuilt) and src/levels/level-08.js — the EVEN/intense analogs (tall tiers, per-tier checkpoints, key in a walked stretch). Use their shape; h:16 tiers.
    - docs/LEVEL-DESIGN.md — §3.1 h:16, §3.2 headroom (rise ≥72 for overlapping tiers), §3.3 rise bands, §3.4 grid trap, §3.5 verticality/overlap/arc hygiene (~70px x-overlap; no stepping stone inside an independently-jumpable gap), §3b coins, §4 density, §5 checkpoints, §6 alcove, §8 ramp (L3–6, ceilings appearing).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — "Level-Author Hard Constraints", "Descriptor Schema" (keys `{ x, y: FLOOR_Y - 32 }`, locks OMITTED), Pitfall 2 (bounds.right), Pitfall 3 (key in a walked stretch).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md — KEY = math-skip token; even = intense+vertical; descents; optional high route (key lives on it); VISIBLE backtracking only; checkpoint-locked A3.
  </read_first>
  <action>
    Author a brand-new level-06 geometry roughly doubling the length (current goal.x 3300 → target ~6600). Pure-data module importing ONLY ../config.js (a727c13). Preserve id "level-06", allowedTables [4,5,6,7], biome "cemetery". Carry an explicit COMPLETE `bounds` literal, bounds.right hand-bumped to goal.x + CONFIG.GOAL_SIZE + a 64–80px buffer; bounds.top 0 (or a small negative top if a climb genuinely needs it — set it to match the highest tier's headroom, mirroring level-08's convention).

    Make it intense + vertical: tall climbs + deliberate DESCENTS, with real ceilings where they appear (overlapping tiers rise 72–75px, headroom ≥24; ~70px x-overlap between consecutive climb tiers; never a stepping stone inside an independently-jumpable gap). Gaps 140–160px, EVERY platform `h: 16`, climb-tier y OFF the grid. Keep every route VISIBLE.

    Place the math-skip KEY on the OPTIONAL high route: `keys: [{ x: <in a walked stretch of the high route>, y: FLOOR_Y - 32 }]`, OMIT `locks`. Key in a stretch the driven player WALKS (Pitfall 3); the safe route reaches goal without it. Math density LOCKED: one door + one enemy (solid floor), `mathGates: []`, end goal. Every coin collectable; the high route carries extra coins; checkpoints one near spawn + one before every spike cluster (and per-tier where a climb has hazards); exactly one secret alcove. CONFIG expressions for all y-anchors.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-endgate-key.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` PASS, zero HARD-FAIL for level-06; ZERO `key-lock` rows (keys-without-locks clean); any overlapping tier passes headroom.
    - `grep -c 'locks:' src/levels/level-06.js` is 0; level-06 has exactly one `keys:` entry, y `FLOOR_Y - 32`.
    - `node scripts/browser-boot.mjs` reaches level-06 goal.x (±32); `node scripts/audit-coins.mjs` collects every coin.
    - `node scripts/audit-endgate-key.mjs` exits 0 and covers level-06: Path A key collected + no math at goal + clear marker; Path B math opens at goal.
    - `grep -c 'h: 24' src/levels/level-06.js` is 0; bounds.right hand-bumped; goal.x ~2x the old 3300; exactly one door, one enemy, `mathGates: []`, one secret alcove.
  </acceptance_criteria>
  <done>level-06 is a rebuilt, ~2x, intense/vertical cemetery with a proven optional math-skip key, clean on every gate.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6-09 | DoS (self) | softlock via unreachable/never-walked key | low | mitigate | Math-skip key is OPTIONAL (miss → normal math path, never a dead end); audit-endgate-key Path A proves collection |
| T-34.6-SC | Tampering | package installs | n/a | accept | No installs this phase |

Static offline browser game — no network/PII/auth. Geometry-only change.
</threat_model>

<verification>
Full gate suite + audit (LEVEL-DESIGN §9):
```
bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh \
  && bash scripts/check-progress.sh && bash scripts/check-terrain-atlas.sh && bash scripts/check-pink-gate.sh \
  && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs \
  && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs \
  && node scripts/audit-endgate-key.mjs
```
audit-phase21-mechanics output: level-06's door + enemy `triggered: true`. (No smoke fixture for level-06.)
</verification>

<success_criteria>
- level-06 rebuilt ~2x, intense/vertical cemetery (L3–6 band, ceilings ok), with an optional math-skip key proven both paths in-engine.
- Zero HARD-FAILs; every coin collected; completable from spawn; keys-without-locks a clean PASS.
</success_criteria>

<output>
Create `.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-07-SUMMARY.md` when done.
</output>
