---
phase: 34.6-level-redesign-rebuild-and-double-every-level
plan: 06
type: execute
wave: 4
depends_on: [34.6-04]
files_modified:
  - src/levels/level-05.js
autonomous: true
requirements: [LEN-01, LEN-02]
must_haves:
  truths:
    - "level-05 is REBUILT from scratch at ~2x length as the calmer CEMETERY intro (odd archetype)"
    - "level-05 uses the L3–6 band (140–160px gaps, mixed 60–75px rises); any overlapping tier keeps headroom ≥24"
    - "level-05 has a descent + an optional visible high route (extra coins, no key); no hidden routes"
    - "Every coin collectable in-engine; completable from spawn; math density exactly 1 door + 1 enemy + end gate"
  artifacts:
    - "src/levels/level-05.js — rebuilt cemetery-intro descriptor; bounds.right hand-bumped"
  key_links:
    - "validate-levels + audit-coins + browser-boot green on level-05 (no smoke fixture exists for 05)"
---

<objective>
Rebuild `src/levels/level-05.js` from scratch as the calmer CEMETERY intro (odd archetype) at ~2x length, to the checkpoint-proven shape. Cemetery is the L3–6 band; level-05 is the calmer of the cemetery pair (level-06 is the intense one). Odd level → NO key. Pure-data rebuild; append-only suspended. Level-05 has NO smoke-progress golden fixture (only 01–04 do), so no fixture re-baseline is needed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-PATTERNS.md
@docs/LEVEL-DESIGN.md
@src/levels/level-05.js
@src/levels/level-01.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild level-05 (cemetery intro, calm, ~2x, L3–6 band)</name>
  <files>src/levels/level-05.js</files>
  <read_first>
    - src/levels/level-05.js — the file being rebuilt (discard current geometry, do NOT append). Preserve id "level-05", allowedTables [2,3,4,5], biome "cemetery", and the explicit `bounds` field (used AS-IS — hand-bump bounds.right).
    - src/levels/level-01.js — the ODD/calm analog for the calm-intro shape.
    - docs/LEVEL-DESIGN.md — §3.1 h:16, §3.2 headroom, §3.3 rise bands, §3.4 grid trap, §3b coins, §4 density, §5 checkpoints, §6 alcove, §8 ramp (L3–6).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — "Level-Author Hard Constraints" + "Descriptor Schema".
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md — level-shape brief; checkpoint-locked A3 ceiling decision.
  </read_first>
  <action>
    Author a brand-new level-05 geometry roughly doubling the length (current goal.x 3340 → target ~6600–6800). Pure-data module importing ONLY ../config.js (a727c13). Preserve id "level-05", allowedTables [2,3,4,5], biome "cemetery". Carry an explicit COMPLETE `bounds` literal with bounds.right hand-bumped to goal.x + CONFIG.GOAL_SIZE + a 64–80px buffer; bounds.top 0.

    L3–6 band: gaps 140–160px, mixed 60–75px rises, EVERY platform `h: 16`, climb-tier y OFF the grid; any overlapping tier satisfies headroom ≥24 (rise ≥72). Keep it the CALMER cemetery level. Include a descent + an optional visible high route with extra coins (NO key). Every route VISIBLE. Math density LOCKED: one door + one enemy (solid floor), `mathGates: []`, end goal. Every coin collectable; checkpoints one near spawn + one before every spike cluster; exactly one secret alcove ~70px above an early platform. CONFIG expressions for all y-anchors.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` PASS, zero HARD-FAIL for level-05.
    - `node scripts/browser-boot.mjs` reaches level-05 goal.x (±32) from spawn; `node scripts/audit-coins.mjs` collects every level-05 coin.
    - `grep -c 'h: 24' src/levels/level-05.js` is 0; bounds.right hand-bumped; goal.x ~2x the old 3340.
    - Exactly one door, one enemy, `mathGates: []`, one secret alcove, no `keys:`/`locks:` (odd level); zero `headroom` HARD-FAIL rows.
  </acceptance_criteria>
  <done>level-05 is a rebuilt, ~2x, calm cemetery intro that validates and drives clean with every coin collectable.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6-08 | DoS (self) | softlock/dead-end from bad geometry | low | mitigate | validate-levels + browser-boot HARD gates; zero HARD-FAIL |
| T-34.6-SC | Tampering | package installs | n/a | accept | No installs this phase |

Static offline browser game — no network/PII/auth. Geometry-only change.
</threat_model>

<verification>
Full gate suite (LEVEL-DESIGN §9):
```
bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh \
  && bash scripts/check-progress.sh && bash scripts/check-terrain-atlas.sh && bash scripts/check-pink-gate.sh \
  && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs \
  && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs
```
audit-phase21-mechanics output: level-05's door + enemy `triggered: true`. (No smoke fixture for level-05.)
</verification>

<success_criteria>
- level-05 rebuilt ~2x, calm cemetery intro (L3–6 band), descent + optional visible high route, no key.
- Zero HARD-FAILs; every coin collected; completable from spawn.
</success_criteria>

<output>
Create `.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-06-SUMMARY.md` when done.
</output>
