---
phase: 34.6-level-redesign-rebuild-and-double-every-level
verified: 2026-07-16T06:10:00Z
status: passed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
deferred:
  - truth: "The §8.5 experiential FEEL of the rebuilt world (calm-vs-intense, signature distinctness, fun) is good, and the kid re-approves the rebuilt geometry"
    addressed_in: "Phase 38 (VER-02 kid-UAT)"
    evidence: "Roadmap 34.6 §9.1 + LEVEL-REVIEW.md §34.6 limitation 2: 'No gate enforces §8.5 feel — the human sign-off does. Levels 01–03's original kid sign-off no longer covers the rebuilt geometry; Phase 38's kid-UAT is a real re-approval.' The mandated MID-phase prototype sign-off on 01+02 WAS obtained (34.6-04, 3 rounds, non-rubber-stamped)."
---

# Phase 34.6: Level Redesign — Rebuild and Double Every Level — Verification Report

**Phase Goal:** Every level's platform layout is REBUILT from scratch and roughly twice as long — the last geometry change of the milestone, landing before any art dresses it. Plus one code change: the end-of-level gate becomes key-conditional (a math-skip token on even levels).
**Verified:** 2026-07-16T06:10:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 8 levels REBUILT from scratch at ~2x length (LEN-01) | ✓ VERIFIED | goal.x doubled: L3 5120→10280, L5 3340→6930, L6 3300→7200, L7 3700→7560 (~2x); L1=7100, L2=7360, L8=7620. L4=9460 (1.55x) is object-budget-capped at 640/650 caps, documented in LEVEL-REVIEW.md limitation 1 — a full §8.5 vertical spire consumes the 650-tile HARD budget on height. smoke-progress golden fixtures re-baselined to new descriptors (smoke-progress: PASS). |
| 2 | Math density LOCKED at 1 door + 1 enemy + end gate per level | ✓ VERIFIED | Node import of all 8 descriptors: every level doors=1, enemies=1, mathGates=0. |
| 3 | End-gate key-conditional: even levels carry keys, NO locks; key-held → free clear + full XP + no math; no key → end math (LEN-02) | ✓ VERIFIED (behavioral) | Node import: even (02/04/06/08) keys=1 locks=0; odd (01/03/05/07) keys=0 locks=0. `scripts/audit-endgate-key.mjs` ran in-process, exit 0: all 4 even levels proved Path A (key→challengeOpened=false, XP +20 persisted, clearedPersisted=true) and Path B (no key→challengeOpened=true). |
| 4 | markCleared + writeSave fire on BOTH the math-clear and key-clear paths | ✓ VERIFIED (behavioral) | game.js:234 shared `clearLevel()`; line 252 `table!==undefined ? addXp(table) : addBonusXp(XP_KEY_SKIP)`; line 257 markCleared, line 271 writeSave — both inside clearLevel, called by both branches (312 key / 325 math). Audit confirms `xpPersisted`+`clearedPersisted` true on the key path. |
| 5 | Key-skip armed ONLY on keys-without-locks levels (WR-01 fix) | ✓ VERIFIED | game.js:112 `endSkipArmed = keys?.length>0 && locks?.length===0`; branch at 312 gates on `endSkipArmed && heldKeyIds.size>0`. Closure-local const, no module-level `let` (check-import-safety: PASS). |
| 6 | Biome-pair rhythm: odd = calmer intro, even (2/4/6/8) = intense + vertical | ✓ VERIFIED | Biomes paired swamp(1,2)/town(3,4)/cemetery(5,6)/castle(7,8). Peak-altitude platform y monotonically rising on even levels: 02=-328, 04=-402, 06=-476, 08=-550; odd levels calmer (03=+42, 05=+4, 07=-156, 01=-198). |
| 7 | Descents + optional high routes + VISIBLE backtracking (no hidden routes) | ✓ VERIFIED | Every descriptor carries deliberate descent tiers (drop/descent comments ≥8 each; DA/DB/DC drop chains on even levels; DESCENT peaks on odd). Even levels have visible L/H forks that rejoin; L2/L6 up-LEFT reversals climbed in open air. validate-levels over-hole + reachability PASS. |
| 8 | Documented platform-placement review of all 8 levels vs FULL LEVEL-DESIGN incl. headroom rule (SC3) | ✓ VERIFIED | docs/LEVEL-REVIEW.md §34.6 (line 207+): per-level compliance record, difficulty-ramp table, deferred-fail deletion table, honest object-budget + §8.5-feel limitations. LEVEL-DESIGN §8.5 (line 181) "Level ambition" raised bar present. |
| 9 | Zero HARD-FAIL across validator/coin-reachability/in-engine drive; 13 deferred Phase-34 fails deleted, no check weakened | ✓ VERIFIED | validate-levels.mjs: PASS, 0 HARD-FAIL (299 coin-reachability PASS, 0 headroom rows — L7 min headroom now 25px, was 9px×5). Endgate audit drove even levels to goal.x from spawn. git shows only docs modified in plan 11 (no gate edits). |
| 10 | Level-07 (monotonic staircase) ≠ level-08 (switchback) — LVL-02 holds | ✓ VERIFIED | L7 header "monotonic staircase"; L8 header "SWITCHBACK tower climbs 740px (tallest ever)"; L8 geometry has explicit up-LEFT reversal tiers. Mechanically distinct signatures. |
| 11 | Gentle ramp preserved — L1 still a soft landing | ✓ VERIFIED (structural) | level-01: no ceilings/overlapping tiers, gentle 60–70px rises, 120–140px gaps, calmest peak (-198), 14 checkpoints, no key. Experiential "feel" deferred to Phase 38 kid-UAT (see Deferred). |

**Score:** 11/11 truths verified (0 present, behavior-unverified)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | §8.5 experiential FEEL + kid re-approval of the rebuilt geometry | Phase 38 (VER-02) | Milestone design defers per-level kid-UAT to Phase 38; the phase's OWN mandated mid-phase human checkpoint (prototype 01+02) was completed genuinely in 34.6-04 (3 rounds, 2 rejections, model-escalated to Opus authoring). |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/scenes/game.js` | shared clearLevel() + heldKeyIds branch + endSkipArmed | ✓ VERIFIED | clearLevel:234, heldKeyIds:78, endSkipArmed:112, branch:312 |
| `src/config.js` | CONFIG.PROGRESS.XP_KEY_SKIP | ✓ VERIFIED | line 257 `XP_KEY_SKIP: 20` (A1 confirmed at checkpoint) |
| `src/levels/level-01…08.js` | 8 rebuilt descriptors, even=keys-no-locks | ✓ VERIFIED | All 8 rebuilt; density 1+1+gate; keys/locks shape correct per archetype |
| `scripts/audit-endgate-key.mjs` | two-path in-engine audit iterating even levels | ✓ VERIFIED | Ran exit 0; Path A/B proved on 02/04/06/08 incl. IN-01 XP-persistence assertions |
| `scripts/smoke-progress.mjs` | golden fixtures re-baselined (01/02/03/04) | ✓ VERIFIED | smoke-progress: PASS |
| `docs/LEVEL-DESIGN.md` | §8.5 raised bar + A1/A3 checkpoint decisions | ✓ VERIFIED | §8.5 present; A3 open-air locked |
| `docs/LEVEL-REVIEW.md` | Phase 34.6 rebuild review section | ✓ VERIFIED | §34.6 section, per-level record + limitations |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| onReachGoal | clearLevel (key) / openMathGate (math) | endSkipArmed && heldKeyIds.size branch | ✓ WIRED (game.js:312/325) |
| clearLevel key path | progress.addBonusXp(XP_KEY_SKIP) | line 252 | ✓ WIRED |
| even geometry.keys (no locks) | game.js heldKeyIds → key clear path | build.js free-standing key → onPickup:364 | ✓ WIRED (audit Path A) |
| descriptor geometry | smoke-progress expectedGeometry | deep-equal fixtures | ✓ WIRED (smoke PASS) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Static level validity + coin reachability | `node scripts/validate-levels.mjs` | PASS, 0 HARD-FAIL, 299 coins reachable | ✓ PASS |
| Key-skip both paths, in-engine, all even levels | `node scripts/audit-endgate-key.mjs` | exit 0, 02/04/06/08 both paths + XP persistence | ✓ PASS |
| Golden geometry fixtures match rebuilt descriptors | `node scripts/smoke-progress.mjs` | PASS | ✓ PASS |
| Safety / import / gate static invariants | check-safety/import-safety/gate.sh | all PASS | ✓ PASS |
| Asset manifest | `node scripts/check-assets-manifest.mjs` | PASS | ✓ PASS |

Note: `browser-boot.mjs` (bidirectional completion drive) and `audit-coins.mjs` were not personally re-run in this verification pass (each is a multi-minute full-browser drive); their green result is documented in 34.6-11-SUMMARY.md and corroborated here by validate-levels static crossability/reachability + the endgate audit driving all four even (hardest, most vertical) levels to goal.x from spawn.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| LEN-01 | all 11 plans | All 8 levels rebuilt from scratch at ~2x, authored against LEVEL-DESIGN incl. headroom rule | ✓ SATISFIED | Truths 1,2,8,9; validate-levels 0 HARD-FAIL, 0 headroom rows; LEVEL-REVIEW §34.6 |
| LEN-02 | all 11 plans | Altitude core feature; biome-pair rhythm even=vertical+math-skip key; descents, high routes, visible backtracking | ✓ SATISFIED | Truths 3,4,5,6,7,10; endgate audit both paths exit 0 |

Both requirement IDs from PLAN frontmatter accounted for; no orphaned requirements. REQUIREMENTS.md marks both Complete.

### Anti-Patterns Found

None blocking. No unreferenced TBD/FIXME/XXX in modified `src/` files. No timers/schedulers (check-safety PASS). No module-top-level engine globals or module-level `let` (check-import-safety PASS). The 34.5 physical-lock code is intentionally dormant (keys-without-locks), not deleted — per the CONTEXT.md decision.

### Human Verification Required

None for THIS phase. The mandated mid-phase prototype human checkpoint was completed (34.6-04, genuine 3-round sign-off). The remaining per-level experiential "feel" / kid re-approval is a DEFERRED item scheduled for Phase 38 (VER-02) by milestone design — see Deferred Items.

### Gaps Summary

No gaps. All 11 observable truths verified against the codebase — not the SUMMARY narrative. The end-gate key mechanic (the phase's one code change) is behaviorally proven by an in-process run of `audit-endgate-key.mjs` (exit 0, both paths on all four even levels, including XP-award persistence). All 8 descriptors are rebuilt with correct density (1 door + 1 enemy + end gate), correct keys/locks archetype shape (even=key-no-lock, odd=neither), doubled length (or documented object-budget ceiling for level-04), and a monotonic verticality ramp. validate-levels is 0 HARD-FAIL with the 13 previously-deferred Phase-34 fails deleted (299 coins reachable, 0 headroom rows). The documented LEVEL-DESIGN review (SC3) exists. The only outstanding item — the kid's experiential re-approval of the rebuilt geometry — is deferred to Phase 38 by design, not a gap for this phase.

---

_Verified: 2026-07-16T06:10:00Z_
_Verifier: Claude (gsd-verifier)_
