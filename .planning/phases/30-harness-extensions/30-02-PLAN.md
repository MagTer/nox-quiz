---
phase: 30-harness-extensions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/route-planner.mjs
  - scripts/lib/mechanic-drive.mjs
  - scripts/lib/audit-retry.mjs
autonomous: true
requirements: [MECH-04]

must_haves:
  truths:
    - "A REAL browser-driven `node scripts/audit-phase21-mechanics.mjs` run reports `triggered: true` and `resolved: true` for the `secret-alcove` encounter on level-01 (minimum bar), and ideally across all 8 levels (closing the known automated blind spot per MECH-04's success criterion)."
    - "The alcove detection logic never reads `get(\"challenge\").length` (grep-verifiable) — challenge-open is contractually always false for alcoves."
    - "An untouched/failed alcove never blocks the audit from reaching later door/mathGate/enemy encounters in the same attempt (the alcove is a non-blocking walk-through bonus, unlike the other three mechanics)."
  artifacts:
    - scripts/lib/mechanic-drive.mjs (deriveEncounters alcove branch, driveToXPlanned targetY threading, new driveAndDetectAlcove export)
    - scripts/lib/route-planner.mjs (planTakeoffs optional targetY param — backward-compatible)
    - scripts/lib/audit-retry.mjs (per-encounter branch on tag, non-blocking break-guard fix)
  key_links:
    - "deriveEncounters() output -> auditLevelWithRetries' per-encounter loop -> driveAndDetectAlcove -> get(\"secret-alcove\").length + CONFIG.SAVE.KEY localStorage blob (before/after)"
    - "driveToXPlanned's new opts.targetY -> planTakeoffs' nodeContaining(nodes, targetX, targetY) -> correctly disambiguates the platform node an alcove floats above from the floor node beneath it (WITHOUT this, the driver never leaves the ground and can never physically reach the alcove's height — verified by hand against level-01's real geometry)"
---

<objective>
Make the interactive mechanic audit (`scripts/audit-phase21-mechanics.mjs`) genuinely detect secret-alcove discovery on real level content, using the entity-destroy/XP-delta signal — never the challenge-open signal, which is contractually always false for alcoves (`src/mechanics/secretAlcove.js` never opens a challenge, by design).

Purpose: this is MECH-04's dynamic half — closing the known automated blind spot the audit has had for alcoves since they shipped in Phase 25/29. No player-visible surface, no level content changes; this is pure harness-engineering over the existing Playwright driving machinery.
Output: alcove encounters flow through the same per-level audit pass as door/mathGate/enemy, with correct triggered/resolved detection and correct non-blocking semantics.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-harness-extensions/30-CONTEXT.md
</context>

<artifacts_produced>
- `deriveEncounters()` alcove-emission branch — `scripts/lib/mechanic-drive.mjs`
- `driveAndDetectAlcove(page, encounter, geometry)` — new export, `scripts/lib/mechanic-drive.mjs`
- `driveToXPlanned`'s `opts.targetY` threading — `scripts/lib/mechanic-drive.mjs`
- `planTakeoffs`'s optional `targetY` parameter — `scripts/lib/route-planner.mjs`
- `auditLevelWithRetries`'s per-encounter tag branch + non-blocking guard for `secret-alcove` — `scripts/lib/audit-retry.mjs`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alcove driving + detection — deriveEncounters, targetY threading, driveAndDetectAlcove, retry-loop branch</name>
  <files>scripts/lib/mechanic-drive.mjs, scripts/lib/route-planner.mjs, scripts/lib/audit-retry.mjs</files>
  <read_first>
    scripts/lib/mechanic-drive.mjs (full file — deriveEncounters lines 35-43, driveToXPlanned lines 287-472, its `planTakeoffs(geometry, targetX)` call inside driveToXPlanned)
    scripts/lib/route-planner.mjs (full file — planTakeoffs at lines 348-402, especially `nodeContaining(nodes, targetX)` at line ~352 which has NO y argument today)
    scripts/lib/audit-retry.mjs (full file — the per-encounter loop at lines 63-133, especially the `if (!everTriggered) break;` guard at line ~117)
    scripts/lib/reachability.mjs (`nodeContaining(nodes, x, y)` at lines 63-84 — the exact y-disambiguation behavior being threaded through)
    src/mechanics/secretAlcove.js (full file — confirms alcove touch destroys the entity + calls `progress.addBonusXp`/`save()` synchronously, no challenge ever opens)
    src/progress.js (`hasSecretFound`/`markSecretFound`, `serialize()` — confirms the persisted blob's `xp` field reflects the bonus immediately after `save()`)
    src/config.js (`CONFIG.SAVE.KEY`, `CONFIG.PROGRESS.XP_ALCOVE` — exact constant names/values)
    src/levels/level-01.js (lines 139-146, 52-59 — real `secretAlcove: [{x:400,y:170}]` above `platforms[0]: {x:360,y:240,w:160}`)
  </read_first>
  <behavior>
    - deriveEncounters(geometry) with a `secretAlcove: [{x:400,y:170}]` entry -> returned array includes `{x:400, y:170, tag:"secret-alcove"}`, correctly x-sorted alongside doors/mathGates/enemies.
    - planTakeoffs(geometry, 400, envelope, 170) against level-01-shaped geometry -> resolves its internal targetNode to the platform node (y:240), NOT the floor node (y:320), because |240-170|=70 < |320-170|=150; the returned takeoffs list includes a "mount" takeoff (non-empty), unlike planTakeoffs(geometry, 400) with no targetY (which resolves to the floor node and returns zero takeoffs — a pure walk that never elevates the player).
    - planTakeoffs(geometry, targetX) called with NO 4th argument (every existing call site) -> byte-identical behavior to before this change (targetY defaults to undefined, nodeContaining(nodes, targetX, undefined) === nodeContaining(nodes, targetX)).
  </behavior>
  <action>
    ROOT-CAUSE NOTE (verified by hand against level-01's real geometry, not assumed): `driveToXPlanned(page, targetX, geometry)` currently calls `planTakeoffs(geometry, targetX)` with no y hint. `planTakeoffs`'s `nodeContaining(nodes, targetX)` (route-planner.mjs line ~352) then resolves whichever node's span merely CONTAINS `targetX`, with NO y-based disambiguation — and since `buildNodes` always pushes floor nodes before platform nodes, an x that falls within BOTH a floor's span AND an overlapping platform's span (exactly level-01's case: alcove x:400 sits inside both floor-0's 0..560 AND platform-0's 360..520) always resolves to the FLOOR. This means driving toward an alcove's raw x with the existing machinery would target the ground and produce ZERO takeoffs (the floor already covers that x — no jump needed to "reach" it) — the player would never elevate to platform height (y:240) at all, let alone touch a point floating 70px above it (y:170). Fix this at the source: thread an optional targetY through to `nodeContaining`, exactly mirroring the WR-02 fix already applied to goal/mathGate disambiguation in `reachability.mjs`.

    In `scripts/lib/route-planner.mjs`: change `planTakeoffs(geometry, targetX, envelope = JUMP_ENVELOPE)`'s signature to `planTakeoffs(geometry, targetX, envelope = JUMP_ENVELOPE, targetY = undefined)` (new 4th param, appended after `envelope` so every existing 2-arg and 3-arg call site is byte-unchanged). Change the line `const targetNode = nodeContaining(nodes, targetX);` to `const targetNode = nodeContaining(nodes, targetX, targetY);` — `nodeContaining` (imported from `./reachability.mjs`, already accepts a 3rd `y` param) needs no changes itself.

    In `scripts/lib/mechanic-drive.mjs`:
    1. In `deriveEncounters(geometry)`, add a fourth spread to the `entries` array (before the `.sort()` call): `...(geometry.secretAlcove ?? []).map((a) => ({ x: a.x, y: a.y, tag: "secret-alcove" }))`. This is the ONLY change to this function's existing door/mathGate/enemy entries.
    2. In `driveToXPlanned(page, targetX, geometry, opts = {})`, change the line `const { takeoffs } = planTakeoffs(geometry, targetX);` to `const { takeoffs } = planTakeoffs(geometry, targetX, undefined, opts.targetY);` (passing `undefined` for `envelope` triggers `planTakeoffs`'s own default parameter — this is a no-op for every existing caller that never sets `opts.targetY`).
    3. Add a new exported function `driveAndDetectAlcove(page, encounter, geometry)`:
       - Capture `beforeCount = await page.evaluate(() => get("secret-alcove").length)`.
       - Capture `beforeBlob = await page.evaluate((key) => { try { return JSON.parse(localStorage.getItem(key)); } catch { return null; } }, CONFIG.SAVE.KEY)` (CONFIG is already imported in this file — no new import needed).
       - Call `await driveToXPlanned(page, encounter.x, geometry, { targetY: encounter.y })` — reuses the ALREADY-PROVEN planned-navigation system (handles gap-crossing/platform-mounting reliably across all 8 levels for every other mechanic) rather than inventing new movement code; ignore its own returned `triggered` (based on `get("challenge").length`, meaningless for alcoves).
       - Poll grounded state once: `const s = await page.evaluate(() => { const p = get("player")[0]; return p ? { grounded: p.isGrounded() } : { grounded: false }; })`; if `s.grounded`, press one deliberate "check for a secret" hop: `await page.keyboard.press("Space", { delay: 450 })` (reuse this file's existing `jumpHoldMs=450` convention — held well past the ~371ms apex so `CONFIG.JUMP_CUT` never truncates it), then `await page.waitForTimeout(600)` (mirrors `resolveIfBoxed`'s own 600ms settle-recheck precedent) so the arc plays out and any resulting XP/save/destroy has landed.
       - Capture `afterCount`/`afterBlob` the same way as the `before*` captures.
       - `const triggered = afterCount < beforeCount;` (the alcove entity was destroyed — the only externally-observable proxy for "the player's bounding box overlapped the trigger volume," since `secretAlcove.js` destroys the object in the SAME handler that detects the touch).
       - `const delta = (afterBlob?.xp ?? 0) - (beforeBlob?.xp ?? 0);`
       - `const resolved = triggered && (delta === CONFIG.PROGRESS.XP_ALCOVE || delta === 0);` (the `0` branch documents the already-found-this-run anti-farming case per `secretAlcove.js`'s CR-01 guard; every audit run seeds a fresh `SAVE_BLOB` with no `secretFound` field, so `delta === CONFIG.PROGRESS.XP_ALCOVE` is the branch actually expected to fire in this harness).
       - `return { triggered, resolved };`

    In `scripts/lib/audit-retry.mjs`:
    1. Add `driveAndDetectAlcove` to the existing `import { deriveEncounters, driveToXPlanned, resolveIfBoxed } from './mechanic-drive.mjs';` line.
    2. In the per-encounter loop body (lines ~72-118), branch on `encounter.tag === "secret-alcove"`: for that branch, call `driveAndDetectAlcove(page, encounter, level.geometry)` and use its returned `{triggered, resolved}` directly (still OR-ed against `previous?.triggered`/`previous?.resolved` per this file's existing OR-across-attempts contract — do not skip that OR-ing); for every other tag, keep the existing `driveToXPlanned` + `resolveIfBoxed` path byte-unchanged.
    3. NON-BLOCKING FIX (required — verified against `src/mechanics/secretAlcove.js`'s own header contract: "this never opens the shared math-challenge UI and never freezes the player... this is a walk-through bonus"): the existing `if (!everTriggered) { break; }` guard exists because door/mathGate/enemy are BLOCKING colliders a real player physically cannot walk past unanswered — but the secret alcove is NEVER blocking. Change this guard so it does NOT break the attempt loop when the untriggered/unresolved encounter's tag is `"secret-alcove"` — record its (false/false) outcome in `results` as today, but `continue` to the next encounter in the SAME attempt instead of breaking. Without this fix, any attempt where the alcove touch fails would silently prevent every LATER door/mathGate/enemy encounter in that level from ever being driven that attempt — a regression against this phase's own "no false HARD-FAILs on shipped content" success criterion.
  </action>
  <acceptance_criteria>
    - `grep -n "targetY" scripts/lib/route-planner.mjs scripts/lib/mechanic-drive.mjs` shows the new parameter/threading in both files.
    - `grep -n 'driveAndDetectAlcove' scripts/lib/mechanic-drive.mjs scripts/lib/audit-retry.mjs` shows the export and its import+use.
    - `grep -n 'get("challenge")' scripts/lib/mechanic-drive.mjs` shows zero occurrences inside `driveAndDetectAlcove`'s body (challenge-open is never read for alcove detection).
    - `node scripts/lib/route-planner.mjs` (self-test) still prints `route-planner-selftest: PASS` — confirms the new optional `targetY` param did not change behavior for any existing 2-arg/3-arg call.
    - `grep -n 'secret-alcove' scripts/lib/audit-retry.mjs` shows the non-blocking branch guard.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/lib/route-planner.mjs && grep -n "targetY" scripts/lib/route-planner.mjs scripts/lib/mechanic-drive.mjs && grep -n "driveAndDetectAlcove" scripts/lib/mechanic-drive.mjs scripts/lib/audit-retry.mjs</automated>
  </verify>
  <done>deriveEncounters emits alcove entries, planTakeoffs correctly targets the platform node (not the floor) when a targetY is supplied, driveAndDetectAlcove exists and never reads the challenge-open signal, and an untouched alcove no longer blocks later encounters in the same attempt.</done>
</task>

<task type="auto">
  <name>Task 2: Real browser verification — prove alcove triggered/resolved against real level content</name>
  <files>scripts/lib/mechanic-drive.mjs, scripts/lib/audit-retry.mjs</files>
  <read_first>
    scripts/audit-phase21-mechanics.mjs (full file — how to invoke it and how to read its printed JSON + final `AUDIT:` line)
  </read_first>
  <action>
    Run `node scripts/audit-phase21-mechanics.mjs` for real (this is a genuine browser-driven Playwright run — REQUIRED per 30-CONTEXT.md's explicit static/dynamic verification split; code review of Task 1's logic alone does NOT satisfy this task). Parse the printed JSON results array and confirm every entry whose `tag === "secret-alcove"` has `triggered: true` and `resolved: true`. Level-01 passing is the hard minimum bar (30-CONTEXT.md's MECH-04 success criterion literally says "across all 8 levels" — push for all 8; if any level's alcove placement needs a different `jumpHoldMs`/settle timing than Task 1's default 450ms/600ms to reliably land the "check for a secret" hop, adjust those two values empirically inside `driveAndDetectAlcove` and re-run — this is expected iteration, mirroring this codebase's own extensively-documented empirical-tuning convention (see `route-planner.mjs`'s and `mechanic-drive.mjs`'s own header comments, e.g. the Rule-1/25-07 fixes), not a design failure).

    Do not declare this task done from static reasoning alone. If a level's alcove genuinely cannot be triggered by the automated driver after reasonable tuning, do not silently accept it — flag it explicitly in this plan's SUMMARY as a known gap (mirrors this project's own precedent for `resolved: false` rows being documented as known headless-timing flakiness rather than hidden) rather than reporting false success.
  </action>
  <acceptance_criteria>
    - The real `node scripts/audit-phase21-mechanics.mjs` run's printed JSON contains a `tag: "secret-alcove"` entry for every one of the 8 levels.
    - Every `secret-alcove` entry shows `triggered: true` AND `resolved: true` for at least level-01; the SUMMARY documents the actual result for all 8 (full pass, or named exceptions with rationale).
    - No `secret-alcove` entry's detection ever depended on `get("challenge").length` (confirmed by Task 1's grep, re-confirmed here by the fact this run passes without any challenge-open signal involved for that tag).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/audit-phase21-mechanics.mjs</automated>
  </verify>
  <done>A real browser run's JSON output demonstrably shows the alcove's triggered/resolved signals working against real level-01 content (minimum bar), with the full 8-level result documented in the plan's SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Playwright-driven headless browser <-> its own `localStorage` | `driveAndDetectAlcove` reads/writes only a synthetic, script-seeded `SAVE_BLOB` (per `scripts/audit-phase21-mechanics.mjs`'s existing `SAVE_KEY`/`SAVE_BLOB` pattern) inside an ephemeral, script-controlled browser context |
| `driveToXPlanned`'s new `opts.targetY` | Internal API surface between this repo's own scripts — no external input |

This phase's surface stays narrow (30-CONTEXT.md): no player-visible behavior change, no real save data ever touched (the audit's browser context is recycled per-level and never reads a real player's storage).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-04 | Information Disclosure | `driveAndDetectAlcove`'s `page.evaluate(() => localStorage.getItem(...))` read | low | accept | Runs against an ephemeral, script-controlled headless browser context seeded with synthetic `SAVE_BLOB` data; never touches a real player's browser/data (existing `scripts/audit-phase21-mechanics.mjs` pattern, unchanged). |
| T-30-05 | Tampering | `planTakeoffs`'s new optional `targetY` parameter widens node disambiguation | low | accept | Backward-compatible optional param (defaults to `undefined`, byte-identical behavior for every pre-existing caller — proven by the self-test staying green); no new external input surface. |
| T-30-06 | Denial of Service | `driveAndDetectAlcove`'s extra `page.waitForTimeout(600)` per alcove encounter | low | accept | Bounded, one-shot per level per attempt; does not change any existing `maxMs`/`stallMs` timeout budget already governing `driveToXPlanned`. |
</threat_model>

<verification>
1. `node scripts/lib/route-planner.mjs` — self-test green, confirms `targetY` is backward-compatible.
2. `grep` checks (see Task 1 acceptance_criteria) confirm the challenge-open signal is never used for alcove detection and the non-blocking guard exists.
3. `node scripts/audit-phase21-mechanics.mjs` — REQUIRED real browser run; every `secret-alcove` result entry is `triggered: true`/`resolved: true` for at least level-01, full 8-level result documented in SUMMARY.
</verification>

<success_criteria>
- Alcove encounters are driven and detected using entity-destroy/XP-delta, never challenge-open.
- An untouched alcove never blocks the audit from reaching later encounters.
- A real browser run proves the signals work against real level-01 content (minimum), with the full 8-level picture documented.
</success_criteria>

<output>
Create `.planning/phases/30-harness-extensions/30-02-SUMMARY.md` when done
</output>
