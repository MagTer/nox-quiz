---
phase: 16-remaining-mechanics-difficulty-curve
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/check-gate.sh
  - scripts/check-import-safety.sh
autonomous: true
requirements: [MECH-03, MECH-04, MECH-05, LVL-03]
must_haves:
  truths:
    - "scripts/check-gate.sh sanity-checks src/mechanics/gates.js, src/mechanics/enemy.js, and src/mechanics/collect.js as thin openChallenge callers, alongside the existing door.js/mathGate.js checks"
    - "scripts/check-import-safety.sh includes src/mechanics/gates.js, src/mechanics/enemy.js, and src/mechanics/collect.js in both its existence/syntax loop and its a727c13 negative top-level scan"
    - "The gate extensions do NOT weaken existing assertions (check-gate.sh still targets challenge.js; check-import-safety.sh still calibrates RED/GREEN on fixtures and game.js)"
  artifacts:
    - path: "scripts/check-gate.sh"
      provides: "extended structural gate covering all four mechanic modules as thin callers of the shared challenge seam"
      contains: "mechanics/gates.js"
    - path: "scripts/check-import-safety.sh"
      provides: "extended a727c13 scan list covering the three new mechanic modules"
      contains: "mechanics/gates.js"
  key_links:
    - from: "scripts/check-gate.sh"
      to: "src/ui/challenge.js"
      via: "the one-way seam remains the central target; new modules are verified as callers"
      pattern: "openChallenge"
    - from: "scripts/check-import-safety.sh"
      to: "src/mechanics/gates.js, src/mechanics/enemy.js, src/mechanics/collect.js"
      via: "new modules added to the scanned-file lists so a top-level engine-global leak cannot pass silently"
      pattern: "mechanics/gates.js"
---

<objective>
Extend the project's structural gates so the three new Phase 16 mechanic modules are
policed by the same contracts that Phase 15 established for `door.js` and
`challenge.js`: the shared-seam caller check (`check-gate.sh`) and the a727c13
import-safety check (`check-import-safety.sh`). This is Wave 0 tooling — it runs
BEFORE the new modules exist and is expected to be RED until 16-02 creates them,
then GREEN once 16-03 wires everything.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-remaining-mechanics-difficulty-curve/16-CONTEXT.md
@.planning/phases/16-remaining-mechanics-difficulty-curve/16-RESEARCH.md

# The existing gates to extend:
@scripts/check-gate.sh
@scripts/check-import-safety.sh

# The existing modules the gates already cover (for reference patterns):
@src/ui/challenge.js
@src/mechanics/door.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend check-gate.sh to cover gates/enemy/collect modules</name>
  <files>scripts/check-gate.sh</files>
  <read_first>
    - scripts/check-gate.sh FULL FILE — understand assertions 9-10 (mathGate.js and door.js thin-caller checks) and the strip_comments idiom.
    - src/ui/challenge.js lines 64+ — the public `openChallenge` contract the new modules must call.
  </read_first>
  <action>
    Append three new THIN CALLER assertions to `scripts/check-gate.sh`, mirroring
    assertions 9-10 exactly but for the new mechanic modules. Insert them after the
    existing door.js check and before the final `echo "gate checks: PASS"`:

    1. `src/mechanics/gates.js` must exist and call `openChallenge`.
    2. `src/mechanics/enemy.js` must exist and call `openChallenge`.
    3. `src/mechanics/collect.js` must exist and call `openChallenge`.

    Use the same `strip_comments` + `grep -q 'openChallenge'` pattern as the door.js
    check. Do NOT change any existing assertion. Do NOT make these checks conditional —
    they are real existence/caller gates that will be RED until 16-02 creates the files.
  </action>
  <acceptance_criteria>
    - `node --check scripts/check-gate.sh` passes.
    - The script still contains the existing challenge.js assertions 1-8 unchanged.
    - `grep -q 'src/mechanics/gates.js' scripts/check-gate.sh`.
    - `grep -q 'src/mechanics/enemy.js' scripts/check-gate.sh`.
    - `grep -q 'src/mechanics/collect.js' scripts/check-gate.sh`.
    - Running `bash scripts/check-gate.sh` reports FAIL with a clear "missing" message for the new files (expected RED at this wave).
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/check-gate.sh && grep -q 'src/mechanics/gates.js' scripts/check-gate.sh && grep -q 'src/mechanics/enemy.js' scripts/check-gate.sh && grep -q 'src/mechanics/collect.js' scripts/check-gate.sh && (bash scripts/check-gate.sh 2>&1 | grep -q 'missing\|FAIL') && echo OK</automated>
  </verify>
  <done>check-gate.sh now sanity-checks gates.js, enemy.js, and collect.js as thin openChallenge callers; the script is expected RED until 16-02.</done>
</task>

<task type="auto">
  <name>Task 2: Extend check-import-safety.sh to cover the new mechanic modules</name>
  <files>scripts/check-import-safety.sh</files>
  <read_first>
    - scripts/check-import-safety.sh FULL FILE — understand Section 0 (existence/syntax loop) and Section 2 (negative a727c13 scan loop) and the ENGINE_GLOBALS/TOPLEVEL_TRAP definitions.
    - src/mechanics/door.js lines 1-69 — the a727c13 discipline the new modules must follow.
  </read_first>
  <action>
    Add the three new module paths to BOTH scanned-file loops in
    `scripts/check-import-safety.sh`:

    1. Section 0 existence/syntax loop (the `for f in ...` list that runs `node --check`):
       append `"src/mechanics/gates.js" "src/mechanics/enemy.js" "src/mechanics/collect.js"`.
    2. Section 2 negative a727c13 scan loop (the `for f in ...` list that runs the
       `TOPLEVEL_TRAP` grep): append the same three paths.

    Keep the existing files (title.js, select.js, challenge.js, door.js) in place.
    Do NOT change `ENGINE_GLOBALS` or `TOPLEVEL_TRAP` unless a new engine primitive is
    introduced by this phase (none is).
  </action>
  <acceptance_criteria>
    - `node --check scripts/check-import-safety.sh` passes.
    - `grep -q 'src/mechanics/gates.js' scripts/check-import-safety.sh`.
    - `grep -q 'src/mechanics/enemy.js' scripts/check-import-safety.sh`.
    - `grep -q 'src/mechanics/collect.js' scripts/check-import-safety.sh`.
    - The calibration self-test (RED on bad-scene.js, GREEN on game.js) is still present and unchanged.
    - Running `bash scripts/check-import-safety.sh` reports FAIL because the new files do not exist yet (expected RED at this wave).
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/check-import-safety.sh && grep -q 'src/mechanics/gates.js' scripts/check-import-safety.sh && grep -q 'src/mechanics/enemy.js' scripts/check-import-safety.sh && grep -q 'src/mechanics/collect.js' scripts/check-import-safety.sh && grep -q 'bad-scene.js' scripts/check-import-safety.sh && (bash scripts/check-import-safety.sh 2>&1 | grep -q 'missing\|FAIL') && echo OK</automated>
  </verify>
  <done>check-import-safety.sh now includes gates.js, enemy.js, and collect.js in both its syntax and a727c13 scan loops; expected RED until 16-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static gate source ↔ execution | These edits only extend grep-based gates; they do not change runtime behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01-01 | Denial of Service | Missing new files cause gate to fail early | low | accept | Expected RED state; 16-02 creates the files. The fail-fast message names the missing module. |
| T-16-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | No installs in this plan. |
</threat_model>

<verification>
- `check-gate.sh` and `check-import-safety.sh` both parse and contain the new module checks.
- Both gates are expected RED at the end of 16-01 because the new modules do not exist yet.
- `check-safety.sh` is unchanged (whole-tree scope already covers future modules).
</verification>

<success_criteria>
- `scripts/check-gate.sh` sanity-checks `gates.js`, `enemy.js`, and `collect.js` as `openChallenge` callers.
- `scripts/check-import-safety.sh` includes the three new modules in its existence/syntax and a727c13 scan loops.
- Existing assertions in both scripts are preserved unchanged.
</success_criteria>

<output>
Create `.planning/phases/16-remaining-mechanics-difficulty-curve/16-01-SUMMARY.md` when done.
</output>
