---
phase: 16-remaining-mechanics-difficulty-curve
plan: 02
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - src/ui/challenge.js
  - src/config.js
  - src/levels/level-01.js
  - src/levels/build.js
  - src/mechanics/gates.js
  - src/mechanics/enemy.js
  - src/mechanics/collect.js
  - scripts/smoke-progress.mjs
autonomous: true
requirements: [MECH-03, MECH-04, MECH-05, LVL-03]
must_haves:
  truths:
    - "src/ui/challenge.js accepts optional `question` and `renderChoices` params and returns `{ close }`, fully backward-compatible with existing callers"
    - "src/config.js carries CONFIG.MATH_GATE, CONFIG.ENEMY, and CONFIG.COLLECT tuning blocks following the CONFIG.DOOR/CONFIG.GATE precedent"
    - "src/levels/level-01.js adds mathGates (multiple), enemies (one), and collectZone+answerPickupSlots arrays without changing existing geometry values"
    - "src/levels/build.js instantiates tagged 'math-gate', 'enemy', 'answer-zone', and 'answer-pickup-slot' entities from the new arrays, guarded with ?? []"
    - "src/mechanics/gates.js exports wireGates({player, brain}) and opens each 'math-gate' independently on touch, destroying it before unpausing"
    - "src/mechanics/enemy.js exports wireEnemy({player, brain}) and opens each 'enemy' on touch with a custom prompt, destroying it before unpausing; no contact-damage path exists"
    - "src/mechanics/collect.js exports wireCollect({player, brain}); entering an 'answer-zone' freezes the player, spawns pickups from the current question, and clears on the correct pickup; wrong pickups give only non-punishing feedback"
    - "scripts/smoke-progress.mjs expectedGeometry is updated in lockstep with the new level-01.js arrays so node scripts/smoke-progress.mjs exits 0"
    - "LEVEL_01.allowedTables remains [6,7,8,9] (no accidental difficulty softening)"
  artifacts:
    - path: "src/ui/challenge.js"
      provides: "extended shared seam with optional caller-supplied question, suppressed choices, and external close handle"
      contains: "renderChoices"
    - path: "src/config.js"
      provides: "per-mechanic tuning blocks for checkpoint gates, enemies, and collect-the-answer"
      contains: "MATH_GATE:|ENEMY:|COLLECT:"
    - path: "src/levels/level-01.js"
      provides: "authored placements for multiple checkpoint gates, one enemy, and one collect-the-answer zone with pickup slots"
      contains: "mathGates:|enemies:|collectZones:|answerPickupSlots:"
    - path: "src/levels/build.js"
      provides: "entity instantiation for the new mechanic tags"
      contains: "math-gate|enemy|answer-zone|answer-pickup"
    - path: "src/mechanics/gates.js"
      provides: "MECH-04 multiple checkpoint gate mechanic"
      exports: ["wireGates"]
      contains: "openChallenge"
    - path: "src/mechanics/enemy.js"
      provides: "MECH-05 defeat-enemy mechanic"
      exports: ["wireEnemy"]
      contains: "openChallenge"
    - path: "src/mechanics/collect.js"
      provides: "MECH-03 collect-the-answer mechanic"
      exports: ["wireCollect"]
      contains: "openChallenge"
    - path: "scripts/smoke-progress.mjs"
      provides: "LVL-02 regression fixture kept in sync with new geometry fields"
      contains: "mathGates:|enemies:|collectZones:|answerPickupSlots:"
  key_links:
    - from: "src/mechanics/gates.js"
      to: "src/ui/challenge.js"
      via: "wireGates calls openChallenge directly — one-way seam reuse"
      pattern: "openChallenge"
    - from: "src/mechanics/enemy.js"
      to: "src/ui/challenge.js"
      via: "wireEnemy calls openChallenge directly — one-way seam reuse"
      pattern: "openChallenge"
    - from: "src/mechanics/collect.js"
      to: "src/ui/challenge.js"
      via: "wireCollect calls openChallenge({question, renderChoices:false}) and uses the returned close handle"
      pattern: "renderChoices"
    - from: "src/levels/level-01.js"
      to: "src/levels/build.js"
      via: "new geometry arrays consumed by buildLevel"
      pattern: "g.mathGates|g.enemies|g.collectZones|g.answerPickupSlots"
---

<objective>
Build the three remaining math mechanic modules and their level-data/builder
consumers over the shared `ui/challenge.js` seam. This wave extends the seam
backward-compatibly for collect-the-answer, adds per-mechanic config blocks,
authored placements in level-01, builder instantiation, and the three
`mechanics/*.js` modules. It also keeps the LVL-02 smoke regression green by
mirroring every new geometry field into `scripts/smoke-progress.mjs`.

This plan does NOT touch `src/scenes/game.js` — wiring is 16-03's job, kept
separate so this plan's verification stays static/structural.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-remaining-mechanics-difficulty-curve/16-CONTEXT.md
@.planning/phases/16-remaining-mechanics-difficulty-curve/16-RESEARCH.md

# The seam to extend:
@src/ui/challenge.js

# The precedent every mechanic mirrors:
@src/mechanics/door.js
@src/scenes/game.js
@src/levels/build.js
@src/levels/level-01.js
@src/config.js
@src/fx.js

# The smoke fixture that must stay in sync:
@scripts/smoke-progress.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend challenge.js with optional question, renderChoices, and close handle</name>
  <files>src/ui/challenge.js</files>
  <read_first>
    - src/ui/challenge.js FULL FILE — the existing openChallenge signature, the q generation, the answer-box creation, the close() teardown, and the key controller capture.
    - src/mechanics/door.js lines 49-67 — an existing caller that must continue to work unchanged after this edit.
  </read_first>
  <action>
    Make three minimal, backward-compatible edits to `src/ui/challenge.js`:

    1. Change the function signature to accept two new optional parameters:
       `export function openChallenge({ brain, onSuccess, prompt, question, renderChoices = true } = {})`.
    2. Replace `const q = brain.nextQuestion();` with `const q = question ?? brain.nextQuestion();`.
       This lets MECH-03 pass the SAME question it uses to spawn pickups.
    3. Wrap the answer-box + key-controller creation in `if (renderChoices) { ... }` and make
       `close()` safe when no key controllers were captured (guard the `forEach` or use an
       empty array). After `close()` is defined, return `{ close }` at the end of the function.

    Keep every other behavior identical: the dim, panel, prompt text, wrong-answer nudge,
    correct-answer teardown, and the `onSuccess({ table: q.a })` callback. Existing callers
    `mathGate.js` and `door.js` will omit the new options and ignore the return value, so
    their behavior is byte-identical.
  </action>
  <acceptance_criteria>
    - `node --check src/ui/challenge.js` passes.
    - `grep -q 'renderChoices' src/ui/challenge.js`.
    - `grep -q 'question ?? brain.nextQuestion()' src/ui/challenge.js` (or equivalent).
    - `grep -q 'return { close }' src/ui/challenge.js`.
    - Existing behavior is preserved: `grep -q 'onSuccess?.({ table: q.a })' src/ui/challenge.js` and `grep -q 'destroyAll("challenge")' src/ui/challenge.js`.
    - `bash scripts/check-gate.sh` still passes (challenge.js itself is unchanged in its core contract).
    - `bash scripts/check-safety.sh` still passes.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/ui/challenge.js && grep -q 'renderChoices' src/ui/challenge.js && grep -q 'question' src/ui/challenge.js && grep -q 'return { close }' src/ui/challenge.js && grep -q 'onSuccess?.({ table: q.a })' src/ui/challenge.js && grep -q 'destroyAll("challenge")' src/ui/challenge.js && bash scripts/check-gate.sh && bash scripts/check-safety.sh && echo OK</automated>
  </verify>
  <done>challenge.js now supports caller-supplied question, suppressed answer boxes, and an external close handle, while remaining backward-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Add CONFIG.MATH_GATE, CONFIG.ENEMY, and CONFIG.COLLECT blocks</name>
  <files>src/config.js</files>
  <read_first>
    - src/config.js lines 59-75 — CONFIG.GATE and CONFIG.DOOR blocks, the precedents for feature-tuning blocks.
    - src/config.js lines 156-176 — CONFIG.SELECT block for color/size conventions.
  </read_first>
  <action>
    Append three new grouped tuning blocks to `src/config.js`, placed after CONFIG.DOOR
    (line 75) in the same "one block per feature" style:

    - `MATH_GATE`: `W: 32`, `H: 64`, `LOCKED_GREY: [0x44,0x44,0x44]`, `LOCKED_BORDER: [0x55,0x55,0x55]`, `GLYPH_SIZE: 22`.
      Mirrors CONFIG.DOOR's dimensions and locked-tile palette; the checkpoint gate reads
      as a related but distinct locked barrier.
    - `ENEMY`: `W: 32`, `H: 32`, `COLOR: [0xff,0x44,0x33]` (muted red, no pink), `GLYPH_SIZE: 22`.
      A compact square placeholder; Phase 18 will replace it with a sprite.
    - `COLLECT`: `ZONE_W: 64`, `ZONE_H: 64`, `PICKUP_W: 28`, `PICKUP_H: 28`, `PICKUP_SIZE: 18`, `CORRECT_GLOW: [0x00,0xff,0x88]` (neon green), `WRONG_GLOW: [0xff,0x44,0x33]` (muted red).
      Tunes the invisible trigger zone and the numeric pickup badges.

    Use only data literals (no engine calls), safe at module top level.
  </action>
  <acceptance_criteria>
    - `node --check src/config.js` passes.
    - `grep -q 'MATH_GATE:' src/config.js`.
    - `grep -q 'ENEMY:' src/config.js`.
    - `grep -q 'COLLECT:' src/config.js`.
    - `grep -q 'LOCKED_GREY: \[0x44, 0x44, 0x44\]' src/config.js` (palette preserved).
  </acceptance_criteria>
  <verify>
    <automated>node --check src/config.js && grep -q 'MATH_GATE:' src/config.js && grep -q 'ENEMY:' src/config.js && grep -q 'COLLECT:' src/config.js && grep -q 'LOCKED_GREY: \[0x44, 0x44, 0x44\]' src/config.js && echo OK</automated>
  </verify>
  <done>CONFIG.MATH_GATE, CONFIG.ENEMY, and CONFIG.COLLECT tuning blocks are present.</done>
</task>

<task type="auto">
  <name>Task 3: Author level-01 mechanic placements + keep allowedTables hard</name>
  <files>src/levels/level-01.js</files>
  <read_first>
    - src/levels/level-01.js FULL FILE — existing geometry idiom and the doors/checkpoints arrays.
    - src/config.js (updated in Task 2) for the new mechanic dimensions.
  </read_first>
  <action>
    Add four new arrays to `LEVEL_01.geometry`, placed after the existing `doors` array and
    before the forward-looking optional slots. Do not modify any existing array values.

    1. `mathGates`: two checkpoint gate descriptors, each on a different floor run so MECH-04
       "several in-level math gates" is unambiguous:
       - `{ x: 600, y: FLOOR_Y - CONFIG.MATH_GATE.H }` — just before gap 1, on the opening run.
       - `{ x: 1300, y: FLOOR_Y - CONFIG.MATH_GATE.H }` — on the final run, before the door at 1400.
    2. `enemies`: one enemy descriptor on the middle run:
       - `{ x: 1000, y: FLOOR_Y - CONFIG.ENEMY.H }`.
    3. `collectZones`: one invisible zone descriptor near the start for MECH-03,
       owning the four pickup slot indices:
       - `{ x: 300, y: FLOOR_Y - CONFIG.COLLECT.ZONE_H, slots: [0, 1, 2, 3] }`.
    4. `answerPickupSlots`: four slot positions around the zone where pickups will appear:
       - `{ x: 270, y: FLOOR_Y - 100 }`, `{ x: 330, y: FLOOR_Y - 100 }`,
         `{ x: 270, y: FLOOR_Y - 40 }`, `{ x: 330, y: FLOOR_Y - 40 }`.

    Also add a short comment above `allowedTables` reaffirming it stays `[6,7,8,9]`. Do NOT
    change the value.
  </action>
  <acceptance_criteria>
    - `node --check src/levels/level-01.js` passes.
    - `grep -q 'mathGates:' src/levels/level-01.js`.
    - `grep -q 'enemies:' src/levels/level-01.js`.
    - `grep -q 'collectZones:' src/levels/level-01.js`.
    - `grep -q 'answerPickupSlots:' src/levels/level-01.js`.
    - `allowedTables` is still `[6, 7, 8, 9]`: `grep -q 'allowedTables: \[6, 7, 8, 9\]' src/levels/level-01.js`.
    - Existing geometry arrays (floors, platforms, coins, spikes, goal, checkpoints, doors) are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/levels/level-01.js && grep -q 'mathGates:' src/levels/level-01.js && grep -q 'enemies:' src/levels/level-01.js && grep -q 'collectZones:' src/levels/level-01.js && grep -q 'answerPickupSlots:' src/levels/level-01.js && grep -q 'allowedTables: \[6, 7, 8, 9\]' src/levels/level-01.js && echo OK</automated>
  </verify>
  <done>level-01.js contains authored placements for two checkpoint gates, one enemy, and one collect-the-answer zone with four pickup slots; allowedTables stays hard [6,7,8,9].</done>
</task>

<task type="auto">
  <name>Task 4: Update smoke-progress.mjs expectedGeometry for the new arrays</name>
  <files>scripts/smoke-progress.mjs</files>
  <read_first>
    - scripts/smoke-progress.mjs lines 270-333 — the expectedGeometry literal and the deepEqual helper that checks Object.keys length first.
    - src/levels/level-01.js (updated in Task 3) for the exact new array values.
  </read_first>
  <action>
    Inside `scripts/smoke-progress.mjs`, in the `expectedGeometry` object literal under the
    LVL-02 regression block, add the four new geometry arrays with the EXACT same values as
    `src/levels/level-01.js`:

    - `mathGates: [{ x: 600, y: FLOOR_Y - CONFIG.MATH_GATE.H }, { x: 1300, y: FLOOR_Y - CONFIG.MATH_GATE.H }]`
    - `enemies: [{ x: 1000, y: FLOOR_Y - CONFIG.ENEMY.H }]`
    - `collectZones: [{ x: 300, y: FLOOR_Y - CONFIG.COLLECT.ZONE_H, slots: [0, 1, 2, 3] }]`
    - `answerPickupSlots: [{ x: 270, y: FLOOR_Y - 100 }, { x: 330, y: FLOOR_Y - 100 }, { x: 270, y: FLOOR_Y - 40 }, { x: 330, y: FLOOR_Y - 40 }]`

    This file already imports `CONFIG` and already computes `FLOOR_Y = CONFIG.FLOOR_Y`
    locally inside that block — reuse those, do not redeclare.
  </action>
  <acceptance_criteria>
    - `node --check scripts/smoke-progress.mjs` passes.
    - `grep -q 'mathGates:' scripts/smoke-progress.mjs`.
    - `grep -q 'enemies:' scripts/smoke-progress.mjs`.
    - `grep -q 'collectZones:' scripts/smoke-progress.mjs`.
    - `grep -q 'answerPickupSlots:' scripts/smoke-progress.mjs`.
    - `node scripts/smoke-progress.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/smoke-progress.mjs && grep -q 'mathGates:' scripts/smoke-progress.mjs && grep -q 'enemies:' scripts/smoke-progress.mjs && grep -q 'collectZones:' scripts/smoke-progress.mjs && grep -q 'answerPickupSlots:' scripts/smoke-progress.mjs && node scripts/smoke-progress.mjs && echo OK</automated>
  </verify>
  <done>scripts/smoke-progress.mjs expectedGeometry mirrors the new level-01.js arrays and exits 0.</done>
</task>

<task type="auto">
  <name>Task 5: Extend build.js to instantiate the new mechanic entities</name>
  <files>src/levels/build.js</files>
  <read_first>
    - src/levels/build.js FULL FILE — the doors loop (lines 115-149) is the direct precedent for creating a blocking entity plus a visual glyph.
    - src/config.js (updated in Task 2) for new mechanic dimensions/colors.
  </read_first>
  <action>
    Append four new loops to `buildLevel(levelData)`, after the existing doors loop and before
    the function closes. Each loop is guarded with `g.<array> ?? []`.

    1. `mathGates`: For each gate `g`, create a solid blocking collider
       `rect(CONFIG.MATH_GATE.W, CONFIG.MATH_GATE.H)` with `body({ isStatic: true })`, tag
       `"math-gate"`, color/ outline from CONFIG.MATH_GATE, and a separate "?" glyph text
       entity stashed as `gateObj.glyphObj` (same cleanup idiom as the door).
    2. `enemies`: For each enemy `e`, create a blocking collider
       `rect(CONFIG.ENEMY.W, CONFIG.ENEMY.H)` with `body({ isStatic: true })`, tag `"enemy"`,
       color from CONFIG.ENEMY, and a separate glyph text entity (e.g. "!") stashed as
       `enemyObj.glyphObj`.
    3. `collectZones`: For each zone `z`, create an INVISIBLE trigger area
       `rect(CONFIG.COLLECT.ZONE_W, CONFIG.COLLECT.ZONE_H)` with `area()`, `opacity(0)`, tag
       `"answer-zone"`. No `body()` — it must not block movement, only detect entry. Stash the
       owned slot indices on the zone object: `zoneObj.slots = z.slots`.
    4. `answerPickupSlots`: For each slot `s` (iterate with index `i`), create a small placeholder
       entity `rect(CONFIG.COLLECT.PICKUP_W, CONFIG.COLLECT.PICKUP_H)` with `area()`, tag
       `"answer-pickup-slot"` only (NOT `"answer-pickup"` yet — collect.js activates it by setting
       `value` and opacity), opacity(0) initially. These are the world positions where
       `collect.js` will place numeric values when the zone is entered. Stash the slot index on
       the object as `slotObj.slotIndex = i` so `collect.js` can map it to shuffled choices.

    All engine globals are referenced only inside `buildLevel` (a727c13 discipline).
  </action>
  <acceptance_criteria>
    - `node --check src/levels/build.js` passes.
    - `grep -q 'g.mathGates' src/levels/build.js` and `grep -q '"math-gate"' src/levels/build.js`.
    - `grep -q 'g.enemies' src/levels/build.js` and `grep -q '"enemy"' src/levels/build.js`.
    - `grep -q 'g.collectZones' src/levels/build.js` and `grep -q '"answer-zone"' src/levels/build.js`.
    - `grep -q 'g.answerPickupSlots' src/levels/build.js` and `grep -q '"answer-pickup-slot"' src/levels/build.js`.
    - The zone stores its owned slot indices: `grep -q 'zoneObj.slots' src/levels/build.js`.
    - Each slot stores its index: `grep -q 'slotObj.slotIndex' src/levels/build.js`.
    - Blocking entities use `body({ isStatic: true })` for math-gates and enemies, but NOT for the collect zone or pickups.
    - `bash scripts/check-safety.sh` still passes.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/levels/build.js && grep -q 'g.mathGates' src/levels/build.js && grep -q '"math-gate"' src/levels/build.js && grep -q 'g.enemies' src/levels/build.js && grep -q '"enemy"' src/levels/build.js && grep -q 'g.collectZones' src/levels/build.js && grep -q '"answer-zone"' src/levels/build.js && grep -q 'g.answerPickupSlots' src/levels/build.js && grep -q '"answer-pickup-slot"' src/levels/build.js && grep -q 'zoneObj.slots' src/levels/build.js && grep -q 'slotObj.slotIndex' src/levels/build.js && bash scripts/check-safety.sh && echo OK</automated>
  </verify>
  <done>build.js creates tagged entities for checkpoint gates, enemies, collect zones, and answer-pickup slots from the level descriptor.</done>
</task>

<task type="auto">
  <name>Task 6: src/mechanics/gates.js — multiple checkpoint gate mechanic (MECH-04)</name>
  <files>src/mechanics/gates.js</files>
  <read_first>
    - src/mechanics/door.js FULL FILE — copy the freeze-challenge-destroy-unfreeze pattern.
    - src/ui/challenge.js (updated in Task 1) for the exact openChallenge signature.
  </read_first>
  <action>
    Create `src/mechanics/gates.js` — a near-verbatim adaptation of `door.js` for the
    `"math-gate"` tag:

    - Header comment: state one-way dependency on `../ui/challenge.js`, a727c13 discipline,
      and that it handles multiple gates independently via Kaplay's `onCollide` object argument.
    - Imports: `import { openChallenge } from "../ui/challenge.js";` and `import * as fx from "../fx.js";`.
    - Export `function wireGates({ player, brain })`:
      - `const opened = new Set();`
      - `player.onCollide("math-gate", (gateObj) => { ... })`
      - Freeze: `player.vel = vec2(0); player.paused = true;`
      - Call `openChallenge({ brain, onSuccess() { opened.add(gateObj); fx.clearBurst(); destroy(gateObj); if (gateObj.glyphObj) destroy(gateObj.glyphObj); player.paused = false; } })`.

    The visual and behavior are identical to a door but use the `math-gate` tag so multiple
    independent checkpoint gates can coexist with doors.
  </action>
  <acceptance_criteria>
    - `node --check src/mechanics/gates.js` passes.
    - `grep -q 'export function wireGates' src/mechanics/gates.js`.
    - `grep -q 'import { openChallenge } from "../ui/challenge.js"' src/mechanics/gates.js`.
    - `grep -q 'player.onCollide("math-gate"' src/mechanics/gates.js`.
    - `grep -q 'player.paused = false' src/mechanics/gates.js`.
    - `grep -q 'destroy(gateObj)' src/mechanics/gates.js`.
    - `bash scripts/check-safety.sh` still passes.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/mechanics/gates.js && grep -q 'export function wireGates' src/mechanics/gates.js && grep -q 'import { openChallenge } from "../ui/challenge.js"' src/mechanics/gates.js && grep -q 'player.onCollide("math-gate"' src/mechanics/gates.js && grep -q 'player.paused = false' src/mechanics/gates.js && grep -q 'destroy(gateObj)' src/mechanics/gates.js && bash scripts/check-safety.sh && echo OK</automated>
  </verify>
  <done>src/mechanics/gates.js exports wireGates; each "math-gate" entity opens independently, destroys before unpausing, and never punishes.</done>
</task>

<task type="auto">
  <name>Task 7: src/mechanics/enemy.js — defeat-enemy mechanic (MECH-05)</name>
  <files>src/mechanics/enemy.js</files>
  <read_first>
    - src/mechanics/door.js FULL FILE — the freeze-challenge-destroy-unfreeze pattern.
    - src/ui/challenge.js (updated in Task 1) for the openChallenge signature.
  </read_first>
  <action>
    Create `src/mechanics/enemy.js` — another near-verbatim adaptation of `door.js` for the
    `"enemy"` tag, with a custom prompt and NO contact-damage branch:

    - Header comment: state one-way dependency on `../ui/challenge.js`, a727c13 discipline,
      and the explicit "no contact damage" contract.
    - Imports: `import { openChallenge } from "../ui/challenge.js";` and `import * as fx from "../fx.js";`.
    - Export `function wireEnemy({ player, brain })`:
      - `const defeated = new Set();`
      - `player.onCollide("enemy", (enemyObj) => { ... })`
      - Freeze, then `openChallenge({ brain, prompt: "Answer to defeat the guard:", onSuccess() { defeated.add(enemyObj); fx.clearBurst(); destroy(enemyObj); if (enemyObj.glyphObj) destroy(enemyObj.glyphObj); player.paused = false; } })`.

    There must be NO branch that calls `respawn()`, reduces HP, or otherwise punishes the
    player for touching the enemy. The ONLY resolution path is answering correctly.
  </action>
  <acceptance_criteria>
    - `node --check src/mechanics/enemy.js` passes.
    - `grep -q 'export function wireEnemy' src/mechanics/enemy.js`.
    - `grep -q 'import { openChallenge } from "../ui/challenge.js"' src/mechanics/enemy.js`.
    - `grep -q 'player.onCollide("enemy"' src/mechanics/enemy.js`.
    - `grep -q 'player.paused = false' src/mechanics/enemy.js`.
    - `grep -q 'destroy(enemyObj)' src/mechanics/enemy.js`.
    - No punishment path: `grep -c 'respawn' src/mechanics/enemy.js` is `0`.
    - `bash scripts/check-safety.sh` still passes.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/mechanics/enemy.js && grep -q 'export function wireEnemy' src/mechanics/enemy.js && grep -q 'import { openChallenge } from "../ui/challenge.js"' src/mechanics/enemy.js && grep -q 'player.onCollide("enemy"' src/mechanics/enemy.js && grep -q 'player.paused = false' src/mechanics/enemy.js && grep -q 'destroy(enemyObj)' src/mechanics/enemy.js && test "$(grep -c 'respawn' src/mechanics/enemy.js)" = "0" && bash scripts/check-safety.sh && echo OK</automated>
  </verify>
  <done>src/mechanics/enemy.js exports wireEnemy; touching an enemy opens a challenge, and a correct answer removes it with no contact damage.</done>
</task>

<task type="auto">
  <name>Task 8: src/mechanics/collect.js — collect-the-answer mechanic (MECH-03)</name>
  <files>src/mechanics/collect.js</files>
  <read_first>
    - src/mechanics/door.js FULL FILE — the freeze/unfreeze and onCollide patterns.
    - src/ui/challenge.js (updated in Task 1) for `renderChoices: false` and the `{ close }` return.
    - src/fx.js `pop` function — the brief non-punishing feedback to reuse on a wrong pickup.
  </read_first>
  <action>
    Create `src/mechanics/collect.js`:

    - Header comment: state one-way dependency on `../ui/challenge.js`, a727c13 discipline,
      and that this module generates the question itself so the pickups and the overlay stay
      in sync.
    - Imports: `import { openChallenge } from "../ui/challenge.js";` and `import * as fx from "../fx.js";`.
    - Export `function wireCollect({ player, brain })`:
      - `const cleared = new Set();` keyed by zone object.
      - `player.onCollide("answer-zone", (zoneObj) => { ... })`:
        - If `cleared.has(zoneObj)` return.
        - Freeze player.
        - `const q = brain.nextQuestion();`
        - Use `zoneObj.slots` (the array of slot indices stashed by build.js) to identify this
          zone's pickup slots.
        - Shuffle `q.choices` (Fisher-Yates or use the brain's internal shuffle if exported;
          here, do a local Fisher-Yates shuffle of a copy). For each slot index in
          `zoneObj.slots`, look up the `"answer-pickup-slot"` entity whose `slotIndex` matches,
          set `slotObj.value = shuffledChoices[i]`, make it visible (`slotObj.opacity = 1`), and
          add a centered `text(String(slotObj.value), { size: CONFIG.COLLECT.PICKUP_SIZE })`
          child so the number is readable.
        - `const challenge = openChallenge({ brain, question: q, renderChoices: false, prompt: \`Collect the answer to ${q.a} × ${q.b}\` });`
        - Register a pickup collision handler: `player.onCollide("answer-pickup-slot", (slotObj) => { ... })`. Inside the handler:
          - If `slotObj.value === undefined`, return (slot not activated yet — defensive).
          - `const correct = slotObj.value === q.answer;`
          - `brain.reportResult(q.a, correct);`
          - If correct: `cleared.add(zoneObj); challenge.close(); destroyPickups(zoneObj); player.paused = false; onSuccess?.({ table: q.a });`.
          - If wrong: `fx.pop(slotObj.pos.clone());` — brief non-punishing glow/shake; do NOT close challenge, do NOT unfreeze, do NOT despawn pickups.
      - Helper `destroyPickups(zoneObj)` destroys all `"answer-pickup-slot"` entities whose
        `slotIndex` is in `zoneObj.slots` and clears their `value`.

    The wrong-pickup path MUST NOT call `respawn()`, reduce progress, or close the overlay.
  </action>
  <acceptance_criteria>
    - `node --check src/mechanics/collect.js` passes.
    - `grep -q 'export function wireCollect' src/mechanics/collect.js`.
    - `grep -q 'import { openChallenge } from "../ui/challenge.js"' src/mechanics/collect.js`.
    - `grep -q 'renderChoices: false' src/mechanics/collect.js`.
    - `grep -q 'player.onCollide("answer-zone"' src/mechanics/collect.js`.
    - `grep -q 'player.onCollide("answer-pickup-slot"' src/mechanics/collect.js`.
    - `grep -q 'slotObj.value' src/mechanics/collect.js` (pickup values assigned from the SAME question).
    - `grep -q 'player.paused = false' src/mechanics/collect.js`.
    - Wrong-pickup path is non-punishing: `grep -c 'respawn' src/mechanics/collect.js` is `0` and no punishment tokens appear.
    - `bash scripts/check-safety.sh` still passes.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/mechanics/collect.js && grep -q 'export function wireCollect' src/mechanics/collect.js && grep -q 'import { openChallenge } from "../ui/challenge.js"' src/mechanics/collect.js && grep -q 'renderChoices: false' src/mechanics/collect.js && grep -q 'player.onCollide("answer-zone"' src/mechanics/collect.js && grep -q 'player.onCollide("answer-pickup-slot"' src/mechanics/collect.js && grep -q 'slotObj.value' src/mechanics/collect.js && grep -q 'player.paused = false' src/mechanics/collect.js && test "$(grep -c 'respawn' src/mechanics/collect.js)" = "0" && bash scripts/check-safety.sh && echo OK</automated>
  </verify>
  <done>src/mechanics/collect.js exports wireCollect; entering an answer-zone opens a prompt-only challenge, spawns numeric pickups from the SAME question, and clears only on the correct pickup with non-punishing wrong-pickup feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level geometry data → build.js entity instantiation | Static developer-authored JS; no runtime injection surface. |
| player collision → challenge → entity removal | Load-bearing ordering (destroy before unpause) must hold in gates.js, enemy.js, and collect.js. |
| collect.js question ↔ pickups | The SAME `q` object must be used for both the overlay prompt and the spawned pickup values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02-01 | Denial of Service | Missing `player.paused = false` in any mechanic | high | mitigate | Each mechanic task includes an automated grep for `player.paused = false`; 16-03 browser boot confirms movement post-success. |
| T-16-02-02 | Denial of Service | Destroy/unpause ordering wrong | medium | mitigate | Code action explicitly orders `destroy(...)` before `player.paused = false` in every success handler. |
| T-16-02-03 | Denial of Service | collect.js shows a different question than pickups | medium | mitigate | Task action passes `question: q` into `openChallenge` after generating `q` once; acceptance criteria grep for `renderChoices: false` and `question`. |
| T-16-02-04 | Tampering | Enemy accidentally deals contact damage | high | mitigate | enemy.js acceptance criteria assert `grep -c 'respawn'` is 0 and no punishment tokens; check-safety.sh whole-tree scan also guards. |
| T-16-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No installs in this plan. |
</threat_model>

<verification>
- Per task: `node --check` + targeted greps, plus `bash scripts/check-safety.sh` after Tasks 1, 5-8.
- After this plan (wave merge): `node scripts/smoke-progress.mjs` and `bash scripts/check-safety.sh` pass. `check-gate.sh` and `check-import-safety.sh` will still be RED at this point because `game.js` has not wired the new modules yet — that is 16-03's job.
</verification>

<success_criteria>
- challenge.js is extended backward-compatibly with optional `question`, `renderChoices`, and a `{ close }` return.
- CONFIG gains MATH_GATE, ENEMY, and COLLECT blocks.
- level-01.js gains authored mathGates, enemies, collectZones, and answerPickupSlots; allowedTables stays [6,7,8,9].
- build.js instantiates tagged entities for all four new arrays.
- scripts/smoke-progress.mjs stays green.
- gates.js, enemy.js, and collect.js exist, are a727c13-clean, and follow the freeze-challenge-destroy-unfreeze pattern.
</success_criteria>

<output>
Create `.planning/phases/16-remaining-mechanics-difficulty-curve/16-02-SUMMARY.md` when done.
</output>
