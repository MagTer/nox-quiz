---
phase: 16-remaining-mechanics-difficulty-curve
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - src/scenes/game.js
autonomous: false
requirements: [MECH-03, MECH-04, MECH-05, LVL-03]
must_haves:
  truths:
    - "src/scenes/game.js imports and calls wireGates({player, brain}), wireEnemy({player, brain}), and wireCollect({player, brain}) once each, after both player and brain exist"
    - "src/scenes/game.js still passes level.allowedTables into createBrain({ seedAccuracy, seedHistory, allowedTables: level.allowedTables }) — the LVL-03 seam is verified, not changed"
    - "The full static suite (check-gate.sh + check-import-safety.sh + check-safety.sh + smoke-progress.mjs) is green"
    - "In a real browser: checkpoint gates block, open on correct answer, and never soft-lock (MECH-04)"
    - "In a real browser: the enemy blocks, opens a challenge with a custom prompt, disappears on correct answer, and never deals contact damage or ends the run (MECH-05)"
    - "In a real browser: the collect zone opens a prompt-only challenge, spawns numeric pickups, clears on the correct pickup, and gives only non-punishing feedback on a wrong pickup (MECH-03)"
    - "In a real browser: wrong answers at any mechanic re-ask with no penalty, no progress loss, no despawn, and no restart"
    - "In a real browser: level-01 still feels like the v3.0 difficulty pool (tables 6-9)"
  artifacts:
    - path: "src/scenes/game.js"
      provides: "integration call sites for the three new mechanics and the verified LVL-03 allowedTables wiring"
      contains: "wireGates(|wireEnemy(|wireCollect("
  key_links:
    - from: "src/scenes/game.js"
      to: "src/mechanics/gates.js, src/mechanics/enemy.js, src/mechanics/collect.js"
      via: "imports and calls wireGates, wireEnemy, wireCollect once each"
      pattern: "wireGates\\(|wireEnemy\\(|wireCollect\\("
    - from: "src/scenes/game.js"
      to: "src/math/brain.js"
      via: "createBrain({ ..., allowedTables: level.allowedTables }) is preserved"
      pattern: "allowedTables: level\\.allowedTables"
---

<objective>
Wire the three new mechanic modules into `src/scenes/game.js` — the single integration
wave this phase has been building toward — then run the full static gate suite green
for the first time and perform the mandatory real-browser boot. The boot must prove
all four requirements together: collect-the-answer (MECH-03), multiple checkpoint gates
(MECH-04), defeat-enemy (MECH-05), and the unchanged allowedTables difficulty seam
(LVL-03). This is the ONLY task in the phase that touches `src/scenes/game.js`, and it
is purely additive.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-remaining-mechanics-difficulty-curve/16-CONTEXT.md
@.planning/phases/16-remaining-mechanics-difficulty-curve/16-RESEARCH.md

# The edit target:
@src/scenes/game.js

# The modules to wire:
@src/mechanics/gates.js
@src/mechanics/enemy.js
@src/mechanics/collect.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the three mechanics into game.js + run the full static suite green</name>
  <files>src/scenes/game.js</files>
  <read_first>
    - src/scenes/game.js lines 20-29 (import block), lines 96-98 (player creation), lines 220-228 (where wireDoor is called), and lines 76-82 (createBrain call with allowedTables).
    - src/mechanics/gates.js, src/mechanics/enemy.js, src/mechanics/collect.js — confirm exported signatures.
  </read_first>
  <action>
    Make three surgical edits to `src/scenes/game.js`:

    1. Add three imports alongside the existing sibling-module imports:
       `import { wireGates } from "../mechanics/gates.js";`
       `import { wireEnemy } from "../mechanics/enemy.js";`
       `import { wireCollect } from "../mechanics/collect.js";`
    2. Add three calls right after the existing `wireDoor({ player, brain });` line:
       `wireGates({ player, brain });`
       `wireEnemy({ player, brain });`
       `wireCollect({ player, brain });`
       Each call uses the SAME closure-local `brain` instance the end-of-level gate and door already use.
    3. Verify the `createBrain` call still passes `allowedTables: level.allowedTables`. Do NOT
       change it — LVL-03 is proven by this existing wiring, not by a code change.

    Make NO other change to this file. Do not touch `onReachGoal()`, checkpoint/coin/spike
    wiring, Escape, onUpdate, or onSceneLeave sweeps.

    After the edits, run the full static suite in order and confirm every stage prints PASS:
    `bash scripts/check-gate.sh`, `bash scripts/check-import-safety.sh`, `bash scripts/check-safety.sh`,
    and `node scripts/smoke-progress.mjs`. If ANY stage is red, STOP and report the failing
    assertion — do not proceed to Task 2's browser boot on a red static gate.
  </action>
  <acceptance_criteria>
    - `node --check src/scenes/game.js` passes.
    - `grep -q 'import { wireGates } from "../mechanics/gates.js"' src/scenes/game.js`.
    - `grep -q 'import { wireEnemy } from "../mechanics/enemy.js"' src/scenes/game.js`.
    - `grep -q 'import { wireCollect } from "../mechanics/collect.js"' src/scenes/game.js`.
    - `grep -q 'wireGates({ player, brain })' src/scenes/game.js`.
    - `grep -q 'wireEnemy({ player, brain })' src/scenes/game.js`.
    - `grep -q 'wireCollect({ player, brain })' src/scenes/game.js`.
    - LVL-03 seam preserved: `grep -q 'allowedTables: level.allowedTables' src/scenes/game.js`.
    - `bash scripts/check-gate.sh` exits 0 and prints "gate checks: PASS".
    - `bash scripts/check-import-safety.sh` exits 0 and prints "import-safety checks: PASS".
    - `bash scripts/check-safety.sh` exits 0 and prints "safety checks: PASS".
    - `node scripts/smoke-progress.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/scenes/game.js && grep -q 'import { wireGates } from "../mechanics/gates.js"' src/scenes/game.js && grep -q 'import { wireEnemy } from "../mechanics/enemy.js"' src/scenes/game.js && grep -q 'import { wireCollect } from "../mechanics/collect.js"' src/scenes/game.js && grep -q 'wireGates({ player, brain })' src/scenes/game.js && grep -q 'wireEnemy({ player, brain })' src/scenes/game.js && grep -q 'wireCollect({ player, brain })' src/scenes/game.js && grep -q 'allowedTables: level.allowedTables' src/scenes/game.js && bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/smoke-progress.mjs && echo OK</automated>
  </verify>
  <done>src/scenes/game.js imports and calls wireGates, wireEnemy, and wireCollect once each after wireDoor, with no other change; the full static suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mandatory real browser boot — all three mechanics + no punishment + LVL-03 seam</name>
  <files></files>
  <action>
    Serve the game over HTTP and drive every new mechanic in a real browser, then record a
    blocking human-verify sign-off. Follow the steps below precisely. This is a
    verification-only checkpoint; write no code. If a defect appears, route it back to 16-02
    with the exact symptom and re-verify after the fix.
  </action>
  <what-built>
    The shared `ui/challenge.js` seam now backs five callers: the end-of-level gate, the
    locked door, two checkpoint gates, one enemy, and one collect-the-answer zone. The static
    suite is fully green. This checkpoint is the irreplaceable runtime half: soft-locks,
    punishment drift, behavior drift on the existing gate, and the LVL-03 difficulty seam are
    all invisible to static gates.
  </what-built>
  <how-to-verify>
    1. Serve the game over HTTP from the repo root, e.g. `python3 -m http.server 8000`, then
       open `http://localhost:8000/` in a real browser.
    2. MECH-04 — checkpoint gates: on the opening run, walk into the first checkpoint gate
       near x:600. Confirm it physically blocks, the challenge overlay opens automatically,
       a wrong answer re-asks with no penalty, and a correct answer makes the gate disappear
       and the player can move/jump again. Continue to the final run and test the second gate
       near x:1300 independently — it must block and open on its own correct answer.
    3. MECH-05 — enemy: on the middle run, walk into the enemy near x:1000. Confirm it blocks
       the player, the challenge opens with a prompt like "Answer to defeat the guard:", a
       wrong answer re-asks with no penalty, and a correct answer makes the enemy disappear.
       Confirm touching the enemy NEVER deals damage, NEVER respawns the player, and NEVER
       ends the run.
    4. MECH-03 — collect-the-answer: near the start, walk into the collect zone around x:300.
       Confirm the overlay opens showing ONLY the question prompt (no answer boxes), and
       numeric pickups appear near the zone. Walk into the pickup matching the correct answer
       — confirm the overlay closes, the pickups disappear, and the player can move again.
       Re-enter the zone and walk into a WRONG pickup — confirm the pickup gives only a brief
       visual nudge/pop, the SAME question stays open, the pickups remain, and nothing else
       changes (no penalty, no progress loss, no respawn).
    5. General forgiveness: at every new mechanic, deliberately answer wrong at least twice.
       Confirm no XP loss, no despawn, no restart, no lockout, no countdown, and the SAME
       question remains until a correct answer.
    6. Existing behavior no-drift: play to the end-of-level goal and confirm the existing math
       gate still behaves identically (wrong re-asks, correct shows LEVEL CLEAR, awards XP,
       returns to select). Also confirm the existing locked door near x:1400 still blocks and
       opens cleanly.
    7. LVL-03 seam spot-check: open the browser console and inspect `level.allowedTables` or
       the questions asked — confirm they are drawn from tables 6-9 (the existing v3.0 feel),
       not an easier pool. There is no automated assertion for this; it is a human spot-check.
    8. Confirm `bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/smoke-progress.mjs` are all still green alongside the boot.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all of the following hold: checkpoint gates block/open independently
    with no soft-lock (MECH-04); enemy blocks, prompts, and disappears on correct answer with
    no contact damage (MECH-05); collect zone opens a prompt-only challenge, clears on the
    correct pickup, and never punishes on a wrong pickup (MECH-03); wrong answers re-ask with
    no penalty at every mechanic; the end-of-level gate and existing door still behave
    identically; level-01 still draws from tables 6-9; the full static suite is green. If
    anything fails, describe the exact symptom (which step, observed vs. expected) so it can
    be routed back to 16-02 and re-verified.
  </resume-signal>
  <verify>
    <human-check>Human confirms in a real browser: MECH-03/04/05 each work end-to-end with no punishment and no soft-lock; existing gate/door behavior is unchanged; LVL-03 allowedTables seam still feeds tables 6-9 for level-01; the full static suite is green alongside the boot.</human-check>
  </verify>
  <done>Human sign-off recorded that all three new mechanics work cleanly with no punishment and no soft-lock, existing behavior is unchanged, and the LVL-03 seam is verified; or a defect list routed back to 16-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static gate ↔ runtime boot | A green grep/smoke suite does not prove the player can still move after a gate opens or that wrong answers never punish — the browser run is authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-03-01 | Repudiation | Skipping the browser boot because static gates are green | high | mitigate | Task 2 is `autonomous:false` — a blocking human-verify makes the real browser boot non-skippable. |
| T-16-03-02 | Denial of Service | Leaked mechanic handlers across scene re-entry | low | accept | Each `wire*` registers object-scoped `player.onCollide` handlers, auto-cleaned when the player/scene tears down on `go()`. No new global app-bus controllers are introduced. |
| T-16-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No installs in this plan. |
</threat_model>

<verification>
- Task 1: the full static suite is green (`check-gate.sh && check-import-safety.sh && check-safety.sh && smoke-progress.mjs`) for the first time this phase.
- Task 2: a human confirms, live in a browser, that MECH-03/04/05 each work with no punishment/no soft-lock, existing gate/door behavior is unchanged, and the LVL-03 allowedTables seam still feeds tables 6-9 for level-01.
</verification>

<success_criteria>
- `src/scenes/game.js` wires `wireGates`, `wireEnemy`, and `wireCollect` in three additive calls, with zero other change.
- `createBrain({ ..., allowedTables: level.allowedTables })` is preserved unchanged.
- The full static gate suite is green.
- A human confirms the real-browser behavior for all three new mechanics, existing gate/door no-drift, and the LVL-03 seam.
</success_criteria>

<output>
Create `.planning/phases/16-remaining-mechanics-difficulty-curve/16-03-SUMMARY.md` when done.
</output>
