---
phase: 11-progression-persistence
plan: 01
type: execute
wave: 1
depends_on: ["11-00"]
files_modified:
  - src/progress.js
autonomous: true
requirements: [SAVE-01, SAVE-02]

must_haves:
  truths:
    - "createProgress(saved) returns an object whose XP/level math runs with ZERO storage access (callable in plain node, which has no localStorage)"
    - "addXp(table) awards 20 XP for tables 6–9 and 10 for tables 1–5, carries surplus across a level-up, and returns true exactly when a level-up occurs"
    - "threshold(level) === round(BASE_XP · LEVEL_MULT^(level-1)) (200, 260, ...) matching the validated v1/v2 curve verbatim"
    - "loadSave()/writeSave() guard a missing or blocked localStorage (return defaults / no-op) and never throw into the caller"
    - "A corrupt or wrong-version save falls back to defaults; deserialize copies only whitelisted, range-checked fields (no Object.assign/spread of the untrusted blob)"
  artifacts:
    - path: "src/progress.js"
      provides: "Pure XP/level factory + serialize/deserialize + guarded localStorage seam"
      exports: ["createProgress", "loadSave", "writeSave"]
      min_lines: 80
  key_links:
    - from: "src/progress.js"
      to: "src/config.js"
      via: "reads CONFIG.PROGRESS.* (XP/threshold) and CONFIG.SAVE.* (KEY/VERSION) — the ONLY import"
      pattern: "import \\{ CONFIG \\} from \"\\./config\\.js\""
    - from: "loadSave/writeSave"
      to: "localStorage (browser global)"
      via: "guarded by storageAvailable() + try-catch; never read at module top level or in createProgress"
      pattern: "storageAvailable"
---

<objective>
Create the pure `src/progress.js` — the heart of the phase. It owns the XP/level math AND the
localStorage seam but imports NOTHING from Kaplay (firewall #2), so the XP/level math is
headlessly testable in node. Port the validated v1/v2 curve VERBATIM (do not re-tune) and the
archive's persistence + explicit-field validation, hardened for node (which has no localStorage).

The load-bearing structural decision: createProgress() is PURE — it takes a `saved` object and
NEVER reads storage at construction. localStorage lives ONLY behind guarded loadSave()/writeSave().
This is what lets the Wave-0 smoke exercise SAVE-01 math without a browser.

Purpose: SAVE-01 (earn XP + level up on the v1/v2 curve) and SAVE-02 (versioned, validated,
guarded localStorage persistence). Output: src/progress.js exporting createProgress, loadSave, writeSave.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-progression-persistence/11-RESEARCH.md
@.planning/phases/11-progression-persistence/11-PATTERNS.md
@src/config.js
@src/math/brain.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/progress.js — pure createProgress() factory (XP/level + serialize)</name>
  <files>src/progress.js</files>
  <behavior>
    - threshold(1) === 200; threshold(2) === 260 (round(200·1.3)).
    - createProgress() (no save): getXp()===0, getLevel()===1.
    - addXp(7) → xp 20, returns false (20 < 200); addXp(3) → +10.
    - Repeated addXp on a hard table crossing 200 → returns true exactly on the cross; surplus carried (post-level xp === overflow, not 0); level incremented by 1.
    - createProgress({ xp: 50, level: 3 }) restores xp=50, level=3 (validated: numbers, xp>=0, level>=1 floored).
    - serialize(brainSnapshot) → { version: CONFIG.SAVE.VERSION, xp, level, accuracy, history }; missing snapshot → accuracy/history default to {}.
  </behavior>
  <read_first>
    - src/math/brain.js lines 10-26 — the FIREWALL + ANTI-LEAK header discipline to copy (no engine import; factory not IIFE singleton). NOTE: progress.js lives at src/ (NOT src/math/), so its config import is `./config.js` (ONE dot), unlike brain.js's `../config.js` (11-PATTERNS.md line 34).
    - archive/math-lab.html lines 648-657 — getLevelThreshold/calculateXp to port verbatim (read CONFIG.PROGRESS.* instead of bare CONFIG.*).
    - archive/math-lab.html lines 663-687 — closure xp/level state + addXp surplus carry-over (line 682: `xp -= threshold`, NOT xp=0 — DO NOT change, feels punishing).
    - archive/math-lab.html lines 713-716 — toJSON shallow-copy shape for serialize.
    - 11-PATTERNS.md lines 22-93 and 11-RESEARCH.md Pattern 1 lines 131-181 — the target factory shape (while-loop level-up per RESEARCH; getXp/getLevel/threshold/nextThreshold/addXp/serialize return shape).
    - 11-RESEARCH.md Pitfall 1 lines 341-345 — createProgress MUST NOT read localStorage (keeps it node-importable).
  </read_first>
  <action>
    Create src/progress.js with a firewall/anti-leak header (copy the discipline from brain.js lines 10-26, adapted: this module imports ONLY `./config.js` — zero Kaplay; localStorage lives behind a guarded seam, never at module top level or in createProgress). Import `{ CONFIG } from "./config.js"`.
    Export `createProgress(saved)` returning a fresh closure:
    - Closure `let xp`, `let level` seeded from `saved` with validation (xp: number && >=0 else 0; level: number && >=1 else 1, Math.floor'd) — same guards as archive fromJSON lines 723-725.
    - `threshold(lvl) = Math.round(CONFIG.PROGRESS.BASE_XP * Math.pow(CONFIG.PROGRESS.LEVEL_MULT, lvl - 1))` (port verbatim, archive 651).
    - `calculateXp(table) = CONFIG.PROGRESS.HARD_TABLES.includes(table) ? CONFIG.PROGRESS.XP_HARD : CONFIG.PROGRESS.XP_EASY` (archive 653).
    - Return: getXp(), getLevel(), threshold, nextThreshold: () => threshold(level), addXp(table), serialize(brainSnapshot).
    - addXp(table): `xp += calculateXp(table)`; use a `while (xp >= threshold(level)) { xp -= threshold(level); level += 1; leveledUp = true; }` loop (RESEARCH Pattern 1 — defensive; carry surplus, never xp=0); return whether a level-up happened.
    - serialize(brainSnapshot): return { version: CONFIG.SAVE.VERSION, xp, level, accuracy: brainSnapshot?.accuracy ?? {}, history: brainSnapshot?.history ?? {} }.
    Do NOT read localStorage in this factory. Do NOT add a module-level mutable `let xp/level` (anti-leak — state lives in the closure only).
  </action>
  <verify>
    <automated>node --check src/progress.js && node -e "import('./src/progress.js').then(m=>{const p=m.createProgress();if(p.threshold(1)!==200||p.threshold(2)!==260)throw'threshold';if(p.addXp(7)!==false||p.getXp()!==20)throw'hard xp';const q=m.createProgress();let lvl=false;for(let i=0;i<11;i++)lvl=q.addXp(7)||lvl;if(!lvl||q.getLevel()!==2)throw'levelup';if(q.getXp()!==20)throw'surplus';console.log('PASS')}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <done>createProgress is pure (node import + math run with no localStorage error); threshold curve matches 200/260; addXp awards 20/10 by table, carries surplus, returns true on level-up; serialize emits a version field. node --check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the guarded localStorage seam — storageAvailable/loadSave/writeSave + validated deserialize</name>
  <files>src/progress.js</files>
  <read_first>
    - archive/math-lab.html lines 844-903 — PersistenceStore load()/save() to port (DROP the migrate() v1→v2 branch entirely — CONTEXT lines 35-36 forbid migration; never read mathlab_save_*). Quota handling at lines 896-900.
    - archive/math-lab.html lines 718-743 — fromJSON explicit-field validation: copy named keys only (NEVER Object.assign/spread the untrusted blob — prototype-pollution mitigation T-01-01); parseInt(k,10) on JSON string keys; clamp accuracy to 0..1 and tables to 1..9; filter history to booleans + slice(-MASTERY_WINDOW).
    - 11-RESEARCH.md Pattern 2 lines 183-218 — the node-hardened storageAvailable()/loadSave()/writeSave() target (the three failure modes: localStorage undefined, getItem/setItem throwing, QuotaExceededError).
    - 11-RESEARCH.md Security Domain lines 518-537 — V5 input validation + the corrupt/quota DoS mitigations to encode.
  </read_first>
  <action>
    Append to src/progress.js (still importing only ./config.js):
    - `defaults()` — returns { xp:0, level:1, accuracy:{}, history:{} } (the shape createProgress and createBrain seed from).
    - `validate(data)` — explicit-field copy: xp = (number && >=0) ? data.xp : 0; level = (number && >=1) ? Math.floor : 1; accuracy = walk data.accuracy entries with parseInt(k,10), keep only table 1..9 && number 0..1; history = walk entries, parseInt key, keep arrays filtered to booleans and `.slice(-CONFIG.BRAIN.MASTERY_WINDOW)`. NEVER Object.assign(target, data) or spread the blob. Return { xp, level, accuracy, history }.
    - `storageAvailable()` — try { return typeof localStorage !== "undefined" && localStorage !== null } catch { return false } (accessing localStorage can throw in sandboxed iframes).
    - export `loadSave()` — if (!storageAvailable()) return defaults(); try { raw = localStorage.getItem(CONFIG.SAVE.KEY); if raw===null return defaults(); JSON.parse in an inner try-catch → on parse error console.warn + return defaults(); if data.version !== CONFIG.SAVE.VERSION console.warn + return defaults() (NO migration); return validate(data) } catch (e) { console.warn + return defaults() }.
    - export `writeSave(blob)` — if (!storageAvailable()) return; try { localStorage.setItem(CONFIG.SAVE.KEY, JSON.stringify(blob)) } catch (e) { if e?.name === "QuotaExceededError" console.warn("localStorage full…") else console.warn("save failed…") }. NEVER rethrow (forgiving — the game loop must not crash on a failed save).
    Keep all three functions out of module-top-level execution (defined, not called, at import time).
  </action>
  <verify>
    <automated>node --check src/progress.js && bash scripts/check-progress.sh 2>&1 | grep -qiE 'PASS|hud.js' && node -e "import('./src/progress.js').then(m=>{const d=m.loadSave();if(d.xp!==0||d.level!==1)throw'defaults';m.writeSave({version:1,xp:1,level:1,accuracy:{},history:{}});console.log('PASS')}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <done>loadSave() returns defaults under node (no localStorage) without throwing; writeSave() is a silent no-op under node; deserialize validates explicit fields with no spread of the blob; QuotaExceededError is caught and warned (never rethrown); version mismatch / corrupt JSON → defaults (no migration). `bash scripts/check-progress.sh` now passes its progress.js-firewall and QuotaExceededError greps (it may still FAIL later on the not-yet-existing hud.js — that is expected until Wave 3).</done>
</task>

</tasks>

<verification>
- `node --check src/progress.js` passes.
- `node -e "import('./src/progress.js')"` does NOT throw (proves no localStorage leak to module scope — RESEARCH Pitfall 1 warning sign).
- The Wave-0 firewall grep `! grep -qE 'kaplay|lib/kaplay' src/progress.js` holds (no engine import).
- `node scripts/smoke-progress.mjs` now passes the SAVE-01 threshold/XP/surplus and SAVE-02 round-trip assertions (the SAVE-03 statistical block needs the brain change in Wave 2; if smoke-progress imports brain.snapshot it will still pass SAVE-01/02 and only the seedAccuracy block reflects pre-Wave-2 brain behavior).
</verification>

<success_criteria>
- src/progress.js exports createProgress, loadSave, writeSave; imports ONLY ./config.js.
- XP/level math is pure and node-runnable; curve matches the validated v1/v2 values.
- Persistence is versioned, guarded for node/blocked/quota storage, and validates the blob field-by-field.
- No migration from the school game's save; no module-level mutable run state.
</success_criteria>

<output>
Create `.planning/phases/11-progression-persistence/11-01-SUMMARY.md` when done.
</output>
