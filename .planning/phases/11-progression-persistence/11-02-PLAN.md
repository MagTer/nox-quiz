---
phase: 11-progression-persistence
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/math/brain.js
  - src/ui/mathGate.js
  - src/scenes/game.js
autonomous: true
requirements: [SAVE-02, SAVE-03]

must_haves:
  truths:
    - "createBrain({ seedAccuracy }) injects validated saved per-table accuracy so a returning session resumes weak-spot weighting; createBrain() with no args still works for the gate's fallback caller"
    - "brain.snapshot() returns a read-only copy of accuracy/history; the brain still imports NOTHING from storage or Kaplay (firewall intact)"
    - "A correct answer at the gate fires onClear({ table }) carrying the cleared question's table (q.a); a wrong answer awards nothing (forgiving)"
    - "The scene loads the save on entry, seeds createBrain({ seedAccuracy: saved.accuracy }), awards XP via progress.addXp(table) on clear, and persists on both gate clear and tab-hide (onHide)"
    - "Run/session state (coins, goalReached, position) is NEVER written to the save"
  artifacts:
    - path: "src/math/brain.js"
      provides: "createBrain({ seedAccuracy }) + snapshot()"
      contains: "seedAccuracy"
    - path: "src/ui/mathGate.js"
      provides: "onClear({ table: q.a }) contract"
      contains: "onClear?.({ table: q.a })"
    - path: "src/scenes/game.js"
      provides: "boot load + seedAccuracy brain + addXp-on-clear + save on clear/onHide"
      contains: "seedAccuracy"
  key_links:
    - from: "src/scenes/game.js"
      to: "src/progress.js"
      via: "loadSave() on entry, progress.addXp(table) in onClear, writeSave(progress.serialize(brain.snapshot()))"
      pattern: "progress\\.addXp|writeSave\\("
    - from: "src/scenes/game.js"
      to: "src/math/brain.js"
      via: "createBrain({ seedAccuracy: saved.accuracy }) resumes adaptation (SAVE-03)"
      pattern: "seedAccuracy"
    - from: "src/ui/mathGate.js"
      to: "src/scenes/game.js onClear"
      via: "onClear?.({ table: q.a }) carries the cleared table"
      pattern: "onClear\\?\\.\\(\\{ table"
---

<objective>
Wire the round-trip that makes progression real: extend the brain to RESUME weak-spot adaptation
from saved accuracy (SAVE-03 — the highest-risk behavior), carry the cleared table through the
gate's onClear seam, and have the scene load on entry, award XP on clear, and persist on clear +
tab-hide (SAVE-02 wiring). The brain stays pure (the loader injects saved accuracy; the brain
never reads storage). XP is awarded ONLY on the correct path (forgiving mandate).

Purpose: SAVE-03 (returning resumes XP/level AND adaptation) + SAVE-02 (persist on every
XP/accuracy change). Output: brain seedAccuracy+snapshot, onClear({table}), scene wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-progression-persistence/11-RESEARCH.md
@.planning/phases/11-progression-persistence/11-PATTERNS.md
@src/math/brain.js
@src/ui/mathGate.js
@src/scenes/game.js
@src/progress.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend createBrain({ seedAccuracy }) + snapshot() — firewall intact</name>
  <files>src/math/brain.js</files>
  <behavior>
    - createBrain() with no args behaves exactly as today (gate fallback caller unchanged).
    - createBrain({ seedAccuracy: { 7: 0.05 } }) seeds table 7 to 0.05; drawing many nextQuestion() over-selects table 7 vs a fresh brain.
    - Invalid seed entries are ignored: keys outside 1..9, values outside 0..1, non-numbers, non-object seedAccuracy.
    - snapshot() returns { accuracy, history } as shallow copies; mutating the returned object does not change the brain's internal state.
    - The brain still imports nothing from storage or Kaplay.
  </behavior>
  <read_first>
    - src/math/brain.js lines 40-56 (factory signature + the accuracy literal 43-53 + history 55) and lines 174-205 (the returned object; reportResult ends at 204). DO NOT touch the locked selection math (lines 80-130) or the EWMA reportResult body (193-204).
    - src/math/brain.js lines 10-24, 36-38 — the firewall header (it currently says persistence is Phase 11 and that save/load was DROPPED). Update the wording: the brain still owns NO storage but now EXPOSES snapshot() for the loader to persist.
    - archive/math-lab.html lines 726-733 (accuracy validation rules) and 713-716 (toJSON shallow-copy shape).
    - 11-PATTERNS.md lines 117-138 and 11-RESEARCH.md Pattern 3 lines 221-248 — the additive target (seedAccuracy loop; optional seedHistory; snapshot()).
  </read_first>
  <action>
    Change the signature `export function createBrain()` to `export function createBrain({ seedAccuracy, seedHistory } = {})` (the default empty object keeps the gate's `createBrain()` fallback at mathGate.js line 58 working). After the accuracy literal (lines 43-53), inject saved values using the archive's validation rules: if seedAccuracy is an object, for each entry derive t = parseInt(key, 10); if t is within 1..9 and the value is a number within 0..1, set accuracy[t] = value. Optionally also seed history (archive 734-743 rules: arrays filtered to booleans, sliced to CONFIG.BRAIN.MASTERY_WINDOW) when seedHistory is provided — recommended for full mastery resume, though accuracy alone satisfies SAVE-03's wording.
    Add a `snapshot()` method to the returned object (after reportResult) returning shallow copies of accuracy and history (read-only — the brain reads NO storage; firewall intact).
    Update the firewall header comment (around lines 36-38 / 21) so it reflects: storage is still OUT of the brain, but snapshot() is the one-way export the loader serializes. Do NOT add any storage or Kaplay import.
  </action>
  <verify>
    <automated>node --check src/math/brain.js && ! grep -qE 'localStorage|lib/kaplay' src/math/brain.js && grep -q 'seedAccuracy' src/math/brain.js && grep -q 'snapshot' src/math/brain.js && node -e "import('./src/math/brain.js').then(m=>{const b=m.createBrain({seedAccuracy:{7:0.05}});const f=m.createBrain();let s=0,fr=0;for(let i=0;i<3000;i++){if(b.nextQuestion().a===7)s++;if(f.nextQuestion().a===7)fr++;}if(s<fr*1.4)throw new Error('no resume '+s+' vs '+fr);if(typeof m.createBrain().snapshot!=='function')throw new Error('no snapshot');console.log('PASS',s,fr)}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <done>createBrain accepts seedAccuracy (validated) and exposes snapshot(); a seeded weak table is over-selected vs a fresh brain (statistical resume proof); brain.js imports no storage/Kaplay; createBrain() still works with no args; node --check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Carry the cleared table through onClear({ table }) in the gate (one-line contract change)</name>
  <files>src/ui/mathGate.js</files>
  <read_first>
    - src/ui/mathGate.js line 217 (`onClear?.();` — the single correct-branch fire), lines 162-180 (the choose() correct/wrong branches; q.a is the table, already used at line 170 brain.reportResult(q.a, correct)), and the onClear JSDoc lines 52-54.
    - 11-PATTERNS.md lines 142-154 and 11-RESEARCH.md Pattern 4 lines 250-258 — the exact one-line change and the forgiving mandate (XP must be awarded ONLY through this correct-branch seam; the wrong path 174-180 stays XP-free).
  </read_first>
  <action>
    Change the correct-branch call at line 217 from `onClear?.();` to `onClear?.({ table: q.a });` — passing the cleared question's table (q.a). Update the onClear JSDoc (lines 52-54) to document the `{ table }` payload (the table the player just cleared). Make NO other change: do not import progress, do not call addXp here — the gate must remain the single correct-branch seam, and the wrong-answer path stays XP-free (forgiving). The gate still imports only the brain + CONFIG.
  </action>
  <verify>
    <automated>node --check src/ui/mathGate.js && grep -q 'onClear?.({ table: q.a })' src/ui/mathGate.js && ! grep -q 'addXp' src/ui/mathGate.js</automated>
  </verify>
  <done>onClear fires with { table: q.a } on the correct branch only; the JSDoc documents the payload; the gate does not reference addXp (forgiving — XP is awarded only in the scene); node --check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Scene wiring — load on entry, seed brain, addXp on clear, persist on clear + onHide</name>
  <files>src/scenes/game.js</files>
  <read_first>
    - src/scenes/game.js lines 19-24 (imports), line 56 (`const brain = createBrain();` — to replace), lines 127-154 (onReachGoal + the onClear hook currently just setting levelCleared=true), line 157 (the scene-level onUpdate registration — the bare-global, scene-scoped pattern to mirror for onHide), and lines 33-49 (the closure run state — coinsCollected/goalReached/lastCheckpoint that must NEVER be serialized).
    - 11-PATTERNS.md lines 158-190 and 11-RESEARCH.md Pattern 4 lines 250-268 + boot snippet lines 408-423 — the target wiring (load + createProgress + createBrain({seedAccuracy}) + mountHud INSIDE the scene closure; onClear({table}) drives addXp/hud/save; onHide save). RESEARCH Open Q2 (lines 460-463) locks load+construct INSIDE gameScene, NOT main.js.
    - 11-RESEARCH.md Pitfall 3 lines 353-357 — serialize takes ONLY {xp,level}+brain.snapshot(); run state stays closure-local.
  </read_first>
  <action>
    Add imports alongside lines 19-24: `import { createProgress, loadSave, writeSave } from "../progress.js";` and `import { mountHud } from "../ui/hud.js";` (hud.js is created in Wave 3 — this import resolves at runtime once that file exists; node --check of game.js does not resolve imports, so it passes now).
    Replace the bare `const brain = createBrain();` (line 56) with, inside the scene closure: `const saved = loadSave();` then `const progress = createProgress(saved);` then `const brain = createBrain({ seedAccuracy: saved.accuracy });` then `const hud = mountHud(progress);` then `hud.refresh();` (show loaded XP/level immediately). Keep all of these closure-local (anti-leak — never module-level).
    Extend the onClear hook (lines 144-151) to receive `{ table }`: set levelCleared=true; `const leveledUp = progress.addXp(table);` then `hud.refresh();` then `if (leveledUp) hud.flashLevelUp();` then `writeSave(progress.serialize(brain.snapshot()));` (persist on each clear — SAVE-02).
    Register a tab-hide save mirroring the line-157 onUpdate pattern: `onHide(() => writeSave(progress.serialize(brain.snapshot())));` (KAPLAY's visibilitychange wrapper — verified global; captures accuracy drift from wrong attempts before the tab closes).
    Do NOT pass coinsCollected, goalReached, lastCheckpoint, or player position into serialize — run state stays closure-local and is never persisted (CONTEXT line 37). Do NOT save on a timer (no setInterval — SAFE-01); save only on the clear event and onHide.
  </action>
  <verify>
    <automated>node --check src/scenes/game.js && grep -q 'seedAccuracy' src/scenes/game.js && grep -q 'progress.addXp' src/scenes/game.js && grep -q 'onHide(' src/scenes/game.js && grep -q 'writeSave(' src/scenes/game.js && ! grep -qE 'serialize\([^)]*coins|serialize\([^)]*goalReached' src/scenes/game.js && ! grep -qE 'setInterval' src/scenes/game.js</automated>
  </verify>
  <done>The scene loads the save on entry, seeds createBrain({ seedAccuracy: saved.accuracy }), mounts the HUD, awards XP via progress.addXp(table) in onClear, and persists via writeSave on both clear and onHide; no run/session state is serialized; no timer-based autosave; node --check passes.</done>
</task>

</tasks>

<verification>
- `node --check` passes on brain.js, mathGate.js, game.js.
- `! grep -qE 'localStorage|lib/kaplay' src/math/brain.js` (brain firewall intact); `grep -q 'seedAccuracy' src/math/brain.js && grep -q 'seedAccuracy' src/scenes/game.js` (both ends wired — SAVE-03).
- `! grep -q 'addXp' src/ui/mathGate.js` (forgiving — XP not awarded in the gate).
- `node scripts/smoke-progress.mjs` passes the SAVE-03 statistical weak-spot-resume block (seeded brain over-selects the weak table).
- Manual UAT (deferred to phase gate): earn XP on a weak table across visits; after reload, weak tables keep appearing more often (the highest-risk behavior — also covered by the headless smoke).
</verification>

<success_criteria>
- The brain resumes weak-spot adaptation from saved accuracy and exposes a pure snapshot(); firewall intact.
- onClear carries the cleared table; XP is awarded only on the correct path.
- The scene loads, seeds, awards, and persists (clear + onHide) with run state excluded from the save.
</success_criteria>

<output>
Create `.planning/phases/11-progression-persistence/11-02-SUMMARY.md` when done.
</output>
