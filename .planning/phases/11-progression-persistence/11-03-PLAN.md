---
phase: 11-progression-persistence
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/ui/hud.js
autonomous: false
requirements: [SAVE-04]

must_haves:
  truths:
    - "A fixed Kaplay HUD shows a level badge and an XP fill bar toward the next-level threshold, reading progress ONE-WAY (it never mutates progress)"
    - "refresh() updates the badge text and the fill width from progress.getLevel()/getXp()/nextThreshold()"
    - "flashLevelUp() shows a brief (<=500ms) ADHD-safe level-up moment that self-destroys, fired only when addXp returned a level-up"
    - "The HUD mounts inside the scene closure with tagged objects so it does not leak/stack across replays"
  artifacts:
    - path: "src/ui/hud.js"
      provides: "mountHud(progress) -> { refresh, flashLevelUp } fixed Kaplay HUD + level-up flash"
      exports: ["mountHud"]
      min_lines: 25
  key_links:
    - from: "src/ui/hud.js"
      to: "src/progress.js (via the passed progress object)"
      via: "reads progress.getLevel()/getXp()/nextThreshold() one-way; never calls addXp or assigns level"
      pattern: "progress\\.(getLevel|getXp|nextThreshold)"
    - from: "src/ui/hud.js"
      to: "src/config.js"
      via: "reads CONFIG.HUD layout constants — the only non-engine import"
      pattern: "CONFIG\\.HUD"
---

<objective>
Add the visible progression: a fixed Kaplay HUD (`src/ui/hud.js`) with a level badge + XP fill
bar that reads progress ONE-WAY, plus a distinct, ADHD-safe level-up moment. This is the SAVE-04
deliverable and the only genuinely new Kaplay code in the phase. It mirrors the mathGate.js
fixed()-overlay idiom exactly and reuses the dark-grunge palette (no pink).

The scene already imports and calls mountHud/refresh/flashLevelUp (wired in Wave 2); this plan
creates the module those calls resolve to.

Purpose: SAVE-04 (XP/level visible in-game + a level-up moment). Output: src/ui/hud.js exporting
mountHud(progress) returning { refresh, flashLevelUp }.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-progression-persistence/11-RESEARCH.md
@.planning/phases/11-progression-persistence/11-PATTERNS.md
@src/ui/mathGate.js
@src/config.js
@src/scenes/game.js
@src/progress.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/ui/hud.js — fixed level badge + XP bar (one-way) + level-up flash</name>
  <files>src/ui/hud.js</files>
  <read_first>
    - src/ui/mathGate.js lines 9-16 (engine-global discipline header — Kaplay primitives are bare globals, NOT imported; the only import is CONFIG via `../config.js` since src/ui/ is one dir below src/), lines 37-43 (the dark-grunge palette: PANEL_BORDER [0x33,0x33,0x33], ACCENT_GREEN [0x00,0xff,0x88] — reuse these), lines 68-104 (the add([... fixed(), z(), "tag"]) overlay idiom + tagged teardown).
    - src/scenes/game.js line 97 (the existing tween-flash idiom: tween(...easings.easeOutQuad)) — mirror for the level-up flash.
    - src/config.js CONFIG.HUD (added in Wave 0): X, Y, BADGE_SIZE, BAR_W, BAR_H, BAR_DY, FLASH_SIZE, FLASH_MS.
    - 11-PATTERNS.md lines 97-113 and 11-RESEARCH.md Pattern 5 lines 270-301 — the mountHud target (badge text + track rect + fill rect at high z; refresh() recomputes fill.width = BAR_W * clamp(getXp()/nextThreshold()); flashLevelUp() tweens opacity down then destroy).
    - 11-RESEARCH.md Pitfall 4 lines 359-363 (tag "hud"/"hud-flash", mount inside scene closure — anti double-mount) and Pitfall 5 lines 365-369 (FLASH_MS=450, no scale-bomb — ADHD-safe; NOT the archive's 800).
  </read_first>
  <action>
    Create src/ui/hud.js with an engine-global + firewall header copied from mathGate.js lines 9-16 (Kaplay primitives — add, fixed, z, rect, text, color, pos, anchor, opacity, width, center, tween, easings, destroy — are bare globals, NOT imported; the ONE import is `import { CONFIG } from "../config.js";`). Reuse the palette: track color [0x33,0x33,0x33], fill + badge color ACCENT_GREEN [0x00,0xff,0x88]. State the ONE-WAY contract in the header: the HUD reads progress.getLevel()/getXp()/nextThreshold() and NEVER calls progress.addXp or assigns progress.level.
    Export `mountHud(progress)`:
    - Read M = CONFIG.HUD.
    - Add a level badge: text "LVL " + progress.getLevel() at size M.BADGE_SIZE, pos(M.X, M.Y), fixed(), high z (e.g. 9000), ACCENT_GREEN, tagged "hud". Keep a ref.
    - Add an XP track rect(M.BAR_W, M.BAR_H) at pos(M.X, M.Y + M.BAR_DY), fixed(), z 9000, color [0x33,0x33,0x33], tagged "hud".
    - Add an XP fill rect(1, M.BAR_H) at the same pos, fixed(), z 9001, ACCENT_GREEN, tagged "hud". Keep a ref.
    - `refresh()`: badge.text = "LVL " + progress.getLevel(); frac = Math.min(1, progress.getXp() / progress.nextThreshold()); fill.width = Math.max(1, M.BAR_W * frac) (the rect comp width is settable).
    - `flashLevelUp()`: add a text "LEVEL UP" at size M.FLASH_SIZE, anchor("center"), pos(center()), fixed(), high z (e.g. 9500), ACCENT_GREEN, opacity(1), tagged "hud-flash"; tween opacity 1->0 over M.FLASH_MS/1000 with easings.easeOutQuad, then destroy the object. No scale-bomb; short and subtle (ADHD-safe).
    - return { refresh, flashLevelUp }.
    Do NOT add a module-level mutable singleton; do NOT mutate progress; do NOT use setInterval/setTimeout. Mount is via the scene calling mountHud() inside its closure (already wired Wave 2); tagged "hud" objects are destroyed by KAPLAY on scene teardown.
  </action>
  <verify>
    <automated>node --check src/ui/hud.js && grep -q 'export function mountHud' src/ui/hud.js && grep -q 'fixed(' src/ui/hud.js && grep -qE 'progress\.(getLevel|getXp|nextThreshold)' src/ui/hud.js && ! grep -qE 'progress\.(addXp|level[[:space:]]*=)' src/ui/hud.js && ! grep -qE 'setInterval|setTimeout' src/ui/hud.js && ! grep -qE 'import .*lib/kaplay' src/ui/hud.js</automated>
  </verify>
  <done>mountHud(progress) returns { refresh, flashLevelUp }; the HUD is fixed(), reads progress one-way (no addXp/level= writes), reuses the dark-grunge palette, mounts tagged "hud"/"hud-flash", and the flash is a short opacity tween that self-destroys; no timers; Kaplay primitives are bare globals; node --check passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — full progression loop in the browser (HUD, level-up, persist, resume)</name>
  <files>(no code change — browser UAT over the Wave 0–3 artifacts)</files>
  <action>
    Run the full automated gate first, then hand off to the human checkpoint below. From the
    repo root: `bash scripts/check-progress.sh` (must print "progress checks: PASS" now that
    all targets exist) and `node scripts/smoke-progress.mjs` (SAVE-01/02/03 assertions green).
    Then serve the game over HTTP and present the six manual checks in <how-to-verify> for the
    developer to confirm in the browser. Do NOT mark the plan complete until the developer
    types "approved".
  </action>
  <verify>
    <automated>bash scripts/check-progress.sh &amp;&amp; node scripts/smoke-progress.mjs</automated>
  </verify>
  <done>The automated gate is green (check-progress.sh PASS + smoke-progress PASS) and the developer has confirmed the six manual checks (visible HUD, earn XP + level-up flash, wrong = no XP, persist across close, resume XP/level + weak-spot adaptation, ADHD-safe flash) by typing "approved".</done>
  <what-built>The full progression loop is now wired end to end: a fixed HUD (level badge + XP bar) that reads loaded XP/level on entry, awards XP on a correct gate answer, shows a brief level-up flash on a threshold cross, persists to versioned localStorage (mathlab_platformer_v1) on clear + tab-hide, and resumes XP/level AND weak-spot adaptation on reload.</what-built>
  <how-to-verify>
    1. Serve the game over HTTP (file:// blocks ES modules): from the repo root run `python3 -m http.server 8000`, then open http://localhost:8000/index.html (or your index path) in a browser.
    2. SAVE-04 (visible HUD): confirm a "LVL 1" badge and an XP bar are visible top-left, dark-grunge, no pink.
    3. SAVE-01 (earn XP + level up): reach the goal and answer the math gate correctly; confirm the XP bar fills. Repeat clears (re-trigger the goal) until you cross a threshold; confirm a brief (~450ms) "LEVEL UP" flash appears and the badge increments to "LVL 2". Confirm a WRONG answer awards no XP (the bar does not move) and never ends the run.
    4. SAVE-02/03 (persist + resume): after earning some XP and leveling up, close the tab, reopen http://localhost:8000/...; confirm the badge/bar show the prior level/XP (restored from localStorage). In DevTools → Application → Local Storage, confirm a `mathlab_platformer_v1` key exists with a `version` field and xp/level/accuracy — and NO coins/position keys.
    5. SAVE-03 (adaptation resume): answer one weak table (e.g. 7×) wrong several times, let it persist (clear once, or just close — onHide saves), reload, and confirm that table keeps appearing more often than others (weak-spot weighting resumed).
    6. ADHD-safety: confirm the level-up flash is short and not over-stimulating (no big scale bomb, no lingering banner).
  </how-to-verify>
  <resume-signal>Type "approved" if all six check out, or describe what looked wrong (e.g. XP didn't persist, flash too long, adaptation reset).</resume-signal>
</task>

</tasks>

<verification>
- `node --check src/ui/hud.js` passes.
- `bash scripts/check-progress.sh` now passes fully (all targets exist: progress.js, hud.js, brain.js; firewall/one-way/forgiving greps green; smoke-progress.mjs green).
- `node scripts/smoke-progress.mjs` passes SAVE-01/02/03 assertions.
- HUD one-way grep holds: `! grep -qE 'progress\.(addXp|level[[:space:]]*=)' src/ui/hud.js`.
- Manual UAT checkpoint above confirms the visible HUD, level-up moment, persistence, and adaptation-resume in the browser.
</verification>

<success_criteria>
- A fixed dark-grunge HUD shows level + XP toward the next threshold, one-way from progress.
- A distinct, short, ADHD-safe level-up moment fires only on a threshold cross.
- The full phase loop (earn → level → persist → resume XP + adaptation) verifies in the browser.
- `bash scripts/check-progress.sh` is fully green at phase end.
</success_criteria>

<output>
Create `.planning/phases/11-progression-persistence/11-03-SUMMARY.md` when done.
</output>
