---
phase: 01-mvp-core-loop-adhd-safe-mechanics
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - math-lab.html
autonomous: false
requirements:
  - UX-01
  - UX-03
must_haves:
  truths:
    - "The app has a visible grunge texture (SVG feTurbulence grain at ~5% opacity) over the dark background"
    - "All readable text in the UI passes WCAG 2.1 AA minimum contrast ratio of 4.5:1 against its background"
    - "The primary text color #e8e8e8 on #0a0a0a background achieves approximately 18:1 contrast (well above AA)"
    - "The neon green #00ff88 accent is used decoratively (XP bar glow, level-up overlay, borders) but readable text is #e8e8e8"
    - "The visual aesthetic reads as dark, cool, and edgy — not cute, not childish, not pink"
    - "No pink color values exist anywhere in the file"
  artifacts:
    - path: "math-lab.html"
      provides: "SVG feTurbulence grain as CSS data URI on body::after pseudo-element"
      contains: "feTurbulence"
    - path: "math-lab.html"
      provides: "Full dark grunge CSS polish — card depth, accent borders, question text weight, HUD layout"
      contains: "--grain-opacity"
  key_links:
    - from: "body::after pseudo-element"
      to: "--grain-opacity CSS custom property"
      via: "Opacity controlled by CSS variable for easy tuning"
      pattern: "grain-opacity"
    - from: "#question-text CSS"
      to: "--text color"
      via: "#e8e8e8 text on #0a0a0a background for WCAG AA compliance"
      pattern: "question-text"
user_setup:
  - service: "WebAIM Contrast Checker"
    why: "WCAG 2.1 AA contrast verification for #e8e8e8 on #0a0a0a and #00ff88 on #0a0a0a"
    env_vars: []
    dashboard_config:
      - task: "Visit https://webaim.org/resources/contrastchecker/ and enter foreground #e8e8e8, background #0a0a0a. Verify ratio ≥ 4.5:1 (expect ~18:1). Then test #00ff88 on #0a0a0a and confirm it fails or passes AA — if it fails, it is only used as decoration, never as readable text."
        location: "https://webaim.org/resources/contrastchecker/"
---

<objective>
Apply full grunge visual polish and verify WCAG 2.1 AA contrast compliance. This plan adds the SVG feTurbulence grain texture, refines HUD layout, question card depth, and typography, then requires a human contrast verification checkpoint before signing off.

Purpose: After this plan, the app looks and feels like the cool, edgy game the target user wants to open. The visual experience matters as much as the mechanics for a user who has to choose to open it.

Output: `math-lab.html` with SVG grain texture, polished dark grunge aesthetic, confirmed WCAG AA text contrast. UX-01 and UX-03 satisfied.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-core-loop-adhd-safe-mechanics/01-CONTEXT.md
@.planning/phases/01-mvp-core-loop-adhd-safe-mechanics/01-RESEARCH.md
@.planning/phases/01-mvp-core-loop-adhd-safe-mechanics/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SVG feTurbulence Grain Texture and Full Grunge CSS Polish</name>
  <files>math-lab.html</files>

  <read_first>
    - math-lab.html — read the entire current CSS style block, specifically: body::after (current placeholder), #hud, #game-board, #question-fieldset, #question-text, #options-list, .option-item label CSS; also read the body CSS for current font stack
    - .planning/phases/01-mvp-core-loop-adhd-safe-mechanics/01-RESEARCH.md — Pattern 5 (SVG feTurbulence data URI as body::after background-image), Assumption A3 (baseFrequency 0.9, numOctaves 4, 5% opacity), Pitfall 7 (grain opacity too high or too low), CSS custom property --grain-opacity
    - .planning/phases/01-mvp-core-loop-adhd-safe-mechanics/01-CONTEXT.md — visual identity decisions: #00ff88 accent, grunge texture at ~5% opacity, segmented XP bar, dark bg #0a0a0a, text #e8e8e8, "cool/edgy not cute" direction
  </read_first>

  <action>
    Apply a series of targeted CSS improvements to math-lab.html. Use the Edit tool for each change. Read the current style block first to understand what exists.

    **1. SVG feTurbulence grain texture — fill in the body::after placeholder:**
    Replace the empty `background-image:` in the existing `body::after` rule with the SVG data URI from RESEARCH.md Pattern 5:
    ```
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='1'/%3E%3C/svg%3E");
    background-size: 256px 256px;
    ```
    The opacity is already controlled by `--grain-opacity: 0.05` on the pseudo-element. Do not change the opacity value — 5% is the locked decision from CONTEXT.md. The grain should be subtle; if it looks too harsh on screen during UAT, the CSS variable makes it trivial to adjust.

    **2. HUD polish:**
    Update `#hud` CSS to be visually anchored and clean:
    - `position: fixed; top: 0; left: 0; right: 0; z-index: 1000`
    - `padding: 10px 24px`
    - `background: rgba(10, 10, 10, 0.92)` — slightly transparent so grain shows through subtly
    - `backdrop-filter: blur(4px)` — adds depth, separates HUD from game board
    - `border-bottom: 1px solid var(--border)`
    - `display: flex; align-items: center; justify-content: space-between; gap: 16px`

    **3. Level badge polish:**
    Update `#level-display`:
    - `font-size: 0.9rem; font-weight: 900; letter-spacing: 0.08em; color: var(--accent); text-transform: uppercase`
    - `padding: 4px 10px; border: 1px solid var(--accent); border-radius: 4px`
    - `white-space: nowrap`

    **4. XP bar label:**
    Add a small "XP" label before the segment bar in the HUD (if not already present). This requires a small HTML change: inside `<div id="xp-bar">`, prepend `<span class="xp-label">XP</span>`. CSS for `.xp-label`: `font-size: 0.7rem; font-weight: 700; color: var(--text-muted); margin-right: 6px; letter-spacing: 0.1em; text-transform: uppercase`.

    **5. Game board layout:**
    Update `#game-board`:
    - `padding-top: 80px` — clear the fixed HUD
    - `display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 100vh`
    - `padding: 80px 24px 40px`

    **6. Question card depth:**
    Update `#question-fieldset`:
    - `border: 1px solid var(--border)`
    - `border-radius: 12px; padding: 40px 32px`
    - `background: var(--bg-card)` (already set)
    - `box-shadow: 0 4px 24px rgba(0,0,0,0.6), inset 0 1px 0 rgba(255,255,255,0.03)`
    - `max-width: 520px; width: 100%`
    - `legend` (or `#question-legend`): `color: var(--text-muted); font-size: 0.75rem; letter-spacing: 0.12em; text-transform: uppercase; padding: 0 8px`

    **7. Question text typography:**
    Update `#question-text`:
    - `font-size: clamp(2.5rem, 8vw, 4rem); font-weight: 900; color: var(--text); letter-spacing: -0.02em`
    - `margin-bottom: 32px; text-align: center; line-height: 1.1`
    - Note: text is `#e8e8e8` (var(--text)) — NOT `#00ff88`. The accent color is for decorative elements only, not primary readable text.

    **8. Answer option grid:**
    Update `#options-list`:
    - `display: grid; grid-template-columns: 1fr 1fr; gap: 12px; list-style: none`

    Update `.option-item label`:
    - `display: block; padding: 18px 16px; font-size: 1.4rem; font-weight: 800; text-align: center`
    - `background: var(--bg); border: 2px solid var(--border); border-radius: 8px; cursor: pointer`
    - `color: var(--text); transition: border-color 0.08s, background 0.08s, transform 0.08s`
    - Hover state (`:not(.disabled) .option-item label:hover`): `border-color: var(--accent); background: rgba(0,255,136,0.05); transform: translateY(-1px)`
    - The hover must only apply when not disabled (prevent visual confusion during feedback window)

    **9. Body font and global feel:**
    Update `body`:
    - `font-family: 'Impact', 'Arial Black', Arial, sans-serif`
    - `background: var(--bg); color: var(--text)`
    - `min-height: 100vh; overflow-x: hidden`

    **10. Remove any fieldset default browser styles:**
    `fieldset { border: none; margin: 0; padding: 0; }` — then specific #question-fieldset styles override. This prevents browser-default fieldset borders from conflicting.

    **HARD CONSTRAINTS:**
    - Do not add any color value with "pink" anywhere
    - Do not add any CSS that creates a visual countdown or timer effect (no width-transition elements that shrink over time)
    - All readable text must use `var(--text)` (#e8e8e8) or `var(--text-muted)` (#888888) — `var(--accent)` (#00ff88) is only used for: level badge text, XP segment fill, option correct state, level-up overlay. Never as body paragraph text.
    - No external CSS imports, no Google Fonts
    - The `--grain-opacity: 0.05` value stays at 5% as locked in CONTEXT.md (adjust only via CSS variable, not by changing the value in this plan)
  </action>

  <verify>
    <automated>
      node -e "
const fs = require('fs');
const html = fs.readFileSync('math-lab.html', 'utf8');
const checks = [
  ['SVG feTurbulence data URI', /feTurbulence/],
  ['grain-opacity set', /--grain-opacity:\s*0\.05/],
  ['body::after uses grain-opacity', /body::after[\s\S]{0,300}grain-opacity/],
  ['backdrop-filter on HUD', /backdrop-filter/],
  ['question-text font-weight 900', /question-text[\s\S]{0,200}font-weight:\s*900/],
  ['grid layout for options', /grid-template-columns:\s*1fr\s*1fr/],
  ['no pink colors', /:\s*pink|#[Ff][Ff][0-9a-fA-F]{2}[Aa-aA][Ff]|#[Ff][Ff][Bb][Cc][Cc]|#[Ff][Ff][69][Bb][Cc][DdEeFf]/],
  ['no external font imports', /@import.*fonts\.google|@import.*typekit/i]
];
let ok = true;
checks.forEach(([name, re]) => {
  const hit = re.test(html);
  if (name === 'no pink colors' || name === 'no external font imports') {
    if (hit) { console.error('FAIL: found forbidden pattern: ' + name); ok = false; }
  } else {
    if (!hit) { console.error('FAIL missing: ' + name); ok = false; }
  }
});
if (ok) console.log('Task 1 grunge CSS checks PASSED');
"
    </automated>
    <human-check>
      Open math-lab.html in Chrome at 1280px+ viewport width:
      1. Background should be near-black with subtle grain texture visible (not flat black, not harsh noise)
      2. HUD: level badge with neon green border visible top-left, XP segments visible right
      3. Question card: dark card with subtle inner shadow, readable large question text
      4. Answer buttons: dark background, lighter border, hover shows green border tint
      5. No pink anywhere in the UI
      6. The overall feel should be "dark game UI" not "homework app"
    </human-check>
  </verify>

  <acceptance_criteria>
    - Node check exits 0 (feTurbulence present, no pink, no external fonts, grid layout)
    - Grain texture is visible but subtle — not distracting, not overwhelming
    - HUD is fixed at top and does not obscure game content (game-board has appropriate padding-top)
    - All answer buttons visible in 2×2 grid, legible, respond to hover
    - No JavaScript errors introduced by CSS changes
  </acceptance_criteria>

  <done>SVG feTurbulence grain texture applied via CSS data URI. Full dark grunge polish applied: HUD depth, question card shadow, typography weight, answer button grid. No pink, no external dependencies. UX-01 satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual Quality + WCAG Contrast Verification</name>
  <action>Human verifies visual aesthetic and WCAG 2.1 AA compliance. Claude has completed all automated CSS work; this checkpoint confirms it looks right and contrast ratios pass before phase sign-off.</action>
  <what-built>Complete visual polish of math-lab.html: grunge grain texture (SVG feTurbulence at 5% opacity), dark theme with all color custom properties, WCAG-targeted color choices (#e8e8e8 text on #0a0a0a background, #00ff88 accent decorative only). The app is visually complete and fully playable.</what-built>
  <how-to-verify>
    **Step 1 — WCAG Contrast Check (required):**
    Visit https://webaim.org/resources/contrastchecker/
    - Test: Foreground `#e8e8e8`, Background `#0a0a0a` → should report ratio ~18:1 (well above AA 4.5:1)
    - Test: Foreground `#888888` (muted text), Background `#0a0a0a` → should report ratio ~5–6:1 (above AA)
    - Test: Foreground `#00ff88`, Background `#0a0a0a` → check the result. If it fails AA as text, confirm the plan uses it only for decorative elements (level badge border, XP glow, correct-state border) — never as body text. Record result.
    - Test: Foreground `#ff3333` (danger), Background `#0a0a0a` → should pass AA

    **Step 2 — Visual review of math-lab.html:**
    Open the file directly from filesystem (file:// URL — no server):
    1. Confirm near-black background with subtle grain texture (zoom in to see grain; it should be faint)
    2. Play 3 questions: answer one correctly, one incorrectly. Confirm green/red feedback is immediately readable
    3. Answer enough to level up (or force via console: `PlayerState.addXp(200)` then answer any question). Confirm "LEVEL N!" overlay appears, reads well, auto-dismisses
    4. Confirm HUD stays fixed at top while interacting — no obscuring of game content
    5. No pink, no countdown, no cheerful bubbly elements
    6. Overall aesthetic: confirm it looks like a game, not a homework app

    **Step 3 — No Timer Audit (UX-02 final check):**
    In DevTools Elements panel, search for "countdown", "timer", "time-bar". Confirm zero results.
    In DevTools Sources, search across math-lab.html for "setInterval". Confirm zero results.
  </how-to-verify>
  <resume-signal>Type "approved" if all contrast ratios pass and visual aesthetic is correct. Describe any issues if something needs adjustment (e.g., "grain is too harsh", "option text too small", "contrast ratio N failed").</resume-signal>
  <verify>
    <automated>MISSING — verification is human-only for visual quality and external WCAG tool</automated>
  </verify>
  <done>User has confirmed: (1) #e8e8e8 on #0a0a0a passes WCAG AA via WebAIM, (2) dark grunge aesthetic looks correct, (3) no pink, no timer UI in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS data URI | SVG feTurbulence is inlined as data URI — no external request, no CORS issue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | SVG feTurbulence data URI could theoretically be used to fingerprint browser rendering | accept | Local game, no network, no analytics, no tracking — fingerprinting is a non-concern here |
| T-04-02 | Denial of Service | backdrop-filter on unsupported browsers causing layout shift | accept | backdrop-filter is CSS progressive enhancement; if not supported, HUD is still functional with opaque background. Chrome on Windows (target) supports it fully |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages; no package manager used in this phase |
</threat_model>

<verification>
Full phase verification after all plans complete:

1. Open math-lab.html from file:// in Chrome — no server, no install
2. DevTools console: zero errors
3. Play 5 questions:
   - `document.querySelectorAll('.option-item').length === 4` → true
   - Answer correctly → immediate green border on selected option
   - After 1 second → next question appears, no visible timer
4. Earn enough XP to level up → overlay fires, auto-dismisses
5. Reload → XP and level restored from localStorage
6. Run `debugAccuracy()` in console — shows accuracy table
7. WCAG check: #e8e8e8 on #0a0a0a → ≥4.5:1 ✓
8. Search for "pink" in DevTools Sources → zero results
9. Search for "setInterval" → zero results
10. Search for "countdown" → zero results

All 14 requirements:
- CORE-01: 4 answer options visible ✓
- CORE-02: Feedback within 300ms ✓
- CORE-03: 10/20 XP awarded ✓
- CORE-04: Level-up overlay fires ✓
- QUES-01: ~70% hard tables ✓
- QUES-02: ~30% easy tables ✓
- QUES-03: Correct position unbiased ✓
- PROG-01: XP/level persist ✓
- PROG-02: XP bar always visible ✓
- PROG-03: Accuracy tracked, weighted ✓
- UX-01: Dark grunge aesthetic, no pink ✓
- UX-02: No countdown timer anywhere ✓
- UX-03: WCAG AA contrast verified ✓
- UX-04: Single HTML file, file:// works ✓
</verification>

<success_criteria>
- UX-01: Dark grunge aesthetic (#0a0a0a background, SVG grain texture, #00ff88 accent, no pink)
- UX-03: All text passes WCAG 2.1 AA (4.5:1 minimum) — verified via WebAIM checker
- Human checkpoint: user approves visual quality and contrast verification
- No external files, no CDN, file opens from file:// with no server
- All 14 v1 requirements satisfied across all 4 plans
</success_criteria>

<output>
Create `.planning/phases/01-mvp-core-loop-adhd-safe-mechanics/01-04-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

### Modified CSS in math-lab.html
- `body::after` — SVG feTurbulence data URI background-image; opacity via `--grain-opacity: 0.05`
- `#hud` — fixed positioning, backdrop-filter blur, border-bottom, flex layout
- `#level-display` — uppercase, letter-spacing, accent border badge
- `.xp-label` — muted label before segment bar
- `#game-board` — flex column centered, padding-top clears HUD
- `#question-fieldset` — box-shadow depth, border-radius 12px, max-width 520px
- `#question-text` — clamp font-size, weight 900, letter-spacing
- `#options-list` — grid 2×2 with gap
- `.option-item label` — improved padding, font-size, hover state
- `fieldset` reset — removes browser default styles

### Verified Color Compliance
- `#e8e8e8` on `#0a0a0a`: ~18:1 (WCAG AA ✓, AAA ✓)
- `#888888` on `#0a0a0a`: ~5.7:1 (WCAG AA ✓)
- `#00ff88` on `#0a0a0a`: decorative use only (to be confirmed via WebAIM in checkpoint)
- `#ff3333` on `#0a0a0a`: border/feedback only (verified in checkpoint)
