---
phase: 05-full-floor-loop-balance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - math-lab.html
autonomous: true
requirements:
  - DC-01
  - LOOT-01
  - LOOT-02
  - LOOT-03

must_haves:
  truths:
    - "CONFIG.DUNGEON contains 8 new constants: BOSS_HP_MULT, FINAL_BOSS_HP, FINAL_BOSS_XP, MAX_FLOORS, SWORD_BONUS, SHIELD_REDUCTION, POTION_HEAL, LOOT_DROP_RATE"
    - "FloorConfig.getFloor(4) returns a valid floor definition with enemy 'Dragon Lord', emoji '🐲', hp 40, xpReward 150, tablePools [7,8,9]"
    - "CombatEngine.resolveAnswer(true) with a sword in loot deals DAMAGE_CORRECT + SWORD_BONUS (4) to the enemy"
    - "CombatEngine.resolveAnswer(false) with a shield in loot deals DAMAGE_WRONG - SHIELD_REDUCTION (5) to the player"
    - "CombatEngine.resolveAnswer(false) with no shield deals full DAMAGE_WRONG (8) to the player"
    - "DungeonState.init() only resets HP, room, and loot — PlayerState XP and level are untouched"
  artifacts:
    - path: "math-lab.html"
      provides: "Extended CONFIG.DUNGEON, FloorConfig floor 4, loot-aware CombatEngine.resolveAnswer"
      contains: "BOSS_HP_MULT"
  key_links:
    - from: "CombatEngine.resolveAnswer"
      to: "DungeonState.get().loot"
      via: "loot snapshot read at resolve time; sword/shield flags checked against CONFIG.DUNGEON constants"
      pattern: "DungeonState\\.get\\(\\)\\.loot"
    - from: "FloorConfig"
      to: "CONFIG.DUNGEON"
      via: "FINAL_BOSS_HP and FINAL_BOSS_XP referenced by floor 4 definition"
      pattern: "FINAL_BOSS_HP"
---

<objective>
Extend CONFIG.DUNGEON with 8 balance constants, add Floor 4 (Final Boss) to FloorConfig, and make CombatEngine.resolveAnswer() loot-aware so sword/shield effects auto-apply at resolve time.

Purpose: This plan lays the data and logic foundation for the full loop. Wave 2 (DungeonRunner) depends on MAX_FLOORS, BOSS_HP_MULT, and loot constants being in place before orchestration is wired.

Output: math-lab.html with 8 new CONFIG constants, Floor 4 definition, and loot-modified damage math in resolveAnswer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@math-lab.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CONFIG.DUNGEON with 8 balance constants + add Floor 4 to FloorConfig</name>
  <files>math-lab.html</files>
  <read_first>
    - math-lab.html lines 581-594 (CONFIG.DUNGEON block — append constants after SAVE_KEY_V2)
    - math-lab.html lines 1013-1037 (FloorConfig FLOORS object — add floor 4 entry)
  </read_first>
  <action>
    In math-lab.html at the CONFIG.DUNGEON block (line 594, after SAVE_KEY_V2), append these 8 constants inside the same object literal (no separate statement):

      BOSS_HP_MULT:    2,      // boss room enemy HP multiplier (floor enemy HP * 2)
      FINAL_BOSS_HP:   40,     // Floor 4 Final Boss base HP
      FINAL_BOSS_XP:   150,    // XP awarded on Final Boss defeat
      MAX_FLOORS:      4,      // total number of dungeon floors
      SWORD_BONUS:     1,      // extra damage to enemy per correct answer when sword held
      SHIELD_REDUCTION: 3,     // damage reduction per wrong answer when shield held
      POTION_HEAL:     25,     // HP restored instantly when potion picked up
      LOOT_DROP_RATE:  0.6     // probability that a regular enemy drops loot (60%)

    In FloorConfig FLOORS object (line 1018, after the Dragon entry), add:

      4: { enemy: 'Dragon Lord', emoji: '🐲', hp: CONFIG.DUNGEON.FINAL_BOSS_HP, xpReward: CONFIG.DUNGEON.FINAL_BOSS_XP, tablePools: [7, 8, 9] }

    Use the same object structure as existing floor entries. No magic numbers — reference CONFIG.DUNGEON constants.

    Add Dragon Lord flavor text to DungeonRenderer FLAVOR object after the Dragon entry (line ~1258):

      'Dragon Lord': [
        'An ancient power stirs in the darkness.',
        'Its roar shakes the dungeon walls.',
        'The Dragon Lord opens one eye.'
      ]

    This allows renderCombatQuestion() to show flavor text for floor 4 without falling back to empty string.
  </action>
  <verify>
    <automated>grep -c "BOSS_HP_MULT" math-lab.html</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "BOSS_HP_MULT" math-lab.html returns 1 or more (constant exists)
    - grep -c "FINAL_BOSS_HP" math-lab.html returns 2 or more (defined in CONFIG, used in FloorConfig)
    - grep -c "MAX_FLOORS" math-lab.html returns 1 or more
    - grep -c "Dragon Lord" math-lab.html returns 2 or more (FloorConfig entry + FLAVOR entry)
    - Opening math-lab.html in a browser shows no console errors
    - In browser console: FloorConfig.getFloor(4) returns { enemy: 'Dragon Lord', emoji: '🐲', hp: 40, xpReward: 150, tablePools: [7,8,9] }
    - In browser console: CONFIG.DUNGEON.LOOT_DROP_RATE === 0.6
    - In browser console: CONFIG.DUNGEON.MAX_FLOORS === 4
  </acceptance_criteria>
  <done>CONFIG.DUNGEON has 8 new constants; FloorConfig.getFloor(4) returns Final Boss definition; Dragon Lord flavor lines exist in DungeonRenderer.FLAVOR</done>
</task>

<task type="auto">
  <name>Task 2: Make CombatEngine.resolveAnswer() loot-aware (sword/shield/potion effects)</name>
  <files>math-lab.html</files>
  <read_first>
    - math-lab.html lines 1130-1161 (CombatEngine.resolveAnswer — current implementation)
    - math-lab.html lines 1040-1101 (DungeonState — init, get, applyLoot methods)
    - math-lab.html lines 1085-1099 (DungeonState.applyLoot — how sword/shield/potion are stored)
  </read_first>
  <action>
    Replace the resolveAnswer() method body inside CombatEngine to read DungeonState.get().loot at the top of each call and derive effective damage values using CONFIG.DUNGEON constants. The loot snapshot must be taken once per call to avoid double-reads.

    At the start of resolveAnswer(isCorrect), before the if(isCorrect) branch, add:

      const lootSnapshot = DungeonState.get().loot;
      const effectiveDamageCorrect = CONFIG.DUNGEON.DAMAGE_CORRECT +
        (lootSnapshot.sword ? CONFIG.DUNGEON.SWORD_BONUS : 0);
      const effectiveDamageWrong = Math.max(0, CONFIG.DUNGEON.DAMAGE_WRONG -
        (lootSnapshot.shield ? CONFIG.DUNGEON.SHIELD_REDUCTION : 0));

    Replace the raw CONFIG.DUNGEON.DAMAGE_CORRECT reference in the correct-answer branch with effectiveDamageCorrect.
    Replace the raw CONFIG.DUNGEON.DAMAGE_WRONG reference in the wrong-answer branch with effectiveDamageWrong.

    Potion heal: DungeonState.applyLoot('potion') already increments loot.potions. The actual HP restore must happen when applyLoot is called from DungeonRunner (Task in Plan 02). No change to resolveAnswer() is needed for potions — potion effect is immediate-on-pickup, not per-combat-tick.

    CombatInputHandler.handleAnswer() calls DungeonRenderer.showDamageNumber() with CONFIG.DUNGEON.DAMAGE_CORRECT and CONFIG.DUNGEON.DAMAGE_WRONG as the displayed number. Update those two showDamageNumber calls in handleAnswer (lines ~1619-1630) to use the effective values returned from resolveAnswer. Since resolveAnswer doesn't currently return effective values, add effectiveDamageCorrect and effectiveDamageWrong to the returned object:

      return { enemyHP: 0, playerHP: ..., killed: true, died: false, leveledUp, effectiveDamageCorrect };
      return { enemyHP: ..., playerHP: 0, killed: false, died: true, leveledUp: false, effectiveDamageWrong };
      return { enemyHP: ..., playerHP: ..., killed: false, died: false, leveledUp: false, effectiveDamageCorrect, effectiveDamageWrong };

    Return effectiveDamageCorrect on all paths (value is always computed). Return effectiveDamageWrong on all paths.

    In CombatInputHandler.handleAnswer(), update the showDamageNumber calls to use result.effectiveDamageCorrect and result.effectiveDamageWrong instead of raw CONFIG constants. This ensures floating numbers reflect actual loot-modified damage.

    ADHD-03 compliance check: confirm DungeonState.init() (line 1051-1056) does NOT touch PlayerState. It resets only floor, room, playerHP, enemyHP, and loot. The existing implementation already satisfies this — do not change it. Add a comment above init() if none exists: "Resets dungeon session state only — PlayerState XP and level are untouched (ADHD-03)."
  </action>
  <verify>
    <automated>grep -c "effectiveDamageCorrect" math-lab.html</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "effectiveDamageCorrect" math-lab.html returns 3 or more (defined, used in resolve, used in showDamageNumber)
    - grep -c "lootSnapshot" math-lab.html returns 1 (single snapshot per resolveAnswer call)
    - grep -c "SWORD_BONUS" math-lab.html returns 2 or more (CONFIG definition + resolveAnswer usage)
    - Opening math-lab.html in browser shows no console errors
    - In browser console: CombatEngine.startCombat(1); DungeonState.applyLoot('sword'); result = CombatEngine.resolveAnswer(true); result.effectiveDamageCorrect === 4
    - In browser console (fresh combat, no sword): CombatEngine.startCombat(1); result = CombatEngine.resolveAnswer(true); result.effectiveDamageCorrect === 3
    - In browser console: DungeonState.applyLoot('shield'); result = CombatEngine.resolveAnswer(false); result.effectiveDamageWrong === 5
    - In browser console (no shield): result = CombatEngine.resolveAnswer(false); result.effectiveDamageWrong === 8
    - DungeonState.init() does not call PlayerState — grep confirms no "PlayerState" inside the init() function body
  </acceptance_criteria>
  <done>resolveAnswer() applies sword/shield loot modifiers from CONFIG constants; effective damage values returned for display; ADHD-03 confirmed: init() never touches PlayerState</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DungeonState.get() → CombatEngine | Snapshot read — caller cannot mutate session state (Object.assign pattern already in place) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | DungeonState.applyLoot | accept | applyLoot throws on unknown items; duplicate sword/shield ignored silently per LOOT-02; no external input reaches this call path |
| T-05-02 | Elevation of privilege | CONFIG.DUNGEON constants | accept | All constants are read-only object properties on a const; no user input modifies them; local file with no network surface |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan — vanilla JS, single file |
</threat_model>

<verification>
Open math-lab.html in browser. In the browser console:

1. `CONFIG.DUNGEON.MAX_FLOORS` → 4
2. `CONFIG.DUNGEON.BOSS_HP_MULT` → 2
3. `CONFIG.DUNGEON.LOOT_DROP_RATE` → 0.6
4. `FloorConfig.getFloor(4)` → { enemy: 'Dragon Lord', emoji: '🐲', hp: 40, xpReward: 150, tablePools: [7,8,9] }
5. `GameFSM.reset(); CombatEngine.startCombat(1); DungeonState.applyLoot('sword'); CombatEngine.resolveAnswer(true).effectiveDamageCorrect` → 4
6. `GameFSM.reset(); CombatEngine.startCombat(1); CombatEngine.resolveAnswer(false).effectiveDamageWrong` → 8
7. `DungeonState.applyLoot('shield'); CombatEngine.resolveAnswer(false).effectiveDamageWrong` → 5
8. No console errors on load
</verification>

<success_criteria>
- All 8 CONFIG.DUNGEON constants present and correctly valued
- FloorConfig.getFloor(4) returns Final Boss definition
- Dragon Lord flavor text entries exist
- resolveAnswer() reads loot at resolve time and returns effective damage in result object
- ADHD-03: DungeonState.init() confirmed not touching PlayerState
- No console errors
</success_criteria>

<output>
Create `.planning/phases/05-full-floor-loop-balance/05-01-SUMMARY.md` when done
</output>
