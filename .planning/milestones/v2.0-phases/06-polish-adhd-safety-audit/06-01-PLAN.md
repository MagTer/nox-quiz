---
phase: 06-polish-adhd-safety-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - math-lab.html
autonomous: true
requirements:
  - ENE-03
must_haves:
  truths:
    - "Each of the 4 enemy types (Goblin, Skeleton, Dragon, Dragon Lord) has exactly 3 flavor text lines"
    - "No flavor line contains the words 'correct', 'wrong', 'answer', or 'question'"
    - "Flavor tone is edgy and menacing — enemies feel threatening, not cartoonish"
    - "All 4 PLACEHOLDER comment lines are removed from the FLAVOR arrays"
    - "getFlavorText() rotation guard is unchanged — no edits to lines 1495–1510"
  artifacts:
    - path: "math-lab.html"
      provides: "Updated FLAVOR arrays at lines 1467–1491"
      contains: "const FLAVOR"
  key_links:
    - from: "FLAVOR arrays (lines 1468–1491)"
      to: "getFlavorText() (lines 1495–1510)"
      via: "FLAVOR[enemyName] array lookup"
      pattern: "FLAVOR\\[enemyName\\]"
---

<objective>
Replace the PLACEHOLDER flavor text in the FLAVOR arrays (Goblin, Skeleton, Dragon, Dragon Lord)
with final edgy-menacing RPG copy. This is the only code change in Phase 6.

Purpose: Deliver ENE-03 — each enemy type has 3+ distinct flavor text lines that prevent repetition
and reinforce the dark grunge aesthetic.

Output: math-lab.html with all 4 FLAVOR arrays populated with final copy; no PLACEHOLDER comments
remain; no forbidden display words; rotation guard untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace FLAVOR array placeholder lines with final RPG copy</name>
  <files>math-lab.html</files>
  <read_first>
    Read math-lab.html lines 1464–1491 before editing. Confirm the exact structure of the FLAVOR
    object and the 3-element arrays for each of the 4 enemy keys (Goblin, Skeleton, Dragon,
    Dragon Lord). Note: Dragon Lord at line 1486 already has real copy (not placeholders) — do
    not change it if its content is already non-placeholder and passes tone review. The 3
    placeholder-marked arrays are Goblin (1468–1473), Skeleton (1474–1479), Dragon (1480–1485).
    Read getFlavorText() at lines 1495–1510 to confirm it is unchanged after editing.
  </read_first>
  <action>
    Replace the string contents of the Goblin, Skeleton, and Dragon arrays — and review Dragon Lord
    — so each has exactly 3 edgy, menacing RPG lines. Tone guidance: enemies are sizing up the
    player; threatening, taunting, cool-dark. Not cartoonishly evil. Not childish.

    Use Edit to replace only the three arrays whose lines are currently placeholder copy. Do NOT
    touch any lines outside the FLAVOR object literal (lines 1467–1491). Do NOT touch
    getFlavorText() at lines 1495–1510.

    Forbidden words in any flavor string: "correct", "wrong", "answer", "question".
    Forbidden aesthetic: cute, bubbly, childish phrasing.

    Suggested copy at Claude's discretion — final wording is Claude's call within the tone
    constraint. Example directions (not required verbatim):

    Goblin: short, feral, cruel — imply it's done this before and won.
    Skeleton: detached and eerie — speaks as if from beyond death, unafraid.
    Dragon: ancient and contemptuous — as if the player is barely worth noticing.
    Dragon Lord: already confirmed good; keep unless it fails tone review.

    After writing, remove all PLACEHOLDER comment lines from the edited arrays. The comment
    at line 1464–1466 (block doc explaining FLAVOR purpose) must remain intact.

    Use a single Edit call scoped to the FLAVOR object (lines 1467–1491). Do not split into
    multiple edits across the four keys.
  </action>
  <verify>
    <automated>grep -n "PLACEHOLDER" math-lab.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PLACEHOLDER" math-lab.html` returns zero results (all placeholder comments removed)
    - `grep -c "'Goblin'" math-lab.html` confirms the Goblin key still exists in FLAVOR
    - Each of the 4 FLAVOR keys (Goblin, Skeleton, Dragon, Dragon Lord) contains exactly 3 string elements
    - None of the new strings contain the literal words: correct, wrong, answer, question
    - Lines 1495–1510 (getFlavorText) are byte-for-byte identical to before this edit — confirm
      by reading those lines after the edit completes
    - The FLAVOR object opens and closes with matching braces; no JSON/syntax errors
      (load math-lab.html in browser DevTools console — no SyntaxError on parse)
  </acceptance_criteria>
  <done>
    All 4 FLAVOR arrays contain 3 edgy, menacing strings each. No PLACEHOLDER comments remain.
    No forbidden display words appear in the new strings. getFlavorText() is unchanged.
    math-lab.html has no syntax errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File edit → browser | String literals written to HTML file; displayed via textContent (not innerHTML) — no XSS risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | FLAVOR string literals | accept | textContent assignment in renderCombat() prevents HTML injection; string values are static author-controlled content |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; single HTML file edit only |
</threat_model>

<verification>
After completing Task 1:

1. `grep -n "PLACEHOLDER" math-lab.html` — must return no output
2. `grep -n "correct\|wrong\|answer\|question" math-lab.html` scoped to FLAVOR block — must return zero hits
   within lines 1467–1491 (CSS class names and JS identifiers outside that block are excluded from audit scope)
3. Open math-lab.html in a browser, open DevTools console — confirm no SyntaxError
4. Enter dungeon, start combat — flavor text line appears in combat panel; cycling through rooms shows different lines
</verification>

<success_criteria>
- All 4 FLAVOR arrays populated with 3 final, edgy RPG lines each
- Zero PLACEHOLDER comments remain in the file
- Zero forbidden display words (correct/wrong/answer/question) in the new flavor strings
- math-lab.html passes browser syntax check (no console errors on load)
- getFlavorText() rotation guard is verified unchanged
</success_criteria>

<output>
Create `.planning/phases/06-polish-adhd-safety-audit/06-01-SUMMARY.md` when done.
</output>
