---
phase: 06-polish-adhd-safety-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .planning/phases/06-polish-adhd-safety-audit/VERIFICATION.md
autonomous: true
requirements:
  - ADHD-01
  - ADHD-02
  - ADHD-04
  - ADHD-05
must_haves:
  truths:
    - "VERIFICATION.md exists with explicit PASS/FAIL per checklist item and evidence for each"
    - "ADHD-01 verified: no timer UI element exists — setTimeout calls confirmed as invisible transition delays only"
    - "ADHD-02 verified: DungeonState.init() does not touch PlayerState XP or level on death/retry"
    - "ADHD-04 verified: all combat animation durations (setTimeout feedback pauses) are ≤500ms"
    - "ADHD-05 verified: death screen (data-panel='dead') contains zero dynamic stat injection"
    - "RPG copy audit verified: no dungeon panel text node renders 'correct', 'wrong', 'answer', or 'question' at runtime"
    - "SC-4 migration browser test documented as human verification step with exact instructions"
  artifacts:
    - path: ".planning/phases/06-polish-adhd-safety-audit/VERIFICATION.md"
      provides: "Phase 6 ADHD safety audit and RPG copy audit results"
      contains: "PASS"
  key_links:
    - from: "VERIFICATION.md"
      to: "math-lab.html source lines"
      via: "Line-referenced evidence for each PASS/FAIL item"
      pattern: "line \\d+"
---

<objective>
Read the existing math-lab.html source and produce a VERIFICATION.md documenting the Phase 6
ADHD safety audit and RPG copy audit. No code changes. The only file created by this plan is
VERIFICATION.md.

Purpose: Provide explicit, line-referenced PASS/FAIL evidence for ADHD-01, ADHD-02, ADHD-04,
ADHD-05, the RPG copy check (COMB-05), and the SC-4 migration human verification note.

Output: VERIFICATION.md at .planning/phases/06-polish-adhd-safety-audit/VERIFICATION.md with
one checklist section per requirement, each citing the specific line numbers that provide evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-adhd-safety-audit/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit math-lab.html — collect evidence for all checklist items</name>
  <files>math-lab.html</files>
  <read_first>
    Read the following line ranges of math-lab.html to gather evidence for each checklist item.
    Read each range exactly once — do not re-read:

    - Lines 580–595: death screen HTML (data-panel="dead") — check for stat injection elements
    - Lines 1425–1440: DungeonState.retryFloor() and DungeonState.init() call — verify PlayerState not touched
    - Lines 1607–1615: floating damage number setTimeout (fallback cleanup) — check duration
    - Lines 1808–1816: CombatInputHandler auto-advance setTimeout — check ADVANCE_DELAY_MS usage
    - Lines 1868–1888: kill and dead branch setTimeout calls — check ADVANCE_DELAY_MS usage
    - Lines 1916–1924: SC-5 assertion — confirm it is present and the formula
    - Lines 1467–1491: FLAVOR arrays after Plan 01 edits — confirm no forbidden words remain

    Additionally, search for ADVANCE_DELAY_MS definition to confirm its value:
      grep -n "ADVANCE_DELAY_MS" math-lab.html | head -5

    Search for any timer UI elements (countdown displays, interval-based counters):
      grep -n "setInterval\|countdown\|timer\|clock" math-lab.html | grep -v "^#" | grep -v "\/\/"

    Search for dungeon panel text nodes containing forbidden words at runtime — check
    textContent assignments and innerHTML writes scoped to dungeon panels:
      grep -n "textContent\|innerHTML" math-lab.html | grep -i "correct\|wrong\|answer\|question"

    Specifically for RPG copy scope: confirm showFeedback() at line 1619 only assigns "Attack!",
    "Critical hit!", or "You took a hit!" — read lines 1616–1625.

    Confirm DungeonState.init() at line 1430 — read lines 1429–1440. Verify the function body
    resets only floor-scoped state (HP, loot, room) and that the word "PlayerState" does not
    appear in the function body.
  </read_first>
  <action>
    This task is read-only verification. Do not modify math-lab.html.

    Collect and record the following evidence items for use in Task 2:

    ADHD-01 (No timer UI):
    - List every setTimeout call found in math-lab.html with its line number and duration.
    - Confirm setInterval is absent (or if present, explain why it is not a timer UI).
    - Note that setTimeout calls are invisible transition delays — no DOM element changes
      during the wait window except feedback color (already shown, not a countdown).

    ADHD-02 (No XP/level loss on death):
    - Read DungeonState.init() body (lines ~1430–1440).
    - Confirm the word "PlayerState" does not appear inside init().
    - Confirm retryFloor() calls DungeonState.init() but PlayerState is NOT reset.
    - Note the ADHD-03 comment at line 1433 as corroborating evidence.

    ADHD-04 (Animations ≤500ms):
    - Identify the value of CONFIG.ADVANCE_DELAY_MS (grep for its assignment).
    - Confirm all setTimeout calls in combat use CONFIG.ADVANCE_DELAY_MS (not hardcoded
      values > 500). The 600ms fallback at line 1611 is an animationend safety fallback for
      a DOM cleanup span — it is not a user-visible pause — note this distinction.
    - If ADVANCE_DELAY_MS ≤ 500, mark PASS. If > 500, mark FAIL and cite the line.

    ADHD-05 (Death screen: no comparison stats):
    - Read lines 586–591 (data-panel="dead").
    - Confirm the panel contains only: h2 "You Fell", p "The dungeon claims another soul.", and
      the retry button. No dynamic IDs for stat injection, no floor counter, no question count.

    RPG copy audit (COMB-05 scope):
    - Search result from: grep -n "textContent\|innerHTML" scoped to lines that are inside
      dungeon panel rendering code — confirm none assign "correct", "wrong", "answer", "question"
      as display strings.
    - Confirm showFeedback() outputs only "Attack!", "Critical hit!", "You took a hit!".
    - Note: CSS class names .correct / .wrong are NOT display text — excluded from audit.
    - Note: JS variable names and code comments are NOT display text — excluded.

    SC-4 migration (human verification note):
    - Document that SC-4 requires a browser test with a real v1 localStorage key
      (key: "mathLabProgress", schema version 1) and cannot be automated by code inspection alone.
    - Record the manual test steps: load v1 save in localStorage, open math-lab.html, confirm
      XP and per-table accuracy data carry over intact.

    SC-5 (damage cap — already passing):
    - Record the assertion at line 1921 and the formula:
      CONFIG.DUNGEON.DAMAGE_WRONG * CONFIG.DUNGEON.ROOMS_PER_FLOOR < CONFIG.DUNGEON.PLAYER_HP
      i.e. 8 * 5 = 40 < 100 — player survives with 60 HP.
    - Note this is already passing per Phase 5 verification.
  </action>
  <verify>
    <automated>grep -n "ADVANCE_DELAY_MS" /home/magnus/dev/nox-quiz/math-lab.html | head -10</automated>
  </verify>
  <acceptance_criteria>
    - ADVANCE_DELAY_MS value is identified (exact line and value recorded)
    - All setTimeout call sites noted with line numbers
    - DungeonState.init() confirmed to not contain "PlayerState"
    - death screen (lines 586–591) confirmed to have no dynamic stat elements
    - showFeedback() outputs confirmed as RPG-safe strings only
    - SC-4 human test steps documented
    - SC-5 formula values confirmed from source
  </acceptance_criteria>
  <done>
    All evidence collected from source — ready to write VERIFICATION.md in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write VERIFICATION.md with PASS/FAIL results and line-referenced evidence</name>
  <files>.planning/phases/06-polish-adhd-safety-audit/VERIFICATION.md</files>
  <read_first>
    No additional reads needed — all evidence was collected in Task 1. Use that evidence directly.
  </read_first>
  <action>
    Write VERIFICATION.md to .planning/phases/06-polish-adhd-safety-audit/VERIFICATION.md
    using the Write tool.

    The document must follow this exact structure:

    ---
    # Phase 6 Verification Report
    **Phase:** 06 — Polish + ADHD Safety Audit
    **Date:** [today's date]
    **Status:** [overall PASS or FAIL — PASS only if all checklist items pass]

    ## ADHD Safety Checklist

    ### ADHD-01: No Timer UI
    **Result:** PASS / FAIL
    **Evidence:**
    - List each setTimeout call with line number and duration
    - Confirm setInterval: absent / present (explain if present)
    - Confirm no countdown UI element exists
    - [If PASS] All setTimeout calls are invisible transition delays (no DOM countdown element)

    ### ADHD-02: No XP/Level Loss on Death
    **Result:** PASS / FAIL
    **Evidence:**
    - DungeonState.init() at line [N]: resets [list fields reset] — PlayerState not called
    - retryFloor() at line [N]: calls DungeonState.init() only; PlayerState XP and level unchanged
    - ADHD-03 comment at line [N] corroborates intent

    ### ADHD-03: Damage Cap (inherited from Phase 5)
    **Result:** PASS (carried forward)
    **Evidence:**
    - SC-5 assertion at line [N]: CONFIG.DUNGEON.DAMAGE_WRONG ([value]) × ROOMS_PER_FLOOR ([value]) = [product] < PLAYER_HP ([value])
    - Player survives 5 wrong answers with [remaining] HP remaining

    ### ADHD-04: Animations ≤500ms
    **Result:** PASS / FAIL
    **Evidence:**
    - CONFIG.ADVANCE_DELAY_MS = [value]ms (line [N]) — used by all combat setTimeout calls
    - setTimeout at line [N]: CONFIG.ADVANCE_DELAY_MS — [value]ms — PASS / FAIL
    - [Repeat for each setTimeout in combat]
    - Note: 600ms at line 1611 is animationend DOM-cleanup fallback for a span element — not a
      user-visible pause. Not subject to ≤500ms animation rule.

    ### ADHD-05: Death Screen Has No Comparison Stats
    **Result:** PASS / FAIL
    **Evidence:**
    - data-panel="dead" (lines [N]–[N]) contains:
      - h2: "You Fell" (static)
      - p: "The dungeon claims another soul." (static)
      - button#dead-retry: "Try Again" (static)
    - No dynamic IDs, no stat injection elements, no floor counter, no score display

    ## RPG Copy Audit (COMB-05)

    **Scope:** Dungeon panel text nodes and DungeonRenderer output only. v1 quiz panel
    (data-screen="quiz") is intentionally excluded.

    **Result:** PASS / FAIL

    **Evidence:**
    - showFeedback() at line [N]: assigns only "Attack!", "Critical hit!", "You took a hit!" — no forbidden words
    - FLAVOR arrays (lines [N]–[N]): no string contains "correct", "wrong", "answer", "question"
    - Grep result: `grep -n "textContent\|innerHTML"` scoped to dungeon renderer — no forbidden
      display strings found
    - CSS class names .correct / .wrong: excluded (not display text)
    - JS variable names and comments: excluded (not display text)

    ## SC-4 Migration Verification (Human Browser Test Required)

    **Result:** REQUIRES HUMAN VERIFICATION — not automatable via code inspection

    **Why manual:** SC-4 requires a real v1 localStorage key ("mathLabProgress", schema v1)
    to be present in the browser before loading the app. Code inspection can confirm the
    migration branch exists but cannot execute it without a live browser environment.

    **Manual test steps:**
    1. Open browser DevTools console on any page
    2. Run: `localStorage.setItem('mathLabProgress', JSON.stringify({version:1, xp:500, level:3, accuracy:{}}))`
    3. Open math-lab.html
    4. Open DevTools → Application → Local Storage
    5. Verify "mathLabProgress" key now has version:2 (or current schema version)
    6. Verify XP value 500 is preserved in the migrated data
    7. Verify per-table accuracy object is preserved (or empty object if v1 had none)
    8. Confirm no console errors during migration

    **Expected result:** XP and level carry over intact; schema version increments; no data loss.

    ## Summary

    | Check | Requirement | Result |
    |-------|-------------|--------|
    | No timer UI | ADHD-01 | PASS / FAIL |
    | No XP loss on death | ADHD-02 | PASS / FAIL |
    | Damage cap | ADHD-03 | PASS (Phase 5) |
    | Animations ≤500ms | ADHD-04 | PASS / FAIL |
    | Death screen: no stats | ADHD-05 | PASS / FAIL |
    | RPG copy audit | COMB-05 | PASS / FAIL |
    | Migration SC-4 | PROG2-03 | HUMAN VERIFY |
    ---

    Substitute actual values for all bracketed placeholders. Do not leave [N] or [value]
    unfilled — pull exact line numbers and values from the evidence collected in Task 1.
    Set the overall Status to PASS only if every automatable check passes.
  </action>
  <verify>
    <automated>grep -c "PASS\|FAIL" /home/magnus/dev/nox-quiz/.planning/phases/06-polish-adhd-safety-audit/VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - VERIFICATION.md exists at .planning/phases/06-polish-adhd-safety-audit/VERIFICATION.md
    - File contains all 5 ADHD checklist sections (ADHD-01 through ADHD-05) with explicit PASS/FAIL
    - File contains RPG copy audit section with explicit PASS/FAIL
    - File contains SC-4 human verification section with exact manual test steps
    - File contains summary table with one row per check
    - No [N] or [value] placeholders remain — all replaced with actual line numbers and values
    - `grep -c "PASS\|FAIL" VERIFICATION.md` returns at least 6 (one per check)
    - Overall Status line reflects the aggregate result
  </acceptance_criteria>
  <done>
    VERIFICATION.md written with all checklist items documented, line-referenced, and marked
    PASS/FAIL based on actual source evidence. SC-4 section includes exact browser test steps.
    No placeholder text remains.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source read → VERIFICATION.md | Read-only analysis of math-lab.html; VERIFICATION.md is a planning artifact, not served content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | VERIFICATION.md | accept | Planning artifact stored locally; no sensitive data; not served to end users |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; read-only audit + file creation only |
</threat_model>

<verification>
After completing both tasks:

1. `ls .planning/phases/06-polish-adhd-safety-audit/VERIFICATION.md` — file must exist
2. `grep -c "PASS\|FAIL" .planning/phases/06-polish-adhd-safety-audit/VERIFICATION.md` — must return ≥6
3. `grep "HUMAN VERIFY" .planning/phases/06-polish-adhd-safety-audit/VERIFICATION.md` — must return SC-4 row
4. `grep "\[N\]\|\[value\]" .planning/phases/06-polish-adhd-safety-audit/VERIFICATION.md` — must return empty
   (no unfilled placeholders)

Human verification (SC-4) is documented with exact steps but requires browser execution outside
this automated plan — it is explicitly flagged as a human checkpoint in the VERIFICATION.md.
</verification>

<success_criteria>
- VERIFICATION.md exists with PASS/FAIL evidence for ADHD-01, ADHD-02, ADHD-04, ADHD-05
- RPG copy audit section present with PASS/FAIL and line references
- SC-4 migration section present with exact manual test steps
- Summary table present with one row per check
- All line number references point to actual math-lab.html content
- No unfilled placeholder text in the document
</success_criteria>

<output>
Create `.planning/phases/06-polish-adhd-safety-audit/06-02-SUMMARY.md` when done.
</output>
