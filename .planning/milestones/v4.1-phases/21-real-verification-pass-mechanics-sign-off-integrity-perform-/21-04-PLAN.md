---
phase: 21-real-verification-pass-mechanics-sign-off-integrity-perform-
plan: 04
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/ui/challenge.js
  - src/mechanics/enemy.js
  - src/levels/build.js
  - src/mechanics/collect.js
  - .planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-FINDINGS.md
autonomous: true
requirements: [VERIFY-02]
must_haves:
  truths:
    - "the enemy encounter now displays the actual arithmetic problem (e.g. '...6 × 7') alongside the guard label, not just the label alone"
    - "challenge.js's question-prompt and answer-box text() calls, and build.js's door/math-gate/enemy glyph text() calls, have explicit color(), matching the codebase's established text()+color() convention"
    - "a full re-run of scripts/audit-phase21-mechanics.mjs across all 4 levels confirms the enemy fix visually and shows zero regressions in door/gates/mathGate/collect resolution"
  artifacts:
    - "src/mechanics/enemy.js (fixed — openChallenge call uses label instead of prompt)"
    - "src/ui/challenge.js (additive label param + defensive color() on both text() calls)"
    - "src/levels/build.js (defensive color() on door/math-gate/enemy glyph text() calls)"
  key_links:
    - "src/mechanics/enemy.js -> src/ui/challenge.js via openChallenge({ label }) — label PREFIXES rather than replaces the default q.a × q.b display, leaving door.js/gates.js/mathGate.js (which never pass label or prompt) and collect.js (which keeps using prompt, unaffected) byte-identical in behavior"
    - "21-FINDINGS.md's Post-Fix Verification section cites the regenerated screenshots/level-01-enemy-1000-before.png as the visual proof this fix actually resolved the live-reported bug, not just a code-level assertion"
---

<objective>
Apply the fixes confirmed by Plan 21-01's interactive audit (enemy.js's arithmetic-display bug,
defensive `color()` consistency across challenge.js and build.js's glyphs, and — conditionally —
the collect-zone dim-overlay contrast fix), then re-run the full interactive audit to prove the
fixes work and nothing regressed.

Purpose: VERIFY-02 requires real bugs found by the interactive audit to be fixed AND
re-verified, the same way the 6 v4.0 post-ship bugs were. This plan is the fix half; Plan 21-01
already did the "verify before fixing" half (per CONTEXT.md Area 1's explicit instruction — the
challenge.js color() hypothesis is already falsified at the code level in 21-RESEARCH.md, so this
plan must not blindly "fix" it without 21-FINDINGS.md's confirmation).

Output: `src/mechanics/enemy.js` and `src/ui/challenge.js` fixed (additive `label` param),
`src/levels/build.js` glyphs gain defensive `color()`, and `21-FINDINGS.md` gains a
`## Post-Fix Verification` section proving the fixes via a full 4-level re-run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-CONTEXT.md
@.planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-RESEARCH.md
@.planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-PATTERNS.md

# This plan's own gating input — Plan 21-01's diagnostic output (genuine dependency: the exact
# edits below are conditioned on what 21-01 confirmed, per Interface Context guidance)
@.planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-FINDINGS.md

# Existing files this plan edits directly
@src/ui/challenge.js
@src/mechanics/enemy.js
@src/levels/build.js
@src/mechanics/collect.js
@scripts/check-gate.sh
@scripts/audit-phase21-mechanics.mjs

<interfaces>
Current src/ui/challenge.js signature + display line (line 70, 77-78):
  export function openChallenge({ brain, onSuccess, prompt, question, renderChoices = true } = {}) {
    const q = question ?? brain.nextQuestion();
    const display = prompt ?? `${q.a} × ${q.b}`;

Current src/mechanics/enemy.js buggy call (lines 41-55):
  openChallenge({
    brain,
    prompt: "Answer to defeat the guard:",   // REPLACES q.a × q.b entirely — the bug
    onSuccess() { defeated.add(enemyObj); fx.clearBurst(); destroy(enemyObj);
      if (enemyObj.glyphObj) destroy(enemyObj.glyphObj); player.paused = false; },
  });

Current src/mechanics/collect.js call (lines 87-92, unaffected unless Finding 3 confirmed):
  const challenge = openChallenge({ brain, question: q, renderChoices: false,
    prompt: `Collect the answer to ${q.a} × ${q.b}` });

Established text()+color() convention to copy (src/scenes/select.js:42, 81, 127, 140):
  const LABEL_FG = [0xe8, 0xe8, 0xe8]; // tile number + heading text (#e8e8e8, ~18:1)
  ...
  color(LABEL_FG[0], LABEL_FG[1], LABEL_FG[2]),

scripts/check-gate.sh's assertions on challenge.js (existence/call-site greps only — confirmed
this session, NOT signature-specific, unaffected by an additive param):
  grep -q 'export function openChallenge'
  grep -q 'brain.nextQuestion' / 'brain.reportResult'
  grep -q 'fixed('
  grep -q 'cancel()'
  grep -Eq 'destroy\(|destroyAll\('
  (each mechanic file): grep -q 'openChallenge'
No check-gate.sh edit is needed for this plan's additive label/dim params.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply confirmed fixes to challenge.js, enemy.js, build.js, and (conditionally) collect.js</name>
  <files>src/ui/challenge.js, src/mechanics/enemy.js, src/levels/build.js, src/mechanics/collect.js</files>
  <read_first>
    .planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-FINDINGS.md
    (read this FIRST — the exact edits below are gated on its Finding 1/2/3 verdicts), src/ui/
    challenge.js (current), src/mechanics/enemy.js (current), src/levels/build.js (current),
    src/mechanics/collect.js (current), src/scenes/select.js (lines 42, 81 — the LABEL_FG
    convention to copy), scripts/check-gate.sh (confirm its assertions are existence/call-site
    greps only, unaffected by an additive signature change)
  </read_first>
  <action>
    Read `21-FINDINGS.md`'s Finding 1/2/3 verdicts first — the edits below are gated on those
    verdicts; do not apply any edit whose triggering Finding was REFUTED.

    REQUIRED (Finding 2 is HIGH-confidence and code-verified in 21-RESEARCH.md — apply once
    `21-FINDINGS.md`'s Finding 2 section confirms it reproduced): in `src/ui/challenge.js`'s
    `openChallenge({ brain, onSuccess, prompt, question, renderChoices = true })` destructuring
    signature (currently line 70), add a new optional `label` parameter. Change the `const display
    = prompt ?? \`${q.a} × ${q.b}\`;` line (currently line 78) so that when `prompt` is absent AND
    `label` is present, `display` becomes the `label` string followed by a space then the
    `${q.a} × ${q.b}` expression; when both `label` and `prompt` are absent, `display` falls back
    to the existing bare `${q.a} × ${q.b}`; `prompt`, when present, still wins outright (fully
    replaces — unchanged behavior for `collect.js`'s existing caller). In
    `src/mechanics/enemy.js`, change the `openChallenge({ brain, prompt: "Answer to defeat the
    guard:", onSuccess() {...} })` call (currently lines 41-55) to pass `label: "Answer to defeat
    the guard:"` instead of `prompt`, leaving the `onSuccess` body byte-identical.

    DEFENSIVE CONSISTENCY (apply regardless of Finding 1's verdict — this is a codebase-convention
    fix per 21-RESEARCH.md Finding 1, not a bug fix, since an uncolored Kaplay `text()` already
    renders white): define `const LABEL_FG = [0xe8, 0xe8, 0xe8];` in `src/ui/challenge.js` near
    the existing `PANEL_BG`/`BOX_BG` constants (matching `src/scenes/select.js`'s own `LABEL_FG`
    value exactly), and add `color(LABEL_FG[0], LABEL_FG[1], LABEL_FG[2])` to both of
    challenge.js's currently-uncolored `text()` calls: the question-prompt `add()` block
    (currently lines 121-128) and the per-choice answer-box label `add()` block (currently lines
    162-169) — matching the indexed `color(X[0], X[1], X[2])` style already used elsewhere in this
    same file (e.g. the `PANEL_BG`/`BOX_BG` calls), not a spread `color(...X)` style. Also add the
    identical `color(LABEL_FG[0], LABEL_FG[1], LABEL_FG[2])` to `src/levels/build.js`'s three
    currently-uncolored glyph `text()` calls: the door's `"X"` glyph, the math-gate's `"?"` glyph,
    and the enemy's `"!"` glyph (each already has an `anchor("center")`/`pos(...)` pair to add the
    `color(...)` entry alongside).

    CONDITIONAL (only if `21-FINDINGS.md`'s Finding 3 section says CONFIRMED): add a new optional
    `dim = true` parameter to `openChallenge()`'s destructured signature; wrap the existing
    full-screen dim-rect `add([...])` block (currently lines 88-96) in an `if (dim) { ... }` guard
    so it can be skipped entirely; in `src/mechanics/collect.js`'s `openChallenge({ brain,
    question: q, renderChoices: false, prompt: ... })` call (currently lines 87-92), add `dim:
    false` so the collect zone's own pickups are never darkened by a full-screen overlay they
    don't need (the pickups already carry their own explicit `CONFIG.COLLECT.PICKUP_BG`/
    `PICKUP_FG` contrast, per the earlier `2baacef` fix). If Finding 3 says REFUTED, skip this
    edit entirely — do not touch `collect.js` or add a `dim` parameter — and record in
    `21-FINDINGS.md`'s Post-Fix section that no code change was needed for Finding 3.

    Confirm `scripts/check-gate.sh`'s assertions (already read) are existence/call-site greps and
    are unaffected by the additive `label`/`dim` parameters — no edit to `check-gate.sh` is
    needed.
  </action>
  <verify>
    <automated>node --check src/ui/challenge.js && node --check src/mechanics/enemy.js && node --check src/levels/build.js && node --check src/mechanics/collect.js && bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/challenge.js` contains a `label` parameter in `openChallenge`'s destructured
      signature and a `LABEL_FG` constant matching `select.js`'s value.
    - `src/mechanics/enemy.js` passes `label:` (not `prompt:`) to `openChallenge`.
    - `src/levels/build.js`'s door/math-gate/enemy glyph `text()` calls each have an adjacent
      `color(LABEL_FG` entry.
    - `src/mechanics/collect.js`'s `openChallenge` call is unchanged UNLESS `21-FINDINGS.md`
      recorded Finding 3 as CONFIRMED, in which case it carries `dim: false`.
    - `bash scripts/check-gate.sh`, `bash scripts/check-import-safety.sh`, and `bash
      scripts/check-safety.sh` all print their PASS line with zero edits to those three scripts.
  </acceptance_criteria>
  <done>
    enemy.js's arithmetic-display bug is fixed via the additive label param; challenge.js/build.js's
    text() calls now carry explicit color() matching the established codebase convention; the
    collect-zone dim-overlay fix is applied only if Finding 3 was confirmed; all 3 existing static
    gates remain green with zero gate-script edits.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run the full interactive audit (post-fix) + update findings + confirm no regressions</name>
  <files>.planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-FINDINGS.md</files>
  <read_first>
    scripts/audit-phase21-mechanics.mjs (unchanged from Plan 21-01),
    .planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-FINDINGS.md
    (this task's own Task-1-gated edits + the pre-fix baseline to compare against)
  </read_first>
  <action>
    Re-run `node scripts/audit-phase21-mechanics.mjs` (unchanged from Plan 21-01 — this task does
    not modify the script) against the now-fixed codebase. Using the Read tool, view the
    regenerated `screenshots/level-01-enemy-1000-before.png` and confirm the panel now shows the
    arithmetic expression (e.g. "...6 × 7") alongside "Answer to defeat the guard:", not the label
    alone. If Finding 3 was CONFIRMED and fixed in Task 1, also view the regenerated
    `screenshots/level-01-answer-zone-300-before.png` and confirm the pickup labels read more
    legibly than the pre-fix screenshot. Confirm the script's final printed line reads `AUDIT: ALL
    MECHANICS RESOLVED` (every door/math-gate/enemy across all 4 levels still resolves within the
    4-key cycle, and every collect-zone still triggers) — this is the regression check proving the
    additive `label`/`dim` parameters did not break `door.js`/`gates.js`/`mathGate.js`/
    `collect.js`.

    Append a `## Post-Fix Verification` section to `21-FINDINGS.md` recording: the enemy.js fix
    confirmed visually (cite the regenerated screenshot filename), the challenge.js/build.js
    color() consistency fix applied, Finding 3's final disposition (fixed-and-confirmed, or
    not-needed, per Task 1's conditional), and the full re-run's `AUDIT: ALL MECHANICS
    RESOLVED`/`AUDIT: FAILURES DETECTED` outcome with the complete post-fix sweep table (same
    shape as Plan 21-01's `## Full Mechanic Sweep` table).

    Run the full existing static suite one more time (`bash scripts/check-gate.sh && bash
    scripts/check-import-safety.sh && bash scripts/check-safety.sh && node
    scripts/smoke-progress.mjs`) to confirm the whole tree is green, not just the 4 files touched
    in Task 1.
  </action>
  <verify>
    <automated>node scripts/audit-phase21-mechanics.mjs > /tmp/audit21-postfix.log 2>&1; grep -q "AUDIT: ALL MECHANICS RESOLVED" /tmp/audit21-postfix.log && test -f .planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-FINDINGS.md && grep -q "Post-Fix Verification" .planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-FINDINGS.md && bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/smoke-progress.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/audit-phase21-mechanics.mjs`'s final stdout line is `AUDIT: ALL MECHANICS
      RESOLVED` (no `FAILURES DETECTED`).
    - `21-FINDINGS.md` contains a `## Post-Fix Verification` section citing the regenerated
      `screenshots/level-01-enemy-1000-before.png` as visual confirmation of the enemy.js fix.
    - `21-FINDINGS.md`'s Post-Fix Verification section states Finding 3's final disposition
      (fixed-and-confirmed or not-needed).
    - `check-gate.sh`, `check-import-safety.sh`, `check-safety.sh`, and `smoke-progress.mjs` all
      pass after this task.
  </acceptance_criteria>
  <done>
    A full post-fix re-run of the interactive audit confirms the enemy.js fix visually, confirms
    zero regressions across all 4 levels' mechanics, and 21-FINDINGS.md records the outcome; the
    complete static gate suite remains green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Player keyboard input (real or Playwright-synthesized) → src/ui/challenge.js's new `label` param → text() render | The new param crosses from a caller-supplied string into a rendered canvas text — the surface this fix touches |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | src/ui/challenge.js `label` param, src/mechanics/enemy.js caller | medium | mitigate | `label` is a plain caller-supplied JS string concatenated into a template literal fed directly into Kaplay's `text()` call (never a DOM/markup sink) — continues the existing GATE-01 no-DOM-sink guard; `scripts/check-gate.sh`'s existing negative-grep assertion (no DOM-sink tokens) re-confirms this after the edit, per Task 1's verify step |
| T-21-02 | Denial of Service (self) | scripts/audit-phase21-mechanics.mjs (re-run in Task 2) | low | mitigate | Re-run reuses the identical `try`/`finally` cleanup already verified in Plan 21-01 — no new server/browser instances are left open |
</threat_model>

## Artifacts This Phase Produces (this plan's contribution)

- `label` parameter on `src/ui/challenge.js`'s `openChallenge()` — prefixes rather than replaces
  the default arithmetic display; consumed by `src/mechanics/enemy.js`.
- `LABEL_FG` constant in `src/ui/challenge.js`.
- (conditional, only if `21-FINDINGS.md` Finding 3 = CONFIRMED) `dim` parameter on
  `src/ui/challenge.js`'s `openChallenge()`, defaulting to `true`; `dim: false` passed from
  `src/mechanics/collect.js`'s call site.
- No new files.

<verification>
- [ ] `node --check` passes on all 4 edited files
- [ ] `check-gate.sh`, `check-import-safety.sh`, `check-safety.sh`, `smoke-progress.mjs` all pass
- [ ] Re-run of `scripts/audit-phase21-mechanics.mjs` prints `AUDIT: ALL MECHANICS RESOLVED`
- [ ] `21-FINDINGS.md` contains a `## Post-Fix Verification` section with screenshot evidence
</verification>

<success_criteria>
- The confirmed enemy.js bug is fixed and re-verified via a real screenshot, the same way the 6
  v4.0 post-ship bugs were.
- No task in this plan applies a fix whose triggering Finding was REFUTED by Plan 21-01.
- Zero regressions across door/gates/mathGate/collect resolution on all 4 levels.
</success_criteria>

<output>
Create `.planning/phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-04-SUMMARY.md` when done.
</output>
