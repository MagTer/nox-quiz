---
phase: 14-multi-scene-shell
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/main.js
  - src/scenes/game.js
autonomous: true
requirements: [NAV-01, NAV-03, NAV-04]
must_haves:
  truths:
    - "On load the game boots into the title scene (go('title')), not the game scene"
    - "main.js registers all three scenes (title, select, game) before boot, keeping the existing loadSprite asset loads and the +50% canvas display-scale block untouched"
    - "Clearing a level persists (markCleared + writeSave, already present) and then returns to the select scene via go('select') — no auto-advance into the next level, no timer/scheduler"
    - "Pressing Escape during a level returns to select (go('select')); no forced replay of earlier levels"
    - "scripts/check-import-safety.sh is fully green (Section 1 main.js greps now satisfied)"
  artifacts:
    - path: "src/main.js"
      provides: "boot generalization — register title/select/game, go('title')"
      contains: "go(\"title\")"
    - path: "src/scenes/game.js"
      provides: "clear→select return + Escape→select controller (NAV-03)"
      contains: "go(\"select\")"
  key_links:
    - from: "src/main.js"
      to: "src/scenes/title.js"
      via: "imports titleScene, registers scene('title', titleScene), boots go('title')"
      pattern: "scene\\(\"title\""
    - from: "src/main.js"
      to: "src/scenes/select.js"
      via: "imports selectScene, registers scene('select', selectScene)"
      pattern: "scene\\(\"select\""
    - from: "src/scenes/game.js"
      to: "src/scenes/select.js"
      via: "onClear and onKeyPress('escape') both go('select')"
      pattern: "go\\(\"select\""
---

<objective>
Wire the three-scene shell together: flip main.js's boot from go("game", {...}) to
go("title") after registering all three scenes, and turn game.js's clear path into a
return-to-select (NAV-03) plus add an Escape→select bail. This depends on Plan 01
having created src/scenes/title.js and src/scenes/select.js.

After this plan, scripts/check-import-safety.sh goes fully GREEN (its main.js
positive greps are now satisfied) and the full navigation graph exists:
title → select → game → (clear or Escape) → select.

Purpose: complete the boot + clear wiring so the shell is functionally navigable.
Output: edits to src/main.js (boot) and src/scenes/game.js (clear→select + Escape).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-scene-shell/14-RESEARCH.md
@.planning/phases/14-multi-scene-shell/14-PATTERNS.md

# The boot shell to generalize (current registration lines 11-13, 66-68):
@src/main.js
# The clear path to extend (existing onClear lines 168-197; the scene body for Escape):
@src/scenes/game.js
# The scenes Plan 01 created (imported + registered here):
@src/scenes/title.js
@src/scenes/select.js
# The no-timer mandate the clear→select transition must respect:
@scripts/check-safety.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize main.js boot — register title/select/game, go("title")</name>
  <files>src/main.js</files>
  <read_first>
    - src/main.js (the existing gameScene import line 13; the loadSprite asset block lines 50-61 and the canvas display-scale block lines 34-38 — BOTH must stay untouched; the single registration + boot lines 66-68: `scene("game", gameScene); go("game", { startX: 64, startY: 64 });`)
    - src/scenes/title.js, src/scenes/select.js (confirm the exported factory names titleScene / selectScene to import)
    - .planning/phases/14-multi-scene-shell/14-PATTERNS.md (main.js section lines 144-164 — exact before/after)
  </read_first>
  <action>
    Edit src/main.js to register all three scenes then boot the title (RESEARCH Pattern 5,
    PATTERNS main.js section):
      - Add two imports next to the existing gameScene import: titleScene from
        "./scenes/title.js" and selectScene from "./scenes/select.js".
      - Where the single `scene("game", gameScene)` registration is, add
        `scene("title", titleScene)` and `scene("select", selectScene)` BEFORE (or around)
        the existing game registration. All three must be registered before any go().
      - Replace `go("game", { startX: 64, startY: 64 })` with `go("title")`. The title is
        now the boot entry; the game scene gets its start defaults from game.js (data?.startX
        ?? 64), so the select→game handoff need not pass start coords.
    LEAVE the kaplay() init, the canvas display-scale block (lines 34-38), and ALL loadSprite
    asset calls (lines 50-61) exactly as they are — assets load before any scene runs. Make
    NO other behavior change.
  </action>
  <acceptance_criteria>
    - `node --check src/main.js` passes.
    - `grep -q 'scene("title"' src/main.js` AND `grep -q 'scene("select"' src/main.js` AND `grep -q 'scene("game"' src/main.js` (all three registered).
    - `grep -q 'go("title"' src/main.js` (boots the title).
    - The old boot is gone: `grep -q 'go("game", { startX' src/main.js` finds NOTHING.
    - Assets preserved: `grep -c 'loadSprite(' src/main.js` is unchanged from before (5 calls), and `grep -q 'canvas.style.width' src/main.js` still present (display-scale block intact).
    - Imports present: `grep -q 'titleScene' src/main.js` AND `grep -q 'selectScene' src/main.js`.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/main.js && grep -q 'scene("title"' src/main.js && grep -q 'scene("select"' src/main.js && grep -q 'scene("game"' src/main.js && grep -q 'go("title"' src/main.js && ! grep -q 'go("game", { startX' src/main.js && grep -q 'titleScene' src/main.js && grep -q 'selectScene' src/main.js && test "$(grep -c 'loadSprite(' src/main.js)" = "5" && echo OK</automated>
  </verify>
  <done>main.js registers title/select/game and boots go("title"); the old go("game",{startX,startY}) boot is removed; loadSprite asset loads and the canvas display-scale block are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: game.js clear→select return + Escape→select (NAV-03)</name>
  <files>src/scenes/game.js</files>
  <read_first>
    - src/scenes/game.js (the EXISTING onClear callback lines 168-197 — markCleared line 180 and writeSave line 194 already exist; the levelId read line 65 already exists — do NOT rebuild either; the scene body where controllers are registered, e.g. the onUpdate at lines 201-209 and the onSceneLeave cancels lines 226-241)
    - scripts/check-safety.sh (the banned tokens wait(/loop(/setTimeout — the clear→select transition must NOT introduce any of these; RESEARCH Pitfall 4)
    - .planning/phases/14-multi-scene-shell/14-PATTERNS.md (game.js section lines 114-141 — the two surgical edits)
  </read_first>
  <action>
    Two surgical edits to src/scenes/game.js — do NOT touch the levelId read (line 65,
    already wired) and do NOT change the existing markCleared/writeSave/addXp/HUD/fx lines:

    (1) Clear → return to select (NAV-03): add `go("select")` as the FINAL line of the
    existing onClear callback, AFTER the existing writeSave(progress.serialize(...)) call.
    This turns the previously-terminal "LEVEL CLEAR" stay into a return; the gate's
    gate-cleared canvas objects are torn down by go() cleanly (they are fixed() objects,
    no stay()). The next level's unlock derives fresh when select re-renders — NO auto-advance
    into the next level. Do NOT add a wait()/timer/scheduler to "let the banner show"
    (SAFE-01 violation — check-safety.sh bans wait(/loop(/setTimeout). fx.clearBurst() still
    fires for the current frame before teardown.

    (2) Escape → select (NAV-03 agency): inside the scene body (NOT module top level),
    register `onKeyPress("escape", () => go("select"))`. This lets her bail back to select
    mid-level with no forced replay; it is an app-bus controller auto-cleared by go() in
    Kaplay 3001 (no manual cancel needed). Place it in the body alongside the other
    in-body registrations.

    Make no other changes. Do NOT introduce stay(). Keep every engine global inside the
    scene body (a727c13 — unchanged from today).
  </action>
  <acceptance_criteria>
    - `node --check src/scenes/game.js` passes.
    - `grep -c 'go("select"' src/scenes/game.js` is 2 (one in onClear, one in the Escape controller).
    - `grep -q 'onKeyPress("escape"' src/scenes/game.js` (Escape→select controller present).
    - The persist still exists and was not removed: `grep -q 'markCleared' src/scenes/game.js` AND `grep -q 'writeSave' src/scenes/game.js`.
    - The levelId read is intact: `grep -q 'data?.levelId' src/scenes/game.js`.
    - No timer introduced: comment-stripped game.js has NO wait(/loop(/setTimeout — `bash scripts/check-safety.sh` stays green.
    - No stay(): `grep -q 'stay(' src/scenes/game.js` finds NOTHING.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/scenes/game.js && test "$(grep -c 'go(\"select\"' src/scenes/game.js)" = "2" && grep -q 'onKeyPress("escape"' src/scenes/game.js && grep -q 'markCleared' src/scenes/game.js && grep -q 'writeSave' src/scenes/game.js && grep -q 'data?.levelId' src/scenes/game.js && ! grep -q 'stay(' src/scenes/game.js && bash scripts/check-safety.sh && echo OK</automated>
  </verify>
  <done>game.js onClear returns to select via go("select") after the existing persist, and an in-body Escape controller also returns to select; no timer, no stay(), the levelId read and the markCleared/writeSave persist are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Full static gate green — run check-import-safety.sh + check-progress.sh</name>
  <files>scripts/check-import-safety.sh</files>
  <read_first>
    - scripts/check-import-safety.sh (created in Plan 01 — its Section 1 main.js positive greps that go green now that Task 1 wired the boot)
    - .planning/phases/14-multi-scene-shell/14-VALIDATION.md (Sampling Rate — per-wave merge runs the full suite)
  </read_first>
  <action>
    No code change here — this is the per-wave full-suite gate now that the boot is wired.
    Run scripts/check-import-safety.sh end-to-end: Section 0 (node --check on title/select/
    game/main), Section 1 (positive greps incl. the now-satisfied main.js scene("title")/
    scene("select")/go("title")), and Section 2 (the scoped a727c13 negative grep on the
    scene modules) must ALL pass and print "import-safety checks: PASS". Also run
    scripts/check-progress.sh to confirm the pure registry/progress layer is untouched and
    still green. If either is red, fix the offending wiring (a missing registration, a typo
    in a scene("...") token the gate greps for, or an accidental top-level engine reference)
    before closing — but do NOT loosen the gate to make it pass.
  </action>
  <acceptance_criteria>
    - `bash scripts/check-import-safety.sh` exits 0 and prints "import-safety checks: PASS".
    - `bash scripts/check-progress.sh` exits 0 and prints "progress checks: PASS".
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && echo OK</automated>
  </verify>
  <done>Both static gates are green: the import-safety gate confirms the three scenes exist, main.js registers + boots the title, and no scene has a module-top-level engine reference; the progress gate confirms the pure layer is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot path → scene graph | main.js is the only post-init module scope where engine globals legally exist; a broken registration order or an a727c13 throw in a scene blanks the canvas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Denial of Service | boot blanks on a727c13 throw / bad registration | mitigate | scenes keep engine globals body-only (Plan 01); check-import-safety.sh asserts registration + boot tokens; the mandatory browser boot (Plan 03) confirms the title actually renders (greps ≠ boots). |
| T-14-04 | Tampering | clear→select transition reaches for a timer (SAFE-01) | mitigate | go("select") fires directly in onClear with no wait()/loop()/setTimeout; check-safety.sh negative-greps these and is run in Task 2's verify. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; only the vendored sha256-pinned Kaplay 3001.0.19 is used (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- Per task: `node --check` on each edited module + the per-task grep batteries.
- After this plan (wave merge): `bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && bash scripts/check-safety.sh` — all green.
- The runtime no-leak proof is Plan 03's mandatory real browser boot (greps ≠ boots).
</verification>

<success_criteria>
- main.js boots go("title") with all three scenes registered; loadSprite asset loads and the canvas display-scale block are untouched (NAV-01 boot).
- game.js returns to select on clear (after the existing persist) and on Escape, with no auto-advance, no timer, no stay() (NAV-03).
- scripts/check-import-safety.sh is fully green; check-progress.sh stays green (NAV-04 static half complete).
</success_criteria>

<output>
Create `.planning/phases/14-multi-scene-shell/14-02-SUMMARY.md` when done.
</output>
