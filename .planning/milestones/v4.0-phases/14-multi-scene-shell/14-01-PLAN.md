---
phase: 14-multi-scene-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-import-safety.sh
  - scripts/fixtures/bad-scene.js
  - src/config.js
  - src/scenes/title.js
  - src/scenes/select.js
autonomous: true
requirements: [NAV-01, NAV-02, NAV-04]
must_haves:
  truths:
    - "scripts/check-import-safety.sh exists, exits 0 on the shipped-good scene modules, and exits non-zero on a deliberately-bad top-level-engine fixture (a727c13)"
    - "title scene factory renders 'Math Lab' + a press-to-start prompt and, from its own body, registers Enter/Space key + full-screen click controllers that go('select')"
    - "select scene factory reads loadSave()->createProgress() fresh on entry, maps LEVEL_ORDER to three distinguishable tile states (locked/unlocked/cleared) via isUnlocked(id,progress)+isLevelCleared(id), and lets only unlocked tiles reach go('game',{levelId})"
    - "neither title.js nor select.js references any engine global at module top level, uses innerHTML/document.*, or reads localStorage directly"
  artifacts:
    - path: "scripts/check-import-safety.sh"
      provides: "a727c13 module-top-level negative-grep gate, scoped to scene modules, modeled on check-progress.sh"
      contains: "strip_comments"
    - path: "scripts/fixtures/bad-scene.js"
      provides: "calibration fixture with a top-level engine call so the gate proves it can go red"
      contains: "add("
    - path: "src/scenes/title.js"
      provides: "titleScene factory — NAV-01 dark-grunge title + dual-input start"
      exports: ["titleScene"]
      min_lines: 30
    - path: "src/scenes/select.js"
      provides: "selectScene factory — NAV-02 level-select tiles with three states + dual-input pick"
      exports: ["selectScene"]
      min_lines: 50
    - path: "src/config.js"
      provides: "CONFIG.TITLE + CONFIG.SELECT layout constants (no magic numbers in scene logic)"
      contains: "TITLE"
  key_links:
    - from: "src/scenes/select.js"
      to: "src/levels/index.js"
      via: "imports LEVEL_ORDER + isUnlocked; derives unlock per tile, never stores it"
      pattern: "isUnlocked"
    - from: "src/scenes/select.js"
      to: "src/progress.js"
      via: "loadSave()->createProgress() fresh on entry; reads isLevelCleared per id"
      pattern: "createProgress\\(loadSave\\("
    - from: "scripts/check-import-safety.sh"
      to: "src/scenes/title.js"
      via: "node --check + scoped top-level-engine negative grep over the new scene modules"
      pattern: "check"
---

<objective>
Land the Phase 14 import-safety infrastructure and the two new navigation scene
modules (title + select) with their layout constants — the Wave-0 gate ships
together with the scene modules it guards.

This plan delivers NAV-01 (the dark-grunge title with dual-input start) and NAV-02
(the level-select listing every registered level in three distinguishable states,
with only unlocked tiles selectable), plus the static half of NAV-04 (the a727c13
`check-import-safety.sh` gate). It does NOT wire the boot or the clear path — that
is Plan 02 (which flips main.js to `go("title")` and adds the game.js clear→select
return). Until Plan 02 lands, these scenes are registered nowhere; that is expected.

Purpose: establish the per-scene factory + closure-state + import-safety contracts
every later engine-touching phase inherits.
Output: scripts/check-import-safety.sh, scripts/fixtures/bad-scene.js, CONFIG.TITLE
+ CONFIG.SELECT blocks, src/scenes/title.js, src/scenes/select.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-scene-shell/14-RESEARCH.md
@.planning/phases/14-multi-scene-shell/14-PATTERNS.md

# Scene-factory + closure-state template (mirror its discipline exactly):
@src/scenes/game.js
# In-scene canvas UI + factory idiom (mountHud) + palette literals at module scope:
@src/ui/hud.js
# Pure read APIs the select screen consumes (LEVEL_ORDER, isUnlocked, getLevel):
@src/levels/index.js
# Structural-gate idiom to copy verbatim and re-scope (strip_comments, node --check loop):
@scripts/check-progress.sh
# Layout-constant pattern (CONFIG.HUD/CONFIG.HINT blocks):
@src/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — create scripts/check-import-safety.sh + a bad fixture, calibrated red-and-green</name>
  <files>scripts/check-import-safety.sh, scripts/fixtures/bad-scene.js</files>
  <read_first>
    - scripts/check-progress.sh (the gate scaffolding to copy: lines 21-34 set -euo pipefail + ROOT=git rev-parse + fail() + strip_comments(); lines 40-45 the node --check existence/syntax loop; lines 108-115 positive greps; lines 117-126 the SCOPED negative a727c13 grep — note it is deliberately scoped to PURE modules and EXCLUDES build.js)
    - .planning/phases/14-multi-scene-shell/14-RESEARCH.md (the "check-import-safety.sh skeleton" code block ~lines 339-373 and Pitfall 5 ~lines 331-336 — the load-bearing scoping detail)
    - .planning/phases/14-multi-scene-shell/14-PATTERNS.md (scripts/check-import-safety.sh section, lines 168-206 — exact anchored top-level-trap regex forms)
  </read_first>
  <action>
    Create scripts/check-import-safety.sh modeled on check-progress.sh. Reuse its
    scaffolding verbatim: set -euo pipefail, ROOT via git rev-parse --show-toplevel,
    a fail() that prints "import-safety checks: FAIL — <msg>" to stderr and exit 1,
    and strip_comments() using the same sed expression.

    Section 0 (existence + syntax): loop over src/scenes/title.js, src/scenes/select.js,
    src/scenes/game.js, src/main.js — each must exist and pass node --check, else fail.

    Section 1 (POSITIVE structural invariants): grep that title.js and select.js each
    export a scene factory (an export of a function whose name ends in Scene); grep
    that main.js registers the title scene, registers the select scene, and boots the
    title scene. Use the exact token forms from the PATTERNS skeleton so they match
    what Plan 02 will write.

    Section 2 (NEGATIVE a727c13 — module TOP LEVEL only): for src/scenes/title.js and
    src/scenes/select.js, comment-strip then grep for the known top-level-trap forms ONLY,
    anchored so an indented in-body call is NOT matched: a column-0 (const|let|var)
    declaration whose initializer calls a Kaplay engine factory (add, rect, sprite, text,
    vec2, rgb, onKeyPress, onKeyDown, onClick, onUpdate), OR a top-level typeof guard on
    an engine symbol (Rect, add, vec2, rgb). On match, fail with a message naming the file
    and a727c13. This MUST stay scoped to module-top-level forms — a naive whole-file grep
    would false-flag the legitimate in-body engine calls in title.js/select.js/game.js
    (RESEARCH Pitfall 5). Do not scan game.js or main.js with the negative grep (they
    legitimately use engine globals inside bodies / at post-init module scope).

    On full success print "import-safety checks: PASS".

    Create scripts/fixtures/bad-scene.js as the calibration fixture: a tiny module whose
    body intentionally contains a column-0 const-assigned engine call (the exact
    top-level-trap form the negative grep targets) so a one-off manual run of the negative
    pattern against the fixture proves the gate can go RED. Add a header comment marking it
    a DELIBERATELY-BAD a727c13 fixture for calibration, not shipped game code.

    Make the script executable (chmod +x).

    Note: until Plan 02 creates main.js's title/select registration + go("title") boot,
    Section 1's main.js greps will FAIL — that is the expected red state this wave (the
    gate is real, not a no-op). Section 0/2 over the new title.js/select.js created in
    Tasks 2-3 must pass. The acceptance command below runs only the calibration of the
    negative grep against the fixture and the good file, which is decoupled from main.js.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/check-import-safety.sh` parses clean (no bash syntax error).
    - The negative-grep CALIBRATION holds both directions, proven by:
      `strip_comments scripts/fixtures/bad-scene.js | grep -E '<the top-level-trap pattern>'` MATCHES (fixture goes red), AND the same pattern over `strip_comments src/scenes/game.js` does NOT match (shipped-good stays green). Run this calibration explicitly and confirm both outcomes.
    - `grep -q 'strip_comments' scripts/check-import-safety.sh` (scaffolding copied).
    - `grep -q 'import-safety checks: PASS' scripts/check-import-safety.sh` (success line present).
    - scripts/check-import-safety.sh is executable.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/check-import-safety.sh && test -f scripts/fixtures/bad-scene.js && grep -q 'strip_comments' scripts/check-import-safety.sh && grep -q 'import-safety checks: PASS' scripts/check-import-safety.sh && echo OK</automated>
  </verify>
  <done>The gate script and bad fixture exist; the negative grep is calibrated to fire on the fixture and stay silent on the shipped-good game.js; scaffolding mirrors check-progress.sh.</done>
</task>

<task type="auto">
  <name>Task 2: Title scene (NAV-01) + CONFIG.TITLE — dark-grunge title with dual-input start</name>
  <files>src/scenes/title.js, src/config.js</files>
  <read_first>
    - src/scenes/game.js (the scene-factory shape, lines 31-33: `export function gameScene(data) {` with engine globals ONLY inside the body; closure-local state seeded from data)
    - src/ui/hud.js (canvas-draw idiom lines 56-63 add([text(...), pos(...), color(...), fixed(), z(...)]); module-scope palette literals lines 35-37 — safe because they call nothing; the flashLevelUp centered-text idiom lines 127-136 add([text(...), anchor("center"), pos(center()), ...]))
    - src/config.js (the CONFIG.HUD block lines 94-103 and CONFIG.HINT lines 138-142 — the layout-constant pattern to mirror for CONFIG.TITLE)
    - .planning/phases/14-multi-scene-shell/14-PATTERNS.md (title.js section lines 22-53)
  </read_first>
  <action>
    Add a CONFIG.TITLE block to src/config.js (append a new key to the CONFIG object,
    following the CONFIG.HUD/CONFIG.HINT pattern): tunable layout constants for the title
    text size, the prompt text size, and any vertical offset between them. No magic numbers
    in the scene logic — every size/position the scene draws reads from CONFIG.TITLE.

    Create src/scenes/title.js exporting `titleScene(data)` — mirror game.js's factory
    discipline exactly: EVERY engine global (add, text, rect, color, rgb, pos, anchor,
    center, fixed, z, go, onKeyPress, onClick) is referenced ONLY inside the function body.
    Module-scope is limited to imports (CONFIG) and plain data literals (color arrays like
    a neon-green accent and a light-grey foreground — copy the hud.js palette literals;
    NO pink per CLAUDE.md). Implements NAV-01:
      - Draw "Math Lab" as a centered fixed() canvas text() using CONFIG.TITLE sizing and
        the accent color; draw a one-line press-to-start prompt below it (e.g. a "press to
        start" line) in the light-grey foreground. Minimal dark-grunge styling only — real
        art is deferred to Phase 18.
      - Register, inside the body, dual-input start controllers that all call go("select"):
        onKeyPress("enter", ...), onKeyPress("space", ...), and a full-screen onClick(...)
        (she plays on a laptop). These are app-bus controllers auto-cleared by go() in
        Kaplay 3001 — no manual cancel required for plain navigation controllers.
    Pure canvas only: NO innerHTML, NO document.*, NO localStorage. Add a short header
    comment noting the a727c13 discipline (engine globals body-only) mirroring hud.js's note.
  </action>
  <acceptance_criteria>
    - `node --check src/scenes/title.js` passes; `node --check src/config.js` passes.
    - `grep -Eq 'export function .*[Ss]cene\(' src/scenes/title.js` (exports a scene factory).
    - `grep -q 'go("select"' src/scenes/title.js` (start advances to select).
    - title.js registers Enter, Space, and click start paths: greps for onKeyPress("enter", onKeyPress("space" (or a space-key form), and onClick( all present.
    - title.js draws the title text: `grep -q 'Math Lab' src/scenes/title.js`.
    - NO DOM/storage sink: `grep -Eq 'innerHTML|document\.|localStorage' src/scenes/title.js` finds NOTHING.
    - `grep -q 'TITLE' src/config.js` (CONFIG.TITLE block added).
    - Negative a727c13: comment-stripped title.js has NO column-0 (const|let|var) engine-call or top-level typeof-engine guard (the check-import-safety.sh Section 2 pattern stays green on it).
  </acceptance_criteria>
  <verify>
    <automated>node --check src/scenes/title.js && node --check src/config.js && grep -Eq 'export function .*[Ss]cene\(' src/scenes/title.js && grep -q 'go("select"' src/scenes/title.js && grep -q 'Math Lab' src/scenes/title.js && grep -q 'TITLE' src/config.js && ! grep -Eq 'innerHTML|document\.|localStorage' src/scenes/title.js && echo OK</automated>
  </verify>
  <done>titleScene renders "Math Lab" + a press-to-start prompt and, from inside its body, wires Enter/Space/click → go("select"); CONFIG.TITLE holds its layout constants; no DOM/storage sink; no module-top-level engine reference.</done>
</task>

<task type="auto">
  <name>Task 3: Select scene (NAV-02) + CONFIG.SELECT — three-state level tiles, dual-input pick, derived unlock</name>
  <files>src/scenes/select.js, src/config.js</files>
  <read_first>
    - src/scenes/game.js (factory shell + closure-local state lines 31-65; the fresh loadSave()->createProgress() read lines 58-59; the data?.levelId payload read line 65; onSceneLeave cancel idiom lines 225-226)
    - src/levels/index.js (the EXACT read APIs: LEVEL_ORDER line 23, getLevel lines 28-30, isUnlocked lines 36-44 — forgiving: i<=0 always open, null progress → only first open)
    - src/progress.js (loadSave line 270 — guarded, never throws; createProgress line 57 — pure factory; isLevelCleared line 128 — strict === true)
    - src/ui/hud.js (tagged canvas object idiom lines 56-84; factory-with-closure pattern; keep refs in an array to recolor — mirror)
    - src/ui/mathGate.js (dual-input idiom: obj-scoped box.onClick auto-cleaned; an app-bus onKeyPress array; a boxes[] array kept to recolor a cursor)
    - src/config.js (CONFIG.HUD/CONFIG.GATE blocks — the layout-constant pattern for CONFIG.SELECT)
    - .planning/phases/14-multi-scene-shell/14-PATTERNS.md (select.js section lines 56-110 — fresh-read-every-entry, three-state render, dual-input, derived-unlock)
  </read_first>
  <action>
    Add a CONFIG.SELECT block to src/config.js (new CONFIG key, following the HUD/GATE
    pattern): tile width/height, spacing between tiles, the row's start X/Y, and text
    sizes for the tile label/glyph. No magic numbers in the scene.

    Create src/scenes/select.js exporting `selectScene(data)` — mirror game.js's factory
    discipline (engine globals body-only; closure-local state, never module-level).
    Module scope holds only imports (CONFIG; LEVEL_ORDER + isUnlocked from
    ../levels/index.js; loadSave + createProgress from ../progress.js) and plain palette
    literals (a bright accent, a dim/locked grey, a cleared-mark color — NO pink).
    Implements NAV-02:
      - At the TOP of the body, read state FRESH every entry (clean state, NAV-04): build
        `progress` from `createProgress(loadSave())`, then map LEVEL_ORDER to a tile model.
        For each id compute the three-way state from the PURE tier: cleared via
        progress.isLevelCleared(id); unlocked via isUnlocked(id, progress) (the registry
        DERIVES unlock — the scene must NOT compute or store its own "unlocked" boolean,
        RESEARCH Pitfall 2 / one source of truth). state = cleared ? "cleared" : unlocked
        ? "unlocked" : "locked".
      - Draw one numbered tile per LEVEL_ORDER entry in a row using CONFIG.SELECT geometry,
        each a tagged (e.g. "select") fixed() canvas rect()+text() so go() teardown wipes
        them. The three states must be VISUALLY DISTINGUISHABLE now (exact art is Phase 18):
        LOCKED = dimmed + a lock glyph, not selectable; UNLOCKED = bright/accent, selectable;
        CLEARED = a check/done mark. Pure canvas only (text()/rect()) — NO innerHTML/document.*.
      - Dual-input selection, locked tiles NEVER selectable (NAV-02): each UNLOCKED tile gets
        an obj-scoped onClick that calls go("game", { levelId: id }); locked tiles get NO
        click handler. Inside the body register app-bus arrow controllers (left/right) that
        move a CLOSURE-LOCAL cursor among UNLOCKED tiles only (skip locked), and an Enter
        controller that plays the cursor's tile via go("game", { levelId }). Keep tile refs
        in a closure array to recolor the cursor highlight. The cursor index is a closure
        `let`, NEVER a module-level let (anti-leak).
      - go("game", { levelId }) is the ONLY cross-scene handoff (game.js already reads
        data?.levelId at line 65 — do NOT pass start coords; game.js keeps its defaults).
    Do NOT introduce stay(). Do NOT read localStorage directly. If a persistent app-bus
    controller you hold a handle to is ever added, cancel it on onSceneLeave (Pattern 6) —
    plain nav onKeyPress/onClick need no manual cancel in Kaplay 3001.
  </action>
  <acceptance_criteria>
    - `node --check src/scenes/select.js` passes; `node --check src/config.js` passes.
    - `grep -Eq 'export function .*[Ss]cene\(' src/scenes/select.js` (scene factory export).
    - Fresh derived read: `grep -q 'createProgress' src/scenes/select.js`, `grep -q 'loadSave' src/scenes/select.js`, `grep -q 'isUnlocked' src/scenes/select.js`, `grep -q 'LEVEL_ORDER' src/scenes/select.js`, `grep -q 'isLevelCleared' src/scenes/select.js`.
    - Pick handoff: `grep -q 'go("game"' src/scenes/select.js` AND the levelId payload appears (`grep -q 'levelId' src/scenes/select.js`).
    - Dual input: greps for onClick( and onKeyPress( both present; arrow + enter forms present.
    - One source of truth: select.js must NOT store an "unlocked" boolean in the save or recompute it independently — it reads isUnlocked (verified by the isUnlocked grep + no write into progress).
    - NO DOM/storage sink: `grep -Eq 'innerHTML|document\.|localStorage' src/scenes/select.js` finds NOTHING.
    - No stay(): `grep -q 'stay(' src/scenes/select.js` finds NOTHING.
    - `grep -q 'SELECT' src/config.js` (CONFIG.SELECT block added).
    - Negative a727c13: comment-stripped select.js has NO column-0 engine-call declaration or top-level typeof-engine guard (Section 2 stays green).
  </acceptance_criteria>
  <verify>
    <automated>node --check src/scenes/select.js && node --check src/config.js && grep -Eq 'export function .*[Ss]cene\(' src/scenes/select.js && grep -q 'createProgress' src/scenes/select.js && grep -q 'isUnlocked' src/scenes/select.js && grep -q 'LEVEL_ORDER' src/scenes/select.js && grep -q 'go("game"' src/scenes/select.js && grep -q 'levelId' src/scenes/select.js && ! grep -Eq 'innerHTML|document\.|localStorage' src/scenes/select.js && ! grep -q 'stay(' src/scenes/select.js && grep -q 'SELECT' src/config.js && echo OK</automated>
  </verify>
  <done>selectScene reads loadSave()->createProgress() fresh on entry, renders every LEVEL_ORDER level with three distinguishable states (locked/unlocked/cleared) derived from isUnlocked+isLevelCleared, lets only unlocked tiles reach go("game",{levelId}) via arrow+Enter and click; no DOM/storage sink, no stay(), no module-top-level engine reference; CONFIG.SELECT holds its layout constants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → select scene | The persisted save blob is read on select entry; it is untrusted (could be stale/foreign/hand-edited). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | new screens (title.js/select.js) DOM sink | mitigate | All UI is Kaplay text()/rect() canvas draws; assert NO innerHTML/document.* in either scene (negative grep in acceptance). No markup-string sink → no injection path. |
| T-14-02 | Tampering | corrupt/foreign save → unlock-all or boot brick | mitigate | select.js reads through loadSave()->createProgress() (guarded, never throws, defaults on any failure) and derives unlock via isUnlocked from LEVEL_ORDER — never trusts a stored "unlocked" flag and never reads raw localStorage. Junk ids resolve forgivingly. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | Phase 14 installs NO packages; the only third-party code is the already-vendored, sha256-pinned Kaplay 3001.0.19 (RESEARCH Package Legitimacy Audit). No install task in this plan. |
</threat_model>

<verification>
- Per task: `node --check` on each edited JS module + the per-task grep batteries above.
- After this plan: `bash scripts/check-progress.sh` still PASSES (we did not touch the pure registry/progress modules); `scripts/check-import-safety.sh` Section 0/2 pass on the new title.js/select.js, while Section 1's main.js greps remain RED until Plan 02 (expected — the gate is real).
- The mandatory real browser boot is Plan 03 (greps ≠ boots).
</verification>

<success_criteria>
- scripts/check-import-safety.sh exists, mirrors check-progress.sh scaffolding, and its negative grep is proven to fire on scripts/fixtures/bad-scene.js and stay silent on the shipped-good src/scenes/game.js.
- src/scenes/title.js (titleScene) renders "Math Lab" + a press-to-start prompt and wires Enter/Space/click → go("select") from inside its body (NAV-01).
- src/scenes/select.js (selectScene) lists every LEVEL_ORDER level in three distinguishable states, derived fresh on entry from isUnlocked+isLevelCleared, with only unlocked tiles reaching go("game",{levelId}) via dual input (NAV-02).
- Neither scene has a module-top-level engine reference, a DOM/storage sink, or a stay() (NAV-04 static half).
- CONFIG.TITLE and CONFIG.SELECT hold all layout constants (no magic numbers in scene logic).
</success_criteria>

<output>
Create `.planning/phases/14-multi-scene-shell/14-01-SUMMARY.md` when done.
</output>