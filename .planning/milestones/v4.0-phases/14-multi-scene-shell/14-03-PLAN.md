---
phase: 14-multi-scene-shell
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified: []
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]
must_haves:
  truths:
    - "The game boots into a dark-grunge title in a real browser and advances to select on Enter/Space/click"
    - "Select shows level-01 unlocked (locked tiles dimmed/non-selectable, cleared marks shown), and an unlocked level plays via both arrows+Enter and mouse click"
    - "Clearing a level returns to select with the next level now derived as unlocked; Escape returns from a level to select; no forced replay, no auto-advance"
    - "Entering→leaving→re-entering title, select, and a level TWICE each leaves no leaked input handlers, colliders, tweens, or effects (no double-fired input, no ghost objects, no blanked canvas, no console errors)"
    - "scripts/check-import-safety.sh is green at the time of the boot (greps ≠ boots — both must hold)"
  artifacts: []
  key_links:
    - from: "real browser boot"
      to: "NAV-04 no-leak guarantee"
      via: "enter→leave→re-enter ×2 per screen, observed live (DevTools handler/object spot-check)"
      pattern: "manual"
---

<objective>
The mandatory real-browser gate for Phase 14 (the single most expensive v3.0 lesson:
greps ≠ boots). No code is written here — this plan runs the static gates green, then
serves the game over HTTP and drives the full navigation flow plus the NAV-04
enter→leave→re-enter-twice leak check in a real browser, with a blocking human-verify
sign-off.

This plan closes NAV-01..04 at runtime. It is autonomous:false — the human confirms
the title renders, the three tile states read correctly, the clear→select return and
Escape→select work, and crucially that re-entering each screen twice leaks nothing.

Purpose: prove the shell actually boots and navigates cleanly in a browser, not just
that it greps clean.
Output: a human-verified pass (or a defect list to feed back into Plan 01/02).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-multi-scene-shell/14-CONTEXT.md
@.planning/phases/14-multi-scene-shell/14-RESEARCH.md
@.planning/phases/14-multi-scene-shell/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full static gate suite green</name>
  <files></files>
  <read_first>
    - .planning/phases/14-multi-scene-shell/14-VALIDATION.md (Full suite command + the phase-gate sampling rule: full suite green AND the mandatory browser boot before sign-off)
  </read_first>
  <action>
    Run the full static suite from the repo root and confirm all three gates print PASS:
    scripts/check-import-safety.sh (the a727c13 + registration + boot gate),
    scripts/check-progress.sh (the pure registry/progress firewall), and
    scripts/check-safety.sh (the no-timer/forgiving mandate). If any is red, STOP and
    report which assertion failed — the browser boot in Task 2 must not run on a red gate
    (a red gate means the wiring from Plan 01/02 is incomplete and must be fixed there).
  </action>
  <acceptance_criteria>
    - `bash scripts/check-import-safety.sh` exits 0 ("import-safety checks: PASS").
    - `bash scripts/check-progress.sh` exits 0 ("progress checks: PASS").
    - `bash scripts/check-safety.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && bash scripts/check-safety.sh && echo OK</automated>
  </verify>
  <done>All three static gates are green; the runtime boot may proceed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mandatory real browser boot — full nav flow + NAV-04 re-entry leak check</name>
  <action>
    Serve the game over HTTP and drive the full navigation flow plus the NAV-04
    enter→leave→re-enter-twice leak check in a real browser, then record a blocking
    human-verify sign-off. The exact steps, what to look for, and the resume signal are in
    the <how-to-verify>/<resume-signal> blocks below — follow them precisely. This is a
    verification-only checkpoint; write no code. If a defect appears, route it back to
    Plan 01/02 with the exact symptom and re-verify after the fix.
  </action>
  <what-built>
    The multi-scene shell: a dark-grunge title scene (NAV-01), a level-select scene
    listing every registered level in three distinguishable states with dual-input pick
    (NAV-02), a game scene that returns to select on clear (next level derives unlocked)
    and on Escape (NAV-03), and the Kaplay-3001 go() teardown leaning on body-only
    controllers for clean state on every entry (NAV-04). The static a727c13 import-safety
    gate is green; this checkpoint is the irreplaceable runtime half (greps ≠ boots).
  </what-built>
  <how-to-verify>
    1. Serve the game over HTTP from the repo root (file:// is blocked for assets), e.g.:
       `python3 -m http.server 8000`  then open  `http://localhost:8000/`  in a real browser
       (the same serve path the project uses; index.html loads src/main.js).
    2. NAV-01 — Title: confirm a dark-grunge title showing "Math Lab" + a press-to-start
       prompt appears on load (NO pink). Press Enter → it goes to select. Reload, press
       Space → select. Reload, click anywhere → select. All three start inputs work.
    3. NAV-02 — Select: confirm level-01 shows as UNLOCKED (bright/selectable). Any
       not-yet-unlocked tiles (none beyond level-01 today, but confirm the rendering) show
       dimmed + a lock glyph and are NOT selectable. Pick level-01 with the mouse (click)
       → the level plays. Return to select (Escape), then pick level-01 with arrow keys +
       Enter → the level plays. Locked tiles never respond to click or to the arrow cursor.
    4. NAV-03 — Clear + resume: play level-01 to the goal, answer the math gate correctly
       to clear it. Confirm you RETURN to select (not auto-advanced into another level),
       and that level-01 now shows a CLEARED mark. Confirm there is NO forced replay — you
       can leave and come back freely. Press Escape mid-level → returns to select.
    5. NAV-04 — Leak check (the load-bearing test): for EACH of title, select, and a level,
       enter → leave → re-enter TWICE. Confirm after each round-trip:
         - a single keypress fires its action exactly ONCE (no double-advance, no
           double-select) — e.g. on the title after a round-trip, one Enter goes to select
           once, not twice;
         - the select cursor does not "remember" a stale position from a prior visit;
         - no ghost colliders/tweens/effects linger (no lingering animation, no object from
           a previous scene visible in the next);
         - the canvas never blanks and the DevTools console shows NO errors (especially no
           "ReferenceError: add/rgb/vec2 is not defined" — the a727c13 signature).
       Optional DevTools spot-check: confirm the app event-listener count does not grow
       across repeated round-trips.
    6. Confirm `bash scripts/check-import-safety.sh` is still green alongside the boot
       (both halves of NAV-04 must hold).
  </how-to-verify>
  <resume-signal>
    Type "approved" if all of NAV-01..04 pass (title renders + dual-input start; three tile
    states + dual-input pick; clear→select with derived unlock + Escape→select, no forced
    replay/auto-advance; and no leaks/double-inputs/console-errors across re-entry ×2). If
    anything fails, describe the exact symptom (which screen, which input, double-fire vs.
    blank vs. console error) so it can be fixed in Plan 01/02 and re-verified.
  </resume-signal>
  <verify>
    <human-check>Human confirms in a real browser: title renders + dual-input start (NAV-01); three tile states + dual-input pick, locked never selectable (NAV-02); clear→select with derived next-level unlock + Escape→select, no forced replay/auto-advance (NAV-03); enter→leave→re-enter ×2 per screen leaks no handlers/colliders/tweens/effects and shows no console errors, with check-import-safety.sh green at boot time (NAV-04).</human-check>
  </verify>
  <done>Human sign-off recorded that the full title→select→play→clear→select flow works and re-entering each screen twice leaks nothing; or a defect list routed back to Plan 01/02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static gate ↔ runtime boot | A green grep does not prove a clean boot; the browser run is the authoritative NAV-04 check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Denial of Service | leaked handler/collider/tween across re-entry | mitigate | The enter→leave→re-enter-twice human check observes double-fired inputs / ghost objects / blank canvas directly — the runtime backstop the static gate cannot provide. |
| T-14-06 | Repudiation | "it greps clean so it works" (skipping the boot) | mitigate | This plan is autonomous:false; the blocking human-verify makes the real browser boot non-skippable (STATE.md cross-cutting mitigation 2). |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No installs; only the vendored sha256-pinned Kaplay 3001.0.19 is served. |
</threat_model>

<verification>
- Task 1: the full static suite is green (`check-import-safety.sh && check-progress.sh && check-safety.sh`).
- Task 2: a human confirms the full nav flow + the NAV-04 enter→leave→re-enter-twice leak check in a real browser, with the import-safety gate green at boot time.
</verification>

<success_criteria>
- The game boots into the dark-grunge title and the full flow title → select → play → clear → select works in a real browser (NAV-01/02/03).
- Re-entering title, select, and a level twice each leaks no handlers/colliders/tweens/effects and shows no console errors (NAV-04 runtime).
- scripts/check-import-safety.sh is green at the time of the boot (NAV-04 — both halves hold).
- Human sign-off recorded (or a defect list routed back to Plan 01/02).
</success_criteria>

<output>
Create `.planning/phases/14-multi-scene-shell/14-03-SUMMARY.md` when done.
</output>
