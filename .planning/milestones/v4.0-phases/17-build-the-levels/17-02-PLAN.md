---
phase: 17-build-the-levels
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/levels/level-02.js
  - src/levels/level-03.js
  - src/levels/level-04.js
autonomous: true
requirements:
  - LVL-01
  - LVL-04
must_haves:
  truths:
    - "level-02, level-03, and level-04 exist as plain-JS descriptors in src/levels/"
    - "Each new level is completable start→goal on the existing movement/collider spine"
    - "Platforming difficulty ramps across levels: length ~2800 / ~3400 / ~4000 px, gap precision increases, hazard density increases"
    - "Table difficulty and platforming difficulty are on decoupled axes (no level spikes both at once)"
    - "Every spike and mechanic has a checkpoint placed before it so respawn never costs meaningful progress"
  artifacts:
    - path: src/levels/level-02.js
      provides: "The Rusted Climb descriptor: ~2800 px, [1,2,3,4,5,6,7], door + 2 gates, 4 spikes"
      min_lines: 80
    - path: src/levels/level-03.js
      provides: "The Hollow descriptor: ~3400 px, [3,4,5,6,7,8,9], enemy + collect + gate, 5-6 spikes"
      min_lines: 80
    - path: src/levels/level-04.js
      provides: "The Last Span descriptor: ~4000 px, [6,7,8,9], door + 2 gates + enemy + collect, 6-7 spikes"
      min_lines: 80
  key_links:
    - from: src/levels/level-02.js
      to: src/levels/build.js
      via: "buildLevel reads geometry arrays and instantiates entities"
    - from: src/levels/level-03.js
      to: src/levels/build.js
      via: "buildLevel reads geometry arrays and instantiates entities"
    - from: src/levels/level-04.js
      to: src/levels/build.js
      via: "buildLevel reads geometry arrays and instantiates entities"
---

<objective>
Author three new hand-built level descriptors that reuse the existing builder and mechanic wiring, delivering a gentle platforming difficulty ramp (D-06) and the per-level table pools decided in Phase 17 context. Keep level-01 untouched on its hard [6,7,8,9] pool per D-08.

Purpose: Turns the single-level slice into the 4-level replayable set required by LVL-01 and LVL-04.
Output: `src/levels/level-02.js`, `src/levels/level-03.js`, `src/levels/level-04.js`.
</objective>

<execution_context>
@GSD core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-build-the-levels/17-CONTEXT.md
@.planning/phases/17-build-the-levels/17-RESEARCH.md
@src/levels/level-01.js
@src/levels/build.js
@src/config.js
@src/mechanics/door.js
@src/mechanics/gates.js
@src/mechanics/enemy.js
@src/mechanics/collect.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author level-02 "The Rusted Climb"</name>
  <files>src/levels/level-02.js</files>
  <action>
    Create src/levels/level-02.js following the exact schema of src/levels/level-01.js. Import only ../config.js and define `const FLOOR_Y = CONFIG.FLOOR_Y`. Set id "level-02", displayName "The Rusted Climb", allowedTables [1,2,3,4,5,6,7] per D-09, and bounds { left:0, right:2800, top:0, bottom:360 } per D-05/D-04. Author geometry so the level is ~2800 px long and completable with existing RUN_SPEED/JUMP_FORCE/GRAVITY/coyote/buffer tuning: use 3-4 floor runs with gradually wider gaps, raised platform stepping-stones, and 4 spikes (D-07). Place a checkpoint just before every spike and one near the start. Include one door (D-10) and two mathGates; leave enemies and collectZones empty. Ensure all gaps are jumpable and no precision-pixel jumps are required. Keep FLOOR_Y=320 and the 16px grid conventions.
  </action>
  <verify>
    <automated>node --check src/levels/level-02.js && bash scripts/check-import-safety.sh</automated>
  </verify>
  <done>
    src/levels/level-02.js exists, parses, contains no engine globals at module top level, has id "level-02", allowedTables [1,2,3,4,5,6,7], bounds.right=2800, 4 spikes, 1 door, 2 mathGates, and a checkpoint before each spike.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author level-03 "The Hollow"</name>
  <files>src/levels/level-03.js</files>
  <action>
    Create src/levels/level-03.js following the exact schema of src/levels/level-01.js. Import only ../config.js and define `const FLOOR_Y = CONFIG.FLOOR_Y`. Set id "level-03", displayName "The Hollow", allowedTables [3,4,5,6,7,8,9] per D-09, and bounds { left:0, right:3400, top:0, bottom:360 } per D-05/D-04. Author geometry so the level is ~3400 px long with a mixed table pool (platforming slightly harder than level-02, tables slightly harder than level-02 but not as narrow as level-04, satisfying the decoupled-axes ramp). Use 4-5 floor runs with narrower/more offset stepping-stones, 5-6 spikes (D-07), one enemy, one collectZone with four answerPickupSlots, and one mathGate per D-10. Place a checkpoint just before every spike/mechanic. Keep all gaps jumpable.
  </action>
  <verify>
    <automated>node --check src/levels/level-03.js && bash scripts/check-import-safety.sh</automated>
  </verify>
  <done>
    src/levels/level-03.js exists, parses, contains no engine globals at module top level, has id "level-03", allowedTables [3,4,5,6,7,8,9], bounds.right=3400, 5-6 spikes, 1 enemy, 1 collectZone, 1 mathGate, and checkpoints before hazards/mechanics.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author level-04 "The Last Span"</name>
  <files>src/levels/level-04.js</files>
  <action>
    Create src/levels/level-04.js following the exact schema of src/levels/level-01.js. Import only ../config.js and define `const FLOOR_Y = CONFIG.FLOOR_Y`. Set id "level-04", displayName "The Last Span", allowedTables [6,7,8,9] per D-09, and bounds { left:0, right:4000, top:0, bottom:360 } per D-05/D-04. Author geometry so the level is ~4000 px long with the hardest platforming and table pool: 5-6 floor runs, the narrowest/most offset stepping-stones, 6-7 spikes (D-07), one door, two mathGates, one enemy, and one collectZone with four answerPickupSlots per D-10. Place a checkpoint just before every spike/mechanic. Ensure every gap remains reachable with existing movement tuning (no engine changes).
  </action>
  <verify>
    <automated>node --check src/levels/level-04.js && bash scripts/check-import-safety.sh</automated>
  </verify>
  <done>
    src/levels/level-04.js exists, parses, contains no engine globals at module top level, has id "level-04", allowedTables [6,7,8,9], bounds.right=4000, 6-7 spikes, 1 door, 2 mathGates, 1 enemy, 1 collectZone, and checkpoints before hazards/mechanics.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author data → player experience | Level geometry must be completable and fair; bad data can soft-lock or frustrate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-02 | Denial of Service | Authoring an impossible gap or misplaced checkpoint | medium | mitigate | Every spike gets a preceding checkpoint; every gap is verified jumpable via browser boot + manual playtest in Plan 03 |
| T-17-03 | Information Disclosure | Level data file accidentally references engine global at module scope | low | mitigate | check-import-safety.sh runs per task; level modules import only ../config.js |
</threat_model>

<verification>
- `node --check` passes for all three new level files.
- `bash scripts/check-import-safety.sh` passes.
- Each descriptor has the correct `id`, `displayName`, `allowedTables`, `bounds`, and mechanic counts per D-03..D-10.
</verification>

<success_criteria>
- `src/levels/level-02.js`, `level-03.js`, and `level-04.js` exist and follow the `level-01` schema.
- Each level is completable on the existing movement spine and uses the correct `bounds.right` (2800 / 3400 / 4000).
- Difficulty ramps as specified: table pools and platforming challenge increase across the set on decoupled axes.
- No engine globals at module top level in any new level file.
</success_criteria>

<output>
Create `.planning/phases/17-build-the-levels/17-02-SUMMARY.md` when done.
</output>
