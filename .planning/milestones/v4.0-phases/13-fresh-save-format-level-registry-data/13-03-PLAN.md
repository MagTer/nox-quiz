---
phase: 13-fresh-save-format-level-registry-data
plan: 03
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - src/levels/build.js
  - src/levels/level-01.js
  - src/levels/index.js
autonomous: true
requirements: [LVL-02, SAVE-06]
must_haves:
  truths:
    - "Levels are plain-JS data modules consumed by ONE parameterized buildLevel(levelData), registered in an ordered registry exposing LEVEL_ORDER + getLevel(id) (LVL-02)"
    - "The v3.0 level lifts in VERBATIM as level-01 — merged-floor colliders and the tightened spike hitbox preserved exactly; no Kaplay addLevel, no Tiled, no build step (LVL-02)"
    - "isUnlocked(id, progress) is DERIVED from LEVEL_ORDER + cleared facts — first level always unlocked, level N unlocked iff predecessor cleared; nothing stored (SAVE-06)"
    - "level-01.js and index.js reference NO engine globals at module top level; build.js keeps its Rect/engine guard INSIDE buildLevel's body (a727c13)"
    - "The descriptor carries id/displayName/geometry/allowedTables PLUS unset optional slots (mechanics/theme/parallax) the builder ignores when unset"
  artifacts:
    - path: "src/levels/build.js"
      provides: "Generalized buildLevel(levelData) with the Rect guard inside the body"
      exports: ["buildLevel"]
    - path: "src/levels/level-01.js"
      provides: "v3.0 geometry lifted verbatim, wrapped in the forward-looking descriptor schema"
      exports: ["LEVEL_01"]
      contains: "geometry"
    - path: "src/levels/index.js"
      provides: "Ordered registry: LEVEL_ORDER, getLevel(id), isUnlocked(id, progress)"
      exports: ["LEVEL_ORDER", "getLevel", "isUnlocked"]
  key_links:
    - from: "src/levels/index.js"
      to: "src/levels/level-01.js"
      via: "imports LEVEL_01 to build the ordered registry + BY_ID map"
      pattern: "from \"\\./level-01\\.js\""
    - from: "src/levels/build.js"
      to: "src/config.js"
      via: "imports CONFIG (../config.js — TWO dirs deep, mirror src/math/brain.js)"
      pattern: "from \"\\.\\./config\\.js\""
---

<objective>
Wave 1 Plan B (levels) — split the shipped `src/level.js` into a `src/levels/` directory: one
parameterized `buildLevel(levelData)`, one verbatim `level-01.js` data module wrapped in the
forward-looking descriptor schema, and an ordered `index.js` registry exposing `LEVEL_ORDER`,
`getLevel(id)`, and the DERIVED `isUnlocked(id, progress)` helper.

This is a VERBATIM LIFT, not a redesign (research Pitfall 3 / "Don't Hand-Roll"): the merged-floor
anti-seam-stick colliders and the tightened spike hitbox are hard-won (Phase 8/9) — lift them
exactly, do NOT switch to Kaplay addLevel or per-tile colliders.

Delivers LVL-02 (registry + parameterized builder + verbatim level-01) and SAVE-06's derived-unlock
helper (the cleared FACTS live in progress.js, Plan 02; the DERIVATION lives here with LEVEL_ORDER).

Purpose: the level half of the data spine every later phase consumes.
Output: `src/levels/{build.js, level-01.js, index.js}`.

Runs in Wave 1 in parallel with Plan 02 (save) — no shared files. Does NOT touch game.js or
delete level.js (that is Wave 2 Plan 04). Turns the Wave 0 registry greps + LVL-02 smoke cases GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fresh-save-format-level-registry-data/13-PATTERNS.md
@.planning/phases/13-fresh-save-format-level-registry-data/13-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- `src/levels/build.js` exporting `buildLevel(levelData)` (reads levelData.geometry; Rect guard INSIDE body)
- `src/levels/level-01.js` exporting `LEVEL_01` — descriptor with `id` "level-01", `displayName`, `allowedTables` [6,7,8,9], `geometry` { floors, platforms, coins, spikes, goal, checkpoints } (verbatim v3.0), and unset optional slots `mechanics: []`, `theme: null`, `parallax: null`
- `src/levels/index.js` exporting `LEVEL_ORDER` (["level-01"]), `getLevel(id)` (forgiving fallback to first), `isUnlocked(id, progress)` (derived from LEVEL_ORDER + progress.isLevelCleared)
NOTE: src/level.js is NOT deleted here (Wave 2 Plan 04 deletes it after game.js rewires). Both can coexist this wave.
</artifacts_this_phase_produces>

<tasks>

<!-- planner-discipline-allow: typeof Rect -->
<!-- planner-discipline-allow: addLevel -->
<!-- planner-discipline-allow: kaplay -->

<task type="auto">
  <name>Task 1: Create src/levels/level-01.js — verbatim geometry in the descriptor schema</name>
  <files>src/levels/level-01.js</files>
  <read_first>
    - src/level.js (the LEVEL object lines 44-107 — lift floors (46-50), platforms (54-59), coins (69-80), spikes (83-87), goal (90), checkpoints (101-106) VERBATIM, including the CONFIG-relative expressions and the explanatory comments)
    - src/config.js (CONFIG.FLOOR_Y line 35, CONFIG.SPIKE_SIZE line 41, CONFIG.GOAL_SIZE line 44 — referenced by the spike/goal/checkpoint expressions)
    - src/math/brain.js (line 32 area: import depth `../config.js` — modules in subdirs of src/ use two dots)
    - .planning/phases/13-fresh-save-format-level-registry-data/13-RESEARCH.md (Pattern 3: descriptor schema; Code Examples)
    - .planning/phases/13-fresh-save-format-level-registry-data/13-PATTERNS.md ("src/levels/level-01.js" section: geometry to preserve + descriptor wrapper)
  </read_first>
  <action>
    Create src/levels/level-01.js. Import CONFIG from "../config.js" (TWO dots — this file is in src/levels/, NOT src/; mirror src/math/brain.js, research Pitfall 5). Define the local const FLOOR_Y = CONFIG.FLOOR_Y to support the spike/goal/checkpoint expressions, exactly as src/level.js does at line 35.

    Export const LEVEL_01 as the descriptor:
    - id: "level-01"
    - displayName: a short dark-grunge-toned title (copy is discretion — e.g. "The First Descent"; placeholder fine)
    - allowedTables: [6, 7, 8, 9]  (difficulty seam — DATA only; wired through in Wave 2; enforced for real in Phase 16 per research A4; do NOT treat as a behavior change)
    - geometry: an object holding floors, platforms, coins, spikes, goal, checkpoints — each value lifted VERBATIM from src/level.js's LEVEL (same numbers, same CONFIG-relative expressions like FLOOR_Y - CONFIG.SPIKE_SIZE, FLOOR_Y - CONFIG.GOAL_SIZE, FLOOR_Y - 48). Keep checkpoints INSIDE geometry (research A5: game.js reads them, and Wave 2 moves the read-site to level.geometry.checkpoints).
    - mechanics: []   (Phase 15/16 placeholder — buildLevel ignores when unset)
    - theme: null     (Phase 18 placeholder)
    - parallax: null  (Phase 18 placeholder)

    This is a pure DATA module: reference NO engine globals at all (no add/rect/Rect/sprite/vec2/typeof Rect). Import only ../config.js. The Wave 0 negative grep asserts this. The Wave 0 regression smoke deep-equals this geometry against the v3.0 values, so the numbers must match byte-for-byte.
  </action>
  <verify>
    <automated>node --check src/levels/level-01.js && node -e "import('./src/levels/level-01.js').then(m=>{const g=m.LEVEL_01.geometry;if(m.LEVEL_01.id!=='level-01')process.exit(1);if(!Array.isArray(g.floors)||g.floors.length!==3)process.exit(1);if(g.floors[0].x!==0||g.floors[0].w!==560)process.exit(1);if(!Array.isArray(g.checkpoints)||g.checkpoints.length!==4)process.exit(1);if(!Array.isArray(m.LEVEL_01.allowedTables))process.exit(1);console.log('level01: OK');}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/levels/level-01.js` exits 0 and the node -e probe prints `level01: OK` (id, geometry shape, first floor run {x:0,w:560}, 4 checkpoints, allowedTables present).
    - `grep -c 'typeof Rect' src/levels/level-01.js` == 0 and `grep -Ec '(^|[^.])(add|rect|sprite|vec2)\(' src/levels/level-01.js` == 0 (pure data, no engine).
    - `grep -c 'from "\.\./config\.js"' src/levels/level-01.js` == 1 (correct two-dot import depth).
    - checkpoints live inside geometry: `grep -c 'checkpoints' src/levels/level-01.js` >= 1 and the value is nested under geometry (verified by the probe reading g.checkpoints).
  </acceptance_criteria>
  <done>level-01.js exports LEVEL_01 with verbatim v3.0 geometry (floors/platforms/coins/spikes/goal/checkpoints) wrapped in the descriptor (id/displayName/allowedTables + unset mechanics/theme/parallax); it references no engine globals and imports only ../config.js.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/levels/build.js — generalized buildLevel(levelData), Rect guard inside body</name>
  <files>src/levels/build.js</files>
  <read_first>
    - src/level.js (buildLevel body lines 114-185 — lift VERBATIM, changing ONLY the geometry source from level.* to levelData.geometry.*: the Rect guard 119-123, merged-floor loop 126-142, platforms 145-156, coins 159-162, tightened spike hitbox 169-181, goal 184; and the leaf consts T/FLOOR_Y at 34-35)
    - .planning/phases/13-fresh-save-format-level-registry-data/13-PATTERNS.md ("src/levels/build.js" section: the a727c13 guard-inside-body rule + the verbatim collider/spike idioms + import depth)
    - .planning/phases/13-fresh-save-format-level-registry-data/13-RESEARCH.md (Pattern 2 + Pitfall 2 + Pitfall 3: guard placement and anti-seam-stick lift)
  </read_first>
  <action>
    Create src/levels/build.js. Import CONFIG from "../config.js" (TWO dots). Define the leaf consts const T = CONFIG.TILE_SIZE and const FLOOR_Y = CONFIG.FLOOR_Y at module top (these are pure config reads, NOT engine globals — safe at top level, exactly as src/level.js does).

    Export function buildLevel(levelData). Inside the function body, read const g = levelData.geometry once. Then lift the ENTIRE buildLevel body from src/level.js VERBATIM, changing only the geometry source (level.floors → g.floors, level.platforms → g.platforms, level.coins → g.coins, level.spikes → g.spikes, level.goal → g.goal):
    - Keep the typeof-Rect fail-loud guard as the FIRST statement INSIDE the function body (a727c13 — NOT at module top level; ES imports are hoisted before kaplay({global}) runs, so a top-level guard throws at import and blanks the game). Update only the error-message filename to reference build.js. Do NOT echo the banned guard token anywhere outside this single in-body guard line.
    - Keep the merged-floor idiom: ONE wide rect()+area()+body({isStatic:true}) "ground" collider per contiguous run, then visual-only sprite("ground") tiles with NO area()/body(). Do NOT switch to per-tile colliders or Kaplay addLevel (Pitfall 3 — reintroduces seam-stick).
    - Keep the platform loop identical (same merged-collider + visual tiles).
    - Keep the coin loop identical (sprite("coin")+area()+"coin", coin.play("spin")).
    - Keep the tightened spike hitbox EXACTLY: spikeOffX/spikeOffY from CONFIG.SPIKE_SIZE/SPIKE_HITBOX_W/SPIKE_HITBOX_H, area({ shape: new Rect(vec2(0), CONFIG.SPIKE_HITBOX_W, CONFIG.SPIKE_HITBOX_H), offset: vec2(spikeOffX, spikeOffY) }), tag "spike".
    - Keep the goal: sprite("goal")+pos(g.goal.x,g.goal.y)+area()+"goal".

    Do NOT build checkpoints here (the scene owns checkpoint markers — they live in geometry and game.js reads them in Wave 2). The optional mechanics/theme/parallax slots are ignored by buildLevel when unset. Reference engine globals (add/rect/sprite/pos/area/body/vec2/Rect) ONLY inside the function body.
  </action>
  <verify>
    <automated>node --check src/levels/build.js && grep -q 'export function buildLevel' src/levels/build.js && grep -q 'isStatic' src/levels/build.js && grep -q 'SPIKE_HITBOX_W' src/levels/build.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/levels/build.js` exits 0.
    - `grep -c 'export function buildLevel' src/levels/build.js` == 1; the merged-floor collider (`isStatic`) and the tightened spike hitbox (`SPIKE_HITBOX_W`) are present.
    - The a727c13 guard is INSIDE the function body, NOT at module top level: the `typeof Rect` guard appears AFTER the `export function buildLevel` line (verify by line order, e.g. `awk` that the first `typeof Rect` line number is greater than the `export function buildLevel` line number).
    - `grep -c 'addLevel' src/levels/build.js` == 0 (no Kaplay addLevel — merged colliders preserved).
    - `grep -c 'from "\.\./config\.js"' src/levels/build.js` == 1 (two-dot import depth).
  </acceptance_criteria>
  <done>build.js exports buildLevel(levelData) reading levelData.geometry, with the Rect/engine guard kept inside the function body, the merged-floor anti-seam-stick colliders and tightened spike hitbox lifted verbatim, no addLevel, and the two-dot config import.</done>
</task>

<task type="auto">
  <name>Task 3: Create src/levels/index.js — ordered registry + derived isUnlocked</name>
  <files>src/levels/index.js</files>
  <read_first>
    - .planning/phases/13-fresh-save-format-level-registry-data/13-RESEARCH.md ("The registry + derived-unlock helper" Code Example — the exact form)
    - .planning/phases/13-fresh-save-format-level-registry-data/13-PATTERNS.md ("src/levels/index.js" section: pure, no engine, derived unlock)
    - src/levels/level-01.js (the LEVEL_01 export created in Task 1 — imported here)
  </read_first>
  <action>
    Create src/levels/index.js. Import LEVEL_01 from "./level-01.js". Define const LEVELS = [LEVEL_01] as the ORDERED registry (the single source of level order — future levels append here).

    Export:
    - const LEVEL_ORDER = LEVELS.map((l) => l.id)  → ["level-01"]
    - getLevel(id): look up via a Map(LEVELS.map(l => [l.id, l])); return the match OR fall back to LEVELS[0] (forgiving — a bad/unknown id never crashes the boot).
    - isUnlocked(id, progress): DERIVE unlock from LEVEL_ORDER + the progress instance's cleared facts (research Pitfall 4 — never store unlocked). Compute i = LEVEL_ORDER.indexOf(id); if i <= 0 return true (first, or unknown→treated as first, always open); otherwise return progress.isLevelCleared(LEVEL_ORDER[i - 1]). Nothing is stored here.

    This is a PURE module: import ONLY ./level-01.js (which itself imports only ../config.js). Reference NO engine globals and NO storage — it must stay node-importable for the smoke. The Wave 0 negative grep asserts index.js has no engine globals at top level.
  </action>
  <verify>
    <automated>node --check src/levels/index.js && node -e "import('./src/levels/index.js').then(m=>{if(m.LEVEL_ORDER[0]!=='level-01')process.exit(1);if(m.getLevel('nope').id!=='level-01')process.exit(1);const fake={isLevelCleared:()=>false};if(m.isUnlocked('level-01',fake)!==true)process.exit(1);console.log('index13: OK');}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/levels/index.js` exits 0 and the node -e probe prints `index13: OK` (LEVEL_ORDER[0]==="level-01"; getLevel("nope") falls back to first; isUnlocked("level-01", fresh)===true).
    - `grep -c 'export' src/levels/index.js` >= 3 (LEVEL_ORDER, getLevel, isUnlocked) and `grep -c 'isUnlocked' src/levels/index.js` >= 1.
    - index.js imports only ./level-01.js: `grep -c 'kaplay' src/levels/index.js` == 0 and `grep -c 'localStorage' src/levels/index.js` == 0.
    - After all of Wave 1: `node scripts/smoke-progress.mjs` prints `smoke-progress: PASS` and `bash scripts/check-progress.sh` prints `progress checks: PASS`.
  </acceptance_criteria>
  <done>index.js exports the ordered LEVEL_ORDER, a forgiving getLevel(id), and a derived isUnlocked(id, progress) computed from LEVEL_ORDER + cleared facts (nothing stored); it imports only the data module and references no engine or storage. The full Wave 0 gate suite goes green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level id → registry | Level ids (from save data / future scene payloads) cross into getLevel/isUnlocked — an unknown/junk id must never crash or unlock a real level |
| import-time → scene-time | The a727c13 boundary: levels modules are evaluated at import (before kaplay init); only buildLevel's BODY runs at scene time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Denial of Service | a727c13 top-level engine global in levels/* | mitigate | Keep the Rect/engine guard INSIDE buildLevel; level-01.js + index.js reference no engine at all; Wave 0 negative grep + mandatory browser boot (Plan 04) catch a hoisted guard |
| T-13-07 | Tampering / integrity | unknown/junk level id in getLevel/isUnlocked | mitigate | getLevel falls back to the first level; isUnlocked derives strictly from LEVEL_ORDER so a junk id can never unlock a real level |
| T-13-08 | Tampering | regressing merged-floor colliders to per-tile/addLevel | mitigate | Verbatim lift; `grep addLevel == 0`; the kid-validated feel is re-confirmed by the Plan 04 browser boot |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero-dependency no-build canon (RESEARCH audit: N/A) |
</threat_model>

<verification>
- `node --check` passes for all three new files; the three node -e probes print OK.
- `grep addLevel src/levels/build.js` == 0; the Rect guard is inside buildLevel's body.
- level-01.js + index.js reference no engine globals; correct two-dot config import depth.
- After Wave 1 (this plan + Plan 02): `node scripts/smoke-progress.mjs` PASS and `bash scripts/check-progress.sh` PASS.
</verification>

<success_criteria>
A pure level registry (ordered LEVEL_ORDER + getLevel + derived isUnlocked) and ONE parameterized
buildLevel consume plain-JS level data; level-01 is the v3.0 level lifted verbatim with merged-floor
colliders and tightened spike hitbox intact; no addLevel/Tiled/build step; a727c13-safe.
</success_criteria>

<output>
Create `.planning/phases/13-fresh-save-format-level-registry-data/13-03-SUMMARY.md` when done.
</output>