---
phase: 13-fresh-save-format-level-registry-data
plan: 04
type: execute
wave: 2
depends_on: ["13-02", "13-03"]
files_modified:
  - src/scenes/game.js
  - src/math/brain.js
  - src/level.js
autonomous: false
requirements: [LVL-02, SAVE-06, SAVE-07]
must_haves:
  truths:
    - "game.js loads a level by id from the registry (getLevel(data?.levelId ?? LEVEL_ORDER[0])) and builds it via buildLevel — proving the spine, with NO scenes added (Phase 14)"
    - "On clear, game.js marks the level cleared (progress.markCleared(level.id)) and persists it via writeSave(progress.serialize(brain.snapshot())) — cleared round-trips under the new key (SAVE-06)"
    - "allowedTables threads from level data into createBrain and through brain.nextQuestion → calculateWeights (the difficulty seam wired, not enforced; algorithm untouched) (SAVE-07/LVL-03-prep)"
    - "The checkpoint read-site follows the geometry move: for (const cp of level.geometry.checkpoints)"
    - "src/level.js is deleted; the single import site is repointed; the game boots in a real browser and cleared persists under the new key (mandatory boot — greps ≠ boots)"
  artifacts:
    - path: "src/scenes/game.js"
      provides: "Registry-driven level load + cleared-persist wiring"
      contains: "markCleared"
    - path: "src/math/brain.js"
      provides: "allowedTables threaded into nextQuestion's calculateWeights call"
      contains: "allowedTables"
  key_links:
    - from: "src/scenes/game.js"
      to: "src/levels/index.js"
      via: "import { getLevel, LEVEL_ORDER } and load by id (replaces the old ../level.js import)"
      pattern: "from \"\\.\\./levels/index\\.js\""
    - from: "src/scenes/game.js"
      to: "src/levels/build.js"
      via: "import { buildLevel } and call buildLevel(level) at scene time"
      pattern: "from \"\\.\\./levels/build\\.js\""
    - from: "src/scenes/game.js"
      to: "src/progress.js"
      via: "progress.markCleared(level.id) on clear + writeSave(progress.serialize(...))"
      pattern: "markCleared"
---

<objective>
Wave 2 (wire) — rewire the single `src/level.js` import site in `src/scenes/game.js` to load a
level by id from the registry, thread `allowedTables` from the level into the brain, persist the
per-level `cleared` fact on clear, move the checkpoint read-site into `geometry`, thread
`allowedTables` through `brain.nextQuestion`, delete the superseded `src/level.js`, and PROVE the
spine with a mandatory real browser boot (greps passing ≠ boots — the most expensive v3.0 lesson).

This phase adds NO scenes (title/level-select is Phase 14). It only proves the data spine loads,
plays, and persists under the new key.

Depends on Plan 02 (new save shape + markCleared) and Plan 03 (registry + buildLevel). Runs alone
in Wave 2 (it touches game.js + brain.js + deletes level.js; no parallel plan).

Purpose: integrate and prove the data spine end-to-end.
Output: rewired game.js + brain.js; level.js removed; a confirmed browser boot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fresh-save-format-level-registry-data/13-PATTERNS.md
@.planning/phases/13-fresh-save-format-level-registry-data/13-RESEARCH.md
@src/scenes/game.js
@src/math/brain.js
</context>

<artifacts_this_phase_produces>
This plan changes:
- `src/scenes/game.js`: import site (line 22) → `../levels/index.js` + `../levels/build.js`; load `const level = getLevel(data?.levelId ?? LEVEL_ORDER[0])`; `createBrain({ ..., allowedTables: level.allowedTables })`; `buildLevel(level)`; checkpoint loop → `level.geometry.checkpoints`; on clear → `progress.markCleared(level.id)` before `writeSave`
- `src/math/brain.js`: `nextQuestion()` threads `allowedTables` (accepted on `createBrain`) into `calculateWeights(allowedTables)` — the LOCKED weighting formulas are untouched
- `src/level.js`: DELETED (superseded by src/levels/)
</artifacts_this_phase_produces>

<tasks>

<!-- planner-discipline-allow: calculateWeights(allowedTables) -->
<!-- planner-discipline-allow: calculateWeights(undefined) -->

<task type="auto">
  <name>Task 1: Thread allowedTables through brain.nextQuestion (wiring only — algorithm locked)</name>
  <files>src/math/brain.js</files>
  <read_first>
    - src/math/brain.js (createBrain signature line 60 — accepts { seedAccuracy, seedHistory }; calculateWeights already accepts allowedTables lines 128-161; nextQuestion calls calculateWeights(undefined) at line 226 — the one wiring gap; the LOCKED weighting math lines 132-160 — DO NOT touch)
    - .planning/phases/13-fresh-save-format-level-registry-data/13-PATTERNS.md ("src/math/brain.js" section: one-line wiring change, do not retune)
  </read_first>
  <action>
    Make the difficulty-seam WIRING change in src/math/brain.js (research: this is plumbing, not an algorithm change — fully enforced in Phase 16):
    - Extend the createBrain destructured options (line 60) to also accept allowedTables: change `{ seedAccuracy, seedHistory } = {}` to `{ seedAccuracy, seedHistory, allowedTables } = {}`. Capture it in the closure.
    - In nextQuestion() (line 226), replace `calculateWeights(undefined)` with `calculateWeights(allowedTables)` so a per-level pool, when provided, takes effect; when undefined, behavior is byte-identical to today (all 9 tables, 6-9 biased).
    - Do NOT alter calculateWeights's formulas (lines 132-160), the EWMA, the distractor logic, or any CONFIG.BRAIN value. The 6-9 weighted selection algorithm is LOCKED (REQUIREMENTS Out of Scope).
    - Update the nextQuestion doc comment (lines 219-224) only to note allowedTables now flows through.
  </action>
  <verify>
    <automated>node --check src/math/brain.js && grep -q 'calculateWeights(allowedTables)' src/math/brain.js && node -e "import('./src/math/brain.js').then(m=>{const b=m.createBrain({allowedTables:[6,7]});for(let i=0;i<200;i++){const a=b.nextQuestion().a;if(![6,7].includes(a))process.exit(1);}console.log('brain13: OK');}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/math/brain.js` exits 0; the brain13 probe prints `brain13: OK` (a [6,7] pool yields only tables 6 and 7).
    - `grep -c 'calculateWeights(allowedTables)' src/math/brain.js` == 1 and `grep -c 'calculateWeights(undefined)' src/math/brain.js` == 0.
    - The LOCKED constants are untouched: `git diff src/math/brain.js` shows changes only to the createBrain options line, the nextQuestion calculateWeights call, and the doc comment — NO change to the weight formulas or CONFIG.BRAIN.
  </acceptance_criteria>
  <done>brain.createBrain accepts allowedTables and nextQuestion threads it into calculateWeights; a per-level pool restricts selection while an undefined pool is byte-identical to today; the weighting algorithm is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire game.js to the registry + persist cleared; delete src/level.js</name>
  <files>src/scenes/game.js, src/level.js</files>
  <read_first>
    - src/scenes/game.js (the import site line 22 to replace; the load/seed/build spine lines 56-78; the checkpoint loop line 89 `for (const cp of LEVEL.checkpoints)`; the onClear block lines 161-177 where markCleared + writeSave go; the closure anti-leak contracts lines 36-47, reset/respawn 103-112, onHide/onSceneLeave 208-224 — all PRESERVED)
    - src/levels/index.js (getLevel, LEVEL_ORDER, isUnlocked — created in Plan 03)
    - src/levels/build.js (buildLevel — created in Plan 03)
    - .planning/phases/13-fresh-save-format-level-registry-data/13-PATTERNS.md ("src/scenes/game.js" section: import swap, load-by-id, allowedTables, checkpoint read-site move, markCleared placement)
  </read_first>
  <action>
    Rewire src/scenes/game.js (minimal — preserve every anti-leak contract and the load/seed/build/persist spine):
    1. Replace the import at line 22 (`import { LEVEL, buildLevel } from "../level.js";`) with two imports: `import { getLevel, LEVEL_ORDER } from "../levels/index.js";` and `import { buildLevel } from "../levels/build.js";`.
    2. Add a level lookup before the brain construction: `const level = getLevel(data?.levelId ?? LEVEL_ORDER[0]);` (load by id; default to the first level — this phase adds no scenes, but the seam is ready for Phase 14).
    3. Thread allowedTables into the brain: add `allowedTables: level.allowedTables` to the createBrain({ ... }) options (lines 58-61), alongside seedAccuracy/seedHistory.
    4. Change `buildLevel(LEVEL)` (line 73) to `buildLevel(level)`.
    5. Move the checkpoint read-site (line 89): change `for (const cp of LEVEL.checkpoints)` to `for (const cp of level.geometry.checkpoints)` (checkpoints now live inside geometry — research A5).
    6. In the onClear handler (lines 161-177), add `progress.markCleared(level.id);` after `const leveledUp = progress.addXp(table);` and before the existing `writeSave(progress.serialize(brain.snapshot()));` so the cleared fact persists in the same write (SAVE-06). The serialize from Plan 02 now includes the levels map.
    7. Do NOT touch reset()/respawn(), the onHide/onSceneLeave cancellers, the coin/spike/goal wiring, or any closure state. No new scenes, no go() to another level.
    8. DELETE src/level.js (it is superseded; the only import site is now repointed — research Open Question #1 recommends delete over shim). Use `git rm src/level.js` or remove the file.

    Verify no other module still imports ../level.js or ./level.js after the change.
  </action>
  <verify>
    <automated>node --check src/scenes/game.js && test ! -f src/level.js && grep -q 'levels/index.js' src/scenes/game.js && grep -q 'markCleared' src/scenes/game.js && grep -q 'level.geometry.checkpoints' src/scenes/game.js && ! grep -rEq 'from "\.\.?/level\.js"' src/</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/scenes/game.js` exits 0.
    - `src/level.js` no longer exists (`test ! -f src/level.js`).
    - No module imports the old level.js: `grep -rEc 'from "\.\.?/level\.js"' src/` == 0.
    - game.js contains the registry imports, `markCleared`, `allowedTables: level.allowedTables`, `buildLevel(level)`, and `level.geometry.checkpoints`.
    - Wave merge gate is green: `bash scripts/check-progress.sh` prints `progress checks: PASS` (which also runs the smoke).
  </acceptance_criteria>
  <done>game.js loads the level by id from the registry, threads allowedTables into the brain, builds via buildLevel(level), reads checkpoints from level.geometry, and persists the cleared fact on clear; src/level.js is deleted and no import references it; the full structural gate + smoke pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mandatory real-browser boot — level-01 loads from registry + cleared persists under the new key</name>
  <what-built>
    The v4.0 data spine is wired end-to-end: a fresh versioned save (new key mathlab_platformer_v2)
    with a per-level cleared map + derived unlock, a level registry + parameterized builder, and
    level-01 lifted verbatim from v3.0. game.js now loads level-01 from the registry and persists
    the cleared fact under the new key. All automated gates (node smoke + structural greps) are green.
    This checkpoint is the MANDATORY real-browser boot — greps passing ≠ boots (the most expensive
    v3.0 lesson; STATE.md cross-cutting mitigation #2).
  </what-built>
  <how-to-verify>
    1. Serve the app over HTTP from the repo root (file:// is blocked by the inline guard):
       run `python3 -m http.server 8000` and open http://localhost:8000/ (or use the existing dev-server / nginx flow). Confirm the index page loads the game.
    2. Confirm the game BOOTS to a visible canvas with level-01 rendered — the dark-grunge floor,
       platforms, coins, spikes, and goal all appear (NOT a blank/loading-stuck screen, which would
       indicate an a727c13 import-time-global regression).
    3. Play level-01: confirm the merged-floor feel is intact — the player does NOT snag on floor
       seams, and spikes only kill on a real points touch (not from empty tile edges). This confirms
       the verbatim collider/spike lift.
    4. Reach the goal, answer the math gate correctly to clear the level.
    5. Open DevTools → Application → Local Storage: confirm a key `mathlab_platformer_v2` exists with
       a JSON value containing a `levels` object marking level-01 `cleared: true`, plus xp/level/
       accuracy/history. Confirm the old `mathlab_platformer_v1` key (if present from v3.0) is NOT
       read or modified.
    6. Reload the page: confirm the game still boots and the save persists (the cleared fact and
       xp/level survive the reload under the new key).
    7. (Optional corrupt-save sanity) In DevTools console, set localStorage.mathlab_platformer_v2 to
       a junk string (e.g. "not json"), reload, and confirm the game still BOOTS to safe defaults
       (never bricks) — then optionally clear it.
  </how-to-verify>
  <resume-signal>Type "approved" if the game boots, level-01 loads from the registry, the merged-floor/spike feel is intact, and cleared persists under mathlab_platformer_v2 across a reload (and a corrupt save never bricks boot). Otherwise describe what you saw (blank screen, seam-stick, wrong key, lost progress).</resume-signal>
  <action>Human-only verification — no code is written in this task. Serve the app over HTTP and perform the boot/play/persist/reload steps in how-to-verify. The executor automates nothing here; it pauses for the human to confirm the real browser boot (greps passing does not prove the game boots — the most expensive v3.0 lesson).</action>
  <verify>
    <human-check>The human follows the how-to-verify steps and confirms: game boots to a visible level-01 from the registry, merged-floor/spike feel intact, cleared persists under the new key across a reload, and a corrupt save never bricks boot.</human-check>
  </verify>
  <done>The human has confirmed (resume-signal "approved") that the game boots in a real browser, level-01 loads from the registry, gameplay feel is intact, and per-level cleared + XP/level persist under the fresh key across a reload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| go() data payload → game.js | data?.levelId is consumed by getLevel — an unknown id must fall back to the first level, never crash |
| import-time → scene-time | a727c13: buildLevel's engine references run only at scene time; a regression here blanks the game (caught only by the boot, not greps) |
| localStorage → game.js | the loaded save seeds progress/brain; corrupt data must yield safe defaults (covered by Plan 02 guards) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Denial of Service | a727c13 regression surfacing only in the browser | mitigate | Mandatory real-browser boot checkpoint (greps ≠ boots); guard stays inside buildLevel (Plan 03) |
| T-13-10 | Tampering / integrity | unknown levelId in the go() payload | mitigate | getLevel falls back to LEVEL_ORDER[0]; isUnlocked derives strictly from LEVEL_ORDER |
| T-13-11 | Repudiation | cleared fact lost across reload | mitigate | markCleared persists in the same writeSave as XP; verified live in the boot checkpoint (step 5-6) |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero-dependency no-build canon (RESEARCH audit: N/A) |
</threat_model>

<verification>
- `node --check` passes for game.js and brain.js; the brain13 probe prints OK.
- `src/level.js` is deleted; no import references the old path.
- `bash scripts/check-progress.sh` prints `progress checks: PASS` (smoke included).
- MANDATORY: the human-verify checkpoint confirms a real browser boot — level-01 loads from the
  registry, merged-floor/spike feel intact, and cleared persists under mathlab_platformer_v2 across
  a reload; a corrupt save never bricks boot.
</verification>

<success_criteria>
The game loads level-01 from the registry by id, threads the level's allowedTables into the
unchanged brain, persists the per-level cleared fact under the fresh versioned key, and survives a
real browser boot + reload with progress intact — the data spine is proven end-to-end with zero
a727c13 risk.
</success_criteria>

<output>
Create `.planning/phases/13-fresh-save-format-level-registry-data/13-04-SUMMARY.md` when done.
</output>