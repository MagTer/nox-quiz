---
phase: 13-fresh-save-format-level-registry-data
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/smoke-progress.mjs
  - scripts/check-progress.sh
autonomous: true
requirements: [SAVE-05, SAVE-06, SAVE-07, LVL-02]
must_haves:
  truths:
    - "The headless node smoke asserts the cleared-map round-trip, derived unlock, corrupt/foreign-save safe-default, the ordered registry, and level-01 verbatim geometry"
    - "The structural grep gate asserts the NEW save key (not the v3.0 key), the levels/cleared/isLevelCleared/markCleared seam in progress.js, the registry files, and a negative no-engine-global grep for src/levels/ data+registry modules"
    - "Both gates are wired and runnable; they go red until the Wave 1/2 implementation lands, then green"
  artifacts:
    - path: "scripts/smoke-progress.mjs"
      provides: "Extended node smoke covering SAVE-05/06/07 + LVL-02 cases"
      contains: "isUnlocked"
    - path: "scripts/check-progress.sh"
      provides: "Extended structural grep gate covering the new save shape + registry + import-safety"
      contains: "src/levels/index.js"
  key_links:
    - from: "scripts/smoke-progress.mjs"
      to: "src/levels/index.js"
      via: "import { LEVEL_ORDER, getLevel, isUnlocked } from registry (asserted after Wave 1)"
      pattern: "from \"\\.\\./src/levels/index\\.js\""
    - from: "scripts/check-progress.sh"
      to: "src/config.js"
      via: "grep for the NEW save key string (replacing the v3.0 key grep at line 41)"
      pattern: "mathlab_platformer_v2"
---

<objective>
Wave 0 — extend the project's two "test framework" scripts (the headless node smoke and the
structural grep gate) to cover the NEW save shape (per-level cleared map + derived unlock +
corrupt-save tolerance), the level registry (ordered, level-01 verbatim, builder importable),
and the a727c13 import-safety invariant for `src/levels/` BEFORE the implementation waves
that depend on them.

Per the Nyquist rule and 13-VALIDATION.md Wave 0 Requirements: these gates are the automated
verify commands every later task runs. They MUST exist first. They will be RED until Wave 1/2
create the modules they assert, then GREEN — a real gate, not a no-op (the same convention the
shipped harness already uses; see the header comments in both files).

Purpose: establish the automated feedback loop the implementation waves verify against.
Output: extended `scripts/smoke-progress.mjs` + `scripts/check-progress.sh`.

This plan picks the NEW save key as `mathlab_platformer_v2` and VERSION `2` (Claude's
discretion per 13-CONTEXT.md; research A1). Wave 1 Plan 02 MUST use the same key/version
string in `src/config.js`, or this gate stays red.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fresh-save-format-level-registry-data/13-VALIDATION.md
@.planning/phases/13-fresh-save-format-level-registry-data/13-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Recorded here so Wave 0 asserts the exact names Waves 1–2 will create:
- New files: `src/levels/index.js`, `src/levels/build.js`, `src/levels/level-01.js`
- New save key + version: `CONFIG.SAVE.KEY = "mathlab_platformer_v2"`, `CONFIG.SAVE.VERSION = 2`
- New progress.js exports/closure helpers: `isLevelCleared(id)`, `markCleared(id)`; defaults()/validate()/serialize() gain a `levels` map
- New registry exports: `LEVEL_ORDER` (string[]), `getLevel(id)`, `isUnlocked(id, progress)`
- New descriptor schema field names (level-01.js): `id`, `displayName`, `allowedTables`, `geometry` (with nested `floors`, `platforms`, `coins`, `spikes`, `goal`, `checkpoints`), and optional unset slots `mechanics`, `theme`, `parallax`
- `src/level.js` is DELETED in Wave 2; `src/scenes/game.js` import site at line 22 moves to the registry/builder
- `src/math/brain.js` `nextQuestion()` threads `allowedTables` into `calculateWeights`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend smoke-progress.mjs with the new-save-shape + registry cases</name>
  <files>scripts/smoke-progress.mjs</files>
  <read_first>
    - scripts/smoke-progress.mjs (the file to extend; reuse the check(cond,msg)+failures idiom at lines 26-31, the serialize→createProgress round-trip at lines 85-90, real-module direct imports at lines 22-24)
    - src/progress.js (the seam being tested: createProgress lines 54-74, defaults() lines 157-159, validate() lines 166-203, serialize() lines 131-139 — these get a `levels` map in Wave 1 Plan 02)
    - src/level.js (the v3.0 LEVEL geometry, lines 44-107 — the verbatim source the regression deep-equal compares against; floors at 46-50, platforms 54-59, coins 69-80, spikes 83-87, goal 90, checkpoints 101-106)
    - .planning/phases/13-fresh-save-format-level-registry-data/13-RESEARCH.md (Code Examples: "Extend the save shape", "registry + derived-unlock helper")
  </read_first>
  <action>
    Append new assertion blocks to scripts/smoke-progress.mjs using the existing check(cond,msg) idiom. Add a top-of-file import of the registry: import { LEVEL_ORDER, getLevel, isUnlocked } from "../src/levels/index.js" (node-relative from repo root, matching the existing ../src/... import style at lines 22-24).

    Add these cases (cite the requirement in each check message):
    - SAVE-06 cleared-map round-trip: create a progress, call markCleared("level-01"), serialize it (pass an empty brain snapshot), reconstruct via createProgress(blob), assert isLevelCleared("level-01") === true and that a never-cleared id is false. Implements SAVE-06.
    - SAVE-06 derived unlock: assert isUnlocked(LEVEL_ORDER[0], freshProgress) === true (first always open) using a progress with nothing cleared. For a hypothetical second id (guard with a length check so a single-level registry does not false-fail), assert it is locked until its predecessor is cleared, then unlocked after markCleared(predecessor). Derives from LEVEL_ORDER per SAVE-06; nothing stored.
    - SAVE-05 never-bricks: build several hostile blobs as plain objects passed DIRECTLY to createProgress (the smoke never touches storage): a corrupt-shaped levels value (levels: "not-an-object"), a junk-id cleared map (levels: { "__proto__": { cleared: true }, "ghost-level": { cleared: "yes" } }), an Infinity level ({ version: 2, level: 1e400 }), and a foreign blob ({ totally: "different" }). Assert createProgress(badBlob) NEVER throws and yields safe values (level >= 1 finite; isLevelCleared("ghost-level") not truthy from a non-boolean; no prototype pollution — assert ({}).cleared is undefined after construction). Implements SAVE-05.
    - LVL-02 registry: assert LEVEL_ORDER[0] === "level-01"; assert getLevel("level-01") returns a descriptor with id "level-01"; assert getLevel("does-not-exist") falls back to the first level (forgiving). Implements LVL-02.
    - LVL-02 regression (verbatim geometry): deep-equal getLevel("level-01").geometry against the EXACT v3.0 values lifted from src/level.js (floors/platforms/coins/spikes/goal/checkpoints). Write a small recursive deep-equal helper inline (no dependency). Because some spike/goal/checkpoint values are expressed via CONFIG (FLOOR_Y - CONFIG.SPIKE_SIZE etc.), import CONFIG (already imported) and compare against the same computed expressions, OR compare numerically by reconstructing the expected object from CONFIG. Proves the lift is byte-equivalent (LVL-02). Implements the "LVL-02 regression" validation row.
    - SAVE-07: keep the existing xp/level/accuracy/history round-trip cases green; add one assertion that the serialized blob now ALSO carries a `levels` object field (so the new shape coexists with the old). Implements SAVE-07.

    Keep the final failures>0 → process.exit(1) block at the end. Do NOT remove or weaken any existing assertion. Do NOT use any test framework or external import.
  </action>
  <verify>
    <automated>node --check scripts/smoke-progress.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/smoke-progress.mjs` exits 0 (the file is syntactically valid).
    - `grep -c 'isUnlocked' scripts/smoke-progress.mjs` >= 1 and `grep -c 'LEVEL_ORDER' scripts/smoke-progress.mjs` >= 1 and `grep -c 'markCleared' scripts/smoke-progress.mjs` >= 1 and `grep -c 'getLevel' scripts/smoke-progress.mjs` >= 1.
    - The import `from "../src/levels/index.js"` is present.
    - Running `node scripts/smoke-progress.mjs` FAILS at this point ONLY because src/levels/index.js does not exist yet (the import throws) — this is the expected RED state documented in the file header; it is NOT a task failure. (It turns green after Wave 1/2.)
  </acceptance_criteria>
  <done>smoke-progress.mjs imports the registry and adds SAVE-05/06/07 + LVL-02 + LVL-02-regression cases using the existing check() idiom; node --check passes; the smoke is intentionally red until the modules exist.</done>
</task>

<!-- planner-discipline-allow: mathlab_platformer_v2 -->
<!-- planner-discipline-allow: mathlab_platformer_v1 -->

<task type="auto">
  <name>Task 2: Extend check-progress.sh — new key, levels seam, registry, import-safety grep</name>
  <files>scripts/check-progress.sh</files>
  <read_first>
    - scripts/check-progress.sh (the file to extend; existence+node --check loop at lines 33-36, positive grep idiom at lines 39-62, negative grep idiom at lines 65-82, the v3.0-key grep to REPLACE at lines 41-42, final smoke invocation at line 85)
    - src/config.js (CONFIG.SAVE block lines 80-86 — the key Wave 1 Plan 02 bumps to mathlab_platformer_v2 / VERSION 2)
    - .planning/phases/13-fresh-save-format-level-registry-data/13-PATTERNS.md (the "check-progress.sh" section: exact asserts to add; the scope-the-grep note so build.js's in-body `Rect` is not false-flagged)
  </read_first>
  <action>
    Edit scripts/check-progress.sh to extend the structural gate. Preserve every existing assertion's intent; ADD the following, mirroring the existing fail()+grep idiom:

    1. REPLACE the v3.0 key grep at lines 41-42: change the matched string from mathlab_platformer_v1 to mathlab_platformer_v2 (the NEW clean-reset key) and update the failure message accordingly. This is the canonical grep-coupling trap (research Pitfall 6) — the key bump in config.js (Wave 1 Plan 02) and this grep must agree.

    2. Add positive greps on src/progress.js for the new cleared-map seam: assert the file contains the tokens for the levels map and the helpers — `markCleared` and `isLevelCleared` and `levels` must each be present (one grep each, with a clear fail message). These prove SAVE-06's stored-fact seam exists.

    3. Add the three registry files to the existence + `node --check` loop (extend the for-loop at lines 33-36 to also cover src/levels/index.js, src/levels/build.js, src/levels/level-01.js), so a missing/syntax-broken registry file yields a clear FAIL.

    4. Add positive greps: `LEVEL_ORDER` exported in src/levels/index.js; `getLevel` in src/levels/index.js; `isUnlocked` in src/levels/index.js; `buildLevel` defined in src/levels/build.js. These prove the LVL-02 registry/builder API exists.

    5. Add NEGATIVE import-safety greps (a727c13) scoped to the DATA + REGISTRY modules ONLY — assert src/levels/level-01.js and src/levels/index.js contain NO engine-global references: strip comments first (the project's gate convention — e.g. sed '/^[[:space:]]*\/\//d' before grep, mirroring check-safety.sh), then fail if any of these match in those two files: a bare engine constructor/factory call or a `typeof Rect` guard or the literal kaplay token. Do NOT scan src/levels/build.js with this negative grep — build.js LEGITIMATELY references Rect/add/etc. INSIDE buildLevel's body (a727c13-correct). Scope the negative assertion to level-01.js and index.js only.

    6. Keep the final `node "$ROOT/scripts/smoke-progress.mjs"` step (line 85) as the last assertion.

    Use grep -Eq / grep -q with comment-stripping where a banned token could otherwise match a comment, consistent with the comment-stripping discipline already used in the repo's safety gate. Choose grep patterns so the banned-token strings appear ONLY inside the matcher, never as bare prose that the file would later echo.
  </action>
  <verify>
    <automated>bash -n scripts/check-progress.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/check-progress.sh` exits 0 (valid shell syntax).
    - `grep -c 'mathlab_platformer_v2' scripts/check-progress.sh` >= 1 AND the old `mathlab_platformer_v1` assertion is gone: `grep -c 'mathlab_platformer_v1' scripts/check-progress.sh` == 0.
    - The script greps for `src/levels/index.js`, `src/levels/build.js`, `src/levels/level-01.js`, `LEVEL_ORDER`, `isUnlocked`, `markCleared`.
    - Running `bash scripts/check-progress.sh` FAILS now ONLY because the new key / modules do not exist yet (expected RED per the file header) — not a task failure.
  </acceptance_criteria>
  <done>check-progress.sh asserts the new key (v2), the progress.js levels/cleared seam, the three registry files (existence + node --check + API greps), and a comment-stripped negative import-safety grep scoped to level-01.js + index.js (NOT build.js); bash -n passes; the gate is intentionally red until the modules land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test scripts → source | The gates only READ source/run node; no untrusted input crosses into them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | check-progress.sh negative grep accidentally false-passing | mitigate | Strip comments before negative greps (repo convention); scope the a727c13 grep to level-01.js + index.js so build.js's legitimate in-body `Rect` is not flagged |
| T-13-02 | Repudiation | gate drifts from the new save shape (silent rot) | mitigate | This whole plan re-points the gates at the new key + registry in the SAME phase as the change (research Pitfall 6) |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — zero-dependency, no-build canon (RESEARCH Package Legitimacy Audit: N/A). No install tasks exist. |
</threat_model>

<verification>
- `node --check scripts/smoke-progress.mjs` passes.
- `bash -n scripts/check-progress.sh` passes.
- Both gates are RED (modules not yet created) — expected; they turn green after Waves 1–2.
- New key string `mathlab_platformer_v2` is asserted; the v3.0 key assertion is removed.
</verification>

<success_criteria>
The two test scripts are extended to cover the new save shape, the registry, and import-safety,
runnable and syntactically valid, and ready to verify Waves 1–2 (red now, green after impl).
</success_criteria>

<output>
Create `.planning/phases/13-fresh-save-format-level-registry-data/13-01-SUMMARY.md` when done.
</output>
