---
phase: 13-fresh-save-format-level-registry-data
plan: 02
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - src/config.js
  - src/progress.js
autonomous: true
requirements: [SAVE-05, SAVE-06, SAVE-07]
must_haves:
  truths:
    - "The game writes/reads a fresh versioned save under a NEW key (mathlab_platformer_v2, VERSION 2); the v3.0 save is NOT migrated (SAVE-05)"
    - "A missing, stale, foreign, or corrupt save loads safe defaults and never bricks boot — the existing isFinite/shape guards extend to the new levels map (SAVE-05)"
    - "Per-level `cleared` facts round-trip through serialize→loadSave→createProgress; only `cleared` is stored, never `unlocked` (SAVE-06)"
    - "XP/level/accuracy/history continue to persist verbatim under the new key and seed the brain (SAVE-07)"
    - "createProgress stays a PURE factory (never reads storage at construction); the loadSave/writeSave/storageAvailable seam stays defined-not-called at import; progress.js imports no engine"
  artifacts:
    - path: "src/config.js"
      provides: "CONFIG.SAVE bumped to the new clean-reset key + version"
      contains: "mathlab_platformer_v2"
    - path: "src/progress.js"
      provides: "Extended save shape: levels cleared-map + isLevelCleared/markCleared helpers"
      exports: ["createProgress", "loadSave", "writeSave"]
      contains: "markCleared"
  key_links:
    - from: "src/progress.js"
      to: "src/config.js"
      via: "reads CONFIG.SAVE.KEY / CONFIG.SAVE.VERSION (new key + version gate)"
      pattern: "CONFIG\\.SAVE\\.(KEY|VERSION)"
    - from: "src/progress.js"
      to: "browser localStorage"
      via: "loadSave/writeSave behind storageAvailable() — defined, not called at import"
      pattern: "storageAvailable"
---

<objective>
Wave 1 Plan A (save) — extend the shipped, validated save seam in `src/progress.js` to a FRESH
versioned key with a per-level `cleared` map and derived-unlock support, keeping the pure-factory
firewall intact. This is a GENERALIZATION of shipped code, not a rewrite (research "Don't
Hand-Roll" / "Key insight": the biggest mistake would be rewriting validated logic during the
extension).

Delivers SAVE-05 (new key, clean reset, never bricks), SAVE-06 (per-level cleared facts persist;
unlock derived — the derivation helper itself lives in the registry, Plan 03), SAVE-07 (XP/level/
accuracy/history persist verbatim under the new key).

Purpose: the persistence half of the data spine every later phase consumes.
Output: extended `src/config.js` (new key/version) + `src/progress.js` (levels map + helpers).

Runs in Wave 1 in parallel with Plan 03 (levels registry) — no shared files. Turns the
Wave 0 progress.js / new-key greps and the SAVE-* smoke cases GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fresh-save-format-level-registry-data/13-PATTERNS.md
@.planning/phases/13-fresh-save-format-level-registry-data/13-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- `CONFIG.SAVE.KEY = "mathlab_platformer_v2"`, `CONFIG.SAVE.VERSION = 2` (config.js)
- `defaults()` gains `levels: {}`
- `validate()` gains a `levels` branch (strict boolean coercion, tolerate junk ids, NEVER spread the blob)
- `createProgress(saved)` gains a closure-local `cleared` map + returns `isLevelCleared(id)` and `markCleared(id)`
- `serialize(brainSnapshot)` gains a `levels` field built from the closure `cleared` map ({ cleared: true } entries only)
The derived-unlock helper (`isUnlocked`) is NOT here — it lives in the registry (Plan 03), which owns LEVEL_ORDER. progress.js owns only the cleared FACTS.
</artifacts_this_phase_produces>

<tasks>

<!-- planner-discipline-allow: mathlab_platformer_v1 -->

<task type="auto" tdd="true">
  <name>Task 1: Bump CONFIG.SAVE to the new clean-reset key + version</name>
  <files>src/config.js</files>
  <read_first>
    - src/config.js (the CONFIG.SAVE block, lines 80-86 — KEY and VERSION to bump; the leaf-only "imports nothing" firewall comment at lines 1-7; the "no magic numbers" canon at line 3)
  </read_first>
  <behavior>
    - CONFIG.SAVE.KEY === "mathlab_platformer_v2" (a NEW string, distinct from the v3.0 key)
    - CONFIG.SAVE.VERSION === 2
    - config.js still imports nothing (leaf module)
  </behavior>
  <action>
    Edit the CONFIG.SAVE block in src/config.js: change KEY from the v3.0 string to "mathlab_platformer_v2" and VERSION from 1 to 2 (Claude's-discretion value per 13-CONTEXT.md; agreed with Wave 0 Plan 01's grep). Update the adjacent comment to note this is the v4.0 clean-reset key — the v3.0 mathlab_platformer_v1 blob is deliberately abandoned (NOT migrated, NOT deleted), made invisible by the new key; the version gate in loadSave is the second guard. Keep CONFIG a leaf module (no imports). Add no other constants — allowedTables is level DATA, not config (research A4).

    The new key is the PRIMARY clean-reset guard (SAVE-05): a v3.0 blob under the old key is never read. Do not reference or read the old key anywhere.
  </action>
  <verify>
    <automated>node --check src/config.js && node -e "import('./src/config.js').then(m=>{if(m.CONFIG.SAVE.KEY!=='mathlab_platformer_v2'||m.CONFIG.SAVE.VERSION!==2)process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/config.js` exits 0.
    - CONFIG.SAVE.KEY === "mathlab_platformer_v2" and CONFIG.SAVE.VERSION === 2 (verified by the node -e check above).
    - `grep -c 'mathlab_platformer_v1' src/config.js` == 0 (the v3.0 key is gone from config).
    - config.js still has no import statement (leaf): `grep -c '^import' src/config.js` == 0.
  </acceptance_criteria>
  <done>CONFIG.SAVE.KEY is the new v2 key and VERSION is 2; the v3.0 key no longer appears in config.js; config.js remains import-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend progress.js with the per-level cleared map + helpers + serialize/validate branches</name>
  <files>src/progress.js</files>
  <read_first>
    - src/progress.js (the whole shipped seam — extend in place: createProgress guards lines 54-74, the returned object getters/addXp/serialize lines 91-140, defaults() lines 157-159, validate() lines 166-203 with the accuracy branch at 182-189 as the idiom to mirror, the guarded seam loadSave/writeSave/storageAvailable lines 205-269)
    - .planning/phases/13-fresh-save-format-level-registry-data/13-RESEARCH.md ("Extend the save shape with a per-level cleared map" Code Example — the exact synthesized form)
    - .planning/phases/13-fresh-save-format-level-registry-data/13-PATTERNS.md (the "src/progress.js" section — defaults/validate/serialize/helpers spots and the firewall note)
  </read_first>
  <behavior>
    - markCleared("level-01") then serialize() → blob.levels["level-01"].cleared === true
    - createProgress(blob).isLevelCleared("level-01") === true; isLevelCleared("never") === false
    - A non-boolean cleared value (cleared: "yes") validates to NOT-cleared (strict === true coercion)
    - A junk/__proto__ id in the saved levels map never pollutes Object.prototype and is tolerated/ignored
    - createProgress(undefined) yields { levels: {} } behavior with no cleared ids; never throws
    - Existing xp/level/accuracy/history round-trip cases stay green; serialize still carries version/xp/level/accuracy/history
  </behavior>
  <action>
    Extend src/progress.js in place (do NOT rewrite the validated XP/level math or the guarded seam):

    1. defaults(): add `levels: {}` to the returned object (mirror research Code Example) so loadSave returns the new shape on every failure path.

    2. validate(): add a `levels` branch AFTER the history branch, mirroring the named-key, range-checked accuracy idiom (lines 182-189) — iterate Object.entries(data.levels) only when data.levels is a non-null object; for each [id, rec] write out.levels[id] = { cleared: rec && rec.cleared === true } using STRICT boolean coercion. Copy into the fresh `out` object from defaults() (NEVER spread/Object.assign the untrusted blob — prototype-pollution mitigation T-01-01). Tolerate unknown/junk ids (a junk id can never unlock a real level — unlock is derived from LEVEL_ORDER in the registry).

    3. createProgress(saved): seed a closure-local `const cleared = {}` map from saved.levels using the same `rec.cleared === true` strictness (guard saved && saved.levels && typeof saved.levels === "object"). Keep the existing xp/level finite guards untouched.

    4. Add to the returned object two helpers next to the getters: isLevelCleared(id) returning cleared[id] === true, and markCleared(id) setting cleared[id] = true. These own the cleared FACTS only — derived unlock lives in the registry (Plan 03); do NOT import LEVEL_ORDER or the engine here (firewall: progress.js imports nothing but ./config.js).

    5. serialize(brainSnapshot): add a `levels` field to the returned blob, built by iterating Object.keys(cleared) and emitting { cleared: true } per id. Keep version/xp/level/accuracy/history exactly as shipped.

    Keep createProgress PURE (never reads localStorage) and keep storageAvailable/loadSave/writeSave defined-not-called at import. Do NOT add any engine import. Keep the ./config.js single-dot import (this file is in src/, not src/levels/).
  </action>
  <verify>
    <automated>node --check src/progress.js && node -e "import('./src/progress.js').then(m=>{const p=m.createProgress();p.markCleared('level-01');const b=p.serialize({});if(b.levels['level-01'].cleared!==true)process.exit(1);const r=m.createProgress(b);if(r.isLevelCleared('level-01')!==true||r.isLevelCleared('never')!==false)process.exit(1);const bad=m.createProgress({version:2,level:1e400,levels:{'__proto__':{cleared:true},ghost:{cleared:'yes'}}});if(({}).cleared!==undefined)process.exit(1);if(bad.isLevelCleared('ghost'))process.exit(1);console.log('progress13: OK');}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/progress.js` exits 0 and the node -e behavior probe above prints `progress13: OK` (proves cleared round-trip, strict boolean coercion, never-bricks on Infinity/junk, and no prototype pollution).
    - `grep -c 'markCleared' src/progress.js` >= 1 and `grep -c 'isLevelCleared' src/progress.js` >= 1.
    - progress.js imports nothing but ./config.js: `grep -c '^import' src/progress.js` == 1 and `grep -c 'kaplay' src/progress.js` == 0.
    - After Plan 03 lands, `node scripts/smoke-progress.mjs` prints `smoke-progress: PASS` (the SAVE-05/06/07 cases go green).
  </acceptance_criteria>
  <done>progress.js carries a per-level cleared map (defaults/validate/serialize branches) and isLevelCleared/markCleared helpers; cleared facts round-trip; a corrupt/foreign/junk save yields safe defaults and never bricks or pollutes the prototype; createProgress stays pure and engine-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → progress.js | The parsed save blob is untrusted input crossing into the app (hand-edited / extension-injected / corrupt) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | validate() levels branch | mitigate | Copy ONLY named, range-checked keys into a fresh defaults() object; strict `=== true` boolean coercion; NEVER spread/Object.assign the blob (prototype-pollution T-01-01) |
| T-13-04 | Denial of Service (self) | createProgress numeric guards | mitigate | Reuse the shipped Number.isFinite + `>= 1` + Math.floor guards (an Infinity level would otherwise freeze progression); extend nothing that weakens them |
| T-13-05 | Denial of Service | loadSave/writeSave seam | accept→mitigated | Shipped storageAvailable() probe + try/catch + QuotaExceededError catch already make every failure path return defaults / no-op; new `levels` data rides through unchanged |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero-dependency no-build canon (RESEARCH audit: N/A) |
</threat_model>

<verification>
- `node --check src/config.js` and `node --check src/progress.js` pass.
- The Task 2 node -e probe prints `progress13: OK`.
- config.js holds the new key (v2) and no longer the v3.0 key; progress.js imports only ./config.js.
- After Plan 03: `node scripts/smoke-progress.mjs` → `smoke-progress: PASS`; `bash scripts/check-progress.sh` → `progress checks: PASS`.
</verification>

<success_criteria>
A fresh, versioned, clean-reset save format under a new key persists XP/level/accuracy/history
PLUS a per-level cleared map; a missing/stale/foreign/corrupt save loads safe defaults and never
bricks boot or pollutes the prototype; the pure-factory + guarded-seam firewall is intact.
</success_criteria>

<output>
Create `.planning/phases/13-fresh-save-format-level-registry-data/13-02-SUMMARY.md` when done.
</output>