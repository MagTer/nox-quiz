---
phase: 18-art-animation-parallax
plan: 04
type: execute
wave: 3
depends_on:
  - 18-02
  - 18-03
files_modified:
  - src/scenes/title.js
  - src/scenes/select.js
  - scripts/browser-boot.mjs
autonomous: false
requirements:
  - ART-04
  - SAFE-05
must_haves:
  truths:
    - "Title and level-select screens share the dark-grunge title-bg backdrop with no pink"
    - "Title screen keeps the neon-green 'Math Lab' wordmark and light-grey prompt readable over the backdrop"
    - "Level-select tiles show locked/unlocked/cleared states with the palette from 18-UI-SPEC.md and a bright white active cursor"
    - "Mandatory real browser boot confirms no runtime errors, no leaked handlers, and animation/parallax/title/select all render correctly"
  artifacts:
    - path: src/scenes/title.js
      provides: "title-bg backdrop + centered wordmark/prompt, no animated distractions"
      min_lines: 80
    - path: src/scenes/select.js
      provides: "title-bg backdrop + styled locked/unlocked/cleared tiles + white cursor"
      min_lines: 200
    - path: scripts/browser-boot.mjs
      provides: "automated title->select->level->select->title round-trip with error capture"
      min_lines: 120
  key_links:
    - from: src/scenes/title.js
      to: assets/tiles/title-bg.png
      via: "sprite(\"title-bg\") loaded in main.js"
    - from: src/scenes/select.js
      to: assets/tiles/title-bg.png
      via: "sprite(\"title-bg\") loaded in main.js"
    - from: src/scenes/select.js
      to: src/config.js
      via: "CONFIG.SELECT / CONFIG.TITLE_BG_Z / palette arrays"
---

<objective>
Skin the title and level-select screens to the dark-grunge aesthetic using the shared `title-bg` backdrop loaded in Plan 18-01. Keep the existing input logic intact: title advances on Enter/Space/click, select advances only on unlocked tiles via arrow keys + Enter or mouse click. Then extend `scripts/browser-boot.mjs` to exercise the full navigation flow and run the full static suite plus a mandatory real-browser checkpoint.

Purpose: ART-04 (styled title/select) and SAFE-05 (non-strobing, no over-stimulation) close the phase; the browser boot proves nothing leaked and the art/parallax/animation all render.
Output: Updated `src/scenes/title.js`, `src/scenes/select.js`, `scripts/browser-boot.mjs`, and sign-off evidence.
</objective>

<execution_context>
@GSD core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-art-animation-parallax/18-CONTEXT.md
@.planning/phases/18-art-animation-parallax/18-UI-SPEC.md
@src/scenes/title.js
@src/scenes/select.js
@src/config.js
@src/main.js
@scripts/browser-boot.mjs
@scripts/check-import-safety.sh
@scripts/check-safety.sh
@scripts/check-gate.sh
@scripts/smoke-progress.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the title-bg backdrop to src/scenes/title.js</name>
  <files>src/scenes/title.js</files>
  <action>
    In `src/scenes/title.js`, add the shared backdrop as the FIRST object in the scene so it renders behind everything:

    ```js
    add([sprite("title-bg"), pos(0, 0), fixed(), z(CONFIG.TITLE_BG_Z), "title"]);
    ```

    Keep the existing "Math Lab" wordmark (neon green `#00ff88`, size `CONFIG.TITLE.TITLE_SIZE`, centered, `fixed()`, `z(9000)`) and the prompt (light grey `#e8e8e8`, size `CONFIG.TITLE.PROMPT_SIZE`, centered below). Do not add animated title effects, scrolling text, or anything that could exceed the 400–500 ms flash/motion cap. The start controllers (Enter/Space/click) remain unchanged.
  </action>
  <verify>
    <automated>grep -E 'sprite\("title-bg"\)|fixed\(\)|z\(CONFIG.TITLE_BG_Z\)' src/scenes/title.js</automated>
  </verify>
  <done>
    `src/scenes/title.js` adds the title-bg sprite before the wordmark, keeps the wordmark/prompt readable, and makes no timer/animation additions. `node --check src/scenes/title.js` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Style the level-select tiles in src/scenes/select.js</name>
  <files>src/scenes/select.js</files>
  <action>
    In `src/scenes/select.js`:
    - Add the shared backdrop first: `add([sprite("title-bg"), pos(0, 0), fixed(), z(CONFIG.TITLE_BG_Z), "select"]);`
    - Replace the tile fill/border logic with the 18-UI-SPEC palette:
      - locked: fill `#444444`, border `#555555`, glyph `X`
      - unlocked: fill `#111111`, border `#00ff88`, no glyph
      - cleared: fill `#111111`, border `#66ccff`, glyph `v`
    - Keep tile dimensions, gap, positions, and the heading exactly as today (`CONFIG.SELECT.TILE_W/H/GAP/ROW_Y/START_X/HEADING_SIZE`).
    - Preserve the dual-input model: arrow keys move only among unlocked tiles, Enter plays the focused tile, and mouse click on an unlocked tile plays it. Locked tiles remain non-selectable and get no click handler.
    - The active keyboard cursor must keep the bright white `#ffffff` outline at width 5; non-active tiles keep their state border at width 2.

    Do not change the `progress`/`isUnlocked`/`isLevelCleared` derivation logic.
  </action>
  <verify>
    <automated>grep -E 'title-bg|#444444|#555555|#00ff88|#66ccff|CURSOR_BORDER|paintCursor' src/scenes/select.js</automated>
  </verify>
  <done>
    `src/scenes/select.js` adds the backdrop, uses the locked/unlocked/cleared palette, preserves input logic, and keeps the white active cursor. `node --check src/scenes/select.js` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend browser-boot.mjs and run the full verification suite</name>
  <files>scripts/browser-boot.mjs</files>
  <action>
    Extend `scripts/browser-boot.mjs` to exercise the full Phase 18 flow:
    - After visiting the levels, inject `go("title")` via `page.evaluate` to return to the title scene, then wait and advance to select again to confirm enter→leave→re-enter produces no leaked handlers or errors.
    - Keep the existing error capture (pageerror, console.error, HTTP 4xx/5xx).
    - Ensure the save blob still unlocks all four levels.

    Then run the full static suite in order:
    - `bash scripts/check-gate.sh`
    - `bash scripts/check-import-safety.sh`
    - `bash scripts/check-safety.sh`
    - `node scripts/smoke-progress.mjs`
    - `node scripts/browser-boot.mjs`

    If any step fails, fix the root cause before claiming the phase complete.
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/smoke-progress.mjs && node scripts/browser-boot.mjs</automated>
  </verify>
  <done>
    All static gates pass, `browser-boot.mjs` runs title→select→levels→title→select round-trip (using an injected `go("title")` for the return leg) with no errors, and no HTTP 4xx/5xx asset failures occur.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Real-browser visual sign-off for animation, parallax, and title/select readability</name>
  <files>src/scenes/title.js, src/scenes/select.js, src/player.js, src/parallax.js, src/scenes/game.js</files>
  <action>
    Open the game in a real browser, serve locally, and visually confirm:
    - Title screen: "Math Lab" is readable over the dark-grunge backdrop; no pink; no distracting animation.
    - Select screen: locked/unlocked/cleared states are visually distinct; the white cursor is obvious; the backdrop does not obscure tiles or heading.
    - In a level: idle/run/jump animations switch correctly, the player faces the movement direction, and the facing direction is preserved when stopping.
    - Parallax layers scroll slower than the camera, are muted/dark, and show no gaps at level edges.
    - Ground tiles show edge/center frames consistently across floors and platforms.
    - Enter→leave→re-enter title, select, and a level twice with no leaked input handlers, tweens, or effects.
  </action>
  <verify>
    <automated>Human approval required — type "approved" or describe issues</automated>
  </verify>
  <done>
    Visual pass confirms animation, parallax, tileset, title/select styling, and no leaks across scene re-entry.
  </done>
  <what-built>
    Phase 18 art/animation/parallax/styling is implemented and the automated suite is green. The remaining verification is the human visual pass for readability, calm motion, and feel.
  </what-built>
  <how-to-verify>
    1. Serve the game locally and open `src/index.html` in a real browser.
    2. On the title screen confirm readability and no pink/motion.
    3. Enter select, confirm tile states and cursor.
    4. Play level-01: watch idle/run/jump/facing, ground tile edges, and parallax depth.
    5. Run to the goal, clear the gate, return to select.
    6. Re-enter the same level, then Escape back to select, then back to title, then into select again — confirm no input lag, double handlers, or visual artifacts.
  </how-to-verify>
  <resume-signal>Type "approved" once the visual pass is clean; describe any animation/parallax/tile/state issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Title/select styling → readability | Low-contrast backdrop could hide text/tiles |
| Browser boot → asset coverage | New assets must not 404 when the boot navigates to styled scenes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-09 | Denial of Service | title-bg obscures text or tiles | low | mitigate | Use z(CONFIG.TITLE_BG_Z) well below UI z(9000); verify readability in browser boot |
| T-18-10 | Denial of Service | New select tile colors reduce locked/unlocked/cleared distinction | low | mitigate | Use the exact 18-UI-SPEC palette; human sign-off confirms distinction |
| T-18-11 | Information Disclosure | HTTP 404 on new assets leaks missing art to the browser console | low | mitigate | browser-boot.mjs already captures HTTP >= 400 responses |
</threat_model>

<verification>
- `node --check src/scenes/title.js` and `node --check src/scenes/select.js` pass.
- `bash scripts/check-import-safety.sh` passes.
- `bash scripts/check-safety.sh` passes.
- `bash scripts/check-gate.sh` passes.
- `node scripts/smoke-progress.mjs` passes.
- `node scripts/browser-boot.mjs` passes with no page/console/http errors.
- Human checkpoint is approved.
</verification>

<success_criteria>
- Title and select screens show the shared dark-grunge backdrop with no pink.
- Title wordmark and prompt remain readable; select tile states are distinct.
- The white active cursor is preserved on the select screen.
- All static gates pass and the browser boot navigates title→select→levels→title→select without errors (return to title injected via `page.evaluate`).
- Human visual sign-off confirms animation, parallax, tileset, and readability.
</success_criteria>

<output>
Create `.planning/phases/18-art-animation-parallax/18-04-SUMMARY.md` when done.
</output>
