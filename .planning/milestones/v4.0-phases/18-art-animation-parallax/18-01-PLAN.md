---
phase: 18-art-animation-parallax
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/player.png
  - assets/tiles/ground.png
  - assets/parallax/far.png
  - assets/parallax/mid.png
  - assets/parallax/near.png
  - assets/tiles/title-bg.png
  - src/config.js
  - src/main.js
  - scripts/generate-art-assets.py
autonomous: true
requirements:
  - ART-01
  - ART-02
  - ART-03
  - ART-04
must_haves:
  truths:
    - "All new sprite sheets match the exact frame dimensions in 18-UI-SPEC.md so slicing and colliders stay valid"
    - "Every new sprite is loaded in src/main.js after kaplay() with loadSprite, not loadSpriteSheet"
    - "Animation, tile-frame, and parallax tuning constants live in src/config.js and are importable by pure modules"
  artifacts:
    - path: assets/player.png
      provides: "80x32 player sheet, 5 frames of 16x32, dark silhouette"
    - path: assets/tiles/ground.png
      provides: "80x16 tile sheet, 5 frames of 16x16, dark-grunge"
    - path: assets/parallax/far.png
      provides: "640x120 tileable far silhouette strip"
    - path: assets/parallax/mid.png
      provides: "640x144 tileable mid-structure strip"
    - path: assets/parallax/near.png
      provides: "640x90 tileable near-grunge strip"
    - path: assets/tiles/title-bg.png
      provides: "640x360 dark-grunge title/select backdrop"
    - path: src/main.js
      provides: "loadSprite calls for player (sliceX+anims), ground (sliceX), parallax layers, title-bg"
    - path: src/config.js
      provides: "PLAYER_* and GROUND_* constants plus PARALLAX and TITLE_BG tuning"
  key_links:
    - from: assets/*.png
      to: src/main.js
      via: "loadSprite(...) paths use ../assets/... web-root convention"
    - from: src/config.js
      to: src/player.js
      via: "CONFIG.PLAYER_FRAMES / PLAYER_ANIM_DEADZONE / PLAYER_IDLE_SPEED / PLAYER_RUN_SPEED"
    - from: src/config.js
      to: src/levels/build.js
      via: "CONFIG.GROUND_FRAMES and CONFIG.TILE_SIZE"
    - from: src/config.js
      to: src/parallax.js
      via: "CONFIG.PARALLAX ratios / z / y-anchor"
---

<objective>
Create the Phase 18 pixel-art assets and the configuration/sprite-loading surface that the rest of the phase depends on. Replace the single-frame `player.png` and `ground.png` with the exact sheet layouts from 18-UI-SPEC.md, add the three parallax strips and the shared title/select backdrop, expose all tuning constants in `src/config.js`, and load every sprite in `src/main.js` after `kaplay()`.

Purpose: Art and constants must exist before the animation state machine, tile-frame selection, parallax wiring, or title/select styling can land.
Output: New PNG assets, updated `src/config.js` and `src/main.js`, and a helper script that can regenerate the placeholder art.
</objective>

<execution_context>
@GSD core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-art-animation-parallax/18-CONTEXT.md
@.planning/phases/18-art-animation-parallax/18-UI-SPEC.md
@src/config.js
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate placeholder pixel-art assets matching the UI-SPEC dimensions</name>
  <files>assets/player.png, assets/tiles/ground.png, assets/parallax/far.png, assets/parallax/mid.png, assets/parallax/near.png, assets/tiles/title-bg.png, scripts/generate-art-assets.py</files>
  <action>
    Create `scripts/generate-art-assets.py` and run it to produce the assets below. The script must use Python PIL (already installed) and must generate PNGs with the exact pixel dimensions and frame counts from 18-UI-SPEC.md. Colors must stay in the dark-grunge palette (#0a0a0a, #111111, #151515, #1a1a1a, #00ff88 accent only where specified, no pink).

    - `assets/player.png`: 80x32, one row of five 16x32 frames. Frames 0-1 idle, 2-3 run, 4 jump. Dark silhouette, feet aligned to frame bottom, no transparent padding that would shift the 16x32 collider footprint.
    - `assets/tiles/ground.png`: 80x16, one row of five 16x16 frames. Frames: 0 single-top, 1 left-edge, 2 center, 3 right-edge, 4 underside/column. Dark-grunge near-black with subtle edge highlights.
    - `assets/parallax/far.png`: 640x120 tileable horizontal strip. Faint mountain/city silhouette in #151515/#0f0f1a.
    - `assets/parallax/mid.png`: 640x144 tileable horizontal strip. Dark structural shapes in #111111/#1a1a20.
    - `assets/parallax/near.png`: 640x90 tileable horizontal strip. Subtle grunge texture in #0f0f0f/#141414.
    - `assets/tiles/title-bg.png`: 640x360 full-screen dark-grunge texture, very low contrast, no animation.

    The exact pixel-art shapes are at Claude's discretion provided the palette and dimensions are locked and the sheets read as silhouettes against the background. Keep the script in `scripts/` so it can be re-run if retuning is needed.
  </action>
  <verify>
    <automated>file assets/player.png assets/tiles/ground.png assets/parallax/far.png assets/parallax/mid.png assets/parallax/near.png assets/tiles/title-bg.png</automated>
  </verify>
  <done>
    All six PNGs exist and report the exact dimensions above (player 80x32, ground 80x16, far 640x120, mid 640x144, near 640x90, title-bg 640x360). `scripts/generate-art-assets.py` exists and exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add animation, tile, and parallax constants to src/config.js</name>
  <files>src/config.js</files>
  <action>
    Append the following visual-tuning blocks to `src/config.js` (no magic numbers in the modules that consume them):

    - Player animation constants:
      - `PLAYER_FRAMES: 5`
      - `PLAYER_ANIM_DEADZONE: 10` (px/s)
      - `PLAYER_IDLE_SPEED: 6` (fps)
      - `PLAYER_RUN_SPEED: 10` (fps)
      - `PLAYER_JUMP_SPEED: 0` (single-frame anim)
    - Ground tile constants:
      - `GROUND_FRAMES: 5`
    - Parallax constants:
      - `PARALLAX: { FAR_RATIO: 0.15, MID_RATIO: 0.45, NEAR_RATIO: 0.75, FAR_Z: -30, MID_Z: -20, NEAR_Z: -10, Y_ANCHOR: 320 }`
    - Title/select backdrop:
      - `TITLE_BG_Z: -100`

    Place them near the existing TILE_SIZE / FLOOR_Y block and the TITLE / SELECT blocks so related constants stay grouped. Do not change any movement, physics, HUD, or gate constants.
  </action>
  <verify>
    <automated>grep -E 'PLAYER_FRAMES|PLAYER_ANIM_DEADZONE|PLAYER_IDLE_SPEED|PLAYER_RUN_SPEED|PLAYER_JUMP_SPEED|GROUND_FRAMES|PARALLAX|TITLE_BG_Z' src/config.js</automated>
  </verify>
  <done>
    `src/config.js` contains all listed constants, parses cleanly with `node --check src/config.js`, and no existing constants were removed or re-typed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Load the new sprite sheets in src/main.js</name>
  <files>src/main.js</files>
  <action>
    Replace the existing single-frame `loadSprite("player", ...)` and `loadSprite("ground", ...)` calls with the sheet versions from 18-UI-SPEC.md, and add the parallax / title-bg loads. Use `loadSprite` only — `loadSpriteSheet` does not exist in Kaplay 3001 and must not be used.

    - `loadSprite("player", "../assets/player.png", { sliceX: CONFIG.PLAYER_FRAMES, anims: { idle: { from: 0, to: 1, loop: true, speed: CONFIG.PLAYER_IDLE_SPEED }, run: { from: 2, to: 3, loop: true, speed: CONFIG.PLAYER_RUN_SPEED }, jump: { from: 4, to: 4, loop: false, speed: CONFIG.PLAYER_JUMP_SPEED } } })`
    - `loadSprite("ground", "../assets/tiles/ground.png", { sliceX: CONFIG.GROUND_FRAMES })`
    - `loadSprite("bg-far", "../assets/parallax/far.png")`
    - `loadSprite("bg-mid", "../assets/parallax/mid.png")`
    - `loadSprite("bg-near", "../assets/parallax/near.png")`
    - `loadSprite("title-bg", "../assets/tiles/title-bg.png")`

    Keep the existing `coin`, `spike`, and `goal` loads unchanged. All loads must remain after `kaplay()` and before the `scene(...)` registrations.
  </action>
  <verify>
    <automated>grep -E 'loadSprite\("player"|loadSprite\("ground"|loadSprite\("bg-far"|loadSprite\("bg-mid"|loadSprite\("bg-near"|loadSprite\("title-bg"' src/main.js</automated>
  </verify>
  <done>
    `src/main.js` loads the player sheet with `sliceX` and `anims`, the ground sheet with `sliceX`, the three parallax layers, and the title backdrop. `node --check src/main.js` passes and `loadSpriteSheet` is not present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset dimensions → sprite slicing | Wrong sheet size silently misaligns frames and breaks colliders/animation |
| main.js load order | `loadSprite` before `kaplay()` would throw; after `kaplay()` only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Denial of Service | Player or ground sheet dimensions do not match UI-SPEC | medium | mitigate | Verify exact PNG dimensions with `file` before any module consumes them |
| T-18-02 | Denial of Service | `loadSpriteSheet` used instead of `loadSprite` | medium | mitigate | Explicitly forbid `loadSpriteSheet` in the plan; grep verify in main.js |
</threat_model>

<verification>
- `node --check src/config.js` and `node --check src/main.js` pass.
- `bash scripts/check-import-safety.sh` still passes (it does not yet reference the not-yet-created `src/parallax.js`).
- `file` confirms every generated PNG dimension matches the UI-SPEC.
- `grep` confirms the six `loadSprite` calls exist and `loadSpriteSheet` does not.
</verification>

<success_criteria>
- All six new PNG assets exist with the exact dimensions from 18-UI-SPEC.md.
- `src/config.js` exposes `PLAYER_*`, `GROUND_FRAMES`, `PARALLAX`, and `TITLE_BG_Z` constants.
- `src/main.js` loads the player sheet (with `sliceX` and named `anims`), ground sheet, parallax layers, and title backdrop after `kaplay()`.
- No `loadSpriteSheet` call is introduced.
</success_criteria>

<output>
Create `.planning/phases/18-art-animation-parallax/18-01-SUMMARY.md` when done.
</output>
