---
phase: 19-polish-consolidated-kid-uat
plan: 04
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
  - 19-03
files_modified:
  - src/config.js
  - src/levels/level-01.js
  - src/levels/level-02.js
  - src/levels/level-03.js
  - src/levels/level-04.js
  - src/parallax.js
  - src/player.js
  - src/scenes/title.js
  - src/scenes/select.js
  - .planning/phases/19-polish-consolidated-kid-uat/19-UAT.md
autonomous: true
requirements:
  - SAFE-04
  - SAFE-05
must_haves:
  truths:
    - "D-10: if a level feels too hard, tune checkpoint placement or gap geometry in the level descriptor only; do NOT add new mechanics or change RUN_SPEED/JUMP_FORCE/movement spine"
    - "D-11: if parallax feels busy, reduce ratios or layer opacity within existing CONFIG.PARALLAX ranges; do not add new layers or disable parallax entirely"
    - "Only small, low-risk polish fixes are applied — no new features, no new modules, no changes to player physics, brain algorithm, XP curve, or save format"
    - "Every fix stays within existing CONFIG ranges (parallax ratios 0.15/0.45/0.75, animation speeds idle ~6–8 fps / run ~10–12 fps, FX durations ≤ 400–500 ms)"
    - "Any soft-lock found during UAT is fixed and the full static suite plus browser boot are re-run green before final sign-off"
    - "19-UAT.md is updated with the final sign-off line after fixes are verified"
  artifacts: []
  key_links:
    - from: 19-UAT.md defect list
      to: src/config.js / src/levels/level-*.js / src/parallax.js / src/player.js
      via: "small polish fixes driven by audit/UAT findings"
---

<objective>
Triage findings from the static audits (19-01, 19-02) and the kid-UAT session (19-03), then apply only small, low-risk polish fixes that address them. Fixes must stay within the existing configuration ranges and must not add features or change core systems. After fixes, re-run the full static suite and browser boot, update `19-UAT.md` with the final sign-off, and close the v4.0 milestone.

Purpose: Phase 19 closes the milestone by converting audit/UAT observations into concrete, minimal improvements and confirming the game remains green.
Output: Any necessary polish edits, an updated `19-UAT.md` with final sign-off, and green verification evidence.
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-polish-consolidated-kid-uat/19-CONTEXT.md
@.planning/phases/19-polish-consolidated-kid-uat/19-UAT.md
@scripts/check-safety.sh
@scripts/check-import-safety.sh
@scripts/check-gate.sh
@scripts/browser-boot.mjs
@src/config.js
@src/parallax.js
@src/player.js
@src/levels/level-01.js
@src/levels/level-02.js
@src/levels/level-03.js
@src/levels/level-04.js
@src/scenes/title.js
@src/scenes/select.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage audit/UAT findings and apply low-risk polish fixes</name>
  <files>19-UAT.md, src/config.js, src/levels/level-01.js, src/levels/level-02.js, src/levels/level-03.js, src/levels/level-04.js, src/parallax.js, src/player.js, src/scenes/title.js, src/scenes/select.js</files>
  <read_first>
    - .planning/phases/19-polish-consolidated-kid-uat/19-UAT.md
    - src/config.js
  </read_first>
  <action>
    Read `19-UAT.md` and the findings from 19-01/19-02. For each finding, classify as:
    - **Blocking bug** (soft-lock, crash, runtime error, a727c13 violation, timer/punishment construct): fix immediately.
    - **Small polish** (animation timing, parallax ratio/opacity, checkpoint placement, minor level-geometry tuning): fix within existing CONFIG ranges.
    - **Non-blocking observation** (kid preference, deferred feature): document in 19-UAT.md and do not change code.

    Allowed fix types (per D-09 through D-12):
    - Tweak `CONFIG.PLAYER_IDLE_SPEED`, `CONFIG.PLAYER_RUN_SPEED`, or `CONFIG.PLAYER_ANIM_DEADZONE` within 18-CONTEXT ranges.
    - Tweak `CONFIG.PARALLAX.FAR_RATIO`, `MID_RATIO`, `NEAR_RATIO`, layer opacity, or `Y_ANCHOR` within established ranges.
    - Adjust checkpoint positions or minor gap/platform geometry in level descriptors.
    - Adjust tile-frame or backdrop usage in `src/scenes/title.js` / `src/scenes/select.js` for readability.

    Forbidden fix types:
    - New math mechanics, new levels, new art assets, new systems.
    - Changing `RUN_SPEED`, `JUMP_FORCE`, gravity, or the movement spine.
    - Changing the brain algorithm, XP curve, or save format.
    - Adding audio, timers, lives, scoring, stars, or game-over states.

    If no findings require code changes, record "no polish fixes applied" in the plan deviations and skip to Task 2.
  </action>
  <acceptance_criteria>
    - Every code change is one of the allowed fix types and stays within existing CONFIG ranges.
    - No new feature, module, asset, or core-system change is introduced.
    - All changes are documented in 19-UAT.md's defect/polish list with a "fixed" note.
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat</automated>
  </verify>
  <done>Audit/UAT findings triaged and small polish fixes applied; no new features introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the full static suite and browser boot</name>
  <files>scripts/check-safety.sh, scripts/check-import-safety.sh, scripts/check-gate.sh, scripts/browser-boot.mjs</files>
  <read_first>
    - scripts/check-safety.sh
    - scripts/check-import-safety.sh
    - scripts/check-gate.sh
    - scripts/browser-boot.mjs
  </read_first>
  <action>
    Run the full verification suite in order:
    1. `bash scripts/check-gate.sh`
    2. `bash scripts/check-import-safety.sh`
    3. `bash scripts/check-safety.sh`
    4. `node scripts/smoke-progress.mjs`
    5. `node scripts/browser-boot.mjs`

    If any step fails, fix the root cause and repeat. Do not proceed to final sign-off until all five steps pass.
  </action>
  <acceptance_criteria>
    - `bash scripts/check-gate.sh` exits 0 and prints "gate checks: PASS".
    - `bash scripts/check-import-safety.sh` exits 0 and prints "import-safety checks: PASS".
    - `bash scripts/check-safety.sh` exits 0 and prints "safety checks: PASS".
    - `node scripts/smoke-progress.mjs` exits 0 and prints "smoke-progress: PASS".
    - `node scripts/browser-boot.mjs` exits 0 and prints the browser-boot PASS message.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/smoke-progress.mjs && node scripts/browser-boot.mjs && echo OK</automated>
  </verify>
  <done>Full static suite and browser boot are green after any polish fixes.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize 19-UAT.md sign-off and update project state</name>
  <files>.planning/phases/19-polish-consolidated-kid-uat/19-UAT.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/19-polish-consolidated-kid-uat/19-UAT.md
    - .planning/STATE.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Update `19-UAT.md` with the final sign-off line: a "Signed off by:" field, the final verdict (PASS / CONDITIONAL / FAIL), and the date. If the verdict is PASS, add a concluding paragraph stating that Phase 19 / SAFE-04 / SAFE-05 are satisfied and the v4.0 milestone is ready to close. If CONDITIONAL, list the accepted minor issues and why they do not block sign-off.

    Update `.planning/REQUIREMENTS.md` to mark SAFE-04 and SAFE-05 as Complete in the traceability table. Update `.planning/STATE.md` to reflect Phase 19 status as Ready to execute (or completed if the workflow expects execution next).
  </action>
  <acceptance_criteria>
    - `19-UAT.md` contains a final sign-off line with date and verdict.
    - `.planning/REQUIREMENTS.md` traceability table shows SAFE-04 and SAFE-05 as Complete.
    - `.planning/STATE.md` reflects Phase 19 status correctly.
  </acceptance_criteria>
  <verify>
    <automated>grep -E 'Signed off by|SAFE-04|SAFE-05' .planning/phases/19-polish-consolidated-kid-uat/19-UAT.md .planning/REQUIREMENTS.md .planning/STATE.md</automated>
  </verify>
  <done>Final UAT sign-off recorded and project state updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT finding → code change | Only low-risk polish fixes are allowed; feature requests or core-system changes must be deferred. |
| Polish fix → regression | Every fix must be followed by the full static suite and browser boot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-04-01 | Tampering | A "polish fix" slips in a new feature or core-system change | medium | mitigate | Restrict fixes to CONFIG ranges and level-geometry/checkpoint tweaks; re-run full suite |
| T-19-04-02 | Denial of Service | Polish fix introduces a soft-lock | medium | mitigate | Mandatory browser boot re-run after any code change |
| T-19-04-03 | Repudiation | Final sign-off recorded before verification | low | mitigate | Task 3 runs only after Task 2 passes |
</threat_model>

<verification>
- Full static suite (`check-gate.sh`, `check-import-safety.sh`, `check-safety.sh`, `smoke-progress.mjs`) is green.
- `browser-boot.mjs` passes with zero runtime errors.
- `19-UAT.md` contains final sign-off.
- `.planning/REQUIREMENTS.md` and `.planning/STATE.md` are updated.
</verification>

<success_criteria>
- All audit/UAT findings are triaged; blocking issues and small polish items are resolved.
- No new features, modules, assets, or core-system changes are introduced.
- The full static suite and browser boot remain green.
- `19-UAT.md` is signed off and project state reflects Phase 19 completion.
</success_criteria>

<output>
Create `.planning/phases/19-polish-consolidated-kid-uat/19-04-SUMMARY.md` when done.
</output>
