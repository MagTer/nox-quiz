---
phase: 19-polish-consolidated-kid-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-import-safety.sh
  - scripts/browser-boot.mjs
autonomous: true
requirements:
  - SAFE-04
must_haves:
  truths:
    - "D-02: keep check-import-safety.sh on its existing scoped module list (title.js, select.js, parallax.js, ui/challenge.js, mechanics/door.js, mechanics/gates.js, mechanics/enemy.js, mechanics/collect.js) plus existence/syntax checks for game.js and main.js; do not expand to a whole-src negative scan"
    - "D-03: keep existing check-gate.sh assertions 10-13 as positive proof that each mechanic calls openChallenge; no dynamic loop needed"
    - "D-04: keep browser-boot.mjs as a navigation/load smoke (title -> select -> all four levels); mechanic forgiveness is verified by static audit and kid-UAT, not by scripted mechanic interaction"
    - "D-14: the final browser boot shows zero page errors and zero 4xx responses; non-blocking warnings are allowed if they do not affect gameplay"
    - "scripts/check-import-safety.sh verifies every Phase 13-18 pure module is a727c13-safe and prints PASS"
    - "scripts/check-gate.sh verifies all five thin callers reuse src/ui/challenge.js and prints PASS"
    - "scripts/browser-boot.mjs loads title -> select -> each of the four levels with no page errors, no console errors, and no HTTP 4xx/5xx responses"
  artifacts: []
  key_links: []
---

<objective>
Run the import-safety and structural-gate audits across all new pure modules, then perform the mandatory real-browser boot regression. Confirm the a727c13 contract holds for every module added in Phases 14–18 and that the assembled milestone boots and navigates through every level without runtime errors.

Purpose: Static gates are necessary but not sufficient for Phase 19. This plan produces the import-safety evidence and the runtime smoke test that SAFE-04 requires.
Output: Updated gate scripts (comment/header only unless a gap is found), a green `browser-boot.mjs` run, and documented evidence.
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-polish-consolidated-kid-uat/19-CONTEXT.md
@scripts/check-import-safety.sh
@scripts/check-gate.sh
@scripts/browser-boot.mjs
@scripts/smoke-progress.mjs
@src/scenes/title.js
@src/scenes/select.js
@src/parallax.js
@src/ui/challenge.js
@src/mechanics/door.js
@src/mechanics/gates.js
@src/mechanics/enemy.js
@src/mechanics/collect.js
@src/main.js
@src/scenes/game.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify import-safety coverage and run check-import-safety.sh</name>
  <files>scripts/check-import-safety.sh, src/scenes/title.js, src/scenes/select.js, src/parallax.js, src/ui/challenge.js, src/mechanics/door.js, src/mechanics/gates.js, src/mechanics/enemy.js, src/mechanics/collect.js</files>
  <read_first>
    - scripts/check-import-safety.sh
  </read_first>
  <action>
    Confirm that `scripts/check-import-safety.sh` already scopes its negative top-level-engine scan to all Phase 13–18 pure modules: `src/scenes/title.js`, `src/scenes/select.js`, `src/parallax.js`, `src/ui/challenge.js`, `src/mechanics/door.js`, `src/mechanics/gates.js`, `src/mechanics/enemy.js`, `src/mechanics/collect.js`. The existence/syntax gate already covers `src/scenes/game.js` and `src/main.js`. Add a header/comment note that Phase 19 verified this scope covers the new mechanics and parallax module. If any new pure module is missing from the negative scan, add it.

    Run `bash scripts/check-import-safety.sh`. If it fails, fix real a727c13 violations by routing to 19-04; document false-positives.
  </action>
  <acceptance_criteria>
    - `bash scripts/check-import-safety.sh` exits 0 and prints "import-safety checks: PASS".
    - The negative scan includes `src/scenes/title.js`, `src/scenes/select.js`, `src/parallax.js`, `src/ui/challenge.js`, `src/mechanics/door.js`, `src/mechanics/gates.js`, `src/mechanics/enemy.js`, `src/mechanics/collect.js`.
    - A comment in `scripts/check-import-safety.sh` notes Phase 19 verified coverage of mechanics and parallax.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-import-safety.sh</automated>
  </verify>
  <done>check-import-safety.sh covers all new pure modules and prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Run check-gate.sh to verify all mechanics reuse challenge.js</name>
  <files>scripts/check-gate.sh, src/ui/challenge.js, src/ui/mathGate.js, src/mechanics/door.js, src/mechanics/gates.js, src/mechanics/enemy.js, src/mechanics/collect.js</files>
  <read_first>
    - scripts/check-gate.sh
  </read_first>
  <action>
    Run `bash scripts/check-gate.sh`. This confirms `src/ui/challenge.js` exposes `openChallenge`, has one-way dependency, no DOM sink, no timer, proper teardown, and that `src/ui/mathGate.js`, `src/mechanics/door.js`, `src/mechanics/gates.js`, `src/mechanics/enemy.js`, and `src/mechanics/collect.js` are thin callers. If it fails, fix the root cause or route to 19-04.
  </action>
  <acceptance_criteria>
    - `bash scripts/check-gate.sh` exits 0 and prints "gate checks: PASS".
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-gate.sh</automated>
  </verify>
  <done>check-gate.sh prints PASS; all mechanic callers reuse the shared seam.</done>
</task>

<task type="auto">
  <name>Task 3: Run the real-browser boot regression</name>
  <files>scripts/browser-boot.mjs, src/index.html, src/main.js</files>
  <read_first>
    - scripts/browser-boot.mjs
  </read_first>
  <action>
    Run `node scripts/browser-boot.mjs` from the repo root. The script must serve `src/`, seed a save unlocking all four levels, navigate title → select → level-01 → select → level-02 → select → level-03 → select → level-04, then return to title and re-enter select. It already captures `pageerror`, `console.error`, and HTTP ≥ 400 responses.

    If the boot fails, capture the exact error and route to 19-04. If the boot passes but the navigation does not yet exercise the return-to-title leg, extend `scripts/browser-boot.mjs` to inject `go("title")` after the last level and re-run the title → select round-trip (already present from Phase 18; keep it).
  </action>
  <acceptance_criteria>
    - `node scripts/browser-boot.mjs` exits 0.
    - The script logs "Browser boot: PASS — title -> select -> all levels loaded with no runtime errors."
    - No page errors, console errors, or HTTP 4xx/5xx responses are captured.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/browser-boot.mjs</automated>
  </verify>
  <done>Browser boot passes with zero runtime errors and zero failed asset requests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Import-time safety → runtime safety | check-import-safety.sh guarantees no import-time engine global; browser boot guarantees the runtime scene graph loads cleanly. |
| Static gate → mechanic behavior | check-gate.sh guarantees structural reuse of challenge.js; actual wrong-answer forgiveness is verified by 19-03 kid-UAT. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-02-01 | Denial of Service | New pure module missing from import-safety scope | medium | mitigate | Verify scoped list includes parallax.js and all four mechanic modules |
| T-19-02-02 | Information Disclosure | Browser boot misses new asset 404s | low | mitigate | HTTP response listener already captures ≥ 400; fail boot on any captured error |
| T-19-02-03 | Denial of Service | Browser boot passes headless but fails on real GPU/browser | low | accept | Headless Chromium is the project’s standard smoke; human UAT in 19-03 covers real-browser feel |
</threat_model>

<verification>
- `bash scripts/check-import-safety.sh` exits 0 and prints "import-safety checks: PASS".
- `bash scripts/check-gate.sh` exits 0 and prints "gate checks: PASS".
- `node scripts/browser-boot.mjs` exits 0 and prints the PASS message with zero captured errors.
</verification>

<success_criteria>
- Import-safety and structural-gate audits pass for every new pure module and mechanic caller.
- The real-browser boot loads title → select → all four levels → title → select with no runtime errors and no 4xx/5xx responses.
</success_criteria>

<output>
Create `.planning/phases/19-polish-consolidated-kid-uat/19-02-SUMMARY.md` when done.
</output>
