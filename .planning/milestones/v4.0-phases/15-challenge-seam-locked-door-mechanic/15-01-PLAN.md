---
phase: 15-challenge-seam-locked-door-mechanic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-gate.sh
  - scripts/check-import-safety.sh
autonomous: true
requirements: [MECH-01, MECH-02]
must_haves:
  truths:
    - "scripts/check-gate.sh exists at the live repo-root path (not just archived) and its TARGET is src/ui/challenge.js, not the old src/ui/mathGate.js"
    - "scripts/check-gate.sh asserts BOTH src/ui/mathGate.js and src/mechanics/door.js call openChallenge (thin-caller proof for both consumers of the shared seam, per CONTEXT-locked decision)"
    - "scripts/check-gate.sh strips // comments before every grep (positive and negative) so a check verifies real code, never explanatory prose"
    - "scripts/check-import-safety.sh's Section 0 existence/syntax loop AND Section 2 negative a727c13 loop both include src/ui/challenge.js and src/mechanics/door.js"
  artifacts:
    - path: "scripts/check-gate.sh"
      provides: "restored + repointed structural firewall gate for the shared challenge seam (10 assertions: 8 re-pointed + 2 new thin-caller checks)"
      contains: "export function openChallenge"
    - path: "scripts/check-import-safety.sh"
      provides: "a727c13 existence + negative-grep gate extended to cover challenge.js/door.js"
      contains: "src/mechanics/door.js"
  key_links:
    - from: "scripts/check-gate.sh"
      to: "src/ui/challenge.js"
      via: "TARGET path + export-name assertion"
      pattern: "export function openChallenge"
    - from: "scripts/check-gate.sh"
      to: "src/ui/mathGate.js and src/mechanics/door.js"
      via: "two new thin-caller assertions (9/10)"
      pattern: "openChallenge"
---

<objective>
Restore and re-point the archived Phase 10 structural firewall (`scripts/check-gate.sh`)
at the shared `src/ui/challenge.js` seam this phase creates, and extend
`scripts/check-import-safety.sh`'s scanned-file lists to cover the two new modules
(`src/ui/challenge.js`, `src/mechanics/door.js`) so the a727c13 module-top-level trap
actually watches them.

This is pure tooling — no `src/` file is touched. Both scripts are EXPECTED to fail
loudly right now (the modules they check for don't exist until 15-02/15-03 land) — that
is the real red state proving these are live gates, not no-ops, matching this project's
established "expected red until X lands" convention (check-progress.sh, check-safety.sh).

Purpose: land the verification infrastructure alongside — not after — the modules it
guards, so 15-02/15-03/15-04 have a real gate to turn green against.
Output: `scripts/check-gate.sh` (restored, repointed, comment-stripped, 10 assertions);
`scripts/check-import-safety.sh` (Section 0 + Section 2 loops extended by two files).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-challenge-seam-locked-door-mechanic/15-CONTEXT.md
@.planning/phases/15-challenge-seam-locked-door-mechanic/15-RESEARCH.md
@.planning/phases/15-challenge-seam-locked-door-mechanic/15-PATTERNS.md

# The archived original to restore-and-edit (RESEARCH Pitfall 2 — this script does NOT
# exist at scripts/check-gate.sh in the working tree today; it must be CREATED there,
# not edited in place):
@.planning/milestones/v3.0-phases/10-math-gate-integration-port-the-brain/scripts/check-gate.sh

# The live gate to extend (Section 0 loop ~lines 75-77, Section 2 loop ~line 116):
@scripts/check-import-safety.sh

# Sibling check-*.sh scripts for the strip_comments convention this restore adopts:
@scripts/check-safety.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore scripts/check-gate.sh, re-point at challenge.js, add thin-caller assertions</name>
  <files>scripts/check-gate.sh</files>
  <read_first>
    - .planning/milestones/v3.0-phases/10-math-gate-integration-port-the-brain/scripts/check-gate.sh (the full 8-assertion archived original — TARGET, existence, syntax, public-API grep, bridge-wiring greps, camera-immune grep, leak-safe-close greps, three negative greps)
    - scripts/check-safety.sh lines 40-43 (the `strip_comments` idiom to adopt — the archived original has NO comment-stripping, a gap this restore fixes)
    - .planning/phases/15-challenge-seam-locked-door-mechanic/15-RESEARCH.md "Pitfall 2" and "Code Examples" sections (why this is a CREATE at a new path, not an edit)
  </read_first>
  <action>
    Create `scripts/check-gate.sh` fresh at the live repo-root path (there is nothing to
    edit there today — RESEARCH Pitfall 2). Base its structure on the archived original
    but apply these deltas:

    1. Set `TARGET="$ROOT/src/ui/challenge.js"` (was `src/ui/mathGate.js`).
    2. Add a `strip_comments() { sed -E 's://.*$::' "$1"; }` helper (mirrors
       `scripts/check-safety.sh`'s idiom) and route EVERY grep — positive and negative —
       through it. The archived original has no comment-stripping at all; without it, a
       positive assertion can accidentally pass on an explanatory comment mentioning the
       same token instead of the real code construct, which is exactly the ambiguity this
       restore must close.
    3. Assertion 2 (public API) becomes: `strip_comments "$TARGET" | grep -q 'export function openChallenge'` (was `openMathGate`).
    4. Assertions 3/4 (bridge wiring `brain.nextQuestion`/`brain.reportResult`, camera-immune
       `fixed(`) stay structurally the same, just routed through `strip_comments` and
       pointed at the new `$TARGET`.
    5. Assertion 5 (leak-safe close) needs a real fix, not a cosmetic re-point: the
       archived grep checks for the literal substring `destroy(`, but `challenge.js`'s
       close() (per 15-PATTERNS.md) tears down via `destroyAll("challenge")` — the
       characters immediately after "destroy" in "destroyAll(" are "A", not "(", so the
       literal substring `destroy(` does NOT match `destroyAll(`. Change this assertion to
       accept EITHER form: `strip_comments "$TARGET" | grep -Eq 'destroy\(|destroyAll\('`.
       Keep the `cancel()` half of assertion 5 unchanged (still routed through strip_comments).
    6. Assertions 6/7/8 (no DOM sink, no timer/scheduler, no scene import) stay the same
       patterns, routed through `strip_comments` and pointed at the new `$TARGET`.
    7. Append TWO NEW assertions (9 and 10), per CONTEXT's locked decision that mathGate.js
       and door.js are each "sanity-checked as thin callers": assertion 9 checks
       `$ROOT/src/ui/mathGate.js` exists and, comment-stripped, contains `openChallenge`
       (fails with a message naming mathGate.js if not — "must be a thin wrapper over the
       shared seam"). Assertion 10 does the identical check against
       `$ROOT/src/mechanics/door.js` (fails naming door.js — "the door mechanic must reuse
       the shared seam"). Both use the existing `fail()` helper and the SAME
       `strip_comments` routing as every other assertion.
    8. Update the header comment block: explain this was restored from the Phase 10
       archive (cite the exact archived path and commit `4e02732`), note the strip_comments
       addition and why, and state plainly this is EXPECTED RED right now (challenge.js/
       mathGate.js-as-thin-wrapper/door.js don't exist yet) — matching the project's
       "expected red until X lands" convention used in check-progress.sh/check-safety.sh.
       Update the trailing "Run from the repo root" docstring path to `scripts/check-gate.sh`
       (the archived docstring pointed at the old `.planning/...` path).
    9. Print `"gate checks: PASS"` on full success (unchanged from the archived original).
  </action>
  <acceptance_criteria>
    - `bash -n scripts/check-gate.sh` passes (valid bash syntax).
    - `grep -q 'export function openChallenge' scripts/check-gate.sh` (assertion 2 re-pointed).
    - `grep -q 'src/ui/challenge.js' scripts/check-gate.sh` (TARGET re-pointed).
    - `grep -q 'src/ui/mathGate.js' scripts/check-gate.sh` AND `grep -q 'src/mechanics/door.js' scripts/check-gate.sh` (both new thin-caller assertions present).
    - `grep -q 'strip_comments' scripts/check-gate.sh` (comment-stripping added).
    - `grep -q 'destroyAll' scripts/check-gate.sh` (assertion 5's fixed pattern accepts the real teardown idiom).
    - Running the script right now correctly reports the missing target, proving it is live and re-pointed, not a stale no-op: `bash scripts/check-gate.sh` exits non-zero with a message citing `src/ui/challenge.js`.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/check-gate.sh && grep -q 'export function openChallenge' scripts/check-gate.sh && grep -q 'src/ui/challenge.js' scripts/check-gate.sh && grep -q 'src/ui/mathGate.js' scripts/check-gate.sh && grep -q 'src/mechanics/door.js' scripts/check-gate.sh && grep -q 'strip_comments' scripts/check-gate.sh && grep -q 'destroyAll' scripts/check-gate.sh && (bash scripts/check-gate.sh 2>&1 | grep -q 'src/ui/challenge.js') && echo OK</automated>
  </verify>
  <done>scripts/check-gate.sh exists at the live path, targets src/ui/challenge.js, comment-strips every assertion, fixes the destroy(/destroyAll( ambiguity, and asserts mathGate.js + door.js are both thin openChallenge callers; it currently fails loudly citing the not-yet-created challenge.js (expected red).</done>
</task>

<task type="auto">
  <name>Task 2: Extend check-import-safety.sh's scanned-file lists to cover challenge.js + door.js</name>
  <files>scripts/check-import-safety.sh</files>
  <read_first>
    - scripts/check-import-safety.sh full file (already read — Section 0 existence/syntax loop at the `for f in ...` block listing title.js/select.js/game.js/main.js; Section 2 negative a727c13 loop at the `for f in "src/scenes/title.js" "src/scenes/select.js"` block; the header comment explaining the scoping rationale)
    - .planning/phases/15-challenge-seam-locked-door-mechanic/15-RESEARCH.md "Pitfall 3" and "Assumption A3" (why extending BOTH loops, not just documenting a manual-review fallback, is the stronger choice)
  </read_first>
  <action>
    Two edits to `scripts/check-import-safety.sh`, both additive (append to existing
    lists, do not remove or reorder the current entries):

    1. Section 0 (existence + syntax gate): append `"src/ui/challenge.js"` and
       `"src/mechanics/door.js"` to the end of the existing `for f in ...` file list
       (currently title.js/select.js/game.js/main.js). This makes Section 0 fail right
       now with a clear "missing module" message the moment it reaches the new entries —
       expected red until 15-02 (challenge.js) and 15-03 (door.js) land.
    2. Section 2 (negative a727c13 module-top-level scan): append the SAME two files to
       the end of that loop's list (currently `"src/scenes/title.js" "src/scenes/select.js"`).
       Both new modules already follow the a727c13 discipline per CONTEXT ("same
       discipline as every other Phase 13/14 module"), so once they exist this scan
       should go straight to green, not stay red — it is a real regression check, not a
       new source of friction.
    3. Update the header comment (the paragraph describing what Section 2 is "SCOPED to")
       to note the scope now also covers the Phase 15 challenge/door modules, so a future
       reader does not assume the scope is still frozen at title.js/select.js only.

    Do not touch the `ENGINE_GLOBALS` alternation, the `TOPLEVEL_TRAP` pattern, or the
    calibration self-test (Section 1b) — none of those need changes for this extension.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/check-import-safety.sh` passes.
    - Section 0 list now includes both new files: `grep -c 'src/ui/challenge.js' scripts/check-import-safety.sh` is `2` (Section 0 + Section 2 mentions) and `grep -c 'src/mechanics/door.js' scripts/check-import-safety.sh` is `2`.
    - Running the script right now correctly reports the missing module, proving the extension is live: `bash scripts/check-import-safety.sh` exits non-zero citing `src/ui/challenge.js`.
    - The existing four files remain in Section 0's list (nothing removed): `grep -q 'src/scenes/title.js' scripts/check-import-safety.sh` AND `grep -q 'src/scenes/game.js' scripts/check-import-safety.sh` AND `grep -q 'src/main.js' scripts/check-import-safety.sh`.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/check-import-safety.sh && test "$(grep -c 'src/ui/challenge.js' scripts/check-import-safety.sh)" = "2" && test "$(grep -c 'src/mechanics/door.js' scripts/check-import-safety.sh)" = "2" && grep -q 'src/scenes/title.js' scripts/check-import-safety.sh && grep -q 'src/scenes/game.js' scripts/check-import-safety.sh && (bash scripts/check-import-safety.sh 2>&1 | grep -q 'src/ui/challenge.js') && echo OK</automated>
  </verify>
  <done>check-import-safety.sh's Section 0 (existence/syntax) and Section 2 (negative a727c13 scan) loops both include src/ui/challenge.js and src/mechanics/door.js, appended after the existing four/two entries; it currently fails loudly citing the not-yet-created challenge.js (expected red).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-invoked static gates (not shipped runtime) | `check-gate.sh` / `check-import-safety.sh` run at commit/verification time only; they never ship to the browser and have no player-facing input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01-01 | Tampering | scripts/check-gate.sh assertions silently pass on prose instead of code | medium | mitigate | Task 1 adds `strip_comments` to every assertion (positive and negative) so a match can only come from real code, never an explanatory comment mentioning the same token (closes the exact `destroy(` vs `destroyAll(` / comment-prose ambiguity found during planning). |
| T-15-01-02 | Repudiation | a loosened or silently-broken gate regex would let an a727c13 or firewall regression through unnoticed | medium | mitigate | check-import-safety.sh already has a two-sided calibration self-test (Section 1b, RED on the bad-scene fixture / GREEN on game.js) that this plan's extension does not touch or weaken; check-gate.sh's own assertions are exercised for real (non-mocked) against the live tree in every later wave of this phase. |
| T-15-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan — pure bash/text edits to existing tooling files; zero new runtime dependencies (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- Per task: `bash -n` syntax check + the grep batteries in each task's `<verify>`.
- After this plan (wave merge): both scripts correctly report their EXPECTED-RED failures (missing `src/ui/challenge.js`) — this is the proof they are live gates, not stale copies. They only turn green once 15-02/15-03/15-04 land.
</verification>

<success_criteria>
- `scripts/check-gate.sh` exists at the repo-root live path, targets `src/ui/challenge.js`, comment-strips every assertion, and asserts both `mathGate.js` and `door.js` call `openChallenge`.
- `scripts/check-import-safety.sh`'s existence and negative-scan loops both cover `src/ui/challenge.js` and `src/mechanics/door.js`.
- Both scripts fail right now with a message citing the not-yet-created `src/ui/challenge.js` — proving the extension is real, not a no-op.
</success_criteria>

<output>
Create `.planning/phases/15-challenge-seam-locked-door-mechanic/15-01-SUMMARY.md` when done.
</output>