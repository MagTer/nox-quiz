---
phase: 15-challenge-seam-locked-door-mechanic
plan: 03
type: execute
wave: 2
depends_on: ["15-02"]
files_modified:
  - src/config.js
  - src/levels/level-01.js
  - scripts/smoke-progress.mjs
  - src/levels/build.js
  - src/mechanics/door.js
autonomous: true
requirements: [MECH-02]
must_haves:
  truths:
    - "CONFIG.DOOR exists with W/H/LOCKED_GREY/LOCKED_BORDER/GLYPH_SIZE, mirroring src/scenes/select.js's LOCKED_GREY/LOCKED_BORDER RGB triples verbatim (CONTEXT-locked visual-language match)"
    - "level-01.js's geometry carries exactly one door descriptor, positioned on the final floor run next to the x:1520 spike (mid-level, hazard-adjacent per success criterion #3)"
    - "scripts/smoke-progress.mjs's LVL-02 regression fixture is updated in lockstep with the new doors field, so `node scripts/smoke-progress.mjs` still exits 0 (a real regression this task must prevent, not optional cleanup)"
    - "buildLevel(level) instantiates a SOLID blocking door collider (body({isStatic:true}), tag 'door') plus a separate lock-glyph text entity for every entry in geometry.doors, with the glyph handle stashed on the door object so it can be cleaned up alongside the door"
    - "src/mechanics/door.js exports wireDoor({player, brain}); on a correct answer it destroys the door AND its glyph BEFORE unpausing the player (destroy-before-unpause ordering), and it ALWAYS unpauses — no soft-lock path exists"
  artifacts:
    - path: "src/config.js"
      provides: "CONFIG.DOOR tuning block (W, H, LOCKED_GREY, LOCKED_BORDER, GLYPH_SIZE)"
      contains: "DOOR:"
    - path: "src/levels/level-01.js"
      provides: "one authored door descriptor, mid-level next to a hazard"
      contains: "doors:"
    - path: "scripts/smoke-progress.mjs"
      provides: "LVL-02 regression fixture kept in sync with the new doors geometry field"
      contains: "doors:"
    - path: "src/levels/build.js"
      provides: "doors array consumer — solid collider + lock-glyph entity creation"
      contains: "g.doors"
    - path: "src/mechanics/door.js"
      provides: "the door collision mechanic (freeze -> challenge -> destroy -> unfreeze)"
      exports: ["wireDoor"]
      contains: "player.paused = false"
  key_links:
    - from: "src/levels/level-01.js"
      to: "src/levels/build.js"
      via: "geometry.doors array consumed by buildLevel's new doors loop"
      pattern: "g.doors"
    - from: "src/mechanics/door.js"
      to: "src/ui/challenge.js"
      via: "wireDoor's onCollide handler calls openChallenge directly — one-way, never through mathGate.js"
      pattern: "openChallenge"
---

<objective>
Build the locked-door entity and its collision mechanic: a `CONFIG.DOOR` tuning block, one
authored door in level-01's geometry (mid-level, next to the existing x:1520 spike), a
`buildLevel()` consumer that instantiates a SOLID blocking collider + lock glyph from it,
and `src/mechanics/door.js` — the new `mechanics/` directory's first module — which wires
the door onto the player via the shared `challenge.js` seam (created in 15-02) with the
freeze-then-challenge-then-destroy-then-UNFREEZE sequence a mid-level mechanic requires
(unlike the end-of-level goal, which never needs to unfreeze because the scene transitions
away instead).

This plan does NOT touch `src/scenes/game.js` — the actual `wireDoor(...)` call site is
15-04's job, kept separate so this plan's own verification stays static/structural.

Purpose: prove the shared challenge seam generalizes to a second, mid-level caller.
Output: `CONFIG.DOOR` (config.js); one `doors` entry (level-01.js) mirrored into
`scripts/smoke-progress.mjs`'s regression fixture; a `doors` consumer in `build.js`;
`src/mechanics/door.js` (new, exports `wireDoor`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-challenge-seam-locked-door-mechanic/15-CONTEXT.md
@.planning/phases/15-challenge-seam-locked-door-mechanic/15-RESEARCH.md
@.planning/phases/15-challenge-seam-locked-door-mechanic/15-PATTERNS.md

# The shared seam this plan's door.js consumes (created in 15-02 — read to confirm the
# exact openChallenge({brain, onSuccess, prompt}) call shape):
@src/ui/challenge.js

# The precedent every piece of this plan mirrors:
@src/config.js
@src/levels/level-01.js
@src/levels/build.js
@src/scenes/select.js
@src/fx.js

# The freeze/bridge-call precedent to ADAPT (not copy verbatim — it never unfreezes,
# because the goal transitions scenes; the door must not repeat that omission):
@src/scenes/game.js

# CRITICAL companion fixture — the LVL-02 regression deep-equal this plan's level-01.js
# edit will break unless updated in the SAME task (lines 270-330, the expectedGeometry
# literal + the key-length-equality deep-equal helper):
@scripts/smoke-progress.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONFIG.DOOR + level-01 doors array + matching smoke-progress.mjs fixture</name>
  <files>src/config.js, src/levels/level-01.js, scripts/smoke-progress.mjs</files>
  <read_first>
    - src/config.js CONFIG.GATE block (lines 60-64) and CONFIG.SELECT block (lines 156-165) — the two precedents for a new grouped tuning block
    - src/scenes/select.js lines 39,43 — the EXACT LOCKED_GREY = [0x44,0x44,0x44] / LOCKED_BORDER = [0x55,0x55,0x55] triples CONFIG.DOOR must mirror verbatim (CONTEXT-locked visual-language match)
    - src/levels/level-01.js spikes array (lines 76-80) and checkpoints array (lines 94-99) — the data-literal idiom the new doors array follows
    - scripts/smoke-progress.mjs lines 270-330 — READ IN FULL. This is the LVL-02 regression: a hand-recursive deepEqual compares getLevel("level-01").geometry against a hardcoded expectedGeometry literal, and the deepEqual implementation checks Object.keys(a).length !== Object.keys(b).length FIRST. The moment level-01.js's geometry gains a `doors` key, this fixture MUST gain the identical key or the key-count check fails and the smoke regresses — this is a REAL bug this task prevents, not optional polish, and it is not called out anywhere in 15-RESEARCH.md/15-PATTERNS.md (found by direct inspection of this file during planning).
  </read_first>
  <action>
    Three edits, all data/config-only (no engine calls, safe at module top level in
    level-01.js and config.js per their existing a727c13-pure-data discipline):

    1. In `src/config.js`, add a `DOOR` block to `CONFIG`, grouped near `CONFIG.GATE`/
       `CONFIG.SELECT` (same file, same "one tunable block per feature" style): `W: 32`
       (px — door footprint width), `H: 64` (px — door footprint height, 2x the 32px
       player sprite height so it can never be jumped over), `LOCKED_GREY: [0x44, 0x44, 0x44]`
       (mirrored verbatim from select.js's `LOCKED_GREY`), `LOCKED_BORDER: [0x55, 0x55, 0x55]`
       (mirrored verbatim from select.js's `LOCKED_BORDER`), `GLYPH_SIZE: 22` (mirrors
       `CONFIG.SELECT.GLYPH_SIZE`'s precedent).
    2. In `src/levels/level-01.js`, add a `doors` array to `LEVEL_01.geometry`, positioned
       after the `checkpoints` array (same file, same data-literal idiom as
       `spikes`/`checkpoints`): exactly one entry,
       `{ x: 1480, y: FLOOR_Y - CONFIG.DOOR.H }`. This sits on the final floor run
       (1360-2240) — 40px before the existing spike at x:1520, satisfying "mid-level, next
       to a hazard" (success criterion #3) unambiguously. Do not touch any other array in
       this file.
    3. In `scripts/smoke-progress.mjs`, inside the "LVL-02 regression" block's
       `expectedGeometry` object literal, add the SAME `doors: [{ x: 1480, y: FLOOR_Y - CONFIG.DOOR.H }]`
       entry (this file already imports `CONFIG` at its top and already computes
       `FLOOR_Y = CONFIG.FLOOR_Y` locally inside that block — reuse it, do not
       redeclare). Without this companion edit, `node scripts/smoke-progress.mjs` starts
       failing the instant level-01.js's geometry gains a `doors` key the fixture lacks.
  </action>
  <acceptance_criteria>
    - `node --check src/config.js` AND `node --check src/levels/level-01.js` AND `node --check scripts/smoke-progress.mjs` all pass.
    - `grep -q 'DOOR:' src/config.js` AND `grep -q 'LOCKED_GREY: \[0x44, 0x44, 0x44\]' src/config.js` (mirrors select.js verbatim).
    - `grep -q 'doors:' src/levels/level-01.js` AND `grep -q '{ x: 1480' src/levels/level-01.js`.
    - `grep -q 'doors:' scripts/smoke-progress.mjs` (companion fixture updated).
    - `node scripts/smoke-progress.mjs` exits `0` — the LVL-02 regression (and every other smoke assertion) still passes with the new field present on BOTH sides of the deep-equal.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/config.js && node --check src/levels/level-01.js && node --check scripts/smoke-progress.mjs && grep -q 'DOOR:' src/config.js && grep -q 'LOCKED_GREY: \[0x44, 0x44, 0x44\]' src/config.js && grep -q 'doors:' src/levels/level-01.js && grep -q 'doors:' scripts/smoke-progress.mjs && node scripts/smoke-progress.mjs && echo OK</automated>
  </verify>
  <done>CONFIG.DOOR exists with the four tuning constants mirroring select.js's locked-tile palette; level-01.js's geometry carries exactly one door entry positioned near the x:1520 spike; scripts/smoke-progress.mjs's expectedGeometry fixture is updated in lockstep so node scripts/smoke-progress.mjs still exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: build.js doors consumer — solid blocking door entity + lock glyph</name>
  <files>src/levels/build.js</files>
  <read_first>
    - src/levels/build.js FULL FILE (already read — the merged-floor `body({isStatic:true})` idiom lines 54-64, the trigger-only spike/coin/goal idiom lines 87-113, the module-level `Rect`-guard-inside-body discipline lines 39-50)
    - src/fx.js lines 65,69-79 — the `obj._fxScaleTween = tween(...)` custom-property-handle-stash idiom this task reuses for `door.glyphObj` (the precedent for "stash a reference on the entity so a later step can clean up a second, separately-created object")
    - CONFIG.DOOR (added in Task 1 of this plan)
  </read_first>
  <action>
    Add a doors loop to `buildLevel(levelData)` in `src/levels/build.js`, placed after the
    existing goal creation (end of the function body), guarded with `g.doors ?? []`
    (mirrors the optional-slot defensive style level-01.js already uses for
    `mechanics`/`theme`/`parallax` — keeps `buildLevel` backward-compatible with any level
    descriptor that omits doors).

    For each door descriptor `d` in `g.doors ?? []`, create TWO entities:

    1. The SOLID blocking collider — `rect(CONFIG.DOOR.W, CONFIG.DOOR.H)`,
       `pos(d.x, d.y)`, `color(...CONFIG.DOOR.LOCKED_GREY)`,
       `outline(2, rgb(...CONFIG.DOOR.LOCKED_BORDER))`, `area()`,
       `body({ isStatic: true })`, tag `"door"`. This is a BLOCKING collider (unlike the
       trigger-only spike/coin/goal idiom above it in this same file) — CONTEXT's locked
       decision is "the player physically cannot pass" while locked, matching the same
       `body({isStatic:true})` shape as the merged-floor runs, just for a single small
       entity rather than a run (the merged-floor anti-seam-stick pattern does not apply
       here — there is only one door this phase, not a contiguous run of them).
    2. A SEPARATE lock-glyph text entity — `text("X", { size: CONFIG.DOOR.GLYPH_SIZE })`,
       `anchor("center")`, `pos(d.x + CONFIG.DOOR.W / 2, d.y + CONFIG.DOOR.H / 2)`, tag
       `"door-glyph"` — deliberately no `area()` (purely visual, must never participate in
       collision). Capture the `add([...])` return value from step 1 as `door` and from
       this step as `glyph`, then stash the reference: `door.glyphObj = glyph;` — a
       custom-property handle on the door entity, the SAME stash-a-handle-for-later-cleanup
       idiom `fx.js` already uses for `player._fxScaleTween`. This lets
       `src/mechanics/door.js`'s success handler (Task 3) destroy both the collider and its
       glyph without a tag-wide `destroyAll("door")` sweep that would (in a future
       multi-door level) wrongly remove every door instead of just the one just opened.
  </action>
  <acceptance_criteria>
    - `node --check src/levels/build.js` passes.
    - `grep -q 'g.doors' src/levels/build.js` (the optional-slot guard is present).
    - `grep -q '"door"' src/levels/build.js` AND `grep -q '"door-glyph"' src/levels/build.js` (both tags present, distinct).
    - `grep -q 'glyphObj' src/levels/build.js` (the stash-a-handle idiom is present).
    - The door collider is BLOCKING, not trigger-only: `grep -c 'body({ isStatic: true })' src/levels/build.js` is `3` (the existing floor + platform occurrences plus this new door occurrence).
    - `bash scripts/check-safety.sh` still passes (no timer/punishment construct introduced).
  </acceptance_criteria>
  <verify>
    <automated>node --check src/levels/build.js && grep -q 'g.doors' src/levels/build.js && grep -q '"door"' src/levels/build.js && grep -q '"door-glyph"' src/levels/build.js && grep -q 'glyphObj' src/levels/build.js && test "$(grep -c 'body({ isStatic: true })' src/levels/build.js)" = "3" && bash scripts/check-safety.sh && echo OK</automated>
  </verify>
  <done>buildLevel(level) creates one solid blocking door collider (tag "door", body({isStatic:true})) plus a separate lock-glyph text entity (tag "door-glyph", no area()) for every entry in level.geometry.doors, with the glyph reference stashed on the door object as glyphObj for later cleanup; g.doors ?? [] keeps buildLevel backward-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: src/mechanics/door.js — the collision-triggered door mechanic</name>
  <files>src/mechanics/door.js</files>
  <read_first>
    - src/scenes/game.js `onReachGoal()` (lines 159-223, already read) — the freeze-then-
      bridge-call SHAPE to adapt: `player.vel = vec2(0);` then `player.paused = true;`
      then a single `openMathGate`/`openChallenge` call. Note precisely what it does NOT
      do: it never sets `player.paused = false`, because the level transitions away via
      `go("select")` right after. A door does NOT transition scenes — copying this
      pattern without adding the unpause is the single highest-risk defect in this whole
      phase (soft-locks the player after the first correctly-answered door).
    - src/scenes/game.js `player.onCollide("checkpoint", (c) => { lastCheckpoint = c.pos.clone(); })`
      (lines 114-116) — the "collided-object-as-callback-argument" idiom this task's
      `player.onCollide("door", (doorObj) => ...)` follows, which generalizes correctly to
      however many "door"-tagged entities exist without needing a per-door wireDoor call.
    - src/fx.js `clearBurst()` (lines 172-210, already read) — called as-is, no new fx
      function this phase (CONTEXT-locked).
    - src/ui/challenge.js (created in 15-02) — confirm the exact `openChallenge({brain, onSuccess})` call shape this task bridges into.
  </read_first>
  <behavior>
    - Touching a "door"-tagged entity for the first time freezes the player
      (`vel = vec2(0)`, `paused = true`) and opens the shared challenge overlay with the
      scene's brain.
    - A wrong answer inside the overlay leaves the player frozen and the door locked —
      challenge.js's own forgiving retry loop handles this; door.js does nothing extra.
    - A correct answer: destroys the door's collider AND its stashed glyph, plays
      `fx.clearBurst()`, marks that specific door object as opened (so a stray re-trigger
      of the SAME touch session is a no-op), and — critically — sets `player.paused = false`
      so gameplay resumes in the same scene.
    - Destroy MUST happen before the unpause line (same synchronous callback, in that
      order) — this is the second-line-of-defense against a same-frame re-collision, on
      top of the `opened` fire-once guard.
    - Touching a DIFFERENT "door"-tagged entity (a future multi-door level) triggers the
      SAME sequence independently, keyed by the touched object's own identity — a single
      `wireDoor({player, brain})` call handles every door without per-door registration.
  </behavior>
  <action>
    Create `src/mechanics/door.js` — the first module in the new `src/mechanics/`
    directory (CONTEXT-locked: Phase 16's defeat-enemy/multiple-gates/collect-the-answer
    mechanics reuse this directory pattern).

    Header comment: state the one-way dependency (`door.js` imports `../ui/challenge.js`
    DIRECTLY, never `../ui/mathGate.js`, which stays end-of-level-specific — CONTEXT-locked),
    the engine-global discipline (`vec2`/`destroy` referenced only inside the exported
    function body — mechanics/ is one directory below src/, sibling imports use `../`),
    and the CRITICAL delta from `onReachGoal()` (must unfreeze; must destroy before
    unfreeze) called out explicitly so a future reader never "simplifies" it back to the
    goal's pattern.

    Imports: `import { openChallenge } from "../ui/challenge.js";` and
    `import * as fx from "../fx.js";`.

    Export `function wireDoor({ player, brain })`:
    - `const opened = new Set();` — closure-local fire-once latch keyed per touched door
      object (never module-level — anti-leak), a belt-and-braces guard on top of Kaplay's
      own onCollide per-touch-session cache.
    - `player.onCollide("door", (doorObj) => { ... })` — registered ONCE inside `wireDoor`,
      works for every "door"-tagged entity via the collided-object callback argument
      (mirrors the checkpoint handler idiom cited above; this is a deliberate, better
      generalization than a per-door `wireDoor(doorObj, ...)` call, since Kaplay's
      `onCollide` already hands back the specific touched object).
    - Inside the handler: `if (opened.has(doorObj)) return;` guard first. Then
      `player.vel = vec2(0);` then `player.paused = true;` (exact same ordering as
      `onReachGoal()`).
    - Call `openChallenge({ brain, onSuccess() { ... } })` — no `prompt` override (the
      default `${q.a} × ${q.b}` display is used for the door, same as the end-of-level
      gate).
    - Inside `onSuccess()`: `opened.add(doorObj);` first (latch after success, mirrors
      `goalReached`'s placement), then `fx.clearBurst();`, then `destroy(doorObj);` and
      `if (doorObj.glyphObj) destroy(doorObj.glyphObj);` (cleans up BOTH entities Task 2
      created), and ONLY THEN `player.paused = false;` — this exact ordering (destroy
      before unpause) is the load-bearing fix for the re-trigger hazard, and the trailing
      unpause line is the load-bearing fix for the soft-lock hazard. Neither may be
      omitted or reordered.
  </action>
  <acceptance_criteria>
    - `node --check src/mechanics/door.js` passes.
    - `grep -q 'export function wireDoor' src/mechanics/door.js`.
    - `grep -q 'import { openChallenge } from "../ui/challenge.js"' src/mechanics/door.js` (one-way seam reuse, never mathGate.js).
    - `grep -c 'from "../ui/mathGate.js"' src/mechanics/door.js` is `0` (never imports the end-of-level-specific wrapper).
    - `grep -q 'player.paused = false' src/mechanics/door.js` (the soft-lock fix is present — non-negotiable per RESEARCH Pitfall 1).
    - `grep -q 'destroy(doorObj)' src/mechanics/door.js` AND `grep -q 'glyphObj' src/mechanics/door.js` (both entities Task 2 created are cleaned up).
    - The destroy-before-unpause ordering (RESEARCH Pitfall 4) holds: the line number of `destroy(doorObj)` is strictly less than the line number of `player.paused = false`.
    - `bash scripts/check-safety.sh` still passes.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/mechanics/door.js && grep -q 'export function wireDoor' src/mechanics/door.js && grep -q 'import { openChallenge } from "../ui/challenge.js"' src/mechanics/door.js && test "$(grep -c 'from "../ui/mathGate.js"' src/mechanics/door.js)" = "0" && grep -q 'player.paused = false' src/mechanics/door.js && grep -q 'destroy(doorObj)' src/mechanics/door.js && grep -q 'glyphObj' src/mechanics/door.js && DESTROY_LINE=$(grep -n 'destroy(doorObj)' src/mechanics/door.js | head -1 | cut -d: -f1) && UNPAUSE_LINE=$(grep -n 'player.paused = false' src/mechanics/door.js | head -1 | cut -d: -f1) && [ "$DESTROY_LINE" -lt "$UNPAUSE_LINE" ] && bash scripts/check-safety.sh && echo OK</automated>
  </verify>
  <done>src/mechanics/door.js exports wireDoor({player, brain}); it registers one onCollide("door", ...) handler covering every door-tagged entity; on a correct answer it destroys the door + its stashed glyph BEFORE unpausing the player, and it always unpauses — verified both by the static line-order check and (in 15-04) the real browser boot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level geometry data → build.js entity instantiation | Static, developer-authored JS (level-01.js), not runtime/user input — no injection surface. |
| player collision → freeze/challenge/destroy/unfreeze sequence | The load-bearing ordering hazard of this entire phase: destroy-before-unpause and always-unpause are both correctness-critical, not stylistic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-03-01 | Denial of Service | src/mechanics/door.js `onSuccess` handler — missing unfreeze | high | mitigate | `player.paused = false` is REQUIRED in `onSuccess` (unlike the end-of-level `onReachGoal()` precedent, which relies on a scene transition instead and never unfreezes). Mitigated by: (a) explicit task action instructing this exact line, (b) an automated `grep -q 'player.paused = false'` acceptance check, and (c) 15-04's mandatory real-browser boot confirming the player can still move/jump after the door opens. |
| T-15-03-02 | Denial of Service | src/mechanics/door.js `onSuccess` handler — destroy/unpause ordering | medium | mitigate | `destroy(doorObj)` (and its glyph) is ordered strictly BEFORE `player.paused = false`, removing the collider before the freeze lifts so no same-frame re-collision is possible; a closure-local per-door `opened` Set is a second guard. Mitigated by an automated line-order check (destroy's line number < unpause's line number) in Task 3's verify. |
| T-15-03-03 | Tampering | build.js door entity creation from level data | low | accept | Level data is static, developer-authored JS, not user/runtime input; every visual uses only Kaplay canvas primitives (rect/text), never a DOM sink. |
| T-15-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No installs in this plan. |
</threat_model>

<verification>
- Per task: `node --check` + the grep/ordering batteries in each task's `<verify>`, plus `bash scripts/check-safety.sh` after Tasks 2 and 3.
- After this plan (wave merge): `node scripts/smoke-progress.mjs` (LVL-02 regression stays green with the new doors field on both sides) and `bash scripts/check-safety.sh` both pass. `scripts/check-gate.sh` and `scripts/check-import-safety.sh` (extended in 15-01) both still correctly report red at this point — they require `src/scenes/game.js` to actually WIRE `wireDoor(...)`, which is 15-04's job — and the mandatory real-browser boot proving no soft-lock is also 15-04's job.
</verification>

<success_criteria>
- `CONFIG.DOOR` exists and mirrors select.js's locked-tile palette verbatim.
- level-01.js carries one door, mid-level, next to the x:1520 spike; `scripts/smoke-progress.mjs`'s LVL-02 regression stays green.
- `buildLevel()` instantiates a solid blocking door + a separately-cleanable lock glyph from `geometry.doors`.
- `src/mechanics/door.js` exports `wireDoor` with the destroy-before-unpause ordering and the always-unpause guarantee, both statically verified.
</success_criteria>

<output>
Create `.planning/phases/15-challenge-seam-locked-door-mechanic/15-03-SUMMARY.md` when done.
</output>
