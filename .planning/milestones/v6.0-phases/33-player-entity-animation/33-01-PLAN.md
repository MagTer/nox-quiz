---
phase: 33-player-entity-animation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-art-assets.py
  - assets/door.png
  - assets/math-gate.png
  - assets/enemy-1.png
  - assets/enemy-2.png
  - assets/enemy-3.png
  - CREDITS.md
autonomous: true
requirements: [ART-05]

must_haves:
  truths:
    - "assets/door.png and assets/math-gate.png are baked at native Gothicvania color (no palette remap) from real church/castle source crops, both 32x64, both pass the pink-gate."
    - "CREDITS.md accurately attributes the new door.png/math-gate.png provenance and no longer credits the retired enemy-1/2/3 sprites."
  artifacts:
    - "assets/door.png (rebaked in place, 32x64, native color)"
    - "assets/math-gate.png (new, 32x64, native color)"
    - "scripts/build-art-assets.py: build_door() replaced (v2, native color), new build_math_gate() added and wired into __main__"
    - "CREDITS.md rows updated for door/math-gate provenance; enemy-1/2/3 row and files retired"
  key_links:
    - "build_door() v2's crop source (Old-dark-Castle-tileset-Files/PNG/old-dark-castle-interior-tileset.png) and build_math_gate()'s crop source (gothicvania church files/ENVIRONMENT/tileset.png) must both exist under the re-fetched assets/_gothicvania-src/ (gitignored, absent in a fresh worktree) before either bake function runs."
---

<objective>
Bake real, native-Gothicvania-color art for the door and math-gate mechanic panels — the two entities the roadmap explicitly names as still shipping "flat-color panels" — sourced from the church/castle OGA packs already pixel-verified pink-clean in this phase's RESEARCH.md, and retire the now-fully-superseded enemy-1/2/3 placeholder sprites.

Purpose: ART-05 requires real animated/non-flat art for doors, checkpoint gates, enemy blockers, and the math gate. The door and math-gate need genuinely new bakes (no Phase 31 output exists for either); the enemy blocker is handled by Phase 31's already-baked Hell hound (wired in Plan 33-02/33-04) — this plan's enemy-touching work is limited to retiring the dead enemy-1/2/3 assets that Hell hound fully replaces.
Output: assets/door.png (overwritten, new content), assets/math-gate.png (new file), scripts/build-art-assets.py extended, CREDITS.md corrected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-player-entity-animation/33-CONTEXT.md
@.planning/phases/33-player-entity-animation/33-RESEARCH.md
@.planning/phases/33-player-entity-animation/33-PATTERNS.md
</context>

<artifacts_produced>
- `scripts/build-art-assets.py`: `build_door()` function body replaced (same name, new native-color castle-pack crop); new `build_math_gate()` function added, called from the `if __name__ == "__main__":` block immediately after `build_door()`
- `assets/door.png` — 32x64 PNG, overwritten in place
- `assets/math-gate.png` — 32x64 PNG, new file
- `CREDITS.md` — door.png row folded into the existing Gothicvania Patreon Collection row; math-gate.png row folded into the existing Gothicvania Church row; enemy-1/2/3 row removed
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Re-fetch the church + castle-collection source packs with integrity and zip-slip checks</name>
  <files>assets/_gothicvania-src/ (gitignored scratch directory, not committed)</files>
  <read_first>
    scripts/build-art-assets.py lines 1083-1121 (`build_enemy_hellhound()` — the exact, already-proven-working extracted-path convention for the Patreon Collection pack, including its leading-space nested folder name)
    .planning/phases/31-asset-bake-style-board-sign-off/31-01-PLAN.md Task 1 (the exact re-fetch/integrity/zip-slip pattern this task mirrors)
    .gitignore (confirm the existing `assets/_gothicvania-src/` exclusion stanza — already added in Phase 31, do not re-add)
  </read_first>
  <action>
    `assets/_gothicvania-src/` is gitignored and absent in this worktree (confirmed this session — Phase 31's fetch was never committed by design). Create the directory, then fetch exactly these 2 zips (not all 5 — swamp/town/cemetery are not needed this phase) with `curl -sSL --max-time 120 -o <dest> <url>`:
    1. Church pack: `https://opengameart.org/sites/default/files/gothicvania%20church%20files.zip` (~733KB) — needed for the math-gate crop.
    2. Patreon Collection pack: `https://opengameart.org/sites/default/files/%20gothicvania%20patreon%20collection.zip` (~4.0MB, note the LEADING `%20` before "gothicvania" in the URL) — needed for the door crop.

    For EACH downloaded file, before extracting: (a) confirm it is a real zip via `python3 -c "import zipfile,sys; exit(0 if zipfile.is_zipfile(sys.argv[1]) else 1)" <file>` — non-zero exit means re-fetch or investigate, do not proceed; (b) confirm no path-traversal entries via `unzip -l <file>` scanned for any listed path containing `../` — abort extraction of that specific zip if found. Extract both with `unzip -q <zip> -d assets/_gothicvania-src/`.

    After extraction, confirm the two specific source images this phase's bake needs actually exist at these paths (matching `build_enemy_hellhound()`'s already-proven folder-naming convention for the Patreon Collection pack, including its odd leading-space nested directory):
    - `assets/_gothicvania-src/gothicvania church files/ENVIRONMENT/tileset.png`
    - `assets/_gothicvania-src/gothicvaniapatreoncollection/ gothicvania patreon collection/Old-dark-Castle-tileset-Files/PNG/old-dark-castle-interior-tileset.png`

    If the actual extracted folder casing/nesting differs once inspected, use the real names — do not force a mismatched rename (this is a live re-fetch, not a cached assumption).
  </action>
  <acceptance_criteria>
    - Both `zipfile.is_zipfile()` checks exit 0.
    - `unzip -l` output for both zips contains zero lines matching `../`.
    - `test -f "assets/_gothicvania-src/gothicvania church files/ENVIRONMENT/tileset.png"` exits 0.
    - `test -f "assets/_gothicvania-src/gothicvaniapatreoncollection/ gothicvania patreon collection/Old-dark-Castle-tileset-Files/PNG/old-dark-castle-interior-tileset.png"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f "assets/_gothicvania-src/gothicvania church files/ENVIRONMENT/tileset.png" && test -f "assets/_gothicvania-src/gothicvaniapatreoncollection/ gothicvania patreon collection/Old-dark-Castle-tileset-Files/PNG/old-dark-castle-interior-tileset.png" && echo "sources present"</automated>
  </verify>
  <done>Both source images this phase's bake needs exist on disk under assets/_gothicvania-src/, verified as genuine zips with zero path-traversal entries.</done>
</task>

<task type="auto">
  <name>Task 2: Bake build_door() v2 (native color) + new build_math_gate(), verify the pink-gate</name>
  <files>scripts/build-art-assets.py, assets/door.png, assets/math-gate.png</files>
  <read_first>
    scripts/build-art-assets.py lines 572-594 (the OLD `build_door()` — its Kenney-sourced `_remap_luminance` pipeline must NOT be reused)
    scripts/build-art-assets.py lines 1006-1121 (`build_player_swamphunter()`/`build_enemy_hellhound()` — the native-color, no-remap bake idiom to mirror exactly: crop -> `Image.NEAREST` resize -> paste on transparent RGBA canvas -> `assert` size -> `save()`)
    scripts/build-art-assets.py lines 79-82 (the shared `save()` helper) and lines 1281-1302 (the `__main__` block's call sequence — `build_door()` is already called; `build_math_gate()` needs to be added right after it)
    src/config.js CONFIG.DOOR / CONFIG.MATH_GATE blocks (both W:32, H:64 — the locked target footprint for both bakes)
    scripts/lib/pink_scan.py (confirm the `pink_fraction()` scan scope/threshold this task's assets must clear)
  </read_first>
  <action>
    Replace the body of `build_door()` (keep the function name — it is already called from `__main__`) with a native-color bake: crop `old-dark-castle-interior-tileset.png` at `(664, 47, 698, 112)` (34x65px, pixel-verified this phase's RESEARCH session as a clean wooden door with stone frame + gold hinge, 0% pink-hue), resize with `Image.NEAREST` to `(32, 64)`, paste onto a transparent RGBA canvas of that exact size, `assert` the final size, `save()` to `assets/door.png`. Do NOT call `_remap`/`_remap_luminance` anywhere in the function body — this ships at native Gothicvania color to match the already-shipped, unremapped castle biome atlas/player/enemy assets (the OLD Kenney-sourced remap pipeline this replaces must not be reused, or the door will visibly clash against the rest of the castle biome). If a short docstring rationale for skipping the remap is useful, phrase it WITHOUT trailing parentheses after the identifiers (e.g. "ships at native color, no _remap/_remap_luminance" — no `(` after either word), mirroring `build_player_swamphunter()`'s own proven-safe docstring phrasing exactly.

    Add a NEW function `build_math_gate()` (add its call to the `if __name__ == "__main__":` block immediately after the existing `build_door()` call, no other line in that list changes): crop `gothicvania church files/ENVIRONMENT/tileset.png` at `(240, 32, 272, 80)` (32x48px, pixel-verified 0% pink-hue — a barred iron-lattice window with a cross plaque, reading as "locked"), resize with `Image.NEAREST` (uniform stretch — the crop is 16px short of the 64px target height, no adjacent fillable content exists in the source to pad with instead) to `(32, 64)`, paste onto a transparent RGBA canvas, `assert` the final size, `save()` to `assets/math-gate.png`. Same no-remap discipline as `build_door()`.

    Do NOT run the full script (`python3 scripts/build-art-assets.py` would fail — its `__main__` block unconditionally rebuilds every biome atlas, including swamp/town/cemetery packs this task did not fetch). Instead invoke ONLY the two target functions via a scoped module load that does not trigger `__main__` (the filename has hyphens, so a plain `import` will not work — use `importlib.util.spec_from_file_location('bake', 'scripts/build-art-assets.py')`, `module_from_spec`, `exec_module`, then call `m.build_door()` and `m.build_math_gate()` directly on the resulting module object).
  </action>
  <acceptance_criteria>
    - `grep -A30 "^def build_door" scripts/build-art-assets.py | grep -c "_remap_luminance("` returns 0.
    - `grep -A30 "^def build_door" scripts/build-art-assets.py | grep -c "_remap("` returns 0.
    - `grep -A30 "^def build_math_gate" scripts/build-art-assets.py | grep -c "_remap_luminance("` returns 0.
    - `grep -A30 "^def build_math_gate" scripts/build-art-assets.py | grep -c "_remap("` returns 0.
    - `grep -c "build_math_gate()" scripts/build-art-assets.py` returns 2 (the `def` line plus the `__main__` call site).
    - The scoped module-load invocation exits 0 and both `assets/door.png`/`assets/math-gate.png` have newer mtimes than before this task ran.
    - `python3 -c "from PIL import Image; assert Image.open('assets/door.png').size == (32, 64)"` exits 0; same check for `assets/math-gate.png`.
    - `bash scripts/check-pink-gate.sh` exits 0 with `pink-gate checks: PASS`.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-pink-gate.sh</automated>
  </verify>
  <done>assets/door.png and assets/math-gate.png are freshly baked at native Gothicvania color (zero remap calls in either function), both exactly 32x64, both pass the pink-gate.</done>
</task>

<task type="auto">
  <name>Task 3: CREDITS.md provenance update + retire the 3 dead enemy PNGs</name>
  <files>CREDITS.md, assets/enemy-1.png, assets/enemy-2.png, assets/enemy-3.png</files>
  <read_first>
    CREDITS.md (full file — the door.png attribution row, the enemy-1/2/3 row, and the existing "Gothicvania Patreon Collection" and "Gothicvania Church" rows this task extends)
  </read_first>
  <action>
    Remove the CREDITS.md row currently attributing `assets/door.png` to the pre-Phase-33 HorusKDI dungeon-tile pack. Fold `assets/door.png` into the file list of the existing "Gothicvania Patreon Collection" row (the same row already crediting `assets/tiles/atlas-castle.png`/`assets/enemy-hellhound.png` from that pack) — matching that row's established multi-file precedent, same author/source/license.

    Fold `assets/math-gate.png` into the file list of the existing "Gothicvania Church" row (currently crediting only `assets/parallax/near-castle.png`) — same author/source/license, extend the "Used for" description to mention the math-gate panel.

    Remove the row crediting `enemy-1.png, enemy-2.png, enemy-3.png` to the New Platformer Pack — the Hell hound sprite (already credited under Gothicvania Patreon Collection) now fully replaces this trio per CONTEXT.md's "ONE shared enemy-blocker sprite" decision, and they have zero remaining consumers after this phase. Delete the 3 files: `assets/enemy-1.png`, `assets/enemy-2.png`, `assets/enemy-3.png`.

    Do not add a standalone changelog "Notes" entry naming the retired files verbatim — the row removal itself is the record; this project's existing Notes section documents PACK-level history (e.g. the Phase 20/27/31 entries), not individual retired-file inventories.
  </action>
  <acceptance_criteria>
    - `test ! -f assets/enemy-1.png` exits 0 (file absent).
    - `test ! -f assets/enemy-2.png` exits 0.
    - `test ! -f assets/enemy-3.png` exits 0.
    - `grep "assets/door.png" CREDITS.md` returns exactly one line, and that line also contains "Gothicvania Patreon Collection".
    - `grep "assets/math-gate.png" CREDITS.md` returns exactly one line, and that line also contains "Gothicvania Church".
  </acceptance_criteria>
  <verify>
    <automated>test ! -f assets/enemy-1.png && test ! -f assets/enemy-2.png && test ! -f assets/enemy-3.png && grep -q "assets/door.png" CREDITS.md && grep -q "assets/math-gate.png" CREDITS.md && echo "credits OK"</automated>
  </verify>
  <done>CREDITS.md accurately reflects the new door/math-gate provenance; the 3 dead enemy PNGs and their attribution row are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Internet (opengameart.org) -> local filesystem | This plan's only external-input surface: 2 third-party zip archives downloaded and extracted onto disk. Everything downstream (build_door/build_math_gate, and later plans' game code) consumes these as trusted local input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Tampering | Zip download (`curl` from opengameart.org) | medium | mitigate | Each downloaded file is verified as a genuine zip (`zipfile.is_zipfile()`) before extraction — catches a redirected/expired URL silently serving an HTML error page saved with a `.zip` extension. |
| T-33-02 | Tampering | Zip extraction (`unzip -d assets/_gothicvania-src/`) | high | mitigate | Every zip's file listing (`unzip -l`) is scanned for `../` path-traversal entries BEFORE extraction; `unzip`'s own default behavior additionally refuses to write outside the target directory. Both checks are real, per Task 1's acceptance criteria. |
| T-33-03 | Tampering | `scripts/build-art-assets.py` edits (`build_door()` body replacement, new `build_math_gate()`) | low | accept | Internal dev/build tooling, never shipped to players; git-tracked and reviewed via this plan's own region-scoped grep acceptance criteria confirming no palette-remap call was reintroduced. |
</threat_model>

<verification>
1. `bash scripts/check-pink-gate.sh` exits 0 (PASS) after both new bakes land.
2. `python3 -c "from PIL import Image; assert Image.open('assets/door.png').size == (32,64); assert Image.open('assets/math-gate.png').size == (32,64)"` exits 0.
3. Neither `build_door()` nor `build_math_gate()` calls `_remap(`/`_remap_luminance(` (region-scoped grep, zero hits).
4. `assets/enemy-1.png`/`enemy-2.png`/`enemy-3.png` are deleted; CREDITS.md rows updated accordingly.
</verification>

<success_criteria>
- assets/door.png and assets/math-gate.png ship at native Gothicvania color, correct 32x64 dimensions, pink-gate clean.
- CREDITS.md has zero stale attribution for the old door source or the retired enemy-1/2/3 trio.
- The 3 dead enemy PNGs are removed from the repo.
</success_criteria>

<output>
Create `.planning/phases/33-player-entity-animation/33-01-SUMMARY.md` when done
</output>
