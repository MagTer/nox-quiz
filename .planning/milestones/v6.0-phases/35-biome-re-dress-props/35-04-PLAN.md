---
phase: 35-biome-re-dress-props
plan: 04
type: execute
wave: 4
depends_on: ["35-03"]
files_modified:
  - src/levels/level-02.js
  - src/levels/level-05.js
autonomous: true
requirements: [ART-06, ART-07]
must_haves:
  truths:
    - "level-02 (swamp) and level-05 (cemetery) each carry a restrained top-level `props` array dressed to the plan-03-approved approach, reusing the already-baked swamp/cemetery sprites"
    - "Both levels' geometry objects are byte-identical to the frozen baseline"
    - "Both levels boot with props under the object/FPS budget"
  artifacts:
    - "src/levels/level-02.js + src/levels/level-05.js — the `props` arrays"
    - ".planning/phases/35-biome-re-dress-props/prop-shots/ — level-02 + level-05 screenshots"
  key_links:
    - "already-baked prop-swamp-*/prop-cemetery-* keys → the two new descriptor props arrays (no new bake)"
---

<objective>
Dress the two remaining levels whose biomes were already baked and approved in the trial — level-02 (swamp, the intense/vertical second swamp visit) and level-05 (cemetery, the calm cemetery intro) — reusing the `prop-swamp-*` and `prop-cemetery-*` sprites already on disk. No new art. Apply the plan-03-approved density/placement discipline exactly.

Purpose: swamp and cemetery vocabularies are already proven (plan 02) and signed off (plan 03), so these two levels are pure placement — the cheapest, lowest-risk half of the remaining-6 rollout. This plan can run in parallel with plan 05 (town/castle bake) since it touches only these two descriptors.

HARD constraints: geometry BYTE-FROZEN (top-level `props` only); RESTRAINED / legibility-first (never obscure route/coins/hazards/key/mechanics/goal); background-depth or on-surface only, NO foreground over the play lane; on-surface `y = surfaceY − spriteHeight`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-biome-re-dress-props/35-CONTEXT.md
@.planning/phases/35-biome-re-dress-props/35-RESEARCH.md
@.planning/phases/35-biome-re-dress-props/35-PATTERNS.md
@.planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md
@src/levels/level-02.js
@src/levels/level-05.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Place the swamp props array on level-02 (intense/vertical swamp)</name>
  <files>src/levels/level-02.js</files>
  <read_first>
    - src/levels/level-02.js — the `geometry` object (its THREE open-air ascent/descent humps, the math-skip key on the third hump peak, the DOOR/ENEMY/spikes/coins/GOAL) and the top-level `mechanics`/`biome`/`parallax` slots where `props` goes. This is the vertical swamp — props must stay off the hump climb lanes and the key.
    - .planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md — the approved density/placement notes from the trial (the binding template for this placement).
    - .planning/phases/35-biome-re-dress-props/35-RESEARCH.md — §"Pattern 1" (top-level props, layer, on-surface `y = surfaceY − spriteHeight`), Pitfalls 1/2/4.
  </read_first>
  <action>
    Append a top-level `props: [...]` to LEVEL_02 (sibling of mechanics/biome/parallax) reusing the baked `prop-swamp-*` keys — geometry untouched. Follow the plan-03-approved swamp approach: a few background trees at `layer:"back"` off the play lane + a handful of on-surface reeds/bones at `layer:"surface"` on floor tops and wide ledges. Because level-02 is vertical (three humps + key), keep props OFF the hump climb/descent lanes and clear of the door/enemy/spikes/coins/key/goal. Restrained density. On-surface `y = surfaceY − spriteHeight` (measure the baked PNG heights).
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-assets-manifest.mjs && node scripts/check-geometry-frozen.mjs</automated>
  </verify>
  <acceptance_criteria>
    - LEVEL_02 has a top-level `props` array referencing only declared `prop-swamp-*` manifest keys.
    - `getLevel("level-02").geometry` byte-identical to baseline (check-geometry-frozen green).
    - validate-levels 0 HARD-FAIL; no prop on a climb lane, hazard, coin, key, or goal.
  </acceptance_criteria>
  <done>level-02 dressed to the approved swamp approach; geometry frozen; gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Place the cemetery props array on level-05 (calm cemetery intro) + capture screenshots</name>
  <files>src/levels/level-05.js</files>
  <read_first>
    - src/levels/level-05.js — the `geometry` object + top-level slots. level-05 is the calmer cemetery intro (odd) — a gentler layout than level-06.
    - .planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md — the approved cemetery approach from the trial.
    - scripts/screenshot-phase35-props.mjs — invocation for specific level ids.
  </read_first>
  <action>
    Append a top-level `props: [...]` to LEVEL_05 reusing the baked `prop-cemetery-*` keys (statue/tombstones/bush on-surface + background trees) to the plan-03-approved cemetery density — geometry untouched, restrained, legibility-first, on-surface `y = surfaceY − spriteHeight`. Then run `node scripts/screenshot-phase35-props.mjs level-02 level-05` to capture spawn shots (+ climb shot for level-02, a vertical even level) into prop-shots/ for the plan-08 spot-check, and run browser-boot to confirm both levels are budget-safe with props.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs && node scripts/screenshot-phase35-props.mjs level-02 level-05</automated>
  </verify>
  <acceptance_criteria>
    - LEVEL_05 has a top-level `props` array (declared `prop-cemetery-*` keys only); geometry byte-identical to baseline.
    - browser-boot green: level-02 + level-05 boot, reach goal, under OBJECT_BUDGET / above FPS_FLOOR.
    - prop-shots/ contains level-02 (spawn+climb) and level-05 (spawn) screenshots.
  </acceptance_criteria>
  <done>level-05 dressed; level-02 + level-05 screenshots captured; both budget-safe; geometry frozen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor data → build.js | In-repo authored data; covered by validate-levels + check-geometry-frozen |

Static offline game; placement-only, no new art, no new input surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-05 | Tampering | geometry drift under an art commit | medium | mitigate | check-geometry-frozen.mjs per descriptor edit; props top-level only |
| T-35-SC | Tampering | package installs | n/a | accept | Zero new dependencies; no new art (reuses trial bakes) |
</threat_model>

<verification>
```
node scripts/validate-levels.mjs && node scripts/check-assets-manifest.mjs \
  && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs \
  && node scripts/screenshot-phase35-props.mjs level-02 level-05
```
</verification>

<success_criteria>
- level-02 (swamp) + level-05 (cemetery) dressed to the approved approach, reusing baked art.
- Geometry byte-frozen; validator-neutral; budget-safe; screenshots captured.
</success_criteria>

<output>
Create `.planning/phases/35-biome-re-dress-props/35-04-SUMMARY.md` when done.
</output>
