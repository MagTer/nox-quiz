---
phase: 35-biome-re-dress-props
plan: 03
type: execute
wave: 3
depends_on: ["35-02"]
files_modified: []
autonomous: false
requirements: [ART-06, ART-07]
must_haves:
  truths:
    - "A human looks at the two trial levels' props IN-ENGINE (screenshots + optionally live) and explicitly approves or rejects the look"
    - "The approval covers BOTH the restrained-density / legibility question AND the per-biome prop vocabulary (two different biomes were dressed for exactly this reason)"
    - "Downstream rollout (plans 04–07) does not proceed until this sign-off passes"
  artifacts:
    - "A recorded human decision (approved / rejected-with-notes) referencing the specific prop-shots"
  key_links:
    - "prop-shots/ screenshots + style-board-{swamp,cemetery}.png → human comparison → approve/reject → gates plans 04–07"
---

<objective>
The load-bearing human sign-off on the 2-level props trial. A real person confirms the restrained props layer reads right in-engine on level-01 (swamp) and level-06 (cemetery) — atmospheric but never obscuring the route, coins, hazards, key, mechanics, or the climb — BEFORE the approach is rolled out to the remaining 6 levels.

Purpose: This is the exact checkpoint where the Phase 32/33 "black mess" parallax regressions were caught. Screenshots and green gates lied through every one of those bugs — no automated gate looks at whether a composed scene is legible. This sign-off is standing precedent (Phases 25/27/28/31/33): it MUST NOT be rubber-stamped, and `auto_advance` MUST NOT auto-approve it. Plans 04–07 depend on it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-biome-re-dress-props/35-CONTEXT.md
@.planning/research/ART-PARITY-STEERING.md
@.planning/research/v6-scouting/styleboard.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human sign-off — 2-level props trial (swamp + cemetery)</name>
  <files></files>
  <action>
    Review the two trial levels' props IN-ENGINE and record a blocking human-verify sign-off —
    the exact evidence, what to look for, and the resume signal are in the
    <what-built>/<how-to-verify>/<resume-signal> blocks below; follow them precisely. This is a
    verification-only checkpoint; write no code. Do NOT auto-approve (auto_advance must not
    rubber-stamp this — standing precedent, and this is where the Phase 32/33 "black mess"
    regressions were caught). If the look is rejected, route the notes back to plan 02's approach
    (density / placement / vocabulary / retint) and re-capture before any further level is dressed.
  </action>
  <what-built>
    A restrained, visual-only `props` layer on two trial levels of two different biomes:
    - level-01 (swamp, calm, thin vocabulary): background trees + a few on-surface reeds/bones.
    - level-06 (cemetery, vertical switchback, rich vocabulary): statue/tombstones/bush on catacomb floors + background trees in the shaft.
    Props render behind the player (Z_BACK −8) or on-surface (Z_SURFACE −3), never over the play lane. Geometry is byte-frozen; validate-levels + check-geometry-frozen + pink-gate + browser-boot are all green. Evidence lives in `.planning/phases/35-biome-re-dress-props/prop-shots/` (level-01-swamp-spawn.png, level-06-cemetery-spawn.png, level-06-cemetery-climb.png).
  </what-built>
  <how-to-verify>
    1. Open the trial screenshots in `.planning/phases/35-biome-re-dress-props/prop-shots/` and compare each against the matching board in `.planning/research/v6-scouting/board/style-board-swamp.png` and `style-board-cemetery.png`.
    2. (Recommended) Serve the game and play both levels live: from the repo root run `python3 -m http.server 8000`, browse to `http://localhost:8000/src/index.html`, and reach level-01 + level-06 (use `?debug=1` if you want to see colliders and confirm no prop sits on a route).
    3. Judge, explicitly, for BOTH levels:
       - LEGIBILITY (the gate that matters): can you always SEE the route, every platform, every coin, the door, the enemy, the spikes, the key spur (L06), and the goal? Does any prop cover the player, a jump arc, or a route? (§8.5 / ADHD mandate — legibility beats atmosphere.)
       - DENSITY: does it read as restrained atmospheric accents, or is it cluttered? (level-06 is the density stress test.)
       - VOCABULARY: do the swamp and cemetery prop sets feel right for their biome and match the approved board direction?
       - PLACEMENT: any prop floating above or sunk into a ledge? (Pitfall 4.)
    4. On the level-06 CLIMB shot: are props still legible / non-obscuring at altitude, not just at spawn?
  </how-to-verify>
  <resume-signal>
    Type "approved" to unlock the remaining-6 rollout (plans 04–07), OR describe exactly what to change (density, specific prop placements, vocabulary, retint) — rejection sends the fixes back to plan 02's approach before rollout. Do NOT auto-approve.
  </resume-signal>
  <done>A human has reviewed both trial levels in-engine and recorded an explicit approve/reject decision referencing the specific prop-shots; on approval the swamp+cemetery approach becomes the proven template for the remaining-6 rollout.</done>
</task>

</tasks>

<verification>
This plan gates on a genuine human decision — there is no automated pass. The prerequisite gates (validate-levels, check-geometry-frozen, check-pink-gate, browser-boot, check-assets-manifest) are already green from plan 02; this checkpoint adds the human legibility/taste judgment those gates structurally cannot make.
</verification>

<success_criteria>
- A human has reviewed both trial levels in-engine and recorded an explicit approve/reject decision referencing the specific prop-shots.
- If approved: the swamp + cemetery approach (density, placement discipline, vocabulary) is the proven template for the remaining-6 rollout.
- If rejected: concrete notes exist and the trial (plan 02) is revised before any further level is dressed.
</success_criteria>

<output>
Create `.planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md` recording the decision (approved/rejected) and any notes that constrain the rollout.
</output>
