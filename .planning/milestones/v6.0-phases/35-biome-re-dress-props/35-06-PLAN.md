---
phase: 35-biome-re-dress-props
plan: 06
type: execute
wave: 5
depends_on: ["35-05"]
files_modified:
  - src/levels/level-03.js
  - src/levels/level-04.js
autonomous: true
requirements: [ART-06, ART-07]
must_haves:
  truths:
    - "level-03 (town intro) and level-04 (town clocktower, intense/vertical) each carry a restrained top-level `props` array using the baked town sprites, dressed to the plan-03-approved approach"
    - "Both levels' geometry objects are byte-identical to the frozen baseline"
    - "Both levels boot with props under the object/FPS budget; screenshots captured"
  artifacts:
    - "src/levels/level-03.js + src/levels/level-04.js — the town `props` arrays"
    - ".planning/phases/35-biome-re-dress-props/prop-shots/ — level-03 + level-04 screenshots"
  key_links:
    - "baked prop-town-* keys → the two town descriptor props arrays"
---

<objective>
Dress the two town levels — level-03 ("The Old Quarter", calm rooftop intro) and level-04 ("The Clocktower", the intense/vertical even, longest even level, 592px switchback spire + math-skip key) — with a restrained town props layer (barrels/crates/signs/lamps/well) using the sprites baked in plan 05, to the density/placement discipline approved in plan 03.

Purpose: town is the RICH-vocabulary biome, so this is a legibility discipline test — street clutter must read as atmosphere without cluttering the rooftop climb (L03) or the switchback spire (L04). Runs in parallel with plan 07 (castle) — different descriptors.

HARD constraints: geometry BYTE-FROZEN (top-level `props` only); RESTRAINED / legibility-first (never obscure route/coins/hazards/door/enemy/key/goal); background-depth or on-surface only, NO foreground over the play lane; on-surface `y = surfaceY − spriteHeight`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-biome-re-dress-props/35-CONTEXT.md
@.planning/phases/35-biome-re-dress-props/35-RESEARCH.md
@.planning/phases/35-biome-re-dress-props/35-PATTERNS.md
@.planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md
@src/levels/level-03.js
@src/levels/level-04.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Place the town props array on level-03 (rooftop town intro)</name>
  <files>src/levels/level-03.js</files>
  <read_first>
    - src/levels/level-03.js — the `geometry` object (rooftop climb + visible fork + optional belltower; DOOR/ENEMY/spikes/coins/GOAL@~10280) and the top-level mechanics/biome/parallax slots.
    - .planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md — the approved density/placement template.
    - .planning/phases/35-biome-re-dress-props/35-RESEARCH.md — §"Pattern 1", Pitfalls 1/2/4; town OBJECT_BUDGET awareness (rich vocabulary — stay sparse).
  </read_first>
  <action>
    Append a top-level `props: [...]` to LEVEL_03 (sibling of mechanics/biome/parallax) using the baked `prop-town-*` keys — geometry untouched. Restrained street dressing: a few on-surface barrels/crates/signs/lamps at `layer:"surface"` on rooftop and street ledges, a background lamp/well at `layer:"back"` for depth. Keep props off the climb lanes, the fork, and clear of the door/enemy/spikes/coins/goal. On-surface `y = surfaceY − spriteHeight`. Restrained — town is rich, so under-dress.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-assets-manifest.mjs && node scripts/check-geometry-frozen.mjs</automated>
  </verify>
  <acceptance_criteria>
    - LEVEL_03 has a top-level `props` array (declared `prop-town-*` keys only); geometry byte-identical to baseline.
    - validate-levels 0 HARD-FAIL; no prop on a route/hazard/coin/mechanic/goal.
  </acceptance_criteria>
  <done>level-03 dressed to the approved town approach; geometry frozen; gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Place the town props array on level-04 (clocktower switchback) + capture screenshots</name>
  <files>src/levels/level-04.js</files>
  <read_first>
    - src/levels/level-04.js — the `geometry` object (592px switchback spire, math-skip key on the spire, the perf-objects budget note — this is the longest even level, closest to the 650 OBJECT_BUDGET ceiling, so props must be MINIMAL here — Pitfall 2).
    - .planning/phases/35-biome-re-dress-props/35-03-SUMMARY.md — approved density template.
    - scripts/screenshot-phase35-props.mjs — invocation for specific levels.
  </read_first>
  <action>
    Append a top-level `props: [...]` to LEVEL_04 using `prop-town-*` keys — geometry untouched. This level is the longest even and closest to the object budget, so keep the prop count MINIMAL (a few accents on wide ledges only, off the switchback climb lane and the key). Restrained, legibility-first, on-surface `y = surfaceY − spriteHeight`. Then run `node scripts/screenshot-phase35-props.mjs level-03 level-04` (level-04 gets spawn+climb) into prop-shots/, and run browser-boot — watch level-04's object count against OBJECT_BUDGET 650; if it approaches the ceiling, thin the props.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs && node scripts/screenshot-phase35-props.mjs level-03 level-04</automated>
  </verify>
  <acceptance_criteria>
    - LEVEL_04 has a top-level `props` array (declared `prop-town-*` keys only); geometry byte-identical to baseline.
    - browser-boot green: level-03 + level-04 boot, reach goal, level-04 stays under OBJECT_BUDGET 650 and above FPS_FLOOR 40 with props.
    - prop-shots/ contains level-03 (spawn) + level-04 (spawn+climb).
  </acceptance_criteria>
  <done>level-04 dressed minimally (budget-safe); town screenshots captured; geometry frozen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor data → build.js | In-repo authored data; validate-levels + check-geometry-frozen coverage |

Static offline game; placement-only.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-08 | Tampering | geometry drift under an art commit | medium | mitigate | check-geometry-frozen.mjs per descriptor edit |
| T-35-09 | DoS (self) | level-04 prop count over OBJECT_BUDGET | medium | mitigate | Minimal props on the longest even level; browser-boot object-count assertion is the ceiling |
| T-35-SC | Tampering | package installs | n/a | accept | Zero new dependencies; reuses baked town art |
</threat_model>

<verification>
```
node scripts/validate-levels.mjs && node scripts/check-assets-manifest.mjs \
  && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs \
  && node scripts/screenshot-phase35-props.mjs level-03 level-04
```
</verification>

<success_criteria>
- level-03 + level-04 dressed to the approved town approach; level-04 budget-safe.
- Geometry byte-frozen; validator-neutral; screenshots captured.
</success_criteria>

<output>
Create `.planning/phases/35-biome-re-dress-props/35-06-SUMMARY.md` when done.
</output>
