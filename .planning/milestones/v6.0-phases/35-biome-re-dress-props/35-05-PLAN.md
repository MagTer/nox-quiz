---
phase: 35-biome-re-dress-props
plan: 05
type: execute
wave: 4
depends_on: ["35-03"]
files_modified:
  - scripts/build-art-assets.py
  - src/assets-manifest.js
  - assets/props/
  - assets/LICENSES/
  - CREDITS.md
autonomous: true
requirements: [ART-06, ART-07]
must_haves:
  truths:
    - "Town prop sprites are baked from the pre-sliced town pack (barrel/crate/sign/street-lamp/wagon/well) with the board's town retint, on disk and manifest-declared"
    - "Castle prop sprites exist — cropped from the castle/church tilesets first (torches/banners/chains/column), sourced from NEW CC0 only where the tilesets are too thin, with licenses + credits added"
    - "Every new prop passes the pink gate and exists on disk (check-assets-manifest + check-pink-gate green); terrain-atlas gate stays green"
  artifacts:
    - "scripts/build-art-assets.py — town + castle prop bakes via bake_prop"
    - "assets/props/town-*.png + assets/props/castle-*.png"
    - "src/assets-manifest.js — kind:'prop' rows for town + castle + updated count"
    - "assets/LICENSES/<source>.txt + CREDITS.md entries for any NEW CC0 castle sources"
  key_links:
    - "pre-sliced town files → bake_prop(retint 215,255,-60) → assets/props/town-*.png"
    - "castle tileset crops (or new CC0) → bake_prop → assets/props/castle-*.png → LICENSES/CREDITS if new source"
---

<objective>
Bake the prop art for the two biomes NOT covered by the trial: town (levels 3–4, RICH pre-sliced vocabulary) and castle (levels 7–8, the THINNEST biome). This is where the locked "source ADDITIONAL CC0 where a biome is thin" decision bites — castle has only `column.png` pre-sliced, so torches/banners/chains come from tileset crops first, and NEW CC0 sourcing (with full Phase-31 license/credit discipline) only where the tilesets can't supply enough variety.

Purpose: Placement plans 06 (town) and 07 (castle) depend on this art existing and being manifest-declared. Runs in parallel with plan 04 (it touches the bake pipeline + manifest + assets, not the level-02/05 descriptors).

HARD constraints (ART-PARITY-STEERING, normative): name EXACT source paths + crop rects + retint tuples; native color only; NEVER `_remap_luminance()`; NEVER scale-to-cell; retint only via `hue_shift_band`; use pre-sliced files / single-panel crops, NEVER tile a multi-panel preview sheet (fact #4 — the "odd bits and pieces" class). Any change to build-art-assets.py mandates a green check-terrain-atlas.sh (the only pixel gate).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-biome-re-dress-props/35-CONTEXT.md
@.planning/phases/35-biome-re-dress-props/35-RESEARCH.md
@.planning/phases/35-biome-re-dress-props/35-PATTERNS.md
@.planning/research/ART-PARITY-STEERING.md
@.planning/research/v6-scouting/styleboard.py
@scripts/build-art-assets.py
@src/assets-manifest.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bake the town prop vocabulary (pre-sliced, board retint) + manifest rows</name>
  <files>scripts/build-art-assets.py, assets/props/, src/assets-manifest.js</files>
  <read_first>
    - .planning/phases/35-biome-re-dress-props/35-PATTERNS.md — the `assets/props/` town row: source dir `gothicvania-town-files/GothicVania-town-files/PNG/environment/props-sliced/` with pre-sliced named files: barrel, crate, crate-stack, sign, street-lamp, wagon, well.
    - .planning/research/v6-scouting/styleboard.py — `town()` uses the town environment art with the no-pink pass `hue_shift_band(prev, 215, 255, -60)` (the board's town retint — steel-blue night).
    - scripts/build-art-assets.py — the `bake_prop` helper added in plan 02 Task 1; `hue_shift_band` (691); `save` (79).
    - .planning/phases/35-biome-re-dress-props/35-RESEARCH.md — §"Prop Sprite Sourcing + Bake", §"Manifest wiring".
  </read_first>
  <action>
    Bake a RESTRAINED town vocabulary (town is rich — pick a handful that read as street clutter without density): from `props-sliced/` bake at minimum barrel, crate (or crate-stack), sign or street-lamp, and well or wagon. Use `bake_prop(out_name, src_path, retint=(215, 255, -60))` (the board's town no-pink tuple) — direct pre-sliced files, no sheet tiling, no scaling, no `_remap_luminance`. Save to `assets/props/town-<name>.png`. Add a kind:"prop" manifest row per sprite and bump the header count/tally. These files are already covered by `assets/LICENSES/gothicvania-town.txt` — no new license step.
  </action>
  <verify>
    <automated>python3 scripts/build-art-assets.py && node scripts/check-assets-manifest.mjs && bash scripts/check-pink-gate.sh && bash scripts/check-terrain-atlas.sh</automated>
  </verify>
  <acceptance_criteria>
    - `assets/props/town-*.png` exist; each has a manifest row; check-assets-manifest green.
    - check-pink-gate green (town retint applied); check-terrain-atlas green (terrain untouched).
    - Each town prop PNG is a single object (no mis-cropped sheet).
  </acceptance_criteria>
  <done>Town prop art baked (pre-sliced, board-retinted, pink-gated) and manifest-declared for plan 06.</done>
</task>

<task type="auto">
  <name>Task 2: Bake/source the castle prop vocabulary (tileset crops first, CC0 where thin) + licenses/credits</name>
  <files>scripts/build-art-assets.py, assets/props/, src/assets-manifest.js, assets/LICENSES/, CREDITS.md</files>
  <read_first>
    - .planning/phases/35-biome-re-dress-props/35-PATTERNS.md + 35-RESEARCH.md §"Prop Sprite Sourcing + Bake" — castle is THINNEST: only `gothicvania-church-files/.../ENVIRONMENT/column.png` (114×190) is pre-sliced; torches/banners/chains must be CROPPED from the Old-dark-Castle + Gothic-Castle tilesets (single-panel crops, NEVER the multi-panel preview sheet — fact #4), OR sourced as NEW CC0.
    - .planning/research/v6-scouting/styleboard.py — `castle()` crops the Old-dark-Castle interior preview at x=250; note the single-panel discipline. Use it to identify exact tileset panels for a torch/banner/chain.
    - .planning/research/ART-PARITY-STEERING.md — fact #4 (never tile a preview sheet: crop ONE panel via islands/bbox), Open Question 2 (tileset crops vs new CC0 — try crops first, fall back to CC0).
    - .planning/phases/35-biome-re-dress-props/35-RESEARCH.md — §"License / credit step": any NEW CC0 source needs `assets/LICENSES/<source>.txt` + a `CREDITS.md` entry + named files + must clear check-pink-gate.
  </read_first>
  <action>
    Bake a RESTRAINED castle vocabulary. FIRST try tileset crops (no new license step — packs already covered): bake the `column.png` pillar directly, and crop a single wall torch, banner, and/or chain from the Old-dark-Castle / Gothic-Castle tilesets using explicit crop rects or `islands()` per object — NEVER tile a preview sheet. Measure castle for pink; retint via `hue_shift_band` only if the scan flags pink (castle "measure first" per RESEARCH). Save `assets/props/castle-<name>.png`.

    ONLY if the tilesets can't supply enough legible variety (per the locked "source additional CC0 where thin" decision), source NEW CC0 castle props: mirror Phase-31 discipline — named files only, add `assets/LICENSES/<source>.txt`, add a `CREDITS.md` entry documenting the source + license, and the new art MUST clear check-pink-gate (retint pink/magenta via the Pillow hue-conform pass). Document in the SUMMARY which castle props came from tileset crops vs new CC0. Add a kind:"prop" manifest row per castle sprite and bump the header count/tally. NO `_remap_luminance`, NO scaling.
  </action>
  <verify>
    <automated>python3 scripts/build-art-assets.py && node scripts/check-assets-manifest.mjs && bash scripts/check-pink-gate.sh && bash scripts/check-terrain-atlas.sh</automated>
  </verify>
  <acceptance_criteria>
    - `assets/props/castle-*.png` exist (pillar + at least a torch/banner/chain); each has a manifest row; check-assets-manifest green.
    - check-pink-gate green over all castle art; check-terrain-atlas green.
    - Every castle prop is a single-panel crop (not a multi-panel sheet slice).
    - If ANY new CC0 source was used: `assets/LICENSES/<source>.txt` + a `CREDITS.md` entry exist and name it.
  </acceptance_criteria>
  <done>Castle prop art exists (tileset crops first, CC0 with full license/credit where thin, pink-gated) and is manifest-declared for plan 07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored pack + NEW CC0 sources → bake → assets/props/ | Build-time asset sourcing; pink-scan + manifest existence gate outputs; licenses/credits for new sources |

Static offline game; new art only. NEW CC0 (if any) follows Phase-31 license/credit discipline.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-06 | Tampering | castle sheet mis-crop ("odd bits and pieces") | medium | mitigate | Single-panel crops only (islands/bbox), never tile a preview sheet (fact #4); pink-gate + terrain-atlas over output |
| T-35-07 | Repudiation | unlicensed/uncredited new CC0 art | medium | mitigate | New sources get assets/LICENSES/<source>.txt + CREDITS.md entry, named files only (Phase-31 discipline) |
| T-35-SC | Tampering | package installs | n/a | accept | Zero new dependencies; assets are CC0 files, not registry packages |
</threat_model>

<verification>
```
python3 scripts/build-art-assets.py && node scripts/check-assets-manifest.mjs \
  && bash scripts/check-pink-gate.sh && bash scripts/check-terrain-atlas.sh
```
- terrain-atlas re-run is mandatory after any build-art-assets.py edit.
- No `*theme*.png` should reappear yet (theme-N deletion is plan 08); note if it does.
</verification>

<success_criteria>
- Town + castle prop art baked (native color / board retint, pink-gated, manifest-declared).
- Castle used tileset crops first; any new CC0 carries licenses + credits.
- Terrain atlases untouched; ready for placement plans 06 + 07.
</success_criteria>

<output>
Create `.planning/phases/35-biome-re-dress-props/35-05-SUMMARY.md` when done — record which castle props are tileset crops vs new CC0.
</output>
