---
phase: 31-asset-bake-style-board-sign-off
plan: 05
type: execute
wave: 4
depends_on: ["31-04"]
files_modified:
  - scripts/build-art-assets.py
  - assets/player-swamphunter.png
  - assets/enemy-hellhound.png
  - docs/LEVEL-DESIGN.md
autonomous: true
requirements: [ART-01]

must_haves:
  truths:
    - "assets/player-swamphunter.png exists as a sprite sheet covering idle/run/jump/fall frames from the Swamp Hunter's own 9-anim source set, at native/near-native colors (no environment-style palette remap) — matching what was actually shown and signed off on the style board."
    - "assets/enemy-hellhound.png exists as a static (non-walk/run) sprite sheet built only from the Hell hound's idle frames — no motion wiring, matching CONTEXT.md's explicit 'this phase only vendors/bakes the static sprite' scope boundary."
    - "docs/LEVEL-DESIGN.md carries a new section documenting the actual 16x32-compatible cap-tile convention and the real measured lip offset (in px) used by Plan 31-04's baked atlases — not an aspirational/theoretical number."
  artifacts:
    - "scripts/build-art-assets.py (extended: build_player_swamphunter(), build_enemy_hellhound(), both appended to the dispatch block)"
    - "assets/player-swamphunter.png"
    - "assets/enemy-hellhound.png"
    - "docs/LEVEL-DESIGN.md (new numbered section, anchor/lip convention)"
  key_links:
    - "The anchor/lip convention documented here must describe the ACTUAL crop rectangles/tile geometry chosen in Plan 31-04's terrain atlas baking (this plan runs AFTER Plan 31-04 specifically so the numbers can be real, not projected) — Phases 32/33 will read this doc as their integration source of truth."
    - "assets/player-swamphunter.png's pink-gate status (per Plan 31-03's documented allowlist finding) must be confirmed one way or the other here — if it still trips scripts/lib/pink_scan.py at bake time, that is the EXPECTED, already-justified allowlisted outcome, not a bug to chase away by remapping the sprite's colors (which would deviate from what was signed off)."
---

<objective>
Bake the two remaining ART-01 sprite assets — `assets/player-swamphunter.png` (the signed-off player, ships across all 4 biomes) and `assets/enemy-hellhound.png` (the signed-off castle enemy, static sprite only) — via `scripts/build-art-assets.py`, then write the anchor/lip pixel convention documentation that Phases 32/33 will build against.

Purpose: closes out the ART-01 asset-baking work with the two character-class sprites plus the written convention doc the ROADMAP's 4th success criterion requires ("baked per-biome atlases follow a written anchor/lip convention"). This plan runs after terrain/parallax baking (Plan 31-04) specifically so the documented lip-offset numbers describe REAL baked geometry, not a projection.
Output: 2 new sprite-sheet PNGs and a new documented convention section in `docs/LEVEL-DESIGN.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-asset-bake-style-board-sign-off/31-CONTEXT.md
</context>

<artifacts_produced>
- `scripts/build-art-assets.py` — new `build_player_swamphunter()`, `build_enemy_hellhound()`, both appended to the bottom dispatch block
- `assets/player-swamphunter.png` — Swamp Hunter sprite sheet (idle/run/jump/fall frames)
- `assets/enemy-hellhound.png` — Hell hound static/idle sprite sheet
- `docs/LEVEL-DESIGN.md` — new section documenting the 16×32-compatible cap-tile + lip-offset convention
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Player (Swamp Hunter) and enemy (Hell hound) sprite baking</name>
  <files>scripts/build-art-assets.py, assets/player-swamphunter.png, assets/enemy-hellhound.png</files>
  <read_first>
    scripts/build-art-assets.py (`build_player()` lines ~570-596 — the shared-scale-factor Pattern 1 idiom: ONE scale factor derived from the tallest content bbox, applied to every frame, so the character doesn't visibly grow/shrink between poses; `build_enemies()` for the independent-scale-per-sprite alternative pattern)
    .planning/phases/31-asset-bake-style-board-sign-off/31-CONTEXT.md ("Player sprite: Swamp Hunter... ships as the player across ALL 4 biomes" and "Hell hound... this phase only vendors/bakes the static sprite")
  </read_first>
  <action>
    Write `build_player_swamphunter()`. Source frames live under `GV_SRC/gothicvania_swamp_files/Gothicvania Swamp files/Sprites/Player/` (confirmed present after Plan 31-01's re-fetch), organized as 9 sub-folders (`idle` 6 frames, `run` 14 frames, `jump` 2 frames, `fall` 2 frames, `stand` 1 frame, `crouch` 3, `crouch-shoot` 3, `hurt` 2, `shoot` 3), each frame a 62×54 native canvas. Build a sheet covering the states Phase 33's ART-04 will need (idle, run, jump, fall — do not include crouch/shoot/hurt, which are not part of this game's move-set): pick a representative frame subset — e.g. `idle1.png`+`idle2.png` for idle, 4-6 frames from `run1.png`..`run14.png` spaced evenly through the cycle for run, `jump1.png`+`jump2.png`, `fall1.png`+`fall2.png` — following `build_player()`'s exact Pattern 1 idiom: compute ONE shared scale factor from the tallest frame's own content bbox across ALL included frames, apply it uniformly, bottom-align feet, center horizontally per frame cell. Do NOT run the assembled sheet through `_remap`/`_remap_luminance` — ship it at its native Gothicvania colors (unlike `build_player()`'s Kenney-sourced sheet, which DOES get `PLAYER_PALETTE`-remapped): this preserves exactly what was shown on and signed off via the style board (Plan 31-02), and CONTEXT.md frames the player pick as a direct style-board selection, not a request for further palette conforming. Resize with `Image.NEAREST` only. Document the exact frame files chosen and the cell layout (frame count, per-frame pixel dimensions) in the function's docstring, mirroring `build_player()`'s own docstring conventions. `assert` the final sheet dimensions and `save()` to `assets/player-swamphunter.png`.

    Write `build_enemy_hellhound()`. Source: `GV_SRC/gothicvaniapatreoncollection/ gothicvania patreon collection/Hell-Hound-Files/PNG/hell-hound-idle.png` ONLY (384×32, 6 frames of 64×32 each) — deliberately NOT `hell-hound-walk.png`/`-run.png`/`-jump.png`, since CONTEXT.md is explicit this phase vendors the static sprite only; motion wiring is Phase 36's job. Slice the 6 idle frames, apply `Image.NEAREST` resize if any size normalization is needed to sit alongside the other enemy sprites' scale, do NOT palette-remap (same reasoning as the player — ship at native color, matching what was style-board-approved), `assert` final size, `save()` to `assets/enemy-hellhound.png`.

    Append both new calls to the bottom `if __name__ == "__main__":` dispatch block, after Plan 31-04's biome calls.

    After baking, run `python3 scripts/lib/pink_scan.py assets/player-swamphunter.png` (Plan 31-03's script). Per that plan's already-documented allowlist finding, this MAY report a fraction at or above 0.08 due to the Swamp Hunter's dark plum/maroon outline-shading color — if it does, this is the EXPECTED, already-justified outcome (the allowlist entry in `scripts/lib/pink_scan.py` already names this exact file), not something to "fix" by remapping the sprite's colors. Record the actual measured fraction in this plan's SUMMARY.md either way, and confirm the allowlist entry's path string (`assets/player-swamphunter.png`) exactly matches this file's real repo-relative path (a typo'd allowlist path would silently fail to suppress the false positive).
  </action>
  <acceptance_criteria>
    - `python3 scripts/build-art-assets.py` runs clean and prints `generated assets/player-swamphunter.png (...)` and `generated assets/enemy-hellhound.png (...)`.
    - `python3 -c "from PIL import Image; print(Image.open('assets/enemy-hellhound.png').size)"` reports a width that is a multiple of 64 (6 idle frames at 64px each, possibly resized — confirm the multiple, not necessarily exactly 384) and height 32 or a clean multiple thereof.
    - `grep -c "hell-hound-walk\|hell-hound-run\|hell-hound-jump" scripts/build-art-assets.py` returns 0 inside `build_enemy_hellhound()` (only the idle source is used).
    - `grep -A5 "def build_player_swamphunter" scripts/build-art-assets.py` shows no call to `_remap(` or `_remap_luminance(` (native color preserved).
    - `bash scripts/check-pink-gate.sh` still exits 0 (the allowlist correctly suppresses the known player-sprite false positive; no OTHER new false positive appears).
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/build-art-assets.py && bash scripts/check-pink-gate.sh</automated>
  </verify>
  <done>Both player-swamphunter.png and enemy-hellhound.png exist, ship at native (non-remapped) Gothicvania colors, use only idle-state Hell hound frames, and the full pink-gate still passes (via the pre-existing, correctly-path-matched allowlist entry).</done>
</task>

<task type="auto">
  <name>Task 2: Anchor/lip convention documentation</name>
  <files>docs/LEVEL-DESIGN.md</files>
  <read_first>
    docs/LEVEL-DESIGN.md (full file — the numbered `## N. Title (REQ-ID)` section convention, the tier-legend preamble at the top, and section "6a. Movers" as the most recent example of a "preview ahead of a future phase" section style to mirror)
    scripts/build-art-assets.py (Task 1's finished `build_biome_atlas_*` functions from Plan 31-04, plus this plan's own additions — read the actual crop-rectangle comments you (or Plan 31-04) wrote, to extract the REAL measured lip-offset number rather than inventing one)
  </read_first>
  <action>
    Add a new numbered section to `docs/LEVEL-DESIGN.md`, following the existing `## N. Title (REQ-ID)` heading convention (e.g. `## 9. Biome atlas anchor/lip convention (ART-01)`), placed after the existing "8. Workflow" section. Content must state, using the ACTUAL numbers from Plan 31-04's baked atlases (read them from the real crop-rectangle comments in `build-art-assets.py`, do not restate SPIKE-FINDINGS.md's exploratory "~20-24px" estimate as if it were final):

    - The locked tile grid: 16px, matching `CONFIG.TILE_SIZE` (`src/config.js`).
    - The cap-tile convention: cap/surface tiles are baked at 16px wide × 32px tall (a "16×32-compatible" footprint matching the player's own locked 16×32 collider), so a single cap-tile sprite can visually read as "one tile of solid ground with a decorative lip" without needing a separate lip-only sprite layer.
    - The lip offset: state the actual number of px, measured from the top of a cap tile's sprite artwork down to where the tile's SOLID/walkable surface visually begins (i.e. how much of the sprite's own height is decorative overhang vs. actual load-bearing ground) — pull this number from whatever Plan 31-04 actually cropped/baked, not a guess. If Plan 31-04's SUMMARY.md or code comments already state this figure, cite it directly; if it varies slightly per biome, state each biome's actual figure rather than averaging them.
    - An explicit warning for Phase 32 (which will consume this convention when wiring the autotile renderer): the SPRITE artwork's decorative lip must never be confused with the COLLIDER, which stays exactly at the tile's solid-ground line (`FLOOR_Y`/platform `y`) — the lip is purely a rendering offset, never a physics offset. This mirrors the existing "sprites don't lie about solid ground" language already used in this phase's ROADMAP success criteria.

    Do not restate or duplicate the existing "1. The physics envelope" section's numbers — cross-reference it by section number instead if useful context is needed.
  </action>
  <acceptance_criteria>
    - `grep -n "^## " docs/LEVEL-DESIGN.md` shows exactly one new numbered heading added, positioned after the existing "8. Workflow" section.
    - The new section states a concrete lip-offset number in px (not a range like "20-24px" carried over unverified from SPIKE-FINDINGS.md, unless Plan 31-04's actual bake independently confirms that exact range holds for the real crops chosen).
    - `grep -c "solid ground" docs/LEVEL-DESIGN.md` returns at least 2 (the existing usage plus this new section's collider-vs-sprite warning).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "^## " docs/LEVEL-DESIGN.md</automated>
  </verify>
  <done>docs/LEVEL-DESIGN.md documents the real, measured 16x32 cap-tile + lip-offset convention actually used by Plan 31-04's baked atlases, with an explicit collider-vs-sprite warning for Phase 32.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local pack files (Plan 31-01, already verified) → build-art-assets.py's Pillow pipeline | Trusted local input; no new external-input boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-11 | Tampering (content-quality) | Player/enemy frame selection from a large multi-anim source set | low | mitigate | Only idle/run/jump/fall frames (player) and idle-only frames (hell hound) are selected per CONTEXT.md's explicit scope; acceptance criteria grep-check that walk/run/jump Hell hound sources are never referenced. |
| T-31-12 | Repudiation | The anchor/lip convention's documented numbers | low | mitigate | The doc task explicitly requires citing REAL measured numbers from Plan 31-04's actual bake, not a carried-over estimate from SPIKE-FINDINGS.md — Phase 32/33 depend on this being accurate. |
</threat_model>

<verification>
1. `python3 scripts/build-art-assets.py` runs clean, producing `player-swamphunter.png` and `enemy-hellhound.png`.
2. `bash scripts/check-pink-gate.sh` still passes (allowlist correctly scoped).
3. `grep -n "^## " docs/LEVEL-DESIGN.md` shows the new anchor/lip section present.
</verification>

<success_criteria>
- Both character sprites are baked at native (unmapped) color, using only the CONTEXT.md-approved frame subsets.
- The anchor/lip convention is documented with real, not projected, numbers.
</success_criteria>

<output>
Create `.planning/phases/31-asset-bake-style-board-sign-off/31-05-SUMMARY.md` when done
</output>
