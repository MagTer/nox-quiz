---
phase: 31-asset-bake-style-board-sign-off
plan: 04
type: execute
wave: 3
depends_on: ["31-02", "31-03"]
files_modified:
  - scripts/build-art-assets.py
  - assets/tiles/atlas-swamp.png
  - assets/tiles/atlas-town.png
  - assets/tiles/atlas-cemetery.png
  - assets/tiles/atlas-castle.png
  - assets/parallax/far-swamp.png
  - assets/parallax/mid-swamp.png
  - assets/parallax/near-swamp.png
  - assets/parallax/far-town.png
  - assets/parallax/mid-town.png
  - assets/parallax/near-town.png
  - assets/parallax/far-cemetery.png
  - assets/parallax/mid-cemetery.png
  - assets/parallax/near-cemetery.png
  - assets/parallax/far-castle.png
  - assets/parallax/mid-castle.png
  - assets/parallax/near-castle.png
autonomous: true
requirements: [ART-01]

must_haves:
  truths:
    - "Each of the 4 biomes has a baked terrain atlas under assets/tiles/ and 3 baked parallax layers (far/mid/near) under assets/parallax/, all following the 16x32-compatible cap-tile convention."
    - "The baked Town terrain atlas and Town far-layer sky no longer read as dominant pink under scripts/lib/pink_scan.py — the hue-conform retint was actually applied and verified, not assumed."
    - "The baked Cemetery far-layer (horizon glow) no longer reads as dominant pink under scripts/lib/pink_scan.py."
    - "Every new bake function follows scripts/build-art-assets.py's existing per-variant extension idiom (build_ground_theme()/build_parallax_theme()'s shape) and hand-cropped-rectangle convention (build_door()'s shape) — no generic auto-slicer is used on any terrain sheet."
  artifacts:
    - "scripts/build-art-assets.py (extended: new GV_SRC constant, build_biome_atlas_<name>() x4, build_biome_parallax_<name>() x4, all appended to the bottom dispatch block)"
    - "assets/tiles/atlas-{swamp,town,cemetery,castle}.png"
    - "assets/parallax/{far,mid,near}-{swamp,town,cemetery,castle}.png (12 files)"
  key_links:
    - "All new bake functions must reuse _remap / _remap_luminance and Image.NEAREST (never LANCZOS, never a third remap function) — the same pipeline every existing themed asset already goes through."
    - "The retint pass for Town/Cemetery must use the SAME hue_shift_band-derived technique already proven in styleboard.py, applied to the SOURCE crop before or during baking, then re-verified with scripts/lib/pink_scan.py (Plan 31-03's output) before this plan is considered done — a visual/numeric regression proof, not an assumption that copying the styleboard.py logic 'must' work."
---

<objective>
Extend `scripts/build-art-assets.py` with 4 new biome terrain-atlas bakers and 4 new biome parallax bakers (3 layers each), producing the actual game-ready art for all 4 biomes (swamp/town/cemetery/castle) from the re-fetched Gothicvania packs — applying the hue-conform no-pink retint to the two biomes that need it (Town, Cemetery) and proving the result is clean via the Plan 31-03 pink-scan gate.

Purpose: this is the core ART-01 deliverable — the actual style-coherent, license-clean pixel art that Phases 32 (terrain/parallax rendering) and 35 (biome re-dress) will later wire into `build.js`/level descriptors. This plan only bakes and vendors static PNGs; no `src/` code is touched.
Output: 4 terrain atlases + 12 parallax layer PNGs under `assets/`, baked via the ONE extended pipeline script, pink-gate clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-asset-bake-style-board-sign-off/31-CONTEXT.md
@.planning/research/v6-scouting/ASSET-SCOUTING.md
@.planning/research/v6-scouting/SPIKE-FINDINGS.md
</context>

<artifacts_produced>
- `scripts/build-art-assets.py` — new `GV_SRC` constant; `build_biome_atlas_swamp()`, `build_biome_atlas_town()`, `build_biome_atlas_cemetery()`, `build_biome_atlas_castle()`; `build_biome_parallax_swamp()`, `build_biome_parallax_town()`, `build_biome_parallax_cemetery()`, `build_biome_parallax_castle()`; all 8 calls appended to the bottom `if __name__ == "__main__":` dispatch block
- `assets/tiles/atlas-swamp.png`, `atlas-town.png`, `atlas-cemetery.png`, `atlas-castle.png`
- `assets/parallax/far-swamp.png`, `mid-swamp.png`, `near-swamp.png`, `far-town.png`, `mid-town.png`, `near-town.png`, `far-cemetery.png`, `mid-cemetery.png`, `near-cemetery.png`, `far-castle.png`, `mid-castle.png`, `near-castle.png`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Terrain atlas baking — 4 biomes, hand-cropped 16x32-cap-tile convention</name>
  <files>scripts/build-art-assets.py, assets/tiles/atlas-swamp.png, assets/tiles/atlas-town.png, assets/tiles/atlas-cemetery.png, assets/tiles/atlas-castle.png</files>
  <read_first>
    scripts/build-art-assets.py (full file — `build_door()` lines ~568-583 for the hand-crop convention; `build_ground_theme()` lines ~637-657 for the per-variant extension shape; `_remap_luminance()` lines ~153-183; `ENVIRONMENT_PALETTE` and its FAR/MID/NEAR derivatives, lines ~66-89; the bottom dispatch block, last ~15 lines)
    .planning/research/v6-scouting/SPIKE-FINDINGS.md (the "Asset-pipeline implication" section — Gothicvania sheets are non-grid ~32px decorative blocks with ~20-24px gold cap lips; the spike's own atlas was cut from a single hand-identified region, `x512,y152` on a DIFFERENT sheet, Old Dark Castle — not a coordinate to reuse verbatim here, only the technique)
    src/config.js (line ~80 — `TILE_SIZE: 16`)
  </read_first>
  <action>
    Add a `GV_SRC = os.path.join(ROOT, "assets", "_gothicvania-src")` constant near the existing `SRC` constant, following the exact same `os.path.join(ROOT, "assets", "_<pack>-src")` shape already used for `SRC` — never a hardcoded absolute path.

    Write 4 new functions, `build_biome_atlas_swamp()`, `build_biome_atlas_town()`, `build_biome_atlas_cemetery()`, `build_biome_atlas_castle()` — one per biome rather than one shared parameterized function, since each biome's source tileset differs enough in layout that a shared signature would obscure more than it clarifies (matching this file's own existing precedent of separate `build_door()`/`build_enemies()` functions rather than forcing everything through one shape). Each function must, in order: (1) load its biome's tileset PNG from the exact path below; (2) crop ONE OR MORE hand-identified rectangles containing a representative, visually-verified set of cap/fill/edge tiles — open each source tileset in an image viewer or via a quick throwaway Pillow crop-preview loop FIRST, choose rectangles that align to the tileset's own visible 16px-or-32px grid seams (do not guess blind — SPIKE-FINDINGS.md's own warning), and record the chosen rectangle(s) in a code comment the same way `build_door()` documents `crop((0, 64, 48, 160))  # 48x96 — closed lattice gate + archway base`; (3) resize with `Image.NEAREST` (never LANCZOS) to a final atlas following the 16×32-compatible cap-tile convention (a horizontal strip of 16px-wide columns, with cap-row tiles occupying the full 32px height and fill/edge tiles as needed — mirror `build_ground()`'s existing horizontal-strip-of-N-frames shape, adapted to 32px tall where a cap tile requires it); (4) remap through `_remap_luminance` using the biome-appropriate palette (reuse `ENVIRONMENT_PALETTE` or its FAR/MID/NEAR derivatives — do not invent a new palette); (5) `assert` the final size; (6) `save()` to the target path.

    Source tileset per biome (confirmed present at these exact paths after Plan 31-01's re-fetch, verified live during this phase's planning session):
    - Swamp: `GV_SRC/gothicvania_swamp_files/Gothicvania Swamp files/Evironment/tileset.png` (336×112 native — note the pack's own misspelling "Evironment", not "Environment"; this is NOT a typo to fix, it's the real folder name) → `assets/tiles/atlas-swamp.png`. This file measured 0% dominant-pink during planning — no retint needed.
    - Town: `GV_SRC/gothicvania-town-files/GothicVania-town-files/PNG/environment/layers/tileset.png` (592×192 native) → `assets/tiles/atlas-town.png`. **This file measured ~33% dominant-pink during planning (band 211-239, sat≥30) — retint REQUIRED.** Apply a `hue_shift_band`-derived pass (adapt the function from `styleboard.py`, copied into `build-art-assets.py` or imported via a small shared helper — Claude's discretion on exact code-sharing mechanism, but do not silently skip the retint) to the cropped region BEFORE the palette remap, rotating the same salmon/mauve hue band toward a steel-blue/cool direction (mirror the exact `hue_shift_band(prev, 215, 255, -60)` call already proven in `styleboard.py`'s `town()` function). After baking, run `python3 scripts/lib/pink_scan.py assets/tiles/atlas-town.png` and confirm it now reads below 0.08 — if it does not, the crop rectangle likely still includes the untreated pink region; adjust the crop or extend the retint band, do not ship a still-pink atlas.
    - Cemetery: `GV_SRC/gothicvania-cemetery-files_1/gothicvania-cemetery-files/PNG/Environment/tileset.png` (448×160 native) → `assets/tiles/atlas-cemetery.png`. This file measured ~9% dominant-pink during planning (right at the edge of the 8% threshold) — its dominant flagged color is a dark violet/plum SHADOW tone on tombstone tiles (RGB samples like `(65,25,59)`), the same class of low-brightness HSV artifact documented in Plan 31-03's player-sprite allowlist finding, not necessarily a genuine "pink" read. Visually inspect the actual crop chosen: if the final baked atlas still reads at/above 0.08 under `pink_scan.py` AND visually looks pink (not just dark violet shadow), apply the same retint approach as Town; if it reads clean or the flagged pixels are clearly shadow-tone (not a visually pink surface), no retint is required — use your own visual judgment per this project's "checks that don't look at pixels lie" standard, and record which path was taken in the plan SUMMARY.md.
    - Castle: `GV_SRC/gothicvaniapatreoncollection/ gothicvania patreon collection/Old-dark-Castle-tileset-Files/PNG/old-dark-castle-interior-tileset.png` (832×240 native, the primary interior terrain source) → `assets/tiles/atlas-castle.png`. This file measured 0% dominant-pink — no retint needed. (`Gothic-Castle-Files/PNG/layers/gothic-castle-tileset.png`, 336×240, is available as secondary/alt material if the interior tileset alone doesn't give enough usable cap/fill variety — also 0% dominant-pink.)

    Append all 4 new calls to the bottom `if __name__ == "__main__":` block, after the existing `build_enemies()`/`build_logo()` calls.
  </action>
  <acceptance_criteria>
    - `python3 scripts/build-art-assets.py` runs clean (exit 0), and prints a `generated assets/tiles/atlas-<biome>.png (...)` line for all 4 biomes.
    - `python3 scripts/lib/pink_scan.py assets/tiles/atlas-town.png` reports a fraction `< 0.08` (retint proof, GREEN after previously RED at ~33%).
    - Each of the 4 atlas files, opened via `python3 -c "from PIL import Image; print(Image.open('assets/tiles/atlas-<biome>.png').size)"`, reports a width that is a multiple of 16 and a height that is either 16 or 32 (the locked cap-tile grid).
    - `grep -c "Image.LANCZOS" scripts/build-art-assets.py` shows LANCZOS is never used inside any of the 4 new `build_biome_atlas_*` functions (grep scoped to those function bodies, or confirm by reading — LANCZOS destroys pixel-art crispness).
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/build-art-assets.py && python3 scripts/lib/pink_scan.py assets/tiles/atlas-town.png</automated>
  </verify>
  <done>All 4 biome terrain atlases exist under assets/tiles/, follow the 16x32-compatible cap-tile convention, and (for Town, and Cemetery if judged necessary) are pink-gate clean after retinting.</done>
</task>

<task type="auto">
  <name>Task 2: Parallax layer baking — 4 biomes x 3 layers (far/mid/near), with Town/Cemetery sky retint</name>
  <files>scripts/build-art-assets.py, assets/parallax/far-swamp.png, assets/parallax/mid-swamp.png, assets/parallax/near-swamp.png, assets/parallax/far-town.png, assets/parallax/mid-town.png, assets/parallax/near-town.png, assets/parallax/far-cemetery.png, assets/parallax/mid-cemetery.png, assets/parallax/near-cemetery.png, assets/parallax/far-castle.png, assets/parallax/mid-castle.png, assets/parallax/near-castle.png</files>
  <read_first>
    scripts/build-art-assets.py (Task 1's edits, plus `build_parallax_theme()` lines ~504-540 for the exact per-layer sizing/save-path shape to mirror — far 640x120, mid 640x144, near 640x90, though this task's SOURCE material differs so exact output dimensions may differ; keep each biome internally consistent)
    .planning/research/v6-scouting/ASSET-SCOUTING.md (the "Proposed biome → level mapping" table's note that Town explicitly combines "Town + collection's night-town layers")
  </read_first>
  <action>
    Write 4 new functions, `build_biome_parallax_swamp()`, `build_biome_parallax_town()`, `build_biome_parallax_cemetery()`, `build_biome_parallax_castle()`, each producing 3 output files (far/mid/near) via the same crop → resize(`Image.NEAREST`) → `_remap_luminance` (using `ENVIRONMENT_PALETTE_FAR`/`_MID`/`_NEAR` respectively, or the plain `ENVIRONMENT_PALETTE` if a layer's own art already reads with enough depth-appropriate contrast) → assert → save pipeline already established by `build_parallax_theme()`.

    Source file per biome per layer (confirmed present after Plan 31-01's re-fetch, dominant-pink fractions measured during this phase's planning session):
    - **Swamp** (all measured 0% pink — no retint needed): far ← `GV_SRC/gothicvania_swamp_files/Gothicvania Swamp files/Evironment/background.png` (96×256); mid ← `.../mid-layer-02.png` (208×256); near ← `.../mid-layer-01.png` (208×256, closest-to-camera per the existing `swamp()` styleboard compositing order, which draws it last/on top).
    - **Town** (mixed): far ← `GV_SRC/gothicvania-town-files/GothicVania-town-files/PNG/environment/layers/background.png` (384×288) — **this IS CONTEXT.md's named "Town dusk sky (salmon-pink)", measured ~64% dominant-pink — retint REQUIRED**, same `hue_shift_band`-derived pass as Task 1's town terrain atlas (reuse the same helper, do not write a second retint implementation). mid ← `GV_SRC/gothicvaniapatreoncollection/ gothicvania patreon collection/night-town-background-files/layers/night-town-background-town.png` (512×99, measured 0% pink, no retint needed — a genuine multi-layer night-town parallax set the Patreon Collection pack ships specifically for this purpose, per ASSET-SCOUTING.md's own "Town + collection's night-town layers" recommendation). near ← the SAME `night-town-background-files/layers/` directory's `night-town-background-far-buildings.png` (320×80, measured 0% pink) as a closer silhouette detail layer — if visual inspection shows `night-town-background-mountains.png` or `-mountains-lights.png` (both also 0% pink, both 96×224) reads better as the near/closest layer, substitute it; this is a visual judgment call, not a fixed requirement.
    - **Cemetery** (mixed): far ← `GV_SRC/gothicvania-cemetery-files_1/gothicvania-cemetery-files/PNG/Environment/background.png` (384×224) — **this IS CONTEXT.md's named "Cemetery horizon glow (magenta)", measured ~79% dominant-pink — retint REQUIRED**, same technique as Town's sky (mirror `styleboard.py`'s own `cemetery()` function's existing `hue_shift_band(bg, 195, 245, -50)` call). mid ← `.../mountains.png` (192×179, measured 0% pink under this plan's refined 211-239 band — no retint strictly required for the gate, though `styleboard.py`'s existing `cemetery()` function also hue-shifts this file for visual cohesion with the retinted sky; apply the same shift here too if it reads better alongside the retinted far layer — a visual call, not a gate requirement). near ← `.../graveyard.png` (384×123, measured 0.1% pink, no retint needed).
    - **Castle** (all measured 0% or near-0% pink — no retint needed): far ← `GV_SRC/gothicvaniapatreoncollection/ gothicvania patreon collection/Gothic-Castle-Files/PNG/layers/gothic-castle-background.png` (480×336, exterior, most distant). mid ← `.../Old-dark-Castle-tileset-Files/PNG/old-dark-castle-interior-background.png` (960×304, interior, closer). near ← `GV_SRC/gothicvania-church-files/gothicvania church files/ENVIRONMENT/backgrounds.png` (624×192, measured ~2% pink, effectively clean) — this is CONTEXT.md's "fold Church's usable interior background accents into the Castle biome" decision, giving Castle's 3rd/nearest parallax layer.

    After baking, re-run `python3 scripts/lib/pink_scan.py assets/parallax/far-town.png` and `python3 scripts/lib/pink_scan.py assets/parallax/far-cemetery.png` — both must report `< 0.08`. Append all 4 new function calls to the bottom dispatch block after Task 1's atlas calls.
  </action>
  <acceptance_criteria>
    - `python3 scripts/build-art-assets.py` runs clean and produces all 12 `assets/parallax/{far,mid,near}-{swamp,town,cemetery,castle}.png` files.
    - `python3 scripts/lib/pink_scan.py assets/parallax/far-town.png` reports `< 0.08` (GREEN after previously RED at ~64%).
    - `python3 scripts/lib/pink_scan.py assets/parallax/far-cemetery.png` reports `< 0.08` (GREEN after previously RED at ~79%).
    - `bash scripts/check-pink-gate.sh` (the full wrapper, Plan 31-03's output, now scanning a real `assets/` tree that includes all 16 new files from this plan) passes cleanly end to end.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/build-art-assets.py && bash scripts/check-pink-gate.sh</automated>
  </verify>
  <done>All 12 parallax layers exist across the 4 biomes; Town's and Cemetery's sky layers are retinted and proven clean via the pink-scan gate; the full gate passes over the complete, newly-expanded assets/ tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local pack files (Plan 31-01, already verified) → build-art-assets.py's Pillow pipeline | Trusted local input; no new external-input boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-09 | Tampering (content-quality) | Terrain atlas hand-crop selection | medium | mitigate | Every crop rectangle is visually verified against the real source sheet before baking (per SPIKE-FINDINGS.md's explicit warning against blind/generic slicing); acceptance criteria check output dimensions align to the 16px grid. |
| T-31-10 | Repudiation | The Town/Cemetery retint decision | low | accept | Both retints are proven via `pink_scan.py` before/after (RED then GREEN), not merely asserted — the numeric proof is the record. |
</threat_model>

<verification>
1. `python3 scripts/build-art-assets.py` runs clean, producing 4 atlases + 12 parallax layers.
2. `python3 scripts/lib/pink_scan.py assets/tiles/atlas-town.png` → `< 0.08`.
3. `python3 scripts/lib/pink_scan.py assets/parallax/far-town.png` → `< 0.08`.
4. `python3 scripts/lib/pink_scan.py assets/parallax/far-cemetery.png` → `< 0.08`.
5. `bash scripts/check-pink-gate.sh` → PASS over the full, newly-expanded `assets/` tree.
</verification>

<success_criteria>
- All 4 biomes have a terrain atlas and 3 parallax layers, baked via the one extended `build-art-assets.py` pipeline.
- Both named pink offenders (Town sky, Cemetery horizon glow) are retinted and numerically proven clean.
- No generic/blind auto-slicing was used on any terrain sheet.
</success_criteria>

<output>
Create `.planning/phases/31-asset-bake-style-board-sign-off/31-04-SUMMARY.md` when done
</output>
