---
phase: 31-asset-bake-style-board-sign-off
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - .planning/research/v6-scouting/styleboard.py
  - .planning/research/v6-scouting/style-board-swamp.png
  - .planning/research/v6-scouting/style-board-town.png
  - .planning/research/v6-scouting/style-board-cemetery.png
  - .planning/research/v6-scouting/style-board-castle.png
  - .planning/research/v6-scouting/style-board-sheet.png
autonomous: false
requirements: [ART-01]

must_haves:
  truths:
    - "The regenerated style-board-town.png, style-board-cemetery.png, and style-board-castle.png renders show the Swamp Hunter sprite as the player (not Gothic Hero), matching CONTEXT.md's locked decision that Swamp Hunter ships across ALL 4 biomes."
    - "The regenerated style-board-castle.png shows the Hell hound sprite in place of the fire skull enemy."
    - "A human (parent, per CONTEXT.md's Phase-26/27 standard) has viewed the FRESH regenerated board at execute time and given an explicit, non-rubber-stamped sign-off — quoted verbatim — distinct from and in addition to today's discuss-phase round-1 kid quotes already recorded in CONTEXT.md."
    - "styleboard.py's compositing/tiling/badge-overlay logic for all 4 scenes is otherwise UNCHANGED — only the player/enemy sprite source references and the ROOT path constant were edited."
  artifacts:
    - ".planning/research/v6-scouting/styleboard.py (ROOT repointed at assets/_gothicvania-src/; player/enemy swaps applied)"
    - ".planning/research/v6-scouting/style-board-{swamp,town,cemetery,castle}.png (regenerated, overwritten in place)"
    - ".planning/research/v6-scouting/style-board-sheet.png (regenerated 2x2 contact sheet)"
  key_links:
    - "styleboard.py's ROOT constant must point at assets/_gothicvania-src/ (Plan 31-01's output) rather than the non-existent .../extracted/ directory it currently references, or every scene function throws FileNotFoundError."
    - "The human sign-off recorded here is what Plans 31-04/31-05 proceed on — per this phase's Primary Recommendation sequencing (RESEARCH.md), atlas/parallax/player/enemy baking does not start until this checkpoint is genuinely approved."
---

<objective>
Regenerate the 4 style-board mock renders (+ contact sheet) with the two CONTEXT.md-locked swaps — Swamp Hunter as the player in all 4 biome renders, Hell hound replacing the fire skull in the castle render — then get a fresh, genuine, non-rubber-stamped human sign-off on THIS regenerated board before any biome art is baked/vendored.

Purpose: today's discuss-phase kid feedback (captured in CONTEXT.md) was real and already changed the plan, but it reviewed the ORIGINAL pre-swap renders, not the final baked-decision artifact. Standing project precedent (Phases 25/27/28: never rubber-stamp a `checkpoint:human-verify` gate) requires a second, distinct round against the actual regenerated images. This plan is the hard gate every downstream baking task in this phase (Plans 31-04/31-05) waits on.
Output: 5 regenerated PNGs (4 biome renders + 1 contact sheet) and a recorded, quoted, genuine sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-asset-bake-style-board-sign-off/31-CONTEXT.md
@.planning/research/v6-scouting/ASSET-SCOUTING.md
</context>

<artifacts_produced>
- `.planning/research/v6-scouting/styleboard.py` — `ROOT` constant repointed; `town()`, `cemetery()`, `castle()` player-sprite loads switched from Gothic Hero to Swamp Hunter; `castle()` enemy-sprite load switched from Fire Skull to Hell hound
- `.planning/research/v6-scouting/style-board-swamp.png`, `style-board-town.png`, `style-board-cemetery.png`, `style-board-castle.png` — regenerated in place (same filenames, overwriting the pre-swap committed versions)
- `.planning/research/v6-scouting/style-board-sheet.png` — regenerated 2×2 contact sheet
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the style board with the Swamp Hunter + Hell hound swaps</name>
  <files>.planning/research/v6-scouting/styleboard.py, .planning/research/v6-scouting/style-board-swamp.png, .planning/research/v6-scouting/style-board-town.png, .planning/research/v6-scouting/style-board-cemetery.png, .planning/research/v6-scouting/style-board-castle.png, .planning/research/v6-scouting/style-board-sheet.png</files>
  <read_first>
    .planning/research/v6-scouting/styleboard.py (full file, 118 lines — every `load()` call inside `swamp()`, `town()`, `cemetery()`, `castle()`, plus the `ROOT`/`OUT`/`REPO` constants at the top)
    .planning/phases/31-asset-bake-style-board-sign-off/31-CONTEXT.md (the two locked swap decisions and the "Today's kid feedback... does NOT substitute for the execute-phase's formal checkpoint" paragraph)
  </read_first>
  <action>
    Edit `styleboard.py` in place (extend, never rewrite — CONTEXT.md/RESEARCH.md both require reusing this exact proven script). Make exactly these changes, nothing else in the compositing/tiling/badge logic:

    1. `ROOT` constant (currently `os.path.dirname(os.path.abspath(__file__)) + "/extracted"`, which does not exist): change it to reference Plan 31-01's actual extraction location, `assets/_gothicvania-src/`, computed relative to `REPO` (the script's own existing hardcoded absolute repo path constant) — i.e. join `REPO` with `"assets"` and `"_gothicvania-src"`, matching the exact directory names Plan 31-01 created. Do not touch `OUT` or `REPO` otherwise.

    2. `town()`: currently loads the player via `frame0(load(".../Gothic-hero-Files/PNG/gothic-hero-idle.png"), 4)`. Change this ONE load call to the Swamp Hunter's own idle sprite from the Swamp pack (`gothicvania_swamp_files/Gothicvania Swamp files/Sprites/Player/idle/idle1.png` — a single-frame PNG, not a multi-frame strip, so do not wrap it in `frame0(...)`; load it directly the way `swamp()`'s own player load already does). Adjust the `put_feet(c, hero, 300, 344)` call's variable name/position only if needed to keep the character standing on the visible ground line — do not otherwise alter town()'s compositing.

    3. `cemetery()`: same player swap as town() — replace the `frame0(load(".../Gothic-hero-Files/...gothic-hero-idle.png"), 4)` call with the same direct Swamp Hunter `idle1.png` load used in swamp()/town(). Leave the ghost enemy and all background/mountain/statue compositing untouched.

    4. `castle()`: apply BOTH swaps. Replace the Gothic Hero player load the same way as town()/cemetery(). Replace the enemy load — currently `frame0(load(".../Fire-Skull-Files/PNG/fire-skull.png"), 8)` — with the Hell hound's idle sprite from the Patreon Collection pack: `gothicvaniapatreoncollection/ gothicvania patreon collection/Hell-Hound-Files/PNG/hell-hound-idle.png` (a 6-frame horizontal strip, 384×32 total, 64×32 per frame — use `frame0(load(...), 6)` to take its first frame, mirroring how the existing `frame0(..., 8)` call already extracts one frame from a multi-frame strip). Leave the Old Dark Castle interior background/tileset compositing untouched.

    5. Do NOT touch `swamp()` — it already uses the Swamp Hunter player natively; no changes needed there.

    After editing, run the script (`python3 .planning/research/v6-scouting/styleboard.py`) from a working directory where it can resolve its own relative paths (it already computes `OUT`/`ROOT` from `__file__`, so it can be invoked from anywhere via its absolute path). Visually confirm each regenerated PNG — open `style-board-town.png`, `style-board-cemetery.png`, and `style-board-castle.png` and confirm the character standing in the scene reads as the Swamp Hunter (green/brown hunter with a bow/rifle silhouette, NOT the caped red/gold Gothic Hero), and that `style-board-castle.png`'s enemy reads as a hound/dog silhouette (NOT a floating fire skull). If a swapped sprite's feet don't align with the scene's ground line (`put_feet`'s `feet_y` parameter) because the Swamp Hunter's native canvas proportions (62×54) differ from the Gothic Hero's, adjust ONLY that call's `feet_y`/`x` arguments to re-align — do not change the surrounding scene composition to compensate.
  </action>
  <acceptance_criteria>
    - `grep -c "gothic-hero" .planning/research/v6-scouting/styleboard.py` returns 0 (every Gothic Hero reference removed).
    - `grep -c "fire-skull" .planning/research/v6-scouting/styleboard.py` returns 0 (the fire skull reference removed).
    - `grep -c "idle1.png" .planning/research/v6-scouting/styleboard.py` returns at least 3 (swamp/town/cemetery/castle all now load the Swamp Hunter's `idle1.png`; swamp already did).
    - `grep -c "hell-hound-idle" .planning/research/v6-scouting/styleboard.py` returns at least 1.
    - Running `python3 .planning/research/v6-scouting/styleboard.py` exits 0 and prints `built swamp`, `built town`, `built cemetery`, `built castle`, and `sheet done (...)` with no traceback.
    - All 5 output files' mtimes are newer than before this task ran (regenerated, not stale).
  </acceptance_criteria>
  <verify>
    <automated>python3 .planning/research/v6-scouting/styleboard.py</automated>
  </verify>
  <done>styleboard.py shows Swamp Hunter as the player in all 4 regenerated renders and Hell hound as the castle enemy, with every other compositing detail unchanged; the script runs clean with no traceback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off — regenerated style board (round 2, genuine, non-rubber-stamped)</name>
  <files></files>
  <action>
    Present the freshly regenerated style board to the parent (and kid, if available — a bonus per CONTEXT.md, not required) and get an explicit, specific round-2 confirmation. This is a real second round, not a rubber stamp of today's earlier discuss-phase feedback — CONTEXT.md is explicit that "liked the concept" during discuss does not equal "approved the final baked asset." Record whatever is said verbatim in the phase SUMMARY.md alongside the round-1 quotes already in CONTEXT.md. If any new objection surfaces (a different biome, a different palette note, anything beyond the two already-decided swaps), stop and route it back to CONTEXT.md/re-planning rather than proceeding to Plan 31-04's baking work.
  </action>
  <what-built>
    Task 1 regenerated `.planning/research/v6-scouting/style-board-swamp.png`, `style-board-town.png`, `style-board-cemetery.png`, `style-board-castle.png`, and the 2×2 `style-board-sheet.png` contact sheet — the same 640×360 mock-screen renders reviewed earlier today, now showing the Swamp Hunter as the player in ALL 4 scenes (previously Gothic Hero appeared in town/cemetery/castle) and the Hell hound in place of the fire skull in the castle scene.
  </what-built>
  <how-to-verify>
    1. Open `.planning/research/v6-scouting/style-board-sheet.png` (the 2×2 contact sheet, 1.5× scale matching the game's real CSS display scale) in an image viewer — this is the same file/URL format shown during today's discuss session.
    2. Confirm the SAME character (Swamp Hunter — a green/brown hunter silhouette) appears standing in all 4 scenes, not just swamp.
    3. Confirm the castle scene's enemy now reads as a hound/dog silhouette, not a floating skull.
    4. Confirm the 4 biomes still read as one coherent, dark, edgy game (not garish, not childish) — the same question asked during today's round 1.
    5. Ask explicitly: "Does this final version — with the hunter character in every level and the new castle monster — look right to ship?" Record the answer verbatim, including any hesitation or follow-up requests.
  </how-to-verify>
  <resume-signal>Type "approved" (with the verbatim quote to record) if the regenerated board is genuinely signed off, or describe the specific issue to route back to CONTEXT.md/re-planning.</resume-signal>
  <verify>
    <human-check>Human (parent, and kid if available) confirms in the regenerated `style-board-sheet.png`: Swamp Hunter appears as the player in all 4 biome scenes, the castle scene's enemy reads as a hound (not a fire skull), the 4 biomes still read as one coherent dark-edgy game, and gives an explicit, quoted, round-2 "looks right to ship" confirmation distinct from today's round-1 discuss-phase quotes.</human-check>
  </verify>
  <done>A genuine, verbatim-quoted round-2 human sign-off is recorded (in the phase SUMMARY.md) confirming the regenerated style board — Swamp Hunter in all 4 biomes, Hell hound in the castle biome — is approved to proceed to baking; or a specific defect is routed back to Task 1/CONTEXT.md instead.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Re-fetched local pack files (Plan 31-01) → styleboard.py compositing | Trusted local input by this point (Plan 31-01 already verified zip integrity + license); no new external-input boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-05 | Repudiation | The style-board human sign-off record | medium | mitigate | The sign-off is quoted VERBATIM in the phase SUMMARY.md, distinct from and in addition to CONTEXT.md's already-recorded round-1 quotes — matching this project's existing evidentiary standard (Phases 26/27) so the sign-off can never later be characterized as informal or assumed. |
| T-31-06 | Tampering | `styleboard.py` edits | low | accept | Internal planning/dev tooling, never shipped to players, git-tracked and reviewed via this plan's own acceptance criteria (`grep` checks confirming exactly the intended swaps and nothing else). |
</threat_model>

<verification>
1. `python3 .planning/research/v6-scouting/styleboard.py` runs clean, regenerating all 5 output files.
2. `grep -c "gothic-hero" .planning/research/v6-scouting/styleboard.py` → 0; `grep -c "fire-skull"` → 0.
3. A genuine, quoted, round-2 human sign-off is recorded in the phase SUMMARY.md, distinct from CONTEXT.md's round-1 quotes.
</verification>

<success_criteria>
- The regenerated style board shows Swamp Hunter in all 4 biomes and Hell hound in the castle biome.
- A real, non-rubber-stamped second sign-off round is recorded, quoted verbatim.
- No other compositing/tiling/badge logic in styleboard.py changed.
</success_criteria>

<output>
Create `.planning/phases/31-asset-bake-style-board-sign-off/31-02-SUMMARY.md` when done
</output>
