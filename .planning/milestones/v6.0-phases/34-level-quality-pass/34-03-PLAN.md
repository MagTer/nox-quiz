---
phase: 34-level-quality-pass
plan: 03
type: execute
wave: 3
depends_on: ["34-01", "34-02"]
files_modified:
  - src/levels/level-04.js
  - src/levels/level-05.js
  - src/levels/level-06.js
  - src/levels/level-07.js
  - scripts/smoke-progress.mjs
autonomous: true
requirements: [LVL-01]

must_haves:
  truths:
    - "Every coin in levels 04, 05, 06 and 07 is reachable — proven by the static coin model AND collected by a real driven player in the real engine."
    - "The coin fix moved COINS, never geometry: the floors and platforms arrays of all four levels are byte-identical to their pre-plan state."
    - "check-progress.sh stays green: smoke-progress.mjs's level-04 golden-geometry fixture is re-baselined to the new coin positions in the same commit that moves them."
  artifacts:
    - "src/levels/level-04.js, level-05.js, level-06.js, level-07.js: repositioned coins arrays"
    - "scripts/smoke-progress.mjs: re-baselined level-04 golden coins fixture"
  key_links:
    - "smoke-progress.mjs deep-equals the FULL geometry of levels 01-04 (coins included) against golden literals — moving a level-04 coin without re-baselining that fixture turns check-progress.sh red. Levels 05-08 have no golden fixture."
    - "The coin work list comes from `node scripts/validate-levels.mjs`'s coin-reachability HARD-FAIL rows (Plan 34-01's model, as tightened by Plan 34-02) — NOT from 34-CONTEXT.md's 32-coin alcove-model table, which over-reports."
---

<objective>
Move every unreachable coin in levels 04-07 onto the player's actual jump arc, by moving COINS ONLY — zero edits to `floors` or `platforms`. This is 34-CONTEXT.md decision 2, and it is what keeps the kid-validated platforming feel byte-identical and makes it impossible for geometry drift to hide inside a later art diff (Phase 35).

Purpose: LVL-01 — every pickup is actually reachable in play, landed in its own commit.
Output: four level descriptors whose coins all pass the static coin model AND get collected in-engine, with the level-04 golden fixture re-baselined so `check-progress.sh` stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-level-quality-pass/34-CONTEXT.md
@.planning/phases/34-level-quality-pass/34-01-SUMMARY.md
@.planning/phases/34-level-quality-pass/34-02-SUMMARY.md
@docs/LEVEL-DESIGN.md
</context>

<artifacts_produced>
- `src/levels/level-04.js`, `src/levels/level-05.js`, `src/levels/level-06.js`, `src/levels/level-07.js`: coins repositioned onto reachable arcs
- `scripts/smoke-progress.mjs`: level-04's golden-geometry coins fixture re-baselined to match
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Reposition the unreachable coins in levels 04-07 (coins only, zero geometry edits)</name>
  <files>src/levels/level-04.js, src/levels/level-05.js, src/levels/level-06.js, src/levels/level-07.js</files>
  <read_first>
    The work list is NOT a copied list from a SUMMARY — run `node scripts/validate-levels.mjs 2>/dev/null | grep 'coin-reachability | HARD-FAIL'` yourself and use ITS live output. (Read `.planning/phases/34-level-quality-pass/34-01-SUMMARY.md` and `34-02-SUMMARY.md` for context on how the model was derived and whether Plan 34-02 tightened it.)
    src/levels/level-04.js, level-05.js, level-06.js, level-07.js (each in full — the geometry shape, the coins array, and each level's inline authoring comments)
    src/levels/level-01.js lines 61-68 (the coin-coordinate convention comment: {x, y} is the coin's TOP-LEFT corner and the 32x32 sprite's visual CENTER sits ~16px right/down of it — coins are deliberately off-grid, authored visual positions, not grid coordinates)
    docs/LEVEL-DESIGN.md sections 1-3 (the physics envelope: maxRise 88.331px, ~162px flat reach; platform rise/overlap rules)
  </read_first>
  <action>
    Derive the work list from the validator itself: `node scripts/validate-levels.mjs 2>/dev/null | grep 'coin-reachability | HARD-FAIL'`. Every HARD-FAIL row for level-04/05/06/07 is a coin you must move. Do NOT use 34-CONTEXT.md's 32-coin table as the work list — it was produced by the secret-alcove POINT model and over-reports (34-CONTEXT.md decision 4 says so explicitly).

    For each offending coin, MOVE THE COIN. Edit ONLY the `coins` array. Do NOT add, remove, resize or reposition any entry in `floors` or `platforms` — not one number, in any of the four files (34-CONTEXT.md decision 2, binding). Do not touch spikes, checkpoints, doors, enemies, the goal, the secret alcove, or bounds either: this plan's entire diff footprint inside `src/levels/` is the four `coins` arrays.

    Reposition each offending coin onto a place the player actually goes:
    - The cheapest, most reliable reachable band is walking height over a floor run or platform: a coin whose `y` is roughly `surfaceY - 56` sits squarely in the player's walk-through box (the player's top-left is at `surfaceY - 32`, and the coin's pass-through box spans `coin.y - 32` .. `coin.y + 32`). The shipped `y: 264` floor coins are exactly this pattern (320 - 56).
    - For an arc coin, keep the same `x` where you can and lower it to a height a real jump from the nearest reachable surface actually passes through — the calibrated ceiling is a maxRise of 88.331px, so a coin more than ~88px above the highest reachable surface beneath it can never be touched, at any x.
    - Preserve each level's visual intent: coins arc OVER jumps and string along runs. Do not collapse a level's coins into a flat line to make the gate happy, and do not delete a coin to "solve" it — the count per level must not change.

    Update the inline comment next to any coin you move to say why it moved (a one-liner: previously unreachable, moved onto the arc from <surface>). The level files are the authoring record.

    Re-run `node scripts/validate-levels.mjs` after each level and iterate until levels 04-07 report zero `coin-reachability | HARD-FAIL` rows. The validator will still be RED overall at this point — levels 01/02/03/08 have not been fixed yet (Plans 34-04 and 34-05). That is expected; scope your green to the four levels this plan owns.
  </action>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs 2>/dev/null | grep 'coin-reachability | HARD-FAIL' | grep -cE 'level-0[4567]'` returns 0.
    - Zero geometry edits: `git diff -U0 -- src/levels/level-04.js src/levels/level-05.js src/levels/level-06.js src/levels/level-07.js | grep -E '^[+-][^+-]' | grep -c 'w:'` returns 0 (floors and platforms are the only arrays carrying a width key, so no changed line may contain one). <!-- planner-discipline-allow: w: -->
    - Coin counts unchanged: `node -e 'Promise.all([4,5,6,7].map(n=>import("./src/levels/level-0"+n+".js"))).then(ms=>{const c=ms.map(m=>Object.values(m)[0].geometry.coins.length); const ok=JSON.stringify(c)==="[19,15,15,17]"; console.log(ok?"COUNTS-OK":"COUNTS-CHANGED "+c); process.exit(ok?0:1);})'` prints `COUNTS-OK`.
    - `bash scripts/check-safety.sh && bash scripts/check-import-safety.sh` green.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validate-levels.mjs 2>/dev/null | grep 'coin-reachability | HARD-FAIL' | grep -cE 'level-0[4567]' | grep -qx 0 && node --input-type=module -e 'import{createHash}from"crypto";import{getLevel}from"./src/levels/index.js";const EXP={"level-04":"71d96d6331c28bbc","level-05":"5b2a2e2bb8e1b3f4","level-06":"2595472f98a44e46","level-07":"a19895f336b8036e"};let bad=0;for(const[id,want]of Object.entries(EXP)){const g=getLevel(id).geometry;const got=createHash("sha256").update(JSON.stringify({floors:g.floors??[],platforms:g.platforms??[]})).digest("hex").slice(0,16);if(got!==want){console.error(`GEOMETRY CHANGED ${id}: ${got} != ${want}`);bad++;}}process.exit(bad?1:0);' && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh</automated>
    <!--
      PLAN-CHECK FIX (FLAG-3 + FLAG-4). The old check was `git diff -U0 ... | grep -c 'w:'` — a
      PROXY for "geometry unchanged", broken two ways:
        FLAG-4 (timing): `git diff` compares the WORKING TREE against HEAD. If the executor
          commits Task 1 before running this verify, the diff is empty and the check passes
          VACUOUSLY — asserting nothing at all.
        FLAG-3 (proxy): it only holds because floor/platform objects happen to be single-line
          literals. A reformat would let a real geometry edit slip through.
      Replaced with an ABSOLUTE assertion: the floors+platforms arrays must hash to their
      exact pre-phase fingerprints (computed from the shipped descriptors at phase start).
      This is immune to commit timing and to formatting, and it pins the actual user decision
      ("move the coins, never the geometry") instead of a textual stand-in for it. Mirrors the
      deep-equal shape 34-04 already uses for level-08.
    -->
    <!-- planner-discipline-allow: w: -->
  </verify>
  <done>Levels 04-07 report zero unreachable coins under the coin model, and their floors/platforms arrays are byte-identical to their pre-plan state.</done>
</task>

<task type="auto">
  <name>Task 2: Re-baseline smoke-progress.mjs's level-04 golden-geometry fixture</name>
  <files>scripts/smoke-progress.mjs</files>
  <read_first>
    scripts/smoke-progress.mjs lines 300-400 (the recursive deepEqual, the golden-geometry block structure, the historical-values comment convention above each block) and lines 610-740 (the level-04 golden block and its `check(deepEqual(actual, expectedGeometry), ...)` assertion)
  </read_first>
  <action>
    `scripts/smoke-progress.mjs` deep-equals the FULL geometry of levels 01-04 — coins included — against golden literals. Moving a level-04 coin without updating that fixture turns `check-progress.sh` red. Update ONLY level-04's `expectedGeometry.coins` array in that file to the new, post-move values, lifted exactly from `src/levels/level-04.js`.

    Follow the file's own re-baseline convention (used at the Phase 24 re-baseline and again at Phase 29's density change): retain the OLD coin values in the block's header comment for historical traceability, and note the reason — Phase 34 LVL-01 coin-reachability fix, coins moved onto reachable arcs, zero geometry change.

    Do not touch levels 01/02/03's golden blocks — Plan 34-05 owns those, in its own commit. Do not touch level-04's floors/platforms/spikes/goal/checkpoints/doors/enemies golden values: if any of those changed, Task 1 broke its own zero-geometry-edit rule and must be corrected instead.
  </action>
  <acceptance_criteria>
    - `bash scripts/check-progress.sh` exits 0 (it ends with `smoke-progress.mjs`).
    - `git diff -U0 -- scripts/smoke-progress.mjs | grep -E '^[+-][^+-]' | grep -c 'w:'` returns 0 — no floor/platform golden value changed. <!-- planner-discipline-allow: w: -->
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-progress.sh && test "$(git diff -U0 -- scripts/smoke-progress.mjs | grep -E '^[+-][^+-]' | grep -c 'w:')" = "0"</automated>
  </verify>
  <done>check-progress.sh is green with the new level-04 coins, and the golden fixture's only change is the coins array.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the moved coins are collectable IN-ENGINE, not just in the graph</name>
  <files>(none — verification-only task)</files>
  <read_first>
    .planning/phases/34-level-quality-pass/34-02-SUMMARY.md (what audit-coins.mjs proves and what it deliberately does not)
  </read_first>
  <action>
    Static green is not proof — the full suite was green while the game shipped a sawtoothed floor, grey-static ground, and 32 unreachable coins. Run the in-engine gate and the driven boot:

    1. `node scripts/audit-coins.mjs` — every coin in levels 04-07 must now carry a witness (zero skipped-HARD-FAIL rows for those levels) and every one must be collected in the real engine. If a moved coin is witnessed but NOT collected in-engine, the model over-credited its new position (obstruction — a platform underside, most likely): move that coin again. Do not touch the gate.
    2. `node scripts/browser-boot.mjs` — the levels still boot and drive across all 8, with the per-level FPS/screenshot/object-budget assertions green.
    3. `node scripts/audit-phase21-mechanics.mjs` — every door/enemy encounter across all 8 levels still reports `triggered: true` (coins moved; mechanics must be unaffected). `resolved: false` rows are known headless-timing flakiness, not acceptance failures.

    Record per-level collected counts for 04-07 in the SUMMARY, citing the run output.
  </action>
  <acceptance_criteria>
    - `node scripts/audit-coins.mjs` exits 0 and reports zero skipped (model HARD-FAIL) coins for level-04/05/06/07.
    - `node scripts/browser-boot.mjs` exits 0.
    - `node scripts/audit-phase21-mechanics.mjs` output shows `triggered: true` for every door/enemy encounter across all 8 levels.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs</automated>
  </verify>
  <done>Every coin in levels 04-07 is collected by a real driven player in the real engine, the game still boots and drives across all 8 levels, and no mechanic regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure level-data edit. No new input, no new dependency, no runtime code change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-08 | Tampering (of kid-validated game feel) | floors/platforms arrays of levels 04-07 | high | mitigate | The single highest risk of this plan is a "small" geometry tweak sneaking in to make a coin reachable. Forbidden by 34-CONTEXT.md decision 2 and gated automatically: no changed line in any of the four descriptors may carry a width key, which floors and platforms alone possess. |
| T-34-09 | Tampering (silent scope loss) | the coins arrays | medium | mitigate | Deleting a coin would "fix" reachability by removing content. Gated: the per-level coin counts (19/15/15/17) are asserted unchanged. |
| T-34-10 | Spoofing (a green gate that lies) | validate-levels.mjs green | high | mitigate | Static green alone is explicitly not accepted: Task 3 requires `audit-coins.mjs` to collect every moved coin with a real driven player in the real engine, plus browser-boot and the mechanic audit. |
| T-34-11 | Denial of service (a red gate elsewhere) | smoke-progress.mjs level-04 golden fixture | medium | mitigate | The fixture deep-equals coins; Task 2 re-baselines it in the same plan/commit, and check-progress.sh gates it. |
</threat_model>

<verification>
1. `node scripts/validate-levels.mjs` reports zero `coin-reachability | HARD-FAIL` rows for levels 04-07 (the overall run stays red until Plans 34-04/34-05 land — expected).
2. No changed line in level-04/05/06/07 carries a width key — floors and platforms are byte-frozen.
3. Coin counts per level unchanged (19/15/15/17).
4. `bash scripts/check-progress.sh` green with the re-baselined level-04 fixture.
5. `node scripts/audit-coins.mjs` collects every level-04..07 coin in the real engine; `node scripts/browser-boot.mjs` green; `node scripts/audit-phase21-mechanics.mjs` shows every encounter triggered.
</verification>

<success_criteria>
- Levels 04-07 have zero unreachable coins, proven statically AND in-engine.
- The fix moved coins, not geometry — a later art diff cannot hide geometry drift here.
- All existing gates stay green; the level-04 golden fixture is honestly re-baselined rather than worked around.
</success_criteria>

<output>
Create `.planning/phases/34-level-quality-pass/34-03-SUMMARY.md` when done
</output>
