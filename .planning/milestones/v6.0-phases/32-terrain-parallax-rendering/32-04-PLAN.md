---
phase: 32-terrain-parallax-rendering
plan: 04
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - src/parallax.js
  - src/scenes/game.js
  - src/main.js
  - .planning/codebase/STACK.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [ART-02, ART-03]

must_haves:
  truths:
    - "Each biome shows a real multi-layer parallax background (sky + 2-3 detail layers) that moves with the camera, keyed off the level's biome field."
    - "The game boots and loads every biome-atlas/biome-bg sprite through the manifest-driven loop, not hand-written per-theme-N calls."
    - "No unused pack content ships: the old per-level tinted theme-N ground/parallax assets are deleted from disk once nothing references them."
  artifacts:
    - "makeParallaxLayers(bounds, biome) — src/parallax.js, parameter renamed from theme, zero other logic change"
    - "src/main.js manifest-driven load loop for biome-atlas/biome-bg entries, old ground/theme-N loads removed"
    - "32 deleted asset files: assets/tiles/ground-theme-{1..8}.png, assets/parallax/{far,mid,near}-theme-{1..8}.png"
  key_links:
    - "level.biome (Plan 32-02) -> src/scenes/game.js call site -> src/parallax.js layerName()"
    - "src/assets-manifest.js (Plan 32-01) -> src/main.js's biome-atlas/biome-bg load loop"
---

<objective>
Finish wiring biome-driven rendering end to end: thread `level.biome` through
`parallax.js`'s existing layer-naming logic (near-zero structural change), switch
`main.js`'s asset loading from the hand-written per-theme-N block to a manifest-driven
loop for the new biome assets, and retire the now-fully-superseded theme-N asset files.
Wave 2, depends on the manifest (32-01) and the CONFIG/biome-field foundation (32-02).

Purpose: `parallax.js` already does everything needed (3-layer build, ratio-based scroll,
bounds-driven tiling) — it only needs a new sprite-name source. `main.js`'s asset-loading
refactor is this phase's other genuinely new-build surface, alongside asset cleanup that
matches CONTEXT's locked "no unused pack content" decision.
Output: `src/parallax.js`, `src/scenes/game.js` (call site), `src/main.js`, 32 deleted
asset files, updated verification-gate docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-terrain-parallax-rendering/32-CONTEXT.md
@.planning/phases/32-terrain-parallax-rendering/32-RESEARCH.md
@.planning/phases/32-terrain-parallax-rendering/32-PATTERNS.md

@src/parallax.js
@src/scenes/game.js
@src/main.js
@src/assets-manifest.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread biome through parallax.js and its game.js call site</name>
  <files>src/parallax.js, src/scenes/game.js</files>
  <read_first>
    src/parallax.js (the full file — `makeParallaxLayers(bounds, theme)`'s `layerName`
    closure, lines 45-97, and `updateParallaxLayers`, lines 105-116, which needs ZERO
    change); src/scenes/game.js line 129, the `makeParallaxLayers(bounds, level.theme)`
    call site and its surrounding "Parallax background (Phase 18 ART-03)" comment
  </read_first>
  <action>
    In `src/parallax.js`: rename `makeParallaxLayers(bounds, theme)`'s second parameter
    from `theme` to `biome`, update its JSDoc `@param {string} [theme]` to `@param
    {string} [biome]` (and the one-line description to reflect a biome id like
    `"swamp"`), and update the `layerName` closure's `(theme ? ... : base)` ternary to
    reference `biome` instead — KEEP the ternary/fallback shape exactly as-is (matches
    the file's own existing per-key-defaulting style immediately above, in `safeBounds`),
    it degrades gracefully to the untinted `bg-far`/`bg-mid`/`bg-near` sprite names if
    `biome` is ever falsy. `makeParallaxLayer` and `updateParallaxLayers` need ZERO
    changes — do not touch them.

    <!-- planner-discipline-allow: level.theme -->
    In `src/scenes/game.js`, update line 129's `makeParallaxLayers(bounds, ...)` call
    site (named in the read-first note above) so its second argument reads the level's new
    biome field instead of the old, now-removed theme field — do not leave a
    migration-note comment naming the old argument. Update the one-line comment
    immediately above it ("Parallax background (Phase 18 ART-03)") to also note the
    Phase 32 biome-driven source, if it reads oddly otherwise — a small, optional clarity
    edit, not a functional one.
  </action>
  <acceptance_criteria>
    `grep -c "biome" src/parallax.js` returns at least `3` (JSDoc + parameter + ternary
    use). `grep -c "level.theme" src/scenes/game.js` returns `0`. `grep -c
    "level.biome" src/scenes/game.js` returns `1`. `node --check src/parallax.js` and
    `node --check src/scenes/game.js` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/parallax.js && node --check src/scenes/game.js && grep -c "level.biome" src/scenes/game.js</automated>
  </verify>
  <done>makeParallaxLayers reads a biome parameter (renamed from theme, same fallback shape); game.js's call site passes level.biome; makeParallaxLayer/updateParallaxLayers are byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Manifest-driven biome asset loading in main.js</name>
  <files>src/main.js</files>
  <read_first>
    src/main.js (the full sprite/sound-load section, lines 52-132 — especially the
    `loadSprite("ground", ...)` call at lines 62-64 and the `for (let n = 1; n <= 8;
    n++) { ... }` theme-N block at lines 98-110, both being replaced; every OTHER
    loadSprite/loadSound/loadMusic call in this range, which stays byte-unchanged);
    src/assets-manifest.js (the `kind` values this task's loop dispatches on:
    `"biome-atlas"` and `"biome-bg"` only — every other kind is intentionally NOT
    looped over here, since those assets are already hand-loaded elsewhere in this same
    file and looping them too would double-load)
  </read_first>
  <action>
    <!-- planner-discipline-allow: loadSprite("ground" -->
    <!-- planner-discipline-allow: for (let n = 1; n <= 8 -->
    In `src/main.js`: add `import { ASSETS_MANIFEST } from "./assets-manifest.js";`
    alongside the existing imports at the top of the file. DELETE the base-ground sprite
    load call named in the read-first note above (lines 62-64) in its entirety — after
    Plan 32-03's build.js change, no code path anywhere reads that sprite key any more
    (verified via grep at planning time: build.js's new atlas selection has no
    theme/ground fallback). DELETE the entire numeric per-theme-N loop block named in
    the read-first note above (lines 98-110) and its preceding "Per-level theme variants"
    comment, in its entirety. Do not leave any migration-note comment naming either
    deleted call/loop. In their place, add a loop over `ASSETS_MANIFEST`: for each entry
    `a`, build `webPath = \`../${a.path}\``; if `a.kind === "biome-atlas"`, call
    `loadSprite(a.key, webPath, { sliceX: 2, sliceY: 1 })` (matches `docs/LEVEL-DESIGN.md`
    §9's documented 32x32 two-frame atlas layout); else if `a.kind === "biome-bg"`, call
    `loadSprite(a.key, webPath)`; every other `kind` is skipped (no `else` branch that
    loads — those assets are loaded by the hand-written calls already elsewhere in this
    file, which you must NOT modify or duplicate). Keep EVERY other existing
    `loadSprite`/`loadSound`/`loadMusic` call in this file (spike, goal, player, coin,
    `bg-far`/`bg-mid`/`bg-near` untinted, title-bg, logo-hero, logo-badge, door,
    enemy-1/2/3, all 7 sfx, ambient music) byte-unchanged.
  </action>
  <acceptance_criteria>
    `grep -c 'loadSprite("ground"' src/main.js` returns `0`. `grep -c "for (let n = 1;
    n <= 8" src/main.js` returns `0`. `grep -c "ASSETS_MANIFEST" src/main.js` returns at
    least `2` (import + loop reference). `grep -c 'loadSprite("player"\|loadSprite("coin"\|loadSound("jump"'
    src/main.js` returns `3` (the hand-written calls survive untouched). `node --check
    src/main.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/main.js && grep -c "ASSETS_MANIFEST" src/main.js</automated>
  </verify>
  <done>main.js loads all 16 biome-atlas/biome-bg assets via a manifest-driven loop; the old ground/theme-N loads are gone; every other hand-written load is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Delete superseded theme-N asset files and update gate docs</name>
  <files>
    assets/tiles/ground-theme-1.png..ground-theme-8.png,
    assets/parallax/far-theme-1.png..near-theme-8.png,
    .planning/codebase/STACK.md, .claude/CLAUDE.md
  </files>
  <read_first>
    32-CONTEXT.md's "Rollout & Cleanup Scope" decision ("Delete the old per-level tinted
    theme-N ground/parallax assets... Git history preserves them if ever needed; no
    on-disk fallback kept"); .planning/codebase/STACK.md and .claude/CLAUDE.md's
    "Verification gates" bullet list (identical content in both — CLAUDE.md is generated
    from the codebase docs via GSD markers, so both need the same edit to stay in sync)
  </read_first>
  <action>
    Delete the 8 files `assets/tiles/ground-theme-1.png` through `ground-theme-8.png` and
    the 24 files `assets/parallax/far-theme-1.png` through `near-theme-8.png` (3 layers x
    8 themes) — 32 files total. Confirm via grep that nothing in `src/` references any
    `*-theme-N` sprite key or path any more (Task 2 already removed the only code path
    that did). Then, in BOTH `.planning/codebase/STACK.md` and `.claude/CLAUDE.md`, add
    one new bullet to the existing "Verification gates" list, immediately after the
    `check-progress.sh` line and before the `validate-levels.mjs` line (matching
    32-RESEARCH.md's own documented "Full suite command" ordering): `` `node
    scripts/check-assets-manifest.mjs` — asset manifest existence gate (every declared
    sprite/sound path exists on disk) ``.
  </action>
  <acceptance_criteria>
    `find assets/tiles -name "ground-theme-*.png" | wc -l` returns `0`. `find
    assets/parallax -name "*-theme-*.png" | wc -l` returns `0`. `grep -rn "theme-[1-8]"
    src/` returns zero matches. `grep -c "check-assets-manifest" .planning/codebase/STACK.md`
    returns `1`. `grep -c "check-assets-manifest" .claude/CLAUDE.md` returns `1`.
  </acceptance_criteria>
  <verify>
    <automated>find assets/tiles assets/parallax -name "*theme*" | wc -l; grep -rn "theme-[1-8]" src/ | wc -l</automated>
  </verify>
  <done>All 32 theme-N asset files are deleted, zero remaining references anywhere in src/, and both STACK.md and CLAUDE.md list the new check-assets-manifest.mjs gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Manifest data -> main.js loader | `ASSETS_MANIFEST` is a repo-committed constant (Plan 32-01); the loop only ever calls `loadSprite` with `key`/`path` values from that fixed array, never runtime input |
| Deleted asset files -> git history | Deletion is reversible via git; no destructive action outside version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | `src/main.js`'s manifest-driven load loop | low | accept | Loop iterates a fixed, repo-committed array; no dynamic path construction from user/runtime input |
| T-32-05 | Repudiation | Asset deletion (32 files) | medium | mitigate | Git history preserves every deleted file (per CONTEXT's own explicit "no on-disk fallback kept... git history preserves them" decision); the deletion is a normal, revertible git commit, not a destructive out-of-band operation |
| T-32-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new dependencies this plan |
</threat_model>

<verification>
`node --check src/parallax.js src/scenes/game.js src/main.js` (syntax); grep-based checks confirm the manifest loop, the deleted `ground`/theme-N loads, the 32 deleted files, and the updated gate-doc bullets. Full end-to-end proof (biome parallax actually visible + moving with camera) lands in Plan 32-05's browser-boot.mjs extension.
</verification>

<success_criteria>
parallax.js/game.js are biome-driven; main.js loads all biome assets via the manifest loop with zero duplicate/dead loads; the 32 superseded theme-N files are gone; both gate-doc copies list the new check-assets-manifest.mjs gate.
</success_criteria>

<output>
Create `.planning/phases/32-terrain-parallax-rendering/32-04-SUMMARY.md` when done
</output>

