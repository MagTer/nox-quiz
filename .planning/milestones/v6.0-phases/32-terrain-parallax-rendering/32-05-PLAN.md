---
phase: 32-terrain-parallax-rendering
plan: 05
type: execute
wave: 3
depends_on: ["32-02", "32-03", "32-04"]
files_modified:
  - scripts/browser-boot.mjs
autonomous: true
requirements: [ART-02, ART-03]

must_haves:
  truths:
    - "Frame rate holds and nothing renders blank at the far end of the longest level — proven by real headless-browser measurement, not assumed."
    - "The autotile fill's silent-blank-chunk trap and the object-count perf cliff are gates, not just documented pitfalls — a regression HARD-FAILs the suite."
    - "Every level's far end (the goal, authored near each level's true end) genuinely renders — not just the entry screen."
  artifacts:
    - "assertScreenshotNonBlank(), assertFpsFloor(), assertObjectBudget() helpers in scripts/browser-boot.mjs"
    - "Far-end non-blank proof via driveToXPlanned(page, level.geometry.goal.x, ...) for every level"
  key_links:
    - "CONFIG.TERRAIN (Plan 32-02) -> the three new assertion helpers' thresholds"
    - "\"ground-cap\"/\"ground-fill\" tags (Plan 32-03) -> assertObjectBudget()'s get() query"
    - "level.biome-driven rendering (Plan 32-03/32-04) -> what the screenshot/fps checks are actually proving"
---

<objective>
Extend the existing `scripts/browser-boot.mjs` per-level loop with the four checks
ROADMAP's Phase 32 success criterion #3 names directly: per-level screenshot non-blank,
FPS floor, far-end (goal) non-blank, and a terrain object-count budget — turning this
phase's Common Pitfalls (silent-blank fill chunk, perf cliff) into real, hard-failing
automated gates rather than documented risks. Wave 3, depends on the full rendering
pipeline (32-02/32-03/32-04) actually being live to test against.

Purpose: "checks that don't play the game lie" (CLAUDE.md's own verification standard) —
this is the phase's actual proof that the new terrain/parallax rendering works, holds
performance, and never silently blanks.
Output: `scripts/browser-boot.mjs` — 3 new assertion helpers + far-end drive, wired into
the existing per-level loop.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-terrain-parallax-rendering/32-CONTEXT.md
@.planning/phases/32-terrain-parallax-rendering/32-RESEARCH.md
@.planning/phases/32-terrain-parallax-rendering/32-PATTERNS.md
@.planning/phases/32-terrain-parallax-rendering/32-VALIDATION.md

@scripts/browser-boot.mjs
@scripts/lib/mechanic-drive.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add screenshot/FPS/object-budget assertion helpers and wire per-level entry checks</name>
  <files>scripts/browser-boot.mjs</files>
  <read_first>
    scripts/browser-boot.mjs (the full file — especially `assertAudioElementCount`, lines
    95-103, the exact `(page, errors, stopLabel)` signature and `errors.push({type,
    message})` shape every new helper mirrors; the per-level loop, lines 371-448,
    specifically the `assertAudioElementCount(page, errors, \`${LEVEL_ORDER[i]}: level
    entry\`);` call at line 391 — the insertion point for the three new per-entry
    checks); 32-PATTERNS.md's "scripts/browser-boot.mjs" section (the exact FPS-sampling
    and object-count-query source lines to reuse)
  </read_first>
  <action>
    Add `import { CONFIG } from "../src/config.js";` to the top imports (alongside the
    existing `LEVEL_ORDER, getLevel` and `deriveEncounters, driveToXPlanned,
    resolveIfBoxed` imports). Add three new helper functions, each following
    `assertAudioElementCount`'s exact `(page, errors, stopLabel)` signature and
    `errors.push({ type, message })` shape (never throw, so one failed level doesn't
    abort the rest of the drive):

    `assertScreenshotNonBlank(page, errors, stopLabel)` — `const buf = await
    page.screenshot();` then, if `buf.length < CONFIG.TERRAIN.MIN_SCREENSHOT_BYTES`, push
    `{ type: "render-blank", message: \`${stopLabel}: screenshot only ${buf.length} bytes
    (< ${CONFIG.TERRAIN.MIN_SCREENSHOT_BYTES} min) — likely a blank/silently-failed
    render\` }`.

    `assertFpsFloor(page, errors, stopLabel)` — `const fps = await page.evaluate(() =>
    debug.fps());` then, if `fps < CONFIG.TERRAIN.FPS_FLOOR`, push `{ type: "perf-fps",
    message: \`${stopLabel}: fps ${fps} < floor ${CONFIG.TERRAIN.FPS_FLOOR}\` }`.

    `assertObjectBudget(page, errors, stopLabel)` — `const count = await
    page.evaluate(() => get("ground-cap").length + get("ground-fill").length);` then, if
    `count > CONFIG.TERRAIN.OBJECT_BUDGET`, push `{ type: "perf-objects", message:
    \`${stopLabel}: terrain object count ${count} > budget
    ${CONFIG.TERRAIN.OBJECT_BUDGET}\` }`.

    Wire calls to all three into the existing per-level loop immediately after the
    existing `await assertAudioElementCount(page, errors, \`${LEVEL_ORDER[i]}: level
    entry\`);` line (still before the `drivableEncounters` loop): `await
    assertScreenshotNonBlank(page, errors, \`${LEVEL_ORDER[i]}: level entry\`);` then
    `await assertFpsFloor(page, errors, \`${LEVEL_ORDER[i]}: level entry\`);` then `await
    assertObjectBudget(page, errors, \`${LEVEL_ORDER[i]}: level entry\`);` — this samples
    right after the level has settled (the existing `await
    page.waitForTimeout(1500);` a few lines above already provides the settle window).
  </action>
  <acceptance_criteria>
    `grep -c "function assertScreenshotNonBlank\|function assertFpsFloor\|function
    assertObjectBudget" scripts/browser-boot.mjs` returns `3`. `grep -c "CONFIG.TERRAIN"
    scripts/browser-boot.mjs` returns at least `3`. `grep -c "debug.fps()"
    scripts/browser-boot.mjs` returns at least `1`. `grep -c 'get("ground-cap")'
    scripts/browser-boot.mjs` returns at least `1`. `node --check scripts/browser-boot.mjs`
    exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/browser-boot.mjs && grep -c "assertScreenshotNonBlank\|assertFpsFloor\|assertObjectBudget" scripts/browser-boot.mjs</automated>
  </verify>
  <done>Three new assertion helpers exist, mirror the file's own errors.push convention exactly, and are wired into the per-level loop right after level entry.</done>
</task>

<task type="auto">
  <name>Task 2: Add the far-end (goal) non-blank check</name>
  <files>scripts/browser-boot.mjs</files>
  <read_first>
    scripts/browser-boot.mjs's per-level loop (lines 371-448, especially the
    `drivableEncounters` for-loop at lines 423-440, which stops after the FIRST
    resolvable mechanic and `break`s — the far-end check is a SEPARATE drive, added
    after that loop, before the "Return to select" step); RESEARCH.md Pitfall 7 (why
    `driveToXPlanned` targeting `level.geometry.goal.x` is the correct, already-proven
    "far end" proxy — every level's goal is authored near its true far end by design)
  </read_first>
  <action>
    Immediately after the existing `drivableEncounters` for-loop completes (and before
    the `// Return to select so the next level can be chosen.` block), add a far-end
    drive: `await driveToXPlanned(page, level.geometry.goal.x, level.geometry);` (reusing
    the already-imported `driveToXPlanned` — no new driving logic), followed by `await
    assertScreenshotNonBlank(page, errors, \`${level.id}: far-end (goal)\`);` (reusing
    Task 1's helper). This proves the level's true far end genuinely renders (parallax +
    terrain fill), not just the entry screen — the highest-risk gap per RESEARCH.md
    Pitfall 7 ("a naive fixed-duration hold-right drive may not cover the full
    distance").
  </action>
  <acceptance_criteria>
    `grep -c "far-end (goal)" scripts/browser-boot.mjs` returns `1`. `grep -c
    "level.geometry.goal.x" scripts/browser-boot.mjs` returns at least `1`. `node --check
    scripts/browser-boot.mjs` exits 0. `node scripts/browser-boot.mjs` exits 0 (the full
    real-browser proof, all 8 levels, including the new far-end checks, genuinely
    passes).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/browser-boot.mjs; echo "exit:$?"</automated>
  </verify>
  <done>Every level's drive reaches its goal.x and passes a non-blank screenshot check there; node scripts/browser-boot.mjs exits 0 across all 8 levels with the full new check battery (screenshot, fps, object-budget, far-end) live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Headless browser (dev/CI tooling) -> local static server | `browser-boot.mjs` already binds its file server to loopback only and clamps served paths to the repo root (existing CR-02 mitigation, unchanged by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-02 | Denial of Service (client-local, perf) | `assertFpsFloor`/`assertObjectBudget` | high | mitigate | This task IS the mitigation for the perf-cliff threat identified in Plan 32-03 — a real, hard-failing (non-zero exit) automated gate against `CONFIG.TERRAIN.FPS_FLOOR`/`OBJECT_BUDGET`, not a documented risk left unchecked |
| T-32-06 | Repudiation | "level renders correctly" claims | medium | mitigate | Prior to this plan, "ground renders as a solid mass" / "parallax moves with camera" were code-review-only claims; `assertScreenshotNonBlank` + the far-end drive make them genuine, automated, repeatable proof across all 8 levels, matching this project's "checks that don't play the game lie" standard |
| T-32-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new dependencies this plan (Playwright already resolved dynamically, pre-existing) |
</threat_model>

<verification>
`node scripts/browser-boot.mjs` exits 0 across all 8 levels with screenshot/FPS/object-budget/far-end checks live. This is the phase's primary automated proof for ROADMAP success criteria #1-#3.
</verification>

<success_criteria>
browser-boot.mjs genuinely proves (not assumes) that terrain renders as a solid non-blank mass, parallax is visible, FPS holds, object count stays in budget, and every level's far end renders — across all 8 levels, in one real headless-browser run.
</success_criteria>

<output>
Create `.planning/phases/32-terrain-parallax-rendering/32-05-SUMMARY.md` when done
</output>
