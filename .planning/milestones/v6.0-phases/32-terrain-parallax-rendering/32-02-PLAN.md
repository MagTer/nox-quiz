---
phase: 32-terrain-parallax-rendering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.js
  - src/levels/level-01.js
  - src/levels/level-02.js
  - src/levels/level-03.js
  - src/levels/level-04.js
  - src/levels/level-05.js
  - src/levels/level-06.js
  - src/levels/level-07.js
  - src/levels/level-08.js
autonomous: true
requirements: [ART-02, ART-03]

must_haves:
  truths:
    - "Every level carries a fixed biome assignment (the Castlevania-arc calm->harsh mapping locked at v6.0 kickoff: levels 1-2 swamp, 3-4 town, 5-6 cemetery, 7-8 castle), so Plan 32-03/32-04's rendering code has a real, required field to key off."
    - "Level geometry arrays stay byte-identical — only a top-level descriptor field is touched, never anything inside `geometry: {...}`."
    - "No magic numbers: every new terrain-rendering tunable (chunk size, fill depths, perf budgets) lives in CONFIG.TERRAIN, none hardcoded in logic modules."
  artifacts:
    - "CONFIG.TERRAIN block in src/config.js (FILL_CHUNK_COLS, FLOOR_FILL_DEPTH_PX, PLATFORM_FILL_DEPTH_PX, OBJECT_BUDGET, FPS_FLOOR, MIN_SCREENSHOT_BYTES)"
    - "biome field (required, one of swamp/town/cemetery/castle) on all 8 src/levels/level-0N.js descriptors, replacing the removed theme field"
  key_links:
    - "CONFIG.TERRAIN -> src/levels/build.js (Plan 32-03 reads FILL_CHUNK_COLS/FLOOR_FILL_DEPTH_PX/PLATFORM_FILL_DEPTH_PX)"
    - "CONFIG.TERRAIN -> scripts/browser-boot.mjs (Plan 32-05 reads OBJECT_BUDGET/FPS_FLOOR/MIN_SCREENSHOT_BYTES)"
    - "level-0N.js biome field -> src/levels/build.js atlas selection AND src/scenes/game.js -> src/parallax.js layer selection (Plan 32-03/32-04)"
---

<objective>
Lay the two pieces of foundation data every other Phase 32 plan reads: the
`CONFIG.TERRAIN` tunables block, and a required `biome` field on all 8 level
descriptors (replacing the now-fully-superseded `theme` field). Pure config/data work,
Wave 1, no dependency on any other plan.

Purpose: give the autotile renderer (32-03) and the parallax/manifest wiring (32-04) a
single, CONFIG-driven source of truth for chunk size, fill depth, and perf budgets — and
give every level a real biome to key sprite selection off, per the level->biome mapping
already locked in `.planning/research/v6-scouting/ASSET-SCOUTING.md` and restated in
32-CONTEXT.md.
Output: `CONFIG.TERRAIN` (src/config.js), `biome` field on all 8 level descriptors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-terrain-parallax-rendering/32-CONTEXT.md
@.planning/phases/32-terrain-parallax-rendering/32-RESEARCH.md
@docs/LEVEL-DESIGN.md

@src/config.js
@src/levels/level-01.js
@src/camera.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the CONFIG.TERRAIN tunables block</name>
  <files>src/config.js</files>
  <read_first>
    src/config.js (the existing `PARALLAX` block, lines 91-99, and its surrounding
    "Visual tuning constants" section — the exact sibling-block shape to copy), src/camera.js
    (confirms `bounds.bottom` — default `CONFIG.LEVEL_BOTTOM` 360 — is the camera's
    lower clamp edge: `clamp(ny, top+halfH, bottom-halfH)`)
  </read_first>
  <action>
    Insert a new `TERRAIN` block into `src/config.js` immediately after the existing
    `PARALLAX: { ... }` block closes (after the `Y_ANCHOR: 320,` line's closing `},`) and
    before `TITLE_BG_Z: -100,` — same "Visual tuning constants" section, matching the
    file's existing sibling-block comment density. Six keys, each with an inline
    rationale comment: `FILL_CHUNK_COLS: 40` (count — max columns per `{tiled:true}` fill
    chunk; the spike-proven ceiling from `.planning/research/v6-scouting/SPIKE-FINDINGS.md`
    Spike B — an oversized chunk silently renders nothing); `FLOOR_FILL_DEPTH_PX: 64` (px —
    fill depth for floor runs; every shipped level's floor sits at `FLOOR_Y` 320 with
    `bounds.bottom` 360 [or the CONFIG.LEVEL_BOTTOM 360 fallback], a real 40px gap to the
    camera's lower clamp edge — 64px gives comfortable margin without a per-level bounds
    lookup); `PLATFORM_FILL_DEPTH_PX: 32` (px — shallow 2-tile fill for floating
    platforms, per 32-CONTEXT.md's "not a deep mass" decision); `OBJECT_BUDGET: 650`
    (count — hard-fail ceiling for the browser-boot per-level terrain object-count
    assertion; the spike measured ~410 objects safe at a synthetic 16-row stress depth,
    real levels land ~400-420 objects at this phase's actual ~2-4-row fill depth per
    32-RESEARCH.md's level-04 Metadata calculation — headroom set above that real max);
    `FPS_FLOOR: 45` (fps — hard-fail floor for the browser-boot `debug.fps()` sample; the
    spike's clean run was 58fps, comfortably below that to absorb headless-Chromium's
    measured pessimistic bias vs a real GPU); `MIN_SCREENSHOT_BYTES: 4000` (bytes — PNG
    screenshot byte-size floor used as a non-blank-render proxy in browser-boot; measured
    locally against this exact game: an `about:blank` page screenshots at ~2,667 bytes,
    a real pre-Phase-32 level-entry screenshot at ~8,735 bytes — 4,000 sits clear of the
    blank floor with margin below real content, and Phase 32's new terrain-mass+parallax
    content should only push real screenshots higher, never lower).
  </action>
  <acceptance_criteria>
    `grep -c "TERRAIN:" src/config.js` returns `1`. `grep -c "FILL_CHUNK_COLS\|FLOOR_FILL_DEPTH_PX\|PLATFORM_FILL_DEPTH_PX\|OBJECT_BUDGET\|FPS_FLOOR\|MIN_SCREENSHOT_BYTES" src/config.js` returns `6`. `node -e "import('./src/config.js').then(m => console.log(m.CONFIG.TERRAIN.FILL_CHUNK_COLS, m.CONFIG.TERRAIN.OBJECT_BUDGET, m.CONFIG.TERRAIN.FPS_FLOOR))"` prints `40 650 45`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "import('./src/config.js').then(m => { const t = m.CONFIG.TERRAIN; if (!t || t.FILL_CHUNK_COLS!==40 || t.OBJECT_BUDGET!==650 || t.FPS_FLOOR!==45 || t.FLOOR_FILL_DEPTH_PX!==64 || t.PLATFORM_FILL_DEPTH_PX!==32 || t.MIN_SCREENSHOT_BYTES!==4000) throw new Error('CONFIG.TERRAIN missing/wrong'); console.log('OK'); })"</automated>
  </verify>
  <done>src/config.js has a TERRAIN block with all 6 keys set to the documented values, inserted alongside PARALLAX in the "Visual tuning constants" section.</done>
</task>

<task type="auto">
  <name>Task 2: Replace theme with biome on all 8 level descriptors</name>
  <files>
    src/levels/level-01.js, src/levels/level-02.js, src/levels/level-03.js,
    src/levels/level-04.js, src/levels/level-05.js, src/levels/level-06.js,
    src/levels/level-07.js, src/levels/level-08.js
  </files>
  <read_first>
    src/levels/level-01.js (the exact `theme: "theme-1", // VIS-03 theme assignment —
    calm moss green, level 1 of 8` line and its position immediately before `parallax:
    null,` at the descriptor's top level, OUTSIDE `geometry: {...}`); 32-CONTEXT.md's
    "Level → Biome Mapping" decision (levels 1-2 swamp, 3-4 town, 5-6 cemetery, 7-8
    castle); RESEARCH.md Assumptions Log A2 and Open Question 1 (the `theme` field's only
    two consumers — `src/scenes/game.js` line 129's `makeParallaxLayers(bounds,
    level.theme)` and `src/levels/build.js` line 76's `groundSprite` ternary — are BOTH
    being replaced by biome-driven equivalents in Plan 32-03/32-04, and no other script
    or gate reads `.theme` — confirmed via a repo-wide grep at planning time, resolving
    RESEARCH's open question: `theme` is fully replaced, not kept alongside)
  </read_first>
  <action>
    In each of the 8 `src/levels/level-0N.js` files, locate that descriptor's existing
    per-level VIS-03 theme-assignment field — the single-line field named in the
    read-first note above, holding a quoted theme id and a matching comment, positioned
    immediately before `parallax: null,` at the descriptor's top level — and replace that
    entire line with a `biome: "<biome>", // Phase 32 (ART-02/ART-03) — level N of 8,
    Castlevania arc calm->harsh (levels 1-2 swamp, 3-4 town, 5-6 cemetery, 7-8 castle)`
    line, at the EXACT SAME position (never inside `geometry: {...}`). Assign per the
    locked mapping: level-01 and level-02 -> `"swamp"`; level-03 and level-04 ->
    `"town"`; level-05 and level-06 -> `"cemetery"`; level-07 and level-08 -> `"castle"`.
    This is a full replacement, not an additive field — after this task, zero references
    to the old field name remain anywhere in `src/levels/`, including in comments (do not
    leave a migration-note comment mentioning the old field by name). Do not touch
    anything inside any file's `geometry: { ... }` object — this task's only legal edit
    per file is that one line.
  </action>
  <acceptance_criteria>
    `grep -rn "theme:" src/levels/level-0*.js` returns zero matches. `grep -c "biome:"
    src/levels/level-01.js src/levels/level-02.js src/levels/level-03.js
    src/levels/level-04.js src/levels/level-05.js src/levels/level-06.js
    src/levels/level-07.js src/levels/level-08.js` shows exactly `1` per file (8 total).
    `grep -l 'biome: "swamp"' src/levels/level-01.js src/levels/level-02.js` matches both
    files; `grep -l 'biome: "town"' src/levels/level-03.js src/levels/level-04.js`
    matches both; `grep -l 'biome: "cemetery"' src/levels/level-05.js
    src/levels/level-06.js` matches both; `grep -l 'biome: "castle"'
    src/levels/level-07.js src/levels/level-08.js` matches both. `git diff -U0
    src/levels/level-0*.js | grep -E '^[+-]' | grep -v '^[+-][+-][+-]' | wc -l` equals
    `16` (exactly one removed + one added line per file, 8 files — proves nothing else in
    any file changed, geometry included). `node scripts/validate-levels.mjs` exits 0 with
    zero HARD-FAIL rows.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validate-levels.mjs; echo "exit:$?"</automated>
  </verify>
  <done>All 8 level descriptors carry a biome field (swamp/town/cemetery/castle per the locked mapping) in place of theme, geometry arrays byte-identical (validate-levels.mjs zero HARD-FAIL, git diff touches exactly 16 lines total across 8 files).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Repo commit -> shipped static bundle | CONFIG.TERRAIN values and level `biome` fields are hand-authored, git-reviewed constants — no runtime/user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | src/config.js CONFIG.TERRAIN values | low | accept | Plain numeric constants, hand-authored, git-reviewed; no injection surface |
| T-32-04 | Repudiation | src/levels/level-0N.js geometry integrity | medium | mitigate | The byte-identical-geometry requirement (success criterion #5) is enforced by this task's own acceptance criteria (git diff line-count check) plus `scripts/validate-levels.mjs`'s zero-HARD-FAIL gate — an accidental geometry edit while touching the theme->biome line is caught before commit, not discovered later |
| T-32-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new dependencies this plan |
</threat_model>

<verification>
`node scripts/validate-levels.mjs` exits 0, zero HARD-FAIL. `node -e` config sanity check confirms all 6 CONFIG.TERRAIN keys. Grep-based checks confirm zero remaining `theme:` references and exactly one `biome:` per level file, correctly mapped.
</verification>

<success_criteria>
CONFIG.TERRAIN exists with the 6 documented tunables; all 8 level descriptors carry the correct biome field in place of theme; geometry arrays are provably byte-identical.
</success_criteria>

<output>
Create `.planning/phases/32-terrain-parallax-rendering/32-02-SUMMARY.md` when done
</output>
