---
phase: 32-terrain-parallax-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets-manifest.js
  - scripts/check-assets-manifest.mjs
autonomous: true
requirements: [ART-02, ART-03]

must_haves:
  truths:
    - "Nothing in the game silently fails to load — every sprite/sound path the game references has a corresponding manifest entry, and a static gate proves the file exists on disk before it ships."
    - "The manifest is the single source of truth both the browser boot path and a Node dev-time gate consume — no build step, no parse round-trip."
  artifacts:
    - "src/assets-manifest.js exporting ASSETS_MANIFEST (a flat {key, path, kind} array, 38 entries)"
    - "scripts/check-assets-manifest.mjs (Node ESM gate script, process.exit(1) on any missing path)"
  key_links:
    - "src/assets-manifest.js -> scripts/check-assets-manifest.mjs (Node import, existence check per entry)"
    - "src/assets-manifest.js -> src/main.js (Plan 32-04 consumes ASSETS_MANIFEST's biome-atlas/biome-bg entries in a load loop)"
---

<objective>
Create the assets manifest — the single `{key, path, kind}` data source both a new Node
existence gate and (in Plan 32-04) `src/main.js`'s biome asset loader consume — and the
gate script that proves every declared asset path exists on disk before it ships. This is
Wave 1 foundation work with no dependency on any other Phase 32 plan.

Purpose: kill the silent-404 class (a wrong/renamed asset path failing invisibly at
runtime) across the FULL asset surface `src/main.js` already loads, not just the new
biome files — per 32-CONTEXT.md's locked "Assets Manifest" decision.
Output: `src/assets-manifest.js`, `scripts/check-assets-manifest.mjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-terrain-parallax-rendering/32-CONTEXT.md
@.planning/phases/32-terrain-parallax-rendering/32-RESEARCH.md

@src/main.js
@scripts/validate-levels.mjs
@scripts/check-progress.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the assets manifest (src/assets-manifest.js)</name>
  <files>src/assets-manifest.js</files>
  <read_first>
    src/main.js (every existing loadSprite/loadSound/loadMusic call — the enumeration
    source), src/levels/level-01.js (pure-data module shape: no engine globals, no
    imports needed beyond what's strictly required, one named export)
  </read_first>
  <action>
    Create `src/assets-manifest.js` as a PURE data module (zero imports, zero engine
    globals — importable by both Node and the browser with no side effects) exporting one
    named constant `ASSETS_MANIFEST`: a flat array of `{ key, path, kind }` objects, `path`
    values repo-root-relative (no leading `../`, matching the `assets/...` convention
    `src/main.js`'s existing loads already use before the `../` prefix is applied at the
    call site). Populate exactly 38 entries, grouped by `kind`:

    `kind: "biome-atlas"` (4 entries, Phase-31-baked 2-frame cap+fill atlases) — key
    `atlas-swamp` path `assets/tiles/atlas-swamp.png`; key `atlas-town` path
    `assets/tiles/atlas-town.png`; key `atlas-cemetery` path
    `assets/tiles/atlas-cemetery.png`; key `atlas-castle` path
    `assets/tiles/atlas-castle.png`.

    `kind: "biome-bg"` (12 entries, Phase-31-baked per-biome parallax layers) — for each
    of the 4 biomes (swamp, town, cemetery, castle) and each of the 3 layers (far, mid,
    near): key `bg-<layer>-<biome>` path `assets/parallax/<layer>-<biome>.png` (e.g. key
    `bg-far-swamp` path `assets/parallax/far-swamp.png`, through key `bg-near-castle` path
    `assets/parallax/near-castle.png`).

    `kind: "sprite"` (12 entries — plain single-argument loadSprite calls that stay
    hand-written in `main.js`; listed here ONLY for gate coverage, per Pitfall 5's full-
    surface mandate): key `bg-far` path `assets/parallax/far.png`; key `bg-mid` path
    `assets/parallax/mid.png`; key `bg-near` path `assets/parallax/near.png`; key `spike`
    path `assets/spike.png`; key `goal` path `assets/goal.png`; key `title-bg` path
    `assets/tiles/title-bg.png`; key `logo-hero` path `assets/logo-hero.png`; key
    `logo-badge` path `assets/logo-badge.png`; key `door` path `assets/door.png`; key
    `enemy-1` path `assets/enemy-1.png`; key `enemy-2` path `assets/enemy-2.png`; key
    `enemy-3` path `assets/enemy-3.png`.

    `kind: "sprite-anim"` (2 entries — need custom sliceX+anims, hand-written in
    `main.js`, gate-coverage only): key `player` path `assets/player.png`; key `coin` path
    `assets/coin.png`.

    `kind: "sound"` (7 entries, hand-written `loadSound` calls, gate-coverage only): key
    `jump` path `assets/sfx/jump.ogg`; key `land` path `assets/sfx/land.ogg`; key
    `correct` path `assets/sfx/correct.ogg`; key `wrong` path `assets/sfx/wrong.ogg`; key
    `door` path `assets/sfx/door.ogg` (same key string as the `sprite`-kind `door` entry
    above — Kaplay's sprite/sound namespaces are independent, `main.js` already reuses the
    literal `"door"` key for both a `loadSprite` and a `loadSound` call); key `clear` path
    `assets/sfx/clear.ogg`; key `pickup` path `assets/sfx/pickup.ogg`.

    `kind: "music"` (1 entry, hand-written `loadMusic` call, gate-coverage only): key
    `ambient` path `assets/music/ambient.ogg`.

    <!-- planner-discipline-allow: theme -->
    Do NOT add an entry for the base ground sprite or any of the now-superseded
    per-level-tinted paths named in 32-RESEARCH.md's Assumptions Log A2 — those are being
    removed from `main.js`'s loads entirely in Plan 32-04 (dead once
    `src/levels/build.js`'s biome-only atlas path lands in Plan 32-03). Do NOT add
    `enemy-hellhound.png`/`player-swamphunter.png` — per 32-RESEARCH.md Pitfall 5, those
    are Phase 33 scope, not yet loaded by `main.js`. Do not add any comment inside
    `src/assets-manifest.js` that names the excluded legacy asset family — the file
    should read as a clean, forward-only manifest.
  </action>
  <acceptance_criteria>
    `node -e "import('./src/assets-manifest.js').then(m => console.log(m.ASSETS_MANIFEST.length))"`
    prints exactly `38`. `node -e "import('./src/assets-manifest.js').then(m =>
    console.log(m.ASSETS_MANIFEST.filter(a=>a.kind==='biome-atlas').length,
    m.ASSETS_MANIFEST.filter(a=>a.kind==='biome-bg').length)"` prints `4 12`. `grep -c
    "^import" src/assets-manifest.js` returns `0` (zero imports — pure data, safe for
    both Node and browser). `grep -c "theme" src/assets-manifest.js` returns `0` (no
    theme-N entries).
  </acceptance_criteria>
  <verify>
    <automated>node -e "import('./src/assets-manifest.js').then(m => { if (m.ASSETS_MANIFEST.length !== 38) throw new Error('expected 38 entries, got ' + m.ASSETS_MANIFEST.length); console.log('OK'); })"</automated>
  </verify>
  <done>src/assets-manifest.js exists, exports ASSETS_MANIFEST with exactly 38 entries covering every biome-atlas/biome-bg asset plus every pre-existing main.js loadSprite/loadSound/loadMusic call (excluding ground/theme-N and Phase-33-scoped hellhound/swamphunter art), zero imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create the manifest existence gate (scripts/check-assets-manifest.mjs)</name>
  <files>scripts/check-assets-manifest.mjs</files>
  <read_first>
    scripts/validate-levels.mjs (Node ESM gate script header/import conventions, direct
    `src/` import, `process.exit()`-based reporting), scripts/check-progress.sh (the
    `fail()`-per-assertion idiom this script mirrors in `.mjs`/`console.error` form)
  </read_first>
  <action>
    Create `scripts/check-assets-manifest.mjs` as a standalone Node ESM CLI script
    (shebang `#!/usr/bin/env node`), NOT wired into `check-gate.sh` — same standalone
    convention `validate-levels.mjs` already established (23-CONTEXT.md's locked
    "Validator Check Design" decision). Import `existsSync` from `"fs"` and
    `ASSETS_MANIFEST` from `"../src/assets-manifest.js"`. Iterate every manifest entry;
    for each whose `existsSync(a.path)` is false, `console.error` a line in the form
    `check-assets-manifest: MISSING <key> -> <path>` and increment a `failures` counter.
    After the loop: if `failures === 0`, `console.log("check-assets-manifest: PASS —
    <N> assets verified on disk.")` (N = manifest length); `process.exit(failures > 0 ? 1
    : 0)`. Run from the repo root (matches every other script's invocation convention —
    `a.path` is repo-root-relative, no `path.resolve` gymnastics needed).
  </action>
  <acceptance_criteria>
    `node scripts/check-assets-manifest.mjs` exits 0 and prints a line containing `PASS`
    and `38 assets verified`. A temporary correctness proof (the gate must actually catch
    a genuine miss, not just always pass): copy `src/assets-manifest.js` to a scratch file
    with one entry's `path` changed to a nonexistent file (e.g.
    `assets/tiles/does-not-exist.png`), run `scripts/check-assets-manifest.mjs`'s logic
    against that scratch copy via a one-off `node -e` invocation importing the scratch
    file instead — confirm it reports exactly one `MISSING` line and exits 1, then delete
    the scratch file. This proves the gate is a real check, not a no-op that always exits
    0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-assets-manifest.mjs; echo "exit:$?"</automated>
  </verify>
  <done>scripts/check-assets-manifest.mjs exists, exits 0 with a PASS message against the real manifest (all 38 paths verified present on disk), and was proven to correctly fail (exit 1, one MISSING line) against a deliberately broken scratch copy during verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Repo commit -> shipped static bundle | Every asset path in the manifest is a hand-authored, git-reviewed constant — no runtime/user input ever reaches `path` |
| Dev machine -> Node gate script | `check-assets-manifest.mjs` runs locally/in CI against the local filesystem only; no network call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | `src/assets-manifest.js` path values | low | accept | Entries are hand-authored, repo-committed constants, never derived from runtime/user input; `existsSync()` operates on these fixed strings only — no path-traversal or injection surface, matches RESEARCH.md's Security Domain finding for this phase |
| T-32-02 | Information Disclosure | `check-assets-manifest.mjs` error output | low | accept | Error lines only ever echo repo-local asset key/path pairs already present in the committed manifest source — no secrets, no environment data |
| T-32-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new dependencies this plan (RESEARCH.md Package Legitimacy Audit: not applicable) — no install task exists to gate |
</threat_model>

<verification>
`node scripts/check-assets-manifest.mjs` exits 0 with a PASS line covering all 38 manifest entries. `node -e` sanity check confirms `ASSETS_MANIFEST.length === 38` and zero `theme` references.
</verification>

<success_criteria>
`src/assets-manifest.js` and `scripts/check-assets-manifest.mjs` exist, the gate passes green against the real filesystem, and was demonstrated to genuinely fail against a broken path (not a rubber-stamp always-pass script).
</success_criteria>

<output>
Create `.planning/phases/32-terrain-parallax-rendering/32-01-SUMMARY.md` when done
</output>
