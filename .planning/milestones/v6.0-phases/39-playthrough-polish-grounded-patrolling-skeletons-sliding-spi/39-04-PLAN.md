---
phase: 39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi
plan: 04
type: execute
wave: 2
depends_on: ["39-02"]
files_modified:
  - src/levels/level-01.js
  - src/levels/level-02.js
autonomous: true
requirements: [POL-01, POL-05]
must_haves:
  truths:
    - "Levels 1 & 2 skeletons walk on the floor with a visibly wide, faster ping-pong sweep, shifted off the floor coins; contact still respawns via the existing patroller seam (POL-01)."
    - "Levels 1 & 2 stay clearable by the walk-only browser-boot driver (a passing gap or a walkable lane remains between/around sweeps)."
    - "The L1/L2 alcove light (now the re-baked lantern) still sits within LINK_DIST of the secret alcove and lights on discovery (POL-05)."
  artifacts:
    - src/levels/level-01.js
    - src/levels/level-02.js
  key_links:
    - "geometry.patrollers is EXEMPT from the frozen hash — edit freely, no --write."
    - "The lantern prop must stay within CONFIG.AMBIENT.LINK_DIST (96px) of secretAlcove@320,184 so the alcove-light link survives."
---

<objective>
Ground and enliven the Level 1 & 2 skeletons (POL-01) and confirm the re-baked lantern's alcove link (POL-05) — the swamp-biome pair. This is a freeze-EXEMPT, level-data-only plan touching two files.

Purpose: Skeletons currently HOVER at y:214 over walk-lanes directly above floor coins, reading as "a stationary skeleton floating on a coin." The user's locked decision #4 (2026-07-19): skeletons = ground patrol, visible sweep, off the coins.
Output: Grounded, wider, faster patroller waypoints on L1/L2; verified lantern alcove link.

Spec-less probe fallback (VISIBLE SKIP): no SPEC/probe predicates; truths derived from POL-01/POL-05 text + CONTEXT.md decisions #1 and #4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/CONTEXT.md
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md
@.claude/CLAUDE.md
@src/levels/level-01.js
@src/levels/level-02.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ground + widen the L1/L2 skeleton patrols, off the coins</name>
  <read_first>
    - src/levels/build.js:580-595 (patroller factory — waypoints/speed/ping-pong; leave AS-IS) to understand what the data drives
    - src/levels/level-01.js:245 (patrollers block, currently `{ x1:1770, y1:214, x2:1880, y2:214 }`) AND the level-01 coins near y:264 on that lane
    - src/levels/level-02.js:323,329 (two patrollers 900-1000 and 4560-4660, both y:214) AND nearby coins
    - src/config.js:296 (CONFIG.PATROLLER.SPEED default 40 — per-patroller `speed` override allowed)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (POL-01 section: skeleton frame 44×52 → feet on FLOOR_Y(320) means waypoint y ≈ 320-52 = 268; verify against sprite anchor)
  </read_first>
  <action>
    For each L1/L2 patroller: lower `y1`/`y2` from 214 to a GROUNDED value (skeleton frame 44×52 → feet on FLOOR_Y 320 ⇒ y ≈ 268; verify against the sprite anchor and nudge so feet visually rest on the floor line). WIDEN the sweep (increase `x2-x1` from ~100px to a visible ~200-320px walk) and add a per-patroller `speed` override well above the 40 default (e.g. 70-90) so it reads as a walking sweep. SHIFT x so the sweep is NOT parked over a floor coin (check coin x on that lane and offset). Keep contact = respawn (already wired at game.js:277 — do NOT touch). CRITICAL clearability: a grounded skeleton across a walk-lane now genuinely blocks — leave a passing gap or keep the lane walkable so the walk-only browser-boot driver still crosses (this is exactly why they were hovered originally).
    - L1: 1 patroller (level-01.js:245).
    - L2: 2 patrollers (level-02.js:323, :329).
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` zero HARD-FAIL for L1/L2.
    - `node scripts/check-geometry-frozen.mjs` GREEN — patrollers are EXEMPT, so these edits do NOT change the frozen hash (proves no accidental frozen-array edit).
    - Source assertion: no patroller in level-01.js/level-02.js still has `y1:214`/`y2:214` (`grep -c '214' src/levels/level-01.js` on patroller lines is 0); sweeps are wider than the original ~100px.
    - `node scripts/browser-boot.mjs` crosses L1 and L2 spawn→goal without a softlock (clearability preserved).
  </acceptance_criteria>
  <done>L1/L2 skeletons walk on the floor with a wide visible sweep off the coins; levels validate, stay frozen-hash-neutral, and remain walk-clearable.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the L1/L2 lantern alcove link survives the re-bake</name>
  <read_first>
    - src/levels/level-01.js:287 AND src/levels/level-02.js:377 (`{ sprite:"prop-swamp-lantern", x:320, y:242, layer:"surface" }`)
    - src/levels/level-01.js:212 / level-02.js:280 (`secretAlcove: [{ x:320, y:184 }]`)
    - src/levels/build.js:490-527 (LIGHT_RE selector + alcove-light link + flicker) AND src/config.js:329 (CONFIG.AMBIENT.LINK_DIST = 96)
  </read_first>
  <action>
    The re-baked lantern (plan 39-02) keeps the same key `prop-swamp-lantern` and coords, so no data change is required for the link — but VERIFY: the prop still references `prop-swamp-lantern` at `{x:320,y:242,layer:"surface"}`, within LINK_DIST (96px) of `secretAlcove@320,184` (distance = 58px < 96 ✓). If a coordinate nudge was needed for legibility, keep it within 96px of the alcove. Do NOT change the key (it must keep matching LIGHT_RE /lantern|lamp|candle/).
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'prop-swamp-lantern' src/levels/level-01.js` ≥ 1 and same for level-02.js; the prop is within 96px of secretAlcove@320,184.
    - `node scripts/validate-levels.mjs` zero HARD-FAIL.
    - Interactive (deferred to plan 39-08 human-verify): the alcove light starts dim and brightens on discovery.
  </acceptance_criteria>
  <done>The lantern prop still links to the secret alcove within LINK_DIST; key unchanged; levels validate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser localStorage → game | Only external surface; no network/backend/auth. |

## STRIDE Threat Register (proportionate — ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `localStorage` progress key | low | accept | Guarded `src/progress.js` seams already mitigate. Level-data edits only — no new surface. |

No package-manager installs. No new external threat surface.
</threat_model>

<verification>
- `node scripts/validate-levels.mjs` zero HARD-FAIL.
- `node scripts/check-geometry-frozen.mjs` green (EXEMPT edits are hash-neutral).
- `node scripts/browser-boot.mjs` crosses L1/L2 without softlock.
</verification>

<success_criteria>
- L1/L2 skeletons are grounded, wide-sweeping, off the coins, still clearable; the lantern alcove link is intact.
</success_criteria>

<output>
Create `.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-04-SUMMARY.md` when done.
</output>
