---
phase: 39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi
plan: 08
type: execute
wave: 3
depends_on: ["39-04", "39-05", "39-06", "39-07"]
files_modified:
  - scripts/fixtures/geometry-frozen-baseline.json
autonomous: false
requirements: [POL-01, POL-02, POL-03, POL-04, POL-05]
must_haves:
  truths:
    - "After all FROZEN edits (plan 39-07's L8 stepping-stone removals), scripts/fixtures/geometry-frozen-baseline.json is re-written via check-geometry-frozen.mjs --write and committed as the deliberate acknowledgment, and the gate is GREEN against it (POL-03)."
    - "The full gate suite + browser-boot + audit-phase21 pass across all 8 levels with the new hazards and relocated movers (POL-01..05)."
    - "A human interactively confirms the revised platforming feels right and no level softlocks — and the Phase 38 VER-02 kid-UAT is queued to RE-RUN after this phase."
  artifacts:
    - scripts/fixtures/geometry-frozen-baseline.json
  key_links:
    - "The re-baseline MUST run only AFTER every frozen edit is complete (all in plan 39-07) — it is its own clearly-marked task, not interleaved."
    - "Verification standard (CLAUDE.md): no phase closes on automation alone — interactive proof + human sign-off; VER-02 kid-UAT re-run is a POST-execution manual step."
---

<objective>
Close the phase: acknowledge the plan-39-07 frozen-geometry changes by re-writing and committing the frozen baseline, run the FULL consolidated gate suite across all 8 levels, and gate the phase on interactive human proof. This plan is deliberately SEPARATE from the data plans so the --write re-baseline runs exactly once, after all frozen edits are settled.

Purpose: POL-03's deliberate check-geometry-frozen.mjs --write acknowledgment + the CLAUDE.md verification standard (interactive proof + human sign-off; VER-02 kid-UAT must re-run after this phase).
Output: A committed new geometry-frozen-baseline.json, a green full-suite run, human sign-off, and a queued VER-02 re-run note.

Spec-less probe fallback (VISIBLE SKIP): no SPEC/probe predicates; truths derived from POL-01..05 outcomes + CONTEXT.md gate-suite section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/CONTEXT.md
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-baseline the frozen geometry (deliberate acknowledgment)</name>
  <read_first>
    - scripts/check-geometry-frozen.mjs (the --write path + the slidingSpikes strip added in plan 39-01)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-07-SUMMARY.md (confirm all L8 frozen edits are complete)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/CONTEXT.md (Frozen-geometry gate section)
  </read_first>
  <action>
    Confirm plans 39-04..39-07 are all complete and the ONLY frozen changes are plan 39-07's stepping-stone removals (patrollers/props/slidingSpikes/mover edits in 39-04..06 are EXEMPT and hash-neutral). Read 39-07's SUMMARY for the WARNING-1 cross-level scan result: the expected frozen-diff scope is level-08 ONLY, unless 39-07's scan found (and the coordinator approved extending to) another stepping-stone-bridged level. Then run `node scripts/check-geometry-frozen.mjs --write` to re-write scripts/fixtures/geometry-frozen-baseline.json capturing the settled frozen state. Inspect the diff: it must show ONLY the level(s) 39-07 removed stepping-stones from (L8 + any WARNING-1-extended level) changing — if any OTHER level's hash changed, STOP and investigate (an unintended frozen edit leaked in). Commit the new baseline.
  </action>
  <verify>
    <automated>node scripts/check-geometry-frozen.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-geometry-frozen.mjs` (no flag) GREEN against the re-written baseline.
    - `git diff` of geometry-frozen-baseline.json shows ONLY the level(s) 39-07 removed stepping-stones from changed (level-08, plus any WARNING-1-extended level); all other levels byte-identical.
    - The new baseline is committed.
  </acceptance_criteria>
  <done>The frozen baseline reflects L8's removed stepping-stones and nothing else; the gate is green.</done>
</task>

<task type="auto">
  <name>Task 2: Full consolidated gate suite across all 8 levels</name>
  <read_first>
    - .claude/CLAUDE.md (Verification gates list)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/CONTEXT.md (Gate suite to run)
  </read_first>
  <action>
    Run the FULL gate suite and confirm every one green: check-gate.sh, check-safety.sh, check-import-safety.sh, check-progress.sh, check-assets-manifest.mjs, check-terrain-atlas.sh, validate-levels.mjs, check-geometry-frozen.mjs, browser-boot.mjs, audit-phase21-mechanics.mjs. Fix any red gate (or route back to the owning plan). browser-boot must cross ALL 8 levels including L8's mover-bridged pits; audit-phase21 mover + sliding-spike encounters must report `triggered: true` (a `resolved:false` row is acceptable known headless flakiness).
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && node scripts/check-assets-manifest.mjs && bash scripts/check-terrain-atlas.sh && node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Every listed gate exits 0 (green).
    - browser-boot.mjs crosses all 8 levels spawn→goal without softlock (incl. L8 riding movers over the moat + chasm).
    - audit-phase21-mechanics.mjs reports `triggered: true` for every mover, patroller, and sliding-spike encounter.
  </acceptance_criteria>
  <done>The full consolidated suite is green across all 8 revised levels.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human interactive playthrough sign-off (all 8 levels)</name>
  <action>Pause for a blocking human verification gate (no code change) — serve the game and walk the human through the playthrough steps below; do not close the phase until they approve.</action>
  <what-built>The full Phase 39 revision: grounded, wide-sweeping skeletons on all 8 levels; ground-sliding spikes on L5/L7; L8 movers spanning the real moat + drawbridge chasm (stepping-stones removed); 6 solid town barrels/crates; lantern (not skull) on L1/L2; well off the L3 spike; L8 columns off the gaps/spawn.</what-built>
  <how-to-verify>
    1. Serve the game: `python3 -m http.server 8000` from the repo ROOT, then browse to `http://localhost:8000/src/index.html` (add `?debug=1` to see colliders).
    2. Play each level spawn→goal. Confirm on every level: skeletons WALK on the floor (not floating), read as real telegraphed hazards, and are passable (a gap or timing exists) — no softlock, no dead end.
    3. L5 & L7: confirm a spike SLIDES horizontally along the ground and touching it respawns (gentle, no game-over).
    4. L8: confirm the moving platforms carry you across the moat and the drawbridge chasm, a missed hop drops you to a respawn (reachable checkpoint), and there is NO route you cannot re-traverse.
    5. L3/L4: confirm the barrels/crates are SOLID and jump-over-able and do not block the only path.
    6. L1/L2: confirm the secret-alcove light is a lantern (no skull) and still lights on discovery. L3: the well is clear of the spike. L8: the face-columns read as intentional wall dressing, not floating over gaps.
    7. Confirm ADHD-safe: no timers/countdowns anywhere; every hazard is respawn-only.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe what feels wrong / where a level softlocks or reads badly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser localStorage → game | Only external surface; no network/backend/auth. |

## STRIDE Threat Register (proportionate — ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `localStorage` progress key | low | accept | Guarded `src/progress.js` seams already mitigate. This plan re-baselines a fixture + runs gates — no runtime/storage surface. |

No package-manager installs. No new external threat surface.
</threat_model>

<verification>
- `node scripts/check-geometry-frozen.mjs` green against the re-written baseline (only L8 changed).
- Full gate suite green; browser-boot crosses all 8 levels; audit-phase21 encounters trigger.
- Human interactive sign-off on the revised platforming (no softlock, hazards read correctly, ADHD-safe).
</verification>

<success_criteria>
- Frozen baseline re-written + committed (L8-only diff); full suite green; human-approved.
- POST-execution manual step queued: RE-RUN Phase 38 VER-02 kid-UAT — it now covers this revised platforming (grounded skeletons, sliding spikes, mover-bridged pits, solid props). This phase does not truly close until VER-02 is re-run and signed off.
</success_criteria>

<output>
Create `.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-08-SUMMARY.md` when done. Record the VER-02 kid-UAT re-run as an outstanding post-execution manual step in STATE.md / the phase closeout.
</output>
