---
phase: 39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/browser-boot.mjs
autonomous: true
requirements: [POL-03]
must_haves:
  truths:
    - "The walk-only spawn→goal driver (browser-boot.mjs) crosses a mover-bridged pit by invoking the SHARED driveToMover (mechanic-drive.mjs:867) — so once a mover spans a real hole (plan 39-07) it can still cross without a softlock (POL-03)."
    - "audit-phase21-mechanics.mjs ALREADY rides a mover for its mover encounters via the same shared driveToMover (reached through audit-retry.mjs) — verified, not re-implemented."
    - "The mover-ride capability lives ONCE in the shared scripts/lib/mechanic-drive.mjs and is reached by both harnesses via import — nothing new is extracted and no ride branch is hand-copied (CLAUDE.md Playwright-duplication rule satisfied by NOT copying)."
  artifacts:
    - scripts/browser-boot.mjs
  key_links:
    - "The ride capability is the SHARED scripts/lib/mechanic-drive.mjs driveToMover (:867): walk to a staging point left of the near endpoint, mount, ride, detect grounded platform-moving frames. browser-boot reaches it by extending its EXISTING line-12 import; audit-phase21 reaches it via audit-retry.mjs (imports driveToMover at :42, drives it at :162)."
    - "Playwright duplication is about COPIED server/guard code — the DRIVER is a shared imported lib, so the ride capability is single-sourced and must NOT be copied into either harness."
---

<objective>
Make the WALK-ONLY spawn→goal driver cross a mover-bridged pit — by wiring it to the SHARED mover-ride driver, not by copying a recipe. `browser-boot.mjs` presses ArrowRight and holds; today its spawn→goal path does not board a mover, so the moment plan 39-07 relocates a mover over a real killing pit (removing the static stepping-stones), it would fall to respawn forever and the no-softlock proof would break. The ride capability ALREADY EXISTS as the shared `driveToMover` in `scripts/lib/mechanic-drive.mjs:867`; this plan invokes it from browser-boot's drive at a mover-bridged pit. The interactive audit (`audit-phase21-mechanics.mjs`) ALREADY rides movers through that same shared driver (via `audit-retry.mjs`) and needs no re-implementation — only verification.

Purpose: POL-03's core promise (movers over real holes) is only verifiable if the walk-only harness can cross a mover-bridged pit. Capability first, data second.
Output: browser-boot.mjs's spawn→goal drive calls the shared `driveToMover` to cross a mover-bridged pit (extending its existing line-12 import; no copied ride branch). Confirmation that audit-phase21 already crosses via the same shared driver.

Spec-less probe fallback (VISIBLE SKIP): no SPEC/probe predicates; truths derived from POL-03 text + CONTEXT.md decision #3 (harness update).

NOTE: full cross-a-real-hole verification cannot happen here (no level has a mover over a real hole until plan 39-07). This plan verifies the ride CAPABILITY is reachable/inert against the CURRENT (stepping-stone-bridged) levels; the real-hole crossing is verified end-to-end in plan 39-08.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/CONTEXT.md
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md
@.claude/CLAUDE.md
@scripts/browser-boot.mjs
</context>

## Artifacts this phase produces (declared for source-grounding)
- A mover-bridged-pit crossing call in `scripts/browser-boot.mjs`'s spawn→goal drive that invokes the SHARED `driveToMover` (added to the existing line-12 `mechanic-drive.mjs` import). No new driver code — a wiring call to the shared lib.

<tasks>

<task type="auto">
  <name>Task 1: Wire browser-boot's spawn→goal drive to the shared driveToMover at a mover-bridged pit</name>
  <read_first>
    - scripts/lib/mechanic-drive.mjs:867 (driveToMover(page, encounter, geometry) — the SHARED ride capability: walk to a staging point, mount, ride, detect grounded platform-moving frames) — READ, do NOT edit (it is 39-01's file; you only CALL it)
    - scripts/browser-boot.mjs:12 (the existing `import { deriveEncounters, driveToXPlanned, resolveIfBoxed } from "./lib/mechanic-drive.mjs"` — EXTEND this line) AND the spawn→goal drive loop (~:486-500 driveToXPlanned per encounter / the ArrowRight-hold path) — the file to edit
    - src/config.js:279-286 (CONFIG.MOVER — WIDTH 48, PERIOD_S 4)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (POL-03 "Harness — teach the drivers to RIDE a mover")
  </read_first>
  <action>
    In browser-boot.mjs, ADD `driveToMover` to the EXISTING line-12 import from `./lib/mechanic-drive.mjs` (do NOT add a second import line, do NOT copy the function body). In the spawn→goal drive: when the planned path reaches a gap bridged only by a `geometry.movers` entry (both floor edges are real pit, no static platform between), call the shared `driveToMover(page, moverEncounter, level.geometry)` to ride across, then resume the ArrowRight walk. Keep the existing walk-only path unchanged for levels with no mover-bridged pit. The ride LOGIC stays single-sourced in mechanic-drive.mjs — browser-boot only invokes it (CLAUDE.md's Playwright-duplication rule is about COPIED server/guard code, not the shared imported driver).
  </action>
  <verify>
    <automated>node scripts/browser-boot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/browser-boot.mjs` GREEN across all 8 levels (no regression — no level has a mover-over-hole yet, so the walk path still succeeds; the new cross-via-driveToMover branch is inert until a mover bridges a real pit).
    - Source assertion (NO duplicate/copied driver): `grep -cE '^\s*(async\s+)?function driveTo|const driveTo\s*=' scripts/browser-boot.mjs` stays 0 (no inline driver DEFINITION is copied in) AND no NEW `import ... from "./lib/mechanic-drive.mjs"` line is added — `driveToMover` joins the existing line-12 import (`grep -c 'from "./lib/mechanic-drive.mjs"' scripts/browser-boot.mjs` stays 1). (NOTE: `grep -c "mechanic-drive" scripts/browser-boot.mjs` is already 3 via the import + comments and legitimately stays ≥3 — do NOT assert it is 0.)
    - Source assertion: `driveToMover` now appears in browser-boot.mjs's import and is invoked in the drive (`grep -c 'driveToMover' scripts/browser-boot.mjs` ≥ 2).
  </acceptance_criteria>
  <done>browser-boot's spawn→goal drive crosses a mover-bridged pit by calling the shared driveToMover; the walk-only path is unchanged for non-mover levels; nothing is copied; boot green on all 8 levels.</done>
</task>

<task type="auto">
  <name>Task 2: Verify audit-phase21 already crosses mover gaps via the shared driveToMover (no re-implementation)</name>
  <read_first>
    - scripts/audit-phase21-mechanics.mjs:34 (`import { auditLevelWithRetries } from "./lib/audit-retry.mjs"` — it carries NO inline ride block) and its encounter dispatch
    - scripts/lib/audit-retry.mjs:37-44 (imports driveToMover from ./mechanic-drive.mjs) and :162 (drives it) — this is how the audit rides movers through the SHARED driver
    - scripts/lib/mechanic-drive.mjs:867 (driveToMover — the shared capability both harnesses reach)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (Harness-parity mandate)
  </read_first>
  <action>
    This is a VERIFICATION task — audit-phase21-mechanics.mjs already rides movers via the SHARED `driveToMover` (mechanic-drive.mjs:867) reached through `audit-retry.mjs` (imports it at :42, drives it at :162). Do NOT port a ride branch into audit-phase21 (it has none, by design), and do NOT extract or copy anything. Simply RUN the audit and confirm every mover encounter still rides + triggers through the shared driver. If a real bug surfaces in the shared ride path, fix it in mechanic-drive.mjs (39-01's file, single source) — not by copying a branch here.
  </action>
  <verify>
    <automated>node scripts/audit-phase21-mechanics.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/audit-phase21-mechanics.mjs` runs; every mover encounter shows `triggered: true` (a `resolved: false` row remains acceptable known headless-timing flakiness per CLAUDE.md).
    - Confirmed via source that audit-phase21-mechanics.mjs carries NO inline ride block and reaches driveToMover only through audit-retry.mjs (`grep -cE 'function driveTo|const driveTo\s*=' scripts/audit-phase21-mechanics.mjs` is 0) — the ride capability is single-sourced in the shared lib, nothing copied.
  </acceptance_criteria>
  <done>The interactive audit crosses mover gaps via the shared driveToMover; all mover encounters trigger; no ride code was duplicated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser localStorage → game | Only external surface; no network/backend/auth. |

## STRIDE Threat Register (proportionate — ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `localStorage` progress key | low | accept | Guarded `src/progress.js` seams already mitigate. This plan edits test harness scripts only — no runtime/storage surface. |

No package-manager installs (Playwright already resolved). No new external threat surface.
</threat_model>

<verification>
- `node scripts/browser-boot.mjs` green on all 8 levels (ride branch inert until a mover bridges a real pit).
- `node scripts/audit-phase21-mechanics.mjs` — mover encounters trigger.
</verification>

<success_criteria>
- browser-boot's spawn→goal drive crosses a mover-bridged pit via the shared driveToMover; audit-phase21 already crosses via the same shared driver (verified); the ride capability is single-sourced in mechanic-drive.mjs with nothing copied; no regression on the current (stepping-stone-bridged) levels.
</success_criteria>

<output>
Create `.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-03-SUMMARY.md` when done.
</output>
