---
phase: 39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi
plan: 05
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - src/levels/level-03.js
  - src/levels/level-04.js
autonomous: true
requirements: [POL-01, POL-04, POL-03, POL-05]
must_haves:
  truths:
    - "Levels 3 & 4 skeletons walk on the floor with a wide visible sweep off the coins; contact respawns (POL-01)."
    - "The town barrels/crates on L3 (4 props) and L4 (2 props) are solid jump-over obstacles that block the player, and NO required route is blocked — each is jump-clearable per the docs/LEVEL-DESIGN.md envelope (POL-04)."
    - "The L3 prop-town-well is moved clear of the F5 spike at x:4460 (POL-05)."
    - "L4's near-zero mover sweep is widened (EXEMPT) so it visibly moves (POL-03, motion-only — no frozen edit here)."
  artifacts:
    - src/levels/level-03.js
    - src/levels/level-04.js
  key_links:
    - "POL-04 solid props are TOP-LEVEL props (EXEMPT); adding solid:true does NOT trip the frozen hash."
    - "A solid prop becomes route-affecting — it must sit where the player can jump over it (jump envelope in docs/LEVEL-DESIGN.md); do NOT place one where it blocks the only path."
    - "The well is a prop (EXEMPT); the F5 spike at x:4460 is FROZEN — move the WELL, never the spike."
---

<objective>
Turn the town-biome pair (Levels 3 & 4) into real platforming: grounded skeletons (POL-01), the 6 town barrels/crates made solid jump-over obstacles (POL-04), the L3 well cleared off the spike (POL-05), and the L4 near-zero mover sweep widened so it visibly moves (POL-03 motion-only). All edits here are freeze-EXEMPT (patrollers, top-level props, movers) — NO frozen re-baseline.

Purpose: User locked decisions — #4 skeletons ground-patrol, #5 default all 6 town props solid (revisit only if a route breaks). CONTEXT POL-05: move the well off the spike. CONTEXT POL-03: L4 has a near-zero (~40px) mover sweep.
Output: L3/L4 grounded patrols, 6 solid town props, well repositioned, L4 mover sweep widened.

Spec-less probe fallback (VISIBLE SKIP): no SPEC/probe predicates; truths derived from POL-01/03/04/05 text + CONTEXT.md decisions #3, #4, #5.

**Dependency note (explicit):** This plan's browser-boot acceptance ("browser-boot clears L3/L4 with solid props as jump-over obstacles") is ONLY achievable because plan 39-01 Task 3 (wave 1) teaches the shared walk-only driver (`mechanic-drive.driveToXPlanned`) to clear a jump-over solid prop instead of stalling on its `body({isStatic:true})`, and teaches `reachability.solidBoxes` + `validate-levels.mjs` to model solid props for clearability. `depends_on: ["39-01"]` (wave 1 → this wave 2) makes that ordering intentional. If 39-01's solid-prop harness capability is not yet in place, this plan's browser-boot acceptance cannot pass — do not proceed until 39-01 is complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/CONTEXT.md
@.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md
@.claude/CLAUDE.md
@src/levels/level-03.js
@src/levels/level-04.js
@docs/LEVEL-DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Level 3 — ground patrol, solid props, well move, mover widen</name>
  <read_first>
    - src/levels/level-03.js:238 (patroller, currently y:214, 1780-1960) — ground it
    - src/levels/level-03.js:269-273 (props: barrel@880, crate@1810, barrel@3400, crate@5420 — the 4 town solids) AND :265 (`prop-town-well` at x:4400,y:255,layer:"back") AND :161 (F5 spike at x:4460 — FROZEN, do NOT move)
    - src/levels/build.js:492-528 solid branch behaviour + docs/LEVEL-DESIGN.md (jump envelope ~88px — a solid prop must be jump-clearable)
    - .planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-PATTERNS.md (POL-04 + POL-05 (b) well)
  </read_first>
  <action>
    (a) POL-01: ground the L3 patroller (level-03.js:238) — lower y from 214 to ~268 (feet on FLOOR_Y), widen the sweep to ~200-320px, add `speed:70-90`, shift x off the nearest floor coin; keep the lane clearable.
    (b) POL-04: add `solid: true` to the 4 town barrels/crates (level-03.js:269-273: barrel@880, crate@1810, barrel@3400, crate@5420). Confirm each sits on a flat floor lane with ≥1 tile of run-up and clear headroom so the player can jump it (docs/LEVEL-DESIGN.md envelope); if any sits where it would block the ONLY path, nudge its x along the same lane (props are EXEMPT). Leave the street-lamp/sign/well NON-solid.
    (c) POL-05: move `prop-town-well` (level-03.js:265) clear of the F5 spike at x:4460 — shift its x well away (e.g. to a clear background stretch) keeping layer:"back". Do NOT touch the spike.
    (d) POL-03 (motion-only, if L3 has a mover with a near-zero sweep): widen the mover's `x2-x1` (EXEMPT) so it visibly moves. If L3 has no mover, skip.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` zero HARD-FAIL for L3.
    - `node scripts/check-geometry-frozen.mjs` GREEN — all L3 edits are EXEMPT (patrollers/props/movers), proving no frozen-array (incl. the spike) was touched.
    - Source assertion: the 4 town barrels/crates carry `solid: true`; the well x no longer overlaps x:4460 (`grep 'prop-town-well' src/levels/level-03.js` shows a new x clear of the spike).
    - `node scripts/browser-boot.mjs` clears L3 spawn→goal (solid props are jump-over, not path-blockers).
  </acceptance_criteria>
  <done>L3 skeleton grounded, 4 town props solid + jump-clearable, well off the spike, mover visibly moving; validate + frozen + boot green.</done>
</task>

<task type="auto">
  <name>Task 2: Level 4 — ground patrols, solid props, mover widen</name>
  <read_first>
    - src/levels/level-04.js:239,245 (two patrollers, y:214, 460-560 and 3200-3300) — ground both
    - src/levels/level-04.js:272-273 (props: barrel@60, crate@2760 — the 2 town solids)
    - src/levels/level-04.js movers block (CONTEXT: L4 near-zero ~40px mover sweep) — widen it
    - docs/LEVEL-DESIGN.md (jump envelope) + src/levels/build.js:492-528 (solid branch)
  </read_first>
  <action>
    (a) POL-01: ground both L4 patrollers (level-04.js:239, :245) — y 214 → ~268, widen sweep to ~200-320px, `speed:70-90`, shift off floor coins, keep lanes clearable.
    (b) POL-04: add `solid: true` to barrel@60 and crate@2760 (level-04.js:272-273). Verify jump-clearance (docs/LEVEL-DESIGN.md); note barrel@60 is near spawn — ensure the player can immediately jump it or nudge its x along the spawn lane.
    (c) POL-03 (motion-only): widen the L4 mover's near-zero (~40px) sweep (EXEMPT `x2-x1`) so it visibly slides; keep BOTH endpoints inside the jump envelope of a reachable ledge (this L4 mover still rides solid floor — do NOT remove any platform here; the real-hole relocation + frozen work is L8's, in plan 39-07).
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` zero HARD-FAIL for L4.
    - `node scripts/check-geometry-frozen.mjs` GREEN — all L4 edits EXEMPT (no frozen-array change; no platform removed).
    - Source assertion: both L4 patrollers no longer at y:214; both L4 props carry `solid: true`; the L4 mover sweep `x2-x1` is materially wider than ~40px.
    - `node scripts/browser-boot.mjs` clears L4 spawn→goal without softlock.
  </acceptance_criteria>
  <done>L4 skeletons grounded, 2 town props solid + jump-clearable, mover visibly moving; validate + frozen + boot green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser localStorage → game | Only external surface; no network/backend/auth. |

## STRIDE Threat Register (proportionate — ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `localStorage` progress key | low | accept | Guarded `src/progress.js` seams already mitigate. Level-data edits only — no new surface. |

No package-manager installs. No new external threat surface.
</threat_model>

<verification>
- `node scripts/validate-levels.mjs` zero HARD-FAIL.
- `node scripts/check-geometry-frozen.mjs` green (all edits EXEMPT).
- `node scripts/browser-boot.mjs` clears L3/L4.
</verification>

<success_criteria>
- L3/L4 grounded patrols; 6 town props solid and jump-clearable; well off the spike; L4 mover visibly moving; all edits frozen-hash-neutral.
</success_criteria>

<output>
Create `.planning/phases/39-playthrough-polish-grounded-patrolling-skeletons-sliding-spi/39-05-SUMMARY.md` when done.
</output>
