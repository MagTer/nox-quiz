---
phase: 34.6.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/coin.png
  - assets/goal.png
  - assets/key.png
  - assets/LICENSES/coin.txt
  - assets/LICENSES/goal.txt
  - assets/LICENSES/key.txt
  - CREDITS.md
  - src/assets-manifest.js
autonomous: false
requirements: [polish]
must_haves:
  truths:
    - "D-02: assets/coin.png is a crisp CC0 gold coin spin sheet, re-gridded to the existing 256x32 / 8-frame geometry so the loader (sliceX:CONFIG.COIN_FRAMES=8) is unchanged"
    - "D-03: assets/goal.png is a CC0 checkered-flag sprite authored to read at 16px (CONFIG.GOAL_SIZE) so the footprint is preserved on wiring"
    - "D-04: assets/key.png is a real CC0 key sprite, present on disk and declared in the assets manifest, authorable to render at the 20px key footprint (CONFIG.KEY.W/H)"
    - "Every vendored asset's source page License(s) field is confirmed literally CC0 (NOT CC-BY/CC-BY-SA/GPL) at a blocking human checkpoint BEFORE the files are committed"
    - "Each asset has a proof file under assets/LICENSES/ (source URL + quoted 'License(s): CC0' + verification date) and a matching CREDITS.md row"
  artifacts:
    - "assets/coin.png (overwritten), assets/goal.png (overwritten), assets/key.png (new)"
    - "assets/LICENSES/coin.txt, assets/LICENSES/goal.txt (updated), assets/LICENSES/key.txt (new)"
    - "CREDITS.md rows for coin/goal/key updated/added"
    - "src/assets-manifest.js: new { key: 'key', path: 'assets/key.png', kind: 'sprite' } entry"
  key_links:
    - "coin re-grid MUST land at 256x32 / 8 frames so CONFIG.COIN_FRAMES stays 8 — the loader (main.js) and sheet must agree or frames render garbage"
    - "the CC0 human-verify is the license-legitimacy gate — a search-snippet 'CC0' tag is NOT sufficient (RESEARCH caught a CC-BY asset masquerading as CC0)"
---

<objective>
Acquire and vendor the three CC0 interactive-entity sprites this phase needs — a crisp gold coin spin sheet (D-02), a checkered finish flag (D-03), and a real key icon (D-04) — with a blocking human license check confirming each source is genuinely CC0 BEFORE anything is committed, plus license proofs, CREDITS rows, and the manifest entry for the new key.

Purpose: The wiring plan (34.6.1-03) references these files by path and the asset-manifest gate requires them on disk; this plan owns the sourcing, the CC0 provenance check, and the licensing paperwork so the wiring plan is a pure code change.
Output: assets/coin.png (crisp, re-gridded), assets/goal.png (flag), assets/key.png (new); three license proofs; CREDITS rows; the manifest `key` entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-CONTEXT.md
@.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-RESEARCH.md
@.claude/CLAUDE.md
@assets/LICENSES/coin.txt
@assets/LICENSES/goal.txt
@CREDITS.md
</context>

<user_setup>
- No account/service setup and no env vars are required. This plan opens public CC0 source pages (opengameart.org, kenney.nl) in a browser to confirm each License(s) field — that confirmation is the human-verify checkpoint in Task 2, not out-of-band setup.
</user_setup>

<artifacts_this_phase_produces>
- assets/coin.png — crisp CC0 gold coin spin sheet, 256x32, 8 uniform 32px frames (overwrite)
- assets/goal.png — CC0 checkered finish flag, authored to read at 16px (overwrite)
- assets/key.png — NEW CC0 key icon sprite, authorable at the 20px key footprint
- assets/LICENSES/coin.txt, assets/LICENSES/goal.txt — updated proofs (new sources)
- assets/LICENSES/key.txt — NEW proof file
- CREDITS.md — updated coin/goal rows + new key row
- src/assets-manifest.js — NEW `{ key: "key", path: "assets/key.png", kind: "sprite" }` entry
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Source and stage the three CC0 candidate sprites (no commit yet)</name>
  <files>assets/coin.png, assets/goal.png, assets/key.png</files>
  <read_first>
    - 34.6.1-RESEARCH.md §3 "CC0 Sprite Sourcing" — the VERIFIED CC0 candidates (rzuf/Kutejnikov coin; Kenney Pixel Platformer key+flag) AND the REJECTED CC-BY "Animated Coins" (Clint Bellanger) trap.
    - 34.6.1-RESEARCH.md §2 — the sprite/footprint API and the coin sheet contract (256x32 / 8 frames).
    - assets/LICENSES/coin.txt — the existing coin's "mechanical re-grid to evenly-gridded 256x32" precedent (re-layout of CC0 pixels stays CC0).
    - CREDITS.md "Notes" — the existing re-grid precedent and the prior CC-BY-SA rejection precedent.
  </read_first>
  <action>
    Download each Claude's-discretion CC0 candidate to a scratch/staging area (NOT yet into assets/): a crisp gold coin spin sheet (prefer the RESEARCH-cited rzuf or Kutejnikov OpenGameArt sets), a checkered finish flag, and a key icon (prefer Kenney Pixel Platformer for a single-source flag+key). Match the dark-grunge palette (gold coin, no pink); a documented recolor/retint to the palette is allowed CC0 (record it in the proof file per the town/cemetery hue-conform precedent).

    Prepare each to its target geometry:
    - Coin: measure the sheet (python3 -c "from PIL import Image; print(Image.open(PATH).size)") and re-grid to an EVENLY-GRIDDED 256x32 strip of 8 uniform 32px frames using Pillow, so CONFIG.COIN_FRAMES stays 8 and the existing loader is unchanged. (This is the required approach — keep loader+sheet agreement inside this asset task; do NOT push a geometry change into config.js, which another plan owns.)
    - Goal flag: author/crop so it reads clearly at 16px (CONFIG.GOAL_SIZE); a checkered black/white flag reading unmistakably as "the end."
    - Key: author/crop so it renders cleanly at the 20px key footprint (CONFIG.KEY.W/H).

    Do NOT overwrite the in-tree assets/*.png or write any license/credits file in this task — staging + preparation only, so the CC0 provenance check (Task 2) gates before anything lands.
  </action>
  <verify>
    <automated>python3 -c "print('pillow ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Three candidate PNGs exist in the scratch/staging area (coin spin sheet, checkered flag, key).
    - The staged coin sheet measures 256x32 (8 x 32px frames) via the Pillow size check.
    - No change yet to assets/coin.png, assets/goal.png, CREDITS.md, or any assets/LICENSES/*.txt (staging only).
  </acceptance_criteria>
  <done>Three CC0 candidates are downloaded and prepared to target geometry in staging; the coin is a 256x32 8-frame strip; nothing committed into assets/ yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: CC0 provenance checkpoint — confirm each source page's License(s) is literally CC0 BEFORE vendoring</name>
  <action>Blocking human license gate. For each staged asset (coin, goal flag, key), a human opens the source page and confirms the literal License(s) field is CC0 — a search-snippet tag is insufficient. No file is committed into assets/ until this passes. Not auto-approvable.</action>
  <what-built>Three CC0 sprite candidates staged (coin spin sheet, checkered finish flag, key icon), each prepared to its target pixel geometry, ready to vendor into assets/ and license.</what-built>
  <how-to-verify>
    For EACH of the three staged assets, open its source page in a browser and confirm the literal License(s) field says CC0 / Creative Commons Zero / Public Domain — NOT CC-BY, NOT CC-BY-SA, NOT GPL. A "CC0" tag in a search snippet is NOT sufficient: RESEARCH caught "Animated Coins" (Clint Bellanger) surfacing as CC0 in search while its own page is CC-BY 3.0 / CC-BY-SA / GPL (REJECTED). Steps:
    1. Coin source page → read the License(s) field → confirm CC0.
    2. Goal-flag source page → read the License(s) field → confirm CC0.
    3. Key source page → read the License(s) field → confirm CC0.
    4. For each, note the exact source URL and the verification date to be recorded in the proof file.
    If ANY candidate is not genuinely CC0, reject it and source a different CC0 asset before proceeding (do not vendor it).
  </how-to-verify>
  <resume-signal>Reply "CC0 confirmed for coin/goal/key" (or name which asset failed and must be re-sourced). This checkpoint is a blocking license gate — it cannot be auto-approved.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Vendor the confirmed assets + write license proofs, CREDITS rows, and the manifest key entry</name>
  <files>assets/coin.png, assets/goal.png, assets/key.png, assets/LICENSES/coin.txt, assets/LICENSES/goal.txt, assets/LICENSES/key.txt, CREDITS.md, src/assets-manifest.js</files>
  <read_first>
    - assets/LICENSES/coin.txt and assets/LICENSES/goal.txt — the EXACT proof-file template to copy (Asset / Source pack / Author / Source URL / License / quoted "License(s): CC0" / verification date / any processing note).
    - CREDITS.md "## Assets" table — the row format to update/add (Asset | File | Author | Source | License | Used for).
    - src/assets-manifest.js lines 39-49 — the `sprite` group where the new `key` entry is added (pure data, node-importable, zero engine globals).
    - 34.6.1-RESEARCH.md §4 seams #9 and #10 — the manifest entry (gate coverage) and the licensing paperwork.
  </read_first>
  <action>
    Only after the CC0 checkpoint passed: overwrite assets/coin.png (crisp 256x32 8-frame sheet) and assets/goal.png (checkered flag), and add the new assets/key.png (key icon).

    Update assets/LICENSES/coin.txt and assets/LICENSES/goal.txt for the new sources, and ADD assets/LICENSES/key.txt — each following the existing template exactly: asset path, source pack + author, source URL, "License: CC0", the quoted "License(s): CC0" declaration, the verification date from the checkpoint, and (for the coin) the "mechanical re-grid to evenly-gridded 256x32 — still CC0" processing note, plus any documented palette retint.

    Update the coin and goal rows and ADD a key row in CREDITS.md "## Assets", matching the surrounding row format (Author, Source URL, License CC0, Used-for description: key = "math-skip key pickup on even levels 02/04/06/08").

    Add one entry to src/assets-manifest.js in the `sprite` group: `{ key: "key", path: "assets/key.png", kind: "sprite" }`. (coin/goal paths are already in the manifest; do not duplicate them.) Keep the module a pure data literal — no imports, no engine globals.
  </action>
  <verify>
    <automated>node --check src/assets-manifest.js && node scripts/check-assets-manifest.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-assets-manifest.mjs` exits 0 (every declared sprite path — including the new assets/key.png — exists on disk).
    - `node --check src/assets-manifest.js` exits 0 and the manifest contains a `key`/`assets/key.png` sprite entry (grep confirms).
    - assets/LICENSES/key.txt exists; coin.txt and goal.txt each contain a quoted "License(s): CC0" line with a 2026-07 verification date.
    - CREDITS.md contains rows for coin, goal, and key each marked CC0.
    - The staged coin, now at assets/coin.png, measures 256x32 (Pillow size check) so CONFIG.COIN_FRAMES=8 stays valid.
  </acceptance_criteria>
  <done>The three CC0 assets are vendored into assets/, each with a proof file + CREDITS row; the manifest declares the new key; check-assets-manifest.mjs is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party asset source → repo | CC0 pixel-art PNGs vendored from public art sites (the one real supply-side concern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6.1-SC | Tampering (supply/provenance) | vendored CC0 assets | high | mitigate | Blocking human-verify (Task 2) opens each source page and confirms the literal License(s): CC0 field BEFORE the file is committed — a search-snippet tag is insufficient (a CC-BY asset already masqueraded as CC0 in research). Proof recorded in assets/LICENSES/ + CREDITS.md. Not auto-approvable. |
| T-34.6.1-02 | Information Disclosure | static image assets | low | accept | Assets are static pixel-art PNGs with no executable content, no metadata egress, no runtime network I/O; the game is client-only static files. No new attack surface. |
</threat_model>

<verification>
- `node scripts/check-assets-manifest.mjs` — every declared sprite path (incl. new key) exists on disk.
- `python3` Pillow size check — coin is 256x32 (8x32px frames), preserving the loader contract.
- Human (Task 2 checkpoint) — each source page's License(s) field is literally CC0.
- Deferred to 34.6.1-04 (human): crispness / "reads as flag" / "reads as key" perceptual proofs (after the sprites are wired in 34.6.1-03).
</verification>

<success_criteria>
- assets/coin.png (crisp 256x32), assets/goal.png (flag), assets/key.png (new) all present.
- Three license proofs + CREDITS rows; the manifest declares `key`.
- CC0 provenance human-confirmed before commit; check-assets-manifest.mjs green.
</success_criteria>

<output>
Create `.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-02-SUMMARY.md` when done.
</output>
