---
phase: 34.6.1
plan: 03
type: execute
wave: 2
depends_on: [34.6.1-01, 34.6.1-02]
files_modified:
  - src/main.js
  - src/config.js
  - src/levels/build.js
  - src/mechanics/key.js
autonomous: true
requirements: [polish]
must_haves:
  truths:
    - "D-02: the coin renders from the crisp vendored assets/coin.png and still plays its looping spin (CONFIG.COIN_FRAMES=8 unchanged)"
    - "D-03: every level's goal renders the checkered-flag sprite while keeping the 16px collision footprint (CONFIG.GOAL_SIZE) — all 8 goals still fire (validate-levels + browser-boot green)"
    - "D-04: the even-level key pickup renders the real key sprite (not a gold rect) while keeping the 20px collision footprint (CONFIG.KEY.W/H) — key pickup still triggers"
    - "D-04: the pickup popup and HUD indicator read as 'grab the key -> skip the final math' (plain ASCII, no TOFU), sourced from config; the key->skip mechanic is unchanged"
    - "New engine globals stay inside function bodies (a727c13); the ?debug=1 magenta key marker still works"
  artifacts:
    - "src/main.js: new loadSprite('key', '../assets/key.png') (static, no anim); coin/goal loads unchanged"
    - "src/config.js: KEY.PICKUP_LABEL (new ASCII string); HUD.KEY_GLYPH reworded to skip-legible ASCII; KEY.COLOR marked vestigial"
    - "src/levels/build.js: goal + key builders render sprites pinned to their footprints"
    - "src/mechanics/key.js: popup text sourced from CONFIG.KEY.PICKUP_LABEL"
  key_links:
    - "goal/key footprints are pinned via sprite({width,height}) at CONFIG.GOAL_SIZE / CONFIG.KEY.W-H + a bare area(); NEVER scale() (which would resize the collider)"
    - "the new key load is a hand-written loadSprite — the manifest loop only auto-loads biome atlases/bg"
---

<objective>
Wire the three vendored sprites into the game (D-02 coin, D-03 goal flag, D-04 key) with their collision footprints preserved, and rework the key framing text so the "grab the key -> skip the final math" flow reads clearly. The key->skip mechanic itself is untouched — this is sprite + text only.

Purpose: Turn the vendored assets (34.6.1-02) into on-screen entities without changing level reachability. Footprint preservation (goal 16px, key 20px) is load-bearing: validate-levels.mjs and browser-boot.mjs must stay green.
Output: A key loadSprite in main.js; config framing strings; footprint-pinned goal/key builders in build.js; a config-sourced pickup popup in key.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-CONTEXT.md
@.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-RESEARCH.md
@.claude/CLAUDE.md
@src/main.js
@src/levels/build.js
@src/mechanics/key.js
@src/ui/hud.js
</context>

<artifacts_this_phase_produces>
- src/main.js: `loadSprite("key", "../assets/key.png")` — static sprite, no anim (hand-written; the manifest loop does not load it)
- src/config.js: `CONFIG.KEY.PICKUP_LABEL` (new ASCII string, e.g. "+ SKIP KEY"); `CONFIG.HUD.KEY_GLYPH` reworded to a skip-legible ASCII string (e.g. "KEY: SKIP END"); `CONFIG.KEY.COLOR` documented vestigial (debug-tint only)
- src/levels/build.js: goal builder renders `sprite("goal", { width: CONFIG.GOAL_SIZE, height: CONFIG.GOAL_SIZE })`; key builder renders `sprite("key", { width: CONFIG.KEY.W, height: CONFIG.KEY.H })` replacing the rect
- src/mechanics/key.js: popup text sourced from `CONFIG.KEY.PICKUP_LABEL`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Register the key sprite in main.js and add the D-04 framing strings to config.js</name>
  <files>src/main.js, src/config.js</files>
  <read_first>
    - src/main.js lines 63-85 — the loadSprite("goal", ...) and coin loadSprite (sliceX:CONFIG.COIN_FRAMES) calls; the `../assets/...` web-root path convention and the "loadSpriteSheet does NOT exist in Kaplay 3001" note.
    - src/config.js CONFIG.KEY block (lines 215-219) and CONFIG.HUD.KEY_GLYPH (line 297) — the reword sites; KEY.W/H=20 are load-bearing and must NOT change.
    - src/config.js CONFIG.COIN_FRAMES/COIN_SPIN_SPEED (lines 136-137) — leave unchanged (the vendored coin was re-gridded to the existing 8x32 geometry in 34.6.1-02).
    - 34.6.1-RESEARCH.md §4 seams #2, #3, #8 — the key load, the coin/goal no-change note, and the config framing rewords.
  </read_first>
  <action>
    In src/main.js, add a static key sprite load near the goal/coin loads (around line 64-85): `loadSprite("key", "../assets/key.png")` — no sliceX, no anims (the key is a single static frame). Leave the existing `loadSprite("goal", "../assets/goal.png")` and the coin loadSprite unchanged: 34.6.1-02 overwrote those files in place at their existing geometry, so no loader change is needed. The new engine-global `loadSprite` reference stays where the file already calls it (module-scope init block that runs after kaplay() — a727c13-consistent with the surrounding loads).

    In src/config.js, inside CONFIG.KEY, add `PICKUP_LABEL: "+ SKIP KEY"` (plain ASCII, TOFU-safe) — the skip-legible pickup popup string consumed by key.js. Keep KEY.W/H at 20 (load-bearing). Add a trailing comment on KEY.COLOR marking it vestigial (used only for the ?debug=1 magenta marker now, not the shipped art). In CONFIG.HUD, reword KEY_GLYPH from "KEY" to a skip-legible ASCII string, e.g. "KEY: SKIP END" — keep it plain ASCII (no emoji/glyph; TOFU-fallback caution) and short enough to sit left of the mute icon (CONFIG.AUDIO.ICON_X:600); exact wording is Claude's-discretion per CONTEXT D-04. Do not change KEY_X/KEY_Y/KEY_SIZE.
  </action>
  <verify>
    <automated>node --check src/main.js && node --check src/config.js && node scripts/check-assets-manifest.mjs && grep -c "loadSprite(\"key\"" src/main.js && grep -c "PICKUP_LABEL" src/config.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/main.js` and `node --check src/config.js` both exit 0.
    - `node scripts/check-assets-manifest.mjs` exits 0.
    - `grep -c 'loadSprite("key"' src/main.js` >= 1 (key sprite registered).
    - `grep -c "PICKUP_LABEL" src/config.js` >= 1 and CONFIG.HUD.KEY_GLYPH no longer equals the bare "KEY" (reworded to a skip-legible ASCII string).
    - CONFIG.KEY.W and CONFIG.KEY.H are both still 20; CONFIG.COIN_FRAMES is still 8.
  </acceptance_criteria>
  <done>main.js registers the key sprite; config.js carries KEY.PICKUP_LABEL and a reworded HUD.KEY_GLYPH; footprints/geometry constants unchanged; asset-manifest green.</done>
</task>

<task type="auto">
  <name>Task 2: Swap the goal and key builders to sprites with pinned footprints (build.js)</name>
  <files>src/levels/build.js</files>
  <read_first>
    - src/levels/build.js line 263 — the goal builder `add([sprite("goal"), pos(g.goal.x, g.goal.y), area(), "goal"])`.
    - src/levels/build.js lines 399-412 — the key loop: `rect(CONFIG.KEY.W, CONFIG.KEY.H)`, the DEBUG branch (magenta color(255,0,255)/opacity(0.8)) vs non-debug (CONFIG.KEY.COLOR gold), the area(), and the `keyObj.keyId = k.keyId ?? null` stash.
    - src/levels/build.js lines 248-260 — the spike builder's `area({ shape: new Rect(...) })` precedent and the guarded `new Rect(...)` scope (for reference only; the goal/key use sprite({width,height})+bare area()).
    - 34.6.1-RESEARCH.md §2 "area() collider derivation + footprint pinning" and §4 seams #4, #5 — render at target px + bare area(); NEVER scale().
  </read_first>
  <action>
    Goal (build.js:263): render the flag pinned to the 16px footprint — change `sprite("goal")` to `sprite("goal", { width: CONFIG.GOAL_SIZE, height: CONFIG.GOAL_SIZE })`, keeping pos, the bare `area()`, and the "goal" tag exactly as-is. This pins the collider at 16px regardless of the flag PNG's native size (footprint preserved; reachability unchanged).

    Key (build.js:399-412): replace `rect(CONFIG.KEY.W, CONFIG.KEY.H)` with `sprite("key", { width: CONFIG.KEY.W, height: CONFIG.KEY.H })`, keeping the bare `area()`, the "key" tag, and the `keyObj.keyId = k.keyId ?? null` stash. Keep the DEBUG branch as a magenta tint over the sprite (color(255,0,255) + opacity(0.8)) so ?debug=1 still marks keys; in the non-debug branch DROP the gold CONFIG.KEY.COLOR fill (the sprite now provides the art) but keep opacity(1). Do NOT use scale() anywhere here — it would resize the bare area() and change the 20px footprint.
  </action>
  <verify>
    <automated>node --check src/levels/build.js && node scripts/validate-levels.mjs && grep -c "sprite(\"key\"" src/levels/build.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/levels/build.js` exits 0.
    - `node scripts/validate-levels.mjs` exits 0 (zero HARD-FAIL; goal 16px + key 20px footprints and reachability unchanged).
    - `grep -c 'sprite("key"' src/levels/build.js` >= 1 and the key `rect(CONFIG.KEY.W, CONFIG.KEY.H)` construct is gone from the key loop.
    - The goal builder passes `{ width: CONFIG.GOAL_SIZE, height: CONFIG.GOAL_SIZE }` to sprite("goal") (grep confirms), with a bare `area()` (no scale()).
  </acceptance_criteria>
  <done>Goal and key render as footprint-pinned sprites (16px / 20px); the ?debug=1 magenta key marker survives; validate-levels.mjs green.</done>
</task>

<task type="auto">
  <name>Task 3: Source the key pickup popup text from config (key.js)</name>
  <files>src/mechanics/key.js</files>
  <read_first>
    - src/mechanics/key.js line 72 — `fx.popupText(keyObj.pos.clone(), "+ KEY")` (the reword site) and the surrounding pickup-feedback triad (fx.pop / fx.popupText / audio.playSfx("pickup")).
    - src/mechanics/key.js lines 60-76 — the onCollide("key") handler; confirm the swap touches ONLY the popup string, not the pickup mechanic (onPickup/hud.showKey/destroy ordering stays).
    - 34.6.1-RESEARCH.md §4 seam #6 — reword to a config-sourced skip-legible ASCII label.
  </read_first>
  <action>
    In src/mechanics/key.js's onCollide("key") handler (line 72), change the popup call to source its text from config: `fx.popupText(keyObj.pos.clone(), CONFIG.KEY.PICKUP_LABEL)` (CONFIG is already imported at the top of the file). Do NOT hardcode the string in key.js — it lives in config (tunables-in-config rule). Leave everything else in the handler unchanged: the onPickup(keyObj.keyId ?? null) call, hud.showKey(), fx.pop(...), audio.playSfx("pickup"), and the destroy(keyObj) LAST ordering. The key->skip mechanic (heldKeyIds/endSkipArmed/XP_KEY_SKIP in game.js) is out of scope and must not be touched.
  </action>
  <verify>
    <automated>node --check src/mechanics/key.js && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && grep -c "CONFIG.KEY.PICKUP_LABEL" src/mechanics/key.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/mechanics/key.js` exits 0.
    - `bash scripts/check-safety.sh` and `bash scripts/check-import-safety.sh` both exit 0 (no timer/scheduler token; engine globals stay inside wireKey()).
    - `grep -c "CONFIG.KEY.PICKUP_LABEL" src/mechanics/key.js` >= 1 and the bare `"+ KEY"` literal is gone from the popup call.
    - The onCollide("key") handler is otherwise byte-unchanged (mechanic untouched).
  </acceptance_criteria>
  <done>The pickup popup renders CONFIG.KEY.PICKUP_LABEL; the key mechanic is unchanged; safety/import-safety gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level data → rendered entities | Pure-data level descriptors build tagged sprite entities; no external/network input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6.1-03 | Tampering | goal/key collider footprint | medium | mitigate | Footprint is pinned via sprite({width,height}) at CONFIG.GOAL_SIZE / CONFIG.KEY.W-H + a bare area() (never scale()); validate-levels.mjs (16px/20px) and browser-boot.mjs assert reachability/pickup are unchanged. Not a security threat per se — logged because a silent footprint change is the load-bearing regression this plan must avoid. |
| T-34.6.1-04 | Injection | key framing strings | low | accept | The reworded popup/HUD strings are static ASCII literals in config rendered via canvas text() — no markup/DOM sink, no user input, no injection path (challenge.js's documented no-injection guard applies to all canvas text). No new attack surface. |
</threat_model>

<verification>
- `node scripts/validate-levels.mjs` — goal 16px + key 20px footprints and reachability unchanged (zero HARD-FAIL).
- `node scripts/check-assets-manifest.mjs` — key/goal/coin paths present.
- `bash scripts/check-safety.sh` + `bash scripts/check-import-safety.sh` — no-timer + a727c13.
- `node scripts/browser-boot.mjs` + `node scripts/audit-phase21-mechanics.mjs` — run at the consolidation wave (34.6.1-04): goals fire, coins spin, keys pick up across all 8 levels.
- Deferred to 34.6.1-04 (human): crispness, "reads as flag", "reads as key", skip-framing legibility, ASCII no-TOFU.
</verification>

<success_criteria>
- Coin renders from the crisp sheet and still spins; goal renders the flag at 16px; key renders the sprite at 20px.
- Pickup popup + HUD read as "grab the key -> skip the final math" (config-sourced ASCII).
- validate-levels.mjs, check-assets-manifest.mjs, check-safety.sh, check-import-safety.sh all green.
</success_criteria>

<output>
Create `.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-03-SUMMARY.md` when done.
</output>
