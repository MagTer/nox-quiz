---
phase: 34.6.1
plan: 02
title: Vendor the 3 CC0 interactive sprites (coin / goal-flag / key)
status: complete
completed: 2026-07-16
commits:
  - 558e0e2
---

# Plan 34.6.1-02 — Summary

Vendored the three interactive-entity sprites (D-02 coin, D-03 goal flag, D-04 key) into `assets/`, with license notes, CREDITS rows, and the manifest entry for the new key.

## What was built

- **`assets/coin.png`** — crisp CC0 gold spin sheet ("Spinning coin anim" by rzuf, OpenGameArt, CC0), downscaled from 1024×128 (8 frames @128) to the existing **256×32 / 8-frame** layout (Pillow LANCZOS) so `CONFIG.COIN_FRAMES=8` and the loader are unchanged.
- **`assets/key.png`** — NEW 20×20 gold key (Kenney "Pixel Platformer" tile_0027, CC0), centered into a 20×20 canvas to match the `CONFIG.KEY.W/H=20` footprint.
- **`assets/goal.png`** — 16×16 baked black/white **checkered finish flag** on a dark pole (original work, public domain), at the `CONFIG.GOAL_SIZE=16` footprint. Replaces the old HorusKDI skull-flag tile that read as a key-ish blob.
- License proofs: `assets/LICENSES/{coin,goal,key}.txt`; CREDITS.md rows updated/added; `src/assets-manifest.js` gains `{ key: "key", path: "assets/key.png", kind: "sprite" }` (sprite count 10→11).

## Verification

- `node scripts/check-assets-manifest.mjs` → **PASS** (all declared sprite paths, incl. new `assets/key.png`, exist on disk).
- `node --check src/assets-manifest.js` → exit 0.
- Coin geometry confirmed 256×32 (8×32px) via Pillow — loader contract preserved.
- **Human aesthetic sign-off:** the three staged sprites were shown to the user in a pixel-perfect before/after preview (artifact) and approved ("go ahead and wire all three in").

## Deviations from plan

1. **Task 1 (sourcing) performed by the orchestrator, not a subagent executor** — the `gsd-executor` has no web/download tools; the orchestrator downloaded + prepared the CC0 candidates (network-verified HTTP 200 to kenney.nl / opengameart.org).
2. **Task 2 blocking CC0 human-verify checkpoint WAIVED by the user** — the user stated this is a personal project for their daughter, not distributed, and their "concern for licenses is limited." The vendored coin + key are still genuinely CC0 (rzuf / Kenney) and the flag is original work; license notes are kept but the blocking per-page verification gate was skipped per that explicit direction.
3. **Goal flag BAKED, not curated** — the Kenney Pixel Platformer pack ships only RED flags, no checkered flag; per the D-03 "checkered flag" decision a 16×16 checkered flag was baked procedurally (original / public-domain) rather than curated.

## Notes

Nothing in the pre-existing 34.6 working tree (STATE.md, level-0N.js, docs, scripts) was touched. STATE.md remains uncommitted for the user to reconcile (pre-existing drift, unrelated to this phase).
