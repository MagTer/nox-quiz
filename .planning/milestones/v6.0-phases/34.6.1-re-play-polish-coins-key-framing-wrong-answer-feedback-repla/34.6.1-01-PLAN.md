---
phase: 34.6.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.js
  - src/ui/challenge.js
autonomous: true
requirements: [polish]
must_haves:
  truths:
    - "D-01: a wrong answer pick fires an amplified RED panel pulse + a stronger shake, and the SAME question stays open and answerable"
    - "D-01: during the brief post-wrong settle, BOTH input paths (box.onClick and onKeyPress 1-4) are inert, so mashing 1-2-3-4 can no longer slip the correct key through"
    - "D-01: the settle is cleared ONLY by the flash tween's onEnd() clearing a closure-local `settling` flag — the challenge never soft-locks (settling always returns to false)"
    - "D-01: no banned scheduler/timer token exists in challenge.js (check-safety.sh green) — SAFE-01 non-punishing (no lost progress, no game-over, no countdown)"
    - "D-01: a correct single pick still fires onSuccess exactly once and closes cleanly"
  artifacts:
    - "src/config.js CONFIG.GATE with WRONG_SETTLE_MS, WRONG_FLASH_OPACITY, WRONG_SHAKE, WRONG_FLASH_Z tunables"
    - "src/ui/challenge.js closure-local `settling` flag + red flash tween in the wrong-answer branch"
  key_links:
    - "choose(i) is the single funnel for both mouse and 1-4 key input — guarding its top covers both paths"
    - "the flash rect carries the per-invocation instanceTag so close() sweeps it if the session tears down mid-flash"
---

<objective>
Implement D-01: amplify wrong-answer feedback in the shared challenge overlay and add a brief, tween-gated input "settle" so mashing all four answer keys can no longer slip a wrong pick past. Fully non-punishing (SAFE-01): the same question stays open, nothing counts down, nothing is lost.

Purpose: The re-play surfaced that `choose(i)` resolves instantly on every keypress, so a burst of 1-2-3-4 lets the correct key close the panel before a wrong pick registers. The fix makes a wrong answer visibly register and be un-mashable, without any punishment.
Output: New CONFIG.GATE.WRONG_* tunables; a closure-local `settling` guard + red-pulse flash tween in src/ui/challenge.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-CONTEXT.md
@.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-RESEARCH.md
@.claude/CLAUDE.md
</context>

<artifacts_this_phase_produces>
- CONFIG.GATE.WRONG_SETTLE_MS (Number, ms) — tween-gated input settle after a wrong pick
- CONFIG.GATE.WRONG_FLASH_OPACITY (Number, 0..1) — peak red-pulse opacity
- CONFIG.GATE.WRONG_SHAKE (Number) — shake magnitude on a wrong pick (replaces inline shake(6))
- CONFIG.GATE.WRONG_FLASH_Z (Number) — z for the pulse rect (above the answer boxes at z 9993)
- src/ui/challenge.js: closure-local `settling` flag; a fixed() DANGER red-pulse rect + one tween().onEnd() in the wrong-answer branch
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the wrong-answer flash/settle tunables to CONFIG.GATE</name>
  <files>src/config.js</files>
  <read_first>
    - src/config.js CONFIG.GATE block (lines ~159-181) — where BOX_W/BOX_H/DIM_OPACITY already live; add the new keys here.
    - src/config.js CONFIG.PALETTE.DANGER (line 31) — the [0xff,0x44,0x33] flash color, already defined (reused, not re-added).
    - 34.6.1-RESEARCH.md §1 "Tunables (all new, in CONFIG.GATE)" — the recommended values and rationale.
  </read_first>
  <action>
    Add four new numeric tunables inside the existing CONFIG.GATE object (src/config.js, alongside DIM_OPACITY/BOX_W/BOX_H) with descriptive trailing comments and no magic numbers left in logic:
    - WRONG_SETTLE_MS: 250 — tween-gated input settle (ms) after a wrong pick (anti-mash); the flash-tween duration that also gates re-input.
    - WRONG_FLASH_OPACITY: 0.45 — peak opacity of the single easeOutQuad red pulse (non-strobing, ADHD-safe).
    - WRONG_SHAKE: 8 — shake magnitude on a wrong pick (stronger than the old inline shake(6)).
    - WRONG_FLASH_Z: 9995 — z of the pulse rect; above the answer boxes (z 9993) and their labels, so the pulse reads over the whole panel.
    Values are Claude's-discretion per CONTEXT D-01 — these are the RESEARCH-recommended sensible defaults; keep them tunable here, never inline in challenge.js. The flash COLOR is CONFIG.PALETTE.DANGER (already defined) — do NOT add a color tunable.
  </action>
  <verify>
    <automated>node --check src/config.js && grep -c "WRONG_SETTLE_MS\|WRONG_FLASH_OPACITY\|WRONG_SHAKE\|WRONG_FLASH_Z" src/config.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/config.js` exits 0 (file parses).
    - `grep -c "WRONG_SETTLE_MS\|WRONG_FLASH_OPACITY\|WRONG_SHAKE\|WRONG_FLASH_Z" src/config.js` reports 4 (all four keys present).
    - The four keys sit inside the CONFIG.GATE object (not a new top-level block).
  </acceptance_criteria>
  <done>CONFIG.GATE carries the four WRONG_* tunables with sensible defaults and comments; config.js parses.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the closure-local settle guard + red-pulse flash tween to challenge.js</name>
  <files>src/ui/challenge.js</files>
  <behavior>
    - A wrong pick sets a closure-local `settling` flag true, plays the existing "wrong" SFX, reddens the picked box, shakes at CONFIG.GATE.WRONG_SHAKE, and pulses a red overlay that fades to 0 over CONFIG.GATE.WRONG_SETTLE_MS.
    - While `settling` is true, choose(i) returns early — both mouse clicks and 1-4 keypresses are inert.
    - When the flash tween's onEnd() fires, `settling` returns to false and the flash rect is destroyed; the SAME question is still open and answerable (no soft-lock).
    - A correct pick still latches `cleared`, fires onSuccess exactly once, and closes cleanly.
  </behavior>
  <read_first>
    - src/ui/challenge.js lines 96-133 — the `let cleared = false;` latch, the `instanceTag`, and the fixed()/z() overlay idiom (the flash reuses instanceTag so close() sweeps it).
    - src/ui/challenge.js lines 277-307 — choose(i): the `if (cleared) return;` guard, the bounds guard, the wrong-answer branch (289-296) with the inline shake(6), and the correct-answer close() path.
    - src/mechanics/key.js lines 87, 108-129 — the `hintShowing` fire-once-cleared-by-tween().onEnd() idiom this change mirrors byte-for-byte (the SAFE-01-compliant reference pattern).
    - 34.6.1-RESEARCH.md §1 — the confirmed settle pattern and the check-safety.sh traps to avoid.
  </read_first>
  <action>
    In openChallenge's closure, declare a closure-local `let settling = false;` immediately beside the existing `let cleared = false;` (line 97) — closure-local, never module-level, matching the file's anti-leak discipline.

    At the very top of choose(i) (line 277), add an early `if (settling) return;` AFTER the existing `if (cleared) return;` and before the bounds guard, so both input paths go inert during the settle.

    In the wrong-answer branch (the `if (!correct)` block, lines 289-296): set `settling = true` first; keep the existing `audio.playSfx("wrong")` and the box reddening (box.color = rgb(CONFIG.PALETTE.DANGER...)); replace the inline `shake(6)` with `shake(CONFIG.GATE.WRONG_SHAKE)`. Then add a fixed(), full-screen red pulse rect built with rect(width(), height()), pos(0,0), color(CONFIG.PALETTE.DANGER[0..2]), opacity(CONFIG.GATE.WRONG_FLASH_OPACITY), z(CONFIG.GATE.WRONG_FLASH_Z), tagged BOTH "challenge" and the per-invocation instanceTag (so close() destroys it if the session tears down mid-flash). Fade it with a single tween from CONFIG.GATE.WRONG_FLASH_OPACITY to 0 over CONFIG.GATE.WRONG_SETTLE_MS/1000 seconds using easings.easeOutQuad, and in that tween's .onEnd() callback set `settling = false` and destroy(flash). Keep the trailing `return;` — no close(), no onSuccess, no state change.

    Drive the settle ENTIRELY off the flash tween's .onEnd() callback. Introduce NO deferred-scheduler construct of any kind (check-safety.sh bans them and scans identifier tokens, not just prose) — the only new engine globals (add, rect, color, opacity, tween, easings, destroy, width, height) must stay INSIDE the openChallenge/choose function bodies (a727c13). Do NOT add a "refuse to answer" or "permanently lock the wrong box" path — the challenge must always stay resolvable.
  </action>
  <verify>
    <automated>bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-gate.sh && grep -c "CONFIG.GATE.WRONG_SHAKE" src/ui/challenge.js</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-safety.sh` exits 0 (no setTimeout/setInterval/wait(/loop(/lifespan(/countdown token in src/; SAFE-01 non-punishing).
    - `bash scripts/check-import-safety.sh` exits 0 (new engine globals stay inside function bodies — a727c13).
    - `bash scripts/check-gate.sh` exits 0 (challenge firewall/anti-leak/no-timer/one-way contracts intact).
    - `grep -c "CONFIG.GATE.WRONG_SHAKE" src/ui/challenge.js` >= 1 (shake magnitude sourced from config, not an inline literal).
    - Source assertion: `settling` is declared with `let` inside openChallenge (closure-local) and cleared inside a `.onEnd(` callback (grep both present in src/ui/challenge.js).
  </acceptance_criteria>
  <done>A wrong pick flashes red + shakes and holds input inert for WRONG_SETTLE_MS via a tween().onEnd()-cleared `settling` flag; a correct pick still closes once; all three gates green. The perceptual mash-test proof is deferred to the consolidation plan (34.6.1-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player input → challenge overlay | Constrained 1-4 key / box-click surface, already bounds-guarded in choose(i) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6.1-01 | Denial of Service | challenge.js settle | low | accept | Near-nil surface: client-only canvas; the settle self-clears via tween().onEnd(), and the anti-soft-lock rule (settling always returns to false, same question stays answerable) is a must-have — a stuck `settling` is prevented by construction, not an external DoS vector. No new input sink, no DOM/markup path (challenge.js is pure canvas draws with a documented no-injection guard). |
</threat_model>

<verification>
- `bash scripts/check-safety.sh` — no banned timer/scheduler token, no punishment construct (the critical D-01 gate).
- `bash scripts/check-import-safety.sh` — a727c13 top-level engine-global trap.
- `bash scripts/check-gate.sh` — challenge seam structural invariants survive the edit.
- Deferred to 34.6.1-04 (human): the mash-test proof (burst 1-2-3-4 on a wrong-first question; wrong registers, panel stays open, correct still closes, never punishing).
</verification>

<success_criteria>
- CONFIG.GATE carries WRONG_SETTLE_MS / WRONG_FLASH_OPACITY / WRONG_SHAKE / WRONG_FLASH_Z.
- challenge.js guards choose(i) with a closure-local `settling` flag cleared by the flash tween's onEnd().
- check-safety.sh, check-import-safety.sh, check-gate.sh all exit 0.
</success_criteria>

<output>
Create `.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-01-SUMMARY.md` when done.
</output>
