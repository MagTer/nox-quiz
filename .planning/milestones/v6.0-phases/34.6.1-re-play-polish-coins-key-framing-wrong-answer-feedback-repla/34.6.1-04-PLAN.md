---
phase: 34.6.1
plan: 04
type: execute
wave: 3
depends_on: [34.6.1-03]
files_modified:
  - .planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-VALIDATION.md
autonomous: false
requirements: [polish]
must_haves:
  truths:
    - "The full gate suite (safety, import-safety, gate, assets-manifest, validate-levels, browser-boot, mechanic-audit) is green after all three fixes land"
    - "D-01: a human confirms a wrong answer visibly registers and mashing 1-2-3-4 can no longer slip a wrong pick past — and it never feels punishing (no lost progress, no game-over)"
    - "D-02: a human confirms the coin reads crisp and on-palette (gold, no pink) and spins smoothly at the 1.5x display scale"
    - "D-03: a human confirms the finish reads unmistakably as a checkered flag (not the old key-ish blob) and the goal still fires on >=2 levels"
    - "D-04: a human confirms the pickup reads as a key, the popup/HUD 'skip the final math' framing is legible ASCII (no TOFU), and holding the key skips the end math for full XP on an even level"
  artifacts:
    - "34.6.1-VALIDATION.md sign-off section updated (gates green + human proofs recorded)"
  key_links:
    - "browser-boot + mechanic-audit are the only gates that actually drive the game across all 8 levels — they prove goals fire / keys pick up after the sprite swaps"
    - "the perceptual proofs (crispness, reads-as, mash feel) are automation-blind — human sign-off is the real end-state proof ('checks that don't play the game lie')"
---

<objective>
Consolidate the phase: run the full automated gate suite over all three fixes, then perform the four Manual-Only human sign-offs the automation is blind to (mash feel, coin crispness, goal legibility, key legibility + skip framing). Record the results in the phase validation doc.

Purpose: The phase's real end-state proof is playing it. The gates prove no regression and no banned construct; the human proofs prove the polish actually reads and feels right to the kid.
Output: A green full-suite run + recorded human sign-off; 34.6.1-VALIDATION.md updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-CONTEXT.md
@.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-VALIDATION.md
@.claude/CLAUDE.md
</context>

<artifacts_this_phase_produces>
- 34.6.1-VALIDATION.md: "Validation Sign-Off" checklist completed; per-task map + Manual-Only rows marked green
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated gate suite green over all three fixes</name>
  <files>.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-VALIDATION.md</files>
  <read_first>
    - 34.6.1-VALIDATION.md "Test Infrastructure" (Full suite command) and "Phase change -> validation map" — the exact gate list and what each proves.
    - .claude/CLAUDE.md "Verification gates" — the serving convention (python3 -m http.server from repo root) that browser-boot.mjs/audit-phase21-mechanics.mjs rely on.
    - 34.6.1-RESEARCH.md §5 "Which gate proves each fix".
  </read_first>
  <action>
    Run the full suite and confirm every gate exits 0:
    bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-gate.sh && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs

    browser-boot.mjs drives all 8 levels in a real browser (goals fire, coins render/spin, keys pick up on even levels); audit-phase21-mechanics.mjs confirms the key-pickup + end-gate mechanics report triggered:true (a `resolved:false` row is known headless-timing flakiness per the mechanic-audit note, not a failure). If any gate fails, capture the failure and route it back to the owning plan (34.6.1-01/02/03) as a gap rather than editing here.

    Update 34.6.1-VALIDATION.md: mark the per-task verification map rows green and record the full-suite result. Do NOT touch src/ in this plan — this is verification + recording only.
  </action>
  <verify>
    <automated>bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-gate.sh && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The chained full-suite command exits 0 (every gate green).
    - browser-boot.mjs reports goals firing and coins/keys rendering across all levels; audit-phase21-mechanics.mjs reports key-pickup + end-gate triggered:true.
    - 34.6.1-VALIDATION.md per-task map rows are updated to reflect the green run.
  </acceptance_criteria>
  <done>The full automated gate suite is green over D-01/D-02/D-03/D-04; results recorded in the validation doc.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — the four Manual-Only re-play proofs (mash feel, coin crispness, flag legibility, key legibility + skip framing)</name>
  <action>Blocking human sign-off. A human serves and plays the game to confirm the four automation-blind proofs below. This is the phase's kid-facing end-state proof; failures route back to the owning plan (34.6.1-01/02/03) as gaps. Not auto-approvable.</action>
  <what-built>All three re-play polish fixes are wired and the full automated gate suite is green: D-01 wrong-answer settle/flash in the challenge dialog, D-02 crisp coin, D-03 checkered-flag goal, D-04 real key sprite + skip framing on even levels.</what-built>
  <how-to-verify>
    Serve the game from the repo root (python3 -m http.server 8000) and open http://localhost:8000/src/index.html (or the host LAN IP for a real device). Confirm all four Manual-Only proofs:
    1. D-01 mash + non-punishing: open a math challenge (door/gate/enemy/end-gate), rapidly mash 1-2-3-4 in one burst on a question where the correct key is not last. A wrong pick MUST visibly register (red flash + shake) and the panel MUST stay open — you cannot blow through it during the brief settle. Then confirm a single CORRECT press still clears instantly (fires success once, closes clean). Confirm it never feels punishing (no lost progress, no game-over, no countdown).
    2. D-02 coin crispness: walk over coins and eyeball the sprite at the 1.5x display scale — crisp (no blur/mush), spins smoothly, gold/grunge (no pink).
    3. D-03 goal legibility: reach the goal on >=2 levels — it reads unmistakably as a checkered finish flag (not the old key-ish blob) and still fires LEVEL CLEAR.
    4. D-04 key legibility + skip framing: on an even level (02/04/06/08), grab the key — the pickup reads as a key (not a box), the popup ("+ SKIP KEY" or similar) and the HUD indicator make the "skip the final math" purpose clear in plain ASCII (no TOFU boxes), and holding it skips the end math for full XP.
  </how-to-verify>
  <resume-signal>Reply "polish approved" once all four proofs pass, or describe which fix needs rework (routes back to the owning plan as a gap). Do not auto-approve — this is the phase's kid-facing end-state proof.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no new code, no new input sink, client-only static game |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6.1-05 | (n/a) | verification/sign-off | low | accept | No new attack surface — this plan runs existing gates and records human sign-off; it introduces no code, assets, or input paths. The one supply-side concern (asset CC0 provenance) was mitigated at 34.6.1-02's blocking checkpoint. |
</threat_model>

<verification>
- Full suite: check-safety.sh, check-import-safety.sh, check-gate.sh, check-assets-manifest.mjs, validate-levels.mjs, browser-boot.mjs, audit-phase21-mechanics.mjs — all exit 0.
- Human (blocking checkpoint): all four Manual-Only proofs pass (mash feel, coin crispness, flag legibility, key legibility + skip framing).
</verification>

<success_criteria>
- Full automated gate suite green over all three fixes.
- All four human Manual-Only proofs signed off.
- 34.6.1-VALIDATION.md sign-off section completed.
</success_criteria>

<output>
Create `.planning/phases/34.6.1-re-play-polish-coins-key-framing-wrong-answer-feedback-repla/34.6.1-04-SUMMARY.md` when done.
</output>
