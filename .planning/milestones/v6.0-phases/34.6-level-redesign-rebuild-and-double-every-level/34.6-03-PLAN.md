---
phase: 34.6-level-redesign-rebuild-and-double-every-level
plan: 03
type: execute
wave: 2
depends_on: [34.6-01, 34.6-02]
files_modified:
  - src/levels/level-02.js
  - scripts/smoke-progress.mjs
  - scripts/audit-endgate-key.mjs
autonomous: true
requirements: [LEN-01, LEN-02]
must_haves:
  truths:
    - "level-02 is REBUILT from scratch at ~2x length as the intense + vertical swamp (even archetype), verticality achieved in OPEN AIR (no overlapping tiers / no ceilings — the L1–2 rule)"
    - "level-02 places an OPTIONAL math-skip key on a high route (geometry.keys, NO geometry.locks); a driven player who takes the high route collects it in a WALKED stretch"
    - "A driven player who HOLDS the key clears level-02 with full XP and the end math challenge never opens; one who does NOT gets the end math challenge"
    - "The keys-without-locks level is a clean validate-levels PASS (zero key-lock softlock rows) and completable from spawn with every coin collectable"
  artifacts:
    - "src/levels/level-02.js — rebuilt intense/vertical descriptor with a math-skip key and NO locks (throwaway 34.5 key+lock pair discarded); bounds.right hand-bumped"
    - "scripts/audit-endgate-key.mjs — NEW two-path in-engine audit (port 8775) iterating every math-skip level: Path A key→free clear, Path B no-key→math required"
    - "scripts/smoke-progress.mjs — level-02 golden fixture re-baselined"
  key_links:
    - "level-02 geometry.keys (no locks) → build.js emits a free-standing walk-through key → game.js heldKeyIds → clearLevel key path (plan 34.6-01)"
    - "audit-endgate-key Path A: get('key').length→0 after high-route drive, get('challenge').length unchanged at goal, gate-cleared marker present"
    - "audit-endgate-key Path B: get('challenge').length increments at goal"
---

<objective>
Rebuild `src/levels/level-02.js` from scratch as the intense + vertical swamp (even archetype) at ~2x length, and stand up the two-path in-engine audit that proves the key-as-math-skip mechanic BOTH ways. level-02 is the second half of the prototype pair the checkpoint (34.6-04) signs off. It carries the phase's first math-skip key: an OPTIONAL collectible on a harder high route with NO physical lock — hold it and the end math gate is skipped (full XP); miss it and answer the end math as normal.

Purpose: prove the intense/vertical archetype AND the redefined key mechanic on one level before committing levels 03–08. The two-path audit is the Wave-0 validation gap (static model is insufficient — milestone precedent).

Output (artifacts):
- rebuilt level-02 descriptor (math-skip key, no locks; the throwaway 34.5 key+lock pair discarded).
- `scripts/audit-endgate-key.mjs` — a NEW driven-player audit (fresh port 8775) that iterates every math-skip level (keys without locks) and proves Path A (key collected → free clear, no math) and Path B (no key → math required).
- re-baselined level-02 golden fixture.

Reconciliation to surface at the checkpoint (A3): level-02 is "intense + vertical" but L1–2 has NO ceilings (§8). Achieve verticality with tall single-tier climbs and descents in OPEN AIR (non-overlapping tiers), NOT by adding a ceiling. If validate-levels emits any `headroom` row on level-02, an overlapping pair slipped in.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-PATTERNS.md
@docs/LEVEL-DESIGN.md
@src/levels/level-02.js
@src/levels/level-08.js
@src/scenes/game.js
@scripts/audit-key-lock.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild level-02 (intense + vertical swamp, math-skip key, no lock) + re-baseline its smoke fixture</name>
  <files>src/levels/level-02.js, scripts/smoke-progress.mjs</files>
  <read_first>
    - src/levels/level-02.js — the file being rebuilt. Preserve id "level-02", allowedTables [2,3,4,5,6,7], biome "swamp". DISCARD the current geometry AND the throwaway 34.5 key+lock pair (keys x:900 + locks x:3960). Note the bounds convention: level-02 carries an explicit `bounds` field used AS-IS — hand-bump bounds.right.
    - src/levels/level-08.js — the EVEN/intense analog per PATTERNS.md (tall tiers, per-tier checkpoints, key-in-a-walked-stretch). Use its SHAPE; note its h:16 corrected tiers. But per RESEARCH Pitfall 1, level-02 stays OPEN-AIR (no overlapping tiers), unlike level-08's ceilings.
    - docs/LEVEL-DESIGN.md — §1 envelope, §2 gaps, §3.1 h:16, §3.3 rise bands, §3.4 grid trap, §3b coins, §4 math density, §5 checkpoints, §6 alcove, §8 ramp (L1–2: 120px gaps, gentle 60–70px, NO ceilings).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — "Level-Author Hard Constraints", "Descriptor Schema" (keys shape: `{ x, y: FLOOR_Y - 32 }`, locks OMITTED), Pitfall 1 (open-air verticality), Pitfall 2 (bounds.right), Pitfall 3 (key in a walked stretch).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md — the KEY = math-skip token decision; even = intense+vertical; descents; optional high route (the key lives on it); VISIBLE backtracking only.
    - scripts/smoke-progress.mjs — the level-02 fixture block (`expectedGeometry` ~lines 421–495, assertion ~496–497). Only this block re-baselines.
  </read_first>
  <action>
    Author a brand-new level-02 geometry roughly doubling the length (current goal.x 4200 → target goal.x in the ~8000–8400 range). Pure-data module importing ONLY ../config.js (a727c13). Preserve id "level-02", allowedTables [2,3,4,5,6,7], biome "swamp". Carry an explicit COMPLETE `bounds` literal { left:0, right:<hand-bumped>, top:0, bottom:360 } — set bounds.right to goal.x + CONFIG.GOAL_SIZE + a 64–80px buffer (the shipped convention); keep bounds.top 0 (verticality stays within the 360px screen — no negative-top climb on a swamp level).

    Make it intense + vertical in OPEN AIR: tall single-tier climbs and deliberate DESCENTS (drops onto ledges/floors below), all with open air above every platform — NO overlapping tiers, NO ceilings (L1–2 rule; verticality does not require a ceiling — Pitfall 1). Gaps 120px, rises gentle 60–70px, EVERY platform `h: 16`, climb-tier y OFF the 16px grid. Keep every route VISIBLE (no hidden branches) — she must always see where she is going.

    Place the math-skip KEY on the OPTIONAL high route: `keys: [{ x: <in a walked stretch of the high route>, y: FLOOR_Y - 32 }]` and OMIT `locks` entirely (math-skip = keys without locks). Per Pitfall 3, put the key squarely in a stretch the driven player WALKS (past every landing, before the next takeoff) so a natural drive collects it — never inside a jump-arc landing zone. The high route is harder (risk/reward) but skippable with no punishment; the safe ground route reaches the goal without the key.

    Math density LOCKED: one door + one enemy (both on solid floor, over-hole safe), `mathGates: []`, the end goal. All coins collectable (§3b); the high route carries extra coins. Checkpoints: one near spawn + one before every spike cluster. Exactly one secret alcove ~70px above an early platform. Use CONFIG expressions for goal/spike/checkpoint/door/enemy/key y-anchors (no raw magic numbers).

    Then re-baseline the level-02 golden fixture in scripts/smoke-progress.mjs: replace only the level-02 `expectedGeometry` with the new geometry so the deep-equal matches (Pitfall 4). Do NOT touch the level-01/03/04 fixture blocks.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/smoke-progress.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` PASS, zero HARD-FAIL for level-02; ZERO `key-lock` rows for level-02 (keys-without-locks empty-clean contract); ZERO `headroom` rows (no overlapping tiers).
    - `grep -c 'locks:' src/levels/level-02.js` is 0 (no locks field); level-02 has exactly one `keys:` entry with y `FLOOR_Y - 32`.
    - `grep -c 'h: 24' src/levels/level-02.js` is 0; bounds.right > goal.x (hand-bumped to the new extent).
    - `node scripts/browser-boot.mjs` reaches level-02 goal.x (±32) from spawn, no pageerror; `node scripts/audit-coins.mjs` collects every level-02 coin.
    - `node scripts/smoke-progress.mjs` green (level-02 fixture matches); only the level-02 fixture block changed.
    - Exactly one door, one enemy, `mathGates: []`, one secret alcove; goal.x ~2x the old 4200.
  </acceptance_criteria>
  <done>level-02 is a rebuilt, ~2x, open-air-vertical swamp with an optional math-skip key (no lock), clean on every static + in-engine gate.</done>
</task>

<task type="auto">
  <name>Task 2: Create the two-path end-gate key audit (scripts/audit-endgate-key.mjs)</name>
  <files>scripts/audit-endgate-key.mjs</files>
  <read_first>
    - scripts/audit-key-lock.mjs — the analog to copy VERBATIM: imports (createRequire/http/fs/path + getLevel/LEVEL_ORDER + deriveEncounters/driveToXPlanned/resolveIfBoxed), resolvePlaywright(), the loopback static server with the CR-02 path-traversal guard + loopback bind, SAVE_KEY/SAVE_BLOB seeding, and title→select→level navigation. The drive pattern (lines 188–265): driveToXPlanned(page, key.x, geometry, { targetY: key.y }), get("key").length === 0, the deriveEncounters(...).filter(...) resolve loop, result/exit convention.
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — "Two-Path Audit Requirement": Path A (key collected → free clear, challenge count unchanged + clear marker present) and Path B (no key → challenge increments); A4 distinguishing signal (get("challenge").length delta + gate-cleared marker).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-PATTERNS.md — "scripts/audit-endgate-key.mjs" section: exact copy list + the two-path drive adaptation + port 8775.
    - src/scenes/game.js — the key path renders "gate-cleared"-tagged objects and transitions to select (plan 34.6-01); the audit detects the clear via that marker / the scene transition.
    - CLAUDE.md — Playwright script duplication is DELIBERATE; copy plumbing by hand, do NOT extract a shared module, do NOT weaken the path-traversal or loopback guard.
  </read_first>
  <action>
    Create scripts/audit-endgate-key.mjs by copying audit-key-lock.mjs's Playwright plumbing verbatim (imports, resolvePlaywright, the loopback static server incl. the CR-02 resolve+clamp-to-ROOT guard and loopback-only bind, SAVE_KEY/SAVE_BLOB seeding, title→select→level navigation), using a fresh PORT 8775 (8774 is taken by audit-key-lock). Discover the target levels dynamically from the live registry: every level whose geometry has `keys?.length > 0` and no `locks?.length` (the math-skip levels — at this plan's time only level-02 qualifies; the script must auto-cover level-04/06/08 as they are rebuilt, so never hardcode level-02).

    For EACH such level run two drives against the real running engine, reading geometry live from getLevel(id) (never a hardcoded x/y):
    - Path B (no key → math required): fresh scene; drive straight toward goal.x, resolving every door/enemy encounter en route via the deriveEncounters/driveToXPlanned/resolveIfBoxed loop (same "resolve every remaining encounter first" pattern as audit-key-lock, so a mid-level challenge does not read as the goal); at the goal assert get("challenge").length INCREMENTS from its baseline (the end math opened).
    - Path A (key collected → free clear): fresh scene/re-enter; drive to the key first with driveToXPlanned(page, key.x, geometry, { targetY: key.y }); assert get("key").length === 0 (collected); resolve any door/enemy that sits between the key and the goal; drive to goal.x and assert get("challenge").length is UNCHANGED (no end math opened) AND a clear marker is present (get("gate-cleared").length > 0 or the scene has transitioned to select).

    Emit JSON results and use the pass/fail exit convention (process.exit(1) on any failure; a PASS line otherwise), matching audit-key-lock.mjs. This is a manually-invoked browser gate — NOT wired into check-gate.sh.
  </action>
  <verify>
    <automated>node scripts/audit-endgate-key.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/audit-endgate-key.mjs` exits 0 and reports, for level-02: Path A key collected + challenge count unchanged at goal + clear marker present; Path B challenge count incremented at goal.
    - The script discovers target levels dynamically (keys && !locks) — `grep -c '"level-02"' scripts/audit-endgate-key.mjs` is 0 (no hardcoded level id); it reads geometry from getLevel(id).
    - Uses PORT 8775; retains the CR-02 path-traversal guard and loopback-only bind verbatim (not weakened).
    - Not referenced by scripts/check-gate.sh (manual browser gate).
  </acceptance_criteria>
  <done>The two-path audit proves the key-skip mechanic both ways on every math-skip level, in the real engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor → build.js | Pure data; the key is a walk-through pickup with no new parsed input |
| local audit static server → filesystem | Dev-only loopback server serving the repo; path-traversal + loopback guard apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6-04 | DoS (self) | softlock via an unreachable/never-walked key | low | mitigate | Math-skip key is OPTIONAL (missing it → normal math path, never a dead end); audit Path A proves a driven player actually collects it (Pitfall 3) |
| T-34.6-05 | Info disclosure (dev-only) | audit-endgate-key.mjs static server | low | mitigate | CR-02 resolve+clamp-to-ROOT path-traversal guard + loopback-only bind copied verbatim from audit-key-lock.mjs; not weakened |
| T-34.6-SC | Tampering | package installs | n/a | accept | No installs this phase — reuses the already-present Playwright via gsd-pi resolution |
</threat_model>

<verification>
Full gate suite + the new audit after both tasks:
```
bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh \
  && bash scripts/check-progress.sh && bash scripts/check-terrain-atlas.sh && bash scripts/check-pink-gate.sh \
  && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs \
  && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs \
  && node scripts/smoke-progress.mjs && node scripts/audit-endgate-key.mjs
```
audit-phase21-mechanics output: level-02's door + enemy `triggered: true`. audit-key-lock.mjs will now report level-02 has no keys/locks to drive (the throwaway pair is gone) — that is expected; audit-endgate-key.mjs is the replacement proof for the math-skip design.
</verification>

<success_criteria>
- level-02 rebuilt ~2x, intense/vertical open-air, with an optional math-skip key (no lock) collectable in a walked stretch.
- audit-endgate-key.mjs proves Path A (key → free clear) and Path B (no key → math) in-engine for every math-skip level.
- Zero HARD-FAILs; every coin collected; completable from spawn; keys-without-locks a clean PASS; smoke-progress green.
</success_criteria>

<output>
Create `.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-03-SUMMARY.md` when done.
</output>
