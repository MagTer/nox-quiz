---
phase: 34.6-level-redesign-rebuild-and-double-every-level
plan: 11
type: execute
wave: 6
depends_on: [34.6-05, 34.6-06, 34.6-07, 34.6-08, 34.6-09, 34.6-10]
files_modified:
  - docs/LEVEL-REVIEW.md
autonomous: true
requirements: [LEN-01, LEN-02]
must_haves:
  truths:
    - "The full consolidated gate suite is green across ALL 8 rebuilt levels in one run — validate-levels zero HARD-FAIL, browser-boot completable from spawn, audit-coins every coin, audit-endgate-key both paths for every even level, smoke-progress all fixtures"
    - "The 13 currently-deferred Phase-34 HARD-FAILs (8 coin on 01–03, 5 headroom on 07) are GONE — the new geometry does not reintroduce them and no check was weakened to pass"
    - "A documented platform-placement review of all 8 rebuilt levels against the FULL docs/LEVEL-DESIGN.md rule set (incl. the headroom rule) exists"
    - "Every level is DISTINCT; even (2/4/6/8) are intense+vertical with a math-skip key; odd (1/3/5/7) are calmer intros; level-07 (staircase) ≠ level-08 (switchback)"
  artifacts:
    - "docs/LEVEL-REVIEW.md — Phase 34.6 rebuild review (per-level rule-compliance record + the deferred-HARD-FAIL deletion confirmation)"
  key_links:
    - "all 8 descriptors + the two-path audit + smoke fixtures → one green consolidated suite (the phase gate)"
---

<objective>
Close Phase 34.6: prove the whole rebuilt world is green in one consolidated run and produce the documented 8-level platform-placement review (SC3). By this point all 8 levels are rebuilt (prototype 01/02 + production 03–08), the end-gate is key-conditional, and the two-path key audit covers every even level. This plan is the phase-level HARD gate: the full LEVEL-DESIGN §9 suite + the two-path key audit + smoke-progress must ALL pass across every level, zero HARD-FAILs, with none of the 13 deferred Phase-34 fails reintroduced.

Output: the documented review in docs/LEVEL-REVIEW.md (per-level compliance against the full rule set, including headroom, plus the deferred-HARD-FAIL deletion confirmation). No source changes here unless a gate turns up a real defect — in which case fix the offending descriptor (do NOT weaken a check to make it green).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-VALIDATION.md
@docs/LEVEL-DESIGN.md
@docs/LEVEL-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full consolidated suite green across all 8 levels (zero HARD-FAIL)</name>
  <files>docs/LEVEL-REVIEW.md</files>
  <read_first>
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-VALIDATION.md — the full-suite command + sampling; "before /gsd-verify-work: full suite green, zero HARD-FAILs across all 8 levels".
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — "Validation Architecture" (Success Criterion → Proving Gate table) and "Common Pitfalls Cross-Check with the 13 deferred HARD-FAILs" (the 8 coin + 5 headroom fails must be gone; do NOT weaken a check).
    - docs/LEVEL-DESIGN.md §9 — the workflow gate list.
  </read_first>
  <action>
    Run the full LEVEL-DESIGN §9 gate suite plus the two-path key audit and smoke-progress, in one pass, and confirm every gate is green across all 8 levels with zero HARD-FAILs. Read the output of the manually-invoked browser gates (audit-phase21-mechanics always exits 0 — every door/enemy must show `triggered: true`; `resolved:false` rows are known headless-timing flakiness). If any gate reports a real HARD-FAIL, fix the OFFENDING descriptor (geometry) and its smoke fixture if applicable, then re-run — never weaken or skip a check to make it green (milestone precedent: checks that don't play the game lie).

    Confirm specifically: validate-levels reports zero HARD-FAIL and NONE of the 13 deferred Phase-34 fails remain (0 coin-reachability on 01–03, 0 headroom on 07); browser-boot reaches goal.x on every level from spawn; audit-coins collects every coin; audit-endgate-key exits 0 covering every even level (02/04/06/08) both paths; smoke-progress passes all four fixtures. Record the run outcome in docs/LEVEL-REVIEW.md (Task 2 owns the full write-up; this task records the green-suite result line).
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && bash scripts/check-terrain-atlas.sh && bash scripts/check-pink-gate.sh && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs && node scripts/smoke-progress.mjs && node scripts/audit-endgate-key.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the verify chain exits 0; `node scripts/validate-levels.mjs` prints PASS with zero HARD-FAIL across all 8 levels.
    - No coin-reachability HARD-FAIL on levels 01–03 and no headroom HARD-FAIL on level-07 (the 13 deferred fails are deleted).
    - `node scripts/audit-endgate-key.mjs` exits 0 and reports Path A + Path B for every even level (02, 04, 06, 08).
    - `node scripts/audit-phase21-mechanics.mjs` output shows `triggered: true` for every level's door + enemy.
    - No check was weakened/skipped (git diff shows no edits to any scripts/check-*.sh, scripts/*.mjs gate thresholds, or validator logic to force green).
  </acceptance_criteria>
  <done>The whole rebuilt world passes the consolidated suite in one run, zero HARD-FAIL, deferred fails deleted, no check weakened.</done>
</task>

<task type="auto">
  <name>Task 2: Write the documented 8-level platform-placement review (docs/LEVEL-REVIEW.md)</name>
  <files>docs/LEVEL-REVIEW.md</files>
  <read_first>
    - docs/LEVEL-REVIEW.md — the existing review doc (Phase-34 input record); append a "Phase 34.6 rebuild review" section rather than deleting the historical record.
    - docs/LEVEL-DESIGN.md — the full rule set the review checks each level against (§1–§8, esp. §3.2 headroom, §4 density, §8 ramp/biome-pair).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md — every level DISTINCT; even=intense+vertical+key; odd=calmer intro; biome pairs; level-07≠level-08.
  </read_first>
  <action>
    Add a "Phase 34.6 rebuild review" section to docs/LEVEL-REVIEW.md documenting, per level (01–08): its archetype (odd calm / even intense+vertical), biome, approximate new length vs old (roughly doubled), and a compliance check against the full LEVEL-DESIGN rule set — gaps, rise bands, h:16, headroom (≥24 on every overlapping tier, or "no overlapping tiers" for L1–2), math density (1 door + 1 enemy + end gate), checkpoints, secret alcove, bounds hand-bump, and (even levels) the math-skip key on a walked high route. Record that every level is distinct, that level-07 (monotonic staircase) and level-08 (switchback) remain mechanically different (LVL-02), and that the 13 deferred Phase-34 HARD-FAILs are deleted. Note the A1/A3 checkpoint decisions locked in plan 34.6-04. Reference the green consolidated-suite result from Task 1. Keep it a factual compliance record, not a to-do list.
  </action>
  <verify>
    <automated>test -s docs/LEVEL-REVIEW.md && grep -qi '34.6' docs/LEVEL-REVIEW.md && echo "review section present"</automated>
  </verify>
  <acceptance_criteria>
    - docs/LEVEL-REVIEW.md contains a Phase 34.6 rebuild-review section covering all 8 levels with a per-level rule-compliance record (incl. headroom + density + biome-pair archetype).
    - The review records the deferred-HARD-FAIL deletion, the level-07≠level-08 differentiation, and the locked A1/A3 decisions.
    - The historical Phase-34 review content is preserved (appended, not overwritten).
  </acceptance_criteria>
  <done>The documented 8-level platform-placement review (SC3) exists and reflects the green rebuilt world.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6-13 | Repudiation | green-suite claimed without proof | low | mitigate | The consolidated verify chain must exit 0; the review records the actual run; no check weakened to pass (git-diff acceptance criterion) |
| T-34.6-SC | Tampering | package installs | n/a | accept | No installs this phase |

Static offline browser game — no network/PII/auth. Documentation + verification only.
</threat_model>

<verification>
This plan IS the phase verification. The Task-1 verify chain is the full LEVEL-DESIGN §9 suite + audit-endgate-key + smoke-progress. All green, zero HARD-FAIL, across all 8 levels, is the phase gate before /gsd-verify-work.
</verification>

<success_criteria>
- Full consolidated suite green across all 8 rebuilt levels in one run; zero HARD-FAILs; the 13 deferred fails deleted; no check weakened.
- Two-path key audit green for every even level; smoke-progress green; every door/enemy triggers.
- Documented 8-level platform-placement review (SC3) recorded in docs/LEVEL-REVIEW.md.
</success_criteria>

<output>
Create `.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-11-SUMMARY.md` when done.
</output>
