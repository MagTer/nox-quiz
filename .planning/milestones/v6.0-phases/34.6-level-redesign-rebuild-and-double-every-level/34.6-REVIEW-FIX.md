---
phase: 34.6-level-redesign-rebuild-and-double-every-level
fixed_at: 2026-07-16T00:00:00Z
review_path: .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-REVIEW.md
iteration: 1
findings_in_scope: 3
fixed: 3
skipped: 0
status: all_fixed
---

# Phase 34.6: Code Review Fix Report

**Source review:** 34.6-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 3 (WR-01, WR-02, IN-01)
- Fixed: 3
- Skipped: 0

## Fixed Issues

### WR-01: End-gate key-skip fired for ANY held key (lock-keys conflated with math-skip tokens)

**Files modified:** `src/scenes/game.js`
**Commit:** 8be9f98
**Applied fix:** Added a closure-local `const endSkipArmed = (level.geometry.keys?.length ?? 0) > 0 && (level.geometry.locks?.length ?? 0) === 0`, computed once at wire time right after the level loads. The end-skip branch in `onReachGoal` now gates on `endSkipArmed && heldKeyIds.size > 0` instead of bare `heldKeyIds.size > 0`. A math-skip level (key, no lock) still skips its end math gate; a hypothetical key+lock level correctly still requires its end math even after its lock-key is collected. No module-level `let` introduced (verified by grep + `check-import-safety.sh`).

### WR-02: LEVEL CLEAR banner + clear SFX duplicated byte-for-byte across two files with magic numbers

**Files modified:** `src/config.js`, `src/ui/mathGate.js`, `src/scenes/game.js`
**Commit:** 0739016
**Applied fix:** Extracted the banner + `audio.playSfx("clear")` into a single exported helper `renderLevelClearBanner()` in `mathGate.js`. The shared `clearLevel()` in `game.js` now calls it exactly once, so BOTH the math path (`openMathGate` onSuccess → onClear → clearLevel) and the key path (onReachGoal endSkip branch → clearLevel) render one identical banner per clear. Neither caller renders its own banner anymore (onSuccess just fires onClear; the key branch's inline `add(...)` block was deleted) — no double-render. The bare literals `size: 30`, `z(9990)`, `z(9994)` were lifted into `CONFIG.GATE.CLEAR_TEXT_SIZE` / `CLEAR_DIM_Z` / `CLEAR_TEXT_Z`. Visual output (text, size, z-order sandwich 9990 < 9993 burst < 9994, sfx) is byte-preserved. Requires human verification of the on-screen banner is not needed beyond the audit signal — it is a pure refactor with identical literals, confirmed by the audit's `clearMarkerPresent`/`bannerCaught` signals on both paths.

### IN-01: audit-endgate-key.mjs asserted "free clear" but never verified XP was awarded or persisted

**Files modified:** `scripts/audit-endgate-key.mjs`
**Commit:** 8fd52e2
**Applied fix:** Path A now snapshots the persisted save before the goal drive (`saveBefore`), reads it back after the clear (`saveAfter`), and asserts `saveAfter.{xp,level}` equals `predictAward(saveBefore, CONFIG.PROGRESS.XP_KEY_SKIP)` — using `progress.js`'s own carry-over math as the single source of truth — plus `saveAfter.levels[levelId].cleared === true`. Both are folded into `pathAHolds` and surfaced in the results row + offender message. Imports `CONFIG` and `predictAward` (both node-safe: config is leaf data, progress.js touches no localStorage at import). A key-path XP/writeSave regression (addBonusXp not called, wrong amount, no-op markCleared/writeSave) now fails the audit.

## Re-verification Results

All MANDATORY re-verification passed:

1. **`node scripts/audit-endgate-key.mjs`** — PASS. 4 math-skip levels (02/04/06/08) proved both ways: key → free clear (challengeOpened=false), no-key → end math required (challengeOpened=true). New IN-01 checks green on every level: XP accumulated exactly +20 per clear (0→20→40→60→80), `xpPersisted=true`, `clearedPersisted=true`.
2. **`node scripts/audit-phase21-mechanics.mjs`** — PASS. "ALL MECHANICS RESOLVED"; door/enemy/secret-alcove all `triggered: true` + `resolved: true`. The LEVEL CLEAR moment (`gate-cleared` marker) confirmed present on both clear paths via the endgate-key audit.
3. **`node scripts/validate-levels.mjs`** — PASS (zero HARD-FAIL).
4. **Static gates** — all green: `check-safety.sh` (no-timer / forgiving), `check-import-safety.sh` (no module-top-level engine global; no module-level `let`), `check-gate.sh` (math-gate invariants), `check-progress.sh` (progress/save invariants incl. smoke-progress).
5. **Banner renders exactly once per clear** — confirmed by construction: the only `text("LEVEL CLEAR")` site is `renderLevelClearBanner()` (mathGate.js), called from the single site `game.js:244` inside `clearLevel()`, which runs once per clear via the `goalReached` + challenge fire-once latches. Both math and key paths route through it; neither caller renders its own banner.

---

_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
