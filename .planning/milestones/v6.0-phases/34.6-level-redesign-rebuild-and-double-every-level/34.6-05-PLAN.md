---
phase: 34.6-level-redesign-rebuild-and-double-every-level
plan: 05
type: execute
wave: 4
depends_on: [34.6-04]
files_modified:
  - src/levels/level-03.js
  - scripts/smoke-progress.mjs
autonomous: true
requirements: [LEN-01, LEN-02]
must_haves:
  truths:
    - "level-03 is REBUILT from scratch at ~2x length as the calmer TOWN intro (odd archetype), authored to the checkpoint-proven shape"
    - "level-03 uses the L3–6 band (140–160px gaps, mixed 60–75px rises); ceilings may start appearing but any overlapping tier keeps headroom ≥24 (rise ≥72 with h:16)"
    - "level-03 has a descent + an optional visible high route (extra coins, no key on this odd level); no hidden routes"
    - "Every coin collectable in-engine; completable from spawn; math density exactly 1 door + 1 enemy + end gate"
  artifacts:
    - "src/levels/level-03.js — rebuilt town-intro descriptor; bounds.right hand-bumped"
    - "scripts/smoke-progress.mjs — level-03 golden fixture re-baselined"
  key_links:
    - "getLevel('level-03').geometry === smoke-progress level-03 expectedGeometry"
    - "validate-levels + audit-coins + browser-boot green on level-03"
---

<objective>
Rebuild `src/levels/level-03.js` from scratch as the calmer TOWN intro (odd archetype) at ~2x length, to the shape signed off on the prototype (34.6-04). Town is the L3–6 band, where ceilings start appearing — but level-03 stays the calmer of the town pair (level-04 is the intense one). Pure-data rebuild; the append-only convention is suspended (LEVEL-DESIGN §9.1).

Output: rebuilt level-03 descriptor + re-baselined smoke fixture. Odd level → NO key.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-PATTERNS.md
@docs/LEVEL-DESIGN.md
@src/levels/level-03.js
@src/levels/level-01.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild level-03 (town intro, calm, ~2x, L3–6 band)</name>
  <files>src/levels/level-03.js</files>
  <read_first>
    - src/levels/level-03.js — the file being rebuilt (discard current geometry, do NOT append). Preserve id "level-03", allowedTables [3,4,5,6,7,8,9], biome "town", and the explicit `bounds` field (used AS-IS — hand-bump bounds.right).
    - src/levels/level-01.js — the ODD/calm analog (post-rebuild if available) for the calm-intro shape.
    - docs/LEVEL-DESIGN.md — §3.1 h:16, §3.2 headroom (rise ≥72 for any overlapping tier), §3.3 rise bands, §3.4 grid trap, §3b coins, §4 density, §5 checkpoints, §6 alcove, §8 ramp (L3–6: 140–160px gaps, mixed 60–75px, ceilings start appearing).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — "Level-Author Hard Constraints" + "Descriptor Schema".
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md — level-shape brief (descents, optional high routes, visible backtracking, odd=calmer intro); plus the checkpoint-locked A3 ceiling decision.
  </read_first>
  <action>
    Author a brand-new level-03 geometry roughly doubling the length (current goal.x 5120 → target ~10000–10400). Pure-data module importing ONLY ../config.js (a727c13). Preserve id "level-03", allowedTables [3,4,5,6,7,8,9], biome "town". Carry an explicit COMPLETE `bounds` literal with bounds.right hand-bumped to goal.x + CONFIG.GOAL_SIZE + a 64–80px buffer; bounds.top 0 (town stays within the 360px screen).

    L3–6 band: gaps 140–160px (bridge anything >160 with a platform), mixed 60–75px rises, EVERY platform `h: 16`, climb-tier y OFF the grid. Ceilings MAY start appearing per the checkpoint-locked A3 rule — but any overlapping tier pair MUST satisfy headroom ≥24 (rise ≥72 with h:16). Keep it the CALMER town level (fewer/gentler ceilings than level-04). Include at least one deliberate DESCENT and one OPTIONAL visible high route carrying extra coins (NO key — odd level). Every route VISIBLE (no hidden branches). Math density LOCKED: one door + one enemy (solid floor, over-hole safe), `mathGates: []`, end goal. Every coin collectable; checkpoints one near spawn + one before every spike cluster; exactly one secret alcove ~70px above an early platform. CONFIG expressions for all y-anchors.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` PASS, zero HARD-FAIL for level-03 (structural, coin, over-hole, headroom).
    - `node scripts/browser-boot.mjs` reaches level-03 goal.x (±32) from spawn; `node scripts/audit-coins.mjs` collects every level-03 coin.
    - `grep -c 'h: 24' src/levels/level-03.js` is 0; bounds.right hand-bumped (> goal.x); goal.x ~2x the old 5120.
    - Exactly one door, one enemy, `mathGates: []`, one secret alcove, no `keys:`/`locks:` fields (odd level).
    - Any overlapping tier passes headroom (zero `headroom` HARD-FAIL rows).
  </acceptance_criteria>
  <done>level-03 is a rebuilt, ~2x, calm town intro that validates and drives clean with every coin collectable.</done>
</task>

<task type="auto">
  <name>Task 2: Re-baseline the level-03 golden fixture (scripts/smoke-progress.mjs)</name>
  <files>scripts/smoke-progress.mjs</files>
  <read_first>
    - scripts/smoke-progress.mjs — the level-03 fixture block (`expectedGeometry` ~lines 523–608, assertion ~609–610). Only this block changes.
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — Pitfall 4 (03/04 have fixtures; re-baseline in the same change).
  </read_first>
  <action>
    Replace the inline level-03 `expectedGeometry` with the new rebuilt geometry so the deep-equal matches the authored descriptor. Do NOT touch the level-01/02/04 fixture blocks.
  </action>
  <verify>
    <automated>node scripts/smoke-progress.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/smoke-progress.mjs` passes; only the level-03 fixture block changed.
  </acceptance_criteria>
  <done>The level-03 golden snapshot tracks the rebuilt descriptor.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6-07 | DoS (self) | softlock/dead-end from bad geometry | low | mitigate | validate-levels + browser-boot HARD gates; zero HARD-FAIL required |
| T-34.6-SC | Tampering | package installs | n/a | accept | No installs this phase |

Static offline browser game — no network/PII/auth. Geometry-only change.
</threat_model>

<verification>
Full gate suite (LEVEL-DESIGN §9):
```
bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh \
  && bash scripts/check-progress.sh && bash scripts/check-terrain-atlas.sh && bash scripts/check-pink-gate.sh \
  && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs \
  && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs \
  && node scripts/smoke-progress.mjs
```
audit-phase21-mechanics output: level-03's door + enemy `triggered: true`.
</verification>

<success_criteria>
- level-03 rebuilt ~2x, calm town intro (L3–6 band), descent + optional visible high route, no key.
- Zero HARD-FAILs; every coin collected; completable from spawn; smoke-progress green.
</success_criteria>

<output>
Create `.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-05-SUMMARY.md` when done.
</output>
