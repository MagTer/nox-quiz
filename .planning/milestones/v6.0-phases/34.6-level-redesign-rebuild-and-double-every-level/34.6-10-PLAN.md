---
phase: 34.6-level-redesign-rebuild-and-double-every-level
plan: 10
type: execute
wave: 5
depends_on: [34.6-04, 34.6-05]
files_modified:
  - src/levels/level-04.js
  - scripts/smoke-progress.mjs
autonomous: true
requirements: [LEN-01, LEN-02]
must_haves:
  truths:
    - "level-04 is REBUILT from scratch at ~2x length as the intense + vertical TOWN (even archetype)"
    - "level-04 places an OPTIONAL math-skip key on a high route (geometry.keys, NO geometry.locks), collectable in a walked stretch"
    - "A driven player who holds level-04's key clears it with full XP and no end math; one who does not gets the end math"
    - "level-04 uses the L3–6 band with real ceilings where present (headroom ≥24); every coin collectable; completable from spawn; density 1 door + 1 enemy + end gate"
  artifacts:
    - "src/levels/level-04.js — rebuilt intense/vertical town descriptor with a math-skip key and NO locks; bounds.right hand-bumped"
    - "scripts/smoke-progress.mjs — level-04 golden fixture re-baselined"
  key_links:
    - "level-04 geometry.keys (no locks) → game.js heldKeyIds → clearLevel key path"
    - "audit-endgate-key.mjs auto-covers level-04: Path A key→free clear, Path B no-key→math"
    - "getLevel('level-04').geometry === smoke-progress level-04 expectedGeometry"
---

<objective>
Rebuild `src/levels/level-04.js` from scratch as the intense + vertical TOWN (even archetype) at ~2x length, to the checkpoint-proven shape, carrying an OPTIONAL math-skip key on a high route (no lock). Town is the L3–6 band, so real ceilings may appear (overlapping tiers, headroom ≥24). Even → intense/vertical + key. Pure-data rebuild; append-only suspended.

This plan is Wave 5 because it re-baselines the level-04 golden fixture in scripts/smoke-progress.mjs, which level-03 (plan 34.6-05, Wave 4) also edits — the shared file serializes them. The two-path audit (audit-endgate-key.mjs) auto-covers level-04 once it has keys-without-locks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-PATTERNS.md
@docs/LEVEL-DESIGN.md
@src/levels/level-04.js
@src/levels/level-02.js
@src/levels/level-08.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild level-04 (town, intense + vertical, math-skip key, L3–6 band)</name>
  <files>src/levels/level-04.js</files>
  <read_first>
    - src/levels/level-04.js — the file being rebuilt (discard current geometry, do NOT append). Preserve id "level-04", allowedTables [6,7,8,9], biome "town", explicit `bounds` (hand-bump bounds.right).
    - src/levels/level-02.js (rebuilt) and src/levels/level-08.js — the EVEN/intense analogs (tall tiers, key in a walked stretch, per-tier checkpoints). h:16 tiers.
    - docs/LEVEL-DESIGN.md — §3.1 h:16, §3.2 headroom (rise ≥72 for overlapping tiers), §3.3 rise bands, §3.4 grid trap, §3.5 verticality/overlap/arc hygiene, §3b coins, §4 density, §5 checkpoints, §6 alcove, §8 ramp (L3–6, ceilings appearing).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — "Level-Author Hard Constraints", "Descriptor Schema" (keys `{ x, y: FLOOR_Y - 32 }`, locks OMITTED), Pitfall 2 (bounds.right), Pitfall 3 (key in a walked stretch).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md — KEY = math-skip token; even = intense+vertical; descents; optional high route (key lives on it); VISIBLE backtracking only; checkpoint-locked A3.
  </read_first>
  <action>
    Author a brand-new level-04 geometry roughly doubling the length (current goal.x 6120 → target ~12000–12400 — level-04 is the longest level; keep it well-paced with breather stretches). Pure-data module importing ONLY ../config.js (a727c13). Preserve id "level-04", allowedTables [6,7,8,9], biome "town". Carry an explicit COMPLETE `bounds` literal, bounds.right hand-bumped to goal.x + CONFIG.GOAL_SIZE + a 64–80px buffer; bounds.top 0 (or a small negative if a climb needs it).

    Make it intense + vertical: tall climbs + deliberate DESCENTS, real ceilings where overlapping tiers occur (rise 72–75px, headroom ≥24; ~70px x-overlap; no stepping stone inside an independently-jumpable gap). Gaps 140–160px, EVERY platform `h: 16`, climb-tier y OFF the grid. Keep every route VISIBLE.

    Place the math-skip KEY on the OPTIONAL high route: `keys: [{ x: <in a walked stretch of the high route>, y: FLOOR_Y - 32 }]`, OMIT `locks`. Key in a stretch the driven player WALKS (Pitfall 3); safe route reaches goal without it. Math density LOCKED: one door + one enemy (solid floor), `mathGates: []`, end goal. Every coin collectable; high route carries extra coins; checkpoints one near spawn + one before every spike cluster (and per-tier for climbs); exactly one secret alcove. CONFIG expressions for all y-anchors. Keep math-barrier spacing 300–750px (avoid a wall-of-interruptions given the extra length).
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-endgate-key.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` PASS, zero HARD-FAIL for level-04; ZERO `key-lock` rows; any overlapping tier passes headroom.
    - `grep -c 'locks:' src/levels/level-04.js` is 0; exactly one `keys:` entry, y `FLOOR_Y - 32`.
    - `node scripts/browser-boot.mjs` reaches level-04 goal.x (±32) from spawn; `node scripts/audit-coins.mjs` collects every coin.
    - `node scripts/audit-endgate-key.mjs` exits 0 and covers level-04: Path A key collected + no math at goal + clear marker; Path B math opens at goal.
    - `grep -c 'h: 24' src/levels/level-04.js` is 0; bounds.right hand-bumped; goal.x ~2x the old 6120; exactly one door, one enemy, `mathGates: []`, one secret alcove.
  </acceptance_criteria>
  <done>level-04 is a rebuilt, ~2x, intense/vertical town with a proven optional math-skip key, clean on every gate.</done>
</task>

<task type="auto">
  <name>Task 2: Re-baseline the level-04 golden fixture (scripts/smoke-progress.mjs)</name>
  <files>scripts/smoke-progress.mjs</files>
  <read_first>
    - scripts/smoke-progress.mjs — the level-04 fixture block (`expectedGeometry` ~lines 613–748, assertion ~749–750). Only this block changes.
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — Pitfall 4 (01–04 have fixtures; re-baseline in the same change).
  </read_first>
  <action>
    Replace the inline level-04 `expectedGeometry` with the new rebuilt geometry so the deep-equal matches the authored descriptor. Do NOT touch the level-01/02/03 fixture blocks (level-03's block was already re-baselined in plan 34.6-05 — leave it intact).
  </action>
  <verify>
    <automated>node scripts/smoke-progress.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/smoke-progress.mjs` passes (all of levels 01–04 fixtures green); only the level-04 fixture block changed in this plan.
  </acceptance_criteria>
  <done>The level-04 golden snapshot tracks the rebuilt descriptor; all four fixtures are green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6-12 | DoS (self) | softlock via unreachable/never-walked key | low | mitigate | Math-skip key is OPTIONAL; audit-endgate-key Path A proves collection; validate-levels + browser-boot HARD gates |
| T-34.6-SC | Tampering | package installs | n/a | accept | No installs this phase |

Static offline browser game — no network/PII/auth. Geometry-only change.
</threat_model>

<verification>
Full gate suite + audit (LEVEL-DESIGN §9):
```
bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh \
  && bash scripts/check-progress.sh && bash scripts/check-terrain-atlas.sh && bash scripts/check-pink-gate.sh \
  && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs \
  && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs \
  && node scripts/smoke-progress.mjs && node scripts/audit-endgate-key.mjs
```
audit-phase21-mechanics output: level-04's door + enemy `triggered: true`.
</verification>

<success_criteria>
- level-04 rebuilt ~2x, intense/vertical town (L3–6 band, ceilings ok), with an optional math-skip key proven both paths in-engine.
- Zero HARD-FAILs; every coin collected; completable from spawn; keys-without-locks a clean PASS; all four smoke fixtures green.
</success_criteria>

<output>
Create `.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-10-SUMMARY.md` when done.
</output>
