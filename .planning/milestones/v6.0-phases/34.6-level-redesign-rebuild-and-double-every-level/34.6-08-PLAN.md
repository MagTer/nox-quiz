---
phase: 34.6-level-redesign-rebuild-and-double-every-level
plan: 08
type: execute
wave: 4
depends_on: [34.6-04]
files_modified:
  - src/levels/level-07.js
autonomous: true
requirements: [LEN-01, LEN-02]
must_haves:
  truths:
    - "level-07 is REBUILT from scratch at ~2x length as the calmer CASTLE intro (odd archetype), whose signature climb is a MONOTONIC STAIRCASE — mechanically distinct from level-08's switchback (LVL-02 preserved)"
    - "level-07 uses the L7–8 band (160px gaps, tight 72–75px climbs, real ceilings) with EVERY overlapping tier at headroom ≥24 (h:16) — deleting the 5 currently-deferred 9px-headroom HARD-FAILs"
    - "level-07 includes at least one deliberate descent; every route VISIBLE; no key (odd level)"
    - "Every coin collectable in-engine; completable from spawn; density 1 door + 1 enemy + end gate"
  artifacts:
    - "src/levels/level-07.js — rebuilt castle-intro staircase descriptor; bounds.right and bounds.top hand-set for the climb"
  key_links:
    - "validate-levels headroom rows for level-07 → ZERO (the rebuild deletes the 5 deferred 9px fails)"
    - "level-07 (staircase) ≠ level-08 (switchback) — LVL-02 differentiation holds"
---

<objective>
Rebuild `src/levels/level-07.js` from scratch as the calmer CASTLE intro (odd archetype) at ~2x length, to the checkpoint-proven shape. Castle is the L7–8 band (tight climbs, real ceilings). level-07's signature is a MONOTONIC STAIRCASE climb — it must stay mechanically DIFFERENT from level-08's switchback (LVL-02). Odd → NO key. This rebuild DELETES the 5 currently-deferred Phase-34 `headroom` HARD-FAILs on level-07's end climb (9px headroom on every tier) by authoring h:16 tiers at 72–75px rises from the start. Level-07 has NO smoke fixture. Pure-data rebuild; append-only suspended.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-PATTERNS.md
@docs/LEVEL-DESIGN.md
@src/levels/level-07.js
@src/levels/level-08.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild level-07 (castle intro, monotonic staircase, ~2x, L7–8 band)</name>
  <files>src/levels/level-07.js</files>
  <read_first>
    - src/levels/level-07.js — the file being rebuilt (discard current geometry, do NOT append). Preserve id "level-07", allowedTables [6,7,8], biome "castle", explicit `bounds` (hand-set bounds.right AND bounds.top for the climb — current is top -360).
    - src/levels/level-08.js — the castle EVEN analog and the switchback reference; level-07 must NOT converge with it (level-07 = monotonic staircase, level-08 = switchback).
    - docs/LEVEL-DESIGN.md — §3.1 h:16, §3.2 headroom (rise ≥72 for overlapping tiers; 9px shipped on level-07 because no rule existed — do not repeat), §3.3 rise bands (72–75 overlapping), §3.4 grid trap, §3.5 verticality/overlap/arc hygiene, §7 camera bounds (climb may go negative), §8 ramp (L7–8: 160px gaps, tight 72–75px, real ceilings), §8 note (level-07 monotonic staircase).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — "Level-Author Hard Constraints" (physics envelope maxRise 88.331 HARD ceiling), Pitfall 2 (bounds), "Common Pitfalls Cross-Check with the 13 deferred HARD-FAILs" (level-07's 5 headroom fails deleted by h:16 + 72–75 rises).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md — level-shape brief (descents; odd=calmer; VISIBLE backtracking only); checkpoint-locked shape.
  </read_first>
  <action>
    Author a brand-new level-07 geometry roughly doubling the length (current goal.x 3700, y −70 → target goal.x ~7400 with a climbing end). Pure-data module importing ONLY ../config.js (a727c13). Preserve id "level-07", allowedTables [6,7,8], biome "castle". Carry an explicit COMPLETE `bounds` literal; hand-set bounds.right to the new horizontal extent (goal.x + CONFIG.GOAL_SIZE + a 64–80px buffer) and bounds.top to cover the climb's highest tier (e.g. −360, matching the shipped castle convention) — the fall-respawn line is the global LEVEL_BOTTOM+FALL_MARGIN, independent of bounds.

    L7–8 band: gaps 160px, tight climbs with rises 72–75px, real ceilings where overlapping tiers occur — EVERY overlapping tier pair MUST satisfy headroom ≥24 (rise − 16 − 32 ≥ 24, i.e. rise ≥72), never exceeding the 88.331px HARD ceiling. ~70px x-overlap between consecutive climb tiers; no stepping stone inside an independently-jumpable gap. EVERY platform `h: 16`, climb-tier y OFF the grid.

    Make level-07's signature a MONOTONIC STAIRCASE end climb (steps rising in one direction) — distinct from level-08's leftward-rising switchback. Per the level-shape brief, also include at least one deliberate DESCENT section earlier in the level (so it is not purely upward), while keeping it the CALMER of the castle pair. Keep every route VISIBLE. Math density LOCKED: one door + one enemy (solid floor), `mathGates: []`, end goal (which may sit on a climb tier at a negative y). NO key (odd level). Every coin collectable; checkpoints one near spawn + one before every spike cluster AND one per climb tier the player lands on; exactly one secret alcove. CONFIG expressions for all y-anchors.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` PASS, zero HARD-FAIL for level-07; ZERO `headroom` rows (the 5 deferred 9px fails are gone).
    - `node scripts/browser-boot.mjs` reaches level-07 goal.x (±32) from spawn, driving the staircase climb, no pageerror; `node scripts/audit-coins.mjs` collects every coin.
    - `grep -c 'h: 24' src/levels/level-07.js` is 0; bounds.right and bounds.top hand-set; goal.x ~2x the old 3700.
    - Exactly one door, one enemy, `mathGates: []`, one secret alcove, no `keys:`/`locks:` (odd level).
    - level-07's climb is a monotonic staircase (distinct from level-08's switchback — the LVL-02 differentiation is preserved).
  </acceptance_criteria>
  <done>level-07 is a rebuilt, ~2x, headroom-clean castle staircase intro, mechanically distinct from level-08, clean on every gate.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6-10 | DoS (self) | cramped-headroom or unreachable climb tier | low | mitigate | validate-levels headroom + reachability HARD gates (rise ≥72, ≤88.331); browser-boot drives the climb from spawn |
| T-34.6-SC | Tampering | package installs | n/a | accept | No installs this phase |

Static offline browser game — no network/PII/auth. Geometry-only change.
</threat_model>

<verification>
Full gate suite (LEVEL-DESIGN §9):
```
bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh \
  && bash scripts/check-progress.sh && bash scripts/check-terrain-atlas.sh && bash scripts/check-pink-gate.sh \
  && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs \
  && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs
```
audit-phase21-mechanics output: level-07's door + enemy `triggered: true`. (No smoke fixture for level-07.)
</verification>

<success_criteria>
- level-07 rebuilt ~2x, calm castle staircase intro (L7–8 band, headroom-clean), with a descent, distinct from level-08, no key.
- Zero HARD-FAILs (the 5 deferred headroom fails deleted); every coin collected; completable from spawn.
</success_criteria>

<output>
Create `.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-08-SUMMARY.md` when done.
</output>
