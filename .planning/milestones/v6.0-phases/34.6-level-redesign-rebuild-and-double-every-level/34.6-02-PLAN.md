---
phase: 34.6-level-redesign-rebuild-and-double-every-level
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/levels/level-01.js
  - scripts/smoke-progress.mjs
autonomous: true
requirements: [LEN-01, LEN-02]
must_haves:
  truths:
    - "level-01 is REBUILT from scratch (not appended to) at roughly double its current length (goal.x ~3560 → ~7000+)"
    - "level-01 stays a soft landing: 120px gaps, gentle 60–70px rises, NO overlapping tiers (no ceilings) — the L1–2 rule that keeps it forgiving"
    - "level-01 has at least one deliberate DESCENT and one OPTIONAL high route (extra coins, no key on this odd level), all in VISIBLE view — no hidden routes"
    - "Every coin is collectable by a real driven player; every platform is h:16; every gap is crossable; the level is completable from spawn"
    - "Math density is exactly 1 door + 1 enemy + end gate (mathGates: [])"
  artifacts:
    - "src/levels/level-01.js — full rebuilt descriptor (no bounds field — level-01 derives its right edge)"
    - "scripts/smoke-progress.mjs — level-01 golden geometry fixture re-baselined to the new descriptor"
  key_links:
    - "getLevel('level-01').geometry === smoke-progress level-01 expectedGeometry (deep-equal)"
    - "validate-levels + audit-coins + browser-boot all green on the new level-01 geometry"
---

<objective>
Rebuild `src/levels/level-01.js` from scratch as the calm swamp intro (odd archetype) at roughly double its current length, authored against the FULL docs/LEVEL-DESIGN.md rule set from the first line. level-01 is the soft landing for a 12-year-old — it keeps 120px gaps, gentle 60–70px rises, and NO ceilings at all (the L1–2 rule), while adding descents, an optional visible high route, and doubling the platforming per math gate.

Purpose: One half of the prototype pair the human-verify checkpoint (34.6-04) signs off on. It proves the calm/soft-landing archetype and the ~2x length before levels 03–08 are committed to the same shape.

Output: the rebuilt level-01 descriptor + its re-baselined smoke-progress golden fixture. This DELETES the level-01 coin-reachability HARD-FAILs currently deferred from Phase 34 (they live on geometry this replaces).

This is a PURE-DATA rebuild: content never touches build.js. The append-only convention is explicitly SUSPENDED for this phase (LEVEL-DESIGN §9.1).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-PATTERNS.md
@docs/LEVEL-DESIGN.md
@src/levels/level-01.js
@src/levels/level-02.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the level-01 descriptor (calm swamp intro, ~2x, no ceilings)</name>
  <files>src/levels/level-01.js</files>
  <read_first>
    - src/levels/level-01.js — the file being rebuilt (current geometry is the "before"; discard it, do NOT append). Preserve id "level-01", allowedTables [6,7,8,9], biome "swamp", and the NO-bounds-field convention (level-01 derives its right edge in game.js — do not add a bounds field).
    - src/levels/level-02.js — the ODD/calm analog per PATTERNS.md (flat-run body: floors + gentle stepping-stone platforms + spikes + one door + one enemy, no ceilings). Use its SHAPE, not its numbers; note its platforms are the wrong h:24 — author h:16.
    - docs/LEVEL-DESIGN.md — the full rule set. Especially: §1 physics envelope (maxRise 88.331, never a bare gap >162), §2 floors/gaps (120px band for L1–2), §3.1 h:16 WYSIWYG, §3.3 rise bands (60–70px non-overlapping), §3.4 grid-snap trap, §3b coins collectable, §4 math barriers (1 door + 1 enemy, mathGates []), §5 checkpoints (one near spawn + one before every spike), §6 secret alcove, §8 ramp (L1–2: 120px gaps, gentle 60–70px, NO ceilings).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — "Level-Author Hard Constraints" (the numbers), "Descriptor Schema" (exact field shapes + y-conventions), and Pitfall 1 (no ceilings on L1–2).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md — level-shape brief: descents, optional high routes, VISIBLE backtracking only, odd=calmer intro, gentle ramp preserved (level-01 stays a soft landing).
  </read_first>
  <action>
    Author a brand-new level-01 geometry object, roughly doubling the length (current goal.x 3560 → target goal.x in the ~7000–7200 range; roughly double the floor runs, platforms, coins, and hazards to match). Keep the descriptor a pure-data module importing ONLY ../config.js (no engine globals — a727c13). Preserve id "level-01", allowedTables [6,7,8,9], biome "swamp"; refresh displayName if desired. Do NOT add a `bounds` field (level-01 alone derives its right edge from geometry).

    Author against the L1–2 band from the start: gaps at 120px (never a bare running-jump gap wider than 160px; bridge anything wider with a platform); rises gentle 60–70px; EVERY platform `h: 16` (WYSIWYG — never carry h:24 forward); do NOT snap climb-tier y to the 16px grid. CRITICAL for the soft landing: NO overlapping tiers anywhere — every platform has open air above it (no ceilings on L1–2). If validate-levels emits any `headroom` row on level-01, an overlapping pair slipped in — remove it.

    Include the level-shape brief: at least one deliberate DESCENT (a drop down onto a ledge/floor below, not only upward steps); one OPTIONAL high route carrying extra coins (risk/reward, but NO key — keys are even-level only); keep every route VISIBLE (no hidden alcove-style branches beyond the single secret alcove; she must always see where she is going). Math density LOCKED: exactly one door (on solid floor, never over a gap), one enemy (on solid floor), `mathGates: []`, and the end goal. Place coins so every one is a fly-through box a driven player actually reaches (§3b). Place checkpoints: one near spawn + one just before every spike cluster (≤700px apart on hazard stretches). Place exactly one secret alcove (24×24) ~70px above an early platform, off the required path.

    Every door/enemy/goal must stand on solid floor (over-hole rule); goal y = FLOOR_Y - CONFIG.GOAL_SIZE (or a descent floor's level); spikes y = FLOOR_Y - CONFIG.SPIKE_SIZE; checkpoints y = FLOOR_Y - 48; door y = FLOOR_Y - CONFIG.DOOR.H; enemy y = FLOOR_Y - CONFIG.ENEMY.H with variant 0. Use CONFIG expressions (not raw magic numbers) for these anchors exactly as the analog descriptors do.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` prints PASS with ZERO HARD-FAIL for level-01 (structural + coin-reachability + over-hole + headroom); the previously-deferred level-01 coin-reachability fails are gone.
    - `node scripts/browser-boot.mjs` drives level-01 from spawn and reaches goal.x (±32) with no pageerror.
    - `node scripts/audit-coins.mjs` collects every level-01 coin in-engine.
    - `grep -c 'h: 24' src/levels/level-01.js` is 0 (all platforms h:16); level-01 has no `bounds:` field.
    - level-01 has exactly one `doors:` entry, one `enemies:` entry, `mathGates: []`, one `secretAlcove:` entry; goal.x is roughly double the old 3560.
    - validate-levels emits ZERO `headroom` rows for level-01 (no overlapping tiers — the soft-landing rule).
  </acceptance_criteria>
  <done>level-01 is a rebuilt, ~2x, ceiling-free calm swamp intro that validates and drives clean with every coin collectable.</done>
</task>

<task type="auto">
  <name>Task 2: Re-baseline the level-01 golden fixture (scripts/smoke-progress.mjs)</name>
  <files>scripts/smoke-progress.mjs</files>
  <read_first>
    - scripts/smoke-progress.mjs — the level-01 fixture block: `expectedGeometry` object near lines ~334–398 and the deep-equal assertion at line ~399–400 (`getLevel("level-01").geometry must deep-equal ...`). Only this level-01 block changes.
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — Pitfall 4: rebuilding a level with an existing golden fixture (01–04) HARD-FAILs smoke unless re-baselined in the same change.
  </read_first>
  <action>
    Replace the inline level-01 `expectedGeometry` with the new rebuilt geometry so the deep-equal fixture matches the authored descriptor byte-for-byte. This is a mechanical re-baseline of the snapshot to the new source of truth — copy the finished level-01 geometry (Task 1) into the fixture. Do NOT touch the level-02/03/04 fixture blocks in this file. Update the stale "v3.0 src/level.js geometry verbatim" wording in the level-01 assertion message to reflect that this is now the Phase-34.6 rebuilt geometry (the old geometry it referenced no longer exists).
  </action>
  <verify>
    <automated>node scripts/smoke-progress.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/smoke-progress.mjs` passes (the level-01 deep-equal fixture matches getLevel("level-01").geometry).
    - Only the level-01 fixture block changed; level-02/03/04 fixtures are untouched (`git diff` shows edits confined to the level-01 block).
  </acceptance_criteria>
  <done>The level-01 golden geometry snapshot tracks the rebuilt descriptor; smoke-progress is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor → build.js | Pure data consumed by the ONE builder; no new input surface |

Static, offline browser game — no network/PII/auth. Geometry-only change; the only integrity concern is the guarded progress seam, unchanged here.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6-03 | DoS (self) | softlock/dead-end from bad geometry | low | mitigate | validate-levels (reachability, over-hole, coin) + browser-boot driven-player completability are HARD gates on this descriptor; zero HARD-FAIL required |
| T-34.6-SC | Tampering | package installs | n/a | accept | No installs this phase — zero new dependencies |
</threat_model>

<verification>
Full gate suite after both tasks (LEVEL-DESIGN §9):
```
bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh \
  && bash scripts/check-progress.sh && bash scripts/check-terrain-atlas.sh && bash scripts/check-pink-gate.sh \
  && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs \
  && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs \
  && node scripts/smoke-progress.mjs
```
Read audit-phase21-mechanics output (not exit code): level-01's door + enemy must show `triggered: true`.
</verification>

<success_criteria>
- level-01 rebuilt from scratch, ~2x length, calm/soft-landing (120px gaps, 60–70px rises, no ceilings), with a descent + an optional visible high route.
- Zero HARD-FAILs across validate-levels; every coin collected in-engine; completable from spawn; smoke-progress green.
- Math density exactly 1 door + 1 enemy + end gate; one secret alcove; no bounds field.
</success_criteria>

<output>
Create `.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-02-SUMMARY.md` when done.
</output>
