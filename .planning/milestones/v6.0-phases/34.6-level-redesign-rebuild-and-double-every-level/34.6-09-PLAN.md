---
phase: 34.6-level-redesign-rebuild-and-double-every-level
plan: 09
type: execute
wave: 4
depends_on: [34.6-04]
files_modified:
  - src/levels/level-08.js
autonomous: true
requirements: [LEN-01, LEN-02]
must_haves:
  truths:
    - "level-08 is REBUILT from scratch at ~2x length as the most intense + vertical CASTLE finale (even archetype), whose signature is a SWITCHBACK climb (leftward-rising reversal, visible doubling-back) — mechanically distinct from level-07's monotonic staircase (LVL-02)"
    - "level-08 places an OPTIONAL math-skip key on a high route (geometry.keys, NO geometry.locks), collectable in a walked stretch"
    - "A driven player who holds level-08's key clears it with full XP and no end math; one who does not gets the end math"
    - "level-08 uses the L7–8 band (160px gaps, tight 72–75px climbs, real ceilings, headroom ≥24); every coin collectable; completable from spawn via the bidirectional driver; density 1 door + 1 enemy + end gate"
  artifacts:
    - "src/levels/level-08.js — rebuilt switchback finale with a math-skip key and NO locks; bounds.right + bounds.top hand-set"
  key_links:
    - "level-08 geometry.keys (no locks) → game.js heldKeyIds → clearLevel key path"
    - "audit-endgate-key.mjs auto-covers level-08: Path A key→free clear, Path B no-key→math"
    - "level-08 switchback ≠ level-07 staircase — LVL-02 differentiation holds; bidirectional browser-boot drives the leftward-rising reversal"
---

<objective>
Rebuild `src/levels/level-08.js` from scratch as the CASTLE finale (even archetype) at ~2x length, to the checkpoint-proven shape — the most intense + vertical level, whose signature is a SWITCHBACK climb (leftward-rising reversal, visible doubling-back — the reference the user read fine), carrying an OPTIONAL math-skip key on a high route (no lock). It must stay mechanically DIFFERENT from level-07's monotonic staircase (LVL-02). Level-08 has NO smoke fixture. Pure-data rebuild; append-only suspended.

The two-path audit (audit-endgate-key.mjs) auto-covers level-08 once it has keys-without-locks and must prove BOTH end-gate paths. The bidirectional driver (mechanic-drive.mjs, Phase 34) makes the switchback drivable/provable from spawn.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md
@.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-PATTERNS.md
@docs/LEVEL-DESIGN.md
@src/levels/level-08.js
@src/levels/level-07.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild level-08 (castle finale, switchback, math-skip key, ~2x, L7–8 band)</name>
  <files>src/levels/level-08.js</files>
  <read_first>
    - src/levels/level-08.js — the file being rebuilt (discard current geometry, do NOT append). Preserve id "level-08", allowedTables [6,7,8,9], biome "castle", explicit `bounds` (hand-set bounds.right AND bounds.top — current top −360, summit y −125). This file is the switchback REFERENCE per PATTERNS.md (tiers reverse direction, ~70px x-overlap, 72–75px rises, per-tier checkpoint on the landing side) — use its structural technique, keep h:16.
    - src/levels/level-07.js — the castle ODD staircase; level-08 must NOT converge with it (switchback vs staircase).
    - docs/LEVEL-DESIGN.md — §3.1 h:16, §3.2 headroom, §3.3 rise bands, §3.4 grid trap, §3.5 verticality/overlap/arc hygiene, §7 camera bounds (negative top for the climb), §8 ramp (L7–8; level-08 switchback).
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-RESEARCH.md — "Level-Author Hard Constraints", "Descriptor Schema" (keys `{ x, y: FLOOR_Y - 32 }`, locks OMITTED), Pitfall 2 (bounds), Pitfall 3 (key in a walked stretch), the bidirectional-driver note.
    - .planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-CONTEXT.md — KEY = math-skip token; even = intense+vertical; descents; optional high route (key lives on it); VISIBLE doubling-back only (level-08 switchback is the reference).
  </read_first>
  <action>
    Author a brand-new level-08 geometry roughly doubling the length (current goal.x 3460, summit y −125 → target goal.x/summit reach roughly double, e.g. goal.x ~7000 with a summit balcony). Pure-data module importing ONLY ../config.js (a727c13). Preserve id "level-08", allowedTables [6,7,8,9], biome "castle". Carry an explicit COMPLETE `bounds` literal; hand-set bounds.right to the new horizontal extent and bounds.top to cover the summit (e.g. −360).

    Signature = a SWITCHBACK climb: tiers reverse direction (leftward-rising reversal), widths growing toward a wide summit balcony, ~70px x-overlap between consecutive climb tiers, rises 72–75px, EVERY overlapping tier at headroom ≥24 (never exceeding 88.331px), climb-tier y OFF the grid, EVERY platform `h: 16`. Per-tier checkpoint on the side the player lands on. Keep the doubling-back VISIBLE (she must always see the next step). Also include a deliberate DESCENT somewhere per the brief. Gaps 160px. It must stay DISTINCT from level-07's monotonic staircase.

    Place the math-skip KEY on the OPTIONAL high route: `keys: [{ x: <in a walked stretch of the high route>, y: FLOOR_Y - 32 }]`, OMIT `locks`. Key in a stretch the driven player WALKS (Pitfall 3); the safe route reaches the summit goal without it. Math density LOCKED: one door + one enemy (solid floor), `mathGates: []`, end goal (at the summit, negative y). Every coin collectable; the high route carries extra coins; exactly one secret alcove. CONFIG expressions for all y-anchors.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-endgate-key.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` PASS, zero HARD-FAIL for level-08; ZERO `key-lock` rows (keys-without-locks clean); every overlapping tier passes headroom; no spawn-goal reachability HARD-FAIL on the switchback.
    - `grep -c 'locks:' src/levels/level-08.js` is 0; exactly one `keys:` entry, y `FLOOR_Y - 32`.
    - `node scripts/browser-boot.mjs` reaches level-08 goal.x/summit (±32) from spawn driving the switchback (bidirectional), no pageerror; `node scripts/audit-coins.mjs` collects every coin.
    - `node scripts/audit-endgate-key.mjs` exits 0 and covers level-08: Path A key collected + no math at goal + clear marker; Path B math opens at goal.
    - `grep -c 'h: 24' src/levels/level-08.js` is 0; bounds.right + bounds.top hand-set; goal.x/summit ~2x; exactly one door, one enemy, `mathGates: []`, one secret alcove; switchback distinct from level-07's staircase.
  </acceptance_criteria>
  <done>level-08 is a rebuilt, ~2x, headroom-clean switchback finale with a proven optional math-skip key, distinct from level-07, clean on every gate.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.6-11 | DoS (self) | switchback unnavigable from spawn / never-walked key | low | mitigate | Bidirectional browser-boot drives the leftward-rising reversal from spawn; audit-endgate-key Path A proves key collection; validate-levels reachability + headroom HARD gates |
| T-34.6-SC | Tampering | package installs | n/a | accept | No installs this phase |

Static offline browser game — no network/PII/auth. Geometry-only change.
</threat_model>

<verification>
Full gate suite + audit (LEVEL-DESIGN §9):
```
bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh \
  && bash scripts/check-progress.sh && bash scripts/check-terrain-atlas.sh && bash scripts/check-pink-gate.sh \
  && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs \
  && node scripts/audit-coins.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs \
  && node scripts/audit-endgate-key.mjs
```
audit-phase21-mechanics output: level-08's door + enemy `triggered: true`. (No smoke fixture for level-08.)
</verification>

<success_criteria>
- level-08 rebuilt ~2x, intense/vertical castle switchback finale (L7–8 band, headroom-clean), with an optional math-skip key proven both paths in-engine, distinct from level-07.
- Zero HARD-FAILs; every coin collected; completable from spawn via the bidirectional driver; keys-without-locks a clean PASS.
</success_criteria>

<output>
Create `.planning/phases/34.6-level-redesign-rebuild-and-double-every-level/34.6-09-SUMMARY.md` when done.
</output>
