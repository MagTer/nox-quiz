---
phase: 36-world-motion-ambient-life
plan: 04
type: execute
wave: 3
depends_on: ["36-03", "36-10"]
files_modified:
  - src/config.js
  - src/levels/build.js
  - src/mechanics/secretAlcove.js
  - src/scenes/game.js
  - src/mechanics/enemy.js
autonomous: true
requirements: [MOT-03, MECH-05]
must_haves:
  truths:
    - "Existing Phase-35 light-source props (torch/candle/lamp) flicker via a pure dt-based `onUpdate` sine on opacity — a continuous visual loop with NO scheduler; goal/checkpoint unlock moments animate via a self-cleaning tween().onEnd()"
    - "Discovering a secret alcove leaves a PERSISTENT, visible ambient change for the rest of the run (a linked dark light brightens and stays lit) — positive-only, nothing ever dimmed or taken away (MECH-05)"
    - "On scene ENTRY, if the alcove was already found in a prior run the linked light renders ALREADY lit — the lit state is DERIVED from progress.hasSecretFound(levelId), never stored as its own new persisted flag (mirrors the derived-unlock convention)"
    - "check-safety stays green — every ambient loop is dt-sine onUpdate or tween().onEnd(); no setTimeout/setInterval/wait()/loop()/lifespan()"
  artifacts:
    - "src/config.js — CONFIG.AMBIENT { flicker amplitude/frequency, unlock-pop tunables } block"
    - "src/levels/build.js — a dt-sine flicker helper applied to tagged light-source props (a727c13-safe, engine refs inside buildLevel)"
    - "src/mechanics/secretAlcove.js — a new `onDiscover(alcoveObj)` callback fired once in the genuine-new-secret branch"
    - "src/scenes/game.js — wires onDiscover to brighten the linked light + derives lit-on-entry from progress.hasSecretFound(level.id)"
    - "src/mechanics/enemy.js — the stale 'MECH-05' header comment corrected (A3)"
  key_links:
    - "light-source prop → dt-sine onUpdate on opacity → continuous flicker (no timer)"
    - "secretAlcove genuine-new-secret branch → onDiscover(alcoveObj) → lightAmbient() (tween up, no reverse)"
    - "scene entry: progress.hasSecretFound(level.id) === true → lightAmbient() (derived lit-on-entry, no new persisted flag)"
---

<objective>
Give the world ambient life (MOT-03) and wire the alcove-discovery persistent ambient change (MECH-05). Torch/candle/lamp props flicker on a dt-based sine; goal/checkpoint unlock moments pop via a self-cleaning tween; discovering a secret alcove permanently lights a linked dark torch for the rest of the run — positive-only, with the lit-on-entry state DERIVED from the existing `progress.hasSecretFound(levelId)` fact (never a new stored flag). All idioms come from the spike/fx.js self-clean playbook; no scheduler ever appears.

Purpose: This is the "ambient" half of "World Motion & Ambient Life" and the phase's MECH-05 requirement. It reuses the Phase-35 light-source props already placed and the existing secretAlcove seam (which never opens a challenge), extending both minimally.

Output: CONFIG.AMBIENT tunables; a dt-sine flicker helper in build.js; a `onDiscover` callback on the alcove seam; the game.js wiring that brightens on discovery and derives lit-on-entry; the corrected stale MECH-05 comment in enemy.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-world-motion-ambient-life/36-CONTEXT.md
@.planning/phases/36-world-motion-ambient-life/36-RESEARCH.md
@src/config.js
@src/levels/build.js
@src/mechanics/secretAlcove.js
@src/scenes/game.js
@src/fx.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONFIG.AMBIENT + dt-sine flicker on light-source props</name>
  <files>src/config.js, src/levels/build.js</files>
  <read_first>
    - src/config.js — a place adjacent to PROPS/FX for the AMBIENT block. All ambient numbers live here (binding rule).
    - src/levels/build.js — the Phase-35 `levelData.props ?? []` prop emit loop (the entity handles to flicker); flicker applies to LIGHT-SOURCE props only.
    - src/fx.js — the self-cleaning tween idiom (the project's ONLY delayed-effect pattern).
    - .planning/phases/36-world-motion-ambient-life/36-RESEARCH.md — §"Verified: ambient flicker as a self-cleaning visual loop" and Pitfall 5 (no scheduler).
  </read_first>
  <action>
    Add `CONFIG.AMBIENT` = { flicker `BASE` opacity, `AMP` amplitude, `FREQ` (rad/s), plus unlock-pop duration/scale used in Task 3 }. In `buildLevel`, when emitting a light-source prop, attach a closure-local `let ft = 0; propObj.onUpdate(() => { ft += dt(); propObj.opacity = CONFIG.AMBIENT.BASE + CONFIG.AMBIENT.AMP * (0.5 + 0.5*Math.sin(ft*CONFIG.AMBIENT.FREQ + Math.sin(ft*/*second harmonic*/))); });` — a pure dt-sine flicker, no scheduler, engine refs inside buildLevel (a727c13). Decide the light-source SELECTOR — a `flicker: true` field on the prop entry is cleanest (opt-in per placement) OR a sprite-key substring match on the light-source keys, which now span ALL FOUR biomes: `prop-swamp-lantern` + `prop-cemetery-lantern` (baked in 36-10) alongside the existing `prop-town-street-lamp` / `prop-castle-candles` / `prop-castle-candle-stand`. If you use the substring match, it MUST include the two new lantern keys (a `-lantern` / `lamp` / `candle` match set) so swamp + cemetery lights flicker too; document the choice. The flicker is purely visual (props stay collider-free) — NEVER add area()/body(). Keep it a modest, ADHD-safe shimmer (no harsh strobing).
  </action>
  <verify>
    <automated>bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node -e "import('./src/config.js').then(m=>{const a=m.CONFIG.AMBIENT;if(!a||!(a.AMP>=0)||!(a.FREQ>0)){console.error('AMBIENT block bad',a);process.exit(1)}console.log('AMBIENT',JSON.stringify(a))})" && node scripts/validate-levels.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `CONFIG.AMBIENT` carries flicker BASE/AMP/FREQ; the flicker loop is dt-sine `onUpdate` on opacity only (no area/body added to props).
    - check-import-safety + check-safety pass (engine refs inside buildLevel; no timer).
    - validate-levels stays 0 HARD-FAIL (props are validator-neutral; flicker changes nothing structural).
  </acceptance_criteria>
  <done>Light-source props flicker on a dt-based sine — ambient life with zero scheduler, all tunables in config.</done>
</task>

<task type="auto">
  <name>Task 2: secretAlcove onDiscover callback</name>
  <files>src/mechanics/secretAlcove.js</files>
  <read_first>
    - src/mechanics/secretAlcove.js — the WHOLE file: the `wireSecretAlcove({ player, progress, hud, levelId, save })` signature, the genuine-NEW-secret branch (after the `hasSecretFound` early-return, where `addBonusXp`/`markSecretFound`/`save` run) vs the already-found replay branch. The onDiscover must fire ONCE, in the genuine-new-secret branch only (a replay of an already-found level derives the lit state on entry — Task 3 — so onDiscover need not fire again).
    - .planning/phases/36-world-motion-ambient-life/36-RESEARCH.md — §Pattern 3 (MECH-05): `onDiscover: (alcoveObj) => lightAmbient()`.
  </read_first>
  <action>
    Add an optional `onDiscover` to the `wireSecretAlcove({ ... })` destructure. In the genuine-NEW-secret branch (right after `progress.markSecretFound(levelId)` / the `save()` call, before/around the existing fx.pop feedback), call `if (onDiscover) onDiscover(alcoveObj);` exactly once. Keep the CRITICAL CONTRACT intact — the alcove still NEVER opens a challenge and NEVER freezes the player; onDiscover is a positive-only visual side effect. Do NOT fire onDiscover in the already-found replay branch (lit-on-entry is handled by Task 3's derivation). Preserve the fire-once `found` Set semantics.
  </action>
  <verify>
    <automated>bash scripts/check-safety.sh && grep -n "onDiscover" src/mechanics/secretAlcove.js && node -e "import('./src/mechanics/secretAlcove.js').then(m=>console.log('exports',Object.keys(m)))"</automated>
  </verify>
  <acceptance_criteria>
    - `wireSecretAlcove` accepts `onDiscover` and calls it once in the genuine-new-secret branch only.
    - The no-challenge / no-freeze contract is unchanged; check-safety passes.
    - The already-found replay branch does NOT call onDiscover.
  </acceptance_criteria>
  <done>The alcove seam exposes a one-shot onDiscover hook for the MECH-05 persistent ambient change, without breaking its walk-through contract.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the persistent alcove light + derive lit-on-entry; fix stale MECH-05 comment</name>
  <files>src/scenes/game.js, src/mechanics/enemy.js</files>
  <read_first>
    - src/scenes/game.js — the `wireSecretAlcove({ player, progress, hud, levelId: level.id, save })` call (line ~344) and the goal/checkpoint reach handlers (for the optional unlock-pop tween). Note `progress.hasSecretFound` is the existing derived fact.
    - src/mechanics/secretAlcove.js — `progress.hasSecretFound(levelId)` usage (the derived fact driving lit-on-entry).
    - src/mechanics/enemy.js line 1 — the stale `// ... defeat-enemy collision mechanic (MECH-05).` header (A3: MECH-05 is actually the alcove ambient change; this comment is requirement-ID drift).
    - .planning/phases/36-world-motion-ambient-life/36-RESEARCH.md — §Pattern 3 (lightAmbient() tween up with no reverse; derive on entry from hasSecretFound), Assumption A3.
  </read_first>
  <action>
    Define a `lightAmbient()` helper in the game scene (engine refs inside the scene body, a727c13) that brightens the linked dark light source — a `tween()` that raises its opacity/scale up and LEAVES it there (no reverse; self-completes, like fx.js one-shots), positive-only (never dims or removes anything). Identify the linked light: a specific tagged/keyed light-source prop authored per level near the alcove (the descriptor names which one; discretion, confirmed at the human checkpoint — a level with no linked light simply no-ops). Pass `onDiscover: () => lightAmbient()` into the `wireSecretAlcove({ ... })` call. On scene ENTRY, after the level builds, add `if (progress.hasSecretFound(level.id)) lightAmbient();` so a level cleared in a prior run renders the light ALREADY lit — DERIVED from the existing fact, NOT a new persisted flag. Optionally add a small goal/checkpoint unlock-pop via `tween(...).onEnd(...)` (never wait()).

    Separately, correct the stale comment in `src/mechanics/enemy.js` line 1: it labels defeat-enemy as "MECH-05" — MECH-05 is the alcove persistent ambient change (this plan). Fix the requirement-ID drift in the comment only (do NOT change enemy.js behavior).
  </action>
  <verify>
    <automated>bash scripts/check-safety.sh && grep -n "lightAmbient" src/scenes/game.js && grep -n "hasSecretFound" src/scenes/game.js && node scripts/browser-boot.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `lightAmbient()` exists in game.js as a tween-up with NO reverse (positive-only); it is passed as `onDiscover` into wireSecretAlcove.
    - Scene entry derives lit-on-entry: `if (progress.hasSecretFound(level.id)) lightAmbient();` — no new persisted flag introduced.
    - The stale `MECH-05` comment in enemy.js is corrected (behavior unchanged).
    - check-safety passes (tween().onEnd only, no scheduler); browser-boot boots all 8 levels + reaches goal.
  </acceptance_criteria>
  <done>Discovering an alcove permanently lights a linked torch, and a previously-found level renders it already lit — MECH-05 satisfied, positive-only, derived from the existing fact; the stale requirement-ID comment is fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → progress.js | The lit-on-entry state is DERIVED from the existing hasSecretFound fact; no new persisted state |

Static, offline browser game — visual ambient loops + a derived light state add no trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-08 | Tampering | ambient flicker uses a scheduler (trips SAFE-01) | medium | mitigate | dt-sine onUpdate + tween().onEnd() only; check-safety green |
| T-36-09 | Tampering | MECH-05 stores a NEW persisted flag instead of deriving | low | mitigate | Lit-on-entry derives from existing hasSecretFound; no new save key |
| T-36-10 | Repudiation | alcove ambient becomes a punishment (something dims/removed) | low | mitigate | lightAmbient is positive-only (brightens, no reverse); nothing ever taken away |
| T-36-SC | Tampering | package installs | n/a | accept | Zero new dependencies this phase |
</threat_model>

<verification>
```
bash scripts/check-import-safety.sh && bash scripts/check-safety.sh \
  && node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs \
  && node scripts/browser-boot.mjs
```
- safety: dt-sine + tween().onEnd only; no scheduler/punishment.
- validate-levels + check-geometry-frozen: byte-identical green (ambient is visual-only, no geometry touched).
- browser-boot: all 8 levels boot + reach goal.
</verification>

<success_criteria>
- Light-source props flicker (dt-sine); optional unlock pop via tween().onEnd.
- Alcove discovery permanently lights a linked torch (MECH-05), positive-only.
- Lit-on-entry derived from hasSecretFound (no new persisted flag).
- Stale MECH-05 comment fixed; all gates green.
</success_criteria>

<output>
Create `.planning/phases/36-world-motion-ambient-life/36-04-SUMMARY.md` when done.
</output>