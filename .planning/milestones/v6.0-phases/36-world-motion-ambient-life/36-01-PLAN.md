---
phase: 36-world-motion-ambient-life
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-geometry-frozen.mjs
autonomous: true
requirements: [MOT-02]
must_haves:
  truths:
    - "Adding a `geometry.movers` (and `geometry.patrollers`) key to a level descriptor NO LONGER trips check-geometry-frozen.mjs — the two motion keys are excluded from the freeze snapshot, so the kid-validated static arrays stay byte-frozen while motion is added"
    - "The freeze gate STILL HARD-FAILs the instant any NON-motion geometry key (floors/platforms/coins/spikes/goal/checkpoints/doors/mathGates/enemies/keys/locks/secretAlcove/bounds) drifts — the exclusion is surgical, the byte-freeze guarantee on every static array is intact"
    - "The Phase-30 mover-reachability row in validate-levels.mjs already reads `geometry.movers` (unchanged) and reports on any mover a level authors — the exclusion is in the FREEZE gate only, never in the validator"
  artifacts:
    - "scripts/check-geometry-frozen.mjs — currentGeometryMap() strips `movers` + `patrollers` before JSON.stringify (explicit exclusion allowlist)"
  key_links:
    - "getLevel(id).geometry → {movers,patrollers,...frozen} destructure → JSON.stringify(frozen) → compare vs geometry-frozen-baseline.json (motion keys never in the snapshot)"
    - "geometry.movers → reachability.mjs mover-reachability row (UNCHANGED — validator still sees movers)"
---

<objective>
Resolve the one real design conflict the research found (§Architecture Patterns, Option C): the Phase-30 mover-reachability validator reads `geometry.movers`, but `check-geometry-frozen.mjs` serializes the WHOLE geometry object and HARD-FAILs on any new top-level key. Teach the freeze gate to EXCLUDE the two Phase-36 motion keys (`movers`, `patrollers`) from its snapshot — the direct analog of how Phase-35 props were made structurally invisible to the gate.

Purpose: Every downstream plan that places a mover or patroller adds `geometry.movers` / `geometry.patrollers`. Without this exclusion the freeze gate blocks the entire phase; with it, static geometry stays byte-frozen (the real defense against accidental drift under a motion diff) AND the validator still sees movers. This is a HARNESS edit, not a geometry edit — squarely in scope.

Output: a surgical exclusion in `currentGeometryMap()`, proven RED→GREEN: a scratch `movers` key trips the gate BEFORE the edit and passes AFTER, while a scratch static-array nudge still fails AFTER (guard intact).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-world-motion-ambient-life/36-CONTEXT.md
@.planning/phases/36-world-motion-ambient-life/36-RESEARCH.md
@scripts/check-geometry-frozen.mjs
@scripts/lib/reachability.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exclude the motion keys from the freeze snapshot (RED-first)</name>
  <files>scripts/check-geometry-frozen.mjs</files>
  <read_first>
    - scripts/check-geometry-frozen.mjs — the WHOLE file, especially `currentGeometryMap()` (line 41-45: `map[id] = JSON.stringify(getLevel(id).geometry)`), the `--write` path (line 52-57), the gate-mode compare (line 61-105), and `firstDifferingKey` (line 114-120). The header comment (lines 12-17) already documents that props were made structurally invisible by living OUTSIDE geometry — movers/patrollers CANNOT use that trick (the validator needs them INSIDE geometry), so the equivalent is this explicit key exclusion.
    - scripts/lib/reachability.mjs — the mover-reachability loop (lines 1104-1131): confirm it reads `geometry.movers` and is NOT touched by this plan. Note the test fixtures at lines 1465/1480 use `movers: [{ x1, y1, x2, y2 }]`.
    - .planning/phases/36-world-motion-ambient-life/36-RESEARCH.md — §"The geometry-freeze reconciliation" (Option C recommended; Options A and B rejected — do NOT `--write` the golden to absorb motion keys, and do NOT move movers to top-level).
  </read_first>
  <action>
    In `currentGeometryMap()`, before serializing, destructure the two Phase-36 motion keys OUT of each level's geometry and stringify only the remainder: replace the `map[id] = JSON.stringify(getLevel(id).geometry)` line with a destructure that separates `movers` and `patrollers` from the rest (`const { movers, patrollers, ...frozen } = getLevel(id).geometry; map[id] = JSON.stringify(frozen);`). Add a short comment naming this an explicit exclusion allowlist for the Phase-36 motion keys and pointing at Option C in 36-RESEARCH — the static arrays (floors/platforms/coins/spikes/goal/checkpoints/doors/mathGates/enemies/keys/locks/secretAlcove/bounds) stay byte-frozen; only the two motion keys are invisible to the snapshot. This same `currentGeometryMap()` feeds BOTH the `--write` baseline path and the gate-compare path, so the baseline and the comparison stay symmetric automatically — no baseline regeneration is needed, and `--write` MUST NOT be run to "absorb" a motion key (Option A, rejected). Do NOT touch `reachability.mjs`, `validate-levels.mjs`, or any level descriptor. Keep the exclusion to exactly `movers` and `patrollers` — every other key must still be frozen.
  </action>
  <verify>
    <automated>node scripts/check-geometry-frozen.mjs && echo FROZEN-GREEN && node -e "import('./src/levels/index.js').then(async m=>{const {getLevel}=m;const g=getLevel('level-01').geometry;g.movers=[{x1:1,y1:2,x2:3,y2:4}];g.patrollers=[{x1:1,y1:2,x2:3,y2:4}];const {execSync}=await import('child_process');/*in-memory only*/console.log('scratch-motion-keys-attached (not persisted)');})"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-geometry-frozen.mjs` PASSES (exit 0) against the EXISTING committed baseline with the exclusion in place (no baseline change).
    - RED→GREEN proof (scratch, revert — do NOT commit): temporarily add `movers: [{x1:100,y1:200,x2:300,y2:200}]` to a real descriptor (e.g. level-01.js) → `node scripts/check-geometry-frozen.mjs` PASSES (motion key excluded). Confirm the same scratch key BEFORE this edit would have HARD-FAILed (naming `movers`) — cite it in the SUMMARY. Revert the scratch.
    - Guard-intact proof (scratch, revert — do NOT commit): temporarily nudge a static coin/platform coordinate → the gate HARD-FAILs naming that static key. Revert.
    - `grep -n "movers" scripts/lib/reachability.mjs` still shows the unchanged mover-reachability loop — this plan did NOT touch the validator.
  </acceptance_criteria>
  <done>The freeze gate excludes exactly `movers` + `patrollers`, proven RED→GREEN, with every static array still byte-frozen and the Phase-30 validator untouched — every downstream plan can now author `geometry.movers`/`geometry.patrollers` without tripping the freeze guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor → gate scripts | The only "input"; static Node data, no network/user input |

Static, offline, single-origin browser game — no network/server/auth/accounts. Editing a Node gate script introduces no trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-01 | Tampering | over-broad exclusion masks static geometry drift | high | mitigate | Exclusion is EXACTLY `movers`+`patrollers` via destructure; guard-intact scratch proof shows a static-array nudge still HARD-FAILs |
| T-36-02 | Tampering | `--write` silently absorbs a motion key into the golden (Option A) | medium | mitigate | Plan forbids `--write` for motion keys; the exclusion makes it unnecessary; SUMMARY records the ban |
| T-36-SC | Tampering | package installs | n/a | accept | Zero new dependencies this phase (vanilla ES2020 + pinned Kaplay; Pillow/Playwright already present) |
</threat_model>

<verification>
```
node scripts/check-geometry-frozen.mjs && node scripts/validate-levels.mjs
```
- check-geometry-frozen: green against the existing baseline with the exclusion; scratch motion key passes, scratch static nudge fails (both reverted).
- validate-levels: unchanged 0 HARD-FAIL (no descriptor authored a mover yet; the validator is untouched).
</verification>

<success_criteria>
- The freeze gate excludes `movers`+`patrollers` and nothing else; static arrays stay byte-frozen.
- RED→GREEN proven; guard-intact proven; validator untouched.
- No baseline regeneration; `--write` not used for motion keys.
</success_criteria>

<output>
Create `.planning/phases/36-world-motion-ambient-life/36-01-SUMMARY.md` when done.
</output>
