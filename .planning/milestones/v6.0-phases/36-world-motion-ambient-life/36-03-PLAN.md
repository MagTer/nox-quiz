---
phase: 36-world-motion-ambient-life
plan: 03
type: execute
wave: 2
depends_on: ["36-01", "36-02", "36-10"]
files_modified:
  - src/config.js
  - src/levels/build.js
  - src/scenes/game.js
autonomous: true
requirements: [MOT-01, MOT-02]
must_haves:
  truths:
    - "A `geometry.movers` entry renders as a solid static-body platform that oscillates between its two declared endpoints on a dt-based raised-cosine (eases to rest at BOTH ends), and the player standing on it is carried NATIVELY (engine `stickToPlatform`) with ZERO rider code in build.js"
    - "A `geometry.patrollers` entry renders as a `patrol()`-driven ping-pong walker with a visible walk-cycle telegraph, tagged `patroller` (DISTINCT from the `enemy` math-blocker tag), each patroller with its OWN fresh waypoints array"
    - "Touching a patroller respawns the player at the nearest checkpoint through the EXISTING respawn() seam — zero hurt/score/game-over/timer wiring added anywhere"
    - "check-safety stays green — motion is `onUpdate`+`dt()` sine and `patrol({endBehavior:'ping-pong'})` only; no setTimeout/setInterval/wait()/loop()/lifespan()"
  artifacts:
    - "src/config.js — CONFIG.MOVER { PERIOD_S, SPRITE, ... } and CONFIG.PATROLLER { SPEED, SPRITE, ... } tunable blocks (no magic numbers in build.js/game.js)"
    - "src/levels/build.js — the guarded `for (const m of g.movers ?? [])` dt-sine emit loop and `for (const p of g.patrollers ?? [])` patrol() loop"
    - "src/scenes/game.js — `player.onCollide('patroller', () => respawn())` mirroring the spike seam"
  key_links:
    - "geometry.movers → build.js add([sprite,pos,area,body({isStatic:true}),'mover']) + onUpdate dt-sine pos mutation → player body() stickToPlatform carries the rider"
    - "geometry.patrollers → build.js add([sprite,pos,area,patrol({waypoints:fresh,endBehavior:'ping-pong'}),'patroller']) → player.onCollide('patroller') → respawn()"
---

<objective>
Build the two moving-entity CLASSES the whole phase places: the moving platform (dt-based raised-cosine sine ping-pong between two validated endpoints, native rider-carry) and the patroller (native `patrol()`, respawn-hazard, visually distinct from the math-blocker enemy). Both primitives are already in the vendored engine and measured in the committed v6.0 spike — the work here is the emit loops, the config tunables, and the one respawn wire. NO level authors motion data yet; this plan is pure entity plumbing, proven inert against the 8 shipped levels.

Purpose: Every trial and rollout plan downstream just adds `geometry.movers` / `geometry.patrollers` arrays; the rendering + carry + respawn behavior must exist first, once, in the ONE builder + the game scene. This is where the measured anti-patterns (hand-carrying the rider; sharing a waypoint array; using patrol() for platforms) are avoided by construction.

Output: CONFIG.MOVER + CONFIG.PATROLLER blocks; the two guarded emit loops in `buildLevel`; the `onCollide("patroller", respawn)` wire in game.js. Inert (`?? []`) against all 8 current levels — all structural gates byte-identical-green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-world-motion-ambient-life/36-CONTEXT.md
@.planning/phases/36-world-motion-ambient-life/36-RESEARCH.md
@.planning/research/ART-PARITY-STEERING.md
@src/config.js
@src/levels/build.js
@src/scenes/game.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONFIG.MOVER + CONFIG.PATROLLER tunable blocks</name>
  <files>src/config.js</files>
  <read_first>
    - src/config.js — the `ENEMY` block (line 263) and `SPIKE_*` tunables (lines 156-158) for the tunable-shape convention; place MOVER + PATROLLER adjacent to ENEMY. All motion numbers live here (no magic numbers in build.js/game.js — binding rule).
    - .planning/phases/36-world-motion-ambient-life/36-RESEARCH.md — §Pattern 1 (period/raised-cosine), §Pattern 2 (patrol speed, ping-pong), §"Source-verified API details" (patrol speed default 100 px/s; a slow, telegraphed patroller wants a LOWER speed).
  </read_first>
  <action>
    Add `CONFIG.MOVER` = { `PERIOD_S`: the default full round-trip period in seconds (a per-mover `period` override is allowed in the descriptor), `SPRITE`: the moving-platform sprite key (reuse an existing biome atlas PLATFORM frame OR a distinct "this one moves" sprite — the choice is confirmed at the human checkpoint; pick a sane existing default here), plus any collider inset if the platform sprite needs one }. Add `CONFIG.PATROLLER` = { `SPEED`: a SLOW default in px/s (well under patrol's 100 default — heavily telegraphed per CONTEXT), `SPRITE`: `"patroller"` — the distinct walk-animated patroller sprite key BAKED + LOADED by plan 36-10 (visually distinct from `CONFIG.ENEMY` / `enemy-hellhound`, with a real `walk` anim per SC1/MOT-01), and an optional `WALK_SPEED` for the anim frame rate }. Comment each with its unit and the rule it serves (dt-sine period for natural endpoint slow-down; slow patrol speed for readability). Do NOT touch ENEMY, SPIKE, or any other block.
  </action>
  <verify>
    <automated>node -e "import('./src/config.js').then(m=>{const {MOVER,PATROLLER}=m.CONFIG;if(!MOVER||!PATROLLER){console.error('missing blocks');process.exit(1)}if(!(MOVER.PERIOD_S>0)){console.error('PERIOD_S must be >0');process.exit(1)}if(!(PATROLLER.SPEED>0)){console.error('SPEED must be >0');process.exit(1)}console.log('MOVER',JSON.stringify(MOVER),'PATROLLER',JSON.stringify(PATROLLER))})"</automated>
  </verify>
  <acceptance_criteria>
    - `CONFIG.MOVER.PERIOD_S > 0` and `CONFIG.PATROLLER.SPEED > 0`; both blocks carry a SPRITE key.
    - PATROLLER.SPEED is below patrol()'s 100 px/s default (slow + telegraphed).
    - No other config block changed.
  </acceptance_criteria>
  <done>All motion tunables live in config; build.js/game.js will carry zero magic numbers.</done>
</task>

<task type="auto">
  <name>Task 2: build.js mover (dt-sine) + patroller (patrol) emit loops</name>
  <files>src/levels/build.js</files>
  <read_first>
    - src/levels/build.js — `buildLevel(levelData)` (line 49), `const g = levelData.geometry` (line 76), the guarded optional-array loops (`g.doors ?? []` ~292, `g.secretAlcove ?? []` ~453, the Phase-35 `levelData.props ?? []` prop loop) for the emit idiom, and where enemies are emitted (so the patroller can sit NEAR that code but stay tagged distinctly).
    - .planning/phases/36-world-motion-ambient-life/36-RESEARCH.md — §Pattern 1 (the raised-cosine `(1-cos(2π t/period))/2` dt-sine loop; the CONTRACT that x1/y1↔x2/y2 are the TRUE extremes so the validator's two tested points are the two points the platform visits) and §Pattern 2 (patrol with a FRESH waypoints array per entity; endBehavior:"ping-pong"; `foe.play("walk")` telegraph). §Anti-Patterns: never add moveBy on the platform (native carry double-applies → slide-off); never share a waypoint array; never use patrol() for platforms.
    - .planning/research/ART-PARITY-STEERING.md — the patroller sprite (`"patroller"`, with a `walk` anim) is already baked + registered + loaded by plan 36-10 (this plan just references the key via CONFIG.PATROLLER.SPRITE and calls `foe.play("walk")`); the mover reuses an existing biome-atlas PLATFORM frame (no new art here).
  </read_first>
  <action>
    In `buildLevel`, after the existing geometry emit loops, add a guarded `for (const m of g.movers ?? [])` loop that does `add([ sprite(CONFIG.MOVER.SPRITE), pos(m.x1, m.y1), area(), body({ isStatic: true }), "mover" ])`, then attaches `plat.onUpdate(() => { t += dt(); const phase = (1 - Math.cos((2*Math.PI/period)*t))/2; plat.pos.x = m.x1 + (m.x2-m.x1)*phase; plat.pos.y = m.y1 + (m.y2-m.y1)*phase; })` with `period = m.period ?? CONFIG.MOVER.PERIOD_S` and a closure-local `let t = 0` PER mover. Add NO rider code — the player's `body()` stickToPlatform carries them natively (measured; hand-carry is the slide-off anti-pattern). The raised-cosine MUST reach exactly (x1,y1) at phase 0 and (x2,y2) at phase 1 so the platform never travels past the two endpoints the validator tests.

    Add a guarded `for (const p of g.patrollers ?? [])` loop that does `add([ sprite(CONFIG.PATROLLER.SPRITE), pos(p.x1, p.y1), area(), patrol({ waypoints: [vec2(p.x1, p.y1), vec2(p.x2, p.y2)], speed: p.speed ?? CONFIG.PATROLLER.SPEED, endBehavior: "ping-pong" }), "patroller" ])` — a FRESH `[vec2(...), vec2(...)]` array literal PER patroller (ping-pong reverses it in place; a shared array corrupts both). Tag it `"patroller"`, NEVER `"enemy"` (the enemy tag routes to enemy.js's math-challenge seam). Call `foe.play("walk")` (or the patroller sprite's walk anim) as the visible telegraph. Keep ALL engine globals (add, sprite, pos, area, body, patrol, vec2, dt) INSIDE `buildLevel` (a727c13). No timers/schedulers — the string `endBehavior:"ping-pong"` is not the banned `loop(` call (check-safety greps `[^a-zA-Z]loop\(`).
  </action>
  <verify>
    <automated>bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node scripts/validate-levels.mjs && grep -n "g.movers ?? \[\]" src/levels/build.js && grep -n "g.patrollers ?? \[\]" src/levels/build.js && grep -c "moveBy" src/levels/build.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'g.movers ?? \[\]'` and `grep -n 'g.patrollers ?? \[\]'` each show exactly the new guarded loop.
    - The mover loop emits `body({ isStatic: true })` + a dt-sine `onUpdate` and adds NO `moveBy` on the platform (native carry only).
    - The patroller loop uses `patrol({ ... endBehavior: "ping-pong" })` with a fresh per-entity waypoints array and the `"patroller"` tag (NOT `"enemy"`), plus a walk-cycle play() call.
    - check-import-safety (a727c13) + check-safety (no timers/punishment) pass; validate-levels stays 0 HARD-FAIL (no descriptor authored movers yet — loops are inert).
  </acceptance_criteria>
  <done>The mover + patroller entity classes exist in the ONE builder, dt-based and check-safety-clean, with native carry and distinct tagging — inert until a level authors motion data.</done>
</task>

<task type="auto">
  <name>Task 3: Wire patroller contact into the existing respawn seam</name>
  <files>src/scenes/game.js</files>
  <read_first>
    - src/scenes/game.js — the respawn seam: `function reset()` (line 189), `const respawn = reset` (line 198), and the spike wire `player.onCollide("spike", () => respawn())` (line 220). The patroller wire mirrors this EXACTLY — zero new punishment wiring.
    - .planning/phases/36-world-motion-ambient-life/36-RESEARCH.md — §Pattern 2 (game.js wire), Pitfall 4 (wrong seam), §"Verified: existing respawn seam to reuse".
  </read_first>
  <action>
    Add exactly `player.onCollide("patroller", () => respawn())` next to the existing spike wire (line ~220), reusing the same forgiving reposition-in-place reset() contract — ZERO hurt, ZERO score change, ZERO game-over, ZERO HP, ZERO timer. Do NOT add any new failure/hurt construct; do NOT touch the enemy (math-blocker) wiring. Keep engine refs inside the scene body (a727c13). Movers need NO game.js wire — the engine's stickToPlatform carries the rider automatically.
  </action>
  <verify>
    <automated>bash scripts/check-safety.sh && grep -n 'onCollide("patroller"' src/scenes/game.js && node scripts/browser-boot.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'onCollide("patroller"' src/scenes/game.js` shows exactly `player.onCollide("patroller", () => respawn())`.
    - check-safety passes (no punishment/timer added); the wire reuses respawn() and adds no new construct.
    - browser-boot still boots all 8 levels + reaches goal (no motion data placed yet — the wire is inert).
  </acceptance_criteria>
  <done>Patroller contact routes through the exact spike respawn seam — the gentle checkpoint-respawn hazard class, zero new punishment wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → progress.js | Only persisted state; unchanged (movers/patrollers are session-only entities) |

Static, offline browser game — moving sprite entities + a respawn wire add no trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-05 | Tampering | hand-carry rider code slides the player off (measured anti-pattern) | high | mitigate | Mover loop adds NO moveBy; native body() stickToPlatform carries; grep asserts zero moveBy in the mover path |
| T-36-06 | Denial of Service | patroller routed through enemy seam (freezes player / no effect) | medium | mitigate | Distinct `"patroller"` tag + explicit onCollide→respawn wire mirroring spike |
| T-36-07 | Tampering | a stray scheduler for platform timing trips SAFE-01 | medium | mitigate | dt-sine onUpdate + patrol ping-pong only; check-safety green |
| T-36-SC | Tampering | package installs | n/a | accept | Zero new dependencies; any new sprite art is CC0 under ART-PARITY-STEERING (pink-gate/license) |
</threat_model>

<verification>
```
bash scripts/check-import-safety.sh && bash scripts/check-safety.sh \
  && node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs \
  && node scripts/browser-boot.mjs
```
- import-safety/safety: engine globals inside buildLevel; no timer/punishment.
- validate-levels + check-geometry-frozen: byte-identical green (no descriptor authored motion data).
- browser-boot: all 8 levels still boot + reach goal (inert loops).
</verification>

<success_criteria>
- Mover + patroller entity classes exist in build.js (dt-sine + patrol, distinct tags, native carry).
- CONFIG.MOVER + CONFIG.PATROLLER carry all tunables.
- Patroller contact → respawn seam wired; zero punishment.
- All structural gates byte-identical-green; no level dressed yet.
</success_criteria>

<output>
Create `.planning/phases/36-world-motion-ambient-life/36-03-SUMMARY.md` when done.
</output>
