---
phase: 36-world-motion-ambient-life
plan: 07
type: execute
wave: 6
depends_on: ["36-06", "36-10"]
files_modified:
  - src/levels/level-02.js
  - src/levels/level-03.js
  - src/levels/level-04.js
autonomous: true
requirements: [MOT-01, MOT-02, MOT-03, MECH-05]
must_haves:
  truths:
    - "level-02 (swamp, intense even), level-03 (town, calm odd), level-04 (town, intense even) each carry FULL motion — ≥1 moving platform + ≥1 patroller + ambient flicker + the alcove-linked torch — tuned to the calm/intense biome-pair rhythm (odd lighter, even heavier), following the trial's approved template"
    - "Every placed mover passes mover-reachability (both endpoints rightward-reachable, worst-case-extreme) and obeys §6b — checkpoint before every mover, WAIT-not-death, no killing pit under a mover; movers MAY be on the critical path but NEVER strand her"
    - "Static geometry of all three levels stays byte-frozen (motion is add-only via geometry.movers/geometry.patrollers + the alcove-link field)"
    - "The interactive audit rides every mover + crosses every patroller on all three; check-safety stays green"
  artifacts:
    - "src/levels/level-02.js, level-03.js, level-04.js — geometry.movers + geometry.patrollers + flicker/alcove-link fields, densities tuned per calm/intense"
  key_links:
    - "each level-0N geometry.movers/patrollers → build.js loops → mover-reachability + ride/cross audit; alcove torch → MECH-05 lightAmbient"
---

<objective>
Roll out FULL motion to levels 02, 03, and 04 using the human-approved trial template — tuned to each level's shape and the calm/intense biome-pair rhythm (odd 03 lighter; even 02/04 heavier, movers allowed on the critical path). Every mover is authored to the LEVEL-DESIGN §6a/§6b no-softlock rules so mover-reachability stays green and no placement can strand her; every level also gets ambient flicker and the MECH-05 alcove-linked torch.

Purpose: The first half of the remaining-6 rollout (this plan) and levels 05/07/08 (plan 08) complete FULL motion across all 8 levels. This runs only after the SC5 sign-off (36-06) proves the placement approach.

Output: geometry.movers + geometry.patrollers (+ flicker + alcove-link) on levels 02/03/04, all gates green including mover-reachability + the ride/cross audit + geometry-freeze.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-world-motion-ambient-life/36-CONTEXT.md
@.planning/phases/36-world-motion-ambient-life/36-05-SUMMARY.md
@.planning/phases/36-world-motion-ambient-life/36-06-SUMMARY.md
@docs/LEVEL-DESIGN.md
@src/levels/level-02.js
@src/levels/level-03.js
@src/levels/level-04.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author motion on level-02 (swamp, intense even) and level-04 (town, intense even) — HEAVIER density</name>
  <files>src/levels/level-02.js, src/levels/level-04.js</files>
  <read_first>
    - src/levels/level-02.js + level-04.js — the WHOLE descriptors: the tall vertical climbs / switchbacks, `bounds` (used AS-IS — §7; do not bump, geometry frozen), checkpoints, enemies, secretAlcove, and the light-source prop nearest each alcove.
    - docs/LEVEL-DESIGN.md — §6a/§6b/§8.5 (movers on the critical path allowed; both endpoints rightward-reachable; checkpoint before every mover; WAIT-not-death; no killing pit; far end telegraphed; vertical travel within one jump at the worst endpoint).
    - .planning/phases/36-world-motion-ambient-life/36-06-SUMMARY.md — the approved density/telegraph/checkpoint-cadence template + any rejection notes to honor.
  </read_first>
  <action>
    For EACH of level-02 and level-04, add `geometry.movers` + `geometry.patrollers` at INTENSE-even density (multiple movers/patrollers, some on the switchback critical path), each authored to the §6b no-softlock rules exactly as the trial: both endpoints reachable rightward from spawn, behind a checkpoint, WAIT-not-death recovery, no killing pit under a mover, far end telegraphed, vertical travel within a single jump's reach at the worst endpoint. Ensure each level has a light-source prop to flicker + link: level-04 (town) already carries `prop-town-street-lamp`; level-02 (swamp) has NO light yet, so PLACE one `prop-swamp-lantern` (the 36-10 key) in its top-level `props` array near its secret alcove (top-level props are outside geometry — check-geometry-frozen stays green). Add ambient flicker to each level's light-source props. Link the MECH-05 torch (the light-source prop nearest each secretAlcove — the swamp lantern for 02, the street lamp for 04) so discovery lights it persistently. Do NOT edit any existing static array or bounds — motion is ADD-ONLY via the new keys/fields; append, never edit inside the kid-validated geometry. Author each level DISTINCTLY (do not clone level-02's mover layout into level-04 — §8.5 rule 7 spirit).
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs 2>&1 | grep -i "mover\|level-02\|level-04\|HARD-FAIL"; node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && bash scripts/check-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - level-02 + level-04 each carry geometry.movers + geometry.patrollers (intense density); every mover-reachability row PASS/WARN (never HARD-FAIL).
    - check-geometry-frozen PASSES (static arrays byte-frozen; bounds unchanged); check-safety passes.
    - level-02 and level-04 mover layouts are distinct from each other and from the trial levels.
  </acceptance_criteria>
  <done>The two intense-even levels 02/04 carry on-path, no-softlock motion + ambient + alcove torch on the approved template.</done>
</task>

<task type="auto">
  <name>Task 2: Author motion on level-03 (town, calm odd) — LIGHT density</name>
  <files>src/levels/level-03.js</files>
  <read_first>
    - src/levels/level-03.js — the WHOLE descriptor: geometry, bounds, checkpoints, secretAlcove, and the light-source prop nearest the alcove.
    - docs/LEVEL-DESIGN.md — §6a/§6b/§8.5 (calm odd = FORGIVING: light density, generous margins, still ≥1 mover + ≥1 patroller, none mover-free).
    - .planning/phases/36-world-motion-ambient-life/36-06-SUMMARY.md — the approved calm-level template.
  </read_first>
  <action>
    Add `geometry.movers` + `geometry.patrollers` at LIGHT calm-odd density (≥1 each, forgiving margins), each authored to the §6b no-softlock rules (both endpoints rightward-reachable, behind a checkpoint, WAIT-not-death, no killing pit, telegraphed). Add ambient flicker to the town light-source props (town-street-lamp). Link the MECH-05 torch nearest the secretAlcove. Do NOT edit any existing static array or bounds — motion is ADD-ONLY. Keep level-03's layout distinct from level-01 (both calm) — different mover placement/shape.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs 2>&1 | grep -i "mover\|level-03\|HARD-FAIL"; node scripts/validate-levels.mjs && node scripts/check-geometry-frozen.mjs && bash scripts/check-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - level-03 carries geometry.movers + geometry.patrollers (light density); every mover-reachability row PASS/WARN.
    - check-geometry-frozen PASSES; check-safety passes; layout distinct from level-01.
  </acceptance_criteria>
  <done>level-03 has forgiving, no-softlock motion — the calm end of the town biome pair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor → build/validate | Static Node data; no network/user input |

Static, offline browser game — authoring motion data adds no trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-14 | Denial of Service | an on-path mover strands the player (softlock) | high | mitigate | mover-reachability green + §6b authoring (checkpoint, WAIT-not-death, no killing pit); ride-audit confirms |
| T-36-15 | Tampering | a static-array coordinate nudged while adding motion | high | mitigate | check-geometry-frozen PASSES (add-only via excluded keys) |
| T-36-16 | Tampering | level-02/03/04 mover layouts cloned from each other/trial | low | mitigate | Author each distinctly (§8.5 rule 7 spirit) |
| T-36-SC | Tampering | package installs | n/a | accept | Zero new dependencies this phase |
</threat_model>

<verification>
```
bash scripts/check-safety.sh && node scripts/validate-levels.mjs \
  && node scripts/check-geometry-frozen.mjs && node scripts/browser-boot.mjs \
  && node scripts/audit-phase21-mechanics.mjs
```
- validate-levels: mover-reachability PASS/WARN for every mover on 02/03/04.
- check-geometry-frozen: static arrays byte-frozen.
- audit: 02/03/04 movers ridden + patrollers crossed (read output).
- browser-boot: all three boot + reach goal with motion live.
</verification>

<success_criteria>
- levels 02/03/04 carry full motion tuned to calm/intense rhythm, no-softlock, geometry byte-frozen.
- Every mover reachability-green; audit rides/crosses all three; MECH-05 torch linked on each.
</success_criteria>

<output>
Create `.planning/phases/36-world-motion-ambient-life/36-07-SUMMARY.md` when done.
</output>
