---
phase: 34.5-key-lock-mechanic-the-first-non-math-gate
plan: 02
type: execute
wave: 2
depends_on: [34.5-01]
files_modified:
  - src/mechanics/key.js
  - scripts/check-import-safety.sh
  - src/levels/build.js
  - src/scenes/game.js
  - src/ui/hud.js
autonomous: true
requirements: [KEY-01]
must_haves:
  truths:
    - "src/mechanics/key.js is scanned by check-import-safety.sh (added to BOTH the node --check loop AND the $TOPLEVEL_TRAP a727c13 grep loop — loop-1-only is a false green) and passes check-safety.sh — no timers, no punishment/game-over, all engine globals inside the wireKey body (a727c13)"
    - "build.js emits a walk-through visible key and an apex-height solid lock blocker + stashed visible panel from geometry.keys/geometry.locks, both `?? []`-guarded so all 8 existing levels still build unchanged"
    - "game.js threads a closure-local keyHeld run-state (survives reposition-in-place respawn, never serialized) into wireKey; the lock reads hasKey() and the pickup flips it"
    - "hud.js exposes showKey() and shows a persistent fixed() key-held indicator; browser-boot and audit-phase21 stay green (no regression on existing levels)"
  artifacts:
    - "src/mechanics/key.js — wireKey({ player, hud, onPickup, hasKey }) wiring the pickup half and the lock half"
    - "scripts/check-import-safety.sh — src/mechanics/key.js added to the scoped module list"
    - "src/levels/build.js — key/lock/lock-panel entity emission from geometry.keys/geometry.locks"
    - "src/scenes/game.js — keyHeld run-state + wireKey call site"
    - "src/ui/hud.js — keyIndicator entity + showKey() added to the returned controller"
  key_links:
    - "game.js onPickup callback flips keyHeld=true; hasKey() closure read by key.js's lock handler decides open-vs-hint"
    - "key.js pickup handler calls hud.showKey() — the HUD controller must expose it (hud.js return extension)"
    - "build.js stashes the visible panel as blocker.panelObj so key.js's lock-open branch can destroy collider + panel together"
    - "the lock's no-key hint self-cleans via tween().onEnd() gated by a closure-local hintShowing boolean — never a scheduler"
---

<objective>
Wire the key/lock mechanic as CODE: a new `src/mechanics/key.js` seam that handles BOTH halves (key pickup, secretAlcove-style; lock-open, door.js-style minus the math challenge), the builder emission of key/lock entities from the new descriptor arrays, the scene's closure-local key-held run-state, and the persistent HUD "key held" indicator — plus the import-safety guard the new module needs.

This is Wave 2 and depends on Wave 1 (the LOCK/KEY/HUD config tunables and the softlock validator that will prove any level using this mechanic is safe). It authors NO level here — the mechanic is inert until a descriptor carries keys/locks (that is Plan 03's level-02 proof and, later, Phase 34.6).

Purpose: KEY-01 — the game's FIRST non-math gate exists (`geometry.keys`/`geometry.locks` + a `src/mechanics/key.js` seam) following the existing mechanic conventions: pure-data levels, the ONE builder owns entity creation, no engine globals at module top level, no timers, no punishment.
Output: `src/mechanics/key.js`, build.js key/lock emission, game.js wiring, hud.js indicator, and the check-import-safety.sh list entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.5-key-lock-mechanic-the-first-non-math-gate/34.5-RESEARCH.md
@.planning/phases/34.5-key-lock-mechanic-the-first-non-math-gate/34.5-PATTERNS.md
@.planning/phases/34.5-key-lock-mechanic-the-first-non-math-gate/34.5-VALIDATION.md
@src/mechanics/secretAlcove.js
@src/mechanics/door.js
</context>

<artifacts_produced>
## Artifacts this plan produces (new symbols/files)

- **`src/mechanics/key.js`** — NEW. Exports `wireKey({ player, hud, onPickup, hasKey })`. Wires `player.onCollide("key", …)` (pickup) and `player.onCollide("lock", …)` (open-if-key-held, else a self-cleaning hint). All engine globals inside the body (a727c13).
- **`scripts/check-import-safety.sh`** — `"src/mechanics/key.js"` added to BOTH module lists: loop 1 (existence + `node --check`, ~84-91) and loop 2 (the `$TOPLEVEL_TRAP` a727c13 grep, ~129-136 — the real top-level-engine-global guard).
- **`src/levels/build.js`** — new emission loops: per `geometry.keys[]` a visible walk-through `area()` key (magenta under `?debug=1`), per `geometry.locks[]` an apex-height invisible solid blocker tagged `"lock"` plus a stashed visible colored-rect panel tagged `"lock-panel"` (`blocker.panelObj`).
- **`src/scenes/game.js`** — `let keyHeld = false;` closure run-state + a `wireKey({ player, hud, onPickup: () => { keyHeld = true; }, hasKey: () => keyHeld });` call alongside the existing mechanic wiring.
- **`src/ui/hud.js`** — a hidden-by-default `fixed()` key indicator entity + `showKey()` added to the returned controller (`{ refresh, flashLevelUp, showKey }`).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create src/mechanics/key.js and add it to the import-safety scan list</name>
  <read_first>
    - src/mechanics/secretAlcove.js (read in full) — the KEY-half template: the imports pattern (CONFIG/fx/audio, lines ~19-21), the closure-local fire-once `const ... = new Set()` latch (~line 47), and the feedback triad (fx.pop / fx.popupText / audio.playSfx) with `.pos.clone()` before destroy, destroy LAST. DROP all progress.* / hasSecretFound persistence — key-held is run-state.
    - src/mechanics/door.js (read in full) — the LOCK-half template: the `opened` Set + the destroy-collider-then-panel cleanup ordering (~lines 74-79). DROP the entire freeze machinery: `let busy`, `player.vel = vec2(0)`, `player.paused = true/false`, and `openChallenge(...)` — the lock NEVER freezes the player; it is a wall, not a math gate.
    - scripts/check-import-safety.sh — it has TWO hardcoded module lists and key.js must go in BOTH: LOOP 1 (~lines 84-91, ending `"src/mechanics/secretAlcove.js" "src/audio.js"; do`) only runs existence + `node --check` (syntax — does NOT catch a top-level `add(...)`/`text(...)`); LOOP 2 (~lines 129-136, the `for f in "src/scenes/title.js" ... "src/audio.js"` block) is the ACTUAL a727c13 guard — it greps `$TOPLEVEL_TRAP` for engine globals at module top level. Adding key.js to loop 1 only would be a FALSE GREEN.
    - 34.5-PATTERNS.md "NEW src/mechanics/key.js" section (the exact stripped excerpts) and Pitfall 2 / Pitfall 3 in RESEARCH.
  </read_first>
  <files>src/mechanics/key.js, scripts/check-import-safety.sh</files>
  <action>
    Create src/mechanics/key.js exporting `wireKey({ player, hud, onPickup, hasKey })`. Import CONFIG from ../config.js, plus fx and audio namespaces (mirror secretAlcove.js:19-21). ALL Kaplay engine globals (add, text, destroy, tween, center, pos, color, anchor, opacity, z, fixed, easings) live INSIDE the wireKey body only — never at module top level (a727c13; enforced by the gate you edit in this same task).
    KEY half (mirror secretAlcove.js): a closure-local `const collected = new Set()` fire-once latch; `player.onCollide("key", (keyObj) => {...})` that returns if already collected, adds to the set, calls onPickup() (flips scene keyHeld), calls hud.showKey(), fires fx.pop(keyObj.pos.clone()) + fx.popupText(keyObj.pos.clone(), "+ KEY") + audio.playSfx("pickup") (REUSE the existing pickup SFX — no new asset), then destroy(keyObj) LAST.
    LOCK half (mirror door.js MINUS the challenge), in the SAME wireKey body: a closure-local `const opened = new Set()` and a closure-local `let hintShowing = false`. `player.onCollide("lock", (lockObj) => {...})`: return if already opened; if hasKey() → add to opened, audio.playSfx("door") (REUSE door SFX), optional fx.clearBurst(), then destroy(lockObj) FIRST and destroy(lockObj.panelObj) if present (door.js:74-79 ordering — collider gone before the next frame so the same touch cannot re-block); return. Else (no key): if hintShowing return; set hintShowing = true; add() a fixed() z-high text entity using CONFIG.LOCK.HINT_TEXT / HINT_SIZE / HINT_Y / PALETTE.TEXT tagged "hud-flash" (so a scene leave sweeps it — anti-leak); fade it with tween(1, 0, CONFIG.LOCK.HINT_MS / 1000, v => hint.opacity = v, easings.easeOutQuad).onEnd(() => { destroy(hint); hintShowing = false; }). This self-cleaning tween is the ONLY dismissal path — do NOT reach for setTimeout / setInterval / wait() / loop() / lifespan() (banned by check-safety.sh). The hint NEVER pauses the player and NEVER opens a challenge (it is a spatial gate, not a math gate — math density stays LOCKED at 3).
    Then edit scripts/check-import-safety.sh: add `"src/mechanics/key.js"` to BOTH module lists — LOOP 1 (~lines 84-91, the existence + `node --check` gate) AND LOOP 2 (~lines 129-136, the `$TOPLEVEL_TRAP` a727c13 grep, which is the ACTUAL top-level-engine-global guard — `node --check` does NOT catch a top-level `add(...)`/`text(...)`). Adding it to loop 1 only is a FALSE GREEN (RESEARCH gate gap #1 — door/gates/enemy/secretAlcove are covered in loop 2 today but a new key.js would be UNGUARDED against the a727c13 trap).
  </action>
  <verify>
    <automated>node --check src/mechanics/key.js && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/mechanics/key.js` passes and the module has zero top-level engine-global references (a727c13).
    - `bash scripts/check-import-safety.sh` PASSes AND src/mechanics/key.js appears in BOTH loops — verifiable that it is in the `$TOPLEVEL_TRAP` scan (loop 2, ~lines 129-136), not just the `node --check` loop; a deliberate temporary top-level `add(...)` in key.js makes the gate FAIL (proving loop 2 covers it), then is removed.
    - `bash scripts/check-safety.sh` PASSes — no setTimeout/setInterval/wait()/loop()/lifespan(), no gameOver/loseLife/penalize constructs anywhere in key.js; the lock hint dismisses only via tween().onEnd().
    - wireKey exposes the signature `wireKey({ player, hud, onPickup, hasKey })` matching the game.js injection contract.
  </acceptance_criteria>
  <done>src/mechanics/key.js exists, wires both halves a727c13-clean, and passes check-safety.sh + check-import-safety.sh (now scanning it).</done>
</task>

<task type="auto">
  <name>Task 2: Emit key and lock entities from the descriptor in build.js</name>
  <read_first>
    - src/levels/build.js — the door blocker loop (lines ~266-289): the apex-blocker split (invisible tall solid collider + separate visible panel stashed on the blocker), the apex formula at line ~273 `Math.ceil((CONFIG.JUMP_FORCE ** 2) / (2 * CONFIG.GRAVITY)) + 64`, the `opacity(HIDDEN)` idiom, and the `?? []` never-brick guard. The secretAlcove loop (lines ~364-372): the walk-through visible `area()` trigger and the `DEBUG ? color(255,0,255)` magenta marker (lines ~72-74 define DEBUG/HIDDEN).
    - 34.5-PATTERNS.md "MODIFY src/levels/build.js" section (the exact excerpts) and Corrections #2 (the lock panel MUST be a colored rect, NOT sprite("door") — wrong art, and a new sprite would churn the manifest).
  </read_first>
  <files>src/levels/build.js</files>
  <action>
    Add two emission loops to buildLevel, both `?? []`-guarded so every existing (key-lock-free) level still builds. LOCK loop `for (const l of g.locks ?? [])`: compute blockerH via the exact door apex formula (build.js:273) `Math.ceil((CONFIG.JUMP_FORCE ** 2) / (2 * CONFIG.GRAVITY)) + 64` — this is what makes the lock genuinely unjumpable, which the validator's x-partition model depends on. add() an invisible tall solid blocker: rect(CONFIG.LOCK.W, blockerH), pos(l.x, l.y + CONFIG.LOCK.H - blockerH), opacity(HIDDEN) (0 normal / 0.35 under ?debug=1), area(), body({ isStatic: true }), tag "lock". Then add() a separate visible panel: rect(CONFIG.LOCK.W, CONFIG.LOCK.H), pos(l.x, l.y), color spread from CONFIG.LOCK.PANEL_COLOR, tag "lock-panel" — a colored-rect placeholder (NO sprite, NO manifest churn; art is Phase 35). Stash the panel on the blocker as `blocker.panelObj` so key.js's lock-open branch destroys both together.
    KEY loop `for (const k of g.keys ?? [])`: add() a VISIBLE walk-through trigger (the key is telegraphed, NOT a secret): rect(CONFIG.KEY.W, CONFIG.KEY.H), pos(k.x, k.y), area(), and — under DEBUG — the magenta marker color(255,0,255)+opacity(0.8) like the alcove; otherwise color spread from CONFIG.KEY.COLOR + opacity(1). Tag "key". No body() — the player walks THROUGH it (pickup is onCollide, not a blocker).
  </action>
  <verify>
    <automated>node scripts/check-assets-manifest.mjs && node scripts/browser-boot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-assets-manifest.mjs` PASSes — zero new sprite/sound paths declared (colored-rect placeholders + reused SFX only).
    - `node scripts/browser-boot.mjs` boots and drives all 8 levels with no console errors — the two new loops are `?? []`-inert on every level (none carry keys/locks yet), proving no regression.
    - The lock blocker uses the exact door apex formula (grep-confirmable: the same `JUMP_FORCE ** 2 / (2 * GRAVITY)` expression the door blocker uses), and the visible panel is a `rect(...)` + `CONFIG.LOCK.PANEL_COLOR`, not `sprite("door")`.
  </acceptance_criteria>
  <done>build.js emits key + lock + lock-panel from geometry.keys/locks (`?? []`-guarded); manifest + browser-boot green with no regression.</done>
</task>

<task type="auto">
  <name>Task 3: Thread keyHeld run-state in game.js and add the HUD key indicator</name>
  <read_first>
    - src/scenes/game.js — the closure run-state block (`let coinsCollected = 0;` ~line 59, `let goalReached = false;` ~line 63, and the "never serialized" comment ~line 79), the wireDoor/wireSecretAlcove call sites (~lines 266, 280), and respawn() (~lines 163-172) which repositions in place (no go()) so closure run-state survives it for free.
    - src/ui/hud.js — the mountHud factory: the badge/XP-fill entity creation (~lines 53-81), the fixed()/z()/anti-leak "hud" tag idiom, the TOFU-fallback note (~line 91), and the `return { refresh, flashLevelUp };` (~line 144).
    - 34.5-PATTERNS.md "MODIFY src/scenes/game.js" and "MODIFY src/ui/hud.js" sections.
  </read_first>
  <files>src/scenes/game.js, src/ui/hud.js</files>
  <action>
    In src/ui/hud.js mountHud: add a hidden-by-default indicator entity alongside the badge/bar — add() text(CONFIG.HUD.KEY_GLYPH, { size: CONFIG.HUD.KEY_SIZE }), pos(CONFIG.HUD.KEY_X, CONFIG.HUD.KEY_Y), color spread from CONFIG.PALETTE.REWARD, fixed(), z(9000), opacity(0) (hidden until collected), tag "hud" (torn down on scene teardown — anti-leak). Define a local `function showKey() { keyIndicator.opacity = 1; }` (opacity(0)→1, no scheduler, stays camera-immune) and add showKey to the returned controller so it becomes `{ refresh, flashLevelUp, showKey }`. If CONFIG.HUD.KEY_GLYPH is a non-ASCII glyph and the executor cannot confirm it renders (TOFU risk, hud.js:91 precedent), fall back to the plain string "KEY".
    In src/scenes/game.js: add `let keyHeld = false;` to the closure run-state block near coinsCollected/goalReached (never module-level — anti-leak; survives reposition-in-place respawn; never serialized, so the save shape / SAVE.VERSION are untouched). After the existing wireDoor/wireSecretAlcove calls, add `wireKey({ player, hud, onPickup: () => { keyHeld = true; }, hasKey: () => keyHeld });` (import wireKey from ../mechanics/key.js). keyHeld resets only on a full scene re-entry (correct — a new run starts without the key).
  </action>
  <verify>
    <automated>bash scripts/check-progress.sh && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-progress.sh` PASSes — keyHeld is run-state only, no save-shape drift (SAVE key / version unchanged, no new serialized field).
    - `node scripts/browser-boot.mjs` green across all 8 levels (no console errors; the HUD indicator starts hidden and no level triggers it yet).
    - `node scripts/audit-phase21-mechanics.mjs` green — the existing door/enemy/alcove encounters on all levels are unaffected by the new wiring.
    - hud.js's returned controller includes showKey; game.js's wireKey call injects onPickup (flips keyHeld) and hasKey (reads keyHeld).
  </acceptance_criteria>
  <done>keyHeld is closure-local run-state (no save drift); wireKey is called; HUD exposes showKey; browser-boot + audit-phase21 + check-progress green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in the web/app-sec sense) | Static client-only browser game — no backend, no accounts, no network I/O. The HUD hint/indicator use canvas `text()` (no innerHTML/DOM), so there is no injection sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34.5-03 | Denial (of progress) | lock same-frame re-block / hint spam / hidden softlock | high | mitigate | Destroy the lock collider + panel BEFORE the next frame on a key-held open (door.js ordering) so the same touch cannot re-block; gate the no-key hint with a closure-local `hintShowing` boolean cleared by its own `tween().onEnd()` (no timer, no stacking). No damage/bounce/reset/game-over — enforced by check-safety.sh. |
| T-34.5-04 | Tampering | unguarded a727c13 trap on the new module | medium | mitigate | Add src/mechanics/key.js to BOTH check-import-safety.sh loops — critically the `$TOPLEVEL_TRAP` grep loop (129-136), not just the `node --check` loop (RESEARCH gap #1); `node --check` does not catch a top-level `add(...)`, so loop-1-only would leave the trap unguarded and blank the game with no gate catching it. |
| T-34.5-SC | Tampering | package installs | low | accept | Zero package installs this phase — colored-rect placeholders + reused pickup/door SFX add no assets and no dependencies. Nothing to audit. |
</threat_model>

<verification>
- `node --check src/mechanics/key.js` — syntax clean.
- `bash scripts/check-safety.sh` — no timers / no punishment in key.js; hint self-cleans via tween().onEnd().
- `bash scripts/check-import-safety.sh` — key.js now scanned; a727c13-clean.
- `bash scripts/check-gate.sh` — math-gate/challenge invariants unaffected (re-run to confirm).
- `bash scripts/check-progress.sh` — key-held is run-state, no save-shape drift.
- `node scripts/check-assets-manifest.mjs` — no new asset paths.
- `node scripts/browser-boot.mjs` — all 8 levels boot/drive clean.
- `node scripts/audit-phase21-mechanics.mjs` — existing mechanics unaffected.
</verification>

<success_criteria>
The key/lock mechanic exists as CODE (KEY-01): `src/mechanics/key.js` wires pickup + lock-open, build.js emits key/lock entities from `geometry.keys`/`geometry.locks` (`?? []`-guarded so no existing level breaks), game.js threads closure-local key-held run-state, and the HUD shows a persistent key-held indicator — all a727c13-clean, no timers, no punishment, no new assets. The mechanic is inert until a descriptor uses it (Plan 03 authors the level-02 proof). Every existing gate stays green.
</success_criteria>

<output>
Create `.planning/phases/34.5-key-lock-mechanic-the-first-non-math-gate/34.5-02-SUMMARY.md` when done.
</output>
