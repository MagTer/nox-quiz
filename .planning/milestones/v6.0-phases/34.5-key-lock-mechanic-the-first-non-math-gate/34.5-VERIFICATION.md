---
phase: 34.5-key-lock-mechanic-the-first-non-math-gate
verified: 2026-07-15T00:00:00Z
status: passed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 34.5: Key & Lock Mechanic — the first non-math gate Verification Report

**Phase Goal:** A key/lock mechanic exists, is provably softlock-free, and is validator- and audit-covered — BEFORE any level is authored to use it.
**Verified:** 2026-07-15
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

All items below were checked against the actual source files (not SUMMARY.md prose) and, wherever behavioral, by RUNNING the command myself in this session — not by reading a prior transcript.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `src/mechanics/key.js` exists with a `wireKey` seam wiring BOTH a walk-through key pickup (no challenge) AND a solid lock that opens only when key-held | ✓ VERIFIED | Read file in full: `export function wireKey({ player, hud, onPickup, hasKey })` wires `player.onCollide("key", …)` (fire-once `collected` Set, fx/audio feedback, destroy last) and `player.onCollide("lock", …)` (`hasKey()` gate: opens+destroys collider+panel, else shows a self-cleaning hint). No `openChallenge()` call anywhere in the file — confirmed by grep. |
| 2 | `geometry.keys`/`geometry.locks` are emitted by `src/levels/build.js` | ✓ VERIFIED | build.js lines 357–403: `for (const l of g.locks ?? [])` emits an apex-tall invisible blocker (tag `"lock"`) + a colored-rect visible panel (tag `"lock-panel"`, stashed as `blocker.panelObj`); `for (const k of g.keys ?? [])` emits a visible walk-through `area()` trigger (tag `"key"`, no `body()`). Both `?? []`-guarded. |
| 3 | `game.js` threads closure-local `keyHeld` run-state (NOT module-level) into `wireKey` | ✓ VERIFIED | `let keyHeld = false;` declared inside `gameScene(data)` body (line 72, alongside `coinsCollected`/`goalReached`), never at module scope. `wireKey({ player, hud, onPickup: () => { keyHeld = true; }, hasKey: () => keyHeld })` called at line 304. |
| 4 | HUD shows a key-held indicator | ✓ VERIFIED | `src/ui/hud.js`: hidden-by-default `keyIndicator` entity (`opacity(0)`, tag `"hud"`) + `function showKey() { keyIndicator.opacity = 1; }`, exported on the returned controller `{ refresh, flashLevelUp, showKey }`. `key.js`'s pickup handler calls `hud.showKey()`. |
| 5 | a727c13 clean: `bash scripts/check-import-safety.sh` PASSES, `src/mechanics/key.js` appears in BOTH loops | ✓ VERIFIED | Ran `bash scripts/check-import-safety.sh` → `import-safety checks: PASS` (exit 0). Confirmed via direct read: `key.js` is in loop 1 (existence+`node --check`, line 89) AND loop 2 (the `$TOPLEVEL_TRAP` a727c13 grep, line 132) — loop-2-only coverage was the specific risk called out in the plan and it is present. |
| 6 | No timers/punishment; `bash scripts/check-safety.sh` PASSES; the lock hint self-cleans via `tween().onEnd()` | ✓ VERIFIED | Ran `bash scripts/check-safety.sh` → `safety checks: PASS` (exit 0). Grepped `key.js` for `setTimeout\|setInterval\|\bwait(\|\bloop(\|lifespan(` — the only hits are in comments documenting the ban, not code. The hint dismissal is a single `tween(1, 0, …).onEnd(() => { destroy(hint); hintShowing = false; })` call — no scheduler. |
| 7 | `scripts/lib/key-lock-check.mjs` exists, is composed into `scripts/validate-levels.mjs`, and its self-test PASSES | ✓ VERIFIED | Ran `node scripts/lib/key-lock-check.mjs` → `key-lock-check-selftest: PASS` (exit 0, 5 cases including the same-run key-behind-lock regression guard, the A2 platform-bridging guard, and multi-pair `keyId` matching). Confirmed composition: `validate-levels.mjs` imports `checkKeyLockReachability` (line 36) and sums `kl.hardFailCount` into `failures` (line 110). |
| 8 | RED-first proof: `node scripts/validate-levels.mjs --fixture scripts/fixtures/bad-level-key.js` exits 1 with a key-lock HARD-FAIL | ✓ VERIFIED | Ran it directly: exit 1, exactly one row — `bad-level-key-fixture \| key-lock \| HARD-FAIL \| key[0] x:600 y:288 NOT reachable before lock x:400 (softlock)`. No other check's rows fired (biome/over-hole/spawn-goal all PASS), so the HARD-FAIL is unambiguously attributable to the key-lock check. |
| 9 | Real levels: `validate-levels.mjs` shows level-02 with a key-lock row that is NOT a HARD-FAIL; the 13 known-deferred HARD-FAILs (8 coin levels 01-03, 5 headroom level-07) are unchanged | ✓ VERIFIED | Ran `node scripts/validate-levels.mjs`: `level-02 \| key-lock \| WARN \| key[0] x:900 reachable spawn-side of lock x:3960 (marginRatio=1.000)`. Final tally: `validate-levels: FAIL — 13 hard-failure(s) across 8 level(s)` — counted exactly 8 `coin-reachability \| HARD-FAIL` rows (levels 01/02/03) + 5 `headroom \| HARD-FAIL` rows (level-07), zero new. Matches the Phase-34 accepted baseline exactly. |
| 10 | In-engine: `node scripts/audit-key-lock.mjs` PASSES — a real driven player picks up the key THEN opens the lock | ✓ VERIFIED | Ran it directly: exit 0. Output: `key.collected:true`, `lock.opened:true`, `triggered:true`. `AUDIT-KEY-LOCK: KEY COLLECTED, LOCK OPENED — SC3 in-engine proof holds.` This is a real Playwright-driven browser session, not a static-model inference. |
| 11 | Locked decisions honored: math density unchanged (3 challenges/level), key opens the exit approach (lock before goal, not co-located), no new sprites/sounds | ✓ VERIFIED | `bash scripts/check-gate.sh` → PASS (math-gate/challenge invariants unaffected). level-02's `mathGates: []`, 1 door (x:1540), 1 enemy (x:1860) — unchanged counts; the key-lock is a 4th NON-math barrier at x:3960, strictly before goal x:4200 (verified by reading level-02.js directly and confirmed by `audit-phase21-mechanics.mjs`'s ALL MECHANICS RESOLVED — same door/enemy encounters trigger unobstructed). `node scripts/check-assets-manifest.mjs` → PASS, 37 assets, zero new paths (lock panel is `CONFIG.LOCK.PANEL_COLOR` = `PALETTE.MUTED`, a colored rect; key/lock/door SFX reused). |

**Score:** 11/11 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/mechanics/key.js` | `wireKey()` two-halves mechanic | ✓ VERIFIED | Exists, exports `wireKey`, a727c13-clean, no timers, wired to game.js. |
| `scripts/lib/key-lock-check.mjs` | `checkKeyLockReachability(geometry)` softlock proof | ✓ VERIFIED | Exists, exports the function with the documented lock-cut-BFS contract, self-test PASSes 5 cases. |
| `scripts/fixtures/bad-level-key.js` | RED-first fixture | ✓ VERIFIED | Exists, single deliberate defect (same-floor-run key-behind-lock), drives the check to HARD-FAIL exactly once. |
| `scripts/audit-key-lock.mjs` | driven-player in-engine audit | ✓ VERIFIED | Exists, port 8774 (collision-checked against other scripts), copies audit-coins.mjs boilerplate, runs green. |
| `src/config.js` LOCK/KEY/HUD.KEY_* blocks | shared tunables | ✓ VERIFIED | `CONFIG.LOCK` (W/H/PANEL_COLOR/HINT_TEXT/HINT_SIZE/HINT_Y/HINT_MS), `CONFIG.KEY` (W/H/COLOR), `CONFIG.HUD.KEY_X/KEY_Y/KEY_SIZE/KEY_GLYPH` all present. |
| `scripts/lib/reachability.mjs` exports | `bfsWithPathMargin`, `bestMarginToPoint` exported | ✓ VERIFIED | `grep -n "export function"` confirms both at lines 323/406. |
| `src/levels/build.js` key/lock emission | entity creation from geometry arrays | ✓ VERIFIED | Two `?? []`-guarded loops present (lines 357–403), exact apex-height formula shared with door/math-gate/enemy blockers. |
| `src/scenes/game.js` keyHeld + wireKey call | closure-local run-state + injection | ✓ VERIFIED | `let keyHeld = false;` in closure; `wireKey({...})` call present. |
| `src/ui/hud.js` keyIndicator + showKey | HUD indicator | ✓ VERIFIED | Hidden-by-default entity + `showKey()` on the returned controller. |
| `src/levels/level-02.js` geometry.keys/locks | throwaway in-engine proof placement | ✓ VERIFIED | `keys: [{ x: 900, y: FLOOR_Y - 32 }]`, `locks: [{ x: 3960, y: FLOOR_Y - CONFIG.LOCK.H }]` — after door/enemy, before goal. |
| `scripts/check-import-safety.sh` | key.js scanned in both loops | ✓ VERIFIED | Confirmed present in loop 1 (line 89) and loop 2 / TOPLEVEL_TRAP (line 132). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `key.js` pickup handler | `game.js` `keyHeld` | `onPickup()` callback | ✓ WIRED | `onPickup: () => { keyHeld = true; }` injected at the `wireKey` call site; `key.js` calls `onPickup()` on first key touch. |
| `key.js` lock handler | `game.js` `keyHeld` | `hasKey()` callback | ✓ WIRED | `hasKey: () => keyHeld` injected; `key.js`'s lock branch reads `hasKey()` to decide open-vs-hint. |
| `key.js` pickup handler | `hud.js` | `hud.showKey()` | ✓ WIRED | Called inside the pickup handler; `hud.js` exports `showKey` on its returned controller. |
| `build.js` lock blocker | `key.js` lock-open branch | `blocker.panelObj` | ✓ WIRED | build.js stashes the visible panel as `blocker.panelObj`; key.js's open branch destroys `lockObj` then `lockObj.panelObj` if present. |
| `key-lock-check.mjs` | `reachability.mjs` | `bfsWithPathMargin`/`bestMarginToPoint` imports | ✓ WIRED | Both now exported; self-test PASSes (would silently return `undefined` results otherwise, and the self-test explicitly proves the lock-cut BFS fires). |
| `validate-levels.mjs` | `key-lock-check.mjs` | `checkKeyLockReachability` import + `hardFailCount` sum | ✓ WIRED | Confirmed by grep: import at line 36, `failures += kl.hardFailCount` at line 110 — a HARD-FAIL genuinely drives `process.exit(1)` (proven by the fixture run). |

### Behavioral Spot-Checks / Gate Runs (executed live, this session)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Import-safety a727c13 gate | `bash scripts/check-import-safety.sh` | `import-safety checks: PASS` | ✓ PASS |
| No-timer/no-punishment gate | `bash scripts/check-safety.sh` | `safety checks: PASS` | ✓ PASS |
| Reachability self-test | `node scripts/lib/reachability.mjs` | `reachability-selftest: PASS` | ✓ PASS |
| Key-lock-check self-test | `node scripts/lib/key-lock-check.mjs` | `key-lock-check-selftest: PASS` | ✓ PASS |
| RED-first fixture | `node scripts/validate-levels.mjs --fixture scripts/fixtures/bad-level-key.js` | exit 1, exactly one `key-lock \| HARD-FAIL` row | ✓ PASS |
| Real levels validator | `node scripts/validate-levels.mjs` | exit 1, 13 HARD-FAILs (8 coin + 5 headroom, unchanged), level-02 `key-lock \| WARN` | ✓ PASS (matches accepted baseline) |
| Asset manifest | `node scripts/check-assets-manifest.mjs` | `PASS — 37 assets verified on disk` | ✓ PASS |
| In-engine key/lock audit | `node scripts/audit-key-lock.mjs` | exit 0, `key.collected:true`, `lock.opened:true`, `triggered:true` | ✓ PASS |
| Math-gate/challenge invariants | `bash scripts/check-gate.sh` | `gate checks: PASS` | ✓ PASS |
| Save-shape / progress invariants | `bash scripts/check-progress.sh` | `smoke-progress: PASS` / `progress checks: PASS` | ✓ PASS |
| Interactive mechanic audit | `node scripts/audit-phase21-mechanics.mjs` | `AUDIT: ALL MECHANICS RESOLVED` | ✓ PASS |
| Full-level boot/drive | `node scripts/browser-boot.mjs` | `Browser boot: PASS — title -> select -> all levels loaded with no runtime errors.` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|--------------|--------|----------|
| KEY-01 | 34.5-02, 34.5-03 | A key/lock mechanic exists — the game's FIRST non-math gate | ✓ SATISFIED | `src/mechanics/key.js` + `geometry.keys`/`geometry.locks` + `build.js` emission all exist, wired end-to-end, no math challenge in the lock path (confirmed no `openChallenge` reference in key.js). |
| KEY-02 | 34.5-01, 34.5-03 | NO SOFTLOCK IS POSSIBLE — validator HARD-proves reachability; in-engine audit proves a real player picks up the key and opens the lock | ✓ SATISFIED | Static proof: `key-lock-check.mjs` self-test + RED-first fixture (exit 1) + real-level PASS/WARN (zero new HARD-FAILs). In-engine proof: `audit-key-lock.mjs` exit 0, both entities confirmed collected/opened by a real driven browser. |

No orphaned requirements — both KEY-01 and KEY-02 map to Phase 34.5 in REQUIREMENTS.md's traceability table and both are covered by plan `requirements:` frontmatter (01: KEY-02, 02: KEY-01, 03: KEY-01+KEY-02).

### Anti-Patterns Found

None. Grepped every file this phase created/modified (`src/mechanics/key.js`, `scripts/lib/key-lock-check.mjs`, `scripts/fixtures/bad-level-key.js`, `scripts/audit-key-lock.mjs`, `src/levels/level-02.js`, `src/levels/build.js`, `src/scenes/game.js`, `src/ui/hud.js`, `src/config.js`, `scripts/validate-levels.mjs`, `scripts/check-import-safety.sh`) for `TBD|FIXME|XXX` — zero hits. No stub bodies, no `return null`/`{}`/`[]` short-circuits on the mechanic's core paths, no hardcoded-empty data feeding a rendered value.

### Human Verification Required

None. Every must-have in this phase's scope resolves to a command-line-verifiable fact (static validator + in-engine Playwright drive), and every relevant command was RUN in this session (not read from a transcript). No visual/UX judgment call is in scope for this phase (placeholder art is explicitly deferred to Phase 35 per CONTEXT).

### Gaps Summary

None. All 11 must-haves verified against the live codebase and live command output. The phase goal — "a key/lock mechanic exists, is provably softlock-free, and is validator- and audit-covered — BEFORE any level is authored to use it" — is achieved: the mechanic (`key.js`), the descriptor fields (`geometry.keys`/`geometry.locks`), the static softlock proof (`key-lock-check.mjs` + RED-first fixture), and the in-engine driven-player proof (`audit-key-lock.mjs`) all exist and all pass when run directly. The one throwaway placement on level-02 is explicitly scoped as a proof vehicle, not level authoring (34.6 rebuilds level-02 from scratch), and is correctly documented as such in both CONTEXT.md and the level-02.js comments.

One notable finding *for* this phase (not against it): Plan 03's own execution caught and fixed two real bugs by actually running the gates rather than trusting the static model — a key placement that sat inside a jump-arc landing zone (x:760 → x:900) and a generic-`triggered`-flag drive-stall — both documented in 34.5-03-SUMMARY.md and confirmed present/correct in the current source (`x:900` in level-02.js; the intervening-encounter resolution loop in audit-key-lock.mjs). This is exactly the discipline the project's "checks that don't play the game lie" standard calls for, and it held up under independent re-verification.

---

_Verified: 2026-07-15_
_Verifier: Claude (gsd-verifier)_
