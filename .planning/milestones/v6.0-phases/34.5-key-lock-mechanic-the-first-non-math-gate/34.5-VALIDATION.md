---
phase: 34.5
slug: key-lock-mechanic-the-first-non-math-gate
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-15
---

# Phase 34.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> This tree has NO JS test framework (no-npm / no-build canon). The suite IS
> `node scripts/*.mjs` self-tests + `bash scripts/check-*.sh` gates + Playwright audits.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — `node scripts/*.mjs` validators/audits + `bash scripts/check-*.sh` gates (CLAUDE.md canon) |
| **Config file** | none |
| **Quick run command** | `node scripts/validate-levels.mjs && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh` |
| **Full suite command** | `bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && node scripts/check-assets-manifest.mjs && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs && node scripts/audit-key-lock.mjs` |
| **Estimated runtime** | ~60–120 seconds (browser-boot + audit dominate) |

---

## Sampling Rate

- **After every task commit:** Run the quick run command (adds `--fixture scripts/fixtures/bad-level-key.js` expecting exit 1 once the fixture exists).
- **After every plan wave:** Run the full suite command.
- **Before `/gsd-verify-work`:** Full suite must be green (every gate exit 0; the RED-first fixture is the one deliberate exit-1, run as a negative assertion).
- **Max feedback latency:** ~120 seconds.

---

## Per-Task Verification Map

| Task ID | Wave | Requirement | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|-------------|-----------------|-----------|-------------------|-------------|--------|
| key-lock-check.mjs + reachability export | 1 | KEY-02 | key proven reachable spawn-side of its lock (no softlock) | static HARD-FAIL | `node scripts/validate-levels.mjs` | ❌ W0 | ⬜ pending |
| bad-level-key.js fixture | 1 | KEY-02 | the HARD check actually fires RED (key sealed behind its own lock) | static self-test (negative) | `node scripts/validate-levels.mjs --fixture scripts/fixtures/bad-level-key.js` → exit 1 | ❌ W0 | ⬜ pending |
| key.js mechanic + import-safety list | 2 | KEY-01 | pickup + lock-open wired; no timers/punishment; a727c13 clean | static gate | `bash scripts/check-safety.sh && bash scripts/check-import-safety.sh` | ✅ (add key.js to list) | ⬜ pending |
| build.js entities + level-02 keys/locks | 2 | KEY-01 | key/lock entities build; no manifest churn | manifest + smoke | `node scripts/check-assets-manifest.mjs && node scripts/browser-boot.mjs` | ✅ | ⬜ pending |
| game.js wireKey + hud.js indicator | 2 | KEY-01 | key-held run-state closure-local; HUD indicator shows | in-engine gate | `node scripts/audit-key-lock.mjs` | ❌ W0 | ⬜ pending |
| audit-key-lock.mjs | 3 | KEY-02 | real driven player collects key THEN opens lock | in-engine gate | `node scripts/audit-key-lock.mjs` (assert `get("key")`/`get("lock")` dropped) | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `scripts/lib/key-lock-check.mjs` — the softlock check + an `isMain` self-test (over-hole-check.mjs idiom).
- [ ] `scripts/fixtures/bad-level-key.js` — the RED-first "key behind its own lock" fixture (must make `validate-levels.mjs --fixture` exit 1).
- [ ] `scripts/audit-key-lock.mjs` — the in-engine driven proof (copies audit-coins.mjs server/guard boilerplate per the no-shared-module convention).
- [ ] Export `bfsWithPathMargin` (reachability.mjs:323) AND `bestMarginToPoint` (reachability.mjs:406) — BOTH currently module-private (pattern-mapper correction to research).
- [ ] Add `"src/mechanics/key.js"` to `scripts/check-import-safety.sh`'s hardcoded scoped file list (or the a727c13 trap goes unguarded on the new module).

*Existing infrastructure (validate-levels.mjs composition, browser-boot.mjs, check-*.sh) covers the rest.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Lock "feels" like a solid wall + hint reads clearly | KEY-01 | Subjective feel; automated audit proves passability, not readability | Serve `?debug=1`, walk into the level-02 lock without the key: confirm it blocks and shows the hint; collect key, confirm it opens. (Level-02 is throwaway — 34.6 rebuilds it — so this is a mechanic smoke test, not a kid-UAT.) |

*All softlock-safety behaviors have automated verification (the HARD validator + RED-first fixture + driven audit). Kid-UAT of real key-lock levels is Phase 38, after 34.6 authors them.*

---

## Validation Sign-Off

- [ ] All tasks have an automated verify command or Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (the 5 gaps above)
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter (set at execution, once Wave 0 lands)

**Approval:** pending
