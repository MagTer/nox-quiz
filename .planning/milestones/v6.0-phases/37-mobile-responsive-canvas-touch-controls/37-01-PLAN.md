---
phase: 37-mobile-responsive-canvas-touch-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/touch-coordinate-probe.mjs
autonomous: true
requirements: [MOB-01]
must_haves:
  truths:
    - "A new Playwright script boots the game in a TOUCH-enabled context (hasTouch:true, isMobile:true), taps the visual center of the #game canvas, reads the resulting game-space coordinate back through the engine's own onTouchStart, and asserts it maps to ~320 (canvas center in 640-wide game space)"
    - "Run against the CURRENT CSS-transform:scale(1.5) build the probe goes RED — the tap reads back ~480 (overshoot by the 1.5x factor), so the assertion fails and the process exits non-zero — proving the documented mouse/touch coordinate desync empirically, not by argument (MOB-01 SC1)"
    - "The probe dups browser-boot.mjs's hardened static server + resolvePlaywright() resolver per the deliberate script-duplication convention (copied server/guard code, fixed identically by hand) — loopback bind, ROOT-clamped path serving, dynamic Playwright resolution"
  artifacts:
    - "scripts/touch-coordinate-probe.mjs — the RED-first permanent touch-coordinate gate"
  key_links:
    - "page.touchscreen.tap(center) → engine Me.touchstart → Qe(win→content) → injected onTouchStart callback writes window.__lastTouch → probe reads it back → assert |x-320|<=2"
    - "RED now (transform build ~480) → GREEN after 37-02 letterbox migration (~320) → stays a permanent gate in the phase suite"
---

<objective>
Author the RED-first Playwright touch-coordinate probe that opens the whole mobile phase. It taps the canvas center in a touch-emulated browser context and asserts the tap lands at game-x ~320. Against today's `transform: scale(1.5)` build it MUST fail (reads ~480) — this is the empirical proof of the mouse/touch coordinate desync that the letterbox migration (37-02) then fixes, turning the probe GREEN. The probe is committed FIRST and kept as a permanent gate.

Purpose: The migration is only trustworthy if a machine proves the desync existed and then proves it is gone. This plan lands that machine and demonstrates RED — no source file changes yet, so the probe fails exactly as intended.

Output: `scripts/touch-coordinate-probe.mjs`, dup'ing browser-boot's hardened server + resolver, proven RED against the current build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-mobile-responsive-canvas-touch-controls/37-CONTEXT.md
@.planning/phases/37-mobile-responsive-canvas-touch-controls/37-RESEARCH.md
@scripts/browser-boot.mjs
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the touch-coordinate probe (dup server + resolver)</name>
  <files>scripts/touch-coordinate-probe.mjs</files>
  <read_first>
    - scripts/browser-boot.mjs — lines 1-120 (createServer static handler, ROOT/ROOT_ABS clamp, MIME map, ephemeral PORT via listen(0), FALLBACK_PLAYWRIGHT_PATH + resolvePlaywright()). COPY this server + resolver verbatim per the deliberate-duplication convention (CLAUDE.md: "fix a bug identically in every copy by hand; do not extract a shared module"). Do NOT weaken the loopback bind or the ROOT path clamp (T-37-01).
    - 37-RESEARCH.md — §"RED-first Playwright touch-coordinate probe" (the exact context options hasTouch:true/isMobile:true, the injected onTouchStart read, the tap-center + assert-320 shape) and §"Source-Verified Engine Findings" finding 2 (why the tap reads ~480 today and ~320 under letterbox) and Assumption A5 (if the cross-frame read flakes, fall back to reading mousePos() after the tap, which touchToMouse also sets).
  </read_first>
  <action>
    Create `scripts/touch-coordinate-probe.mjs`. Copy browser-boot.mjs's static server + `resolvePlaywright()` + `FALLBACK_PLAYWRIGHT_PATH` + ephemeral-port (listen(0)) block verbatim (deliberate duplication). Launch chromium, then `browser.newContext({ viewport: { width: 960, height: 540 }, hasTouch: true, isMobile: true })` so touch events + touchscreen.tap are enabled and the viewport meta is honored. Navigate to the served `src/index.html`, wait for the game to boot (mirror browser-boot's readiness wait — e.g. wait until an engine global like `mousePos` / a booted scene is present). Inject a probe listener with `page.evaluate` using the engine global exposed by `global:true`: `window.__lastTouch = null; onTouchStart((pos) => { window.__lastTouch = { x: pos.x, y: pos.y }; });`. Read the canvas box via `page.locator('#game').boundingBox()`, `page.touchscreen.tap(box.x + box.width/2, box.y + box.height/2)`, await one `requestAnimationFrame`, then read back `window.__lastTouch`. Assert `Math.abs(p.x - 320) <= 2` with a clear message `touch desync: expected game-x~320, got ${p.x}`; exit non-zero on failure, zero on pass. Provide the documented A5 fallback read (read `mousePos()` after the tap) as a commented alternative the executor can switch to only if the injected-callback read flakes across the frame boundary. Keep the port ephemeral so the probe runs concurrently with other harness copies (the Phase-34 EADDRINUSE lesson).
  </action>
  <verify>
    <automated>node --check scripts/touch-coordinate-probe.mjs && grep -q "hasTouch: true" scripts/touch-coordinate-probe.mjs && grep -q "touchscreen.tap" scripts/touch-coordinate-probe.mjs && grep -q "320" scripts/touch-coordinate-probe.mjs && grep -q "listen(0)" scripts/touch-coordinate-probe.mjs</automated>
  </verify>
  <done>The probe file exists, parses, boots a touch-enabled context, taps center, reads game-space back, and asserts ~320 — dup'ing the hardened server + resolver.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the probe goes RED against the current transform build</name>
  <files>scripts/touch-coordinate-probe.mjs</files>
  <read_first>
    - src/main.js lines 28-51 — the `canvas.style.transform = "scale(1.5)"` block still present (unchanged by this plan) is what makes the tap read ~480, not ~320.
    - 37-RESEARCH.md §Source-Verified Engine Findings finding 2 — the exact arithmetic: visual-center tap gives clientX-rect.x = 480, non-letterbox Qe maps 480*640/640 = 480, while the true center is 320.
  </read_first>
  <action>
    Run the probe against the UNCHANGED current build and CONFIRM it fails RED — the tap reads ~480 and the `|x-320|<=2` assertion throws, so the process exits non-zero. Do NOT touch any `src/` file to make it pass — a RED result here is the SUCCESS condition of this task (the migration in 37-02 is what turns it GREEN). Record the observed game-x (~480) in the plan summary as the documented RED baseline. If the probe errors for an infrastructure reason (Playwright not resolvable, boot timeout) rather than an assertion failure, fix the harness until the failure is specifically the ~480-vs-320 assertion, not a crash.
  </action>
  <verify>
    <automated>if node scripts/touch-coordinate-probe.mjs; then echo "UNEXPECTED GREEN — probe must be RED on the pre-migration build"; exit 1; else echo "RED confirmed (expected)"; exit 0; fi</automated>
  </verify>
  <done>The probe fails on the current CSS-transform build with an assertion error reading ~480 (not a crash) — the desync is empirically proven and the RED baseline is recorded. 37-02 will flip it GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe HTTP server → filesystem | Local loopback static server serving repo files to headless chromium; only boundary this plan adds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Information Disclosure | new probe static server path traversal | medium | mitigate | Dup browser-boot's ROOT-clamped path resolution + loopback bind verbatim; do NOT weaken |
| T-37-SC | Tampering | package installs | n/a | accept | Zero new dependencies; Playwright resolved from the existing browser-boot resolver |
</threat_model>

<verification>
```
node --check scripts/touch-coordinate-probe.mjs
node scripts/touch-coordinate-probe.mjs   # EXPECTED RED (exit != 0, assertion ~480 vs 320)
```
- The probe is authored and syntactically valid.
- It fails RED on the current build for the RIGHT reason (assertion, not a crash) — the desync is proven.
</verification>

<success_criteria>
- `scripts/touch-coordinate-probe.mjs` exists, dups the hardened server + resolver, boots a touch context, taps center, asserts ~320.
- It goes RED (~480) on the current transform build — the documented pre-migration baseline.
</success_criteria>

<output>
Create `.planning/phases/37-mobile-responsive-canvas-touch-controls/37-01-SUMMARY.md` when done (record the observed RED game-x).
</output>
