---
phase: 37-mobile-responsive-canvas-touch-controls
plan: 03
type: execute
wave: 3
depends_on: ["37-02"]
files_modified:
  - src/config.js
  - src/input.js
  - src/player.js
autonomous: true
requirements: [MOB-02]
must_haves:
  truths:
    - "A new src/input.js is the ONE input seam: it ORs keyboard held-state with virtual-button held-state for left/right, and owns a jump press/release edge registry — so both keyboard and (future) touch buttons feed the SAME jump path and player.js's LOCKED coyote/buffer/variable-height physics are reused, never re-implemented"
    - "player.js reads movement via input.isLeftHeld()/isRightHeld() and registers its buffer-set + variable-height-cut on input.onJumpPress()/onJumpRelease() instead of directly on onKeyPress/onKeyRelease — keyboard behavior stays byte-identical (same isKeyDown left/a and right/d, same JUMP_KEYS, same player.paused guard and jump SFX)"
    - "No timer anywhere: variable-height stays pure press/release edges + vel.y sign in player.js (check-safety green); no setTimeout/setInterval/wait()/loop()/lifespan()"
    - "No engine global at module top level in input.js — module scope holds only plain data (held-state booleans, callback arrays); every isKeyDown/onKeyPress/onKeyRelease reference lives inside a function body (a727c13, check-import-safety green)"
    - "input.reset() clears virtual held-state and the jump callback registry so nothing accumulates across scene re-entries (anti-leak, mirrors challenge.js controller-cancel discipline)"
    - "CONFIG.TOUCH holds all touch tunables (button sizes >=64px effective, positions, opacity) so touchControls.js (37-06) carries zero magic numbers"
  artifacts:
    - "src/config.js — CONFIG.TOUCH { LEFT/RIGHT/JUMP button rects (X/Y/W/H, W&H >=64), OPACITY, ... }"
    - "src/input.js — setLeftHeld/setRightHeld, onJumpPress/onJumpRelease + fireJumpPress/fireJumpRelease, isLeftHeld/isRightHeld, initKeyboardJump, reset()"
    - "src/player.js — movement reads input.isLeftHeld/isRightHeld; jump buffer-set on input.onJumpPress (player.paused guard + jump SFX preserved); variable-height cut on input.onJumpRelease"
  key_links:
    - "keyboard onKeyPress(JUMP_KEYS) -> input.fireJumpPress() -> player.js onJumpPress cb (paused guard + SFX + buffer=BUFFER_MS) -> existing coyote/buffer consume in player.onUpdate"
    - "keyboard onKeyRelease(JUMP_KEYS) -> input.fireJumpRelease() -> player.js onJumpRelease cb (vel.y<0 ? vel.y*=JUMP_CUT) — variable height, no timer"
    - "isKeyDown(left/a) OR leftBtn -> input.isLeftHeld() -> player.vel.x = dir*RUN_SPEED (touch sets leftBtn later via touchControls.js)"
---

<objective>
Build the ONE input seam (src/input.js) that merges keyboard and virtual-button input into the single path player.js already uses, and wire player.js to read it — so the kid-validated coyote/buffer/variable-height jump is reused verbatim by both keyboard and (in 37-06) touch. Keyboard behavior stays byte-identical; the seam only adds an OR for a future finger. Also land CONFIG.TOUCH so the touch UI in 37-06 has its tunables. No touch UI is drawn yet — this is the plumbing that makes touch jump reuse the locked physics instead of re-deriving them.

Purpose: The forbidden anti-pattern is a parallel touch jump implementation. Routing keyboard THROUGH the seam guarantees exactly one jump code path; the desktop parity gate proves the keyboard feel is unchanged.

Output: CONFIG.TOUCH tunables; src/input.js seam; player.js reads the seam. check-safety + check-import-safety + browser-boot (keyboard drive) all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-mobile-responsive-canvas-touch-controls/37-CONTEXT.md
@.planning/phases/37-mobile-responsive-canvas-touch-controls/37-RESEARCH.md
@src/config.js
@src/player.js
@src/audio.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONFIG.TOUCH tunables + src/input.js seam</name>
  <files>src/config.js, src/input.js</files>
  <read_first>
    - src/config.js — the HINT block (~line 508) + AUDIO block for the tunable-shape convention and where to place a new UI block; all touch numbers live here (binding rule: no magic numbers in touchControls.js).
    - src/audio.js lines 1-28 — the a727c13 module-scope discipline precedent: `musicHandle`/`muted` are plain data at module scope; every engine global stays inside function bodies. src/input.js mirrors this EXACTLY (held booleans + callback arrays at module scope; isKeyDown/onKeyPress inside functions only).
    - src/player.js lines 23-24, 68-110 — JUMP_KEYS = ["space","up","w"]; the current isKeyDown(left/a)/(right/d) reads and the onKeyPress/onKeyRelease jump wiring the seam must preserve.
    - 37-RESEARCH.md §"Pattern 1: The unified input seam" (the exact input.js sketch — a727c13-safe) and §Pitfall 3 (no timer for hold-to-jump) and §Pitfall 4 (no engine global at module top level).
  </read_first>
  <action>
    In config.js add a `TOUCH` block: `LEFT`, `RIGHT`, `JUMP` button rects (each `{ X, Y, W, H }` in 640x360 game space; LEFT/RIGHT bottom-left, JUMP bottom-right; W and H sized so the effective hit zone is >=64px — oversized for a 12-year-old's thumbs), plus `OPACITY` (semi-transparent, dark-grunge) and any label/glyph tunables. Comment each with its unit and role. Do NOT touch other config blocks.

    Create src/input.js. Module scope holds ONLY plain data: `let leftBtn = false, rightBtn = false;` and `const jumpPressCbs = [], jumpReleaseCbs = [];` (no engine globals at top level — a727c13). Export: `setLeftHeld(v)`, `setRightHeld(v)` (called by touchControls per finger); `onJumpPress(cb)`, `onJumpRelease(cb)` (register callbacks); `fireJumpPress()`, `fireJumpRelease()` (invoke all registered callbacks); `isLeftHeld()` returning `isKeyDown("left") || isKeyDown("a") || leftBtn`; `isRightHeld()` returning `isKeyDown("right") || isKeyDown("d") || rightBtn` (isKeyDown referenced INSIDE the function only). Add `initKeyboardJump(jumpKeys)` — an init function called once from player.js's makePlayer AFTER kaplay() has run — that registers `onKeyPress(jumpKeys, () => fireJumpPress())` and `onKeyRelease(jumpKeys, () => fireJumpRelease())` so the keyboard drives the SAME edge path as touch. Add `reset()` that sets leftBtn/rightBtn = false and empties both callback arrays (anti-leak: nothing accumulates across scene re-entries). NO timers anywhere.
  </action>
  <verify>
    <automated>bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && node --check src/input.js && node -e "import('./src/config.js').then(m=>{const t=m.CONFIG.TOUCH;if(!t||!t.LEFT||!t.RIGHT||!t.JUMP){console.error('missing CONFIG.TOUCH');process.exit(1)};for(const b of['LEFT','RIGHT','JUMP']){if(!(t[b].W>=64&&t[b].H>=64)){console.error(b+' hit zone <64');process.exit(1)}};console.log('CONFIG.TOUCH ok')})"</automated>
  </verify>
  <done>CONFIG.TOUCH exists with >=64px hit zones for all 3 buttons; src/input.js exports the seam (held OR + jump edge registry + initKeyboardJump + reset); import-safety + safety green; no engine global at module top level.</done>
</task>

<task type="auto">
  <name>Task 2: Wire player.js to read the seam (keyboard byte-identical)</name>
  <files>src/player.js</files>
  <read_first>
    - src/player.js — the WHOLE file: lines 68-91 (onUpdate horizontal run reading isKeyDown + the buffer/coyote consume), lines 100-104 (onKeyPress(JUMP_KEYS) — the player.paused guard + jump SFX + buffer=BUFFER_MS), lines 108-110 (onKeyRelease(JUMP_KEYS) — the vel.y<0 variable-height cut). This is LOCKED game-feel — the change must preserve every one of these behaviors.
    - src/input.js (from Task 1) — the seam API to call.
    - 37-RESEARCH.md §"Pattern 1" the "player.js change is minimal" paragraph + the anti-leak nuance (register jump callbacks per makePlayer, clear via input.reset() on scene leave — the scene-leave reset is wired in 37-06 game.js, this plan just makes reset() available and registers cleanly).
  </read_first>
  <action>
    In makePlayer: (1) Call `input.initKeyboardJump(JUMP_KEYS)` once so the keyboard's press/release edges flow through the seam. (2) In the horizontal-run onUpdate, replace the two `isKeyDown(...)` reads with `if (input.isLeftHeld()) dir -= 1;` and `if (input.isRightHeld()) dir += 1;` — this still ORs the exact same keyboard keys (left/a, right/d) plus the virtual buttons, so keyboard movement is unchanged. (3) Replace the direct `onKeyPress(JUMP_KEYS, ...)` with `input.onJumpPress(() => { ... })` carrying the SAME body byte-for-byte: the `if (player.paused) return;` guard, `audio.playSfx("jump", CONFIG.AUDIO.JUMP_VOLUME)`, and `buffer = CONFIG.BUFFER_MS / 1000`. (4) Replace the direct `onKeyRelease(JUMP_KEYS, ...)` with `input.onJumpRelease(() => { if (player.vel.y < 0) player.vel.y *= CONFIG.JUMP_CUT; })`. Import input.js (`import * as input from "./input.js";`). Do NOT change the buffer/coyote consume math in onUpdate, the anim state machine, JUMP_FORCE, or any tunable. Keep every engine ref inside makePlayer (a727c13). NO timer introduced — variable-height stays press/release + vel.y sign.
  </action>
  <verify>
    <automated>bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && grep -q "input.isLeftHeld()" src/player.js && grep -q "input.onJumpPress" src/player.js && grep -q "input.onJumpRelease" src/player.js && node scripts/browser-boot.mjs 2>&1 | tail -6</automated>
  </verify>
  <done>player.js reads movement + jump through the input seam; keyboard behavior byte-identical (paused guard, jump SFX, buffer, variable-height cut all preserved); browser-boot keyboard drive reaches every goal across all 8 levels; safety + import-safety green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input.js module singleton | Session-only held-state + callback arrays; no persistence, no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-04 | Tampering | a parallel touch jump path re-derives locked physics (feel drift) | high | mitigate | Keyboard routed THROUGH the seam so exactly one jump path exists; browser-boot keyboard-drive parity gate green |
| T-37-05 | Tampering | a stray timer measuring hold-to-jump trips SAFE-01 | medium | mitigate | Variable-height stays press/release + vel.y sign; check-safety green |
| T-37-06 | Tampering | jump callback registry accumulates across scene re-entries (leak) | medium | mitigate | input.reset() empties arrays; wired on onSceneLeave in 37-06 |
| T-37-SC | Tampering | package installs | n/a | accept | Zero new dependencies |
</threat_model>

<verification>
```
bash scripts/check-import-safety.sh
bash scripts/check-safety.sh
node scripts/browser-boot.mjs        # keyboard-drive parity across all 8 levels — feel unchanged
```
</verification>

<success_criteria>
- src/input.js is the single seam: held-OR for left/right + jump press/release edge registry + initKeyboardJump + reset; a727c13-clean.
- CONFIG.TOUCH holds all touch tunables with >=64px hit zones.
- player.js reads the seam; keyboard feel byte-identical (browser-boot keyboard drive green).
- No timers; check-safety + check-import-safety green.
</success_criteria>

<output>
Create `.planning/phases/37-mobile-responsive-canvas-touch-controls/37-03-SUMMARY.md` when done.
</output>
