---
phase: 30-harness-extensions
plan: 03
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - docs/LEVEL-DESIGN.md
autonomous: true
requirements: [MECH-04, MOT-04]

must_haves:
  truths:
    - "docs/LEVEL-DESIGN.md no longer claims the validator/audit deliberately do not check alcoves."
    - "docs/LEVEL-DESIGN.md no longer mentions collectZone as a required/HARD-checked mechanic (removed in Phase 29)."
    - "All 7 existing gate commands (check-gate.sh, check-safety.sh, check-import-safety.sh, check-progress.sh, validate-levels.mjs, browser-boot.mjs, audit-phase21-mechanics.mjs) exit clean with the new Phase 30 checks live — no false HARD-FAILs on shipped content."
  artifacts:
    - docs/LEVEL-DESIGN.md (sections 4 and 6 edited)
  key_links:
    - "docs/LEVEL-DESIGN.md section 6 -> scripts/validate-levels.mjs + scripts/audit-phase21-mechanics.mjs (the doc now correctly points at real, live coverage instead of claiming none exists)"
    - "Full gate suite -> Plans 30-01/30-02's new checks (this plan is the integration proof that both land together without regressing shipped content)"
---

<objective>
Close out Phase 30: fix the two stale passages in `docs/LEVEL-DESIGN.md` that Plans 30-01/30-02 make untrue (the "validator/audit do NOT check alcoves" line, and adjacent stale `collectZone` references left over from Phase 29's removal), then run the FULL existing 7-command gate suite once, live, to prove the phase's fourth success criterion: the new checks add coverage without a single false HARD-FAIL on shipped content.

Purpose: this is the phase's closing integration proof — no player-visible surface, pure documentation + verification.
Output: an accurate LEVEL-DESIGN.md and a genuine, all-green run of every existing project gate with Phase 30's new checks live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-harness-extensions/30-CONTEXT.md
@.claude/CLAUDE.md
</context>

<artifacts_produced>
- `docs/LEVEL-DESIGN.md` section 4 — stale `collectZone` references removed
- `docs/LEVEL-DESIGN.md` section 6 — "validator/audit do NOT check alcoves" line replaced with a pointer to the real, live coverage
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale LEVEL-DESIGN.md passages (sections 4 and 6)</name>
  <files>docs/LEVEL-DESIGN.md</files>
  <read_first>
    docs/LEVEL-DESIGN.md (full file, 76 lines — section 4 "Math barriers" lines 37-44, section 6 "Secret alcove" lines 53-59)
    .planning/phases/30-harness-extensions/30-CONTEXT.md (the "Documentation & Scope Boundaries" decision — do NOT add motion-authoring rules here; that is explicitly Phase 34/LVL-03, not this phase)
  </read_first>
  <action>
    In section 4 "Math barriers" (~line 43): change the HARD rule line from "every door/mathGate/enemy/collectZone must be reachable from spawn per the validator, and the interactive audit must show `triggered: true` for every encounter" to the same sentence with `/collectZone` removed (the mechanic was fully removed in Phase 29; MECH-01).

    Delete the entire following bullet (~line 44, starting "Collect zones: keep ONE per level...") — it describes a zone→slots→choices contract that no longer exists in the codebase (removed in Phase 29's MECH-01/MECH-02).

    In section 6 "Secret alcove" (~line 59): replace the line "The validator/audit deliberately do NOT check alcoves — verify reachability by playing with `?debug=1` (renders them as magenta markers)." with a corrected statement pointing at the real Phase 30 coverage: the validator (`node scripts/validate-levels.mjs`) checks alcove point-reachability via the `secret-alcove-reachability` row, and the interactive audit (`node scripts/audit-phase21-mechanics.mjs`) verifies real discovery via the entity-destroy/XP-delta signal (MECH-04). Keep a mention that `?debug=1` playtesting remains a valid supplementary manual-eyeball step (still renders alcoves as magenta markers) — just no longer the ONLY verification path.

    Do NOT add any new motion-authoring rules (checkpoint-before-every-mover, missed-platform-means-wait-not-death, or any mover-placement guidance) anywhere in this file — that is explicitly out of scope for Phase 30 (LVL-03 is mapped to Phase 34 in REQUIREMENTS.md's traceability table). This task is a narrow accuracy fix only.
  </action>
  <acceptance_criteria>
    - `grep -c "collectZone" docs/LEVEL-DESIGN.md` returns `0`.
    - `grep -c "deliberately do NOT check alcoves" docs/LEVEL-DESIGN.md` returns `0`.
    - `grep -c "validate-levels.mjs" docs/LEVEL-DESIGN.md` returns at least `1` (the new section-6 pointer).
    - `grep -c "checkpoint before every mover\|missed platform" docs/LEVEL-DESIGN.md` returns `0` (confirms no LVL-03 scope crept in).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "collectZone" docs/LEVEL-DESIGN.md; grep -c "deliberately do NOT check alcoves" docs/LEVEL-DESIGN.md; grep -c "validate-levels.mjs" docs/LEVEL-DESIGN.md</automated>
  </verify>
  <done>Sections 4 and 6 accurately reflect the post-Phase-29/post-Phase-30 codebase, with zero stale collectZone references and zero false "unchecked" claims about alcoves.</done>
</task>

<task type="auto">
  <name>Task 2: Full existing gate suite — confirm zero regressions with Phase 30's checks live</name>
  <files>none (verification-only; no files modified by this task)</files>
  <read_first>
    .claude/CLAUDE.md ("Verification gates" section — the exact 7 commands and their meaning)
  </read_first>
  <action>
    Run, in order, every command CLAUDE.md's "Verification gates" section lists, capturing each one's exit code and a brief summary of its output:
    1. `bash scripts/check-gate.sh`
    2. `bash scripts/check-safety.sh`
    3. `bash scripts/check-import-safety.sh`
    4. `bash scripts/check-progress.sh`
    5. `node scripts/validate-levels.mjs` (zero HARD-FAIL required — this now includes Plan 30-01's two new checks live against all 8 real levels)
    6. `node scripts/browser-boot.mjs`
    7. `node scripts/audit-phase21-mechanics.mjs` (this now includes Plan 30-02's alcove detection live — `resolved: false` rows are acceptable ONLY if already documented as known headless-timing flakiness per this project's existing convention; a `secret-alcove` row failing here after Plan 30-02 supposedly proved it would be a genuine regression, not flakiness — investigate rather than wave it through)

    This is the phase's fourth success criterion made concrete: the new checks add coverage WITHOUT introducing a single false HARD-FAIL on shipped content. If any command fails in a way traceable to Plan 30-01's or 30-02's new code (not pre-existing, already-documented flakiness), that is a genuine regression this task must surface, not silently pass over.
  </action>
  <acceptance_criteria>
    - All 7 commands exit 0 (or, for #7, print `AUDIT: ALL MECHANICS RESOLVED` — any `resolved: false` row must be either pre-existing documented flakiness or explicitly investigated and explained in the SUMMARY, never silently ignored).
    - `node scripts/validate-levels.mjs`'s output shows zero `HARD-FAIL` lines across all 8 real levels.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs && node scripts/audit-phase21-mechanics.mjs</automated>
  </verify>
  <done>All 7 gate commands run clean with Phase 30's new checks live; the SUMMARY records each command's result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan only edits static documentation and re-runs pre-existing, already-threat-modeled gate scripts (check-gate.sh/check-safety.sh/check-import-safety.sh/check-progress.sh/browser-boot.mjs predate this phase; validate-levels.mjs/audit-phase21-mechanics.mjs are covered by Plans 30-01/30-02's own threat models). |

This plan introduces no new code, no new trust boundary, and no new attack surface — it is a documentation fix plus a read-only verification pass over existing, already-shipped tooling.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-07 | N/A | `docs/LEVEL-DESIGN.md` | low | accept | Plain markdown documentation, never executed, never a code/data trust boundary. |
</threat_model>

<verification>
1. `grep` checks on docs/LEVEL-DESIGN.md confirm the stale passages are gone and no LVL-03 scope crept in.
2. The full 7-command gate suite runs clean, confirming Phase 30's success criterion 4 (new coverage without false HARD-FAILs).
</verification>

<success_criteria>
- docs/LEVEL-DESIGN.md accurately describes the post-Phase-30 verification coverage.
- All 7 existing gates pass with the new checks live — the phase closes with a genuine, not assumed, all-green run.
</success_criteria>

<output>
Create `.planning/phases/30-harness-extensions/30-03-SUMMARY.md` when done
</output>
