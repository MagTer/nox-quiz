---
phase: 12-polish-adhd-safety-uat
plan: 02
type: execute
wave: 2
depends_on: ["12-00", "12-01"]
files_modified:
  - src/ui/hud.js
  - .planning/phases/12-polish-adhd-safety-uat/12-UAT.md
autonomous: false
requirements: [SAFE-02, SAFE-03]
must_haves:
  truths:
    - "A small, always-visible controls hint '← → move · SPACE jump' is pinned in a screen corner, readable on #0a0a0a (SAFE-02)"
    - "The hint is a fixed(), camera-immune Kaplay text() overlay, tagged 'hud' so it tears down on replay (SAFE-02)"
    - "The full safety audit (scripts/check-safety.sh) now passes green end-to-end, including the SAFE-02 hint positive (SAFE-01 + SAFE-02)"
    - "A kid-UAT checklist (12-UAT.md) exists covering feel, framing, juice-not-over-stimulating, controls discoverable, no time pressure, readable contrast (SAFE-03)"
  artifacts:
    - path: "src/ui/hud.js"
      provides: "persistent fixed() corner controls hint mounted alongside the HUD, replay-safe"
      contains: "SPACE jump"
    - path: ".planning/phases/12-polish-adhd-safety-uat/12-UAT.md"
      provides: "the kid-UAT sign-off checklist for SAFE-03 + overall feel/framing"
      contains: "over-stimulating"
  key_links:
    - from: "src/ui/hud.js"
      to: "screen overlay"
      via: "add([text('← → move · SPACE jump'), pos(CONFIG.HINT.X, CONFIG.HINT.Y), fixed(), z(), 'hud'])"
      pattern: "SPACE jump"
    - from: "scripts/check-safety.sh"
      to: "src/ui/hud.js"
      via: "grep -Rq 'SPACE jump' src/ — the SAFE-02 positive now passes"
      pattern: "SPACE jump"
---

<objective>
Mount the persistent corner controls hint (SAFE-02 — USER OVERRIDE: always visible, NOT a fading start hint) and author the final kid-UAT checklist (12-UAT.md) for the contrast / over-stimulation / feel sign-off (SAFE-03). Mounting the hint also turns the last red positive in scripts/check-safety.sh (the "SPACE jump" assertion) green, so the whole-game ADHD-safety audit passes end-to-end.

Purpose: controls must be discoverable at any time with zero time pressure on the target Windows laptop, so the hint is a small, always-on fixed() overlay in a corner — reusing the HUD's exact fixed()/z()/"hud"-tag idiom (camera-immune, replay-safe). SAFE-03 (readable contrast + effects not over-stimulating) and overall feel/framing are inherently perceptual and NON-automatable — they are validated WITH THE ACTUAL KID, so this plan ships the checklist and ends in a blocking human sign-off (human_verify_mode is end-of-phase, so the kid UAT is surfaced via the phase verification's human-check, not an inline checkpoint task).
Output: src/ui/hud.js gains the persistent hint; 12-UAT.md (the kid checklist); scripts/check-safety.sh passes fully green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-polish-adhd-safety-uat/12-CONTEXT.md
@.planning/phases/12-polish-adhd-safety-uat/12-RESEARCH.md
@.planning/phases/12-polish-adhd-safety-uat/12-PATTERNS.md
@src/ui/hud.js
@src/config.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mount the persistent fixed() corner controls hint (SAFE-02)</name>
  <files>src/ui/hud.js</files>
  <read_first>
    - src/ui/hud.js lines 50-83 (the mountHud factory + the fixed()/z()/"hud"-tag badge/track/fill overlay shape to mirror), line 31 (the CONFIG import), lines 24-29 (the factory + tag anti-leak discipline), lines 14-16 (no-DOM-sink canon).
    - src/config.js CONFIG.HINT (from Plan 00: X 16, Y 330, SIZE 12 — bottom-left, clear of the top-left HUD at X:16/Y:16).
    - 12-PATTERNS.md lines 181-204 (src/ui/hud.js assignment: reuse the badge comp shape, DIFFERENT corner from the HUD, color(0xe8,0xe8,0xe8) readable on #0a0a0a, tag "hud", the tofu-glyph fallback) and 12-RESEARCH.md lines 247-262 (Pattern 5) + Pitfall 6 (lines 362-366) + Assumption A2 (line 438).
  </read_first>
  <action>
    Inside the mountHud factory closure (so it shares the HUD's replay-teardown and one-way discipline), add ONE more fixed() overlay object — the persistent controls hint — using the SAME comp shape as the badge (hud.js:55-62): `text("← → move · SPACE jump", { size: CONFIG.HINT.SIZE })`, `pos(CONFIG.HINT.X, CONFIG.HINT.Y)`, `color(0xe8, 0xe8, 0xe8)` (#e8e8e8 — ~18:1 on #0a0a0a, readable), `fixed()`, `z(9000)`, and the tag `"hud"` (so it tears down with the rest of the HUD on scene replay — anti-leak). Place it at a DIFFERENT corner from the top-left HUD badge/bar (the HINT defaults put it at bottom-left, X:16/Y:330) so it never overlaps the level badge/XP bar. It must be a Kaplay text() canvas object — NO innerHTML/document.* (hud.js no-DOM-sink canon); the string is a static literal (no injection path). Keep mountHud a factory (no module-level singleton). Do NOT change refresh()/flashLevelUp() or add any write into the progress tracker (the one-way contract holds). TOFU FALLBACK (RESEARCH A2 / Pitfall 6): the `← → ·` glyphs render in Kaplay's default font; if the kid UAT shows tofu boxes, fall back to the text "LEFT/RIGHT move · SPACE jump" — but the literal "SPACE jump" substring MUST remain (the audit's SAFE-02 positive greps for it). The hint text is static and persistent — NOT a fading/timed hint (USER OVERRIDE); do NOT tween or schedule it away.
  </action>
  <verify>
    <automated>node --check src/ui/hud.js && grep -q 'SPACE jump' src/ui/hud.js && grep -q 'CONFIG.HINT' src/ui/hud.js && bash scripts/check-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/ui/hud.js` passes.
    - `grep -q 'SPACE jump' src/ui/hud.js` matches (the hint text literal is present).
    - The hint object uses `fixed()`, a `z()`, the `"hud"` tag, `CONFIG.HINT.X`/`CONFIG.HINT.Y`/`CONFIG.HINT.SIZE`, and `color(0xe8, 0xe8, 0xe8)`.
    - The hint is mounted inside the mountHud factory (not a module-level singleton); refresh()/flashLevelUp() unchanged; no progress write-back added (one-way contract intact — check-progress.sh assertion 8 still passes).
    - No innerHTML/document.*/alert added (no-DOM-sink canon).
    - `bash scripts/check-safety.sh` now exits 0 (PASS) end-to-end — the SAFE-02 hint positive is green AND the no-timer/forgiving greps remain green.
  </acceptance_criteria>
  <done>A small, always-visible controls hint "← → move · SPACE jump" is pinned bottom-left as a camera-immune fixed() Kaplay text() overlay, readable on #0a0a0a, tagged "hud" (replay-safe), inside the mountHud factory; the full safety audit passes green end-to-end; node --check clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author 12-UAT.md — the kid-UAT sign-off checklist (SAFE-03 + feel/framing)</name>
  <files>.planning/phases/12-polish-adhd-safety-uat/12-UAT.md</files>
  <read_first>
    - 12-VALIDATION.md lines 56-69 (the Manual-Only Verifications table — the exact behaviors, requirements, and test instructions to turn into checklist items) and lines 64-67 (no-timer, controls-discoverable, contrast, overall-feel rows).
    - 12-CONTEXT.md line 47 (the UAT scope: feel, framing, juice-not-over-stimulating, controls discoverable, no time pressure felt) and lines 83-86 (the bar is the kid's reaction; effects must be satisfying yet calm).
    - 12-PATTERNS.md lines 226-228 (the 12-UAT.md doc spec) and 12-RESEARCH.md Open Q2 (lines 450-453) re: the optional 12-AUDIT.md companion.
  </read_first>
  <action>
    Create .planning/phases/12-polish-adhd-safety-uat/12-UAT.md as a kid-UAT checklist (a doc, NOT code). Structure it as a markdown checklist the USER runs WITH THE ACTUAL KID on the Windows laptop (serve over HTTP — `python3 -m http.server` — since file:// blocks ES modules). Include, as checkable items with one-line "how to test" instructions sourced from 12-VALIDATION.md's Manual-Only table:
    - JUICE-01: jump + land repeatedly — squash/stretch is subtle and brief, dust is a quick puff, NOT bouncy/over-stimulating; the collider still feels fair (jumps land where expected).
    - JUICE-02: collect coins — a quick pop+fade at each coin, not jarring.
    - JUICE-03: clear the gate — a celebratory burst layered on the "LEVEL CLEAR" banner + level-up flash, brief and NON-STROBING (no flicker).
    - SAFE-01: play through — nothing counts down or time-pressures (note the automated audit `bash scripts/check-safety.sh` also confirms this); wrong answers never punish (forgiving re-ask, no progress lost).
    - SAFE-02: the corner controls hint "← → move · SPACE jump" is visible, readable, and the arrow glyphs render (NOT tofu boxes — else fall back to "LEFT/RIGHT move").
    - SAFE-03: text/sprites/HUD read clearly on #0a0a0a; effects are calm/not over-stimulating; the kid is not overwhelmed.
    - Overall feel/framing: "reads like a real game" — she enjoys it, controls are obvious, nothing stressful (the kid-reaction bar).
    Add a short header (phase, what this is, how to launch the dev server, who signs off = the user with the kid) and a final "Sign-off" line. Optionally append a brief AUDIT note recording the `scripts/check-safety.sh` result (the SAFE-01 automated half) so the doc captures both the automated and human halves. Keep it concise and skimmable — this is a checklist, not prose.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-polish-adhd-safety-uat/12-UAT.md && grep -qi 'over-stimulating' .planning/phases/12-polish-adhd-safety-uat/12-UAT.md && grep -q 'SPACE jump' .planning/phases/12-polish-adhd-safety-uat/12-UAT.md && grep -qi 'no.*time.*pressure\|countdown\|forgiving' .planning/phases/12-polish-adhd-safety-uat/12-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 12-UAT.md exists at the phase path.
    - It contains checklist items covering ALL of: JUICE-01, JUICE-02, JUICE-03, SAFE-01 (no time pressure + forgiving), SAFE-02 (hint visible/readable), SAFE-03 (contrast + not over-stimulating), and overall feel/framing.
    - It mentions over-stimulation (`grep -i 'over-stimulating'`), the hint text ("SPACE jump"), and the no-time-pressure/forgiving mandate.
    - It documents the HTTP-serve launch path and names the user (with the kid) as the sign-off authority.
    - It is a checklist doc (no code); items have one-line how-to-test instructions.
  </acceptance_criteria>
  <done>12-UAT.md is the concise kid-UAT checklist covering juice subtlety, controls discoverability, no time pressure / forgiving, readable contrast / not over-stimulating, and overall feel — with launch instructions and a user-with-kid sign-off line, ready for the phase verification's human-check.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full-suite safety + syntax gate (SAFE-01 + SAFE-02 end-to-end)</name>
  <files>src/ui/hud.js</files>
  <read_first>
    - scripts/check-safety.sh (authored in Plan 00) — confirm it exits 0 now that the hint exists.
    - scripts/check-progress.sh — the Phase 11 gate must still pass (the one-way HUD contract is intact after adding the static hint).
  </read_first>
  <action>
    Run the full structural suite as the phase's automated gate and confirm green: `bash scripts/check-safety.sh` (must PASS — no-timer + forgiving greps clean across all of src/ with comments stripped, the SAFE-02 "SPACE jump" positive green, the fx.js onEnd positive green) AND `bash scripts/check-progress.sh` (the Phase 11 gate must still PASS — adding a read-only static hint does not break the one-way HUD contract or the firewall) AND `node --check` on every changed src module (src/fx.js, src/player.js, src/scenes/game.js, src/ui/hud.js, src/config.js). This task makes no code change beyond what Task 1 already did; it is the explicit per-phase merge gate. If check-progress.sh fails on the one-way contract, the hint introduced a write-back — remove any progress mutation (the hint must be read-only/static).
  </action>
  <verify>
    <automated>bash scripts/check-safety.sh && bash scripts/check-progress.sh && node --check src/fx.js && node --check src/player.js && node --check src/scenes/game.js && node --check src/ui/hud.js && node --check src/config.js</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-safety.sh` exits 0 (prints "safety checks: PASS").
    - `bash scripts/check-progress.sh` exits 0 (prints "progress checks: PASS") — the Phase 11 contracts survive the hint.
    - `node --check` passes on all five changed modules.
    - No new timer/scheduler or DOM sink anywhere (the audit confirms it).
  </acceptance_criteria>
  <done>The whole-game ADHD-safety audit and the Phase 11 progression gate both pass green, and every changed module syntax-checks — the phase's automated half is fully green; only the kid-UAT human sign-off remains (surfaced via phase verification).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hint string → canvas render | the hint is a static literal rendered via Kaplay text() — no user input, no DOM sink |
| hint object → scene replay | the hint must be tagged "hud" and torn down on replay (no leak/stacking) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | DOM/innerHTML injection via the hint string | mitigate | The hint is a static literal rendered as a Kaplay text() canvas object — no innerHTML/document./alert (mirrors GATE-01/hud.js no-DOM-sink canon). |
| T-12-07 | DoS (resource leak) | hint surviving scene replay | mitigate | Mounted inside the mountHud factory and tagged "hud" → destroyed on scene teardown like every HUD object; no module-level singleton. |
| T-12-08 | Information disclosure / over-stimulation (ADHD-safety) | effects too strong / strobing / low contrast | mitigate | SAFE-03 is a kid-UAT human sign-off (12-UAT.md); all effects are one smooth fade, small magnitudes, #e8e8e8/#00ff88 on #0a0a0a readable; back down CONFIG.FX with the kid if "too much". |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | N/A — no packages installed this phase. |
</threat_model>

<verification>
- `bash scripts/check-safety.sh` PASS end-to-end (SAFE-01 no-timer/forgiving + SAFE-02 hint positive + fx onEnd).
- `bash scripts/check-progress.sh` PASS (Phase 11 one-way HUD contract + firewall intact).
- `node --check` clean on all changed modules.
- 12-UAT.md exists and covers JUICE-01/02/03 + SAFE-01/02/03 + overall feel.
- Manual (kid-UAT, BLOCKING human sign-off via phase verification): hint visible/readable, glyphs render, effects calm/non-strobing, contrast readable, no time pressure felt, she enjoys it.
</verification>

<success_criteria>
- SAFE-02: a persistent, readable, camera-immune corner controls hint, replay-safe.
- SAFE-03: the kid-UAT checklist exists; contrast + over-stimulation are signed off with the actual kid.
- The full ADHD-safety audit passes green; the phase is ready for `/gsd-verify-work` with only the human kid sign-off outstanding.
</success_criteria>

<output>
Create `.planning/phases/12-polish-adhd-safety-uat/12-02-SUMMARY.md` when done.
</output>
