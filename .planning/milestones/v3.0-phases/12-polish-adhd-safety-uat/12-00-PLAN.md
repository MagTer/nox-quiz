---
phase: 12-polish-adhd-safety-uat
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/check-safety.sh
  - src/config.js
autonomous: true
requirements: [SAFE-01]
must_haves:
  truths:
    - "scripts/check-safety.sh exits 0 against the current tree (modulo the SAFE-02 hint that Plan 02 adds) and does NOT false-positive on existing comments NOR on src/progress.js's `xp -= threshold` carry-over math"
    - "The audit fails loudly if any timer/scheduler (setTimeout/setInterval/wait(/loop(/lifespan()/countdown) appears in src/ code"
    - "The audit fails loudly if any punishment-specific construct (gameOver/loseLife/lives--/subtractXp/deductXp/xpLoss/penaliz) appears in src/ code — WITHOUT matching the legitimate level-up surplus carry-over"
    - "CONFIG.FX and CONFIG.HINT namespaces exist so later juice/hint tasks reference no magic numbers"
  artifacts:
    - path: "scripts/check-safety.sh"
      provides: "SAFE-01 no-timer + forgiving structural audit (comment-stripped greps + positive hint/fx checks)"
      contains: "strip_comments"
    - path: "src/config.js"
      provides: "CONFIG.FX and CONFIG.HINT tuning namespaces"
      contains: "FX:"
  key_links:
    - from: "scripts/check-safety.sh"
      to: "src/"
      via: "find src -name '*.js' | strip_comments | grep -E banned-tokens"
      pattern: "find .*src.*-name"
---

<objective>
Author the SAFE-01 ADHD-safety audit gate (scripts/check-safety.sh) and add the CONFIG.FX / CONFIG.HINT tuning namespaces, BEFORE any juice or hint code exists. This is Wave 0: it stands up the one repeatable automated gate for the whole-game no-timer / forgiving mandate, and the config keys every later task reads — so later waves' <automated> verification can invoke the audit and reference real CONFIG keys instead of magic numbers (avoiding the Phase-10/11 "verify references a not-yet-authored target" ordering warning).

Purpose: SAFE-01 demands a DOCUMENTED, repeatable audit across the entire src/ tree, not a one-file grep. The critical nuance (RESEARCH Pitfall 3 / 12-PATTERNS.md): banned words already appear legitimately in EXISTING code comments (hud.js:29, game.js:101/191-192, player.js:7), so the audit MUST strip comments before grepping or it false-positives on prose. This plan also pre-creates the FX/HINT config keys so the firewall "no magic numbers" rule holds for the juice and hint plans.
Output: scripts/check-safety.sh (executable, PASS on current clean src/), CONFIG.FX + CONFIG.HINT namespaces appended to src/config.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-polish-adhd-safety-uat/12-RESEARCH.md
@.planning/phases/12-polish-adhd-safety-uat/12-PATTERNS.md
@.planning/phases/12-polish-adhd-safety-uat/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CONFIG.FX and CONFIG.HINT tuning namespaces</name>
  <files>src/config.js</files>
  <read_first>
    - src/config.js lines 88-101 — the existing CONFIG.HUD namespace + the closing `}` of the CONFIG literal; mirror this nested-object-literal-with-unit-comments style.
    - 12-PATTERNS.md lines 207-223 (src/config.js assignment) and 12-RESEARCH.md lines 410-422 ("CONFIG namespaces") — the exact starting values.
  </read_first>
  <action>
    Append two new namespaces inside the CONFIG object literal, after the HUD namespace (before the closing brace at line 101), matching the existing `HUD:`/`GATE:` nested-object style with `// px` / `// ms` / `// unitless` unit comments and a one-line "Phase 12 juice tuning — retuned WITH THE KID in UAT (SAFE-03)" header comment. Use the starting values from 12-RESEARCH.md: FX = { SQUASH_X 1.15, SQUASH_Y 0.85, SQUASH_MS 140, STRETCH_X 0.9, STRETCH_Y 1.1, STRETCH_MS 120, DUST_COUNT 4, DUST_SIZE 3, DUST_SPREAD 8, DUST_RISE 16, DUST_MS 300, POP_SCALE 1.5, POP_MS 220, BURST_MS 400 }; HINT = { X 16, Y 330, SIZE 12 } (bottom-left corner, clear of the top-left HUD at X:16/Y:16). Note in a comment that BURST_MS stays at or below the existing HUD.FLASH_MS (450) feel and all magnitudes are deliberately subtle/brief/non-strobing. Do NOT touch any existing CONFIG field; config.js imports nothing — keep it a leaf module.
  </action>
  <verify>
    <automated>node --check src/config.js && node -e "import('./src/config.js').then(m=>{const f=m.CONFIG.FX,h=m.CONFIG.HINT;if(!f||!h)process.exit(1);for(const k of ['SQUASH_X','SQUASH_Y','SQUASH_MS','DUST_COUNT','DUST_MS','POP_SCALE','POP_MS','BURST_MS'])if(typeof f[k]!=='number')throw new Error('FX.'+k);for(const k of ['X','Y','SIZE'])if(typeof h[k]!=='number')throw new Error('HINT.'+k);})"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/config.js` passes.
    - `CONFIG.FX` exists with numeric SQUASH_X/SQUASH_Y/SQUASH_MS/DUST_COUNT/DUST_MS/POP_SCALE/POP_MS/BURST_MS keys.
    - `CONFIG.HINT` exists with numeric X/Y/SIZE keys.
    - No existing CONFIG field is removed or changed (git diff shows only additions inside the literal).
    - config.js still imports nothing.
  </acceptance_criteria>
  <done>CONFIG.FX and CONFIG.HINT namespaces exist with the subtle/brief starting values; node --check clean; no magic numbers introduced for the later juice/hint plans.</done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/check-safety.sh — the SAFE-01 comment-stripped audit gate</name>
  <files>scripts/check-safety.sh</files>
  <read_first>
    - scripts/check-progress.sh lines 1-36 (header + `set -euo pipefail` + `ROOT=$(git rev-parse --show-toplevel)` + `fail()` + the node --check loop) — COPY this scaffold structure verbatim in spirit.
    - .planning/phases/10-math-gate-integration-port-the-brain/scripts/check-gate.sh lines 57-60 — the negative no-timer grep pattern to extend.
    - 12-PATTERNS.md lines 66-110 (scripts/check-safety.sh assignment, esp. the strip_comments pre-pass at 96-101 and the banned-tokens-only-in-patterns discipline at 110) and 12-RESEARCH.md lines 264-304 (Pattern 6 full reference script).
  </read_first>
  <action>
    Create scripts/check-safety.sh mirroring scripts/check-progress.sh structure: `#!/usr/bin/env bash`, a header comment block explaining it is the SAFE-01 no-timer + forgiving audit and that banned tokens appear ONLY inside grep patterns (never as plain prose, so the script does not match itself), `set -euo pipefail`, `ROOT="$(git rev-parse --show-toplevel)"`, and a `fail()` that prints `safety checks: FAIL — $1` to stderr and exits 1. Define `strip_comments() { sed -E 's://.*$::' "$1"; }` (line-comment strip — the codebase uses `//` line comments almost exclusively, RESEARCH A4). Then implement, each iterating `find "$ROOT/src" -name '*.js'`:
    (0) Syntax gate: `node --check "$f"` each src module, fail with the filename on error.
    (1) NEGATIVE no-timer (SAFE-01): on the comment-stripped view, fail if `grep -Eq 'setTimeout|setInterval|countdown|[^a-zA-Z]wait\(|[^a-zA-Z]loop\(|lifespan\('` matches — message names the file and "(SAFE-01 no-timer mandate)".
    (2) NEGATIVE forgiving: on the stripped view, fail if `grep -Eiq 'gameOver|game_over|loseLife|lives--|subtractXp|deductXp|xpLoss|penaliz'` matches — message names the file and "(forgiving mandate)". CRITICAL: use ONLY these punishment-specific tokens. Do NOT include a broad `xp[[:space:]]*-=` alternative — that false-positives on the LEGITIMATE, firewalled level-up surplus carry-over at src/progress.js:120 (`xp -= threshold(level);` — carry surplus over, the OPPOSITE of an XP-loss penalty), which would hard-fail the audit on the current clean tree under `set -euo pipefail` and block every downstream gate. progress.js must NOT be edited; the pattern must simply not match its carry-over math.
    (3) POSITIVE (SAFE-02 cross-check, becomes green once Plan 02 lands): `grep -Rq 'SPACE jump' "$ROOT/src"` else fail "missing persistent controls hint (SAFE-02)". Guard so this is the LAST kind of check OR clearly comment that it goes red until the Wave-2 hint exists (a real gate, not a no-op — mirror check-progress.sh's "EXPECTED until later waves land" note).
    (4) POSITIVE (fx self-cleans): if `src/fx.js` exists, `grep -q 'onEnd' "$ROOT/src/fx.js"` else fail "fx.js effects must self-clean via tween().onEnd (no timer)"; if it does not yet exist, skip with a comment (Plan 01 creates it).
    End with `echo "safety checks: PASS"`. CRITICAL: the comment-strip MUST run before EVERY token grep (the existing comments at hud.js:29 / game.js:101,191-192 / player.js:7 contain banned words and would false-positive a naive grep). Keep all banned tokens strictly inside the grep PATTERN strings — never in a prose comment in this script. `chmod +x scripts/check-safety.sh`.
  </action>
  <verify>
    <automated>chmod +x scripts/check-safety.sh && bash -n scripts/check-safety.sh && grep -q 'strip_comments' scripts/check-safety.sh && grep -q "git rev-parse --show-toplevel" scripts/check-safety.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/check-safety.sh` parses with no syntax error.
    - The script contains a `strip_comments` function applied before each token grep (verify: `grep -c 'strip_comments' scripts/check-safety.sh` >= 2 — the definition plus at least one use).
    - Running `bash scripts/check-safety.sh` against the CURRENT tree does NOT fail on the existing comments at hud.js:29 / game.js:101 / game.js:191-192 / player.js:7 (the no-timer and forgiving greps pass because comments are stripped), and does NOT false-positive on src/progress.js:120 `xp -= threshold(level);` (the legitimate carry-over — the forgiving pattern uses only punishment-specific tokens, no broad `xp -=`). Verify the carry-over specifically: `sed -E 's://.*$::' src/progress.js | grep -Eiq 'gameOver|game_over|loseLife|lives--|subtractXp|deductXp|xpLoss|penaliz'` returns NO match (exit 1). The audit's ONLY allowed failure on the current tree is the positive SAFE-02 hint check (expected red until Plan 02) — correct gate behavior, not a false-positive.
    - The banned token literals (setTimeout, setInterval, game-over, etc.) appear in this script ONLY inside grep pattern strings, never as plain prose (verify by reading: no banned word in a `#` comment line).
    - The script resolves ROOT via `git rev-parse --show-toplevel` so it runs from any cwd.
  </acceptance_criteria>
  <done>scripts/check-safety.sh exists, is executable, strips comments before every negative grep (no false-positive on existing comments), bans setTimeout/setInterval/wait(/loop(/lifespan()/countdown and the forgiving punishment constructs across all of src/, and asserts the persistent hint + fx self-clean positives. It is a real fail-fast gate, red only where a later wave's artifact is not yet present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repo | New bash audit script + config edit; no runtime/user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | check-safety.sh self-match | mitigate | Banned tokens live ONLY inside grep pattern strings, never as prose, so the script never matches itself; comment-strip pre-pass prevents false-positives on legitimate code comments (RESEARCH Pitfall 3). |
| T-12-02 | DoS (self-inflicted) | a timer/scheduler sneaking into future src code | mitigate | The negative no-timer grep across all of src/ is this plan's entire purpose — it is the repeatable gate that catches a regression on every commit. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | N/A — this phase installs NO packages (Kaplay vendored; script is bash; config is in-repo). RESEARCH Package Legitimacy Audit = N/A. |
</threat_model>

<verification>
- `node --check src/config.js` clean; CONFIG.FX + CONFIG.HINT present with numeric keys.
- `bash -n scripts/check-safety.sh` clean; `strip_comments` present and used.
- `bash scripts/check-safety.sh` does not false-positive on the existing src comments (no-timer + forgiving greps green); it may legitimately go red on the SAFE-02 hint positive until Plan 02 ships.
</verification>

<success_criteria>
- The SAFE-01 audit gate exists, is executable, and is comment-strip-safe against the current tree.
- CONFIG.FX / CONFIG.HINT exist so Plans 01 and 02 introduce no magic numbers.
- Later waves can invoke `bash scripts/check-safety.sh` as their <automated> safety verification.
</success_criteria>

<output>
Create `.planning/phases/12-polish-adhd-safety-uat/12-00-SUMMARY.md` when done.
</output>
