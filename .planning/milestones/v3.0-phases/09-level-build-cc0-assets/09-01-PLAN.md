---
phase: 09-level-build-cc0-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/tiles/ground.png
  - assets/spike.png
  - assets/goal.png
  - assets/player.png
  - assets/coin.png
  - assets/LICENSES/
  - CREDITS.md
autonomous: false
requirements: [LEVEL-08]
must_haves:
  truths:
    - "Every shipped CC0 image file has a documented source URL and a verified CC0 license proof"
    - "CREDITS.md lists every asset's name, author, source URL, license, and usage"
    - "No vendor logo / brand art ships in the asset subset"
  artifacts:
    - path: "assets/tiles/ground.png"
      provides: "Dark/grunge floor + platform tile(s)"
    - path: "assets/spike.png"
      provides: "Static spike hazard sprite"
    - path: "assets/goal.png"
      provides: "Goal-flag sprite"
    - path: "assets/player.png"
      provides: "Player character sprite"
    - path: "assets/coin.png"
      provides: "Gridded spinning-coin spritesheet"
    - path: "CREDITS.md"
      provides: "Per-asset source + verified CC0 license record"
      contains: "CC0"
    - path: "assets/LICENSES/"
      provides: "Per-asset CC0 license proof files (source URL + quoted license statement)"
  key_links:
    - from: "CREDITS.md"
      to: "assets/LICENSES/"
      via: "each CREDITS row references a proof file capturing the source page CC0 statement"
      pattern: "CC0"
---

<objective>
Source ONE verified-CC0 dark/grunge pixel-art pack (plus a CC0 coin) and vendor the
required PNGs into `assets/`, then document every asset's source + CC0 license proof
(LEVEL-08). This is the gating dependency for all visual work — no sprite loads,
no level renders, until these files exist and are license-verified.

Purpose: Satisfy LEVEL-08 (CC0 sources + licenses documented, no vendor logos) and
retire the highest-value RESEARCH assumptions (A1/A2 — final pack pick + license proof)
before any level/sprite code is written.

Output: vendored `assets/tiles/ground.png`, `assets/spike.png`, `assets/goal.png`,
`assets/player.png`, `assets/coin.png`; `assets/LICENSES/*` proof files; root `CREDITS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-level-build-cc0-assets/09-RESEARCH.md
@.planning/phases/09-level-build-cc0-assets/09-CONTEXT.md
@.planning/phases/09-level-build-cc0-assets/09-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Source + vendor verified-CC0 dark/grunge assets (license gate)</name>
  <files>assets/tiles/ground.png, assets/spike.png, assets/goal.png, assets/player.png, assets/coin.png, assets/LICENSES/</files>
  <read_first>
    - .planning/phases/09-level-build-cc0-assets/09-RESEARCH.md — "CC0 Asset Sourcing" table + "Verification protocol for LEVEL-08" (5 steps) + Assumptions Log A1/A2 + Pitfall 6 (mixed-license risk)
    - .planning/phases/09-level-build-cc0-assets/09-PATTERNS.md — "No Analog Found" section (CREDITS.md + assets/LICENSES/*.txt format)
    - assets/ (currently holds only .gitkeep — confirm the target dir)
    - docker/Dockerfile (lines copying assets/ — confirm vendored files ship automatically; NO Dockerfile change needed)
  </read_first>
  <action>
    Source ONE verified-CC0 dark/grunge pixel pack (lead: 0x72 "16x16 DungeonTileset II" —
    CC0, dark dungeon, spike traps + character) plus a CC0 coin spritesheet (the 0x72 pack
    has no coin; e.g. OpenGameArt spinning-coin). Extract ONLY the needed PNGs into
    `assets/`: `tiles/ground.png`, `spike.png`, `goal.png`, `player.png`, and an
    evenly-gridded `coin.png` strip. Strip any vendor/brand logo art. For EACH asset, follow
    the RESEARCH 5-step LEVEL-08 protocol: open the asset's OWN license page, confirm it reads
    CC0 / Creative Commons Zero / Public Domain (NOT merely "free" or CC-BY), and capture the
    proof (source URL + quoted license statement) into `assets/LICENSES/<asset>.txt`. Then
    STOP for the blocking human-verify checkpoint below — this retires the ASSUMED pack pick
    (A1/A2) before any visual code is written. Fallbacks if the lead pack reads poorly at
    16x16 against `#0a0a0a`: Kenney 1-Bit Pack (CC0) or VEXED Retro Lines (CC0); reject Kenney
    Pixel Platformer (bright/colorful — wrong aesthetic).
  </action>
  <verify>
    <automated>ls -l assets/tiles/ground.png assets/spike.png assets/goal.png assets/player.png assets/coin.png &amp;&amp; ls assets/LICENSES/ &amp;&amp; grep -ril cc0 assets/LICENSES/</automated>
  </verify>
  <done>The five CC0 PNGs are vendored under assets/, each with a license proof in assets/LICENSES/, no vendor logos — confirmed at the blocking human-verify checkpoint.</done>
  <what-built>
    Sourced and vendored the CC0 art into `assets/`: lead candidate 0x72 "16x16
    DungeonTileset II" (CC0, dark dungeon, includes spike traps + a usable character) for
    tiles/spike/goal/player, plus a CC0 coin spritesheet (e.g. OpenGameArt spinning-coin,
    since the 0x72 pack has no coin). Extracted ONLY the needed PNGs:
    `assets/tiles/ground.png`, `assets/spike.png`, `assets/goal.png`, `assets/player.png`,
    `assets/coin.png` (an evenly-gridded horizontal coin spritesheet for sliceX framing).
    Stripped any vendor/brand logo art from the shipped subset.

    For EACH asset, followed the RESEARCH 5-step LEVEL-08 protocol: opened the asset's OWN
    license page, confirmed the line reads CC0 / Creative Commons Zero / Public Domain
    (NOT merely "free" or CC-BY), and captured the proof (source URL + quoted license
    statement) into `assets/LICENSES/<asset>.txt`. Fallbacks if the lead pack reads poorly
    at 16x16 against `#0a0a0a`: Kenney 1-Bit Pack (CC0) or VEXED Retro Lines (CC0) — both
    CC0, dark-capable. Rejected Kenney Pixel Platformer (bright/colorful — wrong aesthetic).
  </what-built>
  <how-to-verify>
    1. Confirm the five PNGs exist and are non-empty:
       `ls -l assets/tiles/ground.png assets/spike.png assets/goal.png assets/player.png assets/coin.png`
    2. Confirm one proof file per asset exists under `assets/LICENSES/` and each names a
       source URL + a CC0/public-domain license statement:
       `ls assets/LICENSES/ && grep -ril cc0 assets/LICENSES/`
    3. Open each PNG (or the source pages) and confirm the art is dark/grunge, readable,
       has NO pink, and contains NO vendor/brand logo.
    4. Confirm `coin.png` is an evenly-gridded horizontal strip (frames in a row) suitable
       for `loadSprite(..., { sliceX: N })`.
    Approve only if every asset's CC0 license is verified at its own source page and no
    logo art ships. ASSUMED pack pick (A1/A2) is being confirmed here.
  </how-to-verify>
  <acceptance_criteria>
    - Files exist and are non-empty: `assets/tiles/ground.png`, `assets/spike.png`, `assets/goal.png`, `assets/player.png`, `assets/coin.png`.
    - `assets/LICENSES/` contains one proof file per asset; each proof file contains the source URL and a CC0 / Creative Commons Zero / Public Domain statement.
    - No shipped PNG contains a vendor logo or brand art.
    - `coin.png` is an evenly-gridded horizontal spritesheet (uniform frame width).
  </acceptance_criteria>
  <resume-signal>Type "approved" once every asset's CC0 license is verified at its source and no logos ship, or describe what to re-source.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author CREDITS.md (per-asset source + verified CC0 license)</name>
  <files>CREDITS.md</files>
  <read_first>
    - .planning/phases/09-level-build-cc0-assets/09-RESEARCH.md — "Verification protocol for LEVEL-08" steps 3 + 5 (CREDITS row contents; CC0 attribution optional but author listed)
    - .planning/phases/09-level-build-cc0-assets/09-PATTERNS.md — "No Analog Found" (CREDITS.md is a new doc, one row per asset: name, author, source URL, CC0 license, usage)
    - assets/LICENSES/ — the proof files written in Task 1 (CREDITS rows must match these sources)
  </read_first>
  <action>
    Create `CREDITS.md` at the repo root satisfying LEVEL-08. Include one row/entry per
    vendored asset (the five PNGs from Task 1) with: asset name, author, source URL,
    license (CC0), and what it is used for in the level. Each entry's source URL MUST match
    the corresponding `assets/LICENSES/<asset>.txt` proof captured in Task 1. State the
    no-vendor-logo confirmation. Keep it plain Markdown, dark-grunge project voice; CC0
    attribution is optional but listing authors is good practice and costs nothing.
  </action>
  <verify>
    <automated>test -f CREDITS.md &amp;&amp; grep -qi cc0 CREDITS.md &amp;&amp; grep -c 'http' CREDITS.md</automated>
  </verify>
  <acceptance_criteria>
    - `CREDITS.md` exists at the repo root.
    - It contains a CC0 license reference and a source URL for every vendored asset.
    - Every source URL in CREDITS.md has a matching proof file in `assets/LICENSES/`.
    - It records that no vendor logos ship.
  </acceptance_criteria>
  <done>CREDITS.md lists each of the five assets with author, source URL, CC0 license, and usage; sources cross-match assets/LICENSES/ proofs; LEVEL-08 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external asset source → repo | Third-party CC0 image files are vendored into the repo and shipped to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Repudiation | Vendored CC0 art (license provenance) | mitigate | Per-asset CC0 verification at the source page + proof captured in `assets/LICENSES/` + `CREDITS.md` row (LEVEL-08); blocking-human checkpoint gates the pack pick |
| T-09-02 | Information disclosure | Shipped art subset | mitigate | Strip any vendor/brand logo from the shipped PNGs; human-verify confirms no logos |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan — only static CC0 image files are vendored (RESEARCH Package Legitimacy Audit: zero packages). Asset legitimacy handled by T-09-01. |
</threat_model>

<verification>
- The five PNGs exist under `assets/` and are non-empty.
- `assets/LICENSES/` holds one CC0 proof (source URL + license statement) per asset.
- `CREDITS.md` lists every asset's source + CC0 license, cross-matching the proofs.
- No vendor logos in the shipped subset.
- No Dockerfile change needed (assets/ already copied into the image).
</verification>

<success_criteria>
LEVEL-08 satisfied: every shipped asset is verified CC0 with documented source + proof,
recorded in CREDITS.md, with no vendor logos. The five sprites are vendored and ready for
`loadSprite` in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/09-level-build-cc0-assets/09-01-SUMMARY.md` when done
</output>
