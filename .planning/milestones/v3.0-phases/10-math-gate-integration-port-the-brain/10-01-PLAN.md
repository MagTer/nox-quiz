---
phase: 10-math-gate-integration-port-the-brain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.js
  - src/math/brain.js
autonomous: true
requirements: [GATE-02, GATE-06]
tags: [math, brain, port, weighted-selection, firewall]

must_haves:
  truths:
    - "Calling createBrain().nextQuestion() returns a question with 4 choices, one of which is the correct answer"
    - "Over many calls the chosen table is biased toward 6–9 (>50% hard)"
    - "The brain module imports nothing from Kaplay and runs headlessly in plain node"
    - "Each createBrain() call yields a fresh, independent accuracy/history closure (no shared module-level state)"
  artifacts:
    - path: "src/math/brain.js"
      provides: "Pure weighted question brain — createBrain() factory exposing nextQuestion + reportResult"
      exports: ["createBrain"]
      contains: "export function createBrain"
      min_lines: 60
    - path: "src/config.js"
      provides: "CONFIG.BRAIN namespace (EWMA/tables) + CONFIG.GATE namespace (panel/dim)"
      contains: "BRAIN"
  key_links:
    - from: "src/math/brain.js"
      to: "src/config.js"
      via: "import { CONFIG } from '../config.js' — reads CONFIG.BRAIN.* tuning constants only"
      pattern: "CONFIG\\.BRAIN\\."
---

<objective>
Port the framework-agnostic math brain (6–9-weighted question selection + in-memory EWMA accuracy weighting) from `archive/math-lab.html` into a NEW pure ES module `src/math/brain.js` with ZERO Kaplay imports (GATE-06 firewall). Add the ported tuning constants to `src/config.js` under a `BRAIN` namespace and the gate-UI panel constants under a `GATE` namespace (so Plan 02 can consume them).

Purpose: This is the keystone's brain — the validated, tuned weighted selector that biases drilling toward the 6–9 tables (GATE-02). It must be a clean, engine-agnostic, headlessly-testable module so the firewall (GATE-06) is provable in plain node and survives game replays without leaking state.

Output: `src/math/brain.js` (new pure module) and `src/config.js` (BRAIN + GATE namespaces added).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-math-gate-integration-port-the-brain/10-CONTEXT.md
@.planning/phases/10-math-gate-integration-port-the-brain/10-RESEARCH.md
@.planning/phases/10-math-gate-integration-port-the-brain/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CONFIG.BRAIN and CONFIG.GATE namespaces to src/config.js</name>
  <files>src/config.js</files>
  <read_first>
    - `src/config.js` (full file, 1-46) — the existing flat `export const CONFIG = { ... }` object with grouped `// ---` section comments and inline-commented constants. Match that exact comment style.
    - `archive/math-lab.html` lines 604-619 — the source CONFIG values to port verbatim (ACCURACY_ALPHA 0.15, MASTERY_THRESHOLD 0.80, STRUGGLE_THRESHOLD 0.60, STRUGGLE_BOOST 1.5, MASTERY_WINDOW 10, HARD_TABLES [6,7,8,9], EASY_TABLES [1,2,3,4,5]).
    - `archive/math-lab.html` line 622 (`CONFIG.DUNGEON = {...}`) — shows the nested-namespace precedent; here we add `BRAIN:` and `GATE:` as nested keys INSIDE the CONFIG literal instead.
    - `.planning/phases/10-.../10-PATTERNS.md` lines 134-156 — the exact BRAIN/GATE block to add and the DROP list.
  </read_first>
  <action>
    Add two new nested namespaces inside the existing `CONFIG` object literal in `src/config.js`, after the existing `GOAL_SIZE` line and before the closing brace, following the file's `// ---` section-comment style.

    Add `BRAIN:` with exactly these keys ported verbatim from archive lines 604-619: ACCURACY_ALPHA 0.15, MASTERY_THRESHOLD 0.80, STRUGGLE_THRESHOLD 0.60, STRUGGLE_BOOST 1.5, MASTERY_WINDOW 10, HARD_TABLES [6, 7, 8, 9], EASY_TABLES [1, 2, 3, 4, 5]. The HARD_TABLES/EASY_TABLES values and the EWMA constants MUST match the archive exactly — the selection math is locked and out of scope to re-tune.

    Add `GATE:` with panel/dim UI constants for Plan 02 to consume: DIM_OPACITY 0.6, PANEL_W 420, PANEL_H 220.

    DROP (do NOT add) all XP/dungeon constants: XP_EASY, XP_HARD, BASE_XP, LEVEL_MULT, ADVANCE_DELAY_MS, SEGMENT_COUNT, SAVE_KEY, and the entire CONFIG.DUNGEON block — those are Phase 11 / never.

    Use a section comment that names the archive source line range and states "DO NOT re-tune" so the locked-math intent is preserved. Do not use the literal token K-A-P-L-A-Y in any added comment text in this file (irrelevant here, but keep comments about the math source only).
  </action>
  <verify>
    <automated>node --check src/config.js && node -e "import('./src/config.js').then(m=>{const b=m.CONFIG.BRAIN,g=m.CONFIG.GATE;const ok=b.ACCURACY_ALPHA===0.15&&b.MASTERY_WINDOW===10&&JSON.stringify(b.HARD_TABLES)==='[6,7,8,9]'&&JSON.stringify(b.EASY_TABLES)==='[1,2,3,4,5]'&&g.PANEL_W===420&&g.DIM_OPACITY===0.6;if(!ok)process.exit(1);console.log('config ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/config.js` passes.
    - `CONFIG.BRAIN.HARD_TABLES` deep-equals `[6,7,8,9]` and `CONFIG.BRAIN.EASY_TABLES` deep-equals `[1,2,3,4,5]`.
    - `CONFIG.BRAIN.ACCURACY_ALPHA===0.15`, `STRUGGLE_THRESHOLD===0.60`, `STRUGGLE_BOOST===1.5`, `MASTERY_THRESHOLD===0.80`, `MASTERY_WINDOW===10`.
    - `CONFIG.GATE.DIM_OPACITY===0.6`, `CONFIG.GATE.PANEL_W===420`, `CONFIG.GATE.PANEL_H===220`.
    - `grep -Eq 'XP_EASY|XP_HARD|BASE_XP|LEVEL_MULT|SAVE_KEY|DUNGEON' src/config.js` finds NOTHING (firewall — no XP/dungeon constants leaked in).
  </acceptance_criteria>
  <done>config.js exposes CONFIG.BRAIN (ported EWMA/table constants, verbatim) and CONFIG.GATE (panel/dim) namespaces; no XP/dungeon constants present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port the pure weighted question brain into src/math/brain.js</name>
  <files>src/math/brain.js</files>
  <behavior>
    - createBrain() returns an object with nextQuestion() and reportResult(table, isCorrect).
    - nextQuestion() returns { a, b, answer, choices } where a=table (1..9), b=multiplicand (1..10), answer=a*b, choices is a 4-element array containing answer plus 3 distinct plausible distractors, already shuffled.
    - answer is always present in choices; all 4 choices are distinct positive integers.
    - Over 1000 calls on a fresh brain, >50% of returned `a` values are in [6,7,8,9] (6–9 bias).
    - reportResult(table, true) repeatedly raises that table's internal accuracy (EWMA toward 1); once accuracy exceeds STRUGGLE_THRESHOLD the struggle boost no longer applies — observable as that table's selection share dropping relative to an untouched run.
    - Two independent createBrain() instances do not share accuracy/history (reporting on one does not affect the other's selection distribution).
  </behavior>
  <read_first>
    - `archive/math-lab.html` lines 911-1030 — the ENTIRE QuestionSelector to port verbatim: `shuffle` Fisher-Yates (915-921), `calculateWeights` (927-956), `weightedRandom` (960-969), `generateDistractors` (975-1009), `selectNext` (1014-1028).
    - `archive/math-lab.html` lines 663-711 — the PlayerState EWMA slice to port: `accuracy` seed (667-668: {1..5:0.5, 6..9:0.4}), `history` (670), `updateAccuracy` (690-700), `getAccuracy` (702-704), `isMastered` (707-711). DROP xp/level/addXp/getXp/getLevel/toJSON/fromJSON.
    - `src/level.js` lines 1-23 — the ES-module doc-header + named-export convention to mirror.
    - `.planning/phases/10-.../10-PATTERNS.md` lines 19-63 — the createBrain() factory shape, the verbatim-port excerpts, and the firewall DROP list.
    - `.planning/phases/10-.../10-RESEARCH.md` lines 111-172, 322-346 — exact port targets, return-shape mapping (a=table, b=multiplicand, choices=options), and the factory anti-leak rationale.
  </read_first>
  <action>
    Create `src/math/brain.js` as a NEW pure ES module. Import ONLY `import { CONFIG } from "../config.js";` (note `../` — the file sits in `src/math/`, one level below `src/`). Import NOTHING from Kaplay — this is the GATE-06 firewall.

    Export a single factory `export function createBrain()`. Inside it, hold per-game state in the closure: `const accuracy = { 1:.5, 2:.5, 3:.5, 4:.5, 5:.5, 6:.4, 7:.4, 8:.4, 9:.4 }` (archive 667-668 verbatim) and `const history = {}`. Do NOT use a module-level `let accuracy`/`let history` — fresh closure per call is the anti-leak contract (RESEARCH Pitfall 2).

    Port these helpers into the closure verbatim from the archive, reading `CONFIG.BRAIN.*` in place of the archive's flat `CONFIG.*`:
    - `shuffle` (Fisher-Yates, 915-921) — keep exactly; never substitute `sort(()=>Math.random())`.
    - `getAccuracy(table)` (702-704) and `isMastered(table)` (707-711) — closure helpers `calculateWeights` depends on.
    - `calculateWeights(allowedTables)` (927-956) — HARD weight `Math.pow(1-acc, 1.5)`, ×`CONFIG.BRAIN.STRUGGLE_BOOST` if `acc < CONFIG.BRAIN.STRUGGLE_THRESHOLD`, ×0.3 if mastered; EASY weight `Math.pow(1-acc, 0.8) * 0.3`, ×0.3 if mastered; zero-total (<1e-9) fallback to equal weights. DO NOT re-tune the exponents, the 0.3 factor, or the 1.5 boost — the math is locked. (The archive passes `playerState`; here read the closure's accuracy/history directly via the closure helpers.)
    - `weightedRandom(weights)` (960-969) — roll-and-walk.
    - `generateDistractors(answer, table, multiplicand)` (975-1009) — ±1/±2 same-table, one wrong-table distractor, ±3 fallback, last-resort pad; dedupe `v !== answer && v > 0`; returns exactly 3.

    Implement the returned object:
    - `nextQuestion()` — port `selectNext` (1014-1028): pick `table` via `weightedRandom(calculateWeights())` (pass `undefined` for allowedTables so all 9 tables are in play, biased to 6–9 per GATE-02), `multiplicand = Math.floor(Math.random()*10)+1`, `answer = table*multiplicand`, `options = shuffle([answer, ...generateDistractors(...)])`. RETURN SHAPE: `{ a: table, b: multiplicand, answer, choices: options }` (resolve Open Question #1 to this shape; DROP the archive's `question:` string field — the gate builds the display string itself in Plan 02).
    - `reportResult(table, isCorrect)` — port `updateAccuracy` (690-700) verbatim, reading `CONFIG.BRAIN.ACCURACY_ALPHA` and `CONFIG.BRAIN.MASTERY_WINDOW`, mutating the closure's `accuracy`/`history`.

    DROP entirely (firewall — RESEARCH Pitfall 4): xp, level, addXp, getXp, getLevel, toJSON, fromJSON, XpCalculator, CONFIG.DUNGEON, GameFSM, combat/HP/potion/SAVE_KEY. None of these tokens may appear in the file.

    Write a doc-header comment (mirroring `src/level.js` 1-23) stating: pure module, zero game-engine imports (GATE-06), ported verbatim from archive 663-711 + 911-1030, factory-per-game to prevent cross-replay state leak. Do NOT write the literal lowercase engine token `kaplay` anywhere in this file's comments or code (GATE-06 grep gate negative-greps for it).
  </action>
  <verify>
    <automated>node --check src/math/brain.js && grep -vE '^\s*(//|\*|/\*)' src/math/brain.js | grep -iq 'kaplay' && { echo 'FIREWALL BREACH: kaplay referenced'; exit 1; }; ! grep -Eqi 'XpCalculator|DUNGEON|level\+\+|combat|potion|SAVE_KEY|toJSON|fromJSON' src/math/brain.js && grep -q 'export function createBrain' src/math/brain.js && node -e "import('./src/math/brain.js').then(m=>{const b=m.createBrain();let hard=0;for(let i=0;i<1000;i++){const q=b.nextQuestion();if(q.choices.length!==4||!q.choices.includes(q.answer)||q.answer!==q.a*q.b){console.error('shape fail',q);process.exit(1);}if([6,7,8,9].includes(q.a))hard++;}if(hard<=500){console.error('not 6-9 biased',hard);process.exit(1);}const b2=m.createBrain();if(b===b2){process.exit(1);}console.log('brain ok hard%='+hard/10);})"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/math/brain.js` passes.
    - Headless `node` import of `./src/math/brain.js` succeeds with NO Kaplay/engine import error (proves GATE-06 firewall + headless importability).
    - Over 1000 `nextQuestion()` calls: every result has `choices.length===4`, `choices.includes(answer)`, and `answer===a*b`; >500 results have `a` in [6,7,8,9] (6–9 bias, GATE-02).
    - `grep -L kaplay src/math/brain.js` lists the file (no Kaplay reference in code; the verify command also rejects any non-comment `kaplay` token).
    - `grep -Eqi 'XpCalculator|DUNGEON|level\+\+|combat|potion|SAVE_KEY|toJSON|fromJSON' src/math/brain.js` finds NOTHING (firewall — no XP/combat/persistence ported).
    - `grep -q 'export function createBrain' src/math/brain.js` succeeds AND there is no top-level `let accuracy`/`let history` (anti-leak: state is closure-local).
  </acceptance_criteria>
  <done>src/math/brain.js is a pure, headlessly-importable factory whose nextQuestion() yields 4-choice 6–9-biased questions and whose reportResult() adapts per-session accuracy; zero Kaplay/XP/combat coupling; fresh closure per createBrain().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (this plan) | Pure in-memory arithmetic module; no untrusted input, no network, no persistence, no DOM. Answer input is constrained to Plan 02's 4 boxes / keys 1–4. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | src/math/brain.js (firewall) | mitigate | Brain imports nothing from the engine and nothing XP/persistence-related; enforced by negative greps (`kaplay`, `XpCalculator`, `SAVE_KEY`, `toJSON/fromJSON`). Keeps Phase-11 concerns out (GATE-06). |
| T-10-02 | Tampering | cross-replay state leak | mitigate | createBrain() factory gives a fresh closure per game; no module-level mutable `accuracy`/`history`. Prevents prior-run adaptation bleeding into a new game. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | ZERO package installs this phase (no-build, no-dep project canon — RESEARCH Package Legitimacy Audit "Not applicable"). Only third-party code is the already-vendored, already-pinned Kaplay (not consumed by this pure module). |
</threat_model>

<verification>
- `node --check src/config.js src/math/brain.js` — syntax gate on both changed modules.
- Headless brain smoke (the one real unit test enabled by the GATE-06 firewall): import the module in plain node, run `nextQuestion()` 1000×, assert 4 distinct choices including the answer, `answer===a*b`, and >50% hard-table bias.
- Firewall greps: no `kaplay` (non-comment), no `XpCalculator|DUNGEON|SAVE_KEY|toJSON|fromJSON|combat|potion`.
- Anti-leak grep: `export function createBrain` present; no top-level `let accuracy`/`let history`.
</verification>

<success_criteria>
- src/config.js exposes CONFIG.BRAIN (verbatim ported EWMA/table constants) and CONFIG.GATE (panel/dim) namespaces; no XP/dungeon constants.
- src/math/brain.js is a pure ES module importing only ../config.js, exporting createBrain(); headlessly importable and runnable in plain node.
- nextQuestion() returns { a, b, answer, choices } with 4 distinct choices (answer included) biased toward 6–9 (GATE-02).
- GATE-06 firewall provable: no Kaplay/XP/combat/persistence tokens in brain.js.
</success_criteria>

## Artifacts this phase produces

> Cumulative across Phase 10; THIS plan (10-01) produces the first six rows. The gate/scene rows are produced by 10-02 / 10-03 and listed here so downstream consumers see the full phase surface.

| Artifact | Kind | Produced by | Notes |
|----------|------|-------------|-------|
| `src/math/brain.js` | new file (pure module) | **10-01** | Zero game-engine imports (GATE-06 firewall); headlessly importable in plain node. |
| `createBrain()` | exported function | **10-01** | Factory returning a fresh per-game brain closure (anti-leak). |
| `brain.nextQuestion()` | returned method | **10-01** | Returns `{ a, b, answer, choices }` — `a`=table, `b`=multiplicand, `choices` = 4 shuffled options incl. the answer (6–9 biased). |
| `brain.reportResult(table, isCorrect)` | returned method | **10-01** | EWMA accuracy update (in-memory only this phase; persistence is Phase 11). |
| `CONFIG.BRAIN` | new config namespace | **10-01** | Ported EWMA/table constants (ACCURACY_ALPHA, STRUGGLE_*, MASTERY_*, HARD_TABLES, EASY_TABLES). |
| `CONFIG.GATE` | new config namespace | **10-01** | Gate-panel UI constants (DIM_OPACITY, PANEL_W, PANEL_H) — consumed by 10-02. |
| `src/ui/mathGate.js` | new file (bridge) | 10-02 | The single brain consumer + in-world gate renderer. |
| `openMathGate({ brain, onClear })` | exported function | 10-02 | Renders the in-world gate; calls `onClear()` once on correct. |
| `"math-gate"` | entity tag | 10-02 | Tags every gate object for `destroy("math-gate")` cleanup on close. |
| `onClear` hook | callback contract | 10-02 (consumed) / 10-03 (supplied) | Clean GATE-03 level-clear hook; Phase 11 attaches XP here. |
| `src/scenes/game.js` onReachGoal wiring | modified seam | 10-03 | Replaces the `text("GOAL!")` stub with the `openMathGate(...)` handoff. |

<output>
Create `.planning/phases/10-math-gate-integration-port-the-brain/10-01-SUMMARY.md` when done.
</output>
