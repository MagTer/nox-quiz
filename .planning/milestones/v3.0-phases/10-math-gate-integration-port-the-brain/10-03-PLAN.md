---
phase: 10-math-gate-integration-port-the-brain
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/scenes/game.js
autonomous: true
requirements: [GATE-03]
tags: [math, gate, integration, scene, seam, onReachGoal, level-clear]

must_haves:
  truths:
    - "Reaching the goal opens the in-world math gate (not the old text('GOAL!') stub)"
    - "A correct answer clears the level with a celebration via the onClear callback"
    - "The brain is constructed once per scene via createBrain() and the gate fully resets on replay (no leaked handlers/state)"
    - "src/scenes/game.js is the ONLY scene-side consumer and it talks to the gate solely through openMathGate (the single bridge)"
  artifacts:
    - path: "src/scenes/game.js"
      provides: "onReachGoal() seam wired to openMathGate({ brain, onClear }); fresh per-scene brain; player-freeze reused; onClear handles the LEVEL CLEAR outcome"
      contains: "openMathGate"
  key_links:
    - from: "src/scenes/game.js"
      to: "src/ui/mathGate.js"
      via: "import { openMathGate } and call it from onReachGoal() — the single scene-to-gate bridge"
      pattern: "openMathGate\\("
    - from: "src/scenes/game.js"
      to: "src/math/brain.js"
      via: "import { createBrain } and construct a fresh brain in the scene closure (anti-leak)"
      pattern: "createBrain\\("
---

<objective>
Wire the math gate into the level. Modify the existing fire-once `onReachGoal()` seam in `src/scenes/game.js` (~lines 112-133): replace the temporary `text("GOAL!")` placeholder stub with a call to `openMathGate({ brain, onClear })` from `src/ui/mathGate.js`. Construct the brain via `createBrain()` (from `src/math/brain.js`) fresh per game/scene so state resets cleanly on replay (anti-leak — scene-closure discipline). Reuse the EXISTING player-freeze (`player.vel = vec2(0); player.paused = true`) to pause the level while the gate is open. The `onClear` callback owns the correct→clears-the-level outcome (celebration + "LEVEL CLEAR") and is kept as a clean hook for Phase 11 XP — but NO XP is implemented here.

Purpose: This is the join point — the milestone keystone's final seam. It turns the static "GOAL!" placeholder into the real in-world math gate so reaching the goal poses a question and a correct answer clears the level (GATE-03), closing the GATE-01..06 loop end-to-end.

Output: `src/scenes/game.js` (modified in place — onReachGoal seam wired to the gate).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-math-gate-integration-port-the-brain/10-CONTEXT.md
@.planning/phases/10-math-gate-integration-port-the-brain/10-RESEARCH.md
@.planning/phases/10-math-gate-integration-port-the-brain/10-PATTERNS.md
@src/scenes/game.js
@src/ui/mathGate.js
@src/math/brain.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire onReachGoal() in src/scenes/game.js to openMathGate (replace the GOAL! stub)</name>
  <files>src/scenes/game.js</files>
  <read_first>
    - `src/scenes/game.js` lines 1-22 (doc-header anti-leak contract + the existing import block to extend), 28-42 (the scene-closure run-state pattern: `coinsCollected`/`goalReached` — where a fresh `brain` belongs), 110-133 (the `onReachGoal()` fire-once seam — "the SINGLE-POINT handoff seam" with the `goalReached` latch at 114-115, the clean stop `player.vel = vec2(0)` at 123, the gentle freeze `player.paused = true` at 124, and the `text("GOAL!")` stub at 125-131 to REPLACE), 133 (`player.onCollide("goal", onReachGoal)` — keep exactly one).
    - `src/ui/mathGate.js` (Plan 02 output) — the `openMathGate({ brain, onClear })` signature being called.
    - `src/math/brain.js` (Plan 01 output) — the `createBrain()` factory being constructed.
    - `.planning/phases/10-.../10-PATTERNS.md` lines 108-130 — the exact in-place edit: imports to add (116-117), the brain construction + openMathGate handoff (121-128), and the "keep the freeze, replace only the banner" instruction.
    - `.planning/phases/10-.../10-CONTEXT.md` lines 44-50 (Integration Seam decisions) and `.planning/phases/10-.../10-RESEARCH.md` lines 314-318, 368-378 (Pitfall 5 + scene-wiring example: onClear called exactly once; single level, no go() to next).
  </read_first>
  <action>
    Modify `src/scenes/game.js` IN PLACE. Do NOT add a second goal handler — there is exactly one `onReachGoal` function and one `player.onCollide("goal", onReachGoal)`.

    1. Add two imports alongside the existing ones (lines 19-22): `import { createBrain } from "../math/brain.js";` and `import { openMathGate } from "../ui/mathGate.js";` (note `../` — scenes/ is one dir below src/).

    2. Construct a fresh brain in the scene closure, near the other closure run-state (the `coinsCollected`/`goalReached` block, ~33-42): `const brain = createBrain();` — closure-local so each `go("game")` / replay gets a fresh accuracy/history and nothing leaks across runs (the same anti-leak discipline as `coinsCollected`). Do NOT make it a module-level `let`.

    3. Inside `onReachGoal()`, KEEP the fire-once latch (114-115), the clean stop `player.vel = vec2(0)` (123), and the gentle freeze `player.paused = true` (124) — these pause the level/physics while the gate is open and leave no residual momentum for resume to inherit. REPLACE the `add([ text("GOAL!"), ... "goal-banner" ])` block (125-131) with a single call: `openMathGate({ brain, onClear() { ... } })`. The `onClear` body carries a comment noting GATE-03 (correct -> clears the level), that Phase 12 polishes the celebration and Phase 11 awards XP at this hook, and that this is a single level (no go() to a next level).

    4. The `onClear` callback this phase handles the correct→clears-the-level outcome at the SCENE level: a simple "LEVEL CLEAR" presentation is acceptable (the gate already shows its own LEVEL CLEAR banner per Plan 02; the scene's onClear may simply leave the player frozen = cleared, and/or set a closure flag like `levelCleared = true`). Do NOT implement XP, leveling, persistence, or next-level wiring — those are Phase 11 / out of scope. Keep `onClear` as the clean, single hook Phase 11 attaches to.

    5. Remove the now-dead `"goal-banner"` placeholder entirely (no `text("GOAL!")` may remain). The gate fully owns the on-goal UI now.

    Result: reaching the goal freezes the player, constructs nothing new (the brain already exists in the closure), and hands off to the single bridge `openMathGate(...)`. A correct answer in the gate calls `onClear()` exactly once, clearing the level (GATE-03).
  </action>
  <verify>
    <automated>node --check src/scenes/game.js &amp;&amp; grep -q 'openMathGate' src/scenes/game.js &amp;&amp; grep -q 'createBrain' src/scenes/game.js &amp;&amp; ! grep -q 'text("GOAL!")' src/scenes/game.js &amp;&amp; [ "$(grep -c 'player.onCollide("goal"' src/scenes/game.js)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/scenes/game.js` passes.
    - The source calls `openMathGate(` from inside the goal handling path (grep finds it).
    - The source constructs the brain via `createBrain(` and the call site references `onClear` (the level-clear hook is present, GATE-03).
    - The existing player-freeze is preserved: the source still contains `player.paused = true` (the level pauses while the gate is open).
    - The old placeholder is gone: grepping the source for `text("GOAL!")` finds NOTHING.
    - One goal handler only: `player.onCollide("goal"` appears exactly once.
    - Browser UAT (phase-gate, per 10-VALIDATION.md): serve over HTTP, run to the goal -> the in-world gate opens over the dimmed/paused level -> picking the correct answer (key 1-4 or click) flashes + LEVEL CLEAR and the run ends cleared; replaying the level opens a fresh gate with no duplicated keypress effects.
  </acceptance_criteria>
  <done>onReachGoal() constructs a fresh per-scene brain and hands off to openMathGate({ brain, onClear }); the old text("GOAL!") stub is removed; the player-freeze pauses the level while the gate is open; a correct answer clears the level via onClear() (GATE-03) with XP left as a clean Phase-11 hook.</done>
</task>

<task type="auto">
  <name>Task 2: Author the structural verification script for the scene wiring</name>
  <files>.planning/phases/10-math-gate-integration-port-the-brain/scripts/check-wiring.sh</files>
  <read_first>
    - `.planning/phases/10-.../10-RESEARCH.md` lines 423-463 (Validation Architecture — structural greps; no harness exists, assertions are authored here).
    - `.planning/phases/10-.../10-VALIDATION.md` lines 38-66 (per-task verification map; the 10-03 row asserts openMathGate is wired in onReachGoal).
    - `src/scenes/game.js` (Task 1 output) — the file the script asserts against.
  </read_first>
  <action>
    Create `scripts/check-wiring.sh` under the phase directory. It is the reusable structural gate for the scene wiring (no test framework — this IS the automated check). Fail-fast and exit non-zero with a clear message on any failed assertion. Assert against `src/scenes/game.js`:

    1. `node --check src/scenes/game.js` passes (syntax gate).
    2. The source calls `openMathGate(` (the single bridge handoff).
    3. The source constructs `createBrain(` and references `onClear` (the GATE-03 level-clear hook).
    4. The source still contains `player.paused = true` (player-freeze preserved — level pauses while the gate is open).
    5. NEGATIVE: grepping for `text("GOAL!")` returns NOTHING (the placeholder stub is removed); the script fails if found.
    6. The source contains exactly ONE `player.onCollide("goal"` (the single-point seam is not duplicated). Fail if the count is not 1.

    Print `wiring checks: PASS` on success.
  </action>
  <verify>
    <automated>bash .planning/phases/10-math-gate-integration-port-the-brain/scripts/check-wiring.sh && echo SCRIPT_RUNS</automated>
  </verify>
  <acceptance_criteria>
    - `bash .planning/phases/10-.../scripts/check-wiring.sh` exits 0 and prints `wiring checks: PASS` when `src/scenes/game.js` satisfies all contracts.
    - The script exits non-zero with a descriptive message if any single assertion fails (each check has its own failure branch).
    - The script encodes all six assertions above (syntax, openMathGate handoff, createBrain + onClear, player-freeze preserved, GOAL! stub removed, single goal handler).
  </acceptance_criteria>
  <done>A reusable scripts/check-wiring.sh exists that fail-fast asserts the scene-to-gate wiring contract and is the per-commit automated gate for src/scenes/game.js.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scene to gate handoff | The scene passes a closure-local `brain` and an `onClear` callback into the gate. No untrusted input crosses; both are in-process objects the scene owns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | cross-replay brain/gate state leak at the seam | mitigate | `brain` is constructed with `createBrain()` in the scene closure (fresh per `go("game")`), and the gate self-cleans on close (cancel + destroy, Plan 02). Replays start clean. Enforced by check-wiring.sh (createBrain present) + check-gate.sh from Plan 02. |
| T-10-07 | Repudiation/Integrity | double-fire of onReachGoal / onClear | mitigate | The existing `goalReached` fire-once latch is preserved (not removed); onClear is called once from the gate's correct branch (Plan 02 latch). check-wiring.sh asserts a single `player.onCollide("goal"` seam. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | ZERO package installs this phase (no-build, no-dep project canon — RESEARCH Package Legitimacy Audit "Not applicable"). |
</threat_model>

<verification>
- `node --check src/scenes/game.js` — syntax gate.
- `bash scripts/check-wiring.sh` — the structural wiring gate (authored in Task 2): openMathGate handoff present, createBrain + onClear present, player-freeze preserved, GOAL! stub removed, single goal handler.
- Manual UAT (phase-gate, per 10-VALIDATION.md): serve over HTTP, run to the goal -> the in-world gate opens over the dimmed/paused level; a correct answer (key or click) clears the level with celebration; replay opens a fresh gate with no duplicated keypress effects (anti-leak confirmed end-to-end).
</verification>

<success_criteria>
- onReachGoal() in src/scenes/game.js hands off to openMathGate({ brain, onClear }) — the single scene-to-gate bridge — with the player-freeze pausing the level.
- The brain is constructed fresh per scene via createBrain() (closure-local, anti-leak); replays reset cleanly.
- A correct answer clears the level via onClear() with a celebration (GATE-03); XP is left as a clean Phase-11 hook (not implemented).
- The old text("GOAL!") stub is removed and exactly one goal handler remains.
</success_criteria>

## Artifacts this phase produces

> Cumulative across Phase 10; THIS plan (10-03) produces the scene-wiring rows. The brain/config rows come from 10-01 and the gate from 10-02 — listed so downstream consumers see the full phase surface.

| Artifact | Kind | Produced by | Notes |
|----------|------|-------------|-------|
| `src/scenes/game.js` onReachGoal wiring | modified seam | **10-03** | Replaces the `text("GOAL!")` stub with the `openMathGate({ brain, onClear })` handoff; player-freeze reused to pause the level. |
| per-scene `createBrain()` construction | scene-closure state | **10-03** | A fresh brain per `go("game")` (anti-leak); resets cleanly on replay. |
| `onClear` callback (supplied) | GATE-03 hook | **10-03** | Correct -> clears the level (celebration); clean single hook Phase 11 attaches XP to (no XP here). |
| `scripts/check-wiring.sh` | new verification script | **10-03** | Structural wiring gate (openMathGate handoff, createBrain+onClear, freeze preserved, GOAL! removed, single goal handler). |
| `src/math/brain.js` / `createBrain()` | (consumed) | 10-01 | The brain this scene constructs. |
| `src/ui/mathGate.js` / `openMathGate(...)` / `onClear` contract | (consumed) | 10-02 | The single bridge this scene calls. |
| `CONFIG.BRAIN` / `CONFIG.GATE` | (consumed transitively) | 10-01 | Tuning constants the brain/gate read. |

<output>
Create `.planning/phases/10-math-gate-integration-port-the-brain/10-03-SUMMARY.md` when done.
</output>
