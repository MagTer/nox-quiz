---
phase: 11-progression-persistence
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/config.js
  - scripts/check-progress.sh
  - scripts/smoke-progress.mjs
autonomous: true
requirements: [SAVE-01, SAVE-02, SAVE-03, SAVE-04]

must_haves:
  truths:
    - "Running `bash scripts/check-progress.sh` from the repo root exits non-zero while progress.js/hud.js do not yet exist (it is a real gate, not a no-op), and prints a clear FAIL message"
    - "Running `node scripts/smoke-progress.mjs` exits non-zero while src/progress.js does not yet exist (the harness imports the real module)"
    - "CONFIG.PROGRESS, CONFIG.SAVE, CONFIG.HUD namespaces exist with the verbatim ported values so Wave 1+ modules can import them"
    - "smoke-progress.mjs asserts a history round-trip (serialize → seedHistory → snapshot preserves the per-table boolean window) so mastery/drill-reduction resume is proven, not just accuracy"
  artifacts:
    - path: "scripts/check-progress.sh"
      provides: "Structural firewall/anti-leak/one-way gate mirroring check-gate.sh"
      contains: "set -euo pipefail"
    - path: "scripts/smoke-progress.mjs"
      provides: "Headless node smoke of the pure XP/level math + seedAccuracy/seedHistory adaptation"
      contains: "seedHistory"
    - path: "src/config.js"
      provides: "CONFIG.PROGRESS / CONFIG.SAVE / CONFIG.HUD namespaces"
      contains: "mathlab_platformer_v1"
  key_links:
    - from: "scripts/check-progress.sh"
      to: "scripts/smoke-progress.mjs"
      via: "the gate invokes the headless smoke as its final step"
      pattern: "node .*scripts/smoke-progress.mjs"
    - from: "src/progress.js (Wave 1)"
      to: "src/config.js"
      via: "progress.js reads CONFIG.PROGRESS.* / CONFIG.SAVE.* authored here"
      pattern: "CONFIG\\.(PROGRESS|SAVE)\\."
---

<objective>
Author the two Wave-0 verification tools FIRST (per VALIDATION.md Wave 0 and the Phase-10
task-ordering lesson where a Task 1 `<automated>` pointed at a not-yet-created script), and
add the CONFIG namespaces every later module depends on. After this plan, Waves 1–3 can point
their `<automated>` verify at `bash scripts/check-progress.sh` and `node scripts/smoke-progress.mjs`.

These scripts ARE the project's automated test layer (no JS test framework — CLAUDE.md). They
are written to FAIL now (the files they check do not exist yet) and turn green as later waves
land each module — exactly the behavior a real gate should have.

Purpose: Make every downstream task's verification runnable and make the firewall/anti-leak
contracts machine-checkable from commit one. Add CONFIG.PROGRESS/SAVE/HUD (verbatim port — DO
NOT re-tune) so progress.js (Wave 1) and hud.js (Wave 3) have their leaf constants.
Output: scripts/check-progress.sh, scripts/smoke-progress.mjs, CONFIG.PROGRESS/SAVE/HUD.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-progression-persistence/11-RESEARCH.md
@.planning/phases/11-progression-persistence/11-PATTERNS.md
@.planning/phases/11-progression-persistence/11-VALIDATION.md
@.planning/phases/10-math-gate-integration-port-the-brain/scripts/check-gate.sh
@src/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CONFIG.PROGRESS / CONFIG.SAVE / CONFIG.HUD namespaces (verbatim port)</name>
  <files>src/config.js</files>
  <read_first>
    - src/config.js lines 46-65 — the existing CONFIG.BRAIN and CONFIG.GATE namespaced blocks (the analog: add three sibling blocks before the closing brace, with the same "ported verbatim — DO NOT re-tune" comment discipline used at lines 46-48).
    - 11-PATTERNS.md lines 194-204 — the exact namespace shapes and the note that HARD_TABLES/EASY_TABLES are intentionally duplicated under BRAIN and PROGRESS (different consumers; the firewall keeps the modules independent).
    - 11-RESEARCH.md lines 388-405 — the source-of-truth values.
    - archive/math-lab.html lines 604-619 — the original CONFIG (XP_EASY 10, XP_HARD 20, BASE_XP 200, LEVEL_MULT 1.3).
  </read_first>
  <action>
    Add three new sibling blocks inside the CONFIG object (after CONFIG.GATE, before the final closing brace at line 65), mirroring the existing namespaced-block style:
    PROGRESS: XP_EASY=10, XP_HARD=20, BASE_XP=200, LEVEL_MULT=1.3, HARD_TABLES=[6,7,8,9], EASY_TABLES=[1,2,3,4,5].
    SAVE: KEY="mathlab_platformer_v1" (the NEW namespaced key — independent of the archive's mathlab_save_*; never read/write the old keys), VERSION=1.
    HUD: X=16, Y=16, BADGE_SIZE=18, BAR_W=160, BAR_H=10, BAR_DY=24, FLASH_SIZE=36, FLASH_MS=450 (FLASH_MS is the ADHD-safe window per STATE.md tech-debt — NOT the archive's 800).
    Port the numbers VERBATIM — do not re-tune (CONTEXT line 29). Add a short comment on each block citing the archive source and "DO NOT re-tune", matching the CONFIG.BRAIN comment at lines 46-48. Do NOT port any HP/combat/dungeon/ADVANCE_DELAY_MS/SEGMENT_COUNT fields — out of milestone scope.
  </action>
  <verify>
    <automated>node --check src/config.js && grep -q 'mathlab_platformer_v1' src/config.js && grep -q 'LEVEL_MULT' src/config.js && grep -q 'FLASH_MS' src/config.js</automated>
  </verify>
  <done>CONFIG.PROGRESS, CONFIG.SAVE (with KEY mathlab_platformer_v1 + VERSION), and CONFIG.HUD blocks exist with the verbatim ported values; node --check passes; no dungeon/HP fields added.</done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/check-progress.sh (structural gate mirroring check-gate.sh)</name>
  <files>scripts/check-progress.sh</files>
  <read_first>
    - .planning/phases/10-math-gate-integration-port-the-brain/scripts/check-gate.sh (FULL FILE) — the exact structure to mirror: shebang + `set -euo pipefail` (line 15), `ROOT="$(git rev-parse --show-toplevel)"` for cwd-independence (line 18), a `fail()` helper echoing to stderr + `exit 1` (lines 21-24), `[ -f "$TARGET" ]` + `node --check` per target (lines 27-30), positive greps for required API/wiring, NEGATIVE greps wrapped in `if grep -Eq ...; then fail` (lines 53-65), final PASS echo (line 67). Note its discipline: banned tokens appear ONLY in the grep PATTERNS, never in matched source.
    - 11-PATTERNS.md lines 208-237 — the Phase-11-specific assertion list.
    - 11-RESEARCH.md lines 497-504 — the Req→test grep map.
  </read_first>
  <action>
    Create scripts/check-progress.sh mirroring check-gate.sh's shape exactly: `#!/usr/bin/env bash`, `set -euo pipefail`, `ROOT="$(git rev-parse --show-toplevel)"`, a `fail()` helper, and a run-from-root usage comment.
    Existence + syntax gate (each guarded so a missing Wave-1+ file produces a clear FAIL, not a bash error): for src/progress.js, src/ui/hud.js, src/math/brain.js — `[ -f "$f" ] || fail "..."` then `node --check "$f" || fail "..."`.
    Positive greps: `grep -q 'QuotaExceededError' "$ROOT/src/progress.js"` (guarded seam); `grep -q 'mathlab_platformer_v1' "$ROOT/src/config.js"` (versioned key); `grep -q 'seedAccuracy' "$ROOT/src/scenes/game.js"` AND `grep -q 'seedAccuracy' "$ROOT/src/math/brain.js"` (accuracy seeding wired both ends); `grep -q 'seedHistory' "$ROOT/src/scenes/game.js"` AND `grep -q 'seedHistory' "$ROOT/src/math/brain.js"` (history/mastery seeding wired both ends — locked, not optional); `grep -q 'snapshot' "$ROOT/src/math/brain.js"` (snapshot exposed); `grep -q 'fixed(' "$ROOT/src/ui/hud.js"` (camera-immune HUD).
    NEGATIVE greps (wrapped `if grep -Eq ...; then fail`): progress.js imports NO kaplay — match `kaplay|lib/kaplay`; brain.js touches NO storage — match `localStorage`; HUD is one-way — match `progress\.(addXp|level[[:space:]]*=)`; forgiving — the gate must NOT award XP, match `addXp` in src/ui/mathGate.js.
    Final step: invoke the headless smoke — `node "$ROOT/scripts/smoke-progress.mjs" || fail "smoke-progress failed"`. End with `echo "progress checks: PASS"`.
    Keep the literal forbidden tokens ONLY inside the grep pattern strings here, never echoed as plain prose, mirroring check-gate.sh's discipline.
  </action>
  <verify>
    <automated>bash -n scripts/check-progress.sh && grep -q 'set -euo pipefail' scripts/check-progress.sh && grep -q 'smoke-progress.mjs' scripts/check-progress.sh && grep -q 'git rev-parse' scripts/check-progress.sh</automated>
  </verify>
  <done>scripts/check-progress.sh parses (`bash -n`), mirrors check-gate.sh structure (set -euo pipefail, git rev-parse root, fail() helper), encodes the firewall/anti-leak/one-way/forgiving greps, and ends by invoking smoke-progress.mjs. It exits non-zero today because src/progress.js etc. do not yet exist (a real gate).</done>
</task>

<task type="auto">
  <name>Task 3: Author scripts/smoke-progress.mjs (headless node math + adaptation smoke)</name>
  <files>scripts/smoke-progress.mjs</files>
  <read_first>
    - 11-PATTERNS.md lines 241-248 — the No-Analog harness shape (plain ESM + console.assert + non-zero exit; imports the pure modules directly; must NOT touch localStorage).
    - 11-RESEARCH.md lines 495-503 — the exact assertions per requirement; and Pitfall 1 (lines 342-345: call createProgress(fixture) directly, never read storage) and Pitfall 2 (lines 348-351: the statistical seed test that guards the SAVE-03 false-pass).
    - archive/math-lab.html lines 648-657 (threshold/calculateXp), 678-687 (addXp surplus carry), 663-670 (initial accuracy 0.4 hard / 0.5 easy).
    - src/math/brain.js lines 40-56, 181-206 — the brain factory + nextQuestion/reportResult the smoke seeds and draws from.
  </read_first>
  <action>
    Create scripts/smoke-progress.mjs as a plain ES module that imports `{ createProgress }` from `../src/progress.js` and `{ createBrain }` from `../src/math/brain.js` (paths relative to scripts/). Track a failure count; use console.assert with descriptive messages; at the end, if any assertion failed, `process.exit(1)`, else print "smoke-progress: PASS".
    Assertions:
    SAVE-01 thresholds — `createProgress().threshold(1) === 200` and `threshold(2) === 260` (round(200·1.3)).
    SAVE-01 calculateXp via addXp — a fresh progress: addXp(7) increments XP by 20 and returns false (20 < 200); addXp(3) increments by 10. Drive XP across a threshold (repeated addXp on a hard table) and assert addXp returns true exactly on the crossing and that surplus carried over (post-level XP equals the overflow, NOT 0).
    SAVE-02 round-trip — build a brain, do a few reportResult calls, then `createProgress(p.serialize(brain.snapshot()))` and assert xp/level survive and the blob carries a `version` field equal to the configured save version, and accuracy keys round-trip.
    SAVE-02/03 history round-trip (mastery resume) — build a brain, drive a table to mastery (e.g. ~10 correct reportResult(7,true) calls), serialize via brain.snapshot(), then reconstruct a new brain from the saved blob's history: `createBrain({ seedHistory: serialized.history })`. Assert the reconstructed brain's snapshot().history for that table preserves the per-table boolean window (same length, same values, clamped to MASTERY_WINDOW). This proves history (not just accuracy) survives serialize→deserialize→reseed, so isMastered drill-reduction resumes (coordinator revision — seedHistory is locked, not optional).
    SAVE-03 statistical — seed a brain with table 7 at 0.05 via `createBrain({ seedAccuracy: { 7: 0.05 } })`, draw ~2000 `nextQuestion()` and count the table-7 share; build a fresh `createBrain()` and draw ~2000; assert the seeded brain's table-7 share is materially higher (e.g. at least ~1.5x the fresh baseline). This is the only automated proof SAVE-03 resumes adaptation.
    Do NOT reference localStorage anywhere (call createProgress with fixtures directly). Use only Math/loops — no test framework, no external deps.
  </action>
  <verify>
    <automated>node --check scripts/smoke-progress.mjs && grep -q 'console.assert' scripts/smoke-progress.mjs && grep -q 'seedAccuracy' scripts/smoke-progress.mjs && grep -q 'seedHistory' scripts/smoke-progress.mjs && ! grep -q 'localStorage' scripts/smoke-progress.mjs</automated>
  </verify>
  <done>scripts/smoke-progress.mjs passes `node --check`, asserts SAVE-01 thresholds/XP/surplus, SAVE-02 serialize round-trip with version field, the SAVE-02/03 history round-trip (mastery resume via seedHistory), and the SAVE-03 statistical weak-spot-resume check; never references localStorage; exits non-zero on assertion failure. It currently fails because src/progress.js does not yet exist (the import throws) — expected until Wave 1.</done>
</task>

</tasks>

<verification>
- `node --check src/config.js && node --check scripts/smoke-progress.mjs && bash -n scripts/check-progress.sh` all pass (syntax).
- `grep -q 'mathlab_platformer_v1' src/config.js` (versioned key authored).
- `bash scripts/check-progress.sh; echo "exit=$?"` exits non-zero today with a clear FAIL pointing at the missing src/progress.js — confirming it is a real gate (it goes green as Waves 1–3 land).
- The two scripts contain no JS-test-framework imports (project canon: none).
</verification>

<success_criteria>
- CONFIG.PROGRESS/SAVE/HUD exist with verbatim values; no dungeon/HP fields leaked in.
- scripts/check-progress.sh mirrors check-gate.sh and invokes smoke-progress.mjs.
- scripts/smoke-progress.mjs encodes SAVE-01/02/03 assertions and never touches localStorage.
- Both scripts are runnable from the repo root and FAIL cleanly until later waves create the modules they verify.
</success_criteria>

<output>
Create `.planning/phases/11-progression-persistence/11-00-SUMMARY.md` when done.
</output>
