---
milestone: v4.1
audited: 2026-07-04T23:45:00Z
status: passed
scores:
  requirements: 10/10
  phases: 2/2
  integration: 10/10
  flows: 4/4
gaps: []
tech_debt:
  - phase: 21-real-verification-pass-mechanics-sign-off-integrity-perform-
    items:
      - "VERIFY-01 (WARNING, non-blocking): 6 of 16 mechanic encounters across the 4 levels remain unreached by the automated interactive-audit traversal (scripts/lib/mechanic-drive.mjs), due to a documented resonance between the constant-jump cadence and floor-embedded spike placement on longer runs. The phase's actual scoped bar — door.js and enemy.js reached via real movement in at least one level each — is met and evidenced (level-01-door-1400, level-01-enemy-1000, level-04-door-900 screenshots). Documented in 21-FINDINGS.md's Methodology Note, including rejected tuning attempts, rather than concealed."
      - "New Finding 4 residual: the visual-overlap half is fixed (hide/restore guard in challenge.js), but no same-time-open *prevention* guard exists — two challenges can still be triggered concurrently in ordinary play (by design, since collect-zone intentionally stays open during movement); the fix ensures they no longer corrupt or visually clash, not that concurrency itself is prevented."
      - "Pre-existing flakiness discovered (not fixed, out of scope) in scripts/check-gate.sh: a pipefail/grep -q SIGPIPE race, unrelated to this phase's own files."
nyquist:
  compliant_phases: 0
  partial_phases: 2
  missing_phases: 0
  overall: partial
---

# Milestone v4.1 — Art Rework — Audit Report

**Audited:** 2026-07-04
**Status:** ✓ PASSED

## Summary

Both phases of v4.1 shipped clean. Phase 20 replaced Phase 18's procedurally-generated
placeholder art with real curated CC0 pixel art (player, tileset, parallax, title/select)
under a genuine two-round human visual sign-off that caught and fixed a real invisible-background
bug and correctly scoped out door/enemy glyph issues as Phase 21's concern. Phase 21 then gave
`door.js`, `gates.js`, `enemy.js`, and `mathGate.js` the same real interactive scrutiny
`collect.js` got post-ship, closing the verification-integrity gap that let Phase 18's placeholder
art and other pre-v4.1 defects ship with unsubstantiated "human sign-off recorded" claims. Along
the way it found and fixed 4 additional real bugs (enemy.js arithmetic-display, a
simultaneous-challenge state-corruption + visual-overlap bug, a jump-over exploit on
checkpoints, and a path-traversal/bind-all-interfaces issue in local test tooling) and corrected
the milestone-audit record for Phase 14/NAV-04's unsupported sign-off claim.

Phase 21's first verification pass found real gaps (1/4 must-haves — door.js/enemy.js had never
actually been driven with real movement). A gap-closure cycle (plans 21-05/06/07) closed all
three gaps; re-verification scored 4/4.

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter | Final Status |
|--------|-------|-----------------|------------------|----------------------|---------------|
| ART-05 | 20 | `[x]` | passed | listed | **satisfied** |
| ART-06 | 20 | `[x]` | passed | listed | **satisfied** |
| ART-07 | 20 | `[x]` | passed | listed | **satisfied** |
| ART-08 | 20 | `[x]` | passed | listed | **satisfied** |
| PROC-01 | 20 | `[x]` | passed | listed | **satisfied** |
| PROC-02 | 20 | `[x]` | passed | listed | **satisfied** |
| VERIFY-01 | 21 | `[x]` | passed (4/4, re-verified) | listed | **satisfied** |
| VERIFY-02 | 21 | `[x]` | passed | listed | **satisfied** |
| VERIFY-03 | 21 | `[x]` | passed | listed | **satisfied** |
| VERIFY-04 | 21 | `[x]` | passed | listed | **satisfied** |

No orphaned requirements — all 10 REQ-IDs in the traceability table appear in a phase
VERIFICATION.md with matching status.

## Phase Verification Summary

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 20 — Real CC0 Art Redo & Human Sign-off | ✓ passed | — | Two-round genuine `AskUserQuestion` human sign-off; one real bug found (invisible background/ledges) and fixed mid-phase |
| 21 — Real Verification Pass — Mechanics & Sign-off Integrity | ✓ passed | 4/4 must-haves (after gap closure; first pass scored 1/4) | 3 gaps found and closed via plans 21-05/06/07; 2 additional real bugs found via code review's `--auto` iteration loop and fixed |

## Cross-Phase Integration

Spawned `gsd-integration-checker` against both phases. Result: **no BLOCKER-level breaks**, one
non-blocking WARNING (see tech debt above). Phase 20's real assets (`assets/player.png`,
`assets/tiles/ground.png`, `assets/parallax/*`, `assets/tiles/title-bg.png`) are loaded by
`main.js` and consumed correctly by `player.js`, `build.js`, `parallax.js`, `title.js`/`select.js`.
Phase 21's mechanic fixes sit directly on top of that same rendering path — confirmed visually via
screenshots showing both phases' changes composing correctly (e.g. the enemy encounter's fixed
two-line challenge overlay rendering against the real Kenney player sprite and real tileset; the
simultaneous-challenge hide/restore fix leaving the real coin-pickup badges intact underneath).

## End-to-End Flows

| Flow | Status |
|------|--------|
| Title → Select → Game (scene handoff) | ✓ Complete |
| Math mechanic → XP/level-up → save persistence | ✓ Complete |
| Phase 20 art + Phase 21 mechanic fixes rendered together | ✓ Complete (visually confirmed) |
| Simultaneous-challenge fix against real art assets | ✓ Complete (visually confirmed, coin sprites intact) |

## Nyquist Compliance Discovery

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 20 | exists | false (frontmatter never flipped post-execution) | Cosmetic — the phase's actual per-task automated verification (static gate suite + browser-boot) ran and passed; frontmatter staleness only |
| 21 | exists | false (frontmatter never flipped post-execution) | Same — plan-checker independently confirmed the underlying automated-verify content satisfies Nyquist; `21-VALIDATION.md`'s frontmatter was simply never updated after execution |

Discovery only, non-blocking — no `/gsd-validate-phase` invoked automatically.

## Tech Debt (non-blocking, see frontmatter for structured list)

1. **VERIFY-01 partial traversal coverage** — 6/16 mechanic encounters remain out of the automated
   audit script's reach across all 4 levels (documented technical reason: spike-hazard timing
   resonance). The requirement's actual scoped bar (door.js/enemy.js reached via real movement in
   at least one level) is met.
2. **New Finding 4's concurrency itself is still possible** by design (collect-zone intentionally
   stays open during movement) — the fix prevents corruption/visual clash, not concurrency.
3. **Pre-existing `scripts/check-gate.sh` flakiness** (SIGPIPE race) — discovered, logged, not
   fixed; unrelated to this milestone's own files.
4. **Both phases' VALIDATION.md frontmatter** (`nyquist_compliant`, `wave_0_complete`) were never
   flipped to `true` post-execution despite the underlying verification actually passing — a
   process/documentation gap, not a functional one.

None of the above block milestone completion.

---
*Audit complete: 2026-07-04*
