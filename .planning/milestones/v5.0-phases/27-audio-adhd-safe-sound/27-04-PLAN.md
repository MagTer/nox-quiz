---
phase: 27-audio-adhd-safe-sound
plan: 04
type: execute
wave: 2
depends_on: ["27-01", "27-02"]
files_modified:
  - src/mechanics/door.js
  - src/mechanics/gates.js
  - src/ui/mathGate.js
  - src/mechanics/collect.js
autonomous: true
requirements: [AUD-01]
must_haves:
  truths:
    - "Door and checkpoint-gate unlocks each play the same distinct 'unlock' SFX, separate from the shared correct-chime, so gate-clear reads as its own event"
    - "Level-clear plays a calm, non-fanfare SFX, distinct from both correct and door"
    - "Collecting the correct pickup plays a light pluck/pop SFX, distinct from the correct-chime"
  artifacts:
    - "src/mechanics/door.js: audio.playSfx(\"door\") in onSuccess, before unfreezing the player"
    - "src/mechanics/gates.js: audio.playSfx(\"door\") in onSuccess, reusing the same cue"
    - "src/ui/mathGate.js: audio.playSfx(\"clear\") in the celebration block"
    - "src/mechanics/collect.js: audio.playSfx(\"pickup\") on correct pickup touch"
  key_links:
    - "Each of these 4 SFX call sites is ADDITIONAL to, never a replacement for, challenge.js's shared correct/wrong seam (Plan 27-03) — door/gates/mathGate/collect all still resolve their underlying answer through openChallenge(), which already plays 'correct'"
---

<objective>
Layer the four mechanic-specific SFX (door-unlock, gate-unlock, level-clear, pickup) on top of the shared correct/wrong seam Plan 27-03 already wired, so each distinct game event (not just "you answered correctly") has its own audible signature.

Purpose: AUD-01 requires door/gate and level-clear to be distinct from each other and from the correct-chime; RESEARCH.md's Architectural Responsibility Map places each of these four call sites in the mechanic's own onSuccess/celebration callback, never inside challenge.js itself (that would duplicate the "correct" chime's job).
Output: door.js and gates.js each play "door" on unlock (same reused cue); mathGate.js plays "clear" on level-clear; collect.js plays "pickup" on a correct pickup touch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-audio-adhd-safe-sound/27-RESEARCH.md
@.planning/phases/27-audio-adhd-safe-sound/27-PATTERNS.md
@src/mechanics/door.js
@src/mechanics/gates.js
@src/ui/mathGate.js
@src/mechanics/collect.js
@src/audio.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire door + gate unlock SFX</name>
  <files>src/mechanics/door.js, src/mechanics/gates.js</files>
  <read_first>src/mechanics/door.js (full file — the onSuccess callback at lines 64-81, specifically the fx.clearBurst() call at line 70 and the destroy(doorObj) block at lines 75-77), src/mechanics/gates.js (full file — the near-identical onSuccess callback at lines 57-73)</read_first>
  <action>
Add `import * as audio from "../audio.js";` to both src/mechanics/door.js and src/mechanics/gates.js's import blocks (alongside each file's existing `import { openChallenge } from "../ui/challenge.js";` and `import * as fx from "../fx.js";` — same `../` sibling-import depth, mechanics/ is one directory below src/).

In door.js's onSuccess callback (inside wireDoor()'s player.onCollide("door", ...) handler), add `audio.playSfx("door");` immediately after the existing `fx.clearBurst();` call (line 70) and before the `destroy(doorObj);` line (line 75) — this plays the unlock cue while the door is still visually present, one frame before it disappears.

In gates.js's onSuccess callback (inside wireGates()'s player.onCollide("math-gate", ...) handler), add the SAME `audio.playSfx("door");` call — reusing the identical "door" SFX name is deliberate per CONTEXT.md ("Door/gate open: short distinct unlock cue... separate from the correct-chime") — immediately after the existing `fx.clearBurst();` call (line 62) and before `destroy(gateObj);` (line 68).

Do NOT add a second correct/wrong playSfx call in either file — door.js/gates.js's underlying question resolution already plays "correct" via challenge.js's choose() (Plan 27-03); this task ONLY adds the mechanic-specific unlock cue layered on top.
  </action>
  <acceptance_criteria>
    - Both door.js and gates.js import "../audio.js" as `audio`
    - Both call audio.playSfx("door") inside their onSuccess callback, after fx.clearBurst() and before the destroy() call
    - Neither file calls playSfx("correct") or playSfx("wrong") (that stays exclusively in challenge.js)
    - node --check passes for both files
  </acceptance_criteria>
  <verify>
    <automated>node --check src/mechanics/door.js && node --check src/mechanics/gates.js && grep -q 'audio.playSfx("door")' src/mechanics/door.js && grep -q 'audio.playSfx("door")' src/mechanics/gates.js && ! grep -q 'playSfx("correct")\|playSfx("wrong")' src/mechanics/door.js && ! grep -q 'playSfx("correct")\|playSfx("wrong")' src/mechanics/gates.js && echo "door/gates unlock SFX OK"</automated>
  </verify>
  <done>Both door.js and gates.js play the shared "door" unlock cue on their own onSuccess, with no duplicated correct/wrong SFX call.</done>
</task>

<task type="auto">
  <name>Task 2: Wire level-clear SFX</name>
  <files>src/ui/mathGate.js</files>
  <read_first>src/ui/mathGate.js (full file — the openMathGate() onSuccess callback at lines 29-57, specifically the celebration block at lines 33-53 before the `onClear?.({ table });` call at line 55)</read_first>
  <action>
Add `import * as audio from "./audio.js";` to src/ui/mathGate.js's import block (alongside the existing `import { openChallenge } from "./challenge.js";` and `import { CONFIG } from "../config.js";` at lines 13-14 — mathGate.js lives in src/ui/, so the sibling audio.js import is "./audio.js", NOT "../audio.js" — same directory as challenge.js).

Inside openMathGate()'s onSuccess callback, add `audio.playSfx("clear");` after the two celebration add() calls (the dim backdrop rect at lines 36-44 and the "LEVEL CLEAR" text at lines 45-53) and before `onClear?.({ table });` (line 55) — so the clear cue plays as the banner appears, a calm non-fanfare resolve cue distinct from both "correct" (already played by challenge.js's choose() before this onSuccess even fires) and "door".
  </action>
  <acceptance_criteria>
    - src/ui/mathGate.js imports "./audio.js" as `audio`
    - audio.playSfx("clear") is called after the "LEVEL CLEAR" banner's add() calls and before onClear?.()
    - node --check src/ui/mathGate.js passes
    - scripts/check-progress.sh's existing negative assertion 9 (no addXp in mathGate.js — the forgiving mandate) still passes: this task adds no XP-related code
  </acceptance_criteria>
  <verify>
    <automated>node --check src/ui/mathGate.js && grep -q 'import \* as audio from "./audio.js"' src/ui/mathGate.js && grep -q 'audio.playSfx("clear")' src/ui/mathGate.js && ! grep -q 'addXp' src/ui/mathGate.js && echo "mathGate.js clear SFX OK"</automated>
  </verify>
  <done>mathGate.js plays "clear" as the LEVEL CLEAR banner appears, distinct from "correct" and "door", with no XP-award regression.</done>
</task>

<task type="auto">
  <name>Task 3: Wire pickup SFX</name>
  <files>src/mechanics/collect.js</files>
  <read_first>src/mechanics/collect.js (full file — the player.onCollide("answer-pickup-slot", ...) handler at lines 108-131, specifically the `if (correct) { ... }` branch at lines 121-126)</read_first>
  <action>
Add `import * as audio from "../audio.js";` to src/mechanics/collect.js's import block (alongside the existing `import { openChallenge } from "../ui/challenge.js";`, `import { CONFIG } from "../config.js";`, and `import * as fx from "../fx.js";` at lines 21-23).

Inside the player.onCollide("answer-pickup-slot", ...) handler's `if (correct) { ... }` branch (lines 121-126), add `audio.playSfx("pickup");` as the first statement in the branch, before `cleared.add(zoneObj);`. This plays only when the TOUCHED pickup's value matches q.answer (the correct pickup) — the `else` branch (wrong pickup, lines 127-130) already has its own non-punishing fx.pop() visual nudge and must NOT gain a new SFX call in this task (CONTEXT.md's "no punishment path" — a distinct sound on a wrong pickup touch was not requested and would risk reading as a mini-buzzer on every miss).

Do not add audio.playSfx("correct") here — collect.js resolves its own pickup-vs-answer match directly (it never calls challenge.js's choose(), since renderChoices:false suppresses the answer-box path) so it has no existing "correct" call to avoid duplicating; "pickup" is this mechanic's own distinct success cue, standing in for both roles at once by design.
  </action>
  <acceptance_criteria>
    - src/mechanics/collect.js imports "../audio.js" as `audio`
    - audio.playSfx("pickup") is called inside the `if (correct)` branch of the answer-pickup-slot handler, before cleared.add(zoneObj)
    - No new SFX call is added to the wrong-pickup `else` branch
    - node --check src/mechanics/collect.js passes
  </acceptance_criteria>
  <verify>
    <automated>node --check src/mechanics/collect.js && grep -q 'import \* as audio from "../audio.js"' src/mechanics/collect.js && grep -q 'audio.playSfx("pickup")' src/mechanics/collect.js && echo "collect.js pickup SFX OK"</automated>
  </verify>
  <done>collect.js plays "pickup" on a correct pickup touch only, with the wrong-pickup path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| src/mechanics/door.js, gates.js, collect.js, src/ui/mathGate.js -> audio.playSfx() | Every `name` argument added in this plan is a hardcoded literal ("door", "clear", "pickup") — never derived from the touched object, the question, or any player input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-02 | Tampering (path injection, latent) | src/mechanics/door.js, gates.js, collect.js, src/ui/mathGate.js | low | accept | Same disposition as 27-02-PLAN.md/27-03-PLAN.md's T-27-02 entry — all 4 new call sites in this plan pass hardcoded literals, no dynamic asset-name construction is introduced |
| T-27-06 | Repudiation / forgiving-mandate regression (project-specific) | src/ui/mathGate.js | low | mitigate | Task 2's acceptance criteria explicitly re-check scripts/check-progress.sh's existing negative assertion (no addXp in mathGate.js) so this SFX addition cannot silently reintroduce a penalty/reward-timing regression in the one file that already has a dedicated static gate for it |

</threat_model>

<verification>
Run `node --check src/mechanics/door.js src/mechanics/gates.js src/ui/mathGate.js src/mechanics/collect.js` and `bash scripts/check-safety.sh && bash scripts/check-progress.sh` (check-progress.sh's assertion 9 specifically re-verifies mathGate.js stays XP-free). Confirm via grep that "door" plays in both door.js and gates.js, "clear" plays only in mathGate.js, and "pickup" plays only in collect.js's correct branch.
</verification>

<success_criteria>
- door.js and gates.js both play the shared "door" unlock cue in their onSuccess callback
- mathGate.js plays "clear" in its celebration block, with no addXp regression
- collect.js plays "pickup" only on a correct pickup touch, never on a wrong one
- check-safety.sh and check-progress.sh remain green
</success_criteria>

<output>
Create `.planning/phases/27-audio-adhd-safe-sound/27-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan's contribution)

- `src/mechanics/door.js`: `audio.playSfx("door")` in `onSuccess`
- `src/mechanics/gates.js`: `audio.playSfx("door")` in `onSuccess` (same cue, reused)
- `src/ui/mathGate.js`: `audio.playSfx("clear")` in the celebration block
- `src/mechanics/collect.js`: `audio.playSfx("pickup")` in the correct-pickup branch