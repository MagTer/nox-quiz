---
phase: 27-audio-adhd-safe-sound
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/_kenney-src/interface-sounds/*
  - assets/_kenney-src/impact-sounds/*
  - assets/_kenney-src/music-jingles/* (or assets/_opengameart-src/<track>/* fallback)
  - assets/sfx/jump.ogg
  - assets/sfx/land.ogg
  - assets/sfx/correct.ogg
  - assets/sfx/wrong.ogg
  - assets/sfx/door.ogg
  - assets/sfx/clear.ogg
  - assets/sfx/pickup.ogg
  - assets/music/ambient.ogg
  - assets/LICENSES/jump.txt
  - assets/LICENSES/land.txt
  - assets/LICENSES/correct.txt
  - assets/LICENSES/wrong.txt
  - assets/LICENSES/door-sfx.txt
  - assets/LICENSES/clear.txt
  - assets/LICENSES/pickup.txt
  - assets/LICENSES/ambient.txt
  - CREDITS.md
  - scripts/check-audio.sh
autonomous: true
requirements: [AUD-01, AUD-02, AUD-03]
must_haves:
  truths:
    - "Jump, land, pickup, correct-answer, soft-neutral-wrong, door/gate-unlock, and level-clear each have an audibly distinct, vendored CC0 sound file with a verified license proof"
    - "Calm ambient music exists as one seamless-looping OGG track, license-proven"
    - "The wrong-answer sound candidate reads as soft/neutral, never a buzzer or startle stinger"
  artifacts:
    - "assets/sfx/{jump,land,correct,wrong,door,clear,pickup}.ogg + assets/music/ambient.ogg"
    - "assets/LICENSES/{jump,land,correct,wrong,door-sfx,clear,pickup,ambient}.txt + matching CREDITS.md rows"
    - "scripts/check-audio.sh (new Wave 0 gate)"
  key_links:
    - "Every vendored asset traces to a CC0 declaration verified on its own source page (Kenney.nl or OpenGameArt.org) before conversion — mirrors the existing door.txt/spike.txt discipline"
---

<objective>
Source, vendor, and license-prove eight CC0 audio assets (7 SFX + 1 ambient music loop) for Nox Run's first-ever audio implementation, and write the new `scripts/check-audio.sh` Wave 0 gate that will later verify the whole phase's asset/license/config-key contract.

Purpose: AUD-01 requires 7 distinct SFX (jump, land, pickup, correct, soft-neutral wrong, door/gate, level-clear) each CC0 with a license proof; AUD-02 requires one seamless ambient OGG loop, also CC0-proven. This plan produces the raw material every later wiring plan (27-03, 27-04, 27-05) references by exact filename, and the static gate (27-06) later runs to green.
Output: `assets/sfx/*.ogg` (7 files), `assets/music/ambient.ogg`, one `assets/LICENSES/*.txt` proof + one `CREDITS.md` row per file, and `scripts/check-audio.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-audio-adhd-safe-sound/27-CONTEXT.md
@.planning/phases/27-audio-adhd-safe-sound/27-RESEARCH.md
@.planning/phases/27-audio-adhd-safe-sound/27-VALIDATION.md
@CREDITS.md
@assets/LICENSES/door.txt
@scripts/check-progress.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source and vendor the 7 SFX (jump, land, correct, wrong, door, clear, pickup)</name>
  <files>assets/_kenney-src/interface-sounds/*, assets/_kenney-src/impact-sounds/*, assets/sfx/jump.ogg, assets/sfx/land.ogg, assets/sfx/correct.ogg, assets/sfx/wrong.ogg, assets/sfx/door.ogg, assets/sfx/clear.ogg, assets/sfx/pickup.ogg</files>
  <read_first>.planning/phases/27-audio-adhd-safe-sound/27-RESEARCH.md (Standard Stack / Supporting — ffmpeg command shapes; Assumptions A1/A2), assets/LICENSES/door.txt (proof-file template), CREDITS.md (row format)</read_first>
  <action>
Download two confirmed-live Kenney.nl CC0 packs and extract their raw files under assets/_kenney-src/, mirroring the existing flat-layout convention already used for assets/_kenney-src/pixel-platformer/ (each pack gets its own directory containing the pack's files plus its own License.txt, flattened — no wrapping zip-root subfolder).

Interface Sounds (100 files, CC0, confirmed live 2026-07-08): download https://kenney.nl/media/pages/assets/interface-sounds/fa43c1dd4d-1677589452/kenney_interface-sounds.zip and extract into assets/_kenney-src/interface-sounds/. Source candidates for: jump (a short, soft "select"/"click"/"toggle"-style blip — NOT a harsh alarm), correct (a bright-but-soft "confirmation"/"positive" chime), wrong (a soft, neutral "error"/"cancel"/"back" tone — this is a hard requirement, not a preference: reject any candidate that reads as a buzzer, alarm, or startle stinger; it must sound calm and informational, like "that's not it" rather than "you failed"), door (a short, distinct "open"/"unlock"/"switch" cue, audibly different from the correct-chime), clear (a calm, non-fanfare "success"/"complete" cue, audibly distinct from both correct and door).

Impact Sounds (130 files, CC0, confirmed live 2026-07-08): download https://kenney.nl/media/pages/assets/impact-sounds/87b4ddecda-1677589768/kenney_impact-sounds.zip and extract into assets/_kenney-src/impact-sounds/. Source candidates for: land (a soft, brief "thud"/"step"/"land"-style impact — subtle, not punchy) and pickup (a light "pluck"/"pop"-style pickup cue, audibly distinct from the correct-chime sourced above).

After inventorying each pack's file listing and picking one candidate per role, convert each with ffmpeg into the vendored OGG Vorbis shape: trim to roughly 0.3-1.5 seconds (use the -t flag with the source's own natural length as the ceiling — do not force-trim a shorter source), apply a loudnorm normalization pass (I=-16:TP=-1.5:LRA=11), encode with the libvorbis codec at quality level 4, and write directly to assets/sfx/&lt;name&gt;.ogg (jump.ogg, land.ogg, correct.ogg, wrong.ogg, door.ogg, clear.ogg, pickup.ogg — exactly these 7 literal filenames, since later plans hardcode them in loadSound() calls).
  </action>
  <acceptance_criteria>
    - assets/_kenney-src/interface-sounds/ and assets/_kenney-src/impact-sounds/ each contain extracted pack files (non-empty directories) plus the pack's own License.txt
    - assets/sfx/jump.ogg, land.ogg, correct.ogg, wrong.ogg, door.ogg, clear.ogg, pickup.ogg all exist and are valid Ogg Vorbis files
    - Each file is under 2 seconds duration
    - The wrong.ogg candidate was selected specifically for a soft/neutral character, not an alarm/buzzer-style sound (documented in the task's own commit message or a one-line note in the SUMMARY)
  </acceptance_criteria>
  <verify>
    <automated>for f in jump land correct wrong door clear pickup; do test -f assets/sfx/$f.ogg && file assets/sfx/$f.ogg | grep -q "Ogg data" || { echo "MISSING or invalid: assets/sfx/$f.ogg"; exit 1; }; done && echo "7/7 SFX vendored"</automated>
  </verify>
  <done>All 7 SFX files exist under assets/sfx/, are valid OGG Vorbis, each under 2s, and the raw Kenney source packs are vendored under assets/_kenney-src/.</done>
</task>

<task type="auto">
  <name>Task 2: Source and vendor the ambient music loop</name>
  <files>assets/_kenney-src/music-jingles/* (or assets/_opengameart-src/&lt;track-slug&gt;/* fallback), assets/music/ambient.ogg</files>
  <read_first>.planning/phases/27-audio-adhd-safe-sound/27-RESEARCH.md (Assumptions A3, Open Question 3 — music loop seam quality), assets/LICENSES/door.txt (proof-file template for the CC0-declaration-verification discipline)</read_first>
  <action>
IMPORTANT — do not use https://kenney.nl/assets/music-loops: this URL returns 404 as of 2026-07-08 (confirmed independently twice — once during 27-RESEARCH.md's session, once during this planning pass). Kenney.nl's current Audio category (confirmed live 2026-07-08) does not list a dedicated ambient-loop pack.

First candidate: Kenney's Music Jingles pack (85 files, CC0, confirmed live 2026-07-08) — download https://kenney.nl/media/pages/assets/music-jingles/f37e530b9e-1677590399/kenney_music-jingles.zip and extract into assets/_kenney-src/music-jingles/. Inventory the file list and durations; despite the pack's name, audition for any track that reads as a calm, sustained, loop-friendly bed rather than a short triumphant/fanfare-style musical sting — most "jingle" files are likely too short (a few seconds) to work as a persistent ambient loop, so this candidate is genuinely uncertain and may fail.

Fallback (expected to be the actual path, given jingles are typically too short): use OpenGameArt.org's advanced search (https://opengameart.org/art-search-advanced) filtered to License = CC0 and Type = Music, with search terms like "calm ambient loop" or "peaceful loop". This mirrors the exact sourcing convention this project already uses for assets/spike.png, assets/goal.png, assets/coin.png, and assets/door.png (all OpenGameArt CC0 assets, credited in CREDITS.md). Before vendoring any candidate, open its OWN content page and confirm the License(s) field states exactly "CC0" (not CC-BY, not CC-BY-SA — reject anything else, mirroring CREDITS.md's own note about rejecting a CC-BY-SA coin candidate). Vendor the raw file under assets/_opengameart-src/&lt;track-slug&gt;/.

Whichever source wins, convert with ffmpeg: trim to a clean loop point if the source has dead air at the start/end (a source that already loops cleanly needs no trim), apply loudnorm (I=-20:TP=-1.5:LRA=11), encode libvorbis at quality level 5, and write to assets/music/ambient.ogg (this exact literal filename — later plans hardcode it in loadMusic()). Target roughly 15-60 seconds so the native HTMLAudioElement loop is not jarringly short.
  </action>
  <acceptance_criteria>
    - assets/music/ambient.ogg exists and is a valid Ogg Vorbis file
    - The track reads as calm/non-intense (not combat/horror/tense) by its source description or title
    - The CC0 declaration was verified on the chosen track's own source page (not inferred from a category listing) before vendoring
    - The raw source is vendored under assets/_kenney-src/music-jingles/ or assets/_opengameart-src/&lt;track-slug&gt;/
  </acceptance_criteria>
  <verify>
    <automated>test -f assets/music/ambient.ogg && file assets/music/ambient.ogg | grep -q "Ogg data" && echo "ambient.ogg vendored"</automated>
  </verify>
  <done>assets/music/ambient.ogg exists, is valid OGG Vorbis, reads as calm/ambient, and its CC0 provenance is verified from the track's own source page.</done>
</task>

<task type="auto">
  <name>Task 3: License proofs, CREDITS.md rows, and the new check-audio.sh gate</name>
  <files>assets/LICENSES/jump.txt, assets/LICENSES/land.txt, assets/LICENSES/correct.txt, assets/LICENSES/wrong.txt, assets/LICENSES/door-sfx.txt, assets/LICENSES/clear.txt, assets/LICENSES/pickup.txt, assets/LICENSES/ambient.txt, CREDITS.md, scripts/check-audio.sh</files>
  <read_first>assets/LICENSES/door.txt (exact proof-file template to mirror), CREDITS.md (exact row format), scripts/check-progress.sh (shell-gate shape: set -euo pipefail, ROOT via git rev-parse, fail(), strip_comments, existence loop, grep assertions, final PASS echo)</read_first>
  <action>
For each of the 8 new vendored files (7 SFX + ambient music), write one assets/LICENSES/&lt;name&gt;.txt proof file following door.txt's exact shape verbatim: Asset path + short description, Source pack name, Author, Source URL (the pack/track's own kenney.nl or opengameart.org page — never a mirror), Source file (the direct download/zip URL used), License (CC0), the quoted license declaration text as it literally appears on the source page, the CC0 full-text URL (https://creativecommons.org/publicdomain/zero/1.0/), a Verification line noting the exact date this session confirmed the License(s)/declaration field, and a Processing note describing the ffmpeg trim/normalize/encode step applied.

CRITICAL naming collision: the door-unlock SFX proof MUST be named door-sfx.txt, NOT door.txt — assets/LICENSES/door.txt already exists (the visual door SPRITE's proof from Phase 26) and would be silently overwritten if reused for the audio asset.

Add one row per new asset to CREDITS.md's existing Assets table (same 6-column format: Asset | File | Author | Source | License | Used for), placed after the existing rows, each cross-referencing its assets/LICENSES/ proof file by content (not by filename, matching the existing rows' style).

Write scripts/check-audio.sh, mirroring scripts/check-progress.sh's shape (shebang, set -euo pipefail, ROOT via git rev-parse --show-toplevel, a fail() helper, a strip_comments() helper). It must assert: (1) existence of all 7 assets/sfx/*.ogg files by exact name (jump/land/correct/wrong/door/clear/pickup) and assets/music/ambient.ogg; (2) existence of all 8 assets/LICENSES/*.txt proof files by exact name (jump/land/correct/wrong/door-sfx/clear/pickup/ambient); (3) that CREDITS.md contains a mention of each new asset's filename (e.g. grep -q for "jump.ogg" etc.); (4) that src/config.js contains the literal string "noxrun_mute_v1" (the mute storage key) AND that this literal is distinct from the existing save key "noxrun_platformer_v1" (a positive grep for the mute key plus a negative grep confirming the mute-key line does not also contain the save-key literal). Assertion (4) will legitimately FAIL until Plan 27-02 lands CONFIG.AUDIO — same "real gate, expected red until a later wave" pattern scripts/check-progress.sh's own header already documents for this project; do not treat that as a bug in this task.
  </action>
  <acceptance_criteria>
    - 8 new assets/LICENSES/*.txt files exist, one per vendored asset, each including a Source URL, quoted CC0 declaration, and verification date
    - assets/LICENSES/door-sfx.txt exists as a NEW file; assets/LICENSES/door.txt is byte-unchanged (git diff shows no modification to the pre-existing file)
    - CREDITS.md has one new row per vendored asset
    - scripts/check-audio.sh exists, is valid bash syntax, and its assertions reference the exact 7 SFX names + ambient.ogg + all 8 license proof filenames + the noxrun_mute_v1 literal
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/check-audio.sh && for f in jump land correct wrong door-sfx clear pickup ambient; do test -f assets/LICENSES/$f.txt || { echo "MISSING proof: $f.txt"; exit 1; }; done && (git diff --name-only -- assets/LICENSES/door.txt | grep -q . && { echo "door.txt was modified — must stay untouched"; exit 1; } || echo "license proofs + check-audio.sh syntax OK")</automated>
  </verify>
  <done>All 8 license proof files exist (door-sfx.txt distinct from the pre-existing door.txt), CREDITS.md lists all 8 new assets, and scripts/check-audio.sh exists with valid syntax and the 4 required assertion classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Kenney.nl / OpenGameArt.org (source pages) -> assets/_kenney-src/ / assets/_opengameart-src/ | Build-time-only trust boundary: raw third-party binary audio files enter the repo, must be license-verified on their own source page before vendoring |
| assets/_kenney-src/ / assets/_opengameart-src/ -> assets/sfx/ / assets/music/ (ffmpeg conversion) | Vendored raw source is transformed into the shipped asset; the shipped file is derived data, never re-fetched at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-SC | Tampering (supply-chain / asset provenance) | assets/_kenney-src/, assets/_opengameart-src/ (build-time vendoring) | high | mitigate | Verify the CC0 declaration on each source asset's OWN page (never a mirror) before vendoring — Task 1/2 name the exact confirmed-live pack/track URLs; write one assets/LICENSES/*.txt proof + one CREDITS.md row per file (Task 3) before any phase-close claim, mirroring the existing door.txt/spike.txt discipline exactly |
| T-27-04 | Information Disclosure / Elevation | n/a | low | accept | This plan performs only one-time, build-time asset downloads over HTTPS from established source domains already used elsewhere in this project (kenney.nl, opengameart.org) — no runtime network calls, no user input, no auth surface (27-RESEARCH.md Security Domain) |

</threat_model>

<verification>
Run `bash -n scripts/check-audio.sh` (syntax only — the script's full run is EXPECTED RED until Plan 27-02 lands CONFIG.AUDIO; do not run it for a green result yet). Confirm all 7 assets/sfx/*.ogg + assets/music/ambient.ogg exist and are valid Ogg Vorbis via `file`. Confirm 8 new assets/LICENSES/*.txt files exist and assets/LICENSES/door.txt is untouched via `git diff --name-only`.
</verification>

<success_criteria>
- 7 SFX + 1 ambient music track vendored as valid OGG Vorbis under assets/sfx/ and assets/music/
- Every vendored asset has a verified CC0 proof file under assets/LICENSES/ and a CREDITS.md row
- scripts/check-audio.sh exists, is syntactically valid, and encodes all 4 required assertion classes (asset existence, proof existence, CREDITS mention, mute-key-distinct-from-save-key)
- assets/LICENSES/door.txt (the pre-existing visual door sprite proof) is byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/27-audio-adhd-safe-sound/27-01-SUMMARY.md` when done. Record: which exact Kenney/OpenGameArt files were picked for each of the 7 SFX roles + the ambient track, and whether Kenney's Music Jingles pack or the OpenGameArt fallback supplied the ambient loop.
</output>

## Artifacts this phase produces (this plan's contribution)

- `assets/sfx/jump.ogg`, `assets/sfx/land.ogg`, `assets/sfx/correct.ogg`, `assets/sfx/wrong.ogg`, `assets/sfx/door.ogg`, `assets/sfx/clear.ogg`, `assets/sfx/pickup.ogg`
- `assets/music/ambient.ogg`
- `assets/LICENSES/jump.txt`, `land.txt`, `correct.txt`, `wrong.txt`, `door-sfx.txt`, `clear.txt`, `pickup.txt`, `ambient.txt`
- 8 new rows in `CREDITS.md`
- `scripts/check-audio.sh` (new Wave 0 gate — asset existence, license-proof existence, CREDITS mention, mute-key-distinct-from-save-key assertions)