---
phase: 24-fix-lengthen-levels-1-4
plan: 06
type: execute
wave: 3
depends_on: ["24-05"]
files_modified:
  - .planning/phases/24-fix-lengthen-levels-1-4/24-FINDINGS.md
  - .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
  - .planning/phases/23-level-validation-harness/23-FINDINGS.md
autonomous: true
requirements: [VALID-04, LVL-01]
must_haves:
  truths:
    - "node scripts/audit-phase21-mechanics.mjs drives all 4 lengthened levels start-to-goal through the select screen, and every mechanic encounter (old + new) reports triggered:true — the CONTEXT.md-locked non-negotiable acceptance bar (resolved:true is the goal but not blocking, per the pre-existing documented resolution-timing flakiness)"
    - "24-FINDINGS.md exists and documents: the 3 over-hole + 8 platform defect fixes with before/after validator evidence, the per-level extension rationale (new mechanic instance counts and why), the interactive audit's triggered/resolved results, and the smoke re-baseline confirmation"
    - "22-FINDINGS.md and 23-FINDINGS.md each carry a brief 'resolved in Phase 24' cross-reference note appended (not rewritten) pointing at 24-FINDINGS.md"
  artifacts:
    - .planning/phases/24-fix-lengthen-levels-1-4/24-FINDINGS.md
  key_links:
    - "scripts/audit-phase21-mechanics.mjs's deriveEncounters -> the new mathGate/spike/door/enemy instances added by Wave 1 (auto-discovered, zero script changes needed)"
    - "24-FINDINGS.md -> 22-FINDINGS.md / 23-FINDINGS.md (historical record, cross-referenced not rewritten, per CONTEXT.md's locked Verification Evidence decision)"
---

<objective>
Re-run the interactive mechanic audit (unmodified) against all 4 now-lengthened, now-fixed levels, confirm every mechanic encounter (old + new) is genuinely triggered, and write `24-FINDINGS.md` as this phase's own evidence artifact — closing out Phase 24 with the same interactive-proof rigor Phase 21 established and Phase 22/23 continued.

Purpose: CONTEXT.md's success criterion 4 requires "the upgraded interactive audit drives each lengthened level start->goal with mechanic encounters resolved" — this is the phase's final proof step, run only after Wave 1 (fixes+extensions) and Wave 2 (smoke re-baseline + full-suite gate) are both green.
Output: `24-FINDINGS.md` (new evidence doc) plus brief cross-reference notes appended to `22-FINDINGS.md` and `23-FINDINGS.md` pointing readers at Phase 24's resolution of the defects those two docs inventoried.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-CONTEXT.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-RESEARCH.md
@.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
@.planning/phases/23-level-validation-harness/23-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the interactive mechanic audit and confirm every encounter is triggered</name>
  <files>none (verification-only — audit-phase21-mechanics.mjs is reused unmodified per CONTEXT.md's Reusable Assets decision, no code changes)</files>
  <read_first>
    scripts/audit-phase21-mechanics.mjs (full file — confirms it drives all 4 levels via LEVEL_ORDER automatically, no per-level args needed, always exits 0)
    scripts/lib/mechanic-drive.mjs (deriveEncounters — confirms new mechanics are auto-discovered by x-sort, zero script changes needed)
    .planning/phases/24-fix-lengthen-levels-1-4/24-CONTEXT.md ("Verification Evidence & Smoke Re-Baselining" section — the exact triggered:true-is-non-negotiable / resolved:true-is-the-goal-but-not-blocking acceptance bar)
  </read_first>
  <action>
    Run `node scripts/audit-phase21-mechanics.mjs`, capturing its full stdout (the JSON `results` array plus the final `AUDIT:` line) to a scratch file for this task's evidence.
    Per CONTEXT.md's locked acceptance bar: the run PASSES this task's gate if and only if every entry in the `results` array has `"triggered": true` — do not block on `resolved:false` rows (the audit script's own printed "AUDIT: FAILURES DETECTED" line may still appear due to resolution-timing flakiness on encounters that WERE triggered; that is an acceptable, pre-existing, documented condition per Phase 21/22/23's own findings, not a Phase 24 regression).
    If any row reports `"triggered": false`, that is a genuine Phase 24 regression (the encounter — old or new — is not being reached) and must be diagnosed against the specific level's geometry (re-check the relevant Wave 1 plan's fix/extension for a placement error) before this task can be marked done — do not weaken the audit or the acceptance bar to work around a real triggered:false.
    Record the count of encounters per level (old + new) and the count with `resolved:false` (if any) for use in Task 2's `24-FINDINGS.md`.
  </action>
  <acceptance_criteria>
    `node scripts/audit-phase21-mechanics.mjs`'s printed JSON `results` array contains zero entries with `"triggered": false`.
    The results array's total entry count across all 4 levels is greater than the pre-Phase-24 baseline (16 encounters per 23-FINDINGS.md's retry-harness table) — proving the new mechanic instances (1 per level-01/02, 2 per level-03, 2 per level-04) are present and being driven.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/audit-phase21-mechanics.mjs > /tmp/nox-24-audit.json 2>&1; F=$(grep -c '"triggered": false' /tmp/nox-24-audit.json); echo "triggered:false count: $F"; test "$F" = "0"; T=$(grep -c '"triggered": true' /tmp/nox-24-audit.json); echo "triggered:true count: $T"; test "$T" -gt "16"</automated>
  </verify>
  <done>node scripts/audit-phase21-mechanics.mjs run against all 4 lengthened levels; every mechanic encounter (old + new) reports triggered:true; total triggered count exceeds the pre-Phase-24 baseline of 16, confirming new mechanics are being exercised.</done>
</task>

<task type="auto">
  <name>Task 2: Write 24-FINDINGS.md and append cross-reference notes to 22-FINDINGS.md and 23-FINDINGS.md</name>
  <files>
    .planning/phases/24-fix-lengthen-levels-1-4/24-FINDINGS.md
    .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
    .planning/phases/23-level-validation-harness/23-FINDINGS.md
  </files>
  <read_first>
    /tmp/nox-24-audit.json (Task 1's captured audit output, if still present in this session — otherwise re-run Task 1's command)
    .planning/phases/23-level-validation-harness/23-FINDINGS.md (tail section — the exact append point and citation style to mirror)
    .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md (tail section — the exact append point and citation style to mirror)
  </read_first>
  <action>
    Create `24-FINDINGS.md` (new file, Claude's discretion for internal structure per CONTEXT.md) documenting: (a) the 3 over-hole gate fixes with before/after `validate-levels.mjs` row evidence (level-01 x600→528, x1300→1360; level-04 x1800→1728); (b) the 8 unreachable-platform fixes with before/after evidence, including the direct `bfsReachableSet` query proof captured by Plans 24-03/24-04's Task 1 `<verify>` output (paste the printed JSON reachability results for both levels); (c) each level's extension rationale — final length/goal-x, new mechanic-instance counts and which existing type each reuses (level-01: +1 mathGate, +1 spike; level-02: +1 mathGate, +1 spike; level-03: +1 enemy, +1 mathGate, +2 spikes; level-04: +1 door, +1 mathGate, +2 spikes) — and why (per 24-RESEARCH.md's Open Question 1 recommendation: at least one new instance of an existing type per level, for audit coverage, without introducing new mechanic variety); (d) Task 1's full interactive-audit results summary (triggered/resolved counts per level, any resolved:false rows named explicitly with a note that this is the pre-existing documented flakiness, not a new regression); (e) the final `node scripts/validate-levels.mjs` / `node scripts/smoke-progress.mjs` / `bash scripts/check-progress.sh` PASS confirmation from Plan 24-05's Task 2.
    Using `Edit` (never `Write`, to avoid clobbering existing content), append a brief note to the end of `22-FINDINGS.md` reading approximately: "**Resolved in Phase 24:** the 3 over-hole defects and 8 unreachable-platform candidates this document inventoried (Structural Defect Inventory section) are fixed — see `.planning/phases/24-fix-lengthen-levels-1-4/24-FINDINGS.md` for before/after evidence."
    Using `Edit`, append a similar brief note to the end of `23-FINDINGS.md` reading approximately: "**Resolved in Phase 24:** the 3 confirmed over-hole defects and all 8 individually-arbitrated unreachable platforms (section (e)) this document's RED-first proof named are fixed — see `.planning/phases/24-fix-lengthen-levels-1-4/24-FINDINGS.md` for before/after evidence and the direct BFS-reachability proof for the platforms."
    Do not rewrite or remove any existing content in either file — these are pure appends, per CONTEXT.md's locked "remain historical record" decision.
  </action>
  <acceptance_criteria>
    `24-FINDINGS.md` exists and contains all 5 sections (a) through (e) described above, each with concrete before/after data (not vague summaries).
    `22-FINDINGS.md`'s file length (line count) is strictly greater than its pre-Phase-24 length, and its new final lines contain the literal string "Resolved in Phase 24" plus a path reference to `24-FINDINGS.md`.
    `23-FINDINGS.md`'s file length is strictly greater than its pre-Phase-24 length, and its new final lines contain the literal string "Resolved in Phase 24" plus a path reference to `24-FINDINGS.md`.
    A `git diff` on both `22-FINDINGS.md` and `23-FINDINGS.md` shows ONLY appended lines (no deletions, no modifications to existing lines).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/24-fix-lengthen-levels-1-4/24-FINDINGS.md && echo "24-FINDINGS.md exists"; grep -c "Resolved in Phase 24" .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md; grep -c "Resolved in Phase 24" .planning/phases/23-level-validation-harness/23-FINDINGS.md; git diff --stat .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md .planning/phases/23-level-validation-harness/23-FINDINGS.md</automated>
  </verify>
  <done>24-FINDINGS.md written with full before/after evidence for all defect fixes, extension rationale, and audit results; 22-FINDINGS.md and 23-FINDINGS.md each carry an appended (not rewritten) cross-reference note pointing at 24-FINDINGS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Read-only interactive-audit run against the local, offline, single-player client (served on loopback-only 127.0.0.1:8768 by the pre-existing, already-reviewed audit script) plus documentation-only file appends. No network input from an untrusted source, no auth, no session, no access control, no crypto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | Appends to 22-FINDINGS.md / 23-FINDINGS.md | low | mitigate | Edit tool (scoped append) is used, never Write (whole-file overwrite); acceptance criteria explicitly requires a `git diff --stat` showing only additions, no deletions/modifications to historical record |
| T-24-02 | Repudiation | The "every mechanic encounter triggered:true" claim | low | mitigate | Task 1's acceptance criteria requires the literal captured JSON output (grep-counted, not eyeballed) to show zero `triggered: false` entries, and the total count exceeding the documented 16-encounter pre-Phase-24 baseline, proving new mechanics are actually exercised, not just present in the file |

## Package Legitimacy Audit

Not applicable — this plan installs zero new packages.
</threat_model>

<verification>
Task 1's `node scripts/audit-phase21-mechanics.mjs` run is the final interactive proof required by ROADMAP Phase 24 success criterion 4. Task 2's `24-FINDINGS.md` is the phase's own evidence artifact, cross-referenced from (not overwriting) `22-FINDINGS.md`/`23-FINDINGS.md` per CONTEXT.md's locked "historical record" decision.
</verification>

<success_criteria>
- `node scripts/audit-phase21-mechanics.mjs` shows zero `triggered:false` encounters across all 4 lengthened levels, with a higher total encounter count than the pre-Phase-24 16-encounter baseline.
- `24-FINDINGS.md` exists with concrete before/after evidence for all 11 structural defect fixes (3 gates + 8 platforms), the 4 levels' extension rationale, and the audit results.
- `22-FINDINGS.md` and `23-FINDINGS.md` each carry an appended cross-reference note; `git diff --stat` on both shows additions only.
</success_criteria>

<output>
Create `.planning/phases/24-fix-lengthen-levels-1-4/24-06-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `.planning/phases/24-fix-lengthen-levels-1-4/24-FINDINGS.md` — new: phase evidence doc (defect fixes, extension rationale, audit results) (VALID-04/LVL-01)
- `.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md` — edited: appended "Resolved in Phase 24" cross-reference note
- `.planning/phases/23-level-validation-harness/23-FINDINGS.md` — edited: appended "Resolved in Phase 24" cross-reference note
