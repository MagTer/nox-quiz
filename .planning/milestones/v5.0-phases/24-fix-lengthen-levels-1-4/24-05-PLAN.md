---
phase: 24-fix-lengthen-levels-1-4
plan: 05
type: execute
wave: 2
depends_on: ["24-01", "24-02", "24-03", "24-04"]
files_modified:
  - scripts/smoke-progress.mjs
autonomous: true
requirements: [VALID-04, LVL-01]
must_haves:
  truths:
    - "scripts/smoke-progress.mjs's 4 geometry-pinning blocks (level-01 through level-04) are consciously re-baselined to the post-Phase-24 geometry, each with the OLD pre-Phase-24 values retained in a comment (never silently overwritten)"
    - "node scripts/smoke-progress.mjs exits 0 (PASS) against all 4 levels' new, longer, fixed geometry"
    - "node scripts/validate-levels.mjs exits 0 with zero total HARD-FAILs across all 4 levels — the phase's primary VALID-04 acceptance evidence"
  artifacts:
    - scripts/smoke-progress.mjs
  key_links:
    - "scripts/smoke-progress.mjs's 4 expectedGeometry literals -> the 4 src/levels/level-0N.js descriptors (must deep-equal exactly, byte-for-byte, post-Wave-1)"
    - "node scripts/validate-levels.mjs / node scripts/smoke-progress.mjs / bash scripts/check-progress.sh -> the phase-gate 'full suite green' acceptance bar defined in 24-VALIDATION.md"
---

<objective>
Re-baseline all 4 of `scripts/smoke-progress.mjs`'s geometry-pinning blocks to the post-Phase-24 (fixed + extended) geometry landed by Wave 1's 4 parallel level plans (24-01 through 24-04), then confirm the phase's structural-soundness gates are green in one combined run.

Purpose: CONTEXT.md explicitly requires the smoke fixture be "consciously re-baselined, not deleted" with old values retained for traceability — the fixture exists precisely to catch accidental future geometry drift, so it must track the new v5.0 baseline going forward. This is Wave 2 because it needs ALL 4 levels' FINAL committed geometry, not just one.
Output: `scripts/smoke-progress.mjs` with all 4 `expectedGeometry` blocks updated to match the new descriptors exactly, each preceded by a comment retaining the old pre-Phase-24 values and explaining why the pin changed (Phase 24 lengthening + defect fixes).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-CONTEXT.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-baseline all 4 smoke-progress.mjs geometry-pinning blocks</name>
  <files>scripts/smoke-progress.mjs</files>
  <read_first>
    scripts/smoke-progress.mjs (full file — 4 independent deep-equal blocks: level-01 lines 282-349, level-02 lines 351-414, level-03 lines 416-490, level-04 lines 492-577, per 24-RESEARCH.md's line map — re-read fresh since Wave 1 may have shifted exact line numbers slightly)
    src/levels/level-01.js, src/levels/level-02.js, src/levels/level-03.js, src/levels/level-04.js (the Wave-1-committed FINAL state — the authoritative source of the new `expectedGeometry` values; do not hand-retype from this plan's numbers, copy from the actual committed files since Wave 1 may have iterated a y/x value slightly in response to validate-levels.mjs)
    .planning/phases/24-fix-lengthen-levels-1-4/24-RESEARCH.md ("smoke-progress.mjs Re-Baseline Map" section — the exact re-baseline comment convention to apply)
  </read_first>
  <action>
    For EACH of the 4 blocks (level-01, level-02, level-03, level-04), in order:
    First, capture the block's CURRENT `expectedGeometry` object literal (the pre-Phase-24 value already in the file) — this is the OLD value to preserve.
    Second, insert a comment immediately above the `const expectedGeometry = {` line reading "// Phase 24 re-baseline: level-0N extended + structural fixes applied (VALID-04/LVL-01)." followed by "// OLD (pre-Phase-24, v4.1) values, retained for historical traceability:" followed by one comment line per top-level key that actually changed (floors/platforms/coins/spikes/goal/checkpoints/mathGates/doors/enemies as applicable per level — answerPickupSlots and any key that did NOT change do not need an OLD line), each formatted as `//   <key>: <old array/object literal>` mirroring the exact convention in 24-RESEARCH.md's "smoke-progress.mjs Re-Baseline Map" section.
    Third, replace the `expectedGeometry` object's contents with the new geometry, copied byte-for-byte from the corresponding `getLevel("level-0N").geometry` value (i.e., from the now-final `src/levels/level-0N.js` file read above) — every key (floors, platforms, coins, spikes, goal, checkpoints, doors, mathGates, enemies, collectZones, answerPickupSlots), not just the changed ones, must be present and exactly match the descriptor.
    Level-01's OLD values (for the comment, per CONTEXT.md's explicitly-named fixture): floors `[{x:0,w:560},{x:720,w:480},{x:1360,w:880}]`; mathGates `[{x:600,y:FLOOR_Y-CONFIG.MATH_GATE.H},{x:1300,y:FLOOR_Y-CONFIG.MATH_GATE.H}]`; goal `{x:2160,y:FLOOR_Y-CONFIG.GOAL_SIZE}`; coins/spikes/checkpoints/platforms as currently shown in the level-01 block (lines 290-344) before this edit — capture them verbatim before overwriting.
    Do not change anything else in the file (the SAVE-01 through SAVE-07/LVL-02/LVL-01 assertion blocks above and below the 4 geometry blocks, and the `deepEqual`/`check` helpers, are untouched).
  </action>
  <acceptance_criteria>
    All 4 blocks' `expectedGeometry` objects deep-equal their corresponding `getLevel("level-0N").geometry` exactly (verified by running the script itself, since its own `check(deepEqual(...))` assertions are the exact test).
    Each of the 4 blocks has a preceding comment block containing the literal string "Phase 24 re-baseline" and "OLD (pre-Phase-24".
    `node scripts/smoke-progress.mjs` exits 0 and prints `smoke-progress: PASS`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/smoke-progress.mjs; echo "exit: $?"; C=$(grep -c 'Phase 24 re-baseline' scripts/smoke-progress.mjs); echo "re-baseline comment count: $C"; test "$C" = "4"</automated>
  </verify>
  <done>All 4 smoke-progress.mjs geometry blocks re-baselined to the post-Phase-24 descriptors with old values retained in a comment; node scripts/smoke-progress.mjs exits 0 (PASS).</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the phase-gate full suite is green and LOCKED surfaces are untouched</name>
  <files>none (verification-only — no files modified by this task)</files>
  <read_first>
    .planning/phases/24-fix-lengthen-levels-1-4/24-VALIDATION.md ("Full suite command" and "Phase gate" rows)
    .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md ("LOCKED-surface proof" section — the exact git diff --quiet convention this task mirrors)
  </read_first>
  <action>
    Run the full structural/regression suite in sequence: `node scripts/validate-levels.mjs`, `node scripts/smoke-progress.mjs`, `bash scripts/check-progress.sh`. All three must exit 0.
    Confirm `node scripts/validate-levels.mjs` prints `validate-levels: PASS` with zero total hard-failures across all 4 levels (down from the pre-Phase-24 baseline's 9 named HARD-FAIL rows, per 23-FINDINGS.md's corrected count, plus the 8 individually-arbitrated platform defects fixed by Wave 1).
    Confirm the LOCKED surfaces are untouched by this phase, mirroring 22-FINDINGS.md's own convention: `git diff --quiet <phase-22-baseline-commit> -- src/math` and `git diff --quiet <phase-22-baseline-commit> -- lib/kaplay.mjs` (use the same baseline commit SHA `5eedee870d314307a846bae254f61e7d1e0ef5f4` cited in 22-FINDINGS.md/23-FINDINGS.md) must both exit 0.
    This task produces no file changes — it is the phase-gate confirmation step whose console output feeds directly into Plan 24-06's `24-FINDINGS.md` evidence doc.
  </action>
  <acceptance_criteria>
    `node scripts/validate-levels.mjs` exits 0 and prints `validate-levels: PASS`.
    `node scripts/smoke-progress.mjs` exits 0 and prints `smoke-progress: PASS`.
    `bash scripts/check-progress.sh` exits 0.
    `git diff --quiet 5eedee870d314307a846bae254f61e7d1e0ef5f4 -- src/math` exits 0 (brain LOCKED, untouched).
    `git diff --quiet 5eedee870d314307a846bae254f61e7d1e0ef5f4 -- lib/kaplay.mjs` exits 0 (vendored engine untouched).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/validate-levels.mjs && node scripts/smoke-progress.mjs && bash scripts/check-progress.sh && echo "FULL SUITE: PASS"; git diff --quiet 5eedee870d314307a846bae254f61e7d1e0ef5f4 -- src/math && echo "src/math: LOCKED, clean"; git diff --quiet 5eedee870d314307a846bae254f61e7d1e0ef5f4 -- lib/kaplay.mjs && echo "lib/kaplay.mjs: LOCKED, clean"</automated>
  </verify>
  <done>Full structural/regression suite (validate-levels.mjs + smoke-progress.mjs + check-progress.sh) is green in one run; src/math and lib/kaplay.mjs confirmed byte-identical to the Phase 22 baseline commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure local test-fixture edits (geometry-pinning literals) and read-only verification commands. No network input, no auth, no session, no access control, no crypto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | scripts/smoke-progress.mjs's 4 expectedGeometry literals | low | mitigate | Values must be copied verbatim from the same-commit level descriptor (not hand-retyped); a transcription error is fail-closed — the deepEqual assertion itself would fail immediately, caught by Task 1's own `<verify>` before the plan can be marked done |
| T-24-02 | Repudiation | The "zero HARD-FAILs" phase-gate claim (VALID-04's primary acceptance evidence) | low | mitigate | Task 2 requires the literal printed output `validate-levels: PASS`, not just a non-zero exit code inference, plus the explicit LOCKED-surface git diff proof mirroring 22/23-FINDINGS.md's own evidentiary convention |

## Package Legitimacy Audit

Not applicable — this plan installs zero new packages.
</threat_model>

<verification>
Task 1's own `node scripts/smoke-progress.mjs` run is the exact test of correctness (a deepEqual mismatch fails loudly with the specific offending assertion message). Task 2's full-suite run is the phase-gate confirmation that VALID-04's "validator passes green" success criterion is met across all 4 levels simultaneously, not just level-by-level in Wave 1.
</verification>

<success_criteria>
- All 4 `scripts/smoke-progress.mjs` geometry blocks re-baselined with old values retained in comments; `node scripts/smoke-progress.mjs` exits 0.
- `node scripts/validate-levels.mjs` exits 0 with zero total HARD-FAILs across all 4 levels.
- `bash scripts/check-progress.sh` exits 0.
- `src/math` and `lib/kaplay.mjs` confirmed byte-identical to the Phase 22 baseline commit (LOCKED surfaces untouched).
</success_criteria>

<output>
Create `.planning/phases/24-fix-lengthen-levels-1-4/24-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `scripts/smoke-progress.mjs` — edited: all 4 geometry-pinning blocks (level-01 through level-04) re-baselined to post-Phase-24 geometry, old values retained in comments (VALID-04/LVL-01)
