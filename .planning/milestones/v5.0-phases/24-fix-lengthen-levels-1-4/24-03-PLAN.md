---
phase: 24-fix-lengthen-levels-1-4
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/levels/level-03.js
autonomous: true
requirements: [VALID-04, LVL-01]
must_haves:
  truths:
    - "level-03's 2 known unreachable platforms (x:1880, x:2640) are now BFS-reachable from spawn — proven by directly querying scripts/lib/reachability.mjs's buildNodes/buildGraph/bfsReachableSet, not just by absence of a named validate-levels.mjs row (these platforms never appear as named HARD-FAIL rows per 23-FINDINGS.md section (e), so absence of a row is not sufficient proof)"
    - "level-03 extends from goal x:3320 to x:5120 (+1800px, 52.9% longer than the v4.1 3400px extent) with every new floor/platform/coin/spike/enemy/mathGate/checkpoint appended strictly after x:3400; bounds.right is bumped from 3400 to 5200"
    - "every new hazard/mechanic in the extension has a checkpoint at FLOOR_Y-48 exactly 80px before it; doors stay empty, matching level-03's established identity"
  artifacts:
    - src/levels/level-03.js
  key_links:
    - "level-03.js platform y-values -> scripts/lib/reachability.mjs's canReach (must find a feasible incoming edge from a neighboring floor within JUMP_ENVELOPE.maxRise)"
    - "level-03.js bounds.right -> src/scenes/game.js's `level.bounds ?? {...dynamic}` short-circuit (level-03 HAS an explicit bounds field)"
    - "level-03.js geometry -> scripts/validate-levels.mjs (structural gate, must show zero level-03 HARD-FAIL rows)"
---

<objective>
Fix level-03's 2 known unreachable platforms (VALID-04) by lowering their y until they fall within the calibrated jump envelope's reach, and extend the level 52.9% past its v4.1 length with new floors/platforms/coins/hazards/mechanics/checkpoints appended strictly after the existing kid-validated geometry (LVL-01).

Purpose: level-03 is the third of 4 levels this phase must make structurally sound and noticeably longer. Its 2 defect platforms are narrow-overlap-window cases (only ~40px of shared x-span with their neighboring floor run) that need a more conservative rise than level-04's wider-window platforms — see 24-RESEARCH.md's Pitfall 3.
Output: `src/levels/level-03.js` with 2 platforms lowered (y:184→260, y:192→260), 2 new floor runs, 2 new bridging platforms, 6 new coins, 2 new spikes, 1 new enemy, 1 new mathGate, 4 new checkpoints, a goal moved to x:5120, and `bounds.right` bumped from 3400 to 5200.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-CONTEXT.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-RESEARCH.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix level-03's 2 unreachable platforms and append the new geometry skeleton</name>
  <files>src/levels/level-03.js</files>
  <read_first>
    src/levels/level-03.js (full file — bounds line 16, floors 19-25, platforms 27-35, goal 60)
    src/config.js (CONFIG.FLOOR_Y=320, CONFIG.GOAL_SIZE)
    scripts/lib/jump-envelope.mjs (JUMP_ENVELOPE.maxRise=88.331)
    .planning/phases/24-fix-lengthen-levels-1-4/24-RESEARCH.md ("Structural Defect Fix Targets" — "Unreachable platforms" table and the "Physics basis" paragraph; the two level-03 platforms have only a 40px overlap window with their neighboring floor, requiring rise <= ~60-65px, not the wider 65-75px band that applies to level-04's platforms)
  </read_first>
  <action>
    In the `platforms` array, change the entry `{ x: 1880, y: 184, w: 128, h: 24 }` to `{ x: 1880, y: 260, w: 128, h: 24 }` (rise reduced from 136px to 60px — within JUMP_ENVELOPE.maxRise 88.331 AND within the narrow 40px overlap window with floor-2's 1320..1920 span that 24-RESEARCH.md's physics computation proves comfortably passes at rise<=65px, marginRatio≈0.238). Do not change x or w — CONTEXT.md locks "preserving its x-position/visual role," only y changes.
    Change the entry `{ x: 2640, y: 192, w: 128, h: 24 }` to `{ x: 2640, y: 260, w: 128, h: 24 }` (rise reduced from 128px to 60px — same narrow-window physics as the x:1880 platform, since it also overlaps its neighbor floor-3's 2040..2680 span by exactly 40px). Do not change x or w.
    Update each platform's inline comment to note the Phase 24 rise reduction and cite VALID-04 (e.g. "Phase 24: y lowered 184->260 (rise 136px->60px) — was unreachable, VALID-04").
    Do not touch `floors`, `doors`, `mathGates`, `enemies`, `collectZones`, `answerPickupSlots`, or any existing `coins`/`spikes`/`checkpoints` entries in this task.
    Append exactly 2 new entries to the `floors` array (after the existing `{ x: 2840, w: 560 }`, which ends at 3400): `{ x: 3560, w: 600 }` and `{ x: 4280, w: 920 }`. Comment each as a Phase 24 extension run.
    Append exactly 2 new entries to the `platforms` array: `{ x: 3400, y: 255, w: 128, h: 24 }` (bridges the new 3400..3560 gap, touching floor-5's end at 3400 exactly — rise 65px) and `{ x: 4160, y: 255, w: 112, h: 24 }` (bridges the new 4160..4280 gap, touching the new floor-6's end at 4160 exactly — same 65px rise; these are wide "touching" windows, not the narrow 40px overlap case, so 65px rise carries a comfortable margin per 24-RESEARCH.md's physics).
    Move `goal.x` from 3320 to 5120 (keep `y: FLOOR_Y - CONFIG.GOAL_SIZE`). The new floor-7 ends at 5200, giving an 80px buffer, matching the original's own 3400-3320=80px convention.
    Change `bounds.right` from 3400 to 5200 (keep `left: 0, top: 0, bottom: 360` unchanged) — level-03's explicit `bounds` field is used AS-IS by `src/scenes/game.js`, so this manual bump is required.
  </action>
  <acceptance_criteria>
    The `platforms` array's entry with x:1880 has y:260 (not 184); the entry with x:2640 has y:260 (not 192). Both w values remain 128.
    The `floors` array has exactly 7 entries; the 6th is `{ x: 3560, w: 600 }` and the 7th is `{ x: 4280, w: 920 }`.
    The `platforms` array has exactly 9 entries; the 8th is `{ x: 3400, y: 255, w: 128, h: 24 }` and the 9th is `{ x: 4160, y: 255, w: 112, h: 24 }`.
    `goal.x` equals 5120. `bounds.right` equals 5200.
    `node scripts/validate-levels.mjs` prints zero lines matching `^level-03 .*HARD-FAIL`.
    Both fixed platform nodes (x:1880 and x:2640) are present in the BFS-reachable set from spawn, confirmed by directly querying `scripts/lib/reachability.mjs`'s exported `buildNodes`/`buildGraph`/`bfsReachableSet`/`nodeContaining` against `getLevel("level-03").geometry` and `JUMP_ENVELOPE` — this mirrors 23-FINDINGS.md section (e)'s own "individually arbitrated" evidence-gathering method, since these platforms never appear as named rows in validate-levels.mjs's standard 3-check CLI output.
  </acceptance_criteria>
  <verify>
    <automated>N=$(node scripts/validate-levels.mjs 2>&1 | grep '^level-03 ' | grep -c 'HARD-FAIL'); echo "level-03 HARD-FAILs: $N"; test "$N" = "0"; node --input-type=module -e "
import { getLevel } from './src/levels/index.js';
import { buildNodes, buildGraph, bfsReachableSet, nodeContaining, SPAWN_X } from './scripts/lib/reachability.mjs';
import { JUMP_ENVELOPE } from './scripts/lib/jump-envelope.mjs';
const geometry = getLevel('level-03').geometry;
const nodes = buildNodes(geometry);
const graph = buildGraph(nodes, JUMP_ENVELOPE);
const spawn = nodeContaining(nodes, SPAWN_X);
const reachable = bfsReachableSet(graph, spawn.id);
const targets = [1880, 2640];
const results = targets.map((x) => {
  const n = nodes.find((n) => n.id.startsWith('platform-') && n.xStart === x);
  return [x, Boolean(n && reachable.has(n.id))];
});
console.log(JSON.stringify(results));
process.exit(results.every(([, ok]) => ok) ? 0 : 1);
"</automated>
  </verify>
  <done>Both known unreachable platforms lowered into reach (y:260, rise 60px); 2 new floor runs + 2 new bridging platforms appended after x:3400; goal moved to x:5120; bounds.right bumped to 5200; validate-levels.mjs shows zero level-03 HARD-FAIL rows; the reachability query confirms both platforms are in the spawn-reachable set.</done>
</task>

<task type="auto">
  <name>Task 2: Add level-03's extension coins, hazards, enemy, mechanic, and checkpoints</name>
  <files>src/levels/level-03.js</files>
  <read_first>
    src/levels/level-03.js (post-Task-1 state — re-read after Task 1's edits land)
    src/config.js (CONFIG.SPIKE_SIZE, CONFIG.MATH_GATE.H, CONFIG.ENEMY.H)
    .planning/phases/24-fix-lengthen-levels-1-4/24-PATTERNS.md ("doors: [] convention" and "Enemy + collectZone pattern" sections for level-03)
  </read_first>
  <action>
    Append exactly 6 new entries to the `coins` array (matching level-03's existing ~1-per-309px density): `{ x: 3440, y: 264 }`, `{ x: 3640, y: 184 }`, `{ x: 3900, y: 128 }`, `{ x: 4200, y: 264 }`, `{ x: 4460, y: 176 }`, `{ x: 4900, y: 264 }`.
    Append exactly 2 new entries to the `spikes` array: `{ x: 3680, y: FLOOR_Y - CONFIG.SPIKE_SIZE }` (on the new floor-5, 3560..4160 span) and `{ x: 4800, y: FLOOR_Y - CONFIG.SPIKE_SIZE }` (on the new floor-6, 4280..5200 span).
    Append exactly 1 new entry to the `enemies` array (in addition to the 1 already present): `{ x: 3800, y: FLOOR_Y - CONFIG.ENEMY.H }` (on floor-5, 3560..4160 span) — level-03 already has an enemy encounter, so this reuses an existing mechanic type.
    Append exactly 1 new entry to the `mathGates` array (in addition to the 1 already present): `{ x: 4360, y: FLOOR_Y - CONFIG.MATH_GATE.H }` (on floor-6, 4280..5200 span).
    Append exactly 4 new entries to the `checkpoints` array, clustered before the new back-to-back hazards mirroring level-03's own established "cluster before back-to-back hazards" density (24-PATTERNS.md): `{ x: 3600, y: FLOOR_Y - 48 }` (80px lead before the new spike at 3680), `{ x: 3720, y: FLOOR_Y - 48 }` (80px lead before the new enemy at 3800), `{ x: 4280, y: FLOOR_Y - 48 }` (80px lead before the new mathGate at 4360, at floor-6's start), `{ x: 4720, y: FLOOR_Y - 48 }` (80px lead before the new spike at 4800).
    Do NOT add any `doors` entries and do NOT change `answerPickupSlots` — level-03's `doors: []` is a load-bearing signal CONTEXT.md locks: the extension must keep doors absent from level-03.
  </action>
  <acceptance_criteria>
    `coins.length` is 17, `spikes.length` is 8, `enemies.length` is 2, `mathGates.length` is 2, `checkpoints.length` is 13.
    `doors.length` is 0 (unchanged).
    The new mathGate's footprint (4360..4392) and the new enemy's footprint (3800..3832) are each fully contained within their respective floor spans.
    `node scripts/validate-levels.mjs` prints zero lines matching `^level-03 .*HARD-FAIL`.
  </acceptance_criteria>
  <verify>
    <automated>N=$(node scripts/validate-levels.mjs 2>&1 | grep '^level-03 ' | grep -c 'HARD-FAIL'); echo "level-03 HARD-FAILs: $N"; test "$N" = "0"; D=$(grep -c '^\s*doors: \[\]' src/levels/level-03.js); echo "level-03 doors:[] present: $D"; test "$D" = "1"</automated>
  </verify>
  <done>level-03 extended with 6 new coins, 2 new spikes, 1 new enemy, 1 new mathGate, 4 new checkpoints; doors stay empty; validate-levels.mjs shows zero level-03 HARD-FAIL rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure local static-data edits to a level descriptor, consumed only by the offline single-player Kaplay client and Node-side build/validation tooling. No network input, no auth, no session, no access control, no crypto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | src/levels/level-03.js geometry values | low | accept | Developer-authored static data, not runtime-parsed untrusted input; `node scripts/validate-levels.mjs` gates any malformed/out-of-envelope geometry before it can ship |
| T-24-02 | Repudiation | The 2 fixed platforms' reachability claim (VALID-04) | low | mitigate | Task 1's acceptance criteria requires a direct `bfsReachableSet` query proof (not just absence of a named validator row), matching 23-FINDINGS.md's own precedent for arbitrating platforms the standard 3-check output never names |

## Package Legitimacy Audit

Not applicable — this plan installs zero new packages.
</threat_model>

<verification>
Run `node scripts/validate-levels.mjs` after Task 1 lands and again after Task 2 lands. Both runs must print zero lines matching `^level-03 .*HARD-FAIL`. Additionally, Task 1's inline reachability query must print `true` for both x:1880 and x:2640 — this is the only proof these two platforms (never named in validate-levels.mjs's standard output) are actually fixed.

`node scripts/smoke-progress.mjs` is EXPECTED to fail on its level-03 block after this plan's edits — it is not re-baselined until Plan 24-05 (Wave 2). Do not attempt to fix or work around this failure in this plan; it is an expected, documented interim state.
</verification>

<success_criteria>
- `node scripts/validate-levels.mjs` shows zero `level-03` HARD-FAIL rows.
- Both platforms x:1880 and x:2640 are confirmed BFS-reachable from spawn via direct reachability.mjs query.
- level-03's geometry object has: 7 floors, 9 platforms, 17 coins, 8 spikes, 13 checkpoints, 0 doors (unchanged), 2 mathGates, 2 enemies, 1 collectZone (unchanged), goal.x = 5120, bounds.right = 5200.
- `git diff` on `src/levels/level-03.js` shows only the 2 platform y-value changes (184→260, 192→260) plus pure appends to floors/platforms/coins/spikes/enemies/mathGates/checkpoints/goal plus the `bounds.right` value change.
</success_criteria>

<output>
Create `.planning/phases/24-fix-lengthen-levels-1-4/24-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/levels/level-03.js` — edited: 2 platforms lowered into reach (VALID-04); 2 new floors, 2 new platforms, 6 new coins, 2 new spikes, 1 new enemy, 1 new mathGate, 4 new checkpoints appended after x:3400; goal moved to x:5120; bounds.right bumped to 5200 (LVL-01)
