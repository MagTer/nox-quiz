---
phase: 24-fix-lengthen-levels-1-4
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/levels/level-04.js
autonomous: true
requirements: [VALID-04, LVL-01]
must_haves:
  truths:
    - "level-04's over-hole mathGate (x:1800) no longer floats over a hole, and all 6 known unreachable platforms (x:1080,1400,1760,2140,2520,3240) are now BFS-reachable from spawn — fixing platform x:1760 specifically resolves the level's cascading gap-width (1760..1960), spawn-goal, and enemy-reachability HARD-FAILs documented in 23-FINDINGS.md's post-plan-correction section"
    - "level-04 extends from goal x:3920 to x:6120 (+2200px, 55% longer than the v4.1 4000px extent) with every new floor/platform/coin/spike/door/mathGate/checkpoint appended strictly after x:4000; bounds.right is bumped from 4000 to 6200"
    - "every new hazard/mechanic in the extension has a checkpoint at FLOOR_Y-48 exactly 80px before it"
  artifacts:
    - src/levels/level-04.js
  key_links:
    - "level-04.js platform x:1760's y-value -> scripts/lib/reachability.mjs's canReach chain floor-2->platform->floor-3 (the specific edge that, once feasible, resolves the level's gap-width/spawn-goal/enemy-reachability HARD-FAILs — verified by 23-FINDINGS.md's own buildGraph/bfsReachableSet spot-check)"
    - "level-04.js bounds.right -> src/scenes/game.js's `level.bounds ?? {...dynamic}` short-circuit (level-04 HAS an explicit bounds field)"
    - "level-04.js geometry -> scripts/validate-levels.mjs (structural gate, must show zero level-04 HARD-FAIL rows, down from the current 5 named HARD-FAIL rows: over-hole x1800, spawn-goal, gap-width 1760..1960, mechanic-reachability x1800, mechanic-reachability enemy x2400)"
---

<objective>
Fix level-04's over-hole mathGate (VALID-04) and all 6 known unreachable platforms, then extend the level 55% past its v4.1 length with new floors/platforms/coins/hazards/mechanics/checkpoints appended strictly after the existing kid-validated geometry (LVL-01). Level-04 currently carries the most known structural defects of the 4 levels (1 over-hole gate + 6 unreachable platforms) and 5 of validate-levels.mjs's current 9 named HARD-FAIL rows.

Purpose: level-04 is the fourth and densest of the 4 levels this phase must make structurally sound and noticeably longer. Fixing platform x:1760 (the sole node overlapping the 1760..1960 gap) is the specific change that resolves the cascading gap-width/spawn-goal/enemy-unreachable HARD-FAILs 23-FINDINGS.md traced to this exact node.
Output: `src/levels/level-04.js` with 1 mathGate repositioned (1800→1728), 6 platforms lowered into reach, 3 new floor runs, 3 new bridging platforms, 7 new coins, 2 new spikes, 1 new door, 1 new mathGate, 4 new checkpoints, a goal moved to x:6120, and `bounds.right` bumped from 4000 to 6200.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-CONTEXT.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-RESEARCH.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix level-04's over-hole mathGate and 6 unreachable platforms, and append the new geometry skeleton</name>
  <files>src/levels/level-04.js</files>
  <read_first>
    src/levels/level-04.js (full file — bounds line 16, floors 19-26, platforms 28-38, mathGates 86-89, goal 65)
    src/config.js (CONFIG.FLOOR_Y=320, CONFIG.MATH_GATE.H, CONFIG.GOAL_SIZE)
    scripts/lib/jump-envelope.mjs (JUMP_ENVELOPE.maxRise=88.331)
    .planning/phases/24-fix-lengthen-levels-1-4/24-RESEARCH.md ("Structural Defect Fix Targets" tables; "Level-04's remaining HARD-FAILs re-examined" analysis in 23-FINDINGS.md, which traces the gap-width 1760..1960 / spawn-goal / enemy-x2400 HARD-FAILs to platform x:1760's excessive rise)
  </read_first>
  <action>
    In the `mathGates` array, change the second entry's x from 1800 to 1728 (per VALID-04 — footprint becomes 1728..1760, fully inside floor-2's 1240..1760 span, still before the enemy at x:2400). Keep y unchanged. Update its comment to state the Phase 24 reposition and cite VALID-04.
    In the `platforms` array, change each of these 6 entries' y value (x and w stay unchanged — CONTEXT.md locks x-position preservation): `{ x: 1080, y: 200, ... }` → y:250; `{ x: 1400, y: 216, ... }` → y:250; `{ x: 1760, y: 176, ... }` → y:250; `{ x: 2140, y: 216, ... }` → y:250; `{ x: 2520, y: 192, ... }` → y:250; `{ x: 3240, y: 184, ... }` → y:250. All 6 use y:250 uniformly (rise 70px, within JUMP_ENVELOPE.maxRise 88.331 and within each platform's wide touching/contained span window per 24-RESEARCH.md's physics — these are NOT the narrow 40px-overlap case level-03's 2 platforms needed a more conservative rise for). Update each comment to note the Phase 24 rise reduction and cite VALID-04.
    Do not touch `floors`, `doors`, `enemies`, `collectZones`, `answerPickupSlots`, or any existing `coins`/`spikes`/`checkpoints` entries in this task.
    Append exactly 3 new entries to the `floors` array (after the existing `{ x: 3400, w: 600 }`, which ends at 4000): `{ x: 4160, w: 560 }`, `{ x: 4920, w: 600 }`, `{ x: 5680, w: 520 }`. Comment each as a Phase 24 extension run.
    Append exactly 3 new entries to the `platforms` array: `{ x: 4000, y: 250, w: 128, h: 24 }` (bridges the new 4000..4160 gap, touching floor-6's end at 4000 exactly — rise 70px), `{ x: 4720, y: 250, w: 128, h: 24 }` (bridges the new, wider 4720..4920 200px gap — matching level-04's own existing 200px-gap convention that needed a bridging platform — touching the new floor-7's end at 4720 exactly, rise 70px), `{ x: 5520, y: 250, w: 112, h: 24 }` (bridges the new 5520..5680 gap, touching the new floor-8's end at 5520 exactly, rise 70px).
    Move `goal.x` from 3920 to 6120 (keep `y: FLOOR_Y - CONFIG.GOAL_SIZE`). The new floor-9 ends at 6200, giving an 80px buffer, matching the original's own 4000-3920=80px convention.
    Change `bounds.right` from 4000 to 6200 (keep `left: 0, top: 0, bottom: 360` unchanged) — level-04's explicit `bounds` field is used AS-IS by `src/scenes/game.js`, so this manual bump is required.
  </action>
  <acceptance_criteria>
    The `mathGates` array's second entry has x:1728 (not 1800).
    All 6 target platforms (x:1080, 1400, 1760, 2140, 2520, 3240) have y:250; their x and w values are unchanged from the original file.
    The `floors` array has exactly 9 entries; the 7th is `{ x: 4160, w: 560 }`, the 8th is `{ x: 4920, w: 600 }`, the 9th is `{ x: 5680, w: 520 }`.
    The `platforms` array has exactly 12 entries; the last 3 are `{ x: 4000, y: 250, w: 128, h: 24 }`, `{ x: 4720, y: 250, w: 128, h: 24 }`, `{ x: 5520, y: 250, w: 112, h: 24 }`.
    `goal.x` equals 6120. `bounds.right` equals 6200.
    `node scripts/validate-levels.mjs` prints zero lines matching `^level-04 .*HARD-FAIL` — this is a drop from the current 5 named level-04 HARD-FAIL rows (over-hole x1800, spawn-goal, gap-width 1760..1960, mechanic-reachability x1800, mechanic-reachability enemy x2400) to zero.
    All 6 fixed platform nodes are present in the BFS-reachable set from spawn, confirmed by directly querying `scripts/lib/reachability.mjs`'s exported `buildNodes`/`buildGraph`/`bfsReachableSet`/`nodeContaining` against `getLevel("level-04").geometry` and `JUMP_ENVELOPE`.
  </acceptance_criteria>
  <verify>
    <automated>N=$(node scripts/validate-levels.mjs 2>&1 | grep '^level-04 ' | grep -c 'HARD-FAIL'); echo "level-04 HARD-FAILs: $N"; test "$N" = "0"; node --input-type=module -e "
import { getLevel } from './src/levels/index.js';
import { buildNodes, buildGraph, bfsReachableSet, nodeContaining, SPAWN_X } from './scripts/lib/reachability.mjs';
import { JUMP_ENVELOPE } from './scripts/lib/jump-envelope.mjs';
const geometry = getLevel('level-04').geometry;
const nodes = buildNodes(geometry);
const graph = buildGraph(nodes, JUMP_ENVELOPE);
const spawn = nodeContaining(nodes, SPAWN_X);
const reachable = bfsReachableSet(graph, spawn.id);
const targets = [1080, 1400, 1760, 2140, 2520, 3240];
const results = targets.map((x) => {
  const n = nodes.find((n) => n.id.startsWith('platform-') && n.xStart === x);
  return [x, Boolean(n && reachable.has(n.id))];
});
console.log(JSON.stringify(results));
process.exit(results.every(([, ok]) => ok) ? 0 : 1);
"</automated>
  </verify>
  <done>mathGate repositioned to x:1728; all 6 unreachable platforms lowered to y:250 (rise 70px); 3 new floor runs + 3 new bridging platforms appended after x:4000; goal moved to x:6120; bounds.right bumped to 6200; validate-levels.mjs shows zero level-04 HARD-FAIL rows (down from 5); the reachability query confirms all 6 platforms are in the spawn-reachable set.</done>
</task>

<task type="auto">
  <name>Task 2: Add level-04's extension coins, hazards, mechanics, and checkpoints</name>
  <files>src/levels/level-04.js</files>
  <read_first>
    src/levels/level-04.js (post-Task-1 state — re-read after Task 1's edits land)
    src/config.js (CONFIG.SPIKE_SIZE, CONFIG.DOOR.H, CONFIG.MATH_GATE.H)
    .planning/phases/24-fix-lengthen-levels-1-4/24-PATTERNS.md ("Densest checkpoint pattern" section for level-04)
  </read_first>
  <action>
    Append exactly 7 new entries to the `coins` array (matching level-04's existing ~1-per-327px density): `{ x: 4040, y: 264 }`, `{ x: 4260, y: 184 }`, `{ x: 4460, y: 136 }`, `{ x: 4760, y: 264 }`, `{ x: 5060, y: 176 }`, `{ x: 5300, y: 264 }`, `{ x: 5900, y: 264 }`.
    Append exactly 2 new entries to the `spikes` array: `{ x: 4300, y: FLOOR_Y - CONFIG.SPIKE_SIZE }` (on the new floor-7, 4160..4720 span) and `{ x: 5200, y: FLOOR_Y - CONFIG.SPIKE_SIZE }` (on the new floor-8, 4920..5520 span).
    Append exactly 1 new entry to the `doors` array (in addition to the 1 already present): `{ x: 5000, y: FLOOR_Y - CONFIG.DOOR.H }` (on floor-8, 4920..5520 span) — level-04 already has a door encounter, so this reuses an existing mechanic type.
    Append exactly 1 new entry to the `mathGates` array (in addition to the 2 already present, one of which was repositioned in Task 1): `{ x: 5760, y: FLOOR_Y - CONFIG.MATH_GATE.H }` (on floor-9, 5680..6200 span).
    Append exactly 4 new entries to the `checkpoints` array, mirroring level-04's own densest-of-all-4-levels clustering style: `{ x: 4220, y: FLOOR_Y - 48 }` (80px lead before the new spike at 4300), `{ x: 4920, y: FLOOR_Y - 48 }` (80px lead before the new door at 5000, at floor-8's start), `{ x: 5120, y: FLOOR_Y - 48 }` (80px lead before the new spike at 5200), `{ x: 5680, y: FLOOR_Y - 48 }` (80px lead before the new mathGate at 5760, at floor-9's start).
  </action>
  <acceptance_criteria>
    `coins.length` is 19, `spikes.length` is 9, `doors.length` is 2, `mathGates.length` is 3, `checkpoints.length` is 16.
    The new door's footprint (5000..5032) and the new mathGate's footprint (5760..5792) are each fully contained within their respective floor spans.
    `node scripts/validate-levels.mjs` prints zero lines matching `^level-04 .*HARD-FAIL`.
  </acceptance_criteria>
  <verify>
    <automated>N=$(node scripts/validate-levels.mjs 2>&1 | grep '^level-04 ' | grep -c 'HARD-FAIL'); echo "level-04 HARD-FAILs: $N"; test "$N" = "0"</automated>
  </verify>
  <done>level-04 extended with 7 new coins, 2 new spikes, 1 new door, 1 new mathGate, 4 new checkpoints; validate-levels.mjs shows zero level-04 HARD-FAIL rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure local static-data edits to a level descriptor, consumed only by the offline single-player Kaplay client and Node-side build/validation tooling. No network input, no auth, no session, no access control, no crypto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | src/levels/level-04.js geometry values | low | accept | Developer-authored static data, not runtime-parsed untrusted input; `node scripts/validate-levels.mjs` gates any malformed/out-of-envelope geometry before it can ship |
| T-24-02 | Repudiation | The 6 fixed platforms' reachability claim (VALID-04), especially x:1760 whose fix cascades into 3 other named rows | high | mitigate | Task 1's acceptance criteria requires BOTH the named-row HARD-FAIL count to drop to zero AND a direct `bfsReachableSet` query proof for all 6 platform nodes — closing the gap between "no named row fails" and "the platform is actually reachable," matching 23-FINDINGS.md's own diagnostic method for this exact node |

## Package Legitimacy Audit

Not applicable — this plan installs zero new packages.
</threat_model>

<verification>
Run `node scripts/validate-levels.mjs` after Task 1 lands and again after Task 2 lands. Both runs must print zero lines matching `^level-04 .*HARD-FAIL` (down from the current 5 named HARD-FAIL rows). Task 1's inline reachability query must print `true` for all 6 platform x-values — this is the only proof these platforms (never directly named in validate-levels.mjs's standard output) are actually fixed, and the specific proof that x:1760's fix has resolved the cascading gap-width/spawn-goal/enemy-reachability failures 23-FINDINGS.md traced to it.

`node scripts/smoke-progress.mjs` is EXPECTED to fail on its level-04 block after this plan's edits — it is not re-baselined until Plan 24-05 (Wave 2). Do not attempt to fix or work around this failure in this plan; it is an expected, documented interim state.
</verification>

<success_criteria>
- `node scripts/validate-levels.mjs` shows zero `level-04` HARD-FAIL rows (down from 5).
- All 6 platforms (x:1080,1400,1760,2140,2520,3240) confirmed BFS-reachable from spawn via direct reachability.mjs query.
- level-04's geometry object has: 9 floors, 12 platforms, 19 coins, 9 spikes, 16 checkpoints, 2 doors, 3 mathGates, 1 enemy (unchanged), 1 collectZone (unchanged), goal.x = 6120, bounds.right = 6200.
- `git diff` on `src/levels/level-04.js` shows only the 1 mathGate x-value change (1800→1728), the 6 platform y-value changes, plus pure appends to floors/platforms/coins/spikes/doors/mathGates/checkpoints/goal plus the `bounds.right` value change.
</success_criteria>

<output>
Create `.planning/phases/24-fix-lengthen-levels-1-4/24-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/levels/level-04.js` — edited: mathGate repositioned (VALID-04); 6 platforms lowered into reach (VALID-04); 3 new floors, 3 new platforms, 7 new coins, 2 new spikes, 1 new door, 1 new mathGate, 4 new checkpoints appended after x:4000; goal moved to x:6120; bounds.right bumped to 6200 (LVL-01)
