---
phase: 24-fix-lengthen-levels-1-4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/levels/level-01.js
autonomous: true
requirements: [VALID-04, LVL-01]
must_haves:
  truths:
    - "level-01's 2 known over-hole mathGates (x:600, x:1300) no longer float over a hole; node scripts/validate-levels.mjs prints zero level-01 HARD-FAIL rows"
    - "level-01 extends from goal x:2160 to x:3560 (+1400px, 62.5% longer than the v4.1 ~2240px extent) with every new floor/platform/coin/spike/mathGate/checkpoint appended strictly after x:2240 — the original 0..2240 geometry is never edited except the 2 gate x repositions CONTEXT.md authorizes"
    - "every new hazard/mechanic in the extension (spike x:2640, mathGate x:3120) has a checkpoint at FLOOR_Y-48 exactly 80px before it (x:2560, x:3040)"
  artifacts:
    - src/levels/level-01.js
  key_links:
    - "level-01.js geometry -> scripts/validate-levels.mjs (structural gate, must show zero level-01 HARD-FAIL rows)"
    - "level-01.js geometry -> src/scenes/game.js's dynamic bounds derivation (Math.max over floors/platforms/goal) — no explicit bounds field needed or added"
    - "level-01.js geometry -> scripts/lib/mechanic-drive.mjs's deriveEncounters (auto-discovers the new mathGate/spike with zero script changes)"
---

<objective>
Fix level-01's 2 known over-hole mathGate defects (VALID-04) and extend the level 62.5% past its v4.1 length with new floors/platforms/coins/hazards/checkpoints appended strictly after the existing kid-validated geometry (LVL-01).

Purpose: level-01 is the first of 4 levels this phase must make structurally sound (zero validator HARD-FAILs) and noticeably longer without touching its already-played, kid-validated 0..2240 extent except for the 2 explicitly authorized gate repositions.
Output: `src/levels/level-01.js` with 2 repositioned mathGates, 2 new floor runs, 2 new bridging platforms, 6 new coins, 1 new spike, 1 new mathGate, 2 new checkpoints, and a goal moved to x:3560. No `bounds` field is added (level-01's camera clamp derives dynamically from geometry — see RESEARCH.md's Camera Bounds section).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-CONTEXT.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-RESEARCH.md
@.planning/phases/24-fix-lengthen-levels-1-4/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix level-01's 2 over-hole mathGates and append the new geometry skeleton</name>
  <files>src/levels/level-01.js</files>
  <read_first>
    src/levels/level-01.js (full file — floors lines 40-44, platforms 48-53, goal 84, mathGates 111-114)
    src/config.js (CONFIG.FLOOR_Y=320, CONFIG.MATH_GATE.H, CONFIG.GOAL_SIZE)
    .planning/phases/24-fix-lengthen-levels-1-4/24-RESEARCH.md ("Structural Defect Fix Targets" and "Camera Bounds / Level Extension Mechanics" sections)
  </read_first>
  <action>
    In the `mathGates` array: change the first entry's x from 600 to 528 (per VALID-04 — 24-RESEARCH.md's exact fix target; footprint becomes 528..560, fully inside floor-0's 0..560 span, with 0px margin to spare on the right edge). Keep y unchanged (`FLOOR_Y - CONFIG.MATH_GATE.H`). Update its comment to state the Phase 24 reposition and cite VALID-04.
    Change the second mathGate's x from 1300 to 1360 (footprint becomes 1360..1392, fully inside floor-2's 1360..2240 span, still before the door at 1400). Keep y unchanged. Update its comment the same way.
    Do not touch `floors`, `platforms`, `doors`, `enemies`, `collectZones`, `answerPickupSlots`, or any existing `coins`/`spikes`/`checkpoints` entries in this task — those are untouched original geometry.
    Append exactly 2 new entries to the `floors` array (after the existing `{ x: 1360, w: 880 }` which ends at 2240): `{ x: 2400, w: 480 }` and `{ x: 3040, w: 600 }`. Each needs an inline comment naming the run (mirroring the existing "opening run" / "middle run" style) and noting it is a Phase 24 extension run, e.g. "new run after gap 3 (Phase 24 extension)" and "final run to the new goal (Phase 24 extension), ends at 3640".
    Append exactly 2 new entries to the `platforms` array: `{ x: 2240, y: 250, w: 128, h: 24 }` (bridges the new 2240..2400 gap, touching floor-2's end at 2240 exactly — rise 70px, well inside JUMP_ENVELOPE.maxRise 88.331) and `{ x: 2880, y: 250, w: 112, h: 24 }` (bridges the new 2880..3040 gap, touching the new floor-3's end at 2880 exactly — same 70px rise). Comment each with its bridging role, mirroring "mid-gap-1 stepping stone" style.
    Move `goal.x` from 2160 to 3560 (keep the `y: FLOOR_Y - CONFIG.GOAL_SIZE` expression). Update its comment: the goal now caps the new final run (floor-4 ends at 3640; 80px buffer, matching the original's own 80px goal-to-floor-end buffer convention: 2240-2160=80).
    Do NOT add a `bounds` field to LEVEL_01 — per 24-RESEARCH.md's verified finding, `src/scenes/game.js` (lines 83-93) derives level-01's camera clamp dynamically from `Math.max(...floors, ...platforms, goal.x+GOAL_SIZE)` whenever `level.bounds` is absent; adding a static field would short-circuit that and risk drifting out of sync with any future edit.
  </action>
  <acceptance_criteria>
    The `mathGates` array's first entry has x:528 and the second has x:1360 (neither 600 nor 1300 appears as a mathGate x value anywhere in the file).
    The `floors` array has exactly 5 entries; the 4th is `{ x: 2400, w: 480 }` and the 5th is `{ x: 3040, w: 600 }`.
    The `platforms` array has exactly 6 entries; the 5th is `{ x: 2240, y: 250, w: 128, h: 24 }` and the 6th is `{ x: 2880, y: 250, w: 112, h: 24 }`.
    `goal.x` equals 3560.
    No `bounds:` key exists anywhere in the `LEVEL_01` object.
    `node scripts/validate-levels.mjs` prints zero lines matching `^level-01 .*HARD-FAIL`.
  </acceptance_criteria>
  <verify>
    <automated>N=$(node scripts/validate-levels.mjs 2>&1 | grep '^level-01 ' | grep -c 'HARD-FAIL'); echo "level-01 HARD-FAILs: $N"; test "$N" = "0"; B=$(grep -c 'bounds:' src/levels/level-01.js); echo "level-01 bounds field count: $B"; test "$B" = "0"</automated>
  </verify>
  <done>Both known over-hole mathGates repositioned onto solid floor; 2 new floor runs + 2 new bridging platforms appended after x:2240; goal moved to x:3560; no bounds field added; validate-levels.mjs shows zero level-01 HARD-FAIL rows.</done>
</task>

<task type="auto">
  <name>Task 2: Add level-01's extension coins, hazard, mechanic, and checkpoints</name>
  <files>src/levels/level-01.js</files>
  <read_first>
    src/levels/level-01.js (post-Task-1 state — re-read after Task 1's edits land)
    src/config.js (CONFIG.SPIKE_SIZE, CONFIG.MATH_GATE.H)
    .planning/phases/24-fix-lengthen-levels-1-4/24-RESEARCH.md ("Checkpoint Formula" and "Open Questions" #1 sections)
  </read_first>
  <action>
    Append exactly 6 new entries to the `coins` array (top-left-anchor 32x32 sprite convention, matching level-01's existing ~1-per-220px density and alternating-height arc style): `{ x: 2280, y: 264 }`, `{ x: 2472, y: 184 }`, `{ x: 2600, y: 136 }`, `{ x: 2800, y: 264 }`, `{ x: 3080, y: 264 }`, `{ x: 3400, y: 176 }`.
    Append exactly 1 new entry to the `spikes` array: `{ x: 2640, y: FLOOR_Y - CONFIG.SPIKE_SIZE }` (sits on the new floor-3, 2400..2880 span). Comment it as the new hazard on the extension's first new floor run.
    Append exactly 1 new entry to the `mathGates` array (in addition to the 2 already fixed in Task 1): `{ x: 3120, y: FLOOR_Y - CONFIG.MATH_GATE.H }` (sits on the new floor-4, 3040..3640 span). Comment it as the new MECH-04 instance added for length/audit coverage per 24-RESEARCH.md's Open Question 1 recommendation — level-01 already has doors/mathGates/enemies/collectZones, so a 3rd mathGate reuses an existing type without introducing new variety.
    Append exactly 2 new entries to the `checkpoints` array: `{ x: 2560, y: FLOOR_Y - 48 }` (80px lead before the new spike at 2640, on floor-3) and `{ x: 3040, y: FLOOR_Y - 48 }` (80px lead before the new mathGate at 3120, at floor-4's start) — both follow the exact `FLOOR_Y - 48` / 80px-lead convention CONTEXT.md locks.
    Do NOT touch `doors`, `enemies`, `collectZones`, or `answerPickupSlots` — level-01's extension reuses only mathGate + spike as its new instances; adding more mechanic types is not required and keeps the addition proportionate to the level's existing density.
  </action>
  <acceptance_criteria>
    `coins.length` is 16, `spikes.length` is 4, `mathGates.length` is 3, `checkpoints.length` is 6.
    The new mathGate's footprint (3120..3152) is fully contained within floor-4's 3040..3640 span (over-hole-check.mjs would report it clean).
    `node scripts/validate-levels.mjs` prints zero lines matching `^level-01 .*HARD-FAIL`, and the new mathGate's mechanic-reachability row (`x:3120..3152`) prints PASS or WARN, never HARD-FAIL.
  </acceptance_criteria>
  <verify>
    <automated>N=$(node scripts/validate-levels.mjs 2>&1 | grep '^level-01 ' | grep -c 'HARD-FAIL'); echo "level-01 HARD-FAILs: $N"; test "$N" = "0"; node scripts/validate-levels.mjs 2>&1 | grep '^level-01 | mechanic-reachability' | grep '3120'</automated>
  </verify>
  <done>level-01 extended with 6 new coins, 1 new spike, 1 new mathGate, 2 new checkpoints in the new section; validate-levels.mjs shows zero level-01 HARD-FAIL rows including the new mechanic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure local static-data edits to a level descriptor, consumed only by the offline single-player Kaplay client and Node-side build/validation tooling. No network input, no auth, no session, no access control, no crypto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | src/levels/level-01.js geometry values | low | accept | Developer-authored static data, not runtime-parsed untrusted input; `node scripts/validate-levels.mjs` gates any malformed/out-of-envelope geometry (over-hole, unreachable, gap-too-wide) before it can ship |
| T-24-02 | Denial of Service | scripts/lib/mechanic-drive.mjs's driveToXClimbing bounded loop, exercised indirectly when the interactive audit later drives the new mathGate/spike | low | accept | New mechanic spacing (3120 vs the prior encounter at 1728/1800-area, ~1400px away at most, well within the existing 200-600px order of magnitude per encounter chain) and the pre-existing `maxIterations: 250` guard (Phase 21/23) already bound worst-case iteration; no new unbounded loop introduced |

## Package Legitimacy Audit

Not applicable — this plan installs zero new packages.
</threat_model>

<verification>
Run `node scripts/validate-levels.mjs` after Task 1 lands and again after Task 2 lands. Both runs must print zero lines matching `^level-01 .*HARD-FAIL`. WARN rows for level-01 (e.g. the pre-existing gap-width/spawn-goal WARN rows, or a new WARN on the new mathGate's mechanic-reachability row) are expected and non-blocking per CONTEXT.md's locked "zero HARD-FAILs" definition of green.

`node scripts/smoke-progress.mjs` is EXPECTED to fail on its level-01 block after this plan's edits — it is not re-baselined until Plan 24-05 (Wave 2). Do not attempt to fix or work around this failure in this plan; it is an expected, documented interim state.
</verification>

<success_criteria>
- `node scripts/validate-levels.mjs` shows zero `level-01` HARD-FAIL rows.
- level-01's geometry object has: 5 floors, 6 platforms, 16 coins, 4 spikes, 6 checkpoints, 1 door (unchanged), 3 mathGates, 1 enemy (unchanged), 1 collectZone (unchanged), goal.x = 3560, no `bounds` field.
- `git diff` on `src/levels/level-01.js` shows only the 2 mathGate x-value changes (600→528, 1300→1360) plus pure appends to floors/platforms/coins/spikes/mathGates/checkpoints/goal — zero edits inside the original 0..2240 extent beyond the 2 authorized gate x-values.
</success_criteria>

<output>
Create `.planning/phases/24-fix-lengthen-levels-1-4/24-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/levels/level-01.js` — edited: 2 mathGates repositioned (VALID-04); 2 new floors, 2 new platforms, 6 new coins, 1 new spike, 1 new mathGate, 2 new checkpoints appended after x:2240; goal moved to x:3560 (LVL-01)