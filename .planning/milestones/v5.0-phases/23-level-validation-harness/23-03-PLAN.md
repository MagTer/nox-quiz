---
phase: 23-level-validation-harness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/over-hole-check.mjs
  - scripts/fixtures/bad-level.js
autonomous: true
requirements: [VALID-01]

must_haves:
  truths:
    - "findOverHoleBarriers(geometry) returns a row for every door/mathGate/enemy whose footprint is not fully covered by any floor run, and returns [] for a fully-supported level"
    - "scripts/fixtures/bad-level.js provides a synthetic level that independently proves both an over-hole HARD-FAIL and an unconditionally-unreachable-platform HARD-FAIL, regardless of the exact calibrated envelope numbers"
  artifacts:
    - path: "scripts/lib/over-hole-check.mjs"
      provides: "Promoted exact-interval-arithmetic over-hole checker (VALID-01), already proven correct against the 3 known Phase 22 defects"
      contains: "export function findOverHoleBarriers"
    - path: "scripts/fixtures/bad-level.js"
      provides: "Deliberately-broken level descriptor for the validator's self-test (Wave 3), mirroring bad-scene.js's convention"
      contains: "export const BAD_LEVEL"
  key_links:
    - from: "scripts/fixtures/bad-level.js"
      to: "scripts/lib/over-hole-check.mjs"
      via: "the fixture's mathGate is proven over-hole by calling findOverHoleBarriers directly in this plan's own verify step"
      pattern: "findOverHoleBarriers"
---

<objective>
Promote Phase 22's already-proven scratchpad over-hole interval-arithmetic check into a real, exported module, and build the deliberately-broken fixture that will let the validator (Wave 3) prove it goes RED independent of levels 1-4's specific content.

Purpose: VALID-01 requires a door/mathGate-over-hole check using exact interval arithmetic. This logic already exists and was already proven correct against the 3 known live defects (Phase 22's `interval-check-22-04.mjs` scratchpad) — this plan promotes it verbatim into a permanent module, and separately builds the synthetic fixture Pattern 5 recommends for proving the checker's correctness independent of any specific shipped level's content.
Output: `scripts/lib/over-hole-check.mjs` (exported `findOverHoleBarriers`) and `scripts/fixtures/bad-level.js` (a synthetic, deliberately-broken level descriptor).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/23-level-validation-harness/23-CONTEXT.md
@.planning/phases/23-level-validation-harness/23-RESEARCH.md
@.planning/phases/23-level-validation-harness/23-PATTERNS.md
@.planning/phases/23-level-validation-harness/23-VALIDATION.md
</context>

## Artifacts this phase produces

- `scripts/lib/over-hole-check.mjs` — new module. Exports `findOverHoleBarriers(geometry)` → array of `{ kind, x, w, footprint: [start, end] }` rows (empty array means clean).
- `scripts/fixtures/bad-level.js` — new fixture. Exports `BAD_LEVEL` (`{ id, displayName, allowedTables, geometry: { floors, platforms, doors, mathGates, enemies, collectZones, goal }, mechanics: [], theme: null, parallax: null }`), consumable through `scripts/validate-levels.mjs`'s `--fixture` flag (built in Wave 3).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote the over-hole interval-arithmetic checker</name>
  <files>scripts/lib/over-hole-check.mjs</files>
  <behavior>
    - Given a geometry with floors `[{x:0,w:560},{x:720,w:480}]` (gap 560..720) and `mathGates: [{x:600, y:256}]` (footprint 600..632, squarely in the gap), `findOverHoleBarriers` returns exactly one row: `{kind:"mathGates", x:600, w:32, footprint:[600,632]}`
    - Given a geometry with `doors: [{x:1400, y:256}]` (footprint 1400..1432) fully inside floor `{x:1360,w:880}` (covers 1360..2240), `findOverHoleBarriers` returns `[]`
    - Given a geometry object where `doors`/`mathGates`/`enemies` are all `undefined` (omitted entirely), `findOverHoleBarriers` returns `[]` without throwing (never-brick `?? []` guard)
  </behavior>
  <read_first>
    - .planning/phases/23-level-validation-harness/23-PATTERNS.md (the exact promoted source under "`scripts/lib/over-hole-check.mjs` (utility, transform)" — this IS the reference implementation to promote, byte-for-byte)
    - src/config.js (`CONFIG.DOOR.W`, `CONFIG.MATH_GATE.W`, `CONFIG.ENEMY.W` — all 32; the barrier widths this module reads)
    - src/levels/level-01.js (confirms every door/mathGate/enemy sits at floor level — `y: FLOOR_Y - CONFIG.X.H` — never at platform level, which is why this promoted logic checks only `geometry.floors`, never `geometry.platforms`)
    - scripts/smoke-progress.mjs (the `check(cond, msg)` / failures-counter / `process.exit(1)` self-test idiom to mirror for this module's own inline self-test)
  </read_first>
  <action>
    Create scripts/lib/over-hole-check.mjs. Import `CONFIG` from `../../src/config.js`. Define `BARRIER_WIDTH = { doors: CONFIG.DOOR.W, mathGates: CONFIG.MATH_GATE.W, enemies: CONFIG.ENEMY.W }`. Export `function findOverHoleBarriers(geometry)`: build `runs` from `(geometry.floors ?? []).map(f => [f.x, f.x + f.w])`; define `onFloor(x)` returning true if any run interval `[a,b]` satisfies `x >= a && x <= b`; for each `kind` of `["doors", "mathGates", "enemies"]`, iterate `geometry[kind] ?? []`, and for each entry `e` with `w = BARRIER_WIDTH[kind]`, push `{kind, x: e.x, w, footprint: [e.x, e.x + w]}` whenever `!onFloor(e.x) || !onFloor(e.x + w)`; return the accumulated array. This is a byte-for-byte promotion of the already-proven Phase 22 scratchpad logic — add no new checks (no platform-membership test; every shipped barrier in this game is floor-mounted, source-verified) and no opportunistic extras.

    Add a module header comment documenting: this checks ONLY floor-run coverage (not platforms), an empty return means clean, a non-empty return means HARD-FAIL (consumed by `scripts/validate-levels.mjs`, built in Wave 3), and that this logic is a verbatim promotion of Phase 22's `interval-check-22-04.mjs` scratchpad (already proven correct against the 3 known level-01/level-04 over-hole defects).

    Add a self-test block guarded to run only when this module is executed directly (compare `process.argv[1]` against the module's own resolved path), using the `check(cond, msg)`/failures-counter/`process.exit(1)` idiom from scripts/smoke-progress.mjs, exercising the three documented behavior cases above plus a print of `over-hole-check-selftest: PASS` (or `FAIL — N assertion(s) failed`) on completion.
  </action>
  <verify>
    <automated>node scripts/lib/over-hole-check.mjs && node --check scripts/lib/over-hole-check.mjs && [ "$(grep -c 'export function findOverHoleBarriers' scripts/lib/over-hole-check.mjs)" -eq 1 ] && [ "$(grep -c "over-hole-check-selftest: PASS" scripts/lib/over-hole-check.mjs)" -ge 1 ] && echo OVER-HOLE-CHECK-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/lib/over-hole-check.mjs` (self-test mode) exits 0 and prints a PASS line
    - `findOverHoleBarriers` is exported exactly once, matching the promoted Phase 22 signature
    - Every optional geometry array (`doors`, `mathGates`, `enemies`, `floors`) is `?? []`-guarded — a geometry object with all four omitted returns `[]`, never throws
    - The module checks only `geometry.floors` for support, never `geometry.platforms` (matches the proven scratchpad's scope; every shipped barrier is floor-mounted)
  </acceptance_criteria>
  <done>scripts/lib/over-hole-check.mjs exists, exports findOverHoleBarriers with the exact promoted Phase 22 logic, and its own self-test proves all three documented behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Build the deliberately-broken bad-level.js fixture</name>
  <files>scripts/fixtures/bad-level.js</files>
  <read_first>
    - scripts/fixtures/bad-scene.js (the exact header-comment convention this new fixture mirrors: NOT shipped game content, exists only to prove a checker fires RED independent of real content)
    - scripts/lib/over-hole-check.mjs (just built in Task 1 — used directly in this task's own verify command)
    - src/config.js (`CONFIG.FLOOR_Y` = 320, `CONFIG.MATH_GATE.H` = 64, `CONFIG.GOAL_SIZE` = 16, `CONFIG.GRAVITY` = 1400, `CONFIG.JUMP_FORCE` = 520 — the exact constants used to derive the deliberately-impossible numbers below)
    - .planning/phases/23-level-validation-harness/23-RESEARCH.md (Pattern 5 — the fixture's required shape: one over-hole barrier with no stepping-stone, AND one isolated platform whose Δy/run combination exceeds even the theoretical, non-margin-adjusted closed-form envelope, so it is unconditionally unreachable no matter what the real calibration measures)
  </read_first>
  <action>
    Create scripts/fixtures/bad-level.js. Export `const BAD_LEVEL = { id: "bad-level-fixture", displayName: "Deliberately Broken (validator self-test)", allowedTables: [6,7,8,9], geometry: {...}, mechanics: [], theme: null, parallax: null }` matching the shape `getLevel()` normally returns for a real level — only the fields the validator's checks actually read are required (floors, platforms, doors, mathGates, enemies, collectZones, goal); coins/spikes/checkpoints/answerPickupSlots are irrelevant to `findOverHoleBarriers`/`checkLevelReachability` and can be omitted.

    Use exactly these numbers, all derived from `src/config.js` constants (`FLOOR_Y=320`, `MATH_GATE.H=64`, `GOAL_SIZE=16`):
    - `geometry.floors`: two runs, `{x:0, w:300}` (floor A, spawn at x:64 sits on it) and `{x:700, w:300}` (floor B). The gap between them is 400px — this exceeds even the theoretical closed-form max horizontal jump distance (`RUN_SPEED * 2*JUMP_FORCE/GRAVITY` ≈ 178px) with zero margin applied, so no direct hop can ever bridge it.
    - `geometry.platforms`: one entry, `{x:450, y:30, w:80, h:24}`, sitting inside the 300..700 gap. Its height difference from the floor (`FLOOR_Y - y` = `320 - 30` = 290px of required rise) exceeds the theoretical closed-form max rise (`JUMP_FORCE**2 / (2*GRAVITY)` ≈ 96.6px) by a wide margin — this platform is UNCONDITIONALLY unreachable from either floor regardless of the exact empirically-calibrated numbers scripts/lib/jump-envelope.mjs eventually produces.
    - `geometry.mathGates`: one entry, `{x:380, y: 320 - 64}` (footprint 380..412), squarely inside the 300..700 gap with no floor beneath it anywhere — an over-hole HARD-FAIL independent of the platform defect above.
    - `geometry.doors`, `geometry.enemies`, `geometry.collectZones`: empty arrays (not needed to exercise the two defects).
    - `geometry.goal`: `{x:900, y: 320 - 16}`, on floor B (900 is within 700..1000) — this goal becomes unconditionally unreachable from spawn given the two defects above, so the validator's spawn→goal check will also independently HARD-FAIL, not only the over-hole check.

    Write a header comment mirroring bad-scene.js's convention: state this is NOT shipped game content; it exists only so `scripts/validate-levels.mjs`'s self-test (built in Wave 3, invoked via `--fixture scripts/fixtures/bad-level.js`) can be proven to go RED independent of whatever levels 1-4 currently contain; name the two deliberately-injected defects (the over-hole mathGate at x:380, and the unconditionally-unreachable platform/oversized gap combination blocking the goal at x:900).
  </action>
  <verify>
    <automated>node --check scripts/fixtures/bad-level.js && node -e "import('./scripts/fixtures/bad-level.js').then(async m => { const { findOverHoleBarriers } = await import('./scripts/lib/over-hole-check.mjs'); const rows = findOverHoleBarriers(m.BAD_LEVEL.geometry); if (rows.length !== 1 || rows[0].kind !== 'mathGates') { console.error('EXPECTED 1 mathGates OVER-HOLE ROW, GOT', JSON.stringify(rows)); process.exit(1); } console.log('BAD-LEVEL-OVERHOLE-OK'); })"</automated>
  </verify>
  <acceptance_criteria>
    - `BAD_LEVEL.geometry.mathGates[0]`'s footprint (380..412) is not contained in either floor run `[0,300]` or `[700,1000]` — confirmed by `findOverHoleBarriers` returning exactly one row for it
    - The gap between floor A (ends at 300) and floor B (starts at 700) is 400px, exceeding the theoretical closed-form max run distance (≈178px) with zero margin
    - The lone platform's required rise (290px) exceeds the theoretical closed-form max rise (≈96.6px) — unconditionally unreachable, independent of any future calibration result
    - `BAD_LEVEL` exports the same top-level shape (`id`, `displayName`, `allowedTables`, `geometry`, `mechanics`, `theme`, `parallax`) that `getLevel()` returns for real levels
  </acceptance_criteria>
  <done>scripts/fixtures/bad-level.js exists as a self-contained synthetic level proving both an over-hole HARD-FAIL and an unconditionally-unreachable-goal HARD-FAIL, ready for scripts/validate-levels.mjs's self-test (Wave 3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor → over-hole checker | `findOverHoleBarriers` parses `geometry` objects; this plan's fixture is synthetic/developer-authored data, same trust level as the real `src/levels/*.js` descriptors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-02 | Denial of Service | malformed/adversarial level descriptor crashing the checker | low | mitigate | Every optional geometry array (`floors`, `doors`, `mathGates`, `enemies`) is `?? []`-guarded per the project's established `build.js`/`deriveEncounters` "never brick" convention; Task 1's self-test proves an all-omitted geometry never throws |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `node scripts/lib/over-hole-check.mjs` self-test exits 0, proving the three documented behaviors
- `scripts/fixtures/bad-level.js` exists and its mathGate is proven over-hole by a direct call to `findOverHoleBarriers`
- Neither file touches `src/levels/*.js` — zero level-descriptor edits this plan
</verification>

<success_criteria>
- The door/mathGate-over-hole check (VALID-01) is a real, permanent, exported module rather than one-off scratchpad evidence
- A synthetic self-test fixture exists that can prove the validator's checks fire correctly independent of levels 1-4's specific content
</success_criteria>

<output>
Create `.planning/phases/23-level-validation-harness/23-03-SUMMARY.md` when done.
</output>
