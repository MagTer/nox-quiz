---
phase: 23-level-validation-harness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/audit-retry.mjs
  - scripts/audit-phase21-mechanics.mjs
  - .planning/phases/23-level-validation-harness/23-FINDINGS.md
autonomous: true
requirements: [VALID-02]

must_haves:
  truths:
    - "The interactive audit reruns each still-unreached encounter up to 5 times before excluding it, and any encounter reached in ANY attempt counts as reached"
    - "Every encounter still unreached after the retry budget is individually documented in 23-FINDINGS.md with level, position, mechanic type, and technical reason"
    - "scripts/lib/mechanic-drive.mjs and scripts/browser-boot.mjs remain byte-identical — the retry upgrade is isolated to the mechanic-drive audit path only"
  artifacts:
    - path: "scripts/lib/audit-retry.mjs"
      provides: "Bounded OR-across-attempts retry wrapper composing mechanic-drive.mjs's unmodified exports"
      contains: "export async function auditLevelWithRetries"
    - path: ".planning/phases/23-level-validation-harness/23-FINDINGS.md"
      provides: "VALID-03 groundwork evidence: retry-harness results table + individually-documented exclusions"
      contains: "VALID-03 groundwork"
  key_links:
    - from: "scripts/audit-phase21-mechanics.mjs"
      to: "scripts/lib/audit-retry.mjs"
      via: "imports and calls auditLevelWithRetries once per level, supplying a reloadLevel callback"
      pattern: "auditLevelWithRetries"
---

<objective>
Shrink the interactive mechanic-drive audit's 6/16 encounter blind spot with a bounded retry strategy, without touching the traversal model itself — this is Roadmap Phase 23 Success Criterion 4 (VALID-03 groundwork; full 8-level closure is Phase 28's job).

Purpose: 22-FINDINGS.md's Baseline documents that several unreached encounters are timing-sensitive (they flip between identical-code runs), not fundamentally unreachable. A 3–5-attempt OR-across-attempts retry wrapper distinguishes genuinely-unreachable encounters from merely timing-sensitive ones, per CONTEXT's locked design.
Output: `scripts/lib/audit-retry.mjs` (new retry-wrapper module) + an in-place upgrade to `scripts/audit-phase21-mechanics.mjs` + a new `.planning/phases/23-level-validation-harness/23-FINDINGS.md` recording real retry-harness results and every remaining documented exclusion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/23-level-validation-harness/23-CONTEXT.md
@.planning/phases/23-level-validation-harness/23-RESEARCH.md
@.planning/phases/23-level-validation-harness/23-PATTERNS.md
@.planning/phases/23-level-validation-harness/23-VALIDATION.md
@.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
</context>

## Artifacts this phase produces

- `scripts/lib/audit-retry.mjs` — new module. Exports `auditLevelWithRetries(page, level, { maxAttempts = 5, reloadLevel } = {})` returning a `Map` of `${tag}@${x}` → `{ triggered, resolved, attempts }`. Imports `deriveEncounters`/`driveToXClimbing`/`resolveIfBoxed` from `./mechanic-drive.mjs` UNCHANGED.
- `scripts/audit-phase21-mechanics.mjs` — upgraded in place (per CONTEXT's discretion) to drive each level through `auditLevelWithRetries` instead of a single pass; same PORT 8768, same SAVE_KEY/SAVE_BLOB, same final `AUDIT: ALL MECHANICS RESOLVED`/`AUDIT: FAILURES DETECTED` JSON reporting contract.
- `.planning/phases/23-level-validation-harness/23-FINDINGS.md` — new file, `## Interactive Audit Retry Harness (VALID-03 groundwork)` section (Plan 23-05, Wave 3, later APPENDS its own RED-first-proof section to this same file — never overwrites this one).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the bounded OR-across-attempts retry wrapper</name>
  <files>scripts/lib/audit-retry.mjs</files>
  <read_first>
    - scripts/lib/mechanic-drive.mjs (exact exported signatures of `deriveEncounters(geometry)`, `driveToXClimbing(page, targetX, opts)`, `resolveIfBoxed(page, renderChoices)` — these three imports must stay byte-identical, this plan never edits this file)
    - scripts/audit-phase21-mechanics.mjs (the current single-pass caller — its per-level loop structure, PORT 8768, and `warmupUntilFirstGap` usage for the level's first/lowest-x encounter, all of which Task 2 will restructure around this wrapper)
    - .planning/phases/23-level-validation-harness/23-RESEARCH.md (Pitfall 5 — "Full-level reload cost multiplying the retry harness's runtime unnecessarily" — the reason already-triggered encounters must be skipped on later attempts, not re-driven)
  </read_first>
  <behavior>
    - Given attempt 1's result for key `math-gate@1300` is `{triggered: false}` and attempt 2's result for the same key is `{triggered: true, resolved: true}`, the wrapper's final returned Map entry for that key has `triggered: true, resolved: true` (OR-across-attempts, never let a later attempt's own state erase an earlier attempt's success)
    - Given a key already has `{triggered: true, resolved: true}` recorded after attempt 1, attempt 2 does not call `driveToXClimbing` or `resolveIfBoxed` again for that key (skip re-driving proven-reachable encounters — the reload still happens, only the per-encounter re-drive is skipped)
    - Given every encounter's key is `triggered: true` after attempt 3 (with `maxAttempts = 5`), the wrapper does not call `reloadLevel` a 4th or 5th time — it exits the attempts loop early
    - Given `maxAttempts` is not supplied, the wrapper defaults to 5 (the upper bound of CONTEXT's 3–5 range, matching the "effort ceiling" framing)
  </behavior>
  <action>
    Create scripts/lib/audit-retry.mjs. Import `deriveEncounters`, `driveToXClimbing`, `resolveIfBoxed` from `./mechanic-drive.mjs` — do not modify that file. Export `async function auditLevelWithRetries(page, level, { maxAttempts = 5, reloadLevel } = {})`. `reloadLevel` is a caller-supplied `async () => void` callback that re-enters the level fresh (the wrapper owns no navigation of its own — navigation mechanics belong to the caller script, per CONTEXT's "changes isolated to the mechanic-drive audit path" constraint, which this plan interprets as "the wrapper composes existing exports and knows nothing about select-screen navigation").

    Maintain a `results` Map keyed by `` `${tag}@${x}` ``. Compute `encounters = deriveEncounters(level.geometry)` once and `firstFloorEnd` the same way the existing script does (level.geometry.floors?.[0] end x, or 0). For `attempt` from 1 to `maxAttempts`: if `attempt > 1`, `await reloadLevel()` first. For each encounter in order: if `results.get(key)?.triggered` is already true, skip driving it entirely (Pitfall 5's cost guard) but still count it toward the "every encounter triggered" early-exit check. Otherwise compute `driveOpts = encounter.x < firstFloorEnd ? { warmupUntilFirstGap: true } : {}`, call `driveToXClimbing(page, encounter.x, driveOpts)`, and if `triggered && encounter.renderChoices` call `resolveIfBoxed(page, true)`; merge the outcome into `results` with `triggered: triggered || previous?.triggered`, `resolved: resolved ?? previous?.resolved ?? null`, `attempts: (previous?.attempts ?? 0) + 1`; if this encounter did NOT trigger on this attempt, stop driving further encounters for this attempt (preserves the existing sequential-approach semantics — an untriggered mechanic blocks progress to later ones in the same pass). After each attempt, if every encounter's key is `triggered: true` in `results`, break out of the attempts loop early. Return the final `results` Map.
  </action>
  <verify>
    <automated>node --check scripts/lib/audit-retry.mjs && [ "$(grep -c "from './mechanic-drive.mjs'" scripts/lib/audit-retry.mjs)" -ge 1 ] && [ "$(grep -c 'export async function auditLevelWithRetries' scripts/lib/audit-retry.mjs)" -eq 1 ] && [ "$(grep -c 'reloadLevel' scripts/lib/audit-retry.mjs)" -ge 1 ] && [ "$(grep -c 'maxAttempts = 5' scripts/lib/audit-retry.mjs)" -ge 1 ] && echo RETRY-WRAPPER-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/lib/audit-retry.mjs` passes
    - The module imports `deriveEncounters`, `driveToXClimbing`, `resolveIfBoxed` from `./mechanic-drive.mjs` and defines no competing traversal logic of its own
    - The exported function signature is `auditLevelWithRetries(page, level, { maxAttempts = 5, reloadLevel } = {})`
    - An already-triggered encounter is never re-driven on a later attempt (the skip-if-already-triggered branch exists before any `driveToXClimbing` call)
    - The attempts loop exits early once every encounter key is `triggered: true`, without calling `reloadLevel` for unused remaining attempts
  </acceptance_criteria>
  <done>scripts/lib/audit-retry.mjs exists, composes mechanic-drive.mjs's three unmodified exports, and implements bounded (default 5) OR-across-attempts retry semantics with an early exit and a per-encounter skip-when-already-reached optimization.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the caller script, run it, and record the retry-harness evidence</name>
  <files>scripts/audit-phase21-mechanics.mjs, .planning/phases/23-level-validation-harness/23-FINDINGS.md</files>
  <read_first>
    - scripts/audit-phase21-mechanics.mjs (the file being modified — its current per-level loop: select-cursor navigation, `page.keyboard.press("Enter")`, `deriveEncounters`/`driveToXClimbing`/`resolveIfBoxed` calls, screenshot capture, and the final `results`/`AUDIT:` reporting block at the bottom)
    - scripts/lib/audit-retry.mjs (just built in Task 1 — the exact `auditLevelWithRetries` signature to call)
    - .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md (the "Post-Fix Regression" 16-row table and its "Baseline ground-truth" stable-core/timing-sensitive classification — the exact numbers this task's new run must be compared against)
    - .planning/milestones/v4.1-phases/21-real-verification-pass-mechanics-sign-off-integrity-perform-/21-FINDINGS.md (the Methodology Note style for documenting an individually-excluded encounter — mirror this format for any row still unreached after retries)
  </read_first>
  <action>
    In scripts/audit-phase21-mechanics.mjs, add `import { auditLevelWithRetries } from "./lib/audit-retry.mjs";`. Inside the existing `for (let i = 0; i < LEVEL_ORDER.length; i++)` loop, after the level is first entered (existing Enter press + 1500ms wait), define an inline `async function reloadLevel()` closure that performs exactly the navigation the current loop already does to re-enter this same level: press Escape, wait 800ms, move the select cursor back to tile `i` via `i` ArrowRight presses (150ms apart, matching the existing cursor-repositioning code), press Enter, wait 1500ms. Replace the current single-pass `for (const [encounterIdx, encounter] of encounters.entries())` block with one call: `const levelResults = await auditLevelWithRetries(page, level, { maxAttempts: 5, reloadLevel })`. Convert `levelResults` (a Map) into the same flat `results` array shape the script already builds and prints (`{level: level.id, tag, x, reachedX: null, triggered, resolved}` per entry — `reachedX` is not tracked by the wrapper's Map; omit it or set null, since only `triggered`/`resolved` feed the existing pass/fail logic at the bottom of the file, which reads only `r.triggered`/`r.resolved`/`r.tag`). Keep PORT 8768, SAVE_KEY, SAVE_BLOB, the Playwright resolution block, the screenshot calls, and the final `AUDIT: ALL MECHANICS RESOLVED`/`AUDIT: FAILURES DETECTED` JSON-printing logic UNCHANGED. Update the file's header comment to note the Phase 23 upgrade: retry-wrapper composition, `maxAttempts=5`, still a diagnostic tool that always exits 0.

    Run `node scripts/audit-phase21-mechanics.mjs` against the real, untouched levels 1-4. Create `.planning/phases/23-level-validation-harness/23-FINDINGS.md` (this is the FIRST write to this file this phase) with a `## Interactive Audit Retry Harness (VALID-03 groundwork)` section containing: (a) the full final per-encounter results table (Level | Mechanic | x | Triggered | Resolved | Attempts, sourced from this run's own printed JSON, not copied from any prior phase's table); (b) an explicit note on which of the three documented timing-sensitive rows (level-01 math-gate x1300, level-01 door x1400, level-03 math-gate x420, per 22-FINDINGS.md's Baseline section) now register as reliably triggered across the retry budget; (c) one individually-documented exclusion entry (level + position + mechanic type + technical reason, mirroring 21-FINDINGS.md's Methodology Note style) for every encounter STILL unreached after 5 attempts; (d) an explicit reached-encounter count comparison against 22-FINDINGS.md's documented baseline (10/16 in Run 1, up to 11/16 after Cluster A fixes) — record whatever this run's real count actually is, do not assume a specific outcome in advance.
  </action>
  <verify>
    <automated>node scripts/audit-phase21-mechanics.mjs; echo "EXIT:$?"; [ "$(grep -c 'VALID-03 groundwork' .planning/phases/23-level-validation-harness/23-FINDINGS.md)" -ge 1 ] && [ "$(grep -c 'Attempts' .planning/phases/23-level-validation-harness/23-FINDINGS.md)" -ge 1 ] && git diff --quiet -- scripts/lib/mechanic-drive.mjs scripts/browser-boot.mjs && echo RETRY-HARNESS-RUN-OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/audit-phase21-mechanics.mjs` still exits 0 and still prints the JSON `results` array followed by either `AUDIT: ALL MECHANICS RESOLVED` or `AUDIT: FAILURES DETECTED` (the existing pass/fail reporting contract is unchanged)
    - The upgraded script drives every level through up to 5 attempts via `auditLevelWithRetries`, not a single pass
    - `.planning/phases/23-level-validation-harness/23-FINDINGS.md` exists with a `## Interactive Audit Retry Harness (VALID-03 groundwork)` section containing a per-encounter table and one individually-documented exclusion entry (level + position + mechanic + reason) for every row still unreached after 5 attempts
    - The section states a concrete before/after reached-encounter count sourced from this real run's output
    - `git diff --quiet -- scripts/lib/mechanic-drive.mjs scripts/browser-boot.mjs` exits 0 (both files remain byte-identical — the upgrade is isolated to the audit-path caller and its new retry-wrapper module only)
  </acceptance_criteria>
  <done>The interactive audit reliably reaches more of the 16 encounters than the single-pass baseline (or explicitly documents why not), every still-excluded encounter carries an individual technical reason, and mechanic-drive.mjs/browser-boot.mjs remain untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-phase21-mechanics.mjs's local HTTP server → loopback | Pre-existing server (port 8768) that this plan's driving-loop restructure runs against, unmodified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Tampering | scripts/audit-phase21-mechanics.mjs's local static server | high | mitigate | Server/CR-02-guard code is NOT touched by this plan (only the per-level driving loop changes) — verified by the Task 2 acceptance criterion diffing the file's server section is unchanged from its already-hardened Phase 21 form |
| T-23-02 | Denial of Service | malformed level geometry crashing the retry wrapper | low | mitigate | `auditLevelWithRetries` consumes `deriveEncounters(level.geometry)`, which already `?? []`-guards every optional array (mechanic-drive.mjs, unmodified) — no new unguarded geometry access is introduced |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `node scripts/lib/audit-retry.mjs` module loads cleanly (node --check) and exposes the documented signature
- `node scripts/audit-phase21-mechanics.mjs` runs against real levels 1-4 with the retry wrapper wired in, still exits 0, still prints its established JSON + AUDIT: line contract
- 23-FINDINGS.md records real retry-harness results with individually-documented exclusions
- mechanic-drive.mjs and browser-boot.mjs remain byte-identical to their pre-plan state
</verification>

<success_criteria>
- The interactive mechanic-drive audit's blind spot shrinks (or is proven not to, with real evidence) via a bounded, documented retry strategy — Roadmap Phase 23 Success Criterion 4 satisfied as VALID-03 groundwork
- Every remaining excluded encounter has an individually-documented technical reason, ready for Phase 28's final closure
</success_criteria>

<output>
Create `.planning/phases/23-level-validation-harness/23-02-SUMMARY.md` when done.
</output>