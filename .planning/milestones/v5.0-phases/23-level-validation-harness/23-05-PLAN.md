---
phase: 23-level-validation-harness
plan: 05
type: execute
wave: 3
depends_on: ["23-02", "23-03", "23-04"]
files_modified:
  - scripts/validate-levels.mjs
  - .planning/phases/23-level-validation-harness/23-FINDINGS.md
autonomous: true
requirements: [VALID-01, VALID-02]

must_haves:
  truths:
    - "node scripts/validate-levels.mjs checks spawn-goal reachability, gap widths vs the calibrated jump envelope, door-over-hole placement, and mechanic reachability on every registered level, exiting non-zero on any HARD-FAIL"
    - "node scripts/validate-levels.mjs --fixture scripts/fixtures/bad-level.js proves the validator goes RED independent of levels 1-4's specific content"
    - "Run against the untouched levels 1-4, the validator non-zero-exits and 23-FINDINGS.md names all three known over-hole defects (level-01 x600, level-01 x1300, level-04 x1800)"
    - "Zero level-descriptor edits landed anywhere in this phase — the RED-first proof is against genuinely untouched levels 1-4"
  artifacts:
    - path: "scripts/validate-levels.mjs"
      provides: "Standalone level-validator CLI orchestrator composing over-hole-check.mjs + reachability.mjs (VALID-01)"
      contains: "export"
    - path: ".planning/phases/23-level-validation-harness/23-FINDINGS.md"
      provides: "RED-first proof evidence section (VALID-02), appended alongside Plan 23-02's retry-harness section"
      contains: "RED-First Proof"
  key_links:
    - from: "scripts/validate-levels.mjs"
      to: "scripts/lib/over-hole-check.mjs"
      via: "imports and calls findOverHoleBarriers once per level"
      pattern: "findOverHoleBarriers"
    - from: "scripts/validate-levels.mjs"
      to: "scripts/lib/reachability.mjs"
      via: "imports and calls checkLevelReachability once per level"
      pattern: "checkLevelReachability"
---

<objective>
Compose the promoted over-hole checker and the new Δy-aware reachability module into the real `scripts/validate-levels.mjs` gate, then prove it RED against the untouched levels 1-4 before it can ever be trusted.

Purpose: VALID-01 requires a standalone validator checking all four roadmap-named properties on every registered level, exiting non-zero on failure. VALID-02 requires that validator be PROVEN by independently catching the two known live bug classes (door-over-hole, unreachable areas) — this is the phase's capstone: without this proof, Wave 1/2's modules are unverified in composition.
Output: `scripts/validate-levels.mjs` (the real, standalone gate) and a new `## RED-First Proof (VALID-02)` section in `.planning/phases/23-level-validation-harness/23-FINDINGS.md`, appended alongside Plan 23-02's retry-harness section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/23-level-validation-harness/23-CONTEXT.md
@.planning/phases/23-level-validation-harness/23-RESEARCH.md
@.planning/phases/23-level-validation-harness/23-PATTERNS.md
@.planning/phases/23-level-validation-harness/23-VALIDATION.md
@.planning/phases/23-level-validation-harness/23-02-SUMMARY.md
@.planning/phases/23-level-validation-harness/23-03-SUMMARY.md
@.planning/phases/23-level-validation-harness/23-04-SUMMARY.md
@.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
</context>

## Artifacts this phase produces

- `scripts/validate-levels.mjs` — new standalone CLI entry point. No exports (script, not a library). Invocation: `node scripts/validate-levels.mjs` (checks every `LEVEL_ORDER` entry) or `node scripts/validate-levels.mjs --fixture <path>` (checks only the fixture module's exported level-shaped constant). Never wired into `check-gate.sh` (CONTEXT-locked: standalone only).
- `.planning/phases/23-level-validation-harness/23-FINDINGS.md` — new `## RED-First Proof (VALID-02)` section appended (the file already exists from Plan 23-02's Wave-1 retry-harness section — this plan uses `Edit`, never `Write`, to avoid clobbering it).

<tasks>

<task type="auto">
  <name>Task 1: Build the validate-levels.mjs orchestrator with --fixture support</name>
  <files>scripts/validate-levels.mjs</files>
  <read_first>
    - scripts/lib/over-hole-check.mjs (the exact `findOverHoleBarriers(geometry)` signature and return shape — built in Plan 23-03)
    - scripts/lib/reachability.mjs (the exact `checkLevelReachability(geometry, envelope)` signature and `{rows, hardFailCount}` return shape — built in Plan 23-04)
    - scripts/fixtures/bad-level.js (the `BAD_LEVEL` shape this task's `--fixture` mode must consume — built in Plan 23-03)
    - scripts/smoke-progress.mjs (the top-of-file documentation-block convention and the `check(cond, msg)`/failures-counter/`process.exit(1)` idiom to mirror, adapted here to per-row reporting instead of per-assertion)
    - src/levels/index.js (`LEVEL_ORDER`/`getLevel` — the registry this script iterates when no `--fixture` flag is given)
    - .planning/phases/23-level-validation-harness/23-CONTEXT.md ("Validator Check Design" — the locked output format: human-readable per-level stdout report of level id / check name / PASS-FAIL(-WARN) / offending descriptor, non-zero exit on any HARD-FAIL, standalone script never wired into check-gate.sh)
  </read_first>
  <action>
    Create scripts/validate-levels.mjs. Import `LEVEL_ORDER`, `getLevel` from `../src/levels/index.js`; `findOverHoleBarriers` from `./lib/over-hole-check.mjs`; `checkLevelReachability` from `./lib/reachability.mjs`.

    Parse `process.argv` for an optional `--fixture <path>` pair (find the flag's index, take the following argv element as a repo-root-relative path). Build the list of `{id, geometry}` descriptors to check: if `--fixture` was supplied, dynamically import it via `pathToFileURL(resolve(fixturePath)).href` (using `path.resolve` and `url.pathToFileURL`), find the first exported value carrying a `.geometry` property, and use only that single descriptor; otherwise map `LEVEL_ORDER` through `getLevel(id)`.

    For each descriptor, run `findOverHoleBarriers(descriptor.geometry)` and print one stdout line per returned row: `` `${descriptor.id} | over-hole | HARD-FAIL | ${row.kind} footprint ${row.footprint[0]}..${row.footprint[1]}` `` (or a single PASS line `` `${descriptor.id} | over-hole | PASS | (no floating barriers)` `` when the array is empty). Run `checkLevelReachability(descriptor.geometry)` and print one stdout line per row in the shape `` `${descriptor.id} | ${row.check} | ${row.status} | ${row.descriptor}` ``. Maintain a running `failures` counter incremented by the over-hole row count plus `hardFailCount` from `checkLevelReachability` — WARN rows never increment `failures`, only HARD-FAIL rows (per the CONTEXT-locked two-tier design).

    After iterating every descriptor, print `` `validate-levels: FAIL — ${failures} hard-failure(s) across ${descriptorCount} level(s)` `` and `process.exit(1)` if `failures > 0`; otherwise print `validate-levels: PASS` and let the process exit 0.

    Add a top-of-file documentation block mirroring smoke-progress.mjs's header style: no JS test framework in this project, this script IS the automated structural gate for level geometry, run via `node scripts/validate-levels.mjs` (add `--fixture <path>` for the self-test), and it is deliberately standalone — never wired into `check-gate.sh` — matching the project's existing no-npm, direct-`node`-invocation pattern already used by `smoke-progress.mjs` and `browser-boot.mjs`. The script never imports `playwright`/`chromium` — it must stay fast enough for routine, per-commit use.
  </action>
  <verify>
    <automated>node --check scripts/validate-levels.mjs && [ "$(grep -c "'--fixture'" scripts/validate-levels.mjs)" -ge 1 ] && [ "$(grep -c 'findOverHoleBarriers' scripts/validate-levels.mjs)" -ge 1 ] && [ "$(grep -c 'checkLevelReachability' scripts/validate-levels.mjs)" -ge 1 ] && [ "$(grep -c 'chromium\|playwright' scripts/validate-levels.mjs)" -eq 0 ] && echo VALIDATOR-SKELETON-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs --fixture scripts/fixtures/bad-level.js` exits non-zero and its stdout contains at least one `over-hole | HARD-FAIL` row citing the mathGate's footprint and at least one `spawn-goal | HARD-FAIL` (or `gap-width | HARD-FAIL`) row
    - `node scripts/validate-levels.mjs` (no flag) iterates all entries of the live `LEVEL_ORDER` import, not a hardcoded level count
    - No WARN-tier row ever increments the `failures` counter — confirmed by construction (only HARD-FAIL rows feed the counter)
    - The file contains zero references to `chromium`/`playwright` — the routine gate never launches a browser
  </acceptance_criteria>
  <done>scripts/validate-levels.mjs exists, runs both checks against either the real registry or a --fixture override, prints per-level/per-check PASS/WARN/HARD-FAIL rows with offending descriptors, and exits non-zero on any HARD-FAIL.</done>
</task>

<task type="auto">
  <name>Task 2: RED-first proof against the untouched levels 1-4</name>
  <files>.planning/phases/23-level-validation-harness/23-FINDINGS.md</files>
  <read_first>
    - .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md ("Structural Defect Inventory" section — the exact 3 known over-hole rows: level-01 mathGate x600..632 / x1300..1332, level-04 mathGate x1800..1832; and the 8 heuristic-candidate platforms this task's WARN/PASS/HARD-FAIL output now arbitrates: level-03 x1880/x2640, level-04 x1080/x1400/x1760/x2140/x2520/x3240)
    - .planning/phases/23-level-validation-harness/23-CONTEXT.md ("RED-First Proof & Findings Documentation" — the exact "Proven RED" definition and the requirement to distinguish HARD-FAIL from WARN in the evidence table, not collapse them)
    - .planning/phases/23-level-validation-harness/23-FINDINGS.md (the file Plan 23-02 already created in Wave 1 with a `## Interactive Audit Retry Harness (VALID-03 groundwork)` section — this task APPENDS a new section via Edit; it must never overwrite that existing content)
  </read_first>
  <action>
    First, confirm levels 1-4 are still byte-identical to the Phase 22 baseline: extract the baseline SHA via `sed -n 's/^Baseline commit: //p' .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md | head -1`, then run `git diff --quiet <that-sha> -- src/levels/level-01.js src/levels/level-02.js src/levels/level-03.js src/levels/level-04.js` and confirm it exits 0. This proves the RED-first proof about to be recorded is genuinely against untouched content — this phase makes ZERO level-descriptor edits (Phase 24 owns fixes, per the roadmap-locked sequencing CONTEXT re-states as non-negotiable).

    Run `node scripts/validate-levels.mjs` (no flag, the real registry) and capture its full stdout and exit code. Using the `Edit` tool, append a new `## RED-First Proof (VALID-02)` section to `.planning/phases/23-level-validation-harness/23-FINDINGS.md` (append after the existing content — do not touch or reorder Plan 23-02's `## Interactive Audit Retry Harness` section) containing: (a) the observed exit code (must be non-zero); (b) the verbatim over-hole HARD-FAIL rows, explicitly confirming they name level-01's x600 and x1300 mathGates and level-04's x1800 mathGate — quote the actual printed footprints from this run's output, not paraphrased values; (c) the verbatim reachability rows, stating for each of Phase 22's 8 heuristic-candidate platforms whether the calibrated envelope now resolves it to PASS, WARN, or HARD-FAIL (this is the "Phase 23's calibrated envelope is the arbiter" claim made concrete with real output — quote the real descriptor strings, do not summarize away the HARD-FAIL/WARN distinction); (d) the git-diff confirmation from above, stated explicitly as evidence that zero level-descriptor edits landed this phase.
  </action>
  <verify>
    <automated>node scripts/validate-levels.mjs; echo "EXIT:$?"; [ "$(grep -c 'RED-First Proof' .planning/phases/23-level-validation-harness/23-FINDINGS.md)" -ge 1 ] && [ "$(grep -c '600' .planning/phases/23-level-validation-harness/23-FINDINGS.md)" -ge 1 ] && [ "$(grep -c '1800' .planning/phases/23-level-validation-harness/23-FINDINGS.md)" -ge 1 ] && [ "$(grep -c 'Interactive Audit Retry Harness' .planning/phases/23-level-validation-harness/23-FINDINGS.md)" -ge 1 ] && BASELINE=$(sed -n 's/^Baseline commit: //p' .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md | head -1) && git diff --quiet "$BASELINE" -- src/levels/level-01.js src/levels/level-02.js src/levels/level-03.js src/levels/level-04.js && echo RED-PROOF-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-levels.mjs` exits non-zero when run against the real, untouched levels 1-4
    - 23-FINDINGS.md's new `## RED-First Proof (VALID-02)` section explicitly names level-01 x600, level-01 x1300, and level-04 x1800 as over-hole HARD-FAIL rows (the roadmap's exact three known defects)
    - `git diff --quiet <baseline-sha> -- src/levels/level-01.js src/levels/level-02.js src/levels/level-03.js src/levels/level-04.js` exits 0, proving zero descriptor edits landed this phase
    - The new section distinguishes HARD-FAIL rows from WARN rows for the 8 heuristic-candidate platforms individually, never collapsing the two tiers into one verdict
    - The `## Interactive Audit Retry Harness (VALID-03 groundwork)` section written by Plan 23-02 in Wave 1 is still present, unmodified, in 23-FINDINGS.md — proving this task appended rather than overwrote
  </acceptance_criteria>
  <done>23-FINDINGS.md records a real, machine-verified RED-first proof: the validator non-zero-exits against untouched levels 1-4, specifically names all three known live over-hole defects, and individually arbitrates the 8 heuristic candidates — with zero level-descriptor edits anywhere in the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level descriptor (real or fixture) → validate-levels.mjs orchestrator | Composes two already-hardened pure-data modules; input stays trusted, developer-authored data throughout |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-02 | Denial of Service | malformed level descriptor or `--fixture` path crashing the orchestrator | low | mitigate | The orchestrator delegates all geometry parsing to already-`?? []`-guarded `findOverHoleBarriers`/`checkLevelReachability`; a missing/unreadable `--fixture` path fails the dynamic `import()` with a clear thrown error rather than silently reporting a false PASS |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase; the orchestrator imports zero new dependencies (Node built-ins + the two Phase 23 lib modules only) |
</threat_model>

<verification>
- `node scripts/validate-levels.mjs --fixture scripts/fixtures/bad-level.js` exits non-zero and names both injected defects
- `node scripts/validate-levels.mjs` (real registry) exits non-zero against the untouched levels 1-4, naming all three known over-hole defects
- `git diff` against the Phase 22 baseline SHA confirms zero level-descriptor edits landed anywhere in Phase 23
- 23-FINDINGS.md carries both the Plan 23-02 retry-harness section AND this plan's RED-first-proof section, neither overwriting the other
</verification>

<success_criteria>
- VALID-01 is fully satisfied: a standalone validator checks spawn-goal reachability, gap widths, door-over-hole, and mechanic reachability on every registered level and exits non-zero on any HARD-FAIL
- VALID-02 is fully satisfied: the validator's jump envelope is empirically calibrated (Plan 23-01) and the validator is PROVEN by independently catching both known live bug classes before Phase 24 is allowed to trust it as a gate
</success_criteria>

<output>
Create `.planning/phases/23-level-validation-harness/23-05-SUMMARY.md` when done.
</output>
