---
phase: 28-full-verification-interactive-sign-off
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified: []
autonomous: false
requirements: [VALID-03]
must_haves:
  truths:
    - "A human has played all 8 levels start->goal in the running game (not a spot-check subset) and confirmed each is completable"
    - "A human has confirmed each level's distinct theme, the Nox Run logo, and the audio all read correctly TOGETHER in the finished game -- a holistic pass, not a re-litigation of the pieces Phases 26/27 already individually signed off"
    - "The sign-off (or any genuinely new issue found and its resolution/deferral) is recorded verbatim in this plan's SUMMARY, not merely implied by automation passing"
  artifacts:
    - "A recorded human sign-off decision for the consolidated levels+themes+logo+audio review, captured in this plan's SUMMARY"
  key_links:
    - "This checkpoint is the phase's one non-automatable verification -- CLAUDE.md's standing 'no phase closes on greps/automation alone' rule and this project's own never-rubber-stamp-checkpoints precedent (Phase 25, reconfirmed at Phase 27's 5-round sign-off) both apply directly; it must NOT be auto-approved under workflow.auto_advance"
---

<objective>
Get the one verification this phase cannot automate: a genuine human playthrough of all 8 levels, start to goal, confirming the finished game -- levels, per-level themes, logo, and audio -- feels right end-to-end, together, in the running game.

Purpose: Plan 28-01 already closed every automatable claim (the consolidated 8-gate suite green, the audio-gesture-gate proof, the save-resume-across-reload proof). ROADMAP criterion 4 explicitly requires human sign-off "for levels, per-level themes, logo, and audio in the running game -- no claim closes on automation alone." Each piece already got its OWN dedicated sign-off in Phase 26 (visuals/logo) and Phase 27 (audio) -- per 28-CONTEXT.md's decision, this pass is a holistic "does the finished game feel right end-to-end" review, not a re-litigation of already-approved pieces, and it must cover ALL 8 levels start->goal (not a spot-check), matching ROADMAP criterion 1's "all eight levels provably completable."
Output: a recorded sign-off decision (approve as-is, or specific named issues with their resolution/deferral) in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-full-verification-interactive-sign-off/28-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Consolidated levels + themes + logo + audio sign-off</name>
  <files></files>
  <action>
    Serve the game over HTTP and manually play through all 8 levels start->goal with sound on. This is a verification-only checkpoint; write no code unless a genuinely new, small, in-boundary issue is found (see below). Follow the &lt;how-to-verify&gt; steps precisely and record the reviewer's exact response in this plan's SUMMARY.md.

    If a genuinely NEW blocking issue surfaces (not one of the known-accepted items listed in &lt;how-to-verify&gt; step 9 below): follow the established Phase 26/27 pattern -- fix inline if it is small and clearly in-boundary (re-verify the relevant automated gate afterward), or capture it as a new backlog entry if it is a bigger design question (mirrors FIX-02's precedent from Phase 22). Do NOT silently fix nothing, and do NOT expand this checkpoint into a new feature-design discussion -- route bigger questions to the backlog exactly like 999.1/999.2 were captured.

    Do NOT re-raise any of the already-accepted deferred issues listed in step 9 as if they were newly discovered -- they are known and out of scope for this milestone.
  </action>
  <what-built>
The complete, closed v5.0 feature set under one running build: 8 pure-data levels (level-01..level-08) with a gentle difficulty ramp and per-level table pools, four forgiving in-world math mechanics wired through the shared challenge seam, 8 distinct per-level palette themes baked through the art pipeline, real CC0 door/enemy sprites, the Nox Run logo with its reveal animation on the title screen, a 2x4 level-select grid with locked/unlocked/cleared semantics, one secret XP alcove per level, and the full audio layer (7 SFX + ambient music + persisted M-key mute) tuned across Phase 27's 5-round human sign-off. Plan 28-01 has just re-confirmed the entire automated gate suite is green, including a fresh proof that audio starts only after the first gesture and that a save under the current key resumes correctly across a reload.
  </what-built>
  <how-to-verify>
Serve the game from the repo root (python3 -m http.server 8000, NOT from src/) and open http://localhost:8000/src/index.html in a real browser with sound on.

1. Open a fresh/incognito window. Confirm silence before pressing anything, then press Space/Enter/click to start -- confirm the ambient music begins and the Nox Run logo reveal plays smoothly (non-strobing, reads cleanly at real size).
2. From level-select, confirm all 8 tiles render in a 2x4 grid with locked/unlocked/cleared states visually distinct (grey+X for locked, accent-bordered for unlocked, blue-bordered+checkmark for cleared as levels are completed).
3. Play level-01 through level-08 IN ORDER, start to goal, on each one: reach and resolve at least one math mechanic (door, checkpoint gate, defeat-enemy, or collect-the-answer, whichever that level has), confirm the level's distinct background/accent theme tint is visually apparent and matches its neighbors' distinctness (no two adjacent levels should look identical), confirm the goal is reachable and clears the level.
4. Across all 8 levels, confirm the audio mix stays consistent with Phase 27's signed-off feel: music clearly below SFX, jump/pickup/correct/door/clear sounds all present and non-startling, wrong answers read as calm/neutral (never a buzzer).
5. On at least 2 levels, try pressing M mid-play to confirm mute still works everywhere and doesn't disrupt gameplay.
6. Confirm death/respawn (from a spike or enemy touch, if easy to trigger) never stacks or breaks the music, and never feels punishing (no lost progress beyond the last checkpoint).
7. Confirm the title screen's "press R to reset progress" control still works if you want to replay from a clean state, and that Reset Progress does not affect the mute setting.
8. Overall: does the finished game -- levels, themes, logo, audio -- feel like one coherent, ADHD-safe experience, not a collection of separately-approved pieces bolted together?
9. Known-accepted issues -- do NOT re-flag these as new findings, they are already accepted and out of this phase's scope: some pickups/ledges unreachable in levels 5-8 and level-07/08's end-climb sections are near-duplicates (25-UAT.md, accepted 2026-07-07); the secret alcove's invisible/silent reward reads as low-discoverability (pending todo, not actioned this milestone); the "n0x" logo wordmark shortening ask is deferred, needing clarification before scoping; backlog items 999.1 (reconsider the collect-the-answer mechanic) and 999.2 (pink spike hazard sprite) are explicitly out of scope for this milestone's closing phase.
  </how-to-verify>
  <resume-signal>
Reply with one of: "approved" (all 8 levels play start->goal, themes/logo/audio read correctly together, no new blocking issues) -- or describe the SPECIFIC new issue found (not one of the step-9 known-accepted items). A vague "looks good" is not sufficient to close VALID-03 -- name what you actually played and checked. This checkpoint does NOT auto-approve under auto_advance; a real, specific response is required before this plan (and VALID-03) can close.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Human playtester -> recorded sign-off decision | The only trust boundary in this plan is procedural: an unrecorded or rubber-stamped "looks good" would silently reintroduce the exact "no phase closes on greps/automation alone" failure mode CLAUDE.md's standing rule exists to prevent, for the milestone's FINAL closing claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-04 | Repudiation (an unverified "all 8 levels + themes + logo + audio are done" claim shipping as the milestone's final claim without real proof) | This checkpoint's resume-signal | high | mitigate | The resume-signal explicitly rejects a vague approval and requires the reviewer to have actually played all 8 levels; the checkpoint is gate="blocking-human" and is NOT eligible for auto_advance silent approval, matching the project's never-rubber-stamp-checkpoints precedent |

</threat_model>

<verification>
This plan's own checkpoint IS the phase's closing human verification for ROADMAP criteria 1 and 4. No further automated check is added here -- Plan 28-01 already closed every automatable claim (gate suite, audio-gesture-gate proof, save-resume proof).
</verification>

<success_criteria>
- A human has actually played all 8 levels start->goal in the running game, not a spot-check subset
- Per-level themes, the logo, and the audio are confirmed to read correctly TOGETHER, not merely as previously-isolated pieces
- A recorded decision (approved, or a specific named new issue and its resolution/deferral) exists in this plan's SUMMARY -- not a generic "looks good"
- No known-accepted deferred issue (25-UAT.md items, secret alcove discoverability, "n0x" ask, 999.1/999.2) is re-raised as if newly discovered
</success_criteria>

<output>
Create `.planning/phases/28-full-verification-interactive-sign-off/28-02-SUMMARY.md` when done. Record the reviewer's exact resume-signal response verbatim and, if any new issue was found, whether it was fixed inline (with re-verification) or captured to the backlog.
</output>

## Artifacts this phase produces (this plan's contribution)

- A recorded human sign-off decision for the consolidated levels+themes+logo+audio review (this plan's SUMMARY.md is the artifact -- no source files are modified by this plan unless a new, small, in-boundary issue is found and fixed inline)
