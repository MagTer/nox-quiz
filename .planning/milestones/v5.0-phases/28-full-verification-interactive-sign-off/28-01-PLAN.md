---
phase: 28-full-verification-interactive-sign-off
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/browser-boot.mjs
autonomous: true
requirements: [VALID-03]
must_haves:
  truths:
    - "node scripts/browser-boot.mjs proves, in the same fresh browser context it already boots, that zero <audio> elements exist before any title-screen gesture and exactly one exists immediately after the first Space press (not merely <=1)"
    - "node scripts/browser-boot.mjs proves that a save written under CONFIG.SAVE.KEY (noxrun_platformer_v1) survives a real page.reload() byte-for-byte, AND that the derived-unlock state it encodes is still correctly honored after the reload -- a level unlocked ONLY by the resumed save (level-03, never cleared this session) is genuinely enterable post-reload and its first mechanic encounter triggers"
    - "The full 8-command consolidated gate suite (check-safety.sh, check-import-safety.sh, check-gate.sh, check-progress.sh, check-audio.sh, check-rebrand.sh, node scripts/validate-levels.mjs, node scripts/browser-boot.mjs) all exit 0 in one sequential run"
  artifacts:
    - "scripts/browser-boot.mjs extended with two new proof functions: a pre/post-gesture audio-count check and a standalone save-resume-across-reload proof"
  key_links:
    - "The save-resume proof's post-reload reachability check routes through the SAME deriveEncounters/driveToXPlanned helpers the existing per-level drive already imports from scripts/lib/mechanic-drive.mjs -- proving genuine gameplay reachability after reload, not just a JSON string round-trip"
---

<objective>
Extend the existing real-browser boot check with the two automated proofs ROADMAP criterion 3 needs (reinterpreted per 28-CONTEXT.md's decision) but no prior script covers: (a) audio genuinely starts ONLY after the first title-screen gesture, never before, and (b) a save written under the CURRENT key persists and resumes correctly across a real page reload -- then run the complete, consolidated 8-command gate suite and confirm it is green in one pass.

Purpose: 28-CONTEXT.md's decisions section is explicit that ROADMAP criterion 3's original "a pre-rebrand save still resumes" clause is stale (Phase 26 intentionally renamed the save key with no migration) and must be replaced with a fresh-context proof that a save under the CURRENT key (noxrun_platformer_v1) resumes correctly, plus a stricter gesture-gate check than Phase 27 shipped. This plan is also this phase's consolidated automated-gate pass (ROADMAP criterion 2: "green in one run") -- the human sign-off in Plan 28-02 reviews a system that is already fully green, mirroring Phase 27's 27-06 -> 27-07 sequencing.
Output: scripts/browser-boot.mjs extended with both new proofs; a full 8-command gate-suite run recorded green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-full-verification-interactive-sign-off/28-CONTEXT.md
@.planning/phases/28-full-verification-interactive-sign-off/28-PATTERNS.md
@scripts/browser-boot.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audio-gesture-gate + save-resume-across-reload proofs to browser-boot.mjs</name>
  <files>scripts/browser-boot.mjs</files>
  <read_first>scripts/browser-boot.mjs (full file -- already read this session: the SAVE_KEY/SAVE_BLOB constants at lines 73-86, the assertAudioElementCount helper at lines 95-103, the module-scope browser/context/page setup at lines 134-136, the errors array + page event listeners at lines 138-147, the title->select gesture at lines 159-162, the per-level loop's row/col cursor math at lines 189-204 and mechanic-drive usage at lines 232-253, and the final errors.length > 0 / PASS reporting block at lines 271-287), src/audio.js (ensureMusicPlaying's module-level musicHandle idempotency guard, lines 78-85 -- this is what the pre/post-gesture proof is exercising), src/levels/index.js (LEVEL_ORDER order and isUnlocked's derivation: level i is unlocked iff LEVEL_ORDER[i-1] is cleared), scripts/lib/mechanic-drive.mjs (deriveEncounters and driveToXPlanned signatures -- both already imported by browser-boot.mjs at line 12)</read_first>
  <action>
Add TWO new proofs to scripts/browser-boot.mjs, both folding their failures into the script's existing top-level `errors` array (never throwing) so a single `node scripts/browser-boot.mjs` run covers everything in one pass.

Proof A -- audio starts only after the first gesture (insert into the EXISTING primary flow, no new browser context needed since nothing before this point in the existing flow produces audio):
- Immediately after the existing `await page.goto(...)` and its `await page.waitForTimeout(1500)` (letting Kaplay init and title paint), and BEFORE the existing SAVE_BLOB seeding `page.evaluate(...localStorage.setItem...)` call, add a check: evaluate `document.querySelectorAll('audio').length` and push a `{ type: "audio-gesture", message: ... }` entry into `errors` if it is not exactly 0 -- proving no audio element exists purely from page load, before any interaction or storage seed.
- Immediately after the existing title->select Space press and its `await page.waitForTimeout(800)` (the same spot the existing `assertAudioElementCount(page, errors, "title->select")` call already runs), add a SECOND, stricter check: evaluate `document.querySelectorAll('audio').length` and push a `{ type: "audio-gesture", message: ... }` entry if it is not exactly 1 -- proving audio genuinely started as a direct result of that one gesture (the existing `assertAudioElementCount` call only enforces a `<=1` ceiling and stays as-is; this is an additional, stricter `===1` floor check at the same stop, not a replacement).

Proof B -- a save under the current key persists and resumes correctly across a real reload (a new, self-contained async function using its OWN fresh browser context, isolated from the primary drive so a mid-script `page.reload()` never disturbs the primary per-level loop's state):
- Define a new function (e.g. `runSaveResumeAcrossReloadProof(errors)`) declared alongside the existing helpers, referencing the module-scope `browser` constant (already in scope by call time via closure -- do not pass it as a parameter).
- Inside: open a fresh `context`/`page` pair via `browser.newContext(...)` / `context.newPage()` (mirrors the module-scope setup at lines 134-136), navigate to the same served URL, wait for Kaplay init.
- Build a NEW, distinct blob (e.g. `RESUME_SAVE_BLOB`) under the SAME `SAVE_KEY` constant already defined in this file, shaped exactly like the existing `SAVE_BLOB` (version: 2, xp, level, accuracy: {}, history: {}) but with `levels` set to ONLY `{ "level-01": { cleared: true }, "level-02": { cleared: true } }` -- a deliberately partial prefix (unlike the existing SAVE_BLOB's near-full unlock), chosen so `level-03` becomes reachable ONLY via a correctly-resumed derived-unlock (a fresh/empty save would leave only level-01 open) while `level-04` and beyond stay locked, making this a meaningful proof rather than a vacuous one.
- Seed `RESUME_SAVE_BLOB` via `page.evaluate` exactly like the existing seeding call. Immediately capture the written value with `const preReloadValue = await page.evaluate(() => localStorage.getItem(key), ...)` (pass `SAVE_KEY`) -- comparing the actual written STRING, not re-deriving JSON, sidesteps any key-ordering concerns.
- Press Space to reach select (harmless; also exercises the gesture gate a second time), wait.
- Call `await page.reload({ waitUntil: "networkidle" })`, then wait ~1500ms for Kaplay to reinit (mirrors the initial boot wait).
- Re-read `localStorage.getItem(SAVE_KEY)` and push a `{ type: "save-resume", message: ... }` error into `errors` if it does not strictly equal `preReloadValue` -- proves byte-for-byte persistence survived the reload untouched.
- Press Space again (post-reload title->select), wait.
- Navigate the keyboard cursor with `page.keyboard.press("ArrowRight")` TWICE (default cursor starts at position 0 = level-01; since RESUME_SAVE_BLOB's cleared prefix leaves tiles 0/1/2 contiguously selectable in row 0, two ArrowRight presses lands the cursor on tile index 2 = level-03 -- mirrors the row/col stepping math the existing per-level loop already uses), then press Enter, then wait ~1500ms for the level to build.
- Using the already-imported `getLevel` and `deriveEncounters`, get `level-03`'s geometry and its first encounter's `x`. Call `driveToXPlanned(page, encounters[0].x, level.geometry)` and push a `{ type: "save-resume", message: "level-03 (resumed-unlock-only) encounter never triggered after reload" }` error if `triggered` is not `true` -- this is the behavioral proof that "resumes correctly" means real gameplay reachability, not just a localStorage byte check.
- In a `finally`, close this proof's OWN context (never the primary one).
- Call `runSaveResumeAcrossReloadProof(errors)` once, from inside the main try block, after the primary per-level drive and round-trip check complete but before the final `if (errors.length > 0) {...}` reporting block -- so its failures are folded into the same single PASS/FAIL determination.

Keep the existing final `if (errors.length > 0) { ... } else { console.log("Browser boot: PASS...") }` block unchanged in shape -- both new proofs simply add more entries to the same `errors` array that block already reports on.
  </action>
  <acceptance_criteria>
    - A pre-gesture check exists asserting exactly 0 audio elements right after initial page load, before any storage seed or interaction
    - A post-gesture check exists asserting exactly 1 audio element right after the first title->select Space press
    - A standalone `runSaveResumeAcrossReloadProof` function exists, using its own isolated browser context, that: seeds a partial-unlock save under CONFIG.SAVE.KEY's literal value, captures the pre-reload localStorage value, calls page.reload(), asserts the post-reload localStorage value strictly equals the pre-reload value, then navigates into the resumed-unlock-only level-03 tile and asserts its first mechanic encounter triggers via driveToXPlanned
    - node --check scripts/browser-boot.mjs passes
    - The script's existing primary per-level mechanic-drive logic, existing assertAudioElementCount call sites, and final PASS/FAIL reporting block are unchanged in shape -- only new error-producing checks are added
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/browser-boot.mjs && grep -q "runSaveResumeAcrossReloadProof" scripts/browser-boot.mjs && grep -q "page.reload(" scripts/browser-boot.mjs && grep -c "querySelectorAll('audio')" scripts/browser-boot.mjs | grep -qv '^0$' && echo "browser-boot.mjs audio-gesture + save-resume proofs added"</automated>
  </verify>
  <done>browser-boot.mjs proves audio is silent before the first gesture and starts exactly on it, and proves a save under the current key both byte-persists across a real reload and correctly resumes a genuinely-unlocked-only-by-save level (level-03) into playable gameplay.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full consolidated 8-command gate suite and confirm green</name>
  <files>(none -- verification only)</files>
  <read_first>scripts/check-safety.sh, scripts/check-import-safety.sh, scripts/check-gate.sh, scripts/check-progress.sh, scripts/check-audio.sh, scripts/check-rebrand.sh, scripts/validate-levels.mjs (their exact "<name> checks: PASS" / "validate-levels: PASS" echo conventions -- already confirmed this session)</read_first>
  <action>
Run all 8 gate commands in sequence and confirm every one exits 0: bash scripts/check-safety.sh, bash scripts/check-import-safety.sh, bash scripts/check-gate.sh, bash scripts/check-progress.sh, bash scripts/check-audio.sh, bash scripts/check-rebrand.sh, node scripts/validate-levels.mjs, node scripts/browser-boot.mjs (the last one now carrying Task 1's two new proofs). This is ROADMAP criterion 2's "one run, all green" requirement, expanded per 28-CONTEXT.md's instruction to include every cheap standing gate (not only the three ROADMAP names explicitly), since they are cheap and the spirit of the criterion is a single consolidated pass. If any command fails, fix the specific regression it names (never silence or skip the assertion) and re-run the FULL 8-command sequence from the top before marking this task done -- per CLAUDE.md's "no phase closes on greps/automation alone" standard, capture the literal PASS line each command prints (not a paraphrase) so Plan 28-03's VERIFICATION.md can cite exact re-run output.
  </action>
  <acceptance_criteria>
    - bash scripts/check-safety.sh exits 0, prints "safety checks: PASS"
    - bash scripts/check-import-safety.sh exits 0, prints "import-safety checks: PASS"
    - bash scripts/check-gate.sh exits 0, prints "gate checks: PASS"
    - bash scripts/check-progress.sh exits 0, prints "progress checks: PASS"
    - bash scripts/check-audio.sh exits 0, prints "audio checks: PASS"
    - bash scripts/check-rebrand.sh exits 0, prints "rebrand checks: PASS"
    - node scripts/validate-levels.mjs exits 0, prints "validate-levels: PASS"
    - node scripts/browser-boot.mjs exits 0, prints "Browser boot: PASS -- title -> select -> all levels loaded with no runtime errors.", including zero entries from Task 1's new audio-gesture and save-resume checks
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-gate.sh && bash scripts/check-progress.sh && bash scripts/check-audio.sh && bash scripts/check-rebrand.sh && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs && echo "CONSOLIDATED 8-GATE SUITE GREEN"</automated>
  </verify>
  <done>All 8 gate commands exit 0 in one sequential run, including the two new browser-boot.mjs proofs from Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Playwright-driven headless browser -> live game DOM/localStorage | Both new proofs read/write the actual rendered page's real localStorage and real `<audio>` elements, not a mock -- including across a genuine `page.reload()`, which is the closest this project's test harness gets to observing real persistence behavior |
| Fresh, isolated Playwright browser context -> localStorage | The save-resume proof's context is a brand-new, isolated context (never the primary drive's context), torn down after use -- test-only, never exposed to a real player |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering (a malformed/tampered localStorage save blob) | src/progress.js's loadSave()/validate() (read by both this plan's proof and every real player session) | medium | accept | Already mitigated by progress.js's existing explicit-field allowlist validation (named-key copying, never spread/Object.assign of the untrusted blob -- prototype-pollution mitigation, confirmed present pre-Phase-28); this plan's new proof exercises that existing gate under a real reload but does not modify the trust boundary itself |
| T-28-02 | Repudiation (an unverified "audio starts only on gesture" / "save resumes across reload" claim shipping as if verified) | scripts/browser-boot.mjs's two new proof functions | high | mitigate | Converts both claims from unverified assertions into automated, repeatable, real-browser proof (mirrors Phase 27's 27-06 T-27-09 disposition) -- pushed into the shared `errors` array so a real regression fails the run non-zero, not silently, on every future gate-suite invocation |
| T-28-03 | Information Disclosure (the save-resume proof's seeded test blob leaking into a real player's browser profile) | The isolated Playwright browser context used by runSaveResumeAcrossReloadProof | low | accept | Test-only, freshly-launched, headless context torn down (`context.close()`) immediately after the proof runs -- never a real player's profile, never persisted outside the test run |

</threat_model>

<verification>
Task 2's own action IS this plan's verification step -- run the full 8-command gate sequence and confirm all exit 0, with Task 1's two new proofs contributing zero entries to browser-boot.mjs's errors array.
</verification>

<success_criteria>
- browser-boot.mjs proves audio is silent before the first gesture and starts on it (exact counts, not a ceiling)
- browser-boot.mjs proves a save under the current key (noxrun_platformer_v1) survives a real reload byte-for-byte and correctly resumes into genuinely-reachable, previously-locked gameplay
- The complete 8-command gate suite (check-safety.sh, check-import-safety.sh, check-gate.sh, check-progress.sh, check-audio.sh, check-rebrand.sh, validate-levels.mjs, browser-boot.mjs) is green in one run
</success_criteria>

<output>
Create `.planning/phases/28-full-verification-interactive-sign-off/28-01-SUMMARY.md` when done. Record the literal PASS output line from all 8 gate commands -- this is the evidence Plan 28-03's VERIFICATION.md cites for ROADMAP criteria 2 and 3.
</output>

## Artifacts this phase produces (this plan's contribution)

- `scripts/browser-boot.mjs` extended with: a pre-gesture `document.querySelectorAll('audio').length === 0` check, a post-gesture `=== 1` check, and a new standalone `runSaveResumeAcrossReloadProof` function (isolated browser context, partial-unlock `RESUME_SAVE_BLOB`, byte-for-byte pre/post-reload localStorage comparison, and a post-reload `driveToXPlanned` reachability proof into `level-03`)
- A recorded 8-command consolidated gate-suite green run (check-safety.sh, check-import-safety.sh, check-gate.sh, check-progress.sh, check-audio.sh, check-rebrand.sh, validate-levels.mjs, browser-boot.mjs)