---
phase: 26
plan: 04
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - scripts/build-art-assets.py
  - assets/_opengameart-src/6-color-dungeon/16x16-dungeon-tiles.png
  - assets/_kenney-src/new-platformer-pack/saw_rest.png
  - assets/_kenney-src/new-platformer-pack/barnacle_attack_a.png
  - assets/_kenney-src/new-platformer-pack/fly_rest.png
  - assets/door.png
  - assets/enemy-1.png
  - assets/enemy-2.png
  - assets/enemy-3.png
  - assets/LICENSES/door.txt
  - assets/LICENSES/enemy.txt
  - CREDITS.md
autonomous: true
requirements: [VIS-04]

must_haves:
  truths:
    - "Real CC0 sprite art exists for the door (1 sprite) and enemies (3 distinct variants), sourced and license-proofed the same way as every prior asset in this project."
  artifacts:
    - "assets/door.png (32x64) — cropped/remapped from the already-vendored-family '6 Color Dungeon 16x16' sheet's gate tile"
    - "assets/enemy-1.png, enemy-2.png, enemy-3.png (32x32 each) — cropped/remapped from Kenney 'New Platformer Pack' v1.1's dedicated Enemies sprite set"
    - "assets/LICENSES/door.txt, assets/LICENSES/enemy.txt — CC0 proof files"
    - "CREDITS.md rows for both new assets"
  key_links:
    - "scripts/build-art-assets.py build_door()/build_enemies() -> assets/door.png, assets/enemy-{1,2,3}.png (consumed by main.js loadSprite in 26-05)"
---

<objective>
Source, vendor, license-proof, and bake real CC0 sprite art for the door and 3 distinct enemy variants, per VIS-04. This closes a debt item open since Phase 18 (v4.0) — the door and enemy have rendered as flat-color rect+glyph placeholders across two prior art passes.

Purpose: VIS-04 was added mid-discuss specifically because the placeholder look was called out as important to fix, echoing the user's original v5.0 kickoff framing. This plan produces the art; 26-05 wires it into build.js's cosmetic panels.
Output: assets/door.png, assets/enemy-1/2/3.png, license proofs, CREDITS.md rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-RESEARCH.md
@CREDITS.md
@assets/LICENSES/spike.txt
@assets/LICENSES/parallax.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source and vendor the door and enemy source art</name>
  <files>assets/_opengameart-src/6-color-dungeon/16x16-dungeon-tiles.png, assets/_kenney-src/new-platformer-pack/saw_rest.png, assets/_kenney-src/new-platformer-pack/barnacle_attack_a.png, assets/_kenney-src/new-platformer-pack/fly_rest.png, assets/LICENSES/door.txt, assets/LICENSES/enemy.txt</name>
  <read_first>assets/LICENSES/spike.txt and assets/LICENSES/parallax.txt (the exact proof-file template to follow byte-for-byte in structure)</read_first>
  <action>
Door source (already verified this session — reuse these exact values, no re-derivation needed): download `https://opengameart.org/sites/default/files/16x16%20dungeon%20tiles.png` (the same "6 Color Dungeon 16x16" pack by HorusKDI that already supplies assets/spike.png and assets/goal.png — CC0, proof already established in assets/LICENSES/spike.txt) to `assets/_opengameart-src/6-color-dungeon/16x16-dungeon-tiles.png` (new vendor dir, parallel convention to assets/_kenney-src/). The sheet is 256x208px (16 columns x 13 rows of 16px tiles). The gate/door art is the CLOSED-LATTICE-PORTCULLIS-OVER-DARK-ARCHWAY assembly at pixel crop box (left=0, top=64, right=48, bottom=160) — i.e. columns 0-2, rows 4-9, a 48x96px region (already visually verified this planning session: top half is a closed iron lattice gate, bottom half is a dark stone archway/threshold — together it reads as a complete standing locked-gate structure). Write `assets/LICENSES/door.txt` following spike.txt's exact template (source URL, source file, "Tile: columns 0-2, rows 4-9 of the 16x16 tile grid (closed lattice gate + archway base)", License: CC0, the quoted "License(s): CC0" declaration, verification date-stamp = today).

Enemy source (already verified this session by downloading and visually inspecting both the "Pixel Platformer" pack's Characters folder — confirmed NOT usable, portrait/face icons only, wrong style for a side-view platformer — and Kenney's "New Platformer Pack" v1.1, which has a dedicated `Sprites/Enemies/Default/` folder with real side-view platformer enemy sprites): download `https://kenney.nl/media/pages/assets/new-platformer-pack/1896103897-1764756702/kenney_new-platformer-pack-1.1.zip`, extract, and vendor exactly these 3 files (64x64px each) to `assets/_kenney-src/new-platformer-pack/`: `Sprites/Enemies/Default/saw_rest.png` -> `saw_rest.png`, `Sprites/Enemies/Default/barnacle_attack_a.png` -> `barnacle_attack_a.png`, `Sprites/Enemies/Default/fly_rest.png` -> `fly_rest.png`. Do NOT vendor the whole zip or any other file from the pack (info-disclosure/scope hygiene — only the 3 needed sprites, matching the project's existing "vendor only what's used" convention). Write `assets/LICENSES/enemy.txt` following spike.txt's template, naming all 3 source files, source URL `https://kenney.nl/assets/new-platformer-pack`, License: CC0 (quote the pack's bundled License.txt: "License (Creative Commons Zero, CC0)"), verification date-stamp = today. Note in the proof file: variety intentionally spans mechanical (saw), one-eyed horned monster (barnacle), and insect (fly) archetypes for thematic richness per CONTEXT.md's "2-3 distinct variants" requirement.
  </action>
  <acceptance_criteria>
    - assets/_opengameart-src/6-color-dungeon/16x16-dungeon-tiles.png exists, 256x208px.
    - assets/_kenney-src/new-platformer-pack/{saw_rest,barnacle_attack_a,fly_rest}.png exist, 64x64px each.
    - assets/LICENSES/door.txt and enemy.txt exist, following the spike.txt template shape (source URL, source file, tile/crop location, License: CC0, quoted declaration, dated verification).
  </acceptance_criteria>
  <verify>
    <automated>test -f assets/_opengameart-src/6-color-dungeon/16x16-dungeon-tiles.png && test -f assets/_kenney-src/new-platformer-pack/saw_rest.png && test -f assets/_kenney-src/new-platformer-pack/barnacle_attack_a.png && test -f assets/_kenney-src/new-platformer-pack/fly_rest.png && test -f assets/LICENSES/door.txt && test -f assets/LICENSES/enemy.txt && grep -qi "CC0" assets/LICENSES/door.txt && grep -qi "CC0" assets/LICENSES/enemy.txt</automated>
  </verify>
  <done>All 4 source PNGs vendored under new/extended vendor dirs; both license proof files exist and declare CC0.</done>
</task>

<task type="auto">
  <name>Task 2: Add build_door() and build_enemies() to build-art-assets.py, run, produce the 4 final sprite PNGs</name>
  <files>scripts/build-art-assets.py, assets/door.png, assets/enemy-1.png, assets/enemy-2.png, assets/enemy-3.png</files>
  <read_first>scripts/build-art-assets.py's build_player() (lines 154-193 — the closest structural template: load source -> crop to content bbox -> scale -> remap -> save) and CONFIG.DOOR.W/H (32x64) and CONFIG.ENEMY.W/H (32x32) from src/config.js</read_first>
  <action>
Add `build_door()`: open `assets/_opengameart-src/6-color-dungeon/16x16-dungeon-tiles.png`, crop to box (0, 64, 48, 160) (48x96px), resize with `Image.NEAREST` to exactly 32x64 (a clean 2/3 scale on both axes — matches CONFIG.DOOR.W/H), remap through `_remap_luminance` using `ENVIRONMENT_PALETTE` (the same base palette `build_ground()` uses — the door is a universal barrier element per 26-RESEARCH.md's Anti-Pattern note: "danger/reward/barrier... must not be re-themed", so it stays on the base neutral palette, not a per-theme variant), assert final size is (32, 64), save to `assets/door.png`.

Add `build_enemies()`: for each of the 3 source files (`saw_rest.png`, `barnacle_attack_a.png`, `fly_rest.png` under `assets/_kenney-src/new-platformer-pack/`), open the file, crop to its own content bbox via `im.getbbox()` (each source is 64x64 with transparent padding around the actual art), compute an INDEPENDENT scale factor per sprite so its bbox's larger dimension becomes 28px (leaving a small margin inside the 32x32 target — independent scaling is correct here since these are 3 unrelated static single-frame sprites, not a multi-frame walk-cycle needing one shared scale like build_player()'s pose set), resize with `Image.NEAREST`, paste centered onto a new 32x32 transparent canvas, remap through `_remap_luminance` using `ENVIRONMENT_PALETTE` (same universal-barrier reasoning as the door — enemies are NOT per-theme tinted per CONTEXT.md's "danger... must not be re-themed" invariant), assert final size (32, 32) for each, save to `assets/enemy-1.png` (saw), `assets/enemy-2.png` (barnacle), `assets/enemy-3.png` (fly).

Call both new functions from `if __name__ == "__main__":`, after the existing build calls and the 26-03 theme loop.
  </action>
  <acceptance_criteria>
    - assets/door.png exists, exactly 32x64px.
    - assets/enemy-1.png, enemy-2.png, enemy-3.png exist, exactly 32x32px each.
    - All 4 pass PIL's size assert inside the script (script does not silently produce a wrong-sized file).
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/build-art-assets.py && python3 -c "
from PIL import Image
assert Image.open('assets/door.png').size == (32,64), 'door wrong size'
for f in ['enemy-1.png','enemy-2.png','enemy-3.png']:
    assert Image.open('assets/'+f).size == (32,32), f+' wrong size'
print('sizes ok')
"</automated>
  </verify>
  <done>assets/door.png (32x64) and assets/enemy-1/2/3.png (32x32 each) exist with correct dimensions, built from the vendored CC0 sources via the existing remap pipeline.</done>
</task>

<task type="auto">
  <name>Task 3: Add CREDITS.md rows for the door and enemy assets</name>
  <files>CREDITS.md</files>
  <read_first>CREDITS.md's existing Assets table row format</read_first>
  <action>
Append 2 new rows to CREDITS.md's Assets table, matching the existing column format exactly (Asset | File | Author | Source | License | Used for): one row for "6 Color Dungeon 16x16 (gate/archway)" | `assets/door.png` | HorusKDI | https://opengameart.org/content/6-color-dungeon-16x16 | CC0 | "Locked door barrier sprite"; one row for "New Platformer Pack (saw/barnacle/fly enemies)" | `assets/enemy-1.png, enemy-2.png, enemy-3.png` | Kenney (Kenney Vleugels) | https://kenney.nl/assets/new-platformer-pack | CC0 | "Defeat-enemy encounter sprites — 3 variants". Add a short Notes bullet (matching the existing Notes section's style) explaining the enemy variants were hand-picked from the pack's larger Enemies roster for a mechanical/monster/insect thematic spread, following the same "no vendor logos, single cropped tile" discipline as every other asset.
  </action>
  <acceptance_criteria>
    - CREDITS.md's Assets table has 2 new rows for door and enemy assets, matching the existing column format.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "assets/door.png" CREDITS.md && grep -c "enemy-1.png" CREDITS.md</automated>
  </verify>
  <done>CREDITS.md documents both new assets in the established table format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External asset download | Downloading a font/image pack from opengameart.org and kenney.nl (both already-trusted sources for 9 prior shipped assets) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-04 | Tampering | Downloaded 6-color-dungeon sheet + new-platformer-pack zip | medium | mitigate | Both sources already have an established, proven CC0 provenance in this project (same pack already supplies spike.png/goal.png/ground.png); license re-verified this session via direct page fetch; only the specific needed files are vendored (not the whole zip), each with its own dated LICENSES/*.txt proof — matches the existing 9-asset precedent exactly |
| T-26-05 | Information Disclosure | Vendoring an entire downloaded zip instead of the needed files | low | mitigate | Task 1 explicitly vendors ONLY the 3 named enemy PNGs (not the whole new-platformer-pack.zip, which also contains unrelated Sounds/Vector/Construct3/Tiled content) |

</threat_model>

<verification>
`python3 scripts/build-art-assets.py` produces assets/door.png (32x64) and assets/enemy-1/2/3.png (32x32 each) with correct sizes. `bash scripts/check-safety.sh` still passes (no src/ touched this plan). CREDITS.md and both LICENSES/*.txt files exist and declare CC0.
</verification>

<success_criteria>
Real CC0 door and 3-variant enemy sprite art exists on disk, license-proofed and credited to the same standard as every prior asset in this project, ready for 26-05 to wire into build.js's cosmetic panels.
</success_criteria>

## Artifacts This Phase Produces

- `assets/_opengameart-src/6-color-dungeon/16x16-dungeon-tiles.png` — new vendor dir + source sheet.
- `assets/_kenney-src/new-platformer-pack/saw_rest.png`, `barnacle_attack_a.png`, `fly_rest.png` — new vendor dir + 3 source sprites.
- `scripts/build-art-assets.py`: `build_door()`, `build_enemies()` functions.
- `assets/door.png` (32x64), `assets/enemy-1.png`, `assets/enemy-2.png`, `assets/enemy-3.png` (32x32 each).
- `assets/LICENSES/door.txt`, `assets/LICENSES/enemy.txt` — new proof files.
- `CREDITS.md`: 2 new asset rows.

<output>
Create `.planning/phases/26-grunge-palette-nox-run-rebrand/26-04-SUMMARY.md` when done
</output>
