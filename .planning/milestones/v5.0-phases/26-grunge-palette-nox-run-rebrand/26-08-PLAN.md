---
phase: 26
plan: 08
type: execute
wave: 6
depends_on: ["26-05", "26-06"]
files_modified:
  - scripts/screenshot-phase26.mjs
autonomous: false
requirements: [VIS-03, VIS-04]

must_haves:
  truths:
    - "All 8 levels are proven visually distinct via real in-browser screenshots, not code review alone (26-RESEARCH.md Pitfall B: a static code check cannot catch a silently-ignored theme parameter)."
    - "Universal hazard/reward colors (spike red, door/gate barrier grey, XP/reward green) remain legible against every one of the 8 per-level theme backgrounds."
    - "Door and enemy sprite art is judged in the running game against the project's dark-grunge/no-bubbly-or-childish standard, not just 'is a sprite present'."
  artifacts:
    - "scripts/screenshot-phase26.mjs — new Playwright screenshot capture script (PORT 8768), following screenshot-phase20.mjs's established skeleton"
    - "10 screenshots under .planning/phases/26-grunge-palette-nox-run-rebrand/ (8 per-level theme shots + 1 door close-up + 1 enemy close-up)"
  key_links:
    - "scripts/screenshot-phase26.mjs -> boots each LEVEL_ORDER entry via the browser-boot.mjs save-seed technique, captures the actual rendered game, not a mockup"
---

<objective>
Prove VIS-03 (8 distinct per-level themes) and VIS-04 (real door/enemy sprites) with real in-browser screenshots, then get explicit human sign-off — per the project's "no phase closes on greps/automation alone" standard and CONTEXT.md's mandatory visual sign-off requirement for both.

Purpose: 26-05/26-06 wired the theming and sprite code; this plan is the proof step. A code review cannot catch a silently-ignored theme parameter (26-RESEARCH.md Pitfall B) or judge whether sourced enemy art actually reads as dark-grunge rather than bubbly/cute — only a real screenshot + human eyes can.
Output: scripts/screenshot-phase26.mjs; 10 real screenshots; a closed human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-VALIDATION.md
@scripts/screenshot-phase20.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/screenshot-phase26.mjs and capture all 10 screenshots</name>
  <files>scripts/screenshot-phase26.mjs</files>
  <read_first>scripts/screenshot-phase20.mjs (full file — the exact skeleton to copy: playwright resolution, static server, MIME map, save-seed technique); scripts/browser-boot.mjs lines 73-86 (SAVE_KEY/SAVE_BLOB unlock-all-levels pattern, derived from the live LEVEL_ORDER import so it never drifts) and lines 146-165 (row/col select-grid navigation math)</read_first>
  <action>
Create scripts/screenshot-phase26.mjs by copying screenshot-phase20.mjs's skeleton verbatim (playwright resolution block, static-file server, MIME map — per this project's established convention of copying Playwright script boilerplate by hand rather than extracting a shared module), using PORT 8768 (distinct from 8765/8766/8767 already in use). Import LEVEL_ORDER from ../src/levels/index.js and derive the save-seed blob the same way browser-boot.mjs does (all levels except the last marked cleared, so every level unlocks) so all 8 levels are reachable regardless of level count. For each of the 8 levels in LEVEL_ORDER: navigate to it via the select screen's row/col keyboard math (copy browser-boot.mjs's row/col ArrowDown/ArrowRight loop verbatim — this project's convention is to duplicate this Playwright navigation code by hand in each script, not share it), wait for the scene to build, and screenshot to phase26-level-0N-theme.png (N = 1..8) under .planning/phases/26-grunge-palette-nox-run-rebrand/. On level-01 specifically (which has both a door at x:1400 and an enemy at x:1000), after the level loads: hold ArrowRight for roughly 4.3 seconds (RUN_SPEED 240px/s times 4.3s is about 1000px, bringing the enemy into frame) and screenshot phase26-enemy-closeup.png, then continue holding ArrowRight for about 1.7 more seconds (another roughly 400px, bringing the door into frame) and screenshot phase26-door-closeup.png, then release the key. Return to select (Escape) between levels as browser-boot.mjs does.
  </action>
  <acceptance_criteria>
    - scripts/screenshot-phase26.mjs exists, runs standalone via `node scripts/screenshot-phase26.mjs`, and does not collide with any other script's port.
    - Running it produces exactly 10 PNG files under the phase directory: phase26-level-01-theme.png through phase26-level-08-theme.png, phase26-door-closeup.png, phase26-enemy-closeup.png.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/screenshot-phase26.mjs && ls .planning/phases/26-grunge-palette-nox-run-rebrand/phase26-*.png | wc -l</automated>
  </verify>
  <done>scripts/screenshot-phase26.mjs exists and produces all 10 real in-browser screenshots.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off — 8 per-level themes + door/enemy sprite quality</name>
  <files></files>
  <action>
    Review the 10 screenshots Task 1 captured, then record a blocking human-verify sign-off.
    This is a verification-only checkpoint; write no code. If a defect appears, route it back
    to 26-03 (theme rebake), 26-04 (re-source door/enemy art), or 26-05 (wiring) as appropriate,
    then re-run Task 1 and re-verify.
  </action>
  <what-built>10 real in-browser screenshots: one per level (all 8) showing that level's per-level theme (background/ground tint), plus a close-up of the door sprite and a close-up of an enemy sprite in level-01, all captured from the actual running game (not mockups).</what-built>
  <how-to-verify>
    1. Open the 8 `phase26-level-0N-theme.png` files under `.planning/phases/26-grunge-palette-nox-run-rebrand/`. Confirm each level's background/ground reads as visibly distinct from its neighbors, and that the overall progression (level 1 through 8) reads as calm greens transitioning toward harsher rust/steel tones.
    2. In each screenshot, confirm the universal hazard/reward colors — spike red, door/gate grey, XP/HUD green — are still clearly legible against that level's tinted background (this is a load-bearing gameplay-safety check, not just an aesthetic one).
    3. Open `phase26-door-closeup.png` and `phase26-enemy-closeup.png`. Judge them against the project's own standing rule: dark grunge, edgy, explicitly no pink, no bubbly or childish elements. The enemy sprite was sourced from a general-purpose CC0 platformer pack whose native art style leans toward round, smiling "cute mobile game" character designs — the remap pipeline retints it onto the dark palette, but does NOT change its underlying shape/expression. If the door or enemy reads as too cute/bubbly/childish even after the dark-grunge retint, reject and describe what's wrong so a different source sprite can be picked in a follow-up plan.
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues (which level's theme, which hazard color, or which sprite needs a redo)</resume-signal>
  <verify>
    <human-check>Human confirms from the real screenshots: all 8 levels read as visibly distinct with a calm-to-harsh progression; hazard/reward colors stay legible on every theme; the door and enemy sprites read as dark-grunge/edgy, not bubbly or childish.</human-check>
  </verify>
  <done>Human sign-off recorded that all 8 per-level themes and the door/enemy sprite art meet the project's visual bar; or a defect list routed back to 26-03/26-04/26-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Read-only verification tooling (Playwright screenshot capture of the already-running local dev server); no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-10 | Tampering | scripts/screenshot-phase26.mjs local static server | low | accept | Mirrors browser-boot.mjs/screenshot-phase20.mjs's already-reviewed loopback-only (127.0.0.1), path-traversal-guarded static server pattern exactly; no new server code invented |

</threat_model>

<verification>
`node scripts/screenshot-phase26.mjs` produces all 10 PNGs with no runtime errors. Human checkpoint explicitly closed with "approved" (or issues captured as a follow-up if rejected).
</verification>

<success_criteria>
VIS-03 and VIS-04's visual claims are backed by real screenshots and explicit human sign-off, not automation alone.
</success_criteria>

## Artifacts This Phase Produces

- `scripts/screenshot-phase26.mjs` — new script.
- `.planning/phases/26-grunge-palette-nox-run-rebrand/phase26-level-01-theme.png` through `phase26-level-08-theme.png` (8 files), `phase26-door-closeup.png`, `phase26-enemy-closeup.png`.

<output>
Create `.planning/phases/26-grunge-palette-nox-run-rebrand/26-08-SUMMARY.md` when done
</output>
