---
phase: 26
plan: 05
type: execute
wave: 5
depends_on: ["26-03", "26-04"]
files_modified:
  - src/levels/build.js
  - src/config.js
  - src/parallax.js
  - src/scenes/game.js
  - src/main.js
autonomous: true
requirements: [VIS-03, VIS-04]

must_haves:
  truths:
    - "buildLevel() reads levelData.theme and selects the matching theme-aware ground sprite (closes 26-RESEARCH.md Pitfall B — the theme field must not silently no-op)."
    - "The door and every enemy panel render as real sprite() entities instead of rect()+color()+text() glyphs; the invisible collision blocker and its size are completely untouched."
    - "makeParallaxLayers threads a theme parameter through to sprite-name selection, with a safe fallback to the base (untinted) layer set when theme is unset."
  artifacts:
    - "src/config.js: CONFIG.DOOR.SPRITES, CONFIG.ENEMY.SPRITES fields"
    - "src/levels/build.js: theme-aware ground sprite selection; door/enemy panels as sprite() entities"
    - "src/parallax.js: makeParallaxLayers(bounds, theme)"
  key_links:
    - "src/scenes/game.js level.theme -> src/parallax.js makeParallaxLayers(bounds, theme) AND src/levels/build.js buildLevel(levelData) (levelData.theme already in scope)"
    - "src/config.js CONFIG.ENEMY.SPRITES[e.variant ?? 0] -> src/levels/build.js enemy-panel sprite() call (variant field added to level descriptors in 26-06)"
---

<objective>
Wire the per-level theming (VIS-03) and door/enemy sprite art (VIS-04) into the running game: `build.js` selects the theme-aware ground sprite and swaps the door/enemy cosmetic panels from `rect()+color()+text()` to `sprite()`; `parallax.js`/`game.js` thread the level's `theme` field through; `config.js` gains sprite-reference fields; `main.js` registers every new sprite asset.

Purpose: 26-03/26-04 produced the art; without this plan the baked PNGs are unreachable dead files (26-RESEARCH.md Pitfall B's exact failure class — "looks done but isn't"). This is where VIS-03 and VIS-04 actually become visible in the game.
Output: theme-aware backgrounds/ground per level; real door/enemy sprites; zero change to collision physics.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-RESEARCH.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-PATTERNS.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-03-SUMMARY.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: build.js — theme-aware ground sprite + door/enemy panel sprite swap (glyph removed)</name>
  <files>src/levels/build.js</files>
  <read_first>src/levels/build.js full file (floor/platform loops lines 86-126; door block lines 156-195; enemy block lines 235-270)</read_first>
  <action>
<!-- planner-discipline-allow: text("X" -->
<!-- planner-discipline-allow: text("!" -->
The two glyph literals named below are the DELETION targets, not example/illustration code to add.
At the top of `buildLevel(levelData)`, after the existing `const g = levelData.geometry;` line, add `const groundSprite = levelData.theme ? \`ground-${levelData.theme}\` : "ground";` (theme-aware with a safe fallback to the base untinted "ground" sprite when theme is unset — preserves node-importability and any future level without a theme). Replace BOTH `sprite("ground", { frame: pickTopFrame(...) })` calls (the floor-run loop and the platform loop) with `sprite(groundSprite, { frame: pickTopFrame(...) })`. Do not touch the invisible collider `rect()+area()+body()` blocks in either loop — only the visual tile sprite name changes.

Door panel: replace the `rect(CONFIG.DOOR.W, CONFIG.DOOR.H), pos(d.x, d.y), color(...CONFIG.DOOR.LOCKED_GREY), outline(2, rgb(...CONFIG.DOOR.LOCKED_BORDER)), "door-panel"` component list with `sprite("door"), pos(d.x, d.y), "door-panel"`. DELETE the door-glyph `add([text("X", ...), ...])` block entirely, and delete the `blocker.glyphObj = glyph;` line (leave `blocker.panelObj = panel;` — the panel stash stays, only the glyph stash goes). The invisible tall blocker `rect(CONFIG.DOOR.W, blockerH), ..., opacity(HIDDEN), area(), body({isStatic:true}), "door"` stays COMPLETELY unchanged (same size, same tag, same position math).

Enemy panel: replace `rect(CONFIG.ENEMY.W, CONFIG.ENEMY.H), pos(e.x, e.y), color(...CONFIG.ENEMY.COLOR), "enemy-panel"` with `sprite(CONFIG.ENEMY.SPRITES[e.variant ?? 0]), pos(e.x, e.y), "enemy-panel"` (falls back to variant 0 / `enemy-1` when a level's enemy object has no `variant` field yet — safe default). DELETE the enemy-glyph `add([text("!", ...), ...])` block entirely, and delete the `enemyObj.glyphObj = glyph;` line (leave `enemyObj.panelObj = panel;`). The invisible tall enemy blocker stays COMPLETELY unchanged.

Do NOT touch the math-gate block (panel, glyph "?", blocker) at all — math-gates explicitly stay flat-color+glyph this phase (VIS-04 only names doors and enemies; 26-RESEARCH.md's Anti-Pattern note requires this divergence be a documented decision, which this comment records).
  </action>
  <acceptance_criteria>
    - Floor and platform visual tiles use a theme-templated sprite name when levelData.theme is set, falling back to "ground" when unset.
    - Door and enemy panels use sprite() instead of rect()+color(); their glyph text() blocks and glyphObj stashes are deleted.
    - Math-gate panel/glyph/blocker are byte-unchanged.
    - Every blocker (door, math-gate, enemy) rect()+area()+body()+opacity(HIDDEN) block is byte-unchanged in size/position math.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/levels/build.js && grep -c 'sprite("door")' src/levels/build.js && grep -c 'CONFIG.ENEMY.SPRITES' src/levels/build.js && grep -c 'text("X"' src/levels/build.js | grep -qx 0 && grep -c 'text("!"' src/levels/build.js | grep -qx 0 && grep -c 'text("?"' src/levels/build.js</automated>
  </verify>
  <done>build.js parses; door/enemy panels are sprite()-based with glyphs removed; math-gate's "?" glyph is untouched; ground sprite selection is theme-aware with a safe fallback.</done>
</task>

<task type="auto">
  <name>Task 2: config.js sprite fields + parallax.js theme threading + game.js wiring</name>
  <files>src/config.js, src/parallax.js, src/scenes/game.js</files>
  <read_first>src/config.js DOOR/ENEMY blocks (post-26-01 state); src/parallax.js full file; src/scenes/game.js lines 118-121 (makeParallaxLayers(bounds) call) and buildLevel(level) call at line 116</read_first>
  <action>
config.js: in the DOOR block, add `SPRITES: ["door"],` and DELETE the now-dead `LOCKED_GREY`, `LOCKED_BORDER`, and `GLYPH_SIZE` fields (no longer read anywhere after Task 1's glyph/panel removal — keep `W`/`H`, still used for blocker sizing). In the ENEMY block, add `SPRITES: ["enemy-1", "enemy-2", "enemy-3"],` and DELETE the now-dead `COLOR` and `GLYPH_SIZE` fields (keep `W`/`H`). Do NOT touch the MATH_GATE block (its LOCKED_GREY/LOCKED_BORDER/GLYPH_SIZE stay live — build.js's math-gate rendering is untouched).

parallax.js: change `makeParallaxLayers(bounds)` to `makeParallaxLayers(bounds, theme)`. Inside, add a local helper `const layerName = (base) => (theme ? \`${base}-${theme}\` : base);` (same per-key-defaulting spirit as the existing `safeBounds` idiom just above it — theme absent means the base untinted sprite set, never a crash). Replace the 3 hardcoded literal names `"bg-far"`, `"bg-mid"`, `"bg-near"` passed into `makeParallaxLayer(...)` with `layerName("bg-far")`, `layerName("bg-mid")`, `layerName("bg-near")` (the `name:` field in the returned layer-descriptor objects should also use the templated name, so `updateParallaxLayers` — which reads `layer.instances`, not `layer.name`, for its per-frame math — stays correct with zero further changes).

game.js: change the call `const parallaxLayers = makeParallaxLayers(bounds);` (line ~121) to `const parallaxLayers = makeParallaxLayers(bounds, level.theme);`. `buildLevel(level)` (line ~116) needs NO change — `level` already carries `.theme` and Task 1's build.js already reads `levelData.theme` directly.
  </action>
  <acceptance_criteria>
    - CONFIG.DOOR has SPRITES, no LOCKED_GREY/LOCKED_BORDER/GLYPH_SIZE. CONFIG.ENEMY has SPRITES, no COLOR/GLYPH_SIZE. CONFIG.MATH_GATE unchanged.
    - makeParallaxLayers accepts a second `theme` argument and templates all 3 layer sprite names through it, falling back to the base names when theme is falsy.
    - game.js passes `level.theme` into makeParallaxLayers.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/config.js && node --check src/parallax.js && node --check src/scenes/game.js && grep -q "makeParallaxLayers(bounds, level.theme)" src/scenes/game.js && grep -q "SPRITES" src/config.js</automated>
  </verify>
  <done>config.js/parallax.js/game.js all parse; dead color/glyph fields removed from DOOR/ENEMY; theme threads from game.js through parallax.js.</done>
</task>

<task type="auto">
  <name>Task 3: main.js — register every new sprite asset</name>
  <files>src/main.js</files>
  <read_first>src/main.js lines 62-90 (existing loadSprite calls)</read_first>
  <action>
Add, after the existing "Shared title / level-select dark-grunge backdrop" loadSprite call: a loop over the 8 baked themes issuing `loadSprite(\`bg-far-theme-${n}\`, \`../assets/parallax/far-theme-${n}.png\`)`, `loadSprite(\`bg-mid-theme-${n}\`, \`../assets/parallax/mid-theme-${n}.png\`)`, `loadSprite(\`bg-near-theme-${n}\`, \`../assets/parallax/near-theme-${n}.png\`)`, and `loadSprite(\`ground-theme-${n}\`, \`../assets/tiles/ground-theme-${n}.png\`, { sliceX: CONFIG.GROUND_FRAMES })` for n = 1 through 8 (matches the sliceX pattern the base `"ground"` loadSprite already uses). Then add `loadSprite("door", "../assets/door.png")` and `loadSprite("enemy-1", "../assets/enemy-1.png")`, `loadSprite("enemy-2", "../assets/enemy-2.png")`, `loadSprite("enemy-3", "../assets/enemy-3.png")`. Use the exact sprite names `bg-far-theme-N`/`bg-mid-theme-N`/`bg-near-theme-N`/`ground-theme-N` (matching parallax.js's `layerName()` template and build.js's `groundSprite` template from Tasks 1-2 exactly, including the literal string `"theme-N"` shape that will be set as each level's `.theme` field in 26-06 — e.g. a level with `theme: "theme-3"` resolves to sprite name `bg-far-theme-3`).
  </action>
  <acceptance_criteria>
    - main.js has 32 new theme-variant loadSprite calls (8 themes x 4 assets) plus 4 new door/enemy loadSprite calls.
    - Every sprite name main.js registers matches exactly what parallax.js's layerName() and build.js's groundSprite template will request at runtime.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/main.js && grep -c "bg-far-theme-" src/main.js && grep -c 'loadSprite("door"' src/main.js && grep -c 'loadSprite("enemy-1"' src/main.js</automated>
  </verify>
  <done>main.js registers all 36 new sprite assets (32 theme variants + door + 3 enemies) under the exact names the rest of the wiring expects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure code wiring — selects among already-vendored, already-license-proofed static assets; no new input surface, no collision/physics change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-06 | Tampering | src/levels/build.js door/enemy blocker sizing | high | mitigate | Task 1 explicitly requires the invisible collision blocker rect()/area()/body() blocks be BYTE-UNCHANGED — only the cosmetic panel's component list changes; acceptance criteria + <verify> both check this; 26-08's regression checkpoint re-runs audit-phase21-mechanics.mjs to prove triggered:true is unchanged on every door/enemy encounter |

</threat_model>

<verification>
`bash scripts/check-safety.sh` PASS. `node scripts/validate-levels.mjs` PASS zero HARD-FAILs (proves door/enemy blocker geometry is untouched — the validator's over-hole/reachability checks read blocker positions, not panel art). `node scripts/browser-boot.mjs` PASS (proves every newly-referenced sprite name actually resolves — a missing/misnamed asset would 404 and fail this real-browser boot check).
</verification>

<success_criteria>
Per-level theming and door/enemy sprite art are fully wired end-to-end: a real browser session loading any level shows that level's themed background/ground and real door/enemy sprites, with zero change to jump physics, collision, or mechanic-trigger behavior.
</success_criteria>

## Artifacts This Phase Produces

- `src/levels/build.js`: local `groundSprite` theme-templated sprite-name const inside `buildLevel()`; door/enemy panels now built with `sprite()` instead of `rect()+color()`; door/enemy glyph text() blocks removed.
- `src/config.js`: `CONFIG.DOOR.SPRITES`, `CONFIG.ENEMY.SPRITES` (new fields); `CONFIG.DOOR.LOCKED_GREY/LOCKED_BORDER/GLYPH_SIZE` and `CONFIG.ENEMY.COLOR/GLYPH_SIZE` removed (dead post-sprite-swap).
- `src/parallax.js`: `makeParallaxLayers(bounds, theme)` new signature; local `layerName()` helper.
- `src/scenes/game.js`: `makeParallaxLayers(bounds, level.theme)` call site update.
- `src/main.js`: 36 new `loadSprite()` calls (32 theme variants + door + 3 enemies).

<output>
Create `.planning/phases/26-grunge-palette-nox-run-rebrand/26-05-SUMMARY.md` when done
</output>
