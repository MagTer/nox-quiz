---
phase: 26
plan: 06
type: execute
wave: 5
depends_on: ["26-03", "26-04"]
files_modified:
  - src/levels/level-01.js
  - src/levels/level-02.js
  - src/levels/level-03.js
  - src/levels/level-04.js
  - src/levels/level-05.js
  - src/levels/level-06.js
  - src/levels/level-07.js
  - src/levels/level-08.js
autonomous: true
requirements: [VIS-03, VIS-04]

must_haves:
  truths:
    - "Each of the 8 levels carries its own distinct theme field value, following the documented calm-to-harsh (green -> blue-grey -> rust) progression across LEVEL_ORDER."
    - "Every existing enemy encounter across all 8 levels carries a variant field selecting among the 3 sourced enemy sprites, so the sprite variety is actually exercised in play, not just baked-but-unused."
  artifacts:
    - "src/levels/level-0N.js x8: theme field set to \"theme-N\" (was null)"
    - "src/levels/level-0N.js (03, 04, 06, 08 — the ones with enemies): enemy geometry objects gain a variant field (0/1/2)"
  key_links:
    - "level-0N.js theme field -> src/levels/build.js groundSprite template + src/parallax.js layerName() template (both from 26-05), which literally construct sprite names like `ground-${levelData.theme}`"
    - "level-0N.js enemy.variant field -> src/levels/build.js CONFIG.ENEMY.SPRITES[e.variant ?? 0] (from 26-05)"
---

<objective>
Set every level descriptor's `theme` field (currently `null` placeholder on all 8) to its assigned theme id, and add a `variant` field to every existing enemy encounter across the 8 levels so the 3 sourced enemy sprites are actually exercised in play. This is pure DATA — no engine references, no geometry changes (no gap/rise/platform edits — `docs/LEVEL-DESIGN.md`'s physics rules do not apply to this plan).

Purpose: 26-05 wired the CODE to read `levelData.theme` and `enemy.variant`; without this plan's data, every level would resolve to the SAME default (theme unset -> base "ground"/"bg-*" sprites, variant unset -> always enemy-1) and the whole per-level-distinct-theme + enemy-variety promise of VIS-03/VIS-04 would silently no-op (26-RESEARCH.md Pitfall B).
Output: 8 levels with distinct theme ids; enemy variant fields on all 6 existing enemy instances.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-RESEARCH.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assign theme field on all 8 level descriptors</name>
  <files>src/levels/level-01.js, src/levels/level-02.js, src/levels/level-03.js, src/levels/level-04.js, src/levels/level-05.js, src/levels/level-06.js, src/levels/level-07.js, src/levels/level-08.js</files>
  <read_first>26-03-SUMMARY.md (theme-1..theme-8 -> level mapping table, if recorded there) or fall back to scripts/build-art-assets.py's THEME_PALETTES comment block (records the same mapping); src/levels/level-01.js line 163 `theme: null,` for the exact field shape to edit</read_first>
  <action>
In each `src/levels/level-0N.js` file, change the `theme: null,` field (with its existing "Phase 18 placeholder — visual theming" comment, where present) to `theme: "theme-N",` using this exact 1:1 level-order mapping (matches 26-03's THEME_PALETTES level-to-theme assignment exactly — do not invent a different mapping): level-01 -> "theme-1", level-02 -> "theme-2", level-03 -> "theme-3", level-04 -> "theme-4", level-05 -> "theme-5", level-06 -> "theme-6", level-07 -> "theme-7", level-08 -> "theme-8". Update the trailing comment to note the theme is now wired (e.g. "VIS-03 theme assignment — calm moss green, level 1 of 8") rather than leaving the stale "Phase 18 placeholder" wording. Do not touch any other field on any level descriptor (no geometry, no allowedTables, no bounds).
  </action>
  <acceptance_criteria>
    - Every level-0N.js has `theme: "theme-N"` where N matches its own file number exactly.
    - No geometry, allowedTables, or bounds field changed in any of the 8 files.
  </acceptance_criteria>
  <verify>
    <automated>for n in 1 2 3 4 5 6 7 8; do grep -q "theme: \"theme-$n\"" "src/levels/level-0$n.js" || exit 1; done && echo "all 8 themes assigned"</automated>
  </verify>
  <done>All 8 level descriptors carry their own distinct theme-N field.</done>
</task>

<task type="auto">
  <name>Task 2: Add variant field to every existing enemy encounter</name>
  <files>src/levels/level-01.js, src/levels/level-03.js, src/levels/level-04.js, src/levels/level-06.js, src/levels/level-08.js</files>
  <read_first>each file's `geometry.enemies` array (confirmed this session: level-01 has 1 enemy at x:1000; level-03 has 2 at x:2400 and x:3800; level-04 has 1 at x:2400; level-06 has 1 at x:2150; level-08 has 1 at x:1600; levels 02/05/07 have zero enemies — no change needed there)</read_first>
  <action>
Add a `variant` field to each existing enemy object in `geometry.enemies`, cycling 0/1/2 across the 6 total encounters in LEVEL_ORDER traversal order (matches CONFIG.ENEMY.SPRITES index order: 0="enemy-1"/saw, 1="enemy-2"/barnacle, 2="enemy-3"/fly) for a distributed, non-repetitive mix: level-01's enemy (x:1000) -> `variant: 0`; level-03's first enemy (x:2400) -> `variant: 1`, level-03's second enemy (x:3800) -> `variant: 2`; level-04's enemy (x:2400) -> `variant: 0`; level-06's enemy (x:2150) -> `variant: 1`; level-08's enemy (x:1600) -> `variant: 2`. Add a short inline comment on each noting which sprite it selects (e.g. `variant: 0, // enemy-1 (saw)`). Do not add a `variant` field to any level that has zero enemies (02/05/07 stay as `enemies: []`).
  </action>
  <acceptance_criteria>
    - All 6 existing enemy objects (across level-01/03/04/06/08) carry a `variant` field with the exact values above.
    - No enemy object's x/y position changed.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "variant: 0" src/levels/level-01.js src/levels/level-04.js | awk -F: '{s+=$2} END{exit (s>=2)?0:1}' && grep -c "variant: 1" src/levels/level-03.js src/levels/level-06.js | awk -F: '{s+=$2} END{exit (s>=2)?0:1}' && grep -c "variant: 2" src/levels/level-03.js src/levels/level-08.js | awk -F: '{s+=$2} END{exit (s>=2)?0:1}'</automated>
  </verify>
  <done>All 6 enemy encounters across the 5 levels that have enemies carry a variant field cycling through all 3 sourced sprites; no position data changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure data-literal edits to already-trusted, already-validated level descriptor files; no engine references added (a727c13-safe by construction — only string/number literal fields change) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-07 | Tampering | src/levels/level-0N.js geometry | medium | mitigate | `node scripts/validate-levels.mjs` re-run in this plan's verification catches any accidental geometry drift; acceptance criteria explicitly forbid touching x/y/w/h fields |

</threat_model>

<verification>
`node scripts/validate-levels.mjs` still passes zero HARD-FAILs (proves no geometry regression from this data-only edit). `bash scripts/check-import-safety.sh` still passes (proves the new `theme`/`variant` string/number literals introduced no engine-global reference in these pure-data files).
</verification>

<success_criteria>
All 8 levels carry a distinct theme id; every enemy encounter selects one of the 3 sourced sprite variants, distributed across the roster rather than defaulting to a single repeated sprite.
</success_criteria>

## Artifacts This Phase Produces

- `src/levels/level-01.js` through `level-08.js`: `theme` field set to `"theme-1"` through `"theme-8"` (was `null` on all 8).
- `src/levels/level-01.js`, `level-03.js`, `level-04.js`, `level-06.js`, `level-08.js`: `variant` field (0/1/2) added to each of the 6 existing `geometry.enemies` objects.

<output>
Create `.planning/phases/26-grunge-palette-nox-run-rebrand/26-06-SUMMARY.md` when done
</output>
