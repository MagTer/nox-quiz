---
phase: 26
plan: 11
type: execute
wave: 9
depends_on: ["26-08", "26-09", "26-10"]
files_modified: []
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-04, BRAND-01, BRAND-02, BRAND-03]

must_haves:
  truths:
    - "The full automated gate suite (structural, safety, import-safety, progress, level-validation, real-browser boot, contrast, rebrand) passes green in one run, with zero regressions introduced anywhere across the phase's 10 prior plans."
    - "Every door/enemy/math-gate encounter across all 8 levels still resolves (triggered:true) after VIS-04's sprite swap — proving the cosmetic-panel-only change claim from 26-05 held in practice, not just by code inspection."
  artifacts: []
  key_links:
    - "This plan has no new artifacts — it is the phase's closing zero-regression proof, matching the project's established closing-sweep pattern (Phase 22-05, Phase 24-06)"
---

<objective>
Run the full verification suite in one pass and re-run the interactive mechanic audit, closing Phase 26 with a genuine zero-regression proof rather than relying on each prior plan's own scoped `<verify>` alone — matching this project's established "no phase closes on greps/automation alone" standard and the precedent set by Phase 22-05's and Phase 24-06's own closing sweeps.

Purpose: 10 prior plans each touched a handful of files with their own scoped verification; this plan is the one place that proves ALL of it holds together simultaneously — including the 2 NEW gates this phase created (check-contrast.mjs, check-rebrand.sh) running alongside the pre-existing suite.
Output: a clean full-suite run; a mechanic-audit regression proof; any found issue fixed before the phase closes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-VALIDATION.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTRAST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full gate suite (including the 2 new Phase 26 gates) in one pass</name>
  <files>none — verification only; fix forward if any gate is unexpectedly red</files>
  <read_first>26-VALIDATION.md's "Full suite command" line</read_first>
  <action>
Run, in order: `bash scripts/check-gate.sh`, `bash scripts/check-safety.sh`, `bash scripts/check-import-safety.sh`, `bash scripts/check-progress.sh`, `node scripts/validate-levels.mjs`, `node scripts/browser-boot.mjs`, `node scripts/check-contrast.mjs`, `bash scripts/check-rebrand.sh`. Every command must exit 0. If any command is unexpectedly red, this is a real regression from one of the phase's 10 prior plans — diagnose which plan's change caused it, fix it directly (do not weaken the failing gate to make it pass), and re-run the full sequence until every command is green. Do not proceed to Task 2 until this task is fully green.
  </action>
  <acceptance_criteria>
    - All 8 commands exit 0 in a single sequential run with no intervening fixes needed (or, if a fix was needed, the SAME sequence re-run afterward is fully green).
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && bash scripts/check-progress.sh && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs && node scripts/check-contrast.mjs && bash scripts/check-rebrand.sh && echo "FULL SUITE: PASS"</automated>
  </verify>
  <done>All 8 gates (6 pre-existing + 2 new this phase) pass green together in one sequential run.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the interactive mechanic audit — prove VIS-04's sprite swap is collision-neutral</name>
  <files>none — verification only</files>
  <read_first>scripts/audit-phase21-mechanics.mjs (the existing harness — `triggered: true` required for every encounter; `resolved: false` rows are known headless-timing flakiness per CLAUDE.md)</read_first>
  <action>
Run `node scripts/audit-phase21-mechanics.mjs`. Confirm every door and every enemy encounter across all 8 levels still shows `triggered: true` (the same result the pre-Phase-26 baseline showed) — this is the direct proof that 26-05's door/enemy panel sprite swap changed ONLY the cosmetic `-panel` entity and left the invisible collision blocker completely intact, as the plan's acceptance criteria required but as only a real interactive run can prove. A `resolved: false` row is tolerated ONLY if it is the known headless-timing flakiness class CLAUDE.md documents (not a new failure class introduced by this phase's sprite/theme changes) — cross-check any red row against whether it involves a door/enemy/math-gate (Phase 26 touched geometry) versus something Phase 26 never touched.
  </action>
  <acceptance_criteria>
    - Every door and enemy encounter across all 8 levels shows triggered:true.
    - No NEW resolved:false row appears that wasn't already a known flaky row before this phase.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/audit-phase21-mechanics.mjs</automated>
  </verify>
  <done>The interactive mechanic audit confirms zero collision/trigger regression from VIS-04's sprite swap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure verification pass — no code changes expected (only fix-forward if a real regression surfaces) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-14 | Repudiation | Phase closing without a combined-gate proof | medium | mitigate | This plan exists specifically to close that gap — every gate the phase touched or created runs together in one sequential pass, matching the project's "checks that don't play the game lie" standard |

</threat_model>

<verification>
Task 1's 8-command sequence is green. Task 2's mechanic audit shows zero new regressions.
</verification>

<success_criteria>
Phase 26 closes with a genuine, combined, zero-regression proof across every gate it touched or created, plus interactive confirmation that the door/enemy sprite swap never altered collision/trigger behavior.
</success_criteria>

## Artifacts This Phase Produces

None — this is the phase's closing verification-only plan; it produces no new files or code symbols, only a proof that all 10 prior plans' artifacts work together with zero regression.

<output>
Create `.planning/phases/26-grunge-palette-nox-run-rebrand/26-11-SUMMARY.md` when done
</output>
