---
phase: 26
plan: 10
type: execute
wave: 8
depends_on: ["26-07", "26-09"]
files_modified:
  - README.md
  - docs/DEPLOY.md
  - docker/Dockerfile
  - CREDITS.md
  - scripts/generate-art-assets.py
  - scripts/build-art-assets.py
  - scripts/check-rebrand.sh
autonomous: true
requirements: [BRAND-02]

must_haves:
  truths:
    - "No 'Math Lab'/'mathlab' string remains in README.md, docs/DEPLOY.md, docker/Dockerfile, CREDITS.md, or either art-pipeline script's docstring."
    - "A permanent, committed grep gate (scripts/check-rebrand.sh) proves the full sweep (both this plan's docs/scripts half and 26-09's core src/ half) stays clean on every future commit, with an explicit allowlist for the 2 historical school-game comments."
  artifacts:
    - "scripts/check-rebrand.sh — new permanent grep gate, mirrors check-safety.sh's comment-stripped whole-tree pattern"
  key_links:
    - "scripts/check-rebrand.sh scans src/, scripts/, docs/, docker/, README.md, CREDITS.md — the FULL sweep scope 26-CONTEXT.md specifies, run AFTER 26-09 so it validates the real, already-swept tree rather than racing it"
---

<objective>
Sweep the remaining "Math Lab"/"mathlab" occurrences out of docs, deploy config comments, and the two art-pipeline script docstrings, then create the permanent `scripts/check-rebrand.sh` regression gate, per BRAND-02. This is the second, parallel half of the full sweep — 26-09 covered the core src/ runtime and the save-key-critical scripts; this plan covers everything else CONTEXT.md's "full sweep" scope names (README, docs/, docker/, CREDITS.md) plus the new permanent gate.

Purpose: sequenced after 26-09 (not parallel with it) specifically so `check-rebrand.sh`'s own <verify> step validates a tree where BOTH sweep halves have already landed — running the gate before 26-09 completes would see 26-09's not-yet-swept files and fail on work that is correctly in flight elsewhere, not actually broken.
Output: zero "Math Lab" strings outside the allowlist, anywhere in the project; `scripts/check-rebrand.sh` as a new permanent gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-RESEARCH.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-PATTERNS.md
@scripts/check-safety.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md, docs/DEPLOY.md, docker/Dockerfile sweep</name>
  <files>README.md, docs/DEPLOY.md, docker/Dockerfile</files>
  <read_first>README.md full file (line 1 heading); docs/DEPLOY.md full file (lines 1, 3, 76-81 — prose + the `mathlab:phase7`/`--name mathlab` Docker command examples); docker/Dockerfile line 1 (comment)</read_first>
  <action>
<!-- planner-discipline-allow: math lab -->
<!-- planner-discipline-allow: mathlab -->
This task's own literal target strings are named below so the executor knows exactly what to remove — this is the rename source text, not example/illustration code.
README.md: change the `# Math Lab v3.0 — The Platformer` heading to `# Nox Run — The Platformer` (drop the stale "v3.0" version tag from the heading — the project is well past v3.0 now; keep the rest of the intro paragraph's meaning, just rebrand the name).
docs/DEPLOY.md: change "Deploying Math Lab to a web URL (Dokploy)" heading and "Math Lab ships as **static files**..." sentence to "Nox Run". Change the literal Docker command examples `docker build -f docker/Dockerfile -t mathlab:phase7 .`, `docker run -d --rm -p 8080:80 --name mathlab mathlab:phase7`, and `docker stop mathlab` to use a Nox Run-branded image/container name (e.g. `noxrun:local` / `--name noxrun`) — these are literal copy-paste command examples a developer would run, so stale "mathlab" naming here is a real (if low-severity) footgun per 26-RESEARCH.md's own flag.
docker/Dockerfile: change the comment "Single-stage static file server for Math Lab." to reference "Nox Run". Do NOT touch any functional directive (FROM/COPY/EXPOSE) — this is a comment-only edit; confirm the diff touches only the comment line.
  </action>
  <acceptance_criteria>
    - README.md heading says "Nox Run", not "Math Lab".
    - docs/DEPLOY.md has zero "Math Lab" occurrences; its docker command examples use a Nox Run-branded name.
    - docker/Dockerfile's only change is its comment line; FROM/COPY/EXPOSE are byte-identical to before.
  </acceptance_criteria>
  <verify>
    <automated>! grep -qi "math lab" README.md docs/DEPLOY.md docker/Dockerfile && ! grep -q "mathlab" docs/DEPLOY.md</automated>
  </verify>
  <done>README.md, docs/DEPLOY.md, docker/Dockerfile carry zero "Math Lab"/"mathlab" text; Dockerfile's functional directives are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: CREDITS.md intro + art-pipeline script docstrings</name>
  <files>CREDITS.md, scripts/generate-art-assets.py, scripts/build-art-assets.py</files>
  <read_first>CREDITS.md line 3 ("Math Lab ships a small subset..."); scripts/generate-art-assets.py's docstring (line 2, "placeholder pixel-art assets for Math Lab"); scripts/build-art-assets.py's docstring (line 2, "real-CC0-art pixel assets for Math Lab")</read_first>
  <action>
<!-- planner-discipline-allow: math lab -->
This task's own literal target string is named below so the executor knows exactly what to remove — this is the rename source text, not example/illustration code.
CREDITS.md: change "Math Lab ships a small subset of third-party pixel art for its platformer level." to "Nox Run ships a small subset of third-party pixel art for its platformer levels." (levels, plural — the project now has 8, not 1; a small accuracy fix alongside the rebrand).
scripts/generate-art-assets.py: change the docstring's "placeholder pixel-art assets for Math Lab" to "...for Nox Run".
scripts/build-art-assets.py: change the docstring's "real-CC0-art pixel assets for Math Lab" to "...for Nox Run".
  </action>
  <acceptance_criteria>
    - CREDITS.md's intro sentence says "Nox Run" and "levels" (plural).
    - Both script docstrings say "Nox Run" instead of "Math Lab".
  </acceptance_criteria>
  <verify>
    <automated>! grep -qi "math lab" CREDITS.md scripts/generate-art-assets.py scripts/build-art-assets.py</automated>
  </verify>
  <done>CREDITS.md and both art-pipeline script docstrings carry zero "Math Lab" text.</done>
</task>

<task type="auto">
  <name>Task 3: Create scripts/check-rebrand.sh (permanent BRAND-02 regression gate)</name>
  <files>scripts/check-rebrand.sh</files>
  <read_first>scripts/check-safety.sh (full file — the exact comment-stripped, whole-tree, clear-PASS-banner pattern to follow); src/progress.js lines 26-30 and 202-205 (the 2 lines that MUST be allowlisted)</read_first>
  <action>
<!-- planner-discipline-allow: math lab -->
<!-- planner-discipline-allow: mathlab -->
This task's own literal target strings are named below because they are the gate script's own matcher pattern, not example/illustration code.
Create scripts/check-rebrand.sh following check-safety.sh's exact shape: `set -euo pipefail`, `ROOT="$(git rev-parse --show-toplevel)"`, a `fail()` helper, and a `strip_comments()` helper for the .js/.py files it scans (reuse check-safety.sh's `sed -E 's://.*$::'` line-comment stripper for .js files; for .py files strip `#`-prefixed lines the same way; docs/README/Dockerfile are scanned raw, no comment-stripping, since their prose IS the content being checked).

Scan scope (the FULL sweep 26-CONTEXT.md specifies): every `*.js`/`*.mjs` file under `src/` and `scripts/`, every `*.py` file under `scripts/`, `*.sh` files under `scripts/` (raw, not comment-stripped — a shell script's own comments are its literal prose here), `README.md`, `CREDITS.md`, `docs/*.md`, `docker/Dockerfile`, `src/index.html`.

Negative grep for `math lab` / `mathlab` (case-insensitive) across that scope, with an explicit allowlist: `src/progress.js` lines matching `mathlab_save` are the ONLY tolerated hits (these describe the unrelated original school-game's keys, per 26-CONTEXT.md's explicit exception) — implement the allowlist by excluding `src/progress.js` from the negative scan for the specific `mathlab_save` pattern only (i.e. scan progress.js too, but pre-filter out lines containing `mathlab_save` before applying the negative grep, so any OTHER "mathlab" occurrence in that file still fails the gate).

On any un-allowlisted hit, `fail` with the file name and matched line (non-zero exit). On full success, print "rebrand checks: PASS".
  </action>
  <acceptance_criteria>
    - scripts/check-rebrand.sh exists, is executable via `bash scripts/check-rebrand.sh`, and follows check-safety.sh's structural pattern (fail() helper, clear PASS banner, non-zero exit on any hit).
    - Running it against the current tree (after Tasks 1-2 of this plan and all of 26-09) exits 0 and prints "rebrand checks: PASS".
    - Temporarily reintroducing a "Math Lab" string in a scanned file (e.g. README.md) and re-running the script produces a non-zero exit with a clear file/line message — prove the gate is real before finalizing (revert the temporary reintroduction after confirming the RED result).
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-rebrand.sh</automated>
  </verify>
  <done>scripts/check-rebrand.sh exists, was proven to catch a real violation (RED-then-reverted), and passes clean (PASS) against the fully-swept tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | New static-analysis gate script only; no runtime/input surface change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-13 | Repudiation | scripts/check-rebrand.sh silently passing on a real violation (false negative) | medium | mitigate | Task 3's acceptance criteria mandates a RED-then-reverted self-test (temporarily reintroducing a violation) before accepting the gate as trustworthy — mirrors Phase 23's own "prove RED before trusted as a gate" precedent for validate-levels.mjs |

</threat_model>

<verification>
`bash scripts/check-rebrand.sh` passes green against the full tree. `bash scripts/check-safety.sh` and `node scripts/validate-levels.mjs` still pass (no src/ behavior touched). Full repo grep `grep -rni "math lab" .` (excluding `.git/`, `.planning/`, `archive/`) returns only the 2 allowlisted src/progress.js lines.
</verification>

<success_criteria>
Zero un-allowlisted "Math Lab"/"mathlab" occurrences remain anywhere in the project; a permanent, proven (RED-tested) regression gate enforces this on every future commit.
</success_criteria>

## Artifacts This Phase Produces

- `scripts/check-rebrand.sh` — new permanent gate script.
- `README.md`, `docs/DEPLOY.md`, `docker/Dockerfile`, `CREDITS.md`, `scripts/generate-art-assets.py`, `scripts/build-art-assets.py` — rebranded prose/docstrings.

<output>
Create `.planning/phases/26-grunge-palette-nox-run-rebrand/26-10-SUMMARY.md` when done
</output>
