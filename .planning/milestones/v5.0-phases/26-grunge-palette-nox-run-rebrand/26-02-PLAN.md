---
phase: 26
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/config.js
  - scripts/check-contrast.mjs
  - scripts/build-art-assets.py
  - .planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTRAST.md
autonomous: false
requirements: [VIS-02]

must_haves:
  truths:
    - "CONFIG.PALETTE has exactly 3 new hue-tinted dark tokens (moss green, cold blue-grey, muted rust/umber), zero pink/magenta anywhere in the palette, and a permanent script proves both facts."
    - "Every text/UI-component palette role clears its WCAG threshold (4.5:1 for text roles, 3:1 for UI-component roles) against the roles it is actually rendered on, recorded in a table in the phase evidence doc."
    - "A human has explicitly signed off, against a rendered swatch proof of every CONFIG.PALETTE role, that none of them reads as pink/magenta/mauve — closing the 'in addition to human sign-off' half of 26-CONTEXT.md's VIS-02 banned-hue decision (the automated isBannedHue() math closes the other half)."
  artifacts:
    - "CONFIG.PALETTE: 3 new keys ACCENT_MOSS, ACCENT_SLATE, ACCENT_RUST"
    - "scripts/check-contrast.mjs — WCAG ratio + banned-hue guardrail script, exits non-zero on any failure"
    - "26-CONTRAST.md — the WCAG contrast table + banned-hue guard result, the phase's evidence doc for VIS-02 sign-off"
    - "26-PALETTE-SWATCH.png — rendered swatch proof of all 15 CONFIG.PALETTE roles, generated by scripts/build-art-assets.py's new build_palette_swatch(), the artifact the human checkpoint reviews"
  key_links:
    - "scripts/check-contrast.mjs reads src/config.js's CONFIG.PALETTE directly (single source of truth, no hardcoded hex copy)"
    - "scripts/build-art-assets.py's build_palette_swatch() also reads CONFIG.PALETTE live via a node subprocess call (never hand-mirrored into a Python constant), keeping the swatch proof in sync with the same source of truth check-contrast.mjs uses"
---

<objective>
Expand `CONFIG.PALETTE` (centralized in 26-01) with exactly the 3 new hue-tinted darks VIS-02 requires, then build the permanent automated proof that every role clears WCAG AA and that no color reads as pink/magenta.

Purpose: VIS-02 must land AFTER centralization (26-01) and BEFORE any per-level theming or door/enemy sprite work reads the new hues — this is the phase's second wave, gating everything downstream that consumes CONFIG.PALETTE's new tokens. 26-CONTEXT.md's VIS-02 decision requires the banned-hue guardrail "in addition to human sign-off" — this plan closes both halves: the automated isBannedHue() math (Task 2) and an explicit human checkpoint against a rendered swatch proof (Tasks 4-5).
Output: 3 new palette tokens; `scripts/check-contrast.mjs`; `26-CONTRAST.md` evidence table; `26-PALETTE-SWATCH.png` proof image; a closed human sign-off checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-RESEARCH.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 new hue-tinted dark tokens to CONFIG.PALETTE</name>
  <files>src/config.js</files>
  <read_first>src/config.js's `const PALETTE = {...}` block created by 26-01</read_first>
  <action>
Add exactly these 3 new keys to the `PALETTE` object created in 26-01 (append after CURSOR, before the closing brace): ACCENT_MOSS: [0x2a, 0x3d, 0x2a] (dark moss green — calm early-level accent), ACCENT_SLATE: [0x2c, 0x38, 0x44] (cold blue-grey — mid-level accent), ACCENT_RUST: [0x5a, 0x33, 0x22] (muted rust/umber — harsh late-level accent). Do not add, rename, or remove any other PALETTE key. Do not touch DOOR/MATH_GATE/ENEMY/COLLECT or any other CONFIG block in this task.
  </action>
  <acceptance_criteria>
    - CONFIG.PALETTE has 15 total keys after this task (12 from 26-01 plus these 3).
    - Each new value is a 3-element array matching the hex triples above exactly.
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "import { CONFIG } from './src/config.js'; const k=['ACCENT_MOSS','ACCENT_SLATE','ACCENT_RUST']; for (const x of k) if (!Array.isArray(CONFIG.PALETTE[x]) || CONFIG.PALETTE[x].length!==3) throw new Error('missing '+x); console.log('ok');"</automated>
  </verify>
  <done>CONFIG.PALETTE has 15 keys total, including the 3 new hue tokens with exact values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build scripts/check-contrast.mjs (WCAG ratio + banned-hue guardrail)</name>
  <files>scripts/check-contrast.mjs</files>
  <read_first>26-RESEARCH.md "WCAG relative-luminance + contrast-ratio formula" code example; src/config.js's finished CONFIG.PALETTE; scripts/validate-levels.mjs (structural precedent for a plain-Node CLI script with a clear pass/fail exit code, no framework)</read_first>
  <behavior>
    - relativeLuminance([r,g,b]) implements the standard W3C sRGB-to-linear-then-luma formula (threshold 0.03928, gamma 2.4).
    - contrastRatio(a,b) returns (lighter+0.05)/(darker+0.05).
    - A "role pairing" table defines which PALETTE role renders on which background, with a threshold: TEXT-on-SURFACE (4.5), TEXT-on-BG (4.5), TEXT_DIM-on-BG (4.5), REWARD-on-BG (4.5, used as text in title/hud/mathGate), DANGER-on-BG (4.5, used as text in title/challenge), CLEARED-on-SURFACE_UNLOCKED (3.0, UI outline), MUTED_BORDER-on-BG (3.0, UI outline), BORDER-on-BG (3.0, UI outline), ACCENT_MOSS/ACCENT_SLATE/ACCENT_RUST-on-BG (3.0, UI-component/background-tint role, not text).
    - isBannedHue([r,g,b]) returns true iff BOTH (r - g) > 20 AND (b - g) > 20 (green channel meaningfully suppressed relative to both red and blue — this is what reads as magenta/pink/mauve regardless of saturation, per 26-CONTEXT.md's explicit "desaturated magenta/mauve reads as pink" concern). Applied to every CONFIG.PALETTE value.
    - Script prints a table (role, pairing, ratio, threshold, pass/fail) to stdout, then exits 1 if ANY pairing fails its threshold OR any color trips isBannedHue; exits 0 and prints "contrast checks: PASS" otherwise.
  </behavior>
  <action>
Create scripts/check-contrast.mjs as a plain Node ESM script (no dependency — matches the project's zero-new-dependency posture) that imports `{ CONFIG }` from `../src/config.js`, implements the behavior above, and is runnable as `node scripts/check-contrast.mjs` from the repo root. Follow validate-levels.mjs's shape: a CLI script with a clear numeric exit code, not a test framework. Do not hardcode a copy of the palette hex values — read them live from `CONFIG.PALETTE` so the script can never drift from the real source of truth.
  </action>
  <verify>
    <automated>node scripts/check-contrast.mjs; echo "exit:$?"</automated>
  </verify>
  <done>scripts/check-contrast.mjs exists, imports CONFIG.PALETTE live, and running it prints a role/ratio/pass-fail table ending in "contrast checks: PASS" with exit code 0 against the current palette.</done>
</task>

<task type="auto">
  <name>Task 3: Write 26-CONTRAST.md evidence doc and run the gate</name>
  <files>.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTRAST.md</files>
  <read_first>26-CONTEXT.md's "Document WCAG AA contrast per named role... as a table in the phase's evidence doc" instruction</read_first>
  <action>
Create 26-CONTRAST.md recording: (1) the full CONFIG.PALETTE role table with hex values, (2) the check-contrast.mjs output (role/pairing/ratio/threshold/pass-fail table, copied verbatim from the script's stdout), (3) a one-line confirmation that the banned-hue guardrail found zero magenta/pink hits, (4) a note that this doc is the VIS-02 sign-off evidence artifact referenced by 26-VALIDATION.md. Run `node scripts/check-contrast.mjs` and paste its real output — do not hand-write the ratios.
  </action>
  <acceptance_criteria>
    - 26-CONTRAST.md exists and contains a table with all 15 CONFIG.PALETTE roles and their actual computed contrast ratios (not placeholder values).
    - Document states the banned-hue guardrail result explicitly (0 flagged).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-contrast.mjs && test -f .planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTRAST.md</automated>
  </verify>
  <done>26-CONTRAST.md exists with the real script output recorded; check-contrast.mjs exits 0.</done>
</task>

<task type="auto">
  <name>Task 4: Render CONFIG.PALETTE as a swatch proof image</name>
  <files>scripts/build-art-assets.py</files>
  <read_first>scripts/build-art-assets.py's save() helper (line 76) and __main__ block (line 331) — the pattern every new build_*() function follows</read_first>
  <action>
Add a new build_palette_swatch() function to scripts/build-art-assets.py, appended to the file after build_title_bg() and called from __main__ after the existing 4 calls (matches the file's established "each new build_ function gets its own call at the end of __main__" convention, same as build_logo() will do in a later plan). It must NOT hand-mirror palette hex values into a Python constant (avoids drift risk) — instead read the LIVE palette by shelling out to node: `subprocess.run(["node", "--input-type=module", "-e", "import { CONFIG } from './src/config.js'; console.log(JSON.stringify(CONFIG.PALETTE));"], cwd=ROOT, capture_output=True, text=True, check=True)`, then `json.loads(result.stdout)` — the exact same "single source of truth, no hardcoded hex copy" principle Task 2's check-contrast.mjs already established, just applied from the Python side. For each resulting role/[r,g,b] entry (in the dict's insertion order), draw one roughly 140x70px rectangle filled with that RGB, with the role name printed inside it via `ImageFont.load_default()` (Pillow's built-in bitmap font — no new font dependency needed for a debug proof image, unlike the real in-game logo font a later plan sources separately) in black or white text chosen by a simple luminance threshold (r+g+b > 384 -> black text, else white) so the label stays legible against both light and dark swatches. Arrange all roles in a fixed 4-column grid (as many rows as needed) on a mid-grey canvas background so near-black swatches like BG remain visible against the canvas edge, with a small margin between cells. Save to `.planning/phases/26-grunge-palette-nox-run-rebrand/26-PALETTE-SWATCH.png` via the existing save() helper.
  </action>
  <acceptance_criteria>
    - build_palette_swatch() reads CONFIG.PALETTE via a live node subprocess call, not a hand-mirrored Python constant.
    - 26-PALETTE-SWATCH.png contains one labeled swatch per CONFIG.PALETTE role (15 total after Task 1), including ACCENT_MOSS/ACCENT_SLATE/ACCENT_RUST.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/build-art-assets.py && test -f .planning/phases/26-grunge-palette-nox-run-rebrand/26-PALETTE-SWATCH.png</automated>
  </verify>
  <done>26-PALETTE-SWATCH.png exists, rendered from the live CONFIG.PALETTE (all 15 roles including ACCENT_MOSS/ACCENT_SLATE/ACCENT_RUST), each swatch labeled with its role name.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human sign-off — no palette role reads as pink/magenta/mauve</name>
  <files></files>
  <action>
    Open the swatch proof image Task 4 generated and record a blocking human-verify sign-off
    against 26-CONTEXT.md's explicit VIS-02 decision: "Add an explicit, checked banned-hue
    guardrail against magenta/pink... in addition to human sign-off." This closes the human
    half of that requirement (Task 2's isBannedHue() script already closed the automated half).
    This is a verification-only checkpoint; write no code. If any swatch reads as pink/magenta/
    mauve, route back to Task 1 (this plan, for the 3 new hues) or flag 26-01's original 12 for
    a follow-up plan, then re-verify.
  </action>
  <what-built>.planning/phases/26-grunge-palette-nox-run-rebrand/26-PALETTE-SWATCH.png — every CONFIG.PALETTE role (all 15, including the 3 new hue-tinted darks ACCENT_MOSS/ACCENT_SLATE/ACCENT_RUST) rendered as a labeled color swatch, read live from src/config.js.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/26-grunge-palette-nox-run-rebrand/26-PALETTE-SWATCH.png` in an image viewer.
    2. Look at every labeled swatch, one at a time, including the 3 new hues (ACCENT_MOSS, ACCENT_SLATE, ACCENT_RUST) and every pre-existing role (BG, SURFACE, SURFACE_ALT, SURFACE_UNLOCKED, BORDER, MUTED, MUTED_BORDER, TEXT, TEXT_DIM, DANGER, REWARD, CLEARED, CURSOR).
    3. Confirm none of them reads as pink, magenta, or mauve to your eye — this is a genuinely subjective color-perception judgment the automated isBannedHue() math check (Task 2) cannot fully substitute for, per 26-CONTEXT.md's "in addition to human sign-off" instruction.
  </how-to-verify>
  <resume-signal>Type "approved" or name the specific role(s) that read as pink/magenta/mauve</resume-signal>
  <verify>
    <human-check>Human confirms from the real swatch image that no CONFIG.PALETTE role (including the 3 new hues) reads as pink/magenta/mauve.</human-check>
  </verify>
  <done>Human sign-off recorded that zero palette roles read as pink/magenta/mauve; or a defect list routed back to the responsible task/plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure palette-data expansion + a new offline verification script + a debug-only Pillow swatch renderer; no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-02 | Tampering | scripts/check-contrast.mjs | low | accept | Read-only script (reads CONFIG.PALETTE, computes, prints) — no writes, no network, no external input; regression risk is limited to producing a wrong pass/fail verdict, caught by the plan's own <verify> gate |
| T-26-03 | Tampering | scripts/build-art-assets.py's build_palette_swatch() node subprocess call | low | accept | Fixed, hardcoded inline `-e` script string (no user input, no shell interpolation of external data) reading the repo's own src/config.js; same trust level as directly `import`-ing it, just from Python — regression risk is a wrong/blank swatch image, caught by the plan's own <verify> gate and the human checkpoint itself |

</threat_model>

<verification>
`node scripts/check-contrast.mjs` exits 0 and prints "contrast checks: PASS". `26-CONTRAST.md` exists with real (not placeholder) ratio values. `26-PALETTE-SWATCH.png` exists and shows all 15 CONFIG.PALETTE roles. `bash scripts/check-safety.sh` still passes (no src/ behavior touched by this plan beyond config.js data). Human checkpoint (Task 5) explicitly closed with "approved" — VIS-02 is not fully satisfied until this sign-off is recorded, even though the automated tasks (1-4) can complete independently of it.
</verification>

<success_criteria>
CONFIG.PALETTE carries the 3 new hue-tinted darks. A permanent, config-driven script proves every role clears its WCAG threshold and that zero palette entries read as pink/magenta. The evidence is recorded in 26-CONTRAST.md, and a human has explicitly signed off against a rendered swatch proof that no role reads as pink/magenta/mauve — VIS-02's "in addition to human sign-off" requirement is not backed by automation alone.
</success_criteria>

## Artifacts This Phase Produces

- `src/config.js`: `CONFIG.PALETTE.ACCENT_MOSS`, `CONFIG.PALETTE.ACCENT_SLATE`, `CONFIG.PALETTE.ACCENT_RUST` (3 new keys).
- `scripts/check-contrast.mjs` — new script; exports/functions `relativeLuminance()`, `contrastRatio()`, `isBannedHue()`, a role-pairing table, and a CLI entry point.
- `.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTRAST.md` — new evidence doc.
- `scripts/build-art-assets.py`: new `build_palette_swatch()` function, called from `__main__`.
- `.planning/phases/26-grunge-palette-nox-run-rebrand/26-PALETTE-SWATCH.png` — new swatch proof image, the artifact the human checkpoint reviews.

<output>
Create `.planning/phases/26-grunge-palette-nox-run-rebrand/26-02-SUMMARY.md` when done
</output>
