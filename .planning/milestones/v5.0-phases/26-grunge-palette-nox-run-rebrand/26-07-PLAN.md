---
phase: 26
plan: 07
type: execute
wave: 6
depends_on: ["26-05"]
files_modified:
  - scripts/build-art-assets.py
  - assets/_font-src/monogram.ttf
  - assets/logo-hero.png
  - assets/logo-badge.png
  - assets/LICENSES/monogram.txt
  - src/config.js
  - src/main.js
  - src/scenes/title.js
  - src/scenes/select.js
  - CREDITS.md
autonomous: false
requirements: [BRAND-01, BRAND-03]

must_haves:
  truths:
    - "The title screen shows a baked 'NOX RUN' pixel-font wordmark (dark-green fill, neon-green edge highlight) instead of the old text(\"Math Lab\") call, revealed via a single non-strobing tween on scene-enter, never gated behind a key press."
    - "Two distinct baked sizes exist (title-screen hero, small UI badge) — no runtime scaling of one asset."
  artifacts:
    - "assets/logo-hero.png, assets/logo-badge.png — baked via Pillow + the monogram CC0 font"
    - "assets/_font-src/monogram.ttf + assets/LICENSES/monogram.txt (CC0 proof)"
    - "src/config.js: CONFIG.TITLE.LOGO_REVEAL_MS"
  key_links:
    - "src/scenes/title.js scene-enter -> add([sprite(\"logo-hero\"), opacity(0), ...]) + tween(0,1,LOGO_REVEAL_MS/1000,...) (self-clean tween idiom, no onEnd(destroy) since the logo persists)"
    - "src/scenes/select.js heading area -> sprite(\"logo-badge\") (the small UI badge's actual use site)"
---

<objective>
Source the CC0 "monogram" pixel font, bake the Nox Run wordmark at two sizes via the existing Pillow pipeline, and wire it into the title screen with a non-strobing reveal tween, per BRAND-01/BRAND-03.

Purpose: the roadmap's build-order rationale places the logo AFTER palette work specifically so it is "designed against final tokens" — this plan is the first to consume CONFIG.PALETTE.ACCENT_MOSS (dark-green fill) and CONFIG.PALETTE.REWARD (neon-green edge), both finalized in 26-01/26-02.
Output: assets/logo-hero.png, assets/logo-badge.png; title.js reveal wiring; select.js badge placement; a human sign-off checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-CONTEXT.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-RESEARCH.md
@.planning/phases/26-grunge-palette-nox-run-rebrand/26-PATTERNS.md
@assets/LICENSES/spike.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source the monogram font and bake logo-hero.png / logo-badge.png</name>
  <files>scripts/build-art-assets.py, assets/_font-src/monogram.ttf, assets/logo-hero.png, assets/logo-badge.png, assets/LICENSES/monogram.txt</files>
  <read_first>scripts/build-art-assets.py's build_title_bg() (lines 304-328, the closest "compose then save at a fixed canvas size" template) and PLAYER_PALETTE/ENVIRONMENT_PALETTE structure; assets/LICENSES/spike.txt (proof-file template)</read_first>
  <action>
Download the monogram TTF (CC0, by datagoblin — re-confirmed live this session at https://datagoblin.itch.io/monogram) to `assets/_font-src/monogram.ttf`. itch.io's free-asset download flow is interactive (not a stable public zip URL) — first try fetching the page HTML for an embedded direct file URL and `curl` it; if that fails, use the project's existing Playwright-resolution pattern (see `scripts/browser-boot.mjs`'s `resolvePlaywright()`) to automate the download click-through in a headless browser and save the resulting `.ttf`. Do not substitute a different, unverified font — if sourcing genuinely fails, halt and report rather than faking a fallback (VIS/BRAND requirements forbid placeholder-lite substitutions). Write `assets/LICENSES/monogram.txt` following spike.txt's template: source URL `https://datagoblin.itch.io/monogram`, License: CC0, quote the page's own "Monogram is a monospace bitmap font, free and CC0!" declaration, dated verification = today.

Add `build_logo()` to scripts/build-art-assets.py: load `assets/_font-src/monogram.ttf` via `PIL.ImageFont.truetype`, render the text "NOX RUN" via `ImageDraw.text(..., fill=ACCENT_MOSS, stroke_width=2, stroke_fill=REWARD_GREEN)` — Pillow's built-in stroke params (available in Pillow 10.2.0) draw the neon-green edge-highlight around a dark-green fill in one call, matching CONTEXT.md's "light/neon edge-highlight... on a dark-green fill" spec. Define `ACCENT_MOSS = (0x2a, 0x3d, 0x2a)` and `REWARD_GREEN = (0x00, 0xff, 0x88)` as new module constants matching CONFIG.PALETTE.ACCENT_MOSS/REWARD exactly (same hand-mirror convention as ENVIRONMENT_PALETTE). Render onto a transparent canvas sized to the text's own bbox plus padding, then use `Image.NEAREST` to scale (pixel-font-appropriate, no smoothing) to two target canvases: hero ~360x90px, badge ~144x36px (badge is exactly a 0.4x linear scale of hero — bake both independently from the source render, do not runtime-scale one into the other). Save `assets/logo-hero.png` and `assets/logo-badge.png`. Call `build_logo()` from `__main__` after the existing calls.
  </action>
  <acceptance_criteria>
    - assets/_font-src/monogram.ttf exists and is a valid TTF (openable by PIL.ImageFont.truetype).
    - assets/logo-hero.png and assets/logo-badge.png exist, both showing "NOX RUN" with a dark-green fill and visible neon-green edge.
    - assets/LICENSES/monogram.txt exists and declares CC0.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/build-art-assets.py && test -f assets/logo-hero.png && test -f assets/logo-badge.png && test -f assets/LICENSES/monogram.txt && grep -qi CC0 assets/LICENSES/monogram.txt</automated>
  </verify>
  <done>Both logo PNGs exist, baked from a genuinely CC0-sourced font, with a dark-green fill + neon-green stroke matching CONFIG.PALETTE tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the logo into title.js (reveal tween) and select.js (badge)</name>
  <files>src/config.js, src/main.js, src/scenes/title.js, src/scenes/select.js</files>
  <behavior>
    - On titleScene() entry, the logo-hero sprite mounts at opacity 0 and unconditionally tweens to opacity 1 over CONFIG.TITLE.LOGO_REVEAL_MS milliseconds — no key press or click required to start the reveal (unlike the audio-gesture-gate pattern; this is a pure visual tween).
    - The reveal never repeats/loops (one-shot, no lifespan()/wait()/loop() — check-safety.sh's existing no-scheduler gate covers this) and completes in <= 500ms.
    - selectScene() renders the logo-badge sprite near its existing "Select a Level" heading.
  </behavior>
  <action>
<!-- planner-discipline-allow: text("Math Lab" -->
The literal named below is the DELETION target, not example/illustration code to add.
config.js: add `LOGO_REVEAL_MS: 400,` to `CONFIG.TITLE` (comfortably under the 500ms cap, matches CONFIG.FX.BURST_MS's existing 400ms non-strobing precedent).

main.js: add `loadSprite("logo-hero", "../assets/logo-hero.png");` and `loadSprite("logo-badge", "../assets/logo-badge.png");` alongside the existing title-bg load.

title.js: DELETE the `text("Math Lab", { size: T.TITLE_SIZE }), anchor("center"), pos(center()), color(ACCENT_GREEN...), fixed(), z(9000), "title"` wordmark block entirely. Replace it with: mount `add([sprite("logo-hero"), anchor("center"), pos(center()), opacity(0), fixed(), z(9000), "title"])`, capture the returned object, then immediately (still at the top level of titleScene(), unconditional — not inside any key/click handler) call `tween(0, 1, CONFIG.TITLE.LOGO_REVEAL_MS / 1000, (v) => (logo.opacity = v), easings.easeOutQuad)`. Do NOT chain `.onEnd(() => destroy(logo))` — unlike the transient "Progress reset." banner, the logo must PERSIST at opacity 1 after the reveal, not self-destroy. This satisfies check-safety.sh's existing "fx effects self-clean via tween" spirit (uses tween(), never a scheduler) while correctly NOT destroying a persistent UI element.

select.js: add `add([sprite("logo-badge"), anchor("center"), pos(center().x, S.ROW_Y - S.TILE_H - 28), fixed(), z(9000), "select"])` immediately before the existing "Select a Level" heading add() call (small badge sits just above the heading text, does not replace it).
  </action>
  <acceptance_criteria>
    - title.js contains zero `text("Math Lab"` calls; contains one `sprite("logo-hero")` add() and one unconditional `tween(0, 1, ...)` call at the scene body's top level (not inside a handler).
    - select.js contains one `sprite("logo-badge")` add() near its heading.
    - CONFIG.TITLE.LOGO_REVEAL_MS === 400.
  </acceptance_criteria>
  <verify>
    <automated>node --check src/config.js && node --check src/main.js && node --check src/scenes/title.js && node --check src/scenes/select.js && grep -qc 'text("Math Lab"' src/scenes/title.js | grep -qx 0; grep -q 'sprite("logo-hero")' src/scenes/title.js && grep -q 'sprite("logo-badge")' src/scenes/select.js && bash scripts/check-safety.sh</automated>
  </verify>
  <done>title.js shows the baked logo with a self-cleaning (non-scheduler) reveal tween that leaves the logo visible; select.js shows the badge; check-safety.sh passes (no scheduler introduced).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off — logo legibility and reveal feel in the running game</name>
  <files></files>
  <action>
    Serve the game over HTTP and view the title/select screens in a real browser, then record
    a blocking human-verify sign-off. This is a verification-only checkpoint; write no code.
    If a defect appears, route it back to Task 1 (rebake) or Task 2 (rewiring) and re-verify.
  </action>
  <what-built>Real baked "NOX RUN" pixel-font logo (dark-green fill, neon-green edge) replacing the old plain-text "Math Lab" wordmark on the title screen, with a non-strobing reveal tween on scene load; a smaller badge version on the level-select heading.</what-built>
  <how-to-verify>
    1. `cd src && python3 -m http.server 8000`, open `http://localhost:8000/` in a browser.
    2. On load, watch the title screen: confirm the "NOX RUN" logo fades/reveals in smoothly over well under a second — no flicker, no repeated flashing (non-strobing), and it plays automatically without needing to press anything.
    3. Confirm the logo is legible at the actual displayed size (960x540, 1.5x scale) — dark-green fill with a visible neon-green edge, readable as "NOX RUN", not a smear of illegible pixels.
    4. Press Space/Enter to reach the level-select screen; confirm the smaller badge logo appears near the "Select a Level" heading and is also legible at its size.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. "reveal too fast/slow", "logo illegible at this size", "wrong colors")</resume-signal>
  <verify>
    <human-check>Human confirms in a real browser: the logo reveal is smooth, one-shot, non-strobing, and completes automatically without a key press; the hero logo is legible at real title-screen scale; the badge logo is legible on level-select.</human-check>
  </verify>
  <done>Human sign-off recorded that the logo and its reveal animation meet the dark-green/neon-edge, non-strobing, ≤500ms, legible-at-real-size bar; or a defect list routed back to Task 1/2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External font download | Downloading a font file from an interactive itch.io flow — a different download mechanism than the project's prior straightforward zip downloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-08 | Tampering | assets/_font-src/monogram.ttf | medium | mitigate | Only the single .ttf file is vendored (not itch.io's bonus "thank-you-gift.zip" or any bundled extras); CC0 license re-verified via direct page fetch this planning session and recorded in a dated proof file, matching the existing 9-asset precedent |
| T-26-09 | Tampering | docker/nginx serving a new font-derived PNG asset | low | accept | logo-hero.png/logo-badge.png are pre-baked static PNGs (not the .ttf itself) shipped the same way every other asset PNG already is; the raw font file stays in `_font-src/` (build-time only, mirrors the existing `_kenney-src`/`_opengameart-src` convention of never shipping raw vendor sources to the Docker image) |

</threat_model>

<verification>
Both logo PNGs exist and are visually legible (human-verified in the checkpoint). `bash scripts/check-safety.sh` still passes (reveal uses tween(), no scheduler). `node scripts/browser-boot.mjs` still passes (proves logo-hero/logo-badge sprite names resolve with no 404/console error).
</verification>

<success_criteria>
The title screen shows a real, CC0-sourced, baked "Nox Run" pixel-font logo with a non-strobing ≤500ms auto-reveal; a badge-sized variant appears on level-select; both are human-signed-off as legible at real display size.
</success_criteria>

## Artifacts This Phase Produces

- `assets/_font-src/monogram.ttf` — new vendor dir + source font.
- `assets/LICENSES/monogram.txt` — new proof file.
- `scripts/build-art-assets.py`: `build_logo()` function.
- `assets/logo-hero.png`, `assets/logo-badge.png`.
- `src/config.js`: `CONFIG.TITLE.LOGO_REVEAL_MS`.
- `src/main.js`: `loadSprite("logo-hero", ...)`, `loadSprite("logo-badge", ...)`.
- `src/scenes/title.js`: logo sprite + unconditional reveal `tween()` replacing the old plain-text wordmark call.
- `src/scenes/select.js`: `sprite("logo-badge")` near the heading.

<output>
Create `.planning/phases/26-grunge-palette-nox-run-rebrand/26-07-SUMMARY.md` when done
</output>
