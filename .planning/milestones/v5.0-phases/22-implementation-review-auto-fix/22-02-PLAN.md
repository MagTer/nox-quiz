---
phase: 22-implementation-review-auto-fix
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/ui/challenge.js
  - src/ui/mathGate.js
  - src/mechanics/door.js
  - src/mechanics/gates.js
  - src/mechanics/enemy.js
  - src/mechanics/collect.js
  - scripts/lib/mechanic-drive.mjs
  - .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "Each of the 6 Cluster A files (challenge.js, mathGate.js, door.js, gates.js, enemy.js, collect.js) has an explicit verdict in 22-FINDINGS.md backed by tiered evidence (source read for unconditional semantics, audit rows or targeted Playwright for behavior)"
    - "The door/gates busy-guard asymmetry (vs enemy.js WR-03) is resolved — guard added OR why-unneeded documented in both files; neither outcome is silently skipped"
    - "The collect.js multi-zone active-slot corruption is dispositioned: zero-behavior-change hardening committed, or escalated to the FIX-02 batch — never fixed with a behavior change and never ignored"
    - "Design-feel items (same-time-open prevention, glyph clarity, answer-box magic numbers) are recorded as escalation candidates with PENDING-DECISION status — none implemented in this plan"
    - "After all Cluster A commits: 4 static gates green, browser-boot exits 0, and the 16-encounter audit rows match the baseline (10 resolved rows identical, same 6 unreached)"
  artifacts:
    - path: "src/mechanics/door.js"
      provides: "busy-guard resolution — WR-03-referencing guard or why-unneeded comment"
      contains: "WR-03"
    - path: "src/mechanics/gates.js"
      provides: "busy-guard resolution — WR-03-referencing guard or why-unneeded comment"
      contains: "WR-03"
    - path: ".planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md"
      provides: "Cluster A numbered findings + 6 verdict rows + Cluster A regression record"
      contains: "Cluster A regression: PASS"
  key_links:
    - from: "src/mechanics/door.js"
      to: "src/mechanics/enemy.js"
      via: "WR-03 busy-guard pattern (commit 5d168dc) copied or explicitly declined with rationale"
      pattern: "WR-03"
    - from: ".planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md"
      to: "scripts/audit-phase21-mechanics.mjs"
      via: "post-cluster audit row-diff against the recorded baseline table"
      pattern: "Cluster A regression"
---

<objective>
Review Cluster A — the challenge seam and all four math mechanics — with behavioral evidence, and land the auto-fixes that fall inside the CONTEXT-locked boundary (FIX-01).

This is the highest-risk cluster (per 22-RESEARCH.md Suspect Surface Inventory): the shared challenge.js seam, its five thin callers, and three pre-seeded latent-defect targets found during research. Auto-fix only objectively-wrong classes (crashes, leaks, handler stacking, dead-object writes); anything touching game feel, mechanic semantics, or visual identity gets a PENDING-DECISION escalation entry instead (CONTEXT locked decision: "when genuinely ambiguous, escalate rather than fix").

Purpose: entity bugs fixed before content doubles on top of them (roadmap Phase 22 goal).
Output: atomic fix commits (0 or more), Cluster A findings + 6 verdict rows in 22-FINDINGS.md, post-cluster regression proof vs the Plan 22-01 baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-implementation-review-auto-fix/22-CONTEXT.md
@.planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md
@.planning/phases/22-implementation-review-auto-fix/22-PATTERNS.md
@.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
</context>

## Artifacts this phase produces

- Conditional (created only if the corresponding review confirms the defect):
  - closure-local `busy` re-entrancy guard flags inside the wire functions of `src/mechanics/door.js` and `src/mechanics/gates.js` (copied from the enemy.js WR-03 shape; closure-local, never module-level)
  - zone-ownership guard condition + zone-entry re-entrancy guard in `src/mechanics/collect.js` (extends the existing single onCollide handler's guard; no new exports)
  - liveness guard (existence check) in `src/ui/challenge.js` close() prior-object restore loop
- New 22-FINDINGS.md content: Cluster A numbered findings, escalation candidate entries (answer-box constants; collect multi-zone if escalated), 6 verdict rows, "Cluster A regression: PASS" record
- Uncommitted throwaway Playwright evidence scripts in the session scratchpad (Phase 21 precedent — not repo artifacts)
- No new exported symbols, CLI flags, or config tokens (CONFIG.GATE token extraction is escalation-gated to Plan 22-05)

<tasks>

<task type="auto">
  <name>Task 1: Review the challenge seam trio — challenge.js, mathGate.js, enemy.js</name>
  <files>src/ui/challenge.js, src/ui/mathGate.js, src/mechanics/enemy.js, .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md</files>
  <read_first>
    - src/ui/challenge.js (329 lines — instanceTag at ~line 133, prior-object hide at ~116–118, close() restore at ~316–325, keyCtrls cancel)
    - src/ui/mathGate.js (thin wrapper; gate-cleared banner teardown relies on scene leave)
    - src/mechanics/enemy.js (70 lines — WR-03 busy guard at ~33–54, 21-04 two-line label fix; this is the analog other tasks copy)
    - src/scenes/game.js (both onSceneLeave registrations at ~lines 295/307 — the sweep the seam relies on)
    - .planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md (Cluster A suspect rows + Kaplay Bug-Pattern Checklist items 1, 2, 3, 5, 6, 11)
    - .planning/phases/22-implementation-review-auto-fix/22-PATTERNS.md (section 5 — analogs eab06ed/f58f3fb and the review-don't-finish rule; section 7 — throwaway evidence script rules)
    - scripts/browser-boot.mjs (server skeleton to copy for throwaway scripts — CR-02 hardened, do not weaken)
    - scripts/lib/mechanic-drive.mjs (deriveEncounters/driveToXClimbing/resolveIfBoxed — never re-derive traversal)
  </read_first>
  <action>
    Work through the three files against the Bug-Pattern Checklist, recording one numbered finding per hypothesis in 22-FINDINGS.md (21-FINDINGS format: what broke / why / fix or disposition / file; CONFIRMED/REFUTED verdicts; evidence tier stated).

    challenge.js — three review targets:
    1. close() dead-object hazard (research flag ⚠): close() un-hides the priorChallengeObjs snapshot taken at open; if a hidden prior object is destroyed in the interim this writes to a dead object. Verify behaviorally with a throwaway Playwright script in the scratchpad (copy the browser-boot.mjs server skeleton verbatim — loopback bind + path clamp — and use a port at 8770 or above; 8765–8768 are taken): (a) stack two challenges (enter the level-01 answer-zone at x300, then trigger the math-gate at x600 so the collect challenge is hidden), resolve the outer one, and assert via page-handle evaluation that the prior challenge's objects are restored un-hidden with zero uncaught page errors; (b) with a challenge open, drive the scene-exit path to select and assert challenge-tagged object count is 0 in the select scene and no uncaught errors fire. If a write-to-destroyed-object is reachable, auto-fix by adding an existence check to the restore loop (bug-pattern #11 class, objectively-wrong: guard each restored object with the engine's liveness predicate before flipping hidden) — atomic commit citing the new finding ID. If unreachable, record the invariant as a REFUTED-hazard finding with the evidence.
    2. Same-time-open concurrency (residual New Finding 4 tech debt): review only. Enrich the pre-seeded escalation entry with evidence from (a) above; the f58f3fb commit message reasoning (refusing a second open would strand a frozen player) is the recommendation for leave-as-designed. Do NOT implement prevention.
    3. Answer-box layout constants (BOX_W 84, BOX_H 44, GAP 16 inline vs sibling CONFIG.GATE tokens — 21-REVIEW IN-03): append a new escalation candidate entry with Status: PENDING-DECISION. Extraction creates new config tokens, which the CONTEXT polish rule (existing tokens only) puts outside auto-fix. Do NOT extract in this plan.

    mathGate.js: verify the gate-cleared banner teardown holds on scene exit (game.js cancels clearTransitionTween on scene leave; banner objects destroyed with the scene). Source-read tier is sufficient if semantics are unconditional; record the verdict.

    enemy.js: verify the WR-03 busy guard and the 21-04 two-line label fix are still present at HEAD (source read) and that the baseline audit row for the level-01 enemy at x1000 showed triggered/resolved true (behavioral confirmation already in the baseline table). Note the invariant that busy is reset only in onSuccess — currently fine because success is the only close path; record as a finding note for future close paths.

    Every fix commit: atomic, subject fix(22-02): per 22-PATTERNS.md format; after each fix run node --check on touched files plus the 4 static gates. Findings/verdict updates go in docs(22-02) commits.
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && bash scripts/check-progress.sh && F=.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md && [ "$(grep -c 'src/ui/challenge.js' "$F")" -ge 2 ] && [ "$(grep -c 'PENDING-DECISION' "$F")" -ge 3 ] && echo TRIO-REVIEW-OK</automated>
  </verify>
  <acceptance_criteria>
    - 22-FINDINGS.md contains numbered findings for the challenge.js close() hazard (CONFIRMED with fix commit SHA, or REFUTED with the throwaway-script evidence described), the mathGate.js teardown verdict, and the enemy.js fix-presence verification
    - The escalation candidates section now includes the answer-box constants entry (at least 3 PENDING-DECISION entries total: glyph clarity, same-time-open, answer-box constants)
    - No diff in this task touches rendering away from Kaplay text()/rect() draws — `bash scripts/check-gate.sh` (DOM-sink assertion 6) passes after every commit
    - Any behavioral claim in the new findings carries a screenshot path or a page-evaluation transcript reference; no finding says "should work"
    - src/ui/challenge.js public API unchanged: openChallenge signature untouched (check-gate.sh assertion 2 passes)
  </acceptance_criteria>
  <done>Three files reviewed with tiered evidence, findings recorded, escalation entries appended, all static gates green on HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve door/gates busy-guard asymmetry and collect.js multi-zone corruption</name>
  <files>src/mechanics/door.js, src/mechanics/gates.js, src/mechanics/collect.js, .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md</files>
  <read_first>
    - src/mechanics/door.js (69 lines) and src/mechanics/gates.js (61 lines) — both lack the busy guard; both set player.paused synchronously
    - src/mechanics/enemy.js lines 31–54 (the exact WR-03 guard shape to copy: closure-local flag, set before mutation, reset only in onSuccess, comment citing the finding ID and GC rationale)
    - src/mechanics/collect.js (140 lines — single active slot, pickup guard condition, labelObj handling; header documents the paused-skip engine semantics and the f541f88 single-handler rule)
    - lib/kaplay.mjs (READ-ONLY context — the collision-pass paused-object handling, to answer the same-frame double-fire question; never modify)
    - src/levels/level-01.js, src/levels/level-02.js, src/levels/level-03.js, src/levels/level-04.js (zone counts: L1/L3/L4 one answer-zone each, L2 zero — the fact that makes the collect fix zero-behavior-change today; READ-ONLY, no geometry edits)
    - .planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md (Open Question 1; Cluster A collect.js rows; Bug-Pattern Checklist items 1 and 2)
  </read_first>
  <action>
    Part 1 — door/gates busy-guard (resolves 22-RESEARCH.md Open Question 1). Determine whether two barrier collisions can fire in the frame before player.paused takes effect, by reading the vendored engine's collision pass (does it snapshot pairs before dispatch? is paused checked per-pair at dispatch time?) and, if inconclusive from source, a targeted throwaway Playwright probe. Then take exactly one of two actions in BOTH files: (a) if any doubt remains, add the closure-local busy re-entrancy guard copying the enemy.js WR-03 shape — flag declared next to the wire function's other closure state, checked at handler entry, set before player.paused mutation, reset in onSuccess, comment citing WR-03 / commit 5d168dc and the new 22-FINDINGS finding ID (auto-fix: leak/re-entrancy defect class per CONTEXT); or (b) if provably unneeded, add a short comment in each file explaining why the WR-03 guard is not required here (citing the engine semantics found), so the asymmetry stops being unexplained. Either outcome: one atomic fix/docs commit per file or one covering both, and a numbered finding with the verdict.

    Part 2 — collect.js multi-zone active-slot corruption (research flag ⚠, latent today). Confirm from source: the single active slot is overwritten when a second answer-zone is entered while the first is open; the pickup guard checks only that a slot is active and has a value, not that the touched pickup belongs to the active slot's zone; an overwritten slot's labelObj reference would leak stacked labels on re-trigger. Default disposition — auto-fix as a defect-class fix (handler-scoping/leak class, inside the CONTEXT auto-fix boundary) with these constraints: keep exactly ONE top-level onCollide registration per event (f541f88 rule — no handler-inside-handler); add a zone-entry re-entrancy guard so that while a slot is active, entry into a DIFFERENT zone returns early (same idiom class as the WR-03 busy guard, consistent with how every other mechanic ignores new triggers while open); extend the pickup guard to also require that the touched pickup belongs to the active slot's zone; all state closure-local (a727c13 rule). This must be provably zero-behavior-change on the current 4 levels (each has at most one answer-zone, so the new guards can never fire today) — proven in Task 3 by the audit row diff. If during implementation the fix cannot be made zero-behavior-change, do NOT commit it; instead append an escalation candidate entry (Status: PENDING-DECISION) recommending the fix land with Phase 25's multi-zone content, per the CONTEXT rule that genuinely ambiguous calls escalate.

    Per-fix cadence: node --check on each touched file + the 4 static gates after every commit. Atomic commits, fix(22-02) subjects, body per 22-PATTERNS.md (symptom → root cause with engine-semantics citation → what the fix does → what it deliberately does not do).
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && bash scripts/check-progress.sh && [ "$(grep -c 'WR-03' src/mechanics/door.js)" -ge 1 ] && [ "$(grep -c 'WR-03' src/mechanics/gates.js)" -ge 1 ] && F=.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md && [ "$(grep -c 'src/mechanics/collect.js' "$F")" -ge 1 ] && echo MECHANICS-OK</automated>
  </verify>
  <acceptance_criteria>
    - Both door.js and gates.js contain a WR-03 reference: either the copied busy guard (closure-local, reset in onSuccess) or the why-unneeded comment citing the engine semantics — `grep -c 'WR-03'` returns ≥1 for each file
    - 22-FINDINGS.md contains a numbered finding resolving Open Question 1 with a CONFIRMED or REFUTED verdict and the evidence tier used
    - collect.js disposition recorded: either fix commit SHA cited in a finding (and the guards are closure-local, single onCollide preserved — no new module-level state, check-import-safety.sh passes) or an escalation entry with Status: PENDING-DECISION exists for it
    - check-gate.sh thin-caller assertions (door/gates/collect still call openChallenge) pass after all commits
    - No commit in this task touches src/levels/ or lib/kaplay.mjs (`git status --porcelain -- src/levels lib` empty before the docs commit)
  </acceptance_criteria>
  <done>Busy-guard asymmetry explained-or-fixed in both files, collect multi-zone corruption dispositioned per the boundary rules, gates green after every commit.</done>
</task>

<task type="auto">
  <name>Task 3: Cluster A regression run and verdict rows</name>
  <files>.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md, scripts/lib/mechanic-drive.mjs</files>
  <read_first>
    - .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md (the baseline 16-row audit table recorded by Plan 22-01 — the comparison target)
    - .planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md (Common Pitfalls 4 — resolveIfBoxed detects resolution via baseline decrease; "Zero-regressions definition" paragraph)
  </read_first>
  <action>
    Run the cluster-boundary regression: `node scripts/browser-boot.mjs` (must exit 0) then `node scripts/audit-phase21-mechanics.mjs` (always exits 0 — compare rows, not exit code). Diff the fresh 16-row results table row-by-row against the baseline table in 22-FINDINGS.md: the 10 baseline-resolved rows must show identical Triggered/Resolved values (reachedX float jitter excepted; collect answer-zones stay resolved-null by design), and the SAME 6 rows must remain unreached (level-01 math-gate x1300, level-02 math-gate x1100, level-02 door x1540, level-03 enemy x2400, level-04 math-gate x1800, level-04 enemy x2400) — improving them is Phase 23 scope, and a newly-REACHED row is also a diff to explain, not silently celebrate.

    If a row differs because a script assumption broke rather than the game (Pitfall 4 — e.g. a re-entrancy guard changed the challenge-open counts resolveIfBoxed keys on), lightly extend scripts/lib/mechanic-drive.mjs (CONTEXT discretion: evidence gathering only, no harness improvements — those are Phase 23's) and state exactly what was extended and why in the findings.

    Record in 22-FINDINGS.md: the fresh 16-row table, the row-by-row diff outcome, and the line `Cluster A regression: PASS` only when the diff is clean (or clean-after-documented-script-extension). Then set the Verdict cell for the 6 Cluster A rows in the Per-Entity Verdict Table (challenge.js, mathGate.js, door.js, gates.js, enemy.js, collect.js) to their final values (clean / fixed / escalated) with finding refs. Commit docs(22-02).
  </action>
  <verify>
    <automated>node scripts/browser-boot.mjs && F=.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md && [ "$(grep -c 'Cluster A regression: PASS' "$F")" -eq 1 ] && [ "$(grep -c '| pending |' "$F")" -eq 18 ] && echo CLUSTER-A-OK</automated>
  </verify>
  <acceptance_criteria>
    - browser-boot.mjs exits 0 (all 4 levels boot with zero uncaught errors) on post-fix HEAD
    - The fresh audit table is recorded in 22-FINDINGS.md next to the baseline with an explicit row-by-row diff statement; the 6 expected-unreached rows are unchanged and the 10 resolved rows match
    - "Cluster A regression: PASS" appears exactly once in 22-FINDINGS.md
    - Per-Entity Verdict Table now has exactly 18 rows still pending (24 minus the 6 Cluster A rows, which carry final verdicts + finding refs)
    - If mechanic-drive.mjs was extended, the findings state what changed and why the game (not the script) is unregressed
  </acceptance_criteria>
  <done>Cluster A proven regression-free against the baseline, 6 verdict rows final, findings committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| question/label text → canvas rendering | challenge.js renders brain-supplied text; must stay Kaplay text() draws (no DOM sink) |
| throwaway evidence scripts → LAN | scratchpad Playwright scripts embed a static server; must inherit CR-02 hardening |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-03 | Tampering | challenge.js rendering (markup injection surface) | medium | mitigate | Keep pure canvas text() rendering; check-gate.sh assertion 6 (DOM-sink ban) runs after every commit in this plan |
| T-22-02 | Information Disclosure | throwaway Playwright static servers | medium | mitigate | Copy the server skeleton from scripts/browser-boot.mjs verbatim (loopback-only bind + path-separator-boundary clamp per commit 20903c4/bf1e2b9); ports ≥8770 |
| T-22-06 | Denial of Service | re-entrancy guards soft-locking the player | medium | mitigate | Guards follow the proven WR-03 shape (reset in onSuccess); zero-behavior-change proven by the Task 3 audit row diff on all 4 levels |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (binding zero-new-dependencies decision) |
</threat_model>

<verification>
- All 4 static gates green after every commit in this plan
- browser-boot green + audit row-diff clean vs baseline at cluster end
- 6 Cluster A verdict rows final; escalation candidates carry PENDING-DECISION status; no design-feel change implemented
</verification>

<success_criteria>
- Cluster A reviewed with the evidence-tier discipline (no code-only verdicts for behavioral claims — v4.1's core lesson)
- Every fix inside the auto-fix boundary, atomic, and regression-proven; every judgment call escalated, not implemented
</success_criteria>

<output>
Create `.planning/phases/22-implementation-review-auto-fix/22-02-SUMMARY.md` when done.
</output>
