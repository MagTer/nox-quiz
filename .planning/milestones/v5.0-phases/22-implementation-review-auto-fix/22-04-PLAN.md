---
phase: 22-implementation-review-auto-fix
plan: 04
type: execute
wave: 4
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - src/player.js
  - src/camera.js
  - src/parallax.js
  - src/fx.js
  - src/config.js
  - src/progress.js
  - src/levels/build.js
  - src/levels/index.js
  - .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "Each of the 12 Cluster C files (player, camera, parallax, fx, config, progress, main-level build/index, and the four level descriptors) has an explicit verdict in 22-FINDINGS.md — completing all 24 verdict rows"
    - "The structural defect inventory table is recorded with disposition deferred-to-phase-24 for every row, over-hole rows marked exact-arithmetic and platform rows marked heuristic candidates"
    - "The four level descriptor data files are byte-identical to the baseline commit — structural defects are inventoried, never fixed (they are Phase 23's RED calibration targets)"
    - "progress.js save-blob validation (explicit-field copy, finite-number guards, version gate, quota-error guard) is verified intact"
    - "After all Cluster C commits: 4 static gates + smoke green and browser-boot exits 0"
  artifacts:
    - path: ".planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md"
      provides: "Cluster C findings, completed 24-row verdict table, structural defect inventory, Cluster C regression record"
      contains: "deferred-to-phase-24"
  key_links:
    - from: ".planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md"
      to: "src/levels/level-01.js"
      via: "inventory rows computed from live descriptors by a pure-data interval check (no engine, no edits)"
      pattern: "over-hole"
    - from: ".planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md"
      to: "git history"
      via: "level-descriptor untouched proof diffed against the recorded baseline commit"
      pattern: "Baseline commit"
---

<objective>
Review Cluster C — world/engine glue and pure data — and produce the structural defect INVENTORY (FIX-01 + roadmap success criterion 4).

Mostly static + smoke-covered review (per 22-RESEARCH.md): player/camera/parallax/fx engine glue, config/progress, and the level registry/builder. The hard rule of this plan: known structural level defects (doors/gates over floor holes, unreachable areas) are INVENTORIED ONLY with disposition deferred-to-phase-24 — fixing even one silently destroys Phase 23's validator RED-first proof (binding STATE.md decision).

Purpose: complete the 24-file review coverage and deliver the calibration-target inventory Phase 23 depends on.
Output: atomic fix commits (0 or more), Cluster C findings, the structural defect inventory table, all 24 verdict rows final.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-implementation-review-auto-fix/22-CONTEXT.md
@.planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md
@.planning/phases/22-implementation-review-auto-fix/22-PATTERNS.md
@.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md
</context>

## Artifacts this phase produces

- New 22-FINDINGS.md content: Cluster C numbered findings, the filled `## Structural Defect Inventory (deferred-to-phase-24)` table, the completed 24-row Per-Entity Verdict Table, "Cluster C regression: PASS" record
- Uncommitted scratchpad script: pure-data interval check over the live level descriptors (from the 22-RESEARCH.md Code Examples snippet — Node import of src/levels/index.js + src/config.js, no engine; evidence tool, not a repo artifact, and NOT a validator — that is Phase 23's VALID-01)
- Conditional: removal of orphaned/dead config tokens in src/config.js (deletions only — no new tokens); paused-guard addition to the player.js jump-cut release handler IF confirmed non-benign
- No new exported symbols, CLI flags, or config tokens

<!-- planner-discipline-allow: | pending | -->

<tasks>

<task type="auto">
  <name>Task 1: Review engine glue — player.js, camera.js, parallax.js, fx.js</name>
  <files>src/player.js, src/camera.js, src/parallax.js, src/fx.js, .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md</files>
  <read_first>
    - src/player.js (jump-buffer write has a paused guard; the onKeyRelease jump-cut does NOT — research says benign today because velocity is zeroed before pause, verify; getCurAnim()-name animation transition guard)
    - src/camera.js and src/parallax.js (per-key fallback defaulting — verify partial bounds objects are actually caught: game.js's whole-object fallback does NOT default individual missing keys, camera.js re-defaults per-key; T-17-01 threat model provenance)
    - src/fx.js (tween onEnd self-clean; _fxScaleTween handle canceled in the game.js sweep; squash/stretch cancel on rapid jump-land sequences)
    - src/scenes/game.js (READ-ONLY here — the sweep that owns fx handles; any game.js change belongs to Plan 22-03's scope and would need a findings cross-reference, not a re-edit)
    - .planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md (Cluster C suspect rows; Bug-Pattern Checklist items 1, 6, 10)
  </read_first>
  <action>
    player.js: verify the paused-guard asymmetry between the jump-buffer write (guarded) and the jump-cut release handler (unguarded). Trace the freeze order in the mechanics (velocity zeroed before player.paused set) to confirm the release path cannot corrupt state while frozen; if a corrupting interleaving exists, add the same paused guard used by the buffer write (auto-fix: state-corruption class) — otherwise record the why-benign chain in the finding. Verify the animation state machine's transition guard handles a missing current animation.

    camera.js / parallax.js: bug-pattern #10 (NaN from malformed descriptor fields — labeled ASSUMED in research, cheap to check regardless): exercise the fallback logic in a scratchpad Node probe with a partial bounds object (missing individual keys) and confirm per-key defaulting yields finite numbers; check loop-index-vs-entry usage in the parallax iteration code. Any NaN path reachable from real descriptor shapes is an auto-fix (silent-invisibility class).

    fx.js: verify every tween self-cleans via onEnd-destroy or is swept by game.js; confirm rapid jump-land squash/stretch sequences cancel the prior tween (single-flight) rather than stacking.

    One numbered finding per hypothesis, tiered evidence (pure-function probes in Node are acceptable for engine-free logic; anything visual needs a screenshot). Atomic fix(22-04) commits; node --check + 4 static gates after each.
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && bash scripts/check-progress.sh && F=.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md && [ "$(grep -c 'src/player.js' "$F")" -ge 2 ] && echo ENGINE-GLUE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Findings recorded for: the jump-cut paused-guard question (guard added or why-benign chain documented), the camera/parallax partial-bounds probe (probe input and output values shown), and the fx single-flight verification
    - No new module-level engine references (check-import-safety.sh passes after every commit)
    - No timer constructs introduced (check-safety.sh passes — timing fixes use the tween onEnd self-clean idiom)
  </acceptance_criteria>
  <done>Four engine-glue files carry evidence-backed findings; any fix is atomic and gate-verified.</done>
</task>

<task type="auto">
  <name>Task 2: Review config.js, progress.js, and the level registry/builder</name>
  <files>src/config.js, src/progress.js, src/levels/build.js, src/levels/index.js, .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md</files>
  <read_first>
    - src/config.js (245 lines of constants — sweep for orphaned/dead tokens by grepping each token's consumers across src/; dead-code removal is in scope when gates stay green)
    - src/progress.js (firewall header; explicit-field copy validation; finite-number guards; version gate; quota-error guard asserted by check-progress.sh)
    - src/levels/build.js (apex-derived blocker heights from WR-04/CR-02 — verify present; loop-index-vs-entry usage in entity iteration; the door glyph label rendering with explicit color per 844cd08)
    - src/levels/index.js (registry/unlock derivation — smoke-covered by smoke-progress.mjs)
    - .planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md (Cluster C rows; Security Domain table — the V5 input-validation contract fixes must preserve)
  </read_first>
  <action>
    config.js: for each exported token, find at least one consumer in src/ (scripted grep sweep in the scratchpad, results table in the finding). Remove tokens with zero consumers ONLY when all gates stay green afterward, one atomic commit for the removal set with the sweep table cited; keep documented feature-flag/threshold tokens that encode decisions (e.g. the IN-03 flag comment) even if read only by humans — removing recorded decisions is not dead-code cleanup.

    progress.js: verify the validated save parsing is intact — version gate, try/catch with defaults, explicit-field copy (no spread from parsed JSON — prototype-pollution mitigation), finite-number guards, junk level id fallback to the first LEVEL_ORDER entry, and the quota-exceeded guard. This is the phase's highest-value security check (planning constraint: do not weaken existing protections). Record verdict; expected clean (lowest-risk file per research) — any fix here must keep check-progress.sh + smoke-progress.mjs green.

    build.js / levels/index.js: verify apex-derived blocker heights are present for every barrier type (bug-pattern #8 — jump-over exploit class); check loop-variable usage in entity construction loops (bug-pattern #10); verify glyph label draws carry explicit color per the 844cd08 convention. Registry: verify unlock derivation matches LEVEL_ORDER and tolerates an unknown persisted level id.

    Atomic commits per fix; node --check + 4 static gates + node scripts/smoke-progress.mjs after each commit touching progress.js or the registry.
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-import-safety.sh && bash scripts/check-safety.sh && bash scripts/check-progress.sh && node scripts/smoke-progress.mjs && F=.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md && [ "$(grep -c 'src/progress.js' "$F")" -ge 2 ] && echo DATA-LAYER-OK</automated>
  </verify>
  <acceptance_criteria>
    - The config token sweep table appears in a finding: every removed token listed with its zero-consumer proof; every kept-but-unconsumed token listed with its keep rationale
    - progress.js verdict cites each verified protection by name (version gate, explicit-field copy, finite-number guards, junk-id fallback, quota guard); check-progress.sh and smoke-progress.mjs pass on HEAD
    - build.js verdict confirms apex-derived blocker heights per barrier type and clean loop-variable usage, or a fix commit is cited
    - No commit changes src/math/ or lib/kaplay.mjs (LOCKED read-only context)
  </acceptance_criteria>
  <done>Config/persistence/builder layer reviewed, dead tokens removed gate-green, save-parsing protections proven intact.</done>
</task>

<task type="auto">
  <name>Task 3: Structural defect inventory (inventory ONLY) + cluster regression + final verdict rows</name>
  <files>.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md</files>
  <read_first>
    - .planning/phases/22-implementation-review-auto-fix/22-RESEARCH.md (Structural defect inventory candidates table — the exact rows and their exact/heuristic labels; Code Examples — the pure-data interval check to reproduce; Common Pitfalls 2)
    - src/levels/level-01.js, src/levels/level-02.js, src/levels/level-03.js, src/levels/level-04.js (READ-ONLY — the descriptors the interval check imports)
    - .planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md (baseline commit line + the audit table's 6 unreached rows, for the correlation note)
  </read_first>
  <action>
    Reproduce the pure-data interval check from the 22-RESEARCH.md Code Examples section as a scratchpad Node script (imports the live registry + CONFIG, computes mechanic footprints vs floor runs — no engine, no repo file). Run it and fill the `## Structural Defect Inventory (deferred-to-phase-24)` section from ITS output (fresh computation, not copied prose), expecting: level-01 math-gates at x600 and x1300 and level-04 math-gate at x1800 as over-hole rows (exact interval arithmetic), and the level-03 platforms (x1880, x2640) plus six level-04 platforms as possibly-unreachable rows explicitly labeled heuristic candidates (crude envelope, no safety factor — Phase 23's calibrated envelope is the arbiter; recording them as confirmed defects is the research's A2 risk). Every row's Disposition column reads deferred-to-phase-24. Add the correlation note: the audit's unreached encounters cluster on the over-hole math gates (spike-timing resonance documented as the cause; gate-over-gap placement plausibly compounds it).

    Confirm the level descriptors are untouched: extract the baseline SHA from the Baseline commit line and verify git diff against it is empty for all four level descriptor data files (this is the roadmap's "inventoried but deliberately left in place" criterion, machine-checked).

    Run the cluster regression: `node scripts/browser-boot.mjs` must exit 0. Record `Cluster C regression: PASS`. Set final verdicts for all 12 Cluster C rows (player, camera, parallax, fx, config, progress, build, levels/index, and the four level descriptors — the descriptor rows get deferred-to-phase-24 verdicts referencing the inventory). After this task the Per-Entity Verdict Table has zero unreviewed rows. Commit docs(22-04).
  </action>
  <verify>
    <automated>F=.planning/phases/22-implementation-review-auto-fix/22-FINDINGS.md && BASE=$(sed -n 's/^Baseline commit: //p' "$F" | head -1) && git diff --quiet "$BASE"..HEAD -- src/levels/level-01.js src/levels/level-02.js src/levels/level-03.js src/levels/level-04.js && node scripts/browser-boot.mjs && [ "$(grep -c 'deferred-to-phase-24' "$F")" -ge 8 ] && [ "$(grep -c 'Cluster C regression: PASS' "$F")" -eq 1 ] && [ "$(grep -c '| pending |' "$F")" -eq 0 ] && echo CLUSTER-C-OK</automated>
  </verify>
  <acceptance_criteria>
    - The Structural Defect Inventory table is filled from a fresh run of the interval check (the script's raw output is quoted or referenced), with the 3 over-hole rows marked exact and all platform rows marked heuristic candidates; every Disposition cell reads deferred-to-phase-24
    - `git diff --quiet <baseline-sha>..HEAD -- <the four level descriptor files>` exits 0 — level geometry byte-identical to baseline
    - The correlation note (unreached encounters cluster on over-hole gates) is present
    - browser-boot.mjs exits 0; "Cluster C regression: PASS" appears exactly once
    - The Per-Entity Verdict Table has zero rows left in the initialized state — all 24 carry final verdicts with finding refs
  </acceptance_criteria>
  <done>Inventory delivered as Phase 23 calibration input, level data provably untouched, all 24 verdict rows final, boot green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage save blob → progress.js parsing | Untrusted persisted JSON crosses into game state here — the phase's main input-validation surface |
| level descriptors → builder | Pure data trusted by build.js; malformed fields surface as NaN/silent-invisibility bugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | progress.js save-blob parsing (V5 input validation) | high | mitigate | Task 2 verifies version gate, explicit-field copy (no spread), finite-number guards, junk-id fallback, quota guard by name; check-progress.sh + smoke-progress.mjs after every commit touching progress.js |
| T-22-04 | Denial of Service | corrupt save blob crashing boot | low | accept | Existing loadSave try/catch + defaults verified unchanged (covered by T-22-01 checks) |
| T-22-07 | Tampering | dead-token removal accidentally deleting a live decision flag | medium | mitigate | Consumer-sweep table required per removed token; documented decision flags kept explicitly; full gate suite green after the removal commit |
| T-22-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- All 4 static gates + smoke green after every commit; browser-boot green at cluster end
- Structural defect inventory recorded inventory-only, level data diff-proven untouched vs baseline
- All 24 verdict rows final — review coverage complete ahead of the FIX-02 round
</verification>

<success_criteria>
- Roadmap success criterion 4 satisfied: known structural defects inventoried but deliberately left in place as Phase 23 calibration targets
- FIX-01's per-entity coverage claim is now backed by a complete verdict table, not partial sampling
</success_criteria>

<output>
Create `.planning/phases/22-implementation-review-auto-fix/22-04-SUMMARY.md` when done.
</output>