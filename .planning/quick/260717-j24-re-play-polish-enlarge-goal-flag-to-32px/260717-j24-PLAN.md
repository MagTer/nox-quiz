---
phase: quick-260717-j24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.js
  - src/levels/build.js
  - src/ui/mathGate.js
  - .planning/phases/35-biome-re-dress-props/35-CONTEXT.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The goal flag renders visibly larger (32px) at level end, no longer looking like half a coin"
    - "The goal trigger collider footprint is byte-identical to today's 16px sprite-derived area, so onCollide still fires on all 8 levels"
    - "The level-complete clear.ogg plays at reduced gain (0.4), no longer ear-piercing"
  artifacts:
    - "src/config.js with GOAL_VISUAL_SIZE and CONFIG.AUDIO.CLEAR_VOLUME"
    - "src/levels/build.js with decoupled two-entity goal (visual flag + hidden collider)"
    - "src/ui/mathGate.js clear-sfx call passing CONFIG.AUDIO.CLEAR_VOLUME"
  key_links:
    - "goal collider (rect(GOAL_SIZE,GOAL_SIZE) + pos(g.goal.x,g.goal.y) + area() + 'goal' tag) unchanged → browser-boot onCollide still fires"
---

<objective>
Two re-play polish fixes from the user's 2026-07-17 play-test of the ALL-8 level redo:

1. Enlarge the goal flag visually to 32px WITHOUT changing the load-bearing 16px goal collider (decouple visual from collider, mirroring the door pattern in build.js).
2. Lower the ear-piercing level-complete sound by adding a per-SFX volume override.

Purpose: Cosmetic/UX polish surfaced during play-test; the flag looked like half a coin and the clear sound was painfully loud.

Output: Edits to src/config.js, src/levels/build.js, src/ui/mathGate.js, plus a deferral note in Phase 35's CONTEXT.

CRITICAL COMMIT CONSTRAINT (both tasks): Commit ONLY the specific files each fix touches. The working tree contains pre-existing unrelated Phase 34.6 WIP — `scripts/smoke-progress.mjs`, `scripts/audit-endgate-key.mjs`, `scripts/lib/mechanic-drive.mjs` — which MUST stay untouched and uncommitted. `git add` only the named src/doc files; NEVER `git add -A` or `git commit -am`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@src/levels/build.js
@src/config.js
@src/ui/mathGate.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decouple the 32px visual goal flag from the load-bearing 16px collider</name>
  <files>src/config.js, src/levels/build.js</files>
  <action>
Add GOAL_VISUAL_SIZE to src/config.js immediately next to GOAL_SIZE (~line 143). Set it to 32. Add a comment explaining: GOAL_SIZE stays the load-bearing collider footprint (16px, harness-critical for browser-boot/audit-endgate-key onCollide — DO NOT change); GOAL_VISUAL_SIZE is the decoupled visual-only flag size. Do NOT change GOAL_SIZE.

In src/levels/build.js, replace the single tagged "goal" entity (currently `add([ sprite("goal", { width: CONFIG.GOAL_SIZE, height: CONFIG.GOAL_SIZE }), pos(g.goal.x, g.goal.y), area(), "goal" ])` at ~lines 268-273) with TWO entities, mirroring the existing door split (invisible collider + separate visible art) in this same file:

  (a) A purely VISUAL larger flag sprite: `sprite("goal", { width: CONFIG.GOAL_VISUAL_SIZE, height: CONFIG.GOAL_VISUAL_SIZE })`. NO area(). NOT tagged "goal" — use a distinct tag "goalflag". Anchor it so its base stays planted on the OLD 16px footprint's bottom edge (original ground line) and it grows UP and to the RIGHT — never sinking into the floor. Use the idiom `anchor("botleft")` + `pos(g.goal.x, g.goal.y + CONFIG.GOAL_SIZE)` (old sprite occupied y..y+GOAL_SIZE; a botleft anchor at y+GOAL_SIZE keeps the base on the old bottom-left and grows upward). Confirm the anchor is a valid Kaplay anchor and that the base sits on the original ground line; if `botleft` behaves unexpectedly, pick whichever anchor/pos idiom keeps the base at the original ground line and grows up+right.

  (b) The UNCHANGED load-bearing trigger collider: `rect(CONFIG.GOAL_SIZE, CONFIG.GOAL_SIZE)` + `pos(g.goal.x, g.goal.y)` + `opacity(HIDDEN)` + `area()` + "goal" tag. HIDDEN is the existing opacity constant used for the door/gate blockers in this file (build.js:74) — reuse it. This yields a collider with the same top-left origin (g.goal.x, g.goal.y) and same 16×16 size as today's sprite-derived area — byte-identical footprint.

Do NOT use scale(). Do NOT change GOAL_SIZE. Keep all engine-global refs (add, sprite, rect, pos, area, anchor, opacity) INSIDE the builder function body per the a727c13 rule — this code is already inside buildLevel, so no new top-level refs.

Update the explanatory comment block above the goal entity (~lines 262-267) to describe the new decoupled two-entity structure and WHY: the visible "goalflag" is cosmetic-only (GOAL_VISUAL_SIZE), while the "goal"-tagged hidden rect keeps the collider byte-identical (GOAL_SIZE, same origin) so the browser-boot/audit-endgate-key harness onCollide still fires — level-06 path-B halts ~13-16px short with ~0-3px overlap, so the collider position/size is genuinely load-bearing.
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh && node scripts/validate-levels.mjs && node scripts/browser-boot.mjs</automated>
  </verify>
  <done>GOAL_VISUAL_SIZE:32 exists in config.js next to unchanged GOAL_SIZE:16. build.js emits a visible "goalflag" sprite at 32px (base on the original ground line) plus a hidden "goal"-tagged 16×16 rect collider at pos(g.goal.x, g.goal.y). browser-boot.mjs drives into the goal and onCollide fires on all 8 levels (only known pre-existing perf-fps warnings, no new failures). No scale() used; GOAL_SIZE unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Lower the level-complete clear.ogg gain via a per-SFX override</name>
  <files>src/config.js, src/ui/mathGate.js</files>
  <action>
Add `CLEAR_VOLUME: 0.4` to the CONFIG.AUDIO block in src/config.js (~lines 422-431), following the exact same per-SFX-override idiom as the existing `JUMP_VOLUME: 0.2` line. Add a short comment: user 2026-07-17 play-test — clear.ogg was ear-piercing at full SFX_VOLUME gain; lowered to 0.4.

In src/ui/mathGate.js (~line 81), change `audio.playSfx("clear")` to `audio.playSfx("clear", CONFIG.AUDIO.CLEAR_VOLUME)`. CONFIG is already imported at line 21, so no new import is needed — confirm and reuse it.
  </action>
  <verify>
    <automated>bash scripts/check-gate.sh && bash scripts/check-safety.sh && bash scripts/check-import-safety.sh</automated>
  </verify>
  <done>CONFIG.AUDIO.CLEAR_VOLUME is 0.4 in config.js (per-SFX idiom matching JUMP_VOLUME). mathGate.js line ~81 calls audio.playSfx("clear", CONFIG.AUDIO.CLEAR_VOLUME). All three shell gates pass with no new failures.</done>
</task>

</tasks>

<capture_note>
CAPTURE-ONLY (NOT a code task — writes no game code): During the 2026-07-17 play-test the user found the neon-green coin-pickup pop() in src/fx.js reads as a missing-sprite placeholder. The user chose to DEFER restyling it to Phase 35 (Biome Re-dress & Props) art pass. The executor MUST append a one-line note to `.planning/phases/35-biome-re-dress-props/35-CONTEXT.md` so it is not lost — e.g. "DEFERRED from quick 260717-j24 (2026-07-17 play-test): restyle the neon-green coin-pickup pop() in src/fx.js — it currently reads as a missing-sprite placeholder; re-dress during the art pass." Commit this doc note together with the STATE/docs commit, NOT as a code commit. Do not edit src/fx.js.
</capture_note>

<commit_discipline>
Commit ONLY the files each fix touches. Stage explicitly by path — never `git add -A` / `git commit -am`.
- Task 1 commit: `git add src/config.js src/levels/build.js` then commit.
- Task 2 commit: `git add src/config.js src/ui/mathGate.js` then commit (note src/config.js is touched by both — stage the specific hunks/file state as it stands after each task; if batching, one combined src commit staging exactly src/config.js, src/levels/build.js, src/ui/mathGate.js is acceptable).
- Doc/capture commit: `git add .planning/phases/35-biome-re-dress-props/35-CONTEXT.md` (plus STATE.md if updated) — separate from code.
- NEVER touch or stage `scripts/smoke-progress.mjs`, `scripts/audit-endgate-key.mjs`, `scripts/lib/mechanic-drive.mjs` — pre-existing Phase 34.6 WIP that stays uncommitted.
</commit_discipline>

<verification>
Run after src/ edits (both tasks):
- `bash scripts/check-gate.sh`
- `bash scripts/check-safety.sh`
- `bash scripts/check-import-safety.sh`
- `node scripts/validate-levels.mjs`
- `node scripts/browser-boot.mjs` — the real proof the goal collider stayed byte-identical: goal reachable and onCollide fires on all 8 levels.
All must pass with no NEW failures. Pre-existing environmental `perf-fps` warnings in browser-boot are known flakiness, not regressions.
</verification>

<success_criteria>
- Goal flag renders at 32px, base on the original ground line, without changing the 16px "goal" collider (byte-identical footprint; browser-boot onCollide fires on all 8 levels).
- clear.ogg plays at 0.4 gain via CONFIG.AUDIO.CLEAR_VOLUME.
- All listed gates green with no new failures.
- Phase 35 CONTEXT carries the deferred fx.js coin-pop restyle note.
- Only the specific edited files committed; Phase 34.6 WIP scripts untouched.
</success_criteria>

<output>
Create `.planning/quick/260717-j24-re-play-polish-enlarge-goal-flag-to-32px/260717-j24-SUMMARY.md` when done.
</output>
